# Handoff — 2026-09-23T11:25Z (rey window; refreshed after the checkpoint, the G4U/W-new merges and the D4/E3B2 reviews)

Supersedes `HANDOFF-20260923T1015Z.md`. Since it: **checkpoint** — `main` fast-forwarded f9ba5d2 → **01a7dfc** (api-test 2993 green on private ports; api-mutants 2349/2350 with the one load-sensitive W3 `sigint_not_handled` runner timeout attributed and killed in isolation; console 289 + typecheck + lint + 104 console mutants; bench 67); **W-new merged** a15fa4b; **G4U merged** 7d21fa7 (+ Makefile list; 22/30); G2 review fix_required (11 blocking; ruling **R91** replay lookup port), W4 confirmation fix_required on 2 coverage gaps (round 3), D4 handed back and under review, E3B2 handed back and reviewed fix_required (4 blocking; rulings **R92–R93**, pending input **P-21**), **G3 dispatched**, **I3B follow-up dispatched**. Next free ruling **R94**. Reviews/refuters/implementers on Opus 5.5; Fable plans/orchestrates; ultracode on (`Workflow` with `model: 'opus'`).

Rules unchanged: WIP-commit and push lane work as you go (the coordinator pushes lane branches); detach > 10 min; never another lane's containers; secrets never printed/logged/committed; AWS CLI via `env -u AWS_ACCESS_KEY_ID -u AWS_SECRET_ACCESS_KEY -u AWS_SESSION_TOKEN aws …`; never the pilot box outside logged coordinator ops. Standing user instruction: **"safely complete and merge tasks, run max parallel agents / tasks — wherever possible"**; "update tracker" stands.

## GOAL
Marlin backend-first program (`research/plan/18-marlin-backend-first.md`, manifest v4): 22/30 merged; in progress E1B (GPU slices), E3B2, D4, G2, W4, G3 (+ the I3B follow-up lane); not started D5, E4B. Session record (append-only): `research/plan/evidence/coordinator/2026-09-22-session-02.md`. Tracker: `python3 research/plan/scripts/progress.py` → `evidence/coordinator/PROGRESS.md` + `progress.html`; publish with `url` https://claude.ai/artifact/3a3VjYKb2Vfh5X89BYMDPZ + the repo's progress.html path (other accounts publish a fresh artifact and record its URL in progress-state `tracker_url`). Manifest status changes: edit `tasks.json` TEXTUALLY (never `json.dump`: it re-encodes non-ASCII), status `implemented` + a merge note appended to `disposition`, then `validate_plan.py --write-ledger`.

## BRANCH STATE (verified local == origin at 2026-09-23T11:25Z)
Integration `claude/backend-impl` = 1a26137+ (this worktree `infrx-impl`); `main` = 01a7dfc. Program base ec6c548.

| worktree | branch @ head | agent | state |
|---|---|---|---|
| codex-d4 | `codex/d4-stream-journal` @ a4598b0 | a3dc7d1193c9ade11 | HANDED BACK (impl ce7659b); review wf_db54ab74-d3f running (3 lenses on private ports 55481–55483 / Valkey 55494–55496); at CONFIRMED head → merge (harness += 0017; Makefile += tests/d/test_code_mutants_d4.py; pgstate additions per its request 8) and dispatch **D5** |
| codex-g2 | `codex/g2-chat-relay` @ 2d742aa+ | a98f6b711111c4380 | review fix_required at 2d742aa (JSON evidence/g/G2-review-2d742aa.json) → fix round running (R91 lookup port + fakes + conformance case; drain; stream bound; …; removes the RecordKeysDropped shim now that W-new is merged) → confirmation next; cutover diff HELD |
| codex-e3b2 | `codex/e3b-phase2-gate` @ 80cef22+ | aff9c42bf5340b59a | review fix_required at 80cef22 (JSON evidence/e/E3B2-review-80cef22.json; F1 dr13 premise, H1 baseline-red mutants, H2 I2B RESIDUAL, H3 e3bm22) → fix round running → confirmation; at merge: IR #1 TASK_BLOCKS + delete the strict-xfail mark, fake requeue event id (R93), drop F2P from PENDING, RESIDUAL/PENDING updates |
| codex-w4 | `codex/w4-measured-tuning` @ b0a44a0+ | a2c1ab13508b58f8b | confirmation fix_required at b0a44a0 (box-safety PASS; 2 decision coverage gaps; JSON evidence/w/W4-confirm-b0a44a0.json) → round 3 running → re-confirm (decision lens only) → merge (Makefile += tests/w/test_w4_mutants.py); phase B = coordinator box re-measurement (maintenance window) |
| codex-g3 | `codex/g3-jobs` @ 2d742aa (base) | a532cdcf3b6126358 | implementing per `.claude/handoff/wave3/G3.md` (stacked on G2; told about R91 and W-new; merges G2's fix head when told) |
| codex-i3bf | `codex/i3b-followup` (from ≥ 1a26137) | (dispatched 11:25Z) | R92 restore ACL normalisation, i3bm57 re-anchor, rc10 body, pending honesty (rc03→G2, rc04 stubbed, rc05b/rc08b owner ids, skip message, `_copy_with_infra` deleted); D task d3, Q port 55497 |
| codex-g4u | `codex/g4u-upload-adapter` @ d1827d5 | closed | MERGED 7d21fa7 |
| codex-wnew | `codex/w-consumed-parameters` @ 483d763 | closed | MERGED a15fa4b |
| codex-ischema / codex-fcancel | — | closed | MERGED bc382ac / 6803d2f |
| codex-e1b | — | — | software slices merged 164e43e; GPU slices wait on the box rollout |

## DETACHED / RUNS
- Workflows running: D4 review wf_db54ab74-d3f (task wbsam5k59). None else.
- Containers: `infrx-d5-postgres`, `infrx-q3-valkey-55490` were the checkpoint's (remove if idle: `docker rm -f`); `infrx-d2-postgres` (owner unknown — a lane's default d2 run; leave); D4 reviewers create `infrx-review-d4-*`; the I3B follow-up uses d3. `gideon-migration-order-test-*` is not ours.
- Merged-tree logs: `.claude-logs/checkpoint2-90aadcf.log` (checkpoint), `g4u-merged-7d21fa7.log` (703 + 241), `contracts-merged-0024da2-d3.log`, `dconf-merged-0024da2.log`.

## NEXT (in order)
1. Process results as they land (parse `result`; save JSON under `research/plan/evidence/<track>/<TASK>-review-<head>.json`; record; pass → merge `--no-ff` with the task's integration requests, run the merged tree's relevant suites on private ports, push; fix_required → SendMessage the lane, then a confirmation workflow on the fix head).
2. Merge order: **D4 (after confirmation) → dispatch D5 on its head → W4 phase A (after re-confirmation) → E3B2 (after confirmation) → G2 (after confirmation) → G3 → I3B follow-up → D5 → E4B.** G2's cutover only when D4, D5, M adapters, W-new (done) and E3B2 bodies exist.
3. Next checkpoint: after D4 + W4 merge, whole-tree api-test + api-mutants on private ports (`INFRX_D_TASK=d5 INFRX_D2_VALKEY_PORT=55467 INFRX_D2_VALKEY_CONTAINER=infrx-d5-valkey INFRX_Q_VALKEY_PORT=55490`, `-rfE` into a log; console/bench too) → ff `main`.
4. Keep pushing lane branches at each handback; tracker after each state change; refresh this handoff at each checkpoint; at ~90 % of the window stop dispatching, write the handoff, commit, push, idle.

## OPEN ITEMS (not blocking merges)
GitHub Dependabot: 1 moderate alert on the default branch (https://github.com/callgideon/model-inference/security/dependabot/1; not touched). P-21 host `ip_local_reserved_ports` (root; user approval). IR-DTO (console); Q Valkey runner env passthrough + `INFRX_Q_VALKEY_CONTAINER`; `Admission.price_snapshot` optional revision; the stale §5.1 paragraph about G1/Q1 constants; I3B `--max-age 180` alert timer; hosted backup rehearsal before any hosted apply; box rollout per I2B runbook; HF_TOKEN registry oids (user); E1 corpus byte-reproducibility per CPU; P-20 (interim `MAX_VIDEO_SECONDS` 72; W4's ceiling 82 s valid with ≥ F−2 frames; E1 32768 keeps 120 s if adopted after the box run); the Q3 drill's unbounded `while not w.leases` wait; the W3 `sigint_not_handled` load-sensitive runner timeout (W follow-up: load-tolerant bound); G2's coordinator requests (daily `gc_outbox` timer; worker composition over `rt.lifetime.reconciler.index`; `MediaCollector` settings PROCESSING_CACHE_MAX_BYTES/MEDIA_GC_INTERVAL_S → preflight TUNABLE + 08 §5.1 at the cutover); G4U's M request `MediaUploads.check_open`; E bench.py upload names (G4U c); doc wording after the mount (G4U d).

## TRAPS
- Five concurrent full suites cross-kill the shared `infrx-q3-valkey` (55462): every full-suite run sets `INFRX_Q_VALKEY_PORT=<free port>` and NO pre-started container (the harness names it `infrx-q3-valkey-<port>`; the drills kill THAT name). The D2 Valkey lock (`INFRX_D2_VALKEY_PORT` 55463 default) is shared too: set it per run with `INFRX_D2_VALKEY_CONTAINER`.
- A gate whose pytest shows ~1 min CPU per hour with an idle Pg session is hung in that cross-kill: kill it, rerun on private ports (ptrace is denied for non-descendants).
- The gate wrapper's `| tail -N` hides failure names and makes `$?` the tail's: use `-rfE` into a log for names.
- `(cd … && setsid …)`; `pkill -f <string>` kills your own shell when the string is in your command line.
- progress-state `inputs` entries are dicts; `reviews` entries need `{phase, since}`.
- Workflow scripts: no backticks inside template literals (use quotes); result files wrap JSON in `{"summary",…,"result"}`; per-agent results in `journal.jsonl`.
- Port 55432 is shared (HarnessBusy = retry). Lane-numbered rulings collide: renumber at merge. `git stash` is shared: never.
