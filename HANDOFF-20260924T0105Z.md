# Handoff — 2026-09-24T01:05Z (sofia window; MERGE UNIT DONE at 92ea6c0; two lanes left before checkpoint 2)

Supersedes `HANDOFF-20260924T0030Z.md`. Since it: phase-3 verifier PASS b9529d1; M pilot-media fix round e5c02f7 + verifier PASS; D5 FINAL f142074 (test pins; battery attributed); cutover review fix_required on THREE TEST GAPS (code confirmed strict by every refuter) → its one fix round is running; **merge-plan steps 1–9b executed on `claude/backend-impl` → 92ea6c0** (D5, phase 3 + IR3F-2(b), cutover code 1bed457, M1-L2, M pilot-media, E4B software half + 4 edits, rollout prep, rulings R95–R103; next free **R104**); manifest **29/30**; `make bench-test` green on the merged tree; SSM `/model-inference/pg_journal_url` CREATED (logged op, P4 closed). `main` = 01a7dfc (remote; local ref stale f9ba5d2). Integration head **9050657+** (pushed).

Rules unchanged (1125Z handoff; ONE fix round per lane; reviews on committed heads; box via SSM with W3's helpers; secrets never in text; AWS CLI via `env -u AWS_ACCESS_KEY_ID -u AWS_SECRET_ACCESS_KEY -u AWS_SESSION_TOKEN aws --region us-east-1`). Session record `research/plan/evidence/coordinator/2026-09-22-session-02.md` (append-only, complete); tracker `python3 research/plan/scripts/progress.py` → publish `scratchpad/marlin-backend-tracker-sofia.html` with url https://claude.ai/artifact/Rk84qe3Yuzr2d357GoeRvZ (v27).

## REMAINING BEFORE CHECKPOINT 2
| step | lane | state | action |
|---|---|---|---|
| **3b** | CUTOVER `codex/cutover-mount` @ 1bed457+ (agent a38c6e31728e30020, resumed 00:56Z) | fix round running: COMP-B1 (7/39-hex release, 63-hex image shapes must be refused; O4/O5/O6 as G/I mutants), COMP-B2 (edge directive scan over the whole site, Server-Timing in PASSTHROUGH; O1/O2 as I mutants), H-B1 (dev-mode refusal or reword "in pilot"), 11 nonblockings folded/stated; JSON `evidence/g/CUTOVER-review-a1e88dc.json` | handback → push → single Opus verifier (tests only; no PostgreSQL needed) → `git merge --no-ff` onto the integration branch as **step 3b** (expect no conflict: tests/g/test_startup.py, tests/i/test_packaging.py + test_install.py, evidence) |
| **8b** | WORKER `codex/i2b-r4-worker` @ 1294d33+ (agent a0bdc78d222ba64c1; namespace e3b2 + d4 ports; merged phase 3 07fe76e) | running (items 1–3 in; item 2 = rehearse.sh on the release tree) | handback → push → single verifier → `merge-plan/8b-i2b-r4-worker.sh` with WORKER_VERIFY (re-check its pins first: written before phase 3's 40d9105) → tasks.json I2B-R4 note; layer 0 must then show NO pending |
| — | INTERIM CHECKPOINT on scratch `scratchpad/ckpt-interim` @ 92ea6c0 (SKIP_L3; d3 ports 55434/55464, Q 55490, MinIO infrx-ckpt2-minio 55694) | started 00:59Z; log `scratchpad/ckpt-interim/.claude-logs/checkpoint2-interim-summary.log` (+ `checkpoint2-92ea6c0/`) | informative only; the FINAL gate runs after 3b+8b |

## CHECKPOINT 2 (after 3b + 8b), then `main`
On the integration worktree, clean tree: `setsid nohup bash research/plan/evidence/coordinator/merge-plan/checkpoint2.sh > .claude-logs/checkpoint2-summary.log 2>&1 &` (api-test whole with `-rfEs`, layer 0, the affected mutant lists with the private MinIO, E4B list, W3 sigint, layer 3 on e3b2 `--only-suites --canary` — e3b2 must be free: the worker lane holds it until its handback). `make bench-test` in the FOREGROUND (SIGINT case). Expect: api-test green (the 23 test_cancel_cause reds are gone: 8c00bb4 merged), layer 0 exit 0 with no PENDING after 8b (else exit 3 with only I2B-R4), mutant lists all killed, layer 3 exit 0. Then: `git fetch origin main && git branch -f main origin/main && git push origin claude/backend-impl:main` (ff; realigns the stale local ref) + tracker + handoff.

## BOX PHASE 2 (after `main`) — logged ops, per `infra/runbooks/rollout.md`
P4 done (pg_journal_url v1). P1–P3: release = the checkpointed full SHA; W1 maintenance edge (saved live Caddyfile pattern) → drain → W6 fresh hosted backup + restore check → W7 migrate 0003–0018 (`plan` digest must equal the copy's; `apply --expect`) → W10 `install.sh INFRX_MODE=pilot RELEASE=<full sha>` with `--set S3_MEDIA_BUCKET=llm-bootcamp-641134885443`, MAX_VIDEO_SECONDS=82, ENGINE_MAX_NUM_SEQS=8, WORKER_CONCURRENCY=8 (ACCOUNTING_REGIME stays legacy_usd) → readiness → tests/m/test_s3.py on the real bucket → edge live → smoke (two G6B keys, `read -rs`) → E1B L2–L7 → `certify.py --box` (E4B protocol v2; 4-hour soak) → release decision → tasks.json E4B box half. Box: i-0e8449a4ffca29bab (engine marlin2b-8000 `--max-num-seqs 32` until the release's serve.sh; start-to-ready 168–181 s).

## RUNS / CONTAINERS (01:05Z)
Lanes: cutover fix round (infrx-cutover-valkey), worker (infrx-d4-postgres / infrx-worker-valkey 55465 / infrx-worker-minio 55781; e3b2). Coordinator: ckpt-interim (infrx-ckpt2-minio 55694, infrx-d3-*). Stale: infrx-d2-postgres; gideon-* not ours. Rehearsal scratches `scratchpad/mergeu`, `mergeu2` (disposable).

## TRAPS (new since 0030Z)
- A Bash `set -e` does not survive an awk-extracted script tail; run whole scripts.
- The fake_vllm tests bind fixed e2-block ports (55580–55582): any live e2 stack makes them red; rerun after the stack is down.
- `tasks.json` E4B disposition now says "software half merged; box half and release decision remain; BACKEND-READY not claimed" — keep it until the box run.
- The cutover review JSON's H-N5: the lanes' "R95" literals are renumbered at the merge (R98 cutover, R99 M pilot); do not edit their evidence.
