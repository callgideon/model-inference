# Handoff — 2026-09-24T10:10Z (sofia window; `main` = 2d4a88b with the preparation loop; SECOND RELEASE 4226315 DEPLOYED; the box half waits on the operator key)

Supersedes `HANDOFF-20260924T0545Z.md`. Since it: PREP-WORKER handed back 01b11ca → targeted verifier PASS (21 checks; F1–F3 nonblocking, F3 applied 933dda3) → merged 28f3153 (+ R104–R105 0e0506a) → focused gate on 4226315 green (suites 2289, D 89, lists 891 + the two contracts mutants re-anchored 2d4a88b, layer 0 202 no PENDING, E4B 140, layer 3 backend 188/0/0 + mutants 248/252 + canary; one P-21 fake_vllm flake 17/17 on rerun) → **`main` 8cabe20 → 2d4a88b** → **second window 10:00–10:04Z: RELEASE 422631591845fbd66b590c73d5ff4150318d9d7a deployed (pilot), image sha256:491697bc…** — worker readyz 200 with the build gauge, public health 200, chat without a key 401, metrics hidden. The pilot box now runs the metered gateway + a worker with BOTH loops (preparation with engine-exact counts, inference) over hosted 0003–0018 + the operator seed. Manifest 29/30 (E4B: software merged; box half pending). Rulings next free **R106**.

Rules unchanged (window steps ONE per command — the auto-mode classifier blocks multi-step deploy commands and secret-store writes; reviews on committed heads; secrets never in text). Session record `research/plan/evidence/coordinator/2026-09-22-session-02.md` (append-only; every box command id is there); tracker `python3 research/plan/scripts/progress.py` → publish `scratchpad/marlin-backend-tracker-sofia.html` with url https://claude.ai/artifact/Rk84qe3Yuzr2d357GoeRvZ.

## BLOCKED ON THE USER
The single operator-audience key (0009 `infrx.bootstrap_operator_key(org, name, prefix, sha256hex, actor, reason)`, hash only) is needed for W12 smoke (consumer keys via `python -m infrx.operations.cli issue-key/revoke-key` with `INFRX_OPERATOR_KEY`), the end-to-end job proof (PREP-WORKER request 1: one text job + one 10 s clip through the public edge; `prepared_prompt_tokens == usage`, video pads in [10, 1960]), E1B L2–L7 and `certify.py --box`. The classifier refused holding the minted secret (SSM put; then a 0600 scratch file). Options for the user: allow the action once, add a Bash permission rule, or mint the secret into SSM `/model-inference/operator_key` themselves (then the coordinator bootstraps the hash: org = a dev account's personal org, e.g. the first profile's; all four hosted profiles are A1-verified, none is the user's).

## NEXT (once the key exists)
1. W12: `issue-key --user c3c1bd36-c7f0-45e3-99fd-03de60bb2154 --name rollout-smoke-test --secret-file <0600>` (+ a second key, revoked) → `HOST=marlin2b.callbill.ai infra/rollout/verify-external.sh` with INFRX_TEST_KEY/INFRX_REVOKED_KEY exported from the files (never argv) → `failures: 0`.
2. End-to-end proof: a text chat and a 10 s clip (video_url) with the test key; read `infrx.jobs`/usage rows on hosted (read-only) for prepared_prompt_tokens == usage.prompt_tokens; the worker journal's `prepared <id>: N prompt tokens (engine /tokenize …)` line; video pads in range. If the real engine's /tokenize does not expand video placeholders, video preparation fails closed (dependency_unavailable) — that is PREP-WORKER Limit 1 and becomes a fix lane.
3. E1B L2–L7 (gateway-side cells; `research/plan/evidence/e/E1B-box-*.md` protocol) then `certify.py --box` (E4B protocol v2; 4-hour soak unless the user shortens it) → release decision → tasks.json E4B box half → W13 record (release, image, engine digest, snapshot snap-0c3f41caccbffbfcf, backups, digest, command ids; release the lock).
4. Docs: the runbook already has W7b (operator seed) and the W10-exit-4 revert row; add the operator-key bootstrap row and the worker-loop checklist lines (`prepared …` INFO, `drained preparation:`).
5. Follow-ups (not blocking): PREP-WORKER requests 2 (0019 zero-ref attach marker), 3 (worker-only media root), 7 (fake-vLLM ports out of the ephemeral range: P-21 hit every gate), F1/F2 of its verifier; VER-N1 (cutover); worker verifier F1/F2; M limits; V-N1 (D5); P-01 (the provisional card is live on hosted; no CREDIT regime); P-19; the dependabot alert on main.

## BOX STATE (10:10Z)
Release 4226315 (pilot): units marlin2b-gateway (8001), infrx-worker (8002), infrx-valkey (6379), marlin2b-vllm (8000, max-num-seqs 8); caddy with the release's edge (admin on the unix socket); checkout /home/ubuntu/model-inference at 4226315; backups /var/backups/infrx/20260924T100129…-4226315… (and …T054001…-27af05a8…); untracked artefacts moved to /home/ubuntu/untracked-aside-20260924T051032Z/. Hosted: 0003–0018 applied; the operator seed; 4 A1-verified dev profiles; 2 pre-cutover consumer keys (untested). Local: disposable scratches removed; logs under `.claude-logs/checkpoint2-27af05a/`, `.claude-logs/gate-prep-4226315/`; stale infrx-d2-postgres (not ours to remove).
