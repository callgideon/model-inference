# Handoff — 2026-09-23T15:00Z (sofia window; Opus available again; six lanes live, one confirmation running)

Supersedes `HANDOFF-20260923T1330Z.md`. Since it (14:00–15:00Z, sofia account): the three dead workflows were rerun — **W4 verifier PASS → W4 MERGED f36c17c** (Makefile `api-mutants` += `tests/w/test_w4_mutants.py`; manifest W4 implemented, **24/30**; merged-tree `tests/w` 180 passed in 553 s, `.claude-logs/w4-merged-f36c17c.log`), **G3 confirmation fix_required** at cb096fb (4 blocking; JSON `research/plan/evidence/g/G3-confirm-cb096fb.json`), **I3B follow-up drill lens + refuters** finished (fix_required; JSON `evidence/i/I3B-followup-review-d99da09.json` complete) — and the dead lanes were re-dispatched on their worktrees (D5, G2 finished round 3 → confirmation running, E3B2, F fakes, I3B fix round, G3 round 3). `main` = 01a7dfc (checkpoint). Rulings R1–R94 (next free **R95**); pending input P-21.

Rules unchanged (1125Z handoff). Standing user instruction: **"safely complete and merge tasks, run max parallel agents / tasks — wherever possible"**; "update tracker" stands. Reviews/refuters/implementers on Opus 5.5; Fable plans/orchestrates; ultracode on. Session record `research/plan/evidence/coordinator/2026-09-22-session-02.md` (append-only). Tracker: `python3 research/plan/scripts/progress.py`; sofia artifact https://claude.ai/artifact/Rk84qe3Yuzr2d357GoeRvZ (file `scratchpad/marlin-backend-tracker-sofia.html` = copy of `evidence/coordinator/progress.html`); rey 3a3VjYKb2Vfh5X89BYMDPZ, dhruv LqEn7ZiMjYgRXZdCUtnL6o (each account can only update its own).

## BRANCH STATE (verified local == origin at 2026-09-23T14:58Z; lane heads move while their agents run — push at each handback)
Integration `claude/backend-impl` = 977e335+ (worktree `infrx-impl`); `main` = 01a7dfc.

| lane | worktree / branch @ head | state | next |
|---|---|---|---|
| G2 | codex-g2 `codex/g2-chat-relay` @ 76ba3bb (round 3 done: 5 blocking + 9 nonblocking closed; G list 306) | **round-3 confirmation running** (`workflows/scripts/confirm-g2-r3-wf_df488b03-ea6.js`, run wf_df488b03-ea6; money/stream/composition lenses + refuters) | pass → **MERGE** `--no-ff` (contracts R91 ports/fakes/conformance + PENDING on D5; cutover diff HELD; coordinator requests R2-1/R2-4 stay open) → tell D5 to merge the integration head and implement `PgJobStore.lookup`; fix_required → SendMessage the lane |
| G3 | codex-g3 `codex/g3-jobs` @ 3194578+ (the G2 r3 merge + R3-1 re-anchor landed) | **round 3 running** (new agent; brief in the dispatch: ADM-R2-B1/B2/B3 + stream-C1 blocking, N1/N3/C2/C3/C4 cheap nonblocking) | handback → re-confirmation (admission + stream lenses; single verifier if tests-only) → **MERGE after G2** (ROUTERS += jobs at the cutover) |
| E3B2 | codex-e3b2 `codex/e3b-phase2-gate` @ f97bfb7+ (1 dirty file; its layer-3 gate on its own `infrx-e3b2-*` stack is running) | **fix round running** (interim report: "gate at f97bfb7 running: suites stage then mutation stage") | handback → confirmation → **MERGE** (IR #1 TASK_BLOCKS + strict-xfail deletion; R93 fake requeue id via the F fakes lane; drop F2P from PENDING; RESIDUAL/PENDING per I3B) |
| F fakes follow-up | codex-ffakes `codex/f-fakes-followup` @ 985ef1c+ (evidence WIP; full mutant list running) | **running** | handback → review (one F lens + refuters) → **MERGE after G2** |
| I3B follow-up | codex-i3bf `codex/i3b-followup` @ e3577b3+ (round 2: DR-4 rc00 owner pins …; bk01f/bk01g restore tests running — needs `INFRX_D1_IMAGE=supabase`) | **fix round running** | handback → confirmation (restore + drill lenses) → **MERGE AFTER E3B2** |
| D5 | codex-d5 `codex/d5-terminal-transaction` @ 9d0b24b+ (6 dirty files; items 1, 3, 5, 10b committed: legacy USD settlement, terminalize, PgJobStore.complete…; now credit_grid/credit_retired mutants) | **running** (ports 55436/55467, Q 55498) | handback → review (3 lenses) → **MERGE after I3B** (+ `lookup` SQL once G2 merges; 0018 in the harness list; Makefile list) |
| E1B | — | GPU slices wait on the box rollout | |
| closed | W4 b1eee7c (f36c17c), D4 90efcb0 (93ba108), G4U d1827d5 (7d21fa7), W-new 483d763 (a15fa4b), I-schema 4c7de56 (bc382ac), F cancel-cause 0fe8fdd (6803d2f) | | |

## DETACHED / RUNS / CONTAINERS
- Workflow running: G2 r3 confirmation wf_df488b03-ea6. Scripts of this window under the session dir `…/7aae6bdd-…/workflows/scripts/` (confirm-g3-rerun-wf_e024f4f2-8dc.js, review-i3bf-drill-wf_28258566-66d.js, verify-w4-r4-rerun-wf_09c50070-a9c.js).
- Containers: `infrx-e3b2-{postgres,valkey,clickhouse,s3}` = E3B2's live gate (its own; do not touch); `infrx-d5-postgres` = D5's; `infrx-d1-postgres` = the I3B fix lane's restore fixture (supabase image); `infrx-d2-postgres` (a lane's default d2 run; leave); `gideon-*` not ours.

## NEXT (in order)
1. Process results as they land (parse `result`; save JSON under `research/plan/evidence/<track>/`; record in the session log + progress-state; tracker; pass → merge `--no-ff` with the integration requests, merged-tree suites on private ports, push; fix_required → SendMessage the lane, then a confirmation on the fix head). Null/empty workflow results are NOT a pass.
2. Merge order: **G2 → G3 → F fakes → E3B2 → I3B follow-up → D5 → E4B.** Then the checkpoint gate: whole-tree api-test + api-mutants on private ports (`INFRX_D_TASK=d3 INFRX_D2_VALKEY_PORT=55464 INFRX_D2_VALKEY_CONTAINER=infrx-d3-valkey INFRX_Q_VALKEY_PORT=55490`, `-rfE` into a log; console + bench) → ff `main`. (W4, D4, G4U, W-new are merged since the 01a7dfc checkpoint with per-merge suite checks only.)
3. W4 phase B: coordinator box re-measurement (E0/E1 candidates; maintenance window; `candidate.sh` preconditions in `research/plan/evidence/w/W4-*.md`).
4. Keep pushing lane branches at each handback; tracker after each state change; refresh this handoff at each checkpoint; at ~90 % of the window stop dispatching, write the handoff, commit, push, idle.

## OPEN ITEMS / TRAPS
As in the 1300Z/1330Z handoffs (Dependabot alert 1 moderate; P-21; DR-2 D-mode harness lock stays shared under a private TMPDIR; G2 R2-1/R2-4; G3's F/D requests in D5 addendum 4; D4 request 9 to W; the E runner `_temp_litter` sweep — E3B2 2cc281c; the Q3 drill's unbounded `while not w.leases` wait; W3 `sigint_not_handled` load-sensitive). Plus: lane worktrees may lack an upstream (`git rev-parse @{u}` empty) — push by branch name from `infrx-impl`; the auto-mode classifier can transiently return no verdict for a Bash call (retry once, then move on).
