# Metadata for the deepseek41fnvfp4 experiment. Data only — logic lives in common/.
EXP=deepseek41fnvfp4
HF_REPO=nvidia/DeepSeek-V4.1-Flash-NVFP4
# S3 mirror prefix. Differs from EXP for historical reasons: the mirror was
# populated before these directory names existed. Do not "fix" it without
# moving the objects.
S3_DIR=deepseek-v41-nvfp4
SIZE_GB=492
GATED=no
NOTES="NVIDIA NVFP4 requant; validated upstream on SGLang, not vLLM"
