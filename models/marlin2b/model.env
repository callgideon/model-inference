# Metadata for the marlin2b experiment. Data only — logic lives in common/.
EXP=marlin2b
HF_REPO=NemoStation/Marlin-2B
# S3 mirror prefix. Differs from EXP for historical reasons: the mirror was
# populated before these directory names existed. Do not "fix" it without
# moving the objects.
S3_DIR=marlin-2b
SIZE_GB=5
GATED=yes
NOTES="GATED. Qwen3.5-2B based video model; transformers>=5.7.0, torchcodec"
