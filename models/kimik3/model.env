# Metadata for the kimik3 experiment. Data only — logic lives in common/.
EXP=kimik3
HF_REPO=moonshotai/Kimi-K3
# S3 mirror prefix. Differs from EXP for historical reasons: the mirror was
# populated before these directory names existed. Do not "fix" it without
# moving the objects.
S3_DIR=kimi-k3
SIZE_GB=1561
GATED=no
NOTES="2.8T MoE, natively MXFP4 (1,561GB on disk); fits one 8xB300 node (~2,144GB usable, 268GB/GPU)"
