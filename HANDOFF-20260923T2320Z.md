# Handoff — 2026-09-23T23:20Z (sofia window; after the second Opus session limit; all lanes resumed)

Supersedes `HANDOFF-20260923T2130Z.md`. Since it: box phase 1 DONE and merged (0117d48: W4 phase B decision **B at 82 s**, P-20 decided; E1B L0/L1/L8 measured; window 19:47–21:54Z closed clean); merge plan merged (58e6fdb, `research/plan/evidence/coordinator/merge-plan/`); M1-L2 FINAL ba26ca4 (verifier pass + V1–V4); E4B software half FINAL 4d9360c (verifier pass + V1–V6); M pilot-media handed back 8b91648 (review rerunning after a 429); phase 3 review fix_required (J2) → fix round; D5 review fix_required (8 test gaps) → fix round; cutover at f7d9b03 (+WIP 92269a6) finishing runs; the Opus limit hit at 22:45Z (reset 23:00Z) and every lane was WIP-saved, pushed and re-dispatched at 23:1xZ. `main` = 01a7dfc (remote); manifest 27/30 (D5, E4B flip at the merges). Rulings R1–R94; the merge plan's step 9 numbers R95 (M1-L2), R96/R97 (E4B), R98 (cutover) — re-read the cutover's COMMITTED evidence first; M pilot-media's (a)–(c) and D5's candidates also need numbers.

Rules unchanged (1125Z handoff; box via SSM only with W3's helpers; secrets never in text). Policy: ONE fix round per lane; reviews on committed heads. Session record `research/plan/evidence/coordinator/2026-09-22-session-02.md` (append-only, complete); tracker `python3 research/plan/scripts/progress.py`; sofia artifact https://claude.ai/artifact/Rk84qe3Yuzr2d357GoeRvZ.

## LANES (all pushed; integration `claude/backend-impl` = 0bdb61a+)
| lane | branch @ head | state | next |
|---|---|---|---|
| D5 | codex-d5 `codex/d5-terminal-transaction` @ 8554b47+ | RESUMED 23:1xZ: fix-round commits in; verification battery + evidence → handback | single Opus verifier (money on both images) → **merge unit step 1** |
| E3B phase 3 | codex-e3b3 `codex/e3b-phase3-bodies` @ 1fa825b+ (contains D5 c67e4f5, cutover 6cb8ebe, M1-L2 e1bb54f, M pilot 8b91648) | RESUMED: fix round (J2 non-embedded video cells run with the M merge; J1 footprint; H-B1; nonblockings) + gate on e3b2 (its stack was left up) | single verifier → **step 2** (with D5) |
| CUTOVER | codex-cutover `codex/cutover-mount` @ 92269a6 (= f7d9b03 + WIP evidence) | RESUMED: harvest its detached runs at f7d9b03, evidence, handback | one-round review → **step 3** |
| M1-L2 | codex-objstore `codex/m1l2-object-store` @ **ba26ca4** FINAL | done | **step 4** |
| M pilot-media | codex-mpilot `codex/m-pilot-media` @ 8b91648 | review RERUN wf_44570caf-07a (parity on PostgreSQL + honesty) | fix round if needed → **step 5** |
| E4B | codex-e4b `codex/e4b-certify` @ **4d9360c** FINAL (software half) | done | **step 6** (Makefile union; regenerate E4B-endpoint.md; 6a/6a2; namespace e4b; tasks.json wording) |
| BOX | `codex/box-measure` eea17d8 merged 0117d48 | done (phase 1) | step 7 = nothing; phase 2 after checkpoint 2 |
| ROLLOUT-PREP | codex-rollout `codex/rollout-prep` @ 0e9f950+ | RESUMED: item 4 tests + item 5 (preflight dry-run with the new keys) | step 8 (evidence) |
| closed | I3B follow-up, E3B2, F fakes, G3, G2, W4, D4, G4U, W-new, I-schema, F cancel-cause | | |

## MERGE (research/plan/evidence/coordinator/merge-plan/README.md)
Steps 1–4 (D5 → phase 3 → cutover → M1-L2) are ONE unit gated on all four verdicts; checks once after step 4. Then 5 (M pilot), 6 (E4B), 8, 9 (rulings). The step scripts were written against older heads (D5 8554b47, phase 3 a4facba, cutover 351d084, M1-L2 0b9fc50) — run `replay.sh` only after checking each script's SHA pins against the final heads; the resolutions are assert-guarded and will refuse on drift. Checkpoint 2 = api-test whole + layer 0 + bench (foreground) + the affected mutant lists (contracts, m/pilot/s3 with MinIO, d, g*, i, E4B) + the W3 sigint mutant on a quiet host + one layer-3 run (expect exit 3 with only I2B-R4 pending, or exit 0) → ff `main` (remote 01a7dfc and the stale local f9ba5d2 are ancestors).

## BOX PHASE 2 (after checkpoint 2)
Per merge-plan README §7 + the rollout-prep checklist `infra/runbooks/rollout.md`: maintenance edge (saved live Caddyfile pattern) → drain → fresh hosted backup → migrate 0018+ → `install.sh INFRX_MODE=pilot RELEASE=<full sha>` with preflight `--set S3_MEDIA_BUCKET=llm-bootcamp-641134885443` (prefix infrx/; the bootcamp role already permits), MAX_VIDEO_SECONDS=82, ENGINE_MAX_NUM_SEQS/WORKER_CONCURRENCY 8, INFRX_RELEASE_SHA/INFRX_IMAGE (build-info), ACCOUNTING_REGIME legacy_usd → readiness → tests/m/test_s3.py against the real bucket → edge live → smoke → E1B L2–L7 → `certify.py --box` (E4B box protocol v2; infrx-certify image with git) → release decision. Start-to-ready measured 168–181 s.

## RUNS / CONTAINERS
Workflows: M pilot-media review wf_44570caf-07a. Containers: infrx-e3b2-* (phase 3's, reused or restarted by the resumed lane), infrx-cutover-valkey + infrx-d3-postgres + infrx-q3-valkey-55492 (cutover's runs), infrx-d5-* (D5), infrx-d2-postgres (stale default), gideon-* (not ours). The cutover's two detached pytest processes at f7d9b03 were left for the resumed agent.

## TRAPS (new)
- An Opus session limit kills every agent and workflow at once; a workflow whose lenses all 429 returns `incomplete`/empty — never a pass. Save (WIP-commit, push), record, re-dispatch fresh agents with "RESUME" prompts pointing at the branch evidence and the review JSON.
- Workflow re-launch by `scriptPath` can be refused; resend the script inline.
- Open items: P-19 (g6e price), P-21 (reserved ports; TIME-WAIT collisions hit five lanes today), I2B-R4 (worker entry point — pilotbox.worker_service + CreditWork is the composition), M3 limits (text-job attach not durable; collector not composed; upload records in-process), G2 B2 retry window after a gateway restart, E4B's served-build needs infrx_build_info (cutover item 7 provides it), the dev host's App dev servers exempt locally, L8 120 s parity mismatch undiagnosed, E1B L2–L7 after the rollout.
