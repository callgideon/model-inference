# Handoff — 2026-09-23T13:00Z (rey window; refreshed at the round-3/4 stage of D4, G2, G3, E3B2, W4 and the I3B follow-up)

Supersedes `HANDOFF-20260923T1125Z.md`. Since it: G3 handed back and reviewed (fix_required; ruling **R94** the execution mode joins the idempotency identity), D4 round 2 confirmed on the journal lens (one adapter test gap → round 3), G2 round 2 confirmed fix_required (5 blocking → round 3), W4 rounds 3–4 (single-verifier check running), E3B2 fix round running, **I3B follow-up handed back** (review running; merges after E3B2), rulings **R92–R94** written (next free **R95**), pending input **P-21**. `main` = 01a7dfc (checkpoint); 22/30 merged. Reviews/refuters/implementers on Opus 5.5; Fable plans/orchestrates; ultracode on.

Rules unchanged (see the 1125Z handoff): coordinator pushes lane branches; detach > 10 min; never another lane's containers; secrets never printed; AWS CLI via `env -u …`; never the pilot box outside logged ops. Standing user instruction: **"safely complete and merge tasks, run max parallel agents / tasks — wherever possible"**; "update tracker" stands.

## GOAL / TRACKER
`research/plan/18-marlin-backend-first.md`, manifest v4; session record `research/plan/evidence/coordinator/2026-09-22-session-02.md` (append-only); tracker `progress.py` → `evidence/coordinator/progress.html`, publish with `url` https://claude.ai/artifact/3a3VjYKb2Vfh5X89BYMDPZ. Manifest status changes: textual edit of `tasks.json` (`implemented` + merge note in `disposition`), then `validate_plan.py --write-ledger`.

## BRANCH STATE (2026-09-23T13:00Z; all lane branches pushed at their last handback)
Integration `claude/backend-impl` = 7af383e+ (worktree `infrx-impl`); `main` = 01a7dfc.

| lane | branch @ head | agent | state / next |
|---|---|---|---|
| D4 | `codex/d4-stream-journal` @ 7d3ef59+ | a3dc7d1193c9ade11 | round 3 (tests + surrogate guard; JSON evidence/d/D4-confirm-7d3ef59.json) → single Opus verifier → **MERGE** (harness += `0017_stream_journal.sql`; Makefile += `tests/d/test_code_mutants_d4.py`; pgstate: two `infrx.jobs` columns + CHECK, six functions, trigger `jobs_terminal_journal_event`; E3B2 rig hook) → **dispatch D5** (worktree codex-d5, branch codex/d5-terminal-transaction, ports 55436/55467; brief `.claude/handoff/wave3/D5.md` addenda 1–4) |
| G2 | `codex/g2-chat-relay` @ a786af7+ | a98f6b711111c4380 | round 3 (JSON evidence/g/G2-confirm-a786af7.json: in-flight replay must not re-prepare or recheck/cancel; drain-before-close and identity-send pinned) → confirmation → **MERGE** (contracts R91: ports/fakes/conformance + PENDING on D5; cutover diff HELD) |
| G3 | `codex/g3-jobs` @ a100aee+ | a532cdcf3b6126358 | merging G2 a786af7 then fix round (JSON evidence/g/G3-review-a100aee.json: R94 in the ingress digest; events observer paths; DELETE on a failed cancel) → merges G2's round-3 head when told → confirmation → **MERGE after G2** (ROUTERS += jobs at the cutover) |
| E3B2 | `codex/e3b-phase2-gate` @ 80cef22+ (local 3438d86+) | aff9c42bf5340b59a | fix round (JSON evidence/e/E3B2-review-80cef22.json + the `_temp_litter` cross-lane sweep) → confirmation → **MERGE** (IR #1 TASK_BLOCKS + delete the strict-xfail mark; fake requeue event id R93 + drop dr04's fake exception; drop F2P from PENDING; RESIDUAL/PENDING per I3B's request 1) |
| W4 | `codex/w4-measured-tuning` @ b1eee7c | a2c1ab13508b58f8b | single-verifier check wf_bfa72784-488 (task wrjnkixr5) → **MERGE** (Makefile += `tests/w/test_w4_mutants.py`, ~22 min list); phase B = coordinator box re-measurement (E0/E1 candidates; maintenance window; candidate.sh preconditions in its evidence) |
| I3B follow-up | `codex/i3b-followup` @ d99da09 | ae27ec5a6720306d3 (done) | review wf_cb972a2c-da6 (task w3mxm87ee) → **MERGE AFTER E3B2** (depends on stack.stubbed() + OWNED_TREES += infra; its RESIDUAL/PENDING diff in the evidence) |
| E1B | — | — | GPU slices wait on the box rollout |
| closed | G4U 7d21fa7, W-new a15fa4b, I-schema bc382ac, F cancel-cause 6803d2f | | |

## DETACHED / RUNS
- Workflows running: W4 verifier wrjnkixr5; I3B follow-up review w3mxm87ee. Lanes running: D4, G2, G3, E3B2.
- Containers: lanes' `infrx-d4-*`, reviewers' `infrx-review-*`/`infrx-confirm-*` (removed at exit), `infrx-d2-postgres` (a lane's default d2 run; leave), `gideon-migration-order-test-*` (not ours). d5's ports (55436/55467) are free for the D5 dispatch.

## NEXT (in order)
1. Process results as they land (parse `result`; save JSON under `research/plan/evidence/<track>/…`; record; pass → merge `--no-ff` with the integration requests, merged-tree suites on private ports, push; fix_required → SendMessage the lane, then a confirmation on the fix head — a single Opus verifier is enough for a tests-only round).
2. Merge order: **W4 → D4 (then dispatch D5) → G2 → G3 → E3B2 → I3B follow-up → D5 → E4B.** Checkpoint after D4 + W4 + G2 merge: whole-tree api-test + api-mutants on private ports (`INFRX_D_TASK=d3` once D5 holds d5, `INFRX_D2_VALKEY_PORT=55464 INFRX_D2_VALKEY_CONTAINER=infrx-d3-valkey INFRX_Q_VALKEY_PORT=55490`, `-rfE` into a log; console/bench) → ff `main`.
3. Coordinator-owned changes queued for merges: R93 fake requeue event id (at the E3B2 merge); D4 request 10 to F (the fake refuses NUL/NaN/Inf and surrogates identically; continues past pruned_to) — a small F fix lane after D4 merges; the CatalogDirectory/G6B adapters and `lookup` SQL are D5's; register owner references `I2B-R4` and `M1-L2` (I3B request 2) in recoverykit.OWNERS or tasks.json.
4. Keep pushing lane branches at each handback; tracker after each state change; refresh this handoff at each checkpoint; at ~90 % of the window stop dispatching, write the handoff, commit, push, idle.

## OPEN ITEMS (not blocking merges)
See the 1125Z handoff's list, plus: P-21 host reserved ports (user root approval); the D restore fixture does not support the plain PostgreSQL image (17.6 client vs 16.14 server — I3B follow-up finding; the fixture should refuse it explicitly); the E runner's `_temp_litter` cross-lane sweep (E3B2's round); G2's R2-1 cutover request (create_app builds adapters from settings) and R2-4 (M: persist the attach with the admission or sweep strays); G3's F/D requests (get_owned_credit state/deadline, max_output_tokens on the owned read, db_now on the port, result_expires_at) → D5 brief addendum 4; W's request 9 from D4 (fixed-point numbers, no NUL/NaN/Inf in event payloads).

## TRAPS
As in the 1125Z handoff, plus: a workflow template literal must not contain backticks; the D restore fixture needs `INFRX_D1_IMAGE=supabase`; `git clone --shared` is the way to build a scratch that merges two unmerged branches (the I3B follow-up review does this).
