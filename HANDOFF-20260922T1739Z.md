# HANDOFF — coordinator restart (2026-09-22, written under a 5-hour account limit)

Written by the Fable coordinator of the infrx Marlin backend-first program, seconds before the
process was killed for an account switch (dhruv → rey). The conversation transcript survives; every
subagent and background shell did NOT. Read this file first, then
`research/plan/evidence/coordinator/2026-09-22-session-02.md` (append-only coordinator record, has
every decision, ruling and handback of this session) and `research/plan/evidence/coordinator/PROGRESS.md`.

## GOAL
Implement the Marlin2B backend-first endpoint per `research/plan/18-marlin-backend-first.md` and
manifest v4 (`research/plan/tasks.json`, `current_execution_scope` BACKEND-FIRST-MARLIN, closure of
E4B = 37 tasks). Coordinator (Fable) plans/reviews/merges; Opus 5 implements; every task gets an
independent adversarial mutation-tested review at HEAD before a `--no-ff` merge onto
`claude/backend-impl`; `main` is fast-forwarded at green checkpoints. User authorization is total
(migrations, cutover, paid calls, compute, main merges) with the operating rules in the session-02
record (log purpose/cost/rollback before irreversible ops; backups before hosted migration; I0
fail-closed installer before public cutover; no secrets printed). Where we are: band B0 (baseline +
contracts) is mid-flight; S1, S2M, E2R item 1 and S1-fix are merged; nothing GPU-side has run.

## BRANCH STATE (all pushed unless stated)
- Integration branch `claude/backend-impl` — head = this handoff's commit (see `git log -1`); base `ec6c548`.
  Merges so far: E2R item 1 `c23d804`; S2M `3aa3535` (merge commit after it); S1-fix `6669e1f`.
- `main` = `origin/main` = `f6fd3e8` (checkpoint 1). Next ff of main: after the next green `make check` on the integration tree.
- Tracker: `research/plan/scripts/progress.py` renders `research/plan/evidence/coordinator/PROGRESS.md`
  + `progress.html` from `tasks.json` + `progress-state.json`. Published artifact
  https://claude.ai/artifact/JBrayu2qRNiZFbwGBqHmra (republish the same scratchpad path
  `marlin-backend-tracker.html`, or pass the URL as `url`). Version 11 at cutoff.
- Lane worktrees under `.claude/worktrees/` (branches NOT pushed; base ec6c548 unless stated):

| lane | worktree / branch | head at cutoff | state |
|---|---|---|---|
| F2R-A | codex-f2r / codex/f2r-contract-carryovers | `48d61b9` = **WIP snapshot commit** of 13 uncommitted files (agent died mid-work) | items 2–5, 9 + relayed additions (ports docstring, money assert pins, MediaRef.local_path, PROCESSING_CACHE_DIR, drop video/mpeg, prepare_request on the port, item 4). Needs a fresh implementer to resume FROM the WIP commit; inspect `git show --stat 48d61b9` first |
| F2R-B | codex-f2r-b / codex/f2r-b-console-contracts | `501c496` (clean) | round 2 green; **independent review was running (died)** → re-dispatch review |
| F2P | codex-f2p / codex/f2p-v2-contracts | `c42b213` (clean) | additive phase handed back; **review + product-rule audit was running (died)** → re-dispatch |
| I0 | codex-i0 / codex/i0-installer-failclosed | `c57b10d` = WIP snapshot over `b818f89` | fix round 1 in progress (review B1 runner layout, B2 apply-level pilot digest case, 7 nonblocking) → resume from WIP |
| E2R | codex-e2r / codex/e2r-harness-repair | `6aa4d77` = WIP snapshot over `2e014b5` | item 1 merged (`ad12914`→`c23d804`); items 2–4 + integration layer-2 red (role matrix vs 0004/0005 grants) + order-dependence + ps-branch in progress → resume from WIP |
| S2M | codex-s2m | `3aa3535` | MERGED |
| Q2 | codex-q2 / codex/q2-valkey-scheduler | `6574bd0` = WIP over `10571c7` | Valkey scheduler adapter in progress, never handed back → resume from WIP |
| M2 | codex-m2 / codex/m2-media-preparation | `12f2cdb` (clean) | round 2 done; **re-review by the M2 reviewer was running (died)** → re-dispatch a short re-review (checks listed in session record) then merge |
| W2 | codex-w2 / codex/w2-attempt-loop | `f03c038` = WIP over `9a606fc` | worker attempt loop in progress, never handed back → resume from WIP |
| E1B | codex-e1b / codex/e1b-baseline-harness | `383981c` = WIP over `20fa07a` | fix round 1 in progress (120 s clip, price ⚠️, KV wording, four test bindings, N1–N8) → resume from WIP |
| S1-fix | codex-s1fix / codex/s1fix-console-balance | `ece02d8` | MERGED |

## DONE (verified)
- I1B live inventory: `research/plan/evidence/i/I1B-inventory-2026-09-22.md`; P-02/P-03/P-04 resolved; EBS snapshot `snap-08732d3ac6376e850` of the pilot box root volume, DeleteOnTermination=false.
- E2R item 1 merged `c23d804`: tests/d 76 passed on merged tree; 2 coordinator mutants killed. Interim `tests/d` exclusion LIFTED for lanes rebased on ≥ c23d804 (lanes still on ec6c548 keep `--ignore=tests/d`).
- S2M merged (profile `research/workloads/marlin-sop.md`): review fix_required → fixed at f9853cc → coordinator digest correction 3aa3535. FACT: Hugging Face returns `*`×64 for `.lfs.oid` on this GATED repo to unauthenticated callers (verified from this host AND from the pilot box) — it is NOT a local redaction filter. Served-bytes sha256 of the three files measured on the box and recorded; registry-oid equality ⚠️ needs `HF_TOKEN` (W3). Permission classifier DENIED (twice) using the box's stored HF credential — do not retry; ask the user for `HF_TOKEN` or the three oids.
- S1 independent review: API/contract side PASS (1588 tests / 1039 mutants, 0 skips, Docker D list included); console B1/B2 fixed by S1-fix, merged `6669e1f` (console 258 tests, mutants 144/39/61/96, 2 coordinator mutants killed).
- Rulings added: R61 (upload ref form; pilot file:// form — AMENDED to `<root>/<org>/<profile>/<digest16>/source.<ext>`), R62 (model_revision keeps the public-id prefix; v1 value not renamed; artifact commit pinned by v2 serving_version). R29 text aligned with the R-3 clamp.
- Manifest: S1 and S2M `implemented`; ledger refreshed (`validate_plan.py --write-ledger`). Validator PASS.
- New pending input P-19 (sourced AWS g6e/L40S price row in cloud-pricing.md before any cost figure is published).

## IN FLIGHT AT CUTOFF (all died with the process; nothing came back)
Reviewers (re-dispatch with the same brief text, which is in the transcript / summarised in the session record):
1. F2R-B review at `501c496` — **ARRIVED after the handoff was written: verdict PASS** (all claims verified from code and D1 SQL; console 263 / mutants 160/40/64/96 / Python 737 and 1612 reproduced; 40 reviewer mutants, 3 survivors that are coverage gaps: NB-1 C-side `sample_count` derived from the capped array needs a ≥50-sample fixture + mutant; NB-2 traces.json R62 alignment unguarded — assert trace fixture models ∈ orgs.models; NB-3 pin MIN_CONSOLE_CURSOR_SECRET_CHARS == 16 with a 15-char refusal). NB-7 ESCALATE before C0: `AuditAction` vocabulary drift (console `grant|suspension_set|entitlements_set` vs D1 0003:782 `admin_grant|admin_set_suspension|admin_set_entitlements`) — first real operator_audit row fails exported conformance; and 08 §5 `FETCH_*` vs code `MEDIA_FETCH_*`. NB-8 coordinator call: `deepseek-v41-flash@2026-08-15` stays unprefixed. **NEXT step 2 becomes: merge F2R-B `501c496` `--no-ff` directly; hand NB-1/2/3 to F2R-A or a tiny follow-up; ruling needed on AuditAction (align console to D1's vocabulary — D1 is authoritative).**
2. F2P review + product-rule audit at `c42b213` (agent aa52e30e419615503) — no result.
3. M2 re-review at `12f2cdb` — **ARRIVED after the handoff was written: verdict PASS** (B1/B2 killable; R10 disposition accepted as equivalent; zero-length uint choice correct; 44 hostile cases + 3×2000 fuzz, 0 non-domain exceptions; tests/m 241, suite 1635, 147 mutant tests). Three nonblocking notes to hand to M3 or fold at merge: Matroska duplicate-Duration guard is value-based (a first 0.0 licenses a second — use a seen flag like ISO's), duplicate TimecodeScale is last-wins, the 9-byte TrackType hostile case should be a non-zero 9-byte TimecodeScale asserting reason == "bad-uint". **NEXT step 2 becomes: merge M2 `12f2cdb` `--no-ff` directly (no re-dispatch), run tests/m + contracts + w/g on the merged tree.**
Implementers (resume from the WIP commits above; each needs a fresh agent with the lane brief `.claude/handoff/wave3/<TASK>.md` + `wave3-common-brief.md` + the fix list from the session record):
4. F2R-A (a052bc57585381792), I0 (a55e0f876b30c12eb), E2R (a6fa6cb58dc6c0d1c), Q2 (a3c52b73454bd65ca), W2 (a0672f058d5a5ef3b), E1B (af357fc9210eb530a).
Background shells: only the F2R-A lane's own mutant run mattered (see DETACHED). My own long runs had all completed.

## DETACHED
- F2R-A full shared mutant run (INFRX_MUTANTS=all, 7 lists minus tests/d), cwd
  `.claude/worktrees/codex-f2r/apps/infrx-api`, against the WIP-committed tree `48d61b9`:
  **PID 3511453**, log `.claude/worktrees/codex-f2r/apps/infrx-api/f2r-a-mutants-detached.log`.
  Check: `pgrep -f test_trace_mutants` (empty = finished) then `tail -3 <log>` (expect `N passed`, ~20–35 min from 18:0x UTC). Note it may collide with any lane running the D harness only if it reaches tests/d — it does not.
  The log file is untracked; do not commit it.

## WORKFLOWS
None (ultracode/Workflow off all session; Agent tool only).

## UNCOMMITTED
Integration worktree: clean after this handoff commit. Lane worktrees: all WIP-committed (see table). Untracked lane logs/scratch are disposable.

## NEXT (in order)
1. `git fetch`; confirm `claude/backend-impl` head; read session-02 record tail. Check the detached log.
2. Re-dispatch the three reviews (F2R-B, F2P, M2 short re-review). On pass: merge F2R-B and M2 `--no-ff` (M2 adds `store.py` +35 lines — F2R-A item 4 touches the same file; merge M2 first, then F2R-A rebases). F2P additive merge after its review passes and 01a §8's 14 proposed rulings are numbered into 08 §10 (R63+) — read them before merging.
3. Re-dispatch the six implementer lanes from their WIP commits (tell each: first `git log --oneline ec6c548..HEAD` and `git show --stat HEAD`; continue; do not rebase; lanes on ec6c548 keep `--ignore=tests/d`).
4. When F2R-A hands back: independent review → merge → then F2P wire-in phase (13 files listed in `research/plan/01a-contracts-v2-map.md` §7) → dispatch D1R (brief drafted at `.claude/handoff/wave3/D1R.md`), G1R, G6B, M3, Q3, W3 (W3 brief must carry: pull vLLM by registry digest, both EOS ids [248044, 248046], profile-v1 flags, `--max-num-seqs` 32-vs-8 reconciliation, `--allowed-local-media-path`, drain > 10 s, record LFS oids with HF_TOKEN).
5. After the next code merge: `make check` on the integration tree; if green, ff `main` and push; regenerate + republish tracker; append the session record.

## TRAPS
- Never `git stash` (shared stack). Never rebase lane branches yourself; lanes may not rebase either — coordinator merges handle it.
- `git worktree add ../worktrees/x` from a worktree nests the path; use absolute paths.
- Push codex branches with `git for-each-ref --format='%(refname:short)' refs/heads/codex/` (branch --list adds `+` markers).
- AWS CLI: `env -u AWS_ACCESS_KEY_ID -u AWS_SECRET_ACCESS_KEY -u AWS_SESSION_TOKEN aws …`; SSM scripts: base64-wrap (`echo <b64> | base64 -d | bash`) — the SSM shell is dash (`set -o pipefail` fails) and quoting breaks.
- Direct Supabase host is IPv6-only; use the pooler `aws-0-us-east-2.pooler.supabase.com`. Hosted `auth.uid()` reads both JWT GUCs (legacy first).
- `progress-state.json` `reviews` entries need `phase`+`since` keys (generator KeyError otherwise); task "done" comes only from `tasks.json` status (implemented/integrated) — update the manifest, then `validate_plan.py --write-ledger`, or the validator fails "ledger stale".
- The D harness now provisions one container per pytest process with a port lock; a concurrent `tests/d` run fails loudly with HarnessBusy — retry, never delete containers. Reviewers must use their own namespaces.
- Mutation runners that copy a tree: check the copied layout preserves `models/marlin2b` and `apps/infrx-api` depth (false-kill path found in I0 and E1B).
- Hosted DB has only 0001–0002 applied; the merged console will show "Balance unavailable" until D1R + hosted apply (intended).
- Do not present the HANDOFF $2.24/h L40S figure as a price; no g6e row exists in cloud-pricing.md (P-19).
