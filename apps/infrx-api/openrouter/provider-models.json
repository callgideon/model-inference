{
  "data": [
    {
      "schema_version": "2.4",
      "id": "nemostation/marlin-2b",
      "name": "NemoStation: Marlin 2B",
      "hugging_face_id": "NemoStation/Marlin-2B",
      "created": 1789776000,
      "quantization": null,
      "tokenizer": "Qwen3",
      "description": "2B video VLM (Qwen3.5-2B base, Apache-2.0) for dense scene+event captioning with second-precise timestamps and natural-language temporal grounding. Send one video (mp4/webm/mov, up to 2 minutes; sampled at 2 fps, max 240 frames) plus a text prompt. Caption prompt: 'Provide a spatial description of this clip followed by time-ranged events.\\nFor each event, give the time range as <start - end> and a short description.' Grounding prompt: 'Identify the timestamps during which \"<event>\" takes place. Output the time range as \"From <start> to <end>.\" (numbers in seconds).' Output begins with a <think> token that the gateway strips.",
      "input_modalities": [
        {
          "type": "text",
          "supported_inputs": {
            "max_context_length": { "value": 32768, "unit": "token" },
            "max_prompt_length": { "value": 30720, "unit": "token" }
          },
          "pricing": [
            { "type": "prompt", "unit": "token", "cost_usd": "0.0000001" }
          ],
          "capacity": [
            { "type": "prompt", "unit": "token", "per": "minute", "value": 2000000 }
          ]
        },
        {
          "type": "video",
          "supported_inputs": {
            "max_prompt_length": { "value": 120, "unit": "second" }
          },
          "pricing": [
            { "type": "prompt", "unit": "token", "cost_usd": "0.0000001" }
          ],
          "capacity": [
            { "type": "prompt", "unit": "second", "per": "minute", "value": 3000 }
          ]
        }
      ],
      "output_modalities": [
        {
          "type": "text",
          "max_length": { "value": 2048, "unit": "token" },
          "streaming": true,
          "supported_parameters": {
            "max_tokens": { "type": "integer", "min": 1, "max": 2048, "default": 2048 },
            "temperature": { "type": "range", "min": 0, "max": 2, "default": 0 },
            "top_p": { "type": "range", "min": 0, "max": 1, "default": 1 },
            "stop_sequences": { "type": "array", "items": { "type": "unknown" }, "max_items": 4 },
            "seed": { "type": "integer" },
            "tools": { "type": "boolean" }
          },
          "pricing": [
            { "type": "completion", "unit": "token", "cost_usd": "0.0000003" }
          ],
          "capacity": [
            { "type": "completion", "unit": "token", "per": "minute", "value": 200000 }
          ]
        }
      ],
      "capacity": [
        { "type": "concurrency", "unit": "request", "value": 16 },
        { "type": "request", "unit": "request", "per": "minute", "value": 240 }
      ],
      "is_ready": true,
      "is_free": false,
      "openrouter": { "slug": "nemostation/marlin-2b" },
      "datacenters": [ { "country_code": "US", "region": "us-east-1" } ],
      "deployment_region": "us-east",
      "compliance": { "zdr": true, "hipaa": false }
    }
  ]
}
