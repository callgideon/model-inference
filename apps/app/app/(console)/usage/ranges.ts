// Integration seam (coordinator): the trace list (track V) shares the usage page's range picker
// values. One definition, in ./view-model.ts; this module only re-exports it.
export { RANGES, type RangeKey } from "./view-model.ts";
