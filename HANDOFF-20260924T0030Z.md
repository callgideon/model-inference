# Handoff — 2026-09-24T00:30Z (sofia window; three lanes final, two verifiers/reviews running, merge unit rehearsed)

Supersedes `HANDOFF-20260923T2320Z.md`. Since it: D5 verifier PASS at 4bfdbf0 (its verification battery still running: r5 pending; then ONE evidence commit with V-N2/3/4 pins); M pilot-media review fix_required at 8b91648 → fix round running (its branch already at 2d1dcbe+: PAR-1/H-B1 write-once attach, PAR-3/H-B2 O4–O6, nonblocking folds); **E3B phase 3 fix round handed back b9529d1** (J2: the worker is always its own process; M3-U1 retired; 14 mutants killed; gate exit 3 PENDING only I2B-R4) → **verifier wf_959f82dd-e03 running (task w1b1r9en3) on namespace e2**; **cutover FINAL 1bed457** (evidence-only on a1e88dc; review wx849k20w still running on a1e88dc); worker lane merged b9529d1 (07fe76e) and reconciled its real-worker path (1294d33). `main` = 01a7dfc; manifest 27/30 (D5, E4B flip at the merge). Rulings R1–R94; step 9 numbers R95–R98; M pilot's (a)–(c) and D5's candidates still need numbers.

Rules unchanged (1125Z handoff; ONE fix round per lane; reviews on committed heads; box via SSM only with W3's helpers; secrets never in text). Session record `research/plan/evidence/coordinator/2026-09-22-session-02.md` (append-only, complete); tracker `python3 research/plan/scripts/progress.py`; sofia artifact https://claude.ai/artifact/Rk84qe3Yuzr2d357GoeRvZ (version 25; file `scratchpad/marlin-backend-tracker-sofia.html` = copy of progress.html).

## LANES (all pushed; integration `claude/backend-impl` = 756997f+)
| lane | branch @ head | state | next |
|---|---|---|---|
| D5 | `codex/d5-terminal-transaction` @ **4bfdbf0** | verifier PASS; battery r5 running (waiter b6sniydb6 on `scratchpad/logs/r{4,5}-driver.done`); agent aae842bfc0f1fedf2 adds the evidence + V-N2/3/4 pins in ONE commit | push it → **merge unit step 1** |
| E3B phase 3 | `codex/e3b-phase3-bodies` @ **b9529d1** (contains D5 c67e4f5, cutover 6cb8ebe, M1-L2 e1bb54f, M pilot 8b91648 pre-fix) | verifier running | pass → **step 2** (with D5); fix_required → the lane is NOT re-dispatched for M's fixed head (the union is checked at step 5 / checkpoint 2) |
| CUTOVER | `codex/cutover-mount` @ **1bed457** FINAL | review wx849k20w on a1e88dc (1bed457 = evidence only) | pass → **step 3** |
| M1-L2 | `codex/m1l2-object-store` @ **ba26ca4** FINAL | done | **step 4** |
| M pilot-media | `codex/m-pilot-media` @ 2d1dcbe+ (fix round; agent a9833f74230b31cc9) | running | handback → push → single verifier → **step 5** |
| E4B | `codex/e4b-certify` @ **4d9360c** FINAL | done | **step 6** |
| ROLLOUT-PREP | `codex/rollout-prep` @ **f0a1a82** DONE | done | **step 8** |
| WORKER (I2B-R4) | `codex/i2b-r4-worker` @ 1294d33+ (agent a0bdc78d222ba64c1; namespace e3b2 + the d4 ports) | running (items 1–3 in; merged phase 3) | handback → push → single verifier → **step 8b** (needs WORKER_VERIFY) |
| closed | box phase 1 0117d48, merge plan 58e6fdb, I3B follow-up, E3B2, F fakes, G3, G2, W4, D4, G4U, W-new, I-schema, F cancel-cause | | |

## MERGE UNIT — REHEARSED CLEAN (2026-09-24T00:2xZ)
Scratch clones `scratchpad/mergeu` (steps 1–4 + 2(e)) and `mergeu2` (5, 6, 8, 9 on top) replayed the scripts at the FINAL heads (D5 4bfdbf0, phase 3 b9529d1, cutover 1bed457, M1-L2 ba26ca4, E4B 4d9360c, rollout f0a1a82; M pilot at its in-progress 2972df1): no conflicts except E4B's Makefile (scripted). Script fixes committed on the integration branch: step 2 = new edit **(e)** IR3F-2(b) (tasklocal `e3b2d` {postgres 55438, valkey 55468}; `run.make_env` passes INFRX_D_TASK/D2 Valkey/Q Valkey while the stack is up; the make_env case pins the keys — verified 3 passed + contracts config 245 passed) and the E3B disposition wording; step 6 = a2 reduced to the one caveat left at 4d9360c, (b) anchored on the e3b2d line (the rehearsal caught e4b reaching harness.py but not tasklocal). Checks on the rehearsal trees so far: layer 0 154/103 (steps 1–4), contracts quick 1030, E4B cases 39 + list 5, union layer 0 200 passed with 2 reds both external (harness[e4b] fixed by the (b) anchor; fake_vllm 55582 collided with the verifier's e2 stack — reruns green). `mergeu-checks/summary.log` still running tests/d, g, i, m+w on the steps-1–4 tree (d3 ports).
**Then:** `research/plan/evidence/coordinator/merge-plan/checkpoint2.sh` (new; detached; d3 ports, private MinIO 55694, api-test + layer 0 + affected lists + E4B list + W3 sigint + layer 3 on e3b2 `--only-suites --canary`); `make bench-test` in the FOREGROUND; then ff `main` (realign the stale local `main` ref f9ba5d2 → remote 01a7dfc first).

## BOX PHASE 2 (after checkpoint 2) — unchanged from the 2320Z handoff
Create SSM `/model-inference/pg_journal_url` (value from the existing params, shell var only) → `infra/runbooks/rollout.md` → maintenance edge → drain → hosted backup → migrate 0003–0018 → `install.sh INFRX_MODE=pilot RELEASE=<full sha>` (S3_MEDIA_BUCKET=llm-bootcamp-641134885443, MAX_VIDEO_SECONDS=82, ENGINE_MAX_NUM_SEQS/WORKER_CONCURRENCY 8, INFRX_RELEASE_SHA/INFRX_IMAGE, ACCOUNTING_REGIME legacy_usd) → readiness → tests/m/test_s3.py on the real bucket → edge live → smoke → E1B L2–L7 → `certify.py --box` → release decision.

## RUNS / CONTAINERS (00:30Z)
Workflows: phase-3 verifier w1b1r9en3 (e2 stack `infrx-e2-*` is its), cutover review wx849k20w (`infrx-review-cutover-honesty-*`). Lanes: D5 (infrx-d5-postgres-supabase, battery), M pilot-media, worker (infrx-d4-postgres + infrx-worker-valkey 55465 + infrx-worker-minio 55781; namespace e3b2). Coordinator: `mergeu-checks/run.sh` (d3 ports, infrx-d3-*). Stale: infrx-d2-postgres; gideon-* not ours. Killed: an orphaned fake_vllm from an old review on 55583.

## TRAPS (new)
- Extracting a script's tail with awk drops its `set -euo pipefail`: a failed assert then slips past `git commit` (that is how the rehearsal committed harness.py without tasklocal). Run whole scripts, or add `set -e` to the extract.
- The fake_vllm tests bind fixed ports in the e2 block (55580–55582): a live e2 stack anywhere on the host makes them red; rerun after the stack is down.
- The rehearsal scratches are `git clone --shared` of the worktree: their `origin/codex/*` are the LOCAL lane branches.
