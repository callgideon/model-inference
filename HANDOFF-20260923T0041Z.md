# HANDOFF — coordinator pause at the 85% window mark (2026-09-23 20260923T0041Z, rey account → sofia next)

Written by the Fable coordinator of the infrx Marlin backend-first program. Read this first, then the tail of
`research/plan/evidence/coordinator/2026-09-22-session-02.md` (append-only record of every decision, ruling, handback and review verdict) and `research/plan/evidence/coordinator/PROGRESS.md`. Supersedes `HANDOFF-20260922T2215Z.md` and `HANDOFF-20260922T1739Z.md`.

## GOAL
Implement the Marlin2B backend-first endpoint per `research/plan/18-marlin-backend-first.md` and manifest v4 (`research/plan/tasks.json`; tracker counts 30 backend packages). Coordinator (Fable) plans/reviews/merges; Opus 5 implements; every task gets an independent adversarial review at HEAD before a `--no-ff` merge onto `claude/backend-impl`; `main` fast-forwards at verified checkpoints. User authorization is total (operating rules in the session-02 record: log purpose/cost/rollback before irreversible ops; backup before hosted migration; no secrets printed). **Standing instruction from 00:19Z: start NO new tasks; finish the in-flight lanes (handback → review → fix → merge → verification) only.** Progress: 11 of 30 packages done (S1, S2M, I0, E2R, F2R, M2, M3, Q2, W2, G6B, D1R); 12 in progress; 7 remaining (G2, G3, G4U, D4, D5, W4, E4B — all HELD).

## BRANCH STATE
- `claude/backend-impl` head **1e56f80** (+ this handoff's commit) — pushed. Tree collects (2485 tests); every list green except the six Q2 Valkey mutants, which Q3 has already fixed on its branch (`9c043db`, unmerged). A whole-tree `make check` on one head has not run since the F2R-A follow-ups merge (D-port contention; the D suite alone passed 241 with mutants on this head — `.claude-logs/d-list-retry2-7784606.log`).
- `main` = **f9ba5d2** (checkpoint 3 + G6B). Advance it after Q3's Q2-mutant fix merges and a whole-tree check is green.
- Tracker: https://claude.ai/artifact/VuAxJjrT2DDgmZ3RpUEYRa (rey-owned; from another account publish a NEW artifact from `research/plan/evidence/coordinator/progress.html` — regenerate with `python3 research/plan/scripts/progress.py`).
- Merges this window: E3B phase 1 c7d715f (gate exit 3), F2R-A follow-ups 5f7ca02 + ac45e65, D1R f302091. Rulings added: R84 (in-place amendment of unapplied migrations); R83 amended (runner honesty: package asserts undeclared, pristine baseline, setup errors = broken_runner).
- Review results saved: `research/plan/evidence/d/D2-review-ebd621d.json`, `research/plan/evidence/a/A1-review-abbf066.json` (four-/three-lens workflows with two refuters per blocking finding).

## IN FLIGHT AT CUTOFF — every lane is committed and pushed; every agent dies with this process
| lane | worktree / branch | head | state at cutoff | next session action |
|---|---|---|---|---|
| D2 (ad9207e2494073334) | codex-d2 / codex/d2-admission-outbox (base f302091) | **c817d46** | Handed back at ebd621d; review = fix_required (3 blocking: R29 clock mutants M1, rebuild/ack fence OB-1, gc live-job check OB-2; 23 fold-ins). Fix round IN PROGRESS: commits e858ba8 (OB-4..7), 12758ba (OB-3/6), 4fba94e (SEC-4/5), c817d46 (SEC-1, H3, H4) are in; M1, OB-1, OB-2 and the money fold-ins M2–M7, SEC-2/3, H1/H5/H6 NOT yet seen in commits. | Re-dispatch a "finish the D2 fix round" agent from c817d46 with the fix list in the session record (section "D2 four-lens review: fix_required") and `.claude/handoff/wave3/D2.md`; then a short confirmation review (re-run the two review lenses money+outbox on the diff); merge; then merge D2's new commits INTO D3's branch (D3 is stacked on 8728aec). |
| A1 (ae7cc208729b34c1c) | codex-a1 / codex/a1-signup-grant (base 0cc4936) | **c4d7fa3** | Handed back at abbf066; review = fix_required (2 blocking: negative-USD hold case M-1, frozen-wallet acceptance cases M-2; 12 fold-ins). Fix round IN PROGRESS: 41f611f (M-5), 587bd5b (M-6), 351a249 (SEC-3), c4d7fa3 (SEC-5) are in; M-1, M-2, M-3/H4, M-4, SEC-4, SEC-6, H3, H5 NOT yet seen. | Re-dispatch "finish the A1 fix round" from c4d7fa3 with the fix list (session record "A1 three-lens review"); confirmation check (money lens); merge; migration 0015 must be appended to `tests/integration/test_harness.py`'s expected list at merge (A1 request 1); record the R-A1 retention ruling (R85) with the digest-lawfulness sentence. |
| F2P wire-in (ab4181c2a86971b8d) | codex-f2p-wirein / codex/f2p-wirein (base 0cc4936) | **949dae7** (WIP snapshot over 2925d63) | 19 commits: items 1–13 appear done (last: 01/06 links; re-anchored V2 mutants; credit_rate case fix) plus the three D2 requests (conformance key fixture, Work.prompt_tokens, v2 JobStore port siblings) — VERIFY from `git log`. Never handed back. | Re-dispatch "finish + evidence + handback" from 949dae7 (brief `.claude/handoff/wave3/F2P-wirein.md`); review workflow (contracts/fakes/console/runner lenses); merge; mark F2P implemented; then G1R rebases (coordinator merge). |
| Q3 (acb9b478a3c479c66) | codex-q3 / codex/q3-outbox-reconciler (base d18177e) | **9c043db** | 12 commits incl. reconciler, 31 mutants, concurrency/cap-free rebuild cases, AND the six Q2 Valkey mutants fixed under R83 (9c043db). Never handed back; the last commit is the IR-A11 fix. | Re-dispatch "finish + evidence + handback" from 9c043db (brief Q3.md); review (outbox/durability lens on real Valkey + honesty); merge — this also clears the integration head's last red list → whole-tree check → ff main. |
| W3 (a4213b90bc62f041f) | codex-w3 / codex/w3-drain-pin (base d18177e) | **61ada17** (WIP over 03a1143) | 7 commits: items up to (6): engine pin, drain, readiness, PERF-PILOT hook, measurement scripts for the box (`models/marlin2b/measure/`), amd64 config digest recorded. Never handed back. | Re-dispatch "finish + evidence + handback" from 61ada17 (brief W3.md); review; merge. THEN the coordinator runs W3's measurement scripts on the pilot box via SSM (base64-wrapped; log purpose/cost/rollback first) and returns raw outputs to a W3 agent to write the measured values. |
| I2B (a32de8255e7318e71) | codex-i2b / codex/i2b-deployment (base d18177e) | **574a7e0** | 21 commits: I2B.a/b done incl. local rehearsal, adopted W3's worker semantics (no reaper timer; WorkerService reaps in process), IR-A12 done (f73a748), default mutant subset. Never handed back. | Re-dispatch "finish + evidence + handback" from 574a7e0 (brief I2B.md); review (deploy fail-closed lens + secrets audit); merge. The box rollout runbook is executed by the coordinator later (snapshot first). |
| G1R (a662c2fedd858587a) | codex-g1r / codex/g1r-audience-ingress (base 5552c40) | **ae29c50** (WIP over 29195be) | 9 commits: R79 docstring, typed startup death mutant, IR-A13 done (tests/g/ops delegates to the shared runner, 29195be). Core G1R items — check `git log` for which of items 1–5 landed. Never handed back. | Re-dispatch "finish + evidence + handback" from ae29c50 (brief G1R.md); review (API-AUTH/SPLIT-CONTRACT/CREDIT-RATE lenses); merge after the F2P wire-in (rebase by coordinator merge). |
| D3 (a124538e184aed8f0) | codex-d3 / codex/d3-fenced-leases (STACKED on D2's 8728aec) | **fd90ae0** (WIP over 684313e) | 20 commits: leases/heartbeat/cancel/reaper, 64 migration mutants for 0016 + a 0003 guard amendment, claim atomic with its lease under a rebuild + store-clock cases (684313e, from the D2 review). Never handed back. | After D2's fix round merges: merge D2's branch into codex/d3 (coordinator), re-dispatch "finish + evidence + handback" from the merge (brief D3.md), review, merge. |
| I3B (a01df5ac9ce28e923) | codex-i3b / codex/i3b-recovery-observe (base 5552c40) | **87f2555** | 4 commits: I3B.a metrics (Prometheus text, stdlib), I3B.b drills rc01–rc10, I3B.c runbooks; evidence WIP (results being filled). Never handed back. | Re-dispatch "finish evidence + handback" from 87f2555 (brief I3B.md); review; merge. The hosted Supabase backup/restore rehearsal it designed is a coordinator operation and MUST precede any hosted apply of 0006+. |
| M4 (ae21e921bcaef6b26) | codex-m4 / codex/m4-media-optimize (base 5552c40) | **0b9f78b** (WIP over 7bd76e5) | 19 commits: measurement harness, parity oracle, item 3 optimisations with before/after runs on this host (7bd76e5 final full run). Never handed back. | Re-dispatch "finish + evidence + handback" from 0b9f78b (brief M4.md); review (MEDIA-SEC regression + measurement honesty); merge. |
| E3B (a20721aa6846dbaca) | codex-e3b | 5e2417b | MERGED (phase 1). Phase 2 is HELD. | — |
| follow-ups (ae384819978bee787) | codex-f2r-int | ac45e65 | MERGED. | worktree can be removed |

Agent IDs above are for the transcript only; every agent is dead after the restart — re-dispatch with a fresh Agent call (model: opus) using the named brief + "FIRST: `git log --oneline <base>..HEAD` and `git show --stat HEAD`; continue from the WIP; never rebase/reset/amend".

## DETACHED
None running for the coordinator. Lane-internal pytest runs (in codex-{f2p-wirein,w3,i2b,q3,g1r,d3,i3b,m4}) die with the process; their results were feeding agents that die too — re-dispatch re-runs them. Earlier detached result carried forward: the F2R-A shared mutant run against its WIP tree (48d61b9) ended `7 failed, 953 passed` — all seven in tests/t/test_trace_mutants.py and tests/w; the lane diagnosed them as anchor drift caused by its own concurrent item-2/item-3 edits (the mutants targeted lines that had moved), re-anchored/retargeted/retired them, and the final run at d7790c6 was `967 passed`; the coordinator record and the lane evidence carry both numbers. Not a crash/durability defect in the spool: the durability semantics were re-proven by the F2R-A review (spool oracle bites; trace accounting base; R81).

## WORKFLOWS
Two review workflows completed and their results are saved (D2 wf_06ba3fc3-975, A1 wf_9d5b5b5f-c8c; scripts under the session's workflows/scripts/). Reuse the script shape for the remaining reviews (per-lens reviewers on distinct harness ports 5567x–5571x + two refuters per blocking finding).

## UNCOMMITTED
Nothing in the integration worktree after this commit. Every lane worktree WIP-committed and pushed (heads in the table). `.claude-logs/` is untracked and disposable.

## NEXT (in order)
1. `git fetch`; confirm `claude/backend-impl` = this handoff's commit; read the session-02 record from "D2 four-lens review" onward.
2. Re-dispatch the ten finish/handback agents (table above) in parallel — they are independent worktrees. Priority for merge order: Q3 (clears the last red list) → D2 fix round → A1 fix round → F2P wire-in → G1R → D3 (after D2 merge) → W3 → I2B → I3B → M4.
3. Review each handback with a lens workflow; merge on green with the merged-tree suites (watch for merge interactions: contracts changes from the wire-in vs G1R/D3; W3's worker settings vs I2B's units; D2/A1/D3 all add migrations 0010–0016 and tests/d files — merge D2 → A1 → D3 in that order and run tests/d with all mutants after each).
4. Whole-tree `make check` on one head when the D port is free (the retry-loop pattern in `.claude-logs/`) → ff `main`.
5. Coordinator operations (only after the lanes merge; log purpose/cost/rollback first): W3 measurements on the box (SSM, base64-wrapped scripts from `models/marlin2b/measure/`); hosted Supabase backup rehearsal (I3B runbook) → then hosted apply of 0006–0016 via I2B's fail-closed migrate script; box rollout per I2B's runbook (snapshot first).
6. Pending user inputs: P-01 rate card; P-19 g6e/L40S price row; `HF_TOKEN` for the registry-oid equality; P-05 hosted onboarding config (signup disabled, autoconfirm off).
7. Still HELD (no new tasks until the user lifts the instruction): G2, G3, G4U, D4, D5, W4, E4B, E3B phase 2.

## TRAPS (new; the earlier handoffs' traps still apply)
- Ten lanes touched tests/d and migrations concurrently: numbering is D2 0010–0014, A1 0015, D3 0016+; D2 amended 0003/0006/0008/0009 in place (R84) and D3 amended a 0003 guard — merge in D2 → A1 → D3 order and expect the harness's expected-migration list in tests/integration/test_harness.py to need every new file.
- D3 is stacked on D2's PRE-review head 8728aec; D2's fix round adds commits on codex/d2 — merge codex/d2 into codex/d3 before D3's finish agent runs, or its tests will diverge.
- Review workflows must give each reviewer its own harness port (patch tests/d/pgharness.py PORT/CONTAINER in the scratch) — the shared port 55432 serialises everything; the A1 security lens lost its live checks to contention.
- A workflow result file wraps the JSON in {"summary","agentCount","logs","result"} — parse `result` (see the python in the session transcript), do not string-search for the first brace.
- Lanes numbering their own rulings must be renumbered at merge (R63–R67 → R79–R83 happened); next free ruling number is **R85** (reserve for A1's retention ruling R-A1).
- The Q2 Valkey mutant fix lives on Q3's branch (9c043db); until Q3 merges, `make api-mutants` on the integration head has six honest-kill failures under R83 — expected, not a regression.
