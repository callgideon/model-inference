# Handoff — 2026-09-23T03:57:31Z (written at the dhruv→next account switch)

Supersedes `HANDOFF-20260923T0339Z.md`. **Every agent and workflow of the dhruv window is dead**; nothing in flight completes on its own except the detached Q-list run below. The sofia window died at ~01:55Z; everything dispatched there was re-dispatched here (session record §"Session switch sofia → dhruv").

Supersedes `HANDOFF-20260923T0041Z.md`. Same rules: WIP-commit and push lane work as you go; detach anything > 10 min; never touch another lane's containers; secrets never printed/logged/committed; AWS CLI via `env -u AWS_ACCESS_KEY_ID -u AWS_SECRET_ACCESS_KEY -u AWS_SESSION_TOKEN aws …`. Standing user instruction: **no new tasks** — finish in-flight lanes only (handback → review → fix → merge → merged-tree verification). Held: G2, G3, G4U, D4, D5, W4, E4B, E3B phase 2, any hosted apply or box rollout.

## GOAL
Marlin backend-first program (`research/plan/18-marlin-backend-first.md`, manifest v4 `tasks.json`): 11/30 packages merged and reviewed; 12 in progress; 7 held. Session record (append-only): `research/plan/evidence/coordinator/2026-09-22-session-02.md`. Tracker: `progress.py` → PROGRESS.md/progress.html; artifact https://claude.ai/artifact/6b9PkyxvkSmdZ4y4p3GAZe (sofia account).

## BRANCH STATE (verified local == origin at 2026-09-23T01:49:09Z)
Integration `claude/backend-impl` = a7f47b1 (this worktree, `infrx-impl`); `main` = f9ba5d2. Base of program ec6c548.

| worktree | branch | local | origin | | |
|---|---|---|---|---|---|
| codex-d2 | `codex/d2-admission-outbox` | c08364c | 0ce4ce6 | UNPUSHED | 3 dirty |
| codex-a1 | `codex/a1-signup-grant` | 47b0382 | 47b0382 | pushed | 0 dirty |
| codex-f2p-wirein | `codex/f2p-wirein` | ff8bec4 | ff8bec4 | pushed | 0 dirty |
| codex-q3 | `codex/q3-outbox-reconciler` | 152de7e | 152de7e | pushed | 0 dirty |
| codex-w3 | `codex/w3-drain-pin` | a0eeaf4 | a0eeaf4 | pushed | 0 dirty |
| codex-i2b | `codex/i2b-deployment` | 5d68e50 | 5d68e50 | pushed | 0 dirty |
| codex-g1r | `codex/g1r-audience-ingress` | d50afa5 | d50afa5 | pushed | 5 dirty |
| codex-d3 | `codex/d3-fenced-leases` | fd90ae0 | fd90ae0 | pushed | 1 dirty |
| codex-i3b | `codex/i3b-recovery-observe` | 48d3bb2 | 48d3bb2 | pushed | 0 dirty |
| codex-m4 | `codex/m4-media-optimize` | 2e323be | 2e323be | pushed | 0 dirty |

"dirty" = a lane agent mid-commit; the agent commits its own work. If a lane shows UNPUSHED, push it (`git -C <wt> push origin <branch>`) before anything else.

## DONE THIS WINDOW
Audit gaps 1–3 resolved. Handbacks recorded and reviews launched for I3B (48d3bb2), I2B (5d68e50), Q3 (152de7e), G1R (d50afa5), D2 fix round (0ce4ce6), M4 (2e323be), W3 (a0eeaf4), A1 fix round (47b0382). Results so far: **G1R PASS** (0 blocking, 17 nonblocking → fold-in round running; JSON `evidence/g/G1R-review-d50afa5.json`); **D2 confirmation fix_required** (MC-1 CREDIT provider_dev refusal unkillable; OB-1b late ack after reopen strands a job; JSON `evidence/d/D2-confirm-0ce4ce6.json`) → D2 round 2 running (MC-1 already committed as c0028b9).

## REVIEW STATE (all review workflows have returned; every reviewer/refuter now runs on Opus 5.5 per the user's 2026-09-23 rule)
| task | latest review | verdict | JSON |
|---|---|---|---|
| G1R | wf_a62153d6-049 | pass → MERGED b609cc9 + 67e0320 | evidence/g/G1R-review-d50afa5.json |
| M4 | wf_564484b4-b63 | pass → MERGED 4185f4d | evidence/m/M4-review-2e323be.json |
| I3B | wf_28336d9f-463 (confirm) | pass → MERGED 8e01fe0 | evidence/i/I3B-confirm-58d5b4f.json |
| D2 | wf_4be1fe7a-e81 (round-2 reconfirm) | pass; round 3 (4 nonblocking) running | evidence/d/D2-confirm-6a8cc8d.json |
| Q3 | wf_fcd2205d-f76 (confirm) | pass; fold-in done 6fa13bf → MERGED 25de7ab + 88a48d7 | evidence/q/Q3-confirm-1ba884b.json |
| W3 | wf_c03ef5ea-7cf (confirm) | pass; fold-in done 4fc1081; measured values c81ef36; MERGE-READY with I2B | evidence/w/W3-confirm-d80c04d.json |
| A1 | wf_17bc7763-582 (round-3 reconfirm) | round 3 dc3cb9c closed RV2-1/RV2-2; fix_required RV3-1 (delete/move race case + mutant, or narrow docstring); RV3-2/3 wording → ROUND 4 needed | evidence/a/A1-confirm-dc3cb9c.json |
| I2B | wf_e0d04fc2-7fb (confirm) | fix_required RB-1 (revert order when the engine is down), RB-2 (positive-int grammar); round 2 running | evidence/i/I2B-confirm-0653c8a.json |
| D3 | wf_a500c185-bb7 | fix_required FE-1/MY-1 (CREDIT held_unknown untested); fix round + D2 merge running | evidence/d/D3-review-b4b1ec7.json |
| F2P wire-in | wf_f57f49c1-e52 | fix_required M-1, M-2, M-3/CFG-1; fix round running | evidence/f/F2P-wirein-review-e307084.json |

## LANE STATE AT THE SWITCH (all pushed; `local == origin` verified) — re-dispatch a FRESH agent per lane from its evidence + review JSON
| lane | head | state / next |
|---|---|---|
| D2 codex/d2-admission-outbox | a7c7a79 (+ WIP snapshot if the table below shows one) | round 3: 4 of 4 code items committed (735f6d8 OB-1b residual, 606480b OB-8, 8b44d4f MC-2b, a7c7a79 MC-3b); both-image sweep + addendum + handback NOT done → re-dispatch to finish (sweep on port 55433, INFRX_D_TASK=d2), quick coordinator check, then MERGE |
| D3 codex/d3-fenced-leases | 3df881c (+ WIP) | fix round in progress: was merging origin/codex/d2 (a7c7a79), OB-5b reaper rule, FE-1/MY-1 + 12 nonblocking (evidence/d/D3-review-b4b1ec7.json); check `git log`/diff for what landed; re-dispatch; then confirm (Opus) and merge after D2 |
| A1 codex/a1-signup-grant | dc3cb9c | ROUND 4: RV3-1 (third `_behind` race deleting the owner's membership during the claim → 23514; mutant narrowing the guard lock to array[new.org_id]), RV3-2 (Limits: two same-user claims serialise on the identity claim; the 40P01 surface is a multi-org membership writer; add 40P01 as retry to the A2 fixture table), RV3-3 (record the join-during-retirement race as an R-A1 limit); then a one-lens Opus reconfirm; merge after D2 (append 0015 to tests/integration/test_harness.py; Makefile += tests/d/test_signup.py; R85 = R-A1) |
| F2P wire-in codex/f2p-wirein | 67e2c4b (+ WIP) | fix round was IN PROGRESS when killed (M-1, M-2, M-3/CFG-1 blocking + 13 nonblocking; evidence/f/F2P-wirein-review-e307084.json); inspect `git log 0cc4936..HEAD` and the WIP diff; re-dispatch to finish; confirm (Opus); merge (delete tests/contracts/v2/mutants_v2.py's superseded dies_by on conflict; take D2's _first_line hunk; apply G1R's C1 hunk) |
| I2B codex/i2b-deployment | 7c422ae (+ WIP) | round 2 in progress (RB-1 engine restart inside rollback.sh behind ENGINE=restart; RB-2 positive-int grammar; 12 nonblocking; evidence/i/I2B-confirm-0653c8a.json); re-dispatch; confirm (Opus); merge TOGETHER with W3 |
| W3 codex/w3-drain-pin | c81ef36 | MERGE-READY (review + confirm pass; fold-in; measured box values). Merge in the same step as I2B (FC-1: the base vLLM unit's --max-num-seqs 32 vs W3's serve.sh); Makefile += tests/w/test_w3_mutants.py; I3B's --max-age 180 on the alert timer at I2B's merge |

## DETACHED (verified at 03:59Z; each runs under its own session id, so it survives this process)
- Q lists on the integration tree: pid 3057767/3057769, `.claude-logs/q3-merged-88a48d7.log` (ends with EXIT=).
- F2P wire-in fix-round Python mutant list (the killed agent's last run): pid 2874620 in codex-f2p-wirein, log `/tmp/claude-1000/f2p-wirein/r2-mutants.log` (at 53% when saved). Its committed items are on origin at 67e2c4b; anything it changed after that is in the worktree (WIP-snapshotted).
- D3 fix-round Supabase-image sweep: pid 2959749 in codex-d3, logs `/tmp/claude-1000/d3-fix/plain.log` (finished 03:51Z) and `/tmp/claude-1000/d3-fix/supabase.log` (running).
- D2 round-3 sweeps finished: `/tmp/claude-1000/d2-round3/r8_plain.log`, `r8_supabase.log` (03:57Z) — read the tails; the addendum + handback were never written (agent died) → the next D2 agent quotes them.
- Docker left running on purpose by lanes: infrx-d2-postgres-supabase, infrx-d3-postgres-supabase (their sweeps); remove after the logs end. gideon-migration-order-test-* is not ours.
Q lists on the merged tree: pid 3057767, `.claude-logs/q3-merged-88a48d7.log` (Q1 + Q2 Valkey + Q3 lists; ends with EXIT=). Read the tail first thing; if red, it is the first coordinator item. Older merged-tree logs all green: g1r 697, m4 242, i3b 78/77+control.

## NEXT (in order)
1. Process each workflow result as it lands: parse `result`; save JSON under `research/plan/evidence/<track>/<TASK>-review-<head>.json`; append to the session record + progress-state; **pass** → merge (`--no-ff` onto claude/backend-impl, apply that task's integration requests and the Makefile list additions at merge, run the merged tree's relevant suites, record, push); **fix_required** → SendMessage the lane agent the confirmed blocking + nonblocking items, then a confirmation workflow on the fix head (2 lenses + refuters, own containers/ports).
2. Merge order (G1R, M4, I3B, Q3 done; 15/30): Q3 (after fold-in; Makefile += tests/q/test_reconcile_mutants.py) → D2 (after round 3 handback + a quick check) → A1 (after round 3 + reconfirm; append 0015 to tests/integration/test_harness.py; Makefile += tests/d/test_signup.py; R85 = R-A1) → F2P wire-in (after fix round + reconfirm; resolve tests/contracts/v2/mutants_v2.py by deleting the branch's superseded dies_by; take D2's _first_line hunk; apply G1R's C1 hunk) → D3 (after fix round + confirm; already merges D2) → W3 + I2B together (after I2B round 2 + reconfirm; W3 request 12 / I2B FC-1; Makefile += tests/w/test_w3_mutants.py; I3B's --max-age 180 on the alert timer). Old order text: Q3 (after fix round + confirmation; clears the six Q2 Valkey mutants red) → D2 (after round 2 + re-confirmation) → A1 (after closure pass; append 0015 to `tests/integration/test_harness.py`; record R85 = R-A1 retention; next free ruling number R85) → F2P wire-in (apply G1R's C1 hunk to its rate-card test at merge) → D3 (merge codex/d2 into it first; re-run tests/d both images) → W3 → I2B (units per FC-1 in the same deploy as W3's serve.sh) → I3B → M4. New §10 ruling for dr17/auth_context/requested-name still to write (G1R request 10).
3. Makefile `api-mutants` additions at the respective merges: `tests/q/test_reconcile_mutants.py` (Q3), `tests/w/test_w3_mutants.py` (W3), G list is already listed (now 186 mutants), plus whatever I2B/I3B/M4/D3 declare.
4. W3 box measurements: inventory + capability DONE (evidence/w/box/, SSM ids in the session record); the concurrency sweep is HELD (needs a checkout + corpus on the box; user: no new work). Previously: the W3 pin lens reports the three scripts safe: log purpose/cost/rollback in the session record first; run `models/marlin2b/measure/inventory.sh` (read-only) then `capability.sh` then `concurrency.sh` via SSM on i-0e8449a4ffca29bab with the evidence's wrapper; HF_TOKEN never printed (registry-oid check stays ⚠️ unless the user supplies the token; the classifier refused using the box's stored credential — do not retry). Then SendMessage W3's agent the raw outputs so it writes the measured values.
5. I2B rollout hazard: the deployed vLLM unit passes `--max-num-seqs 32`, which W3's serve.sh refuses (exit 2) — I2B's units must change in the same deploy (see W3 requests 3–5). No box rollout this window.
6. Whole-tree `make check` on one head when the D port is free (retry loop on HarnessBusy) → ff `main`.
7. Refresh this handoff at every checkpoint; at ~90% of the dhruv window stop dispatching, commit, push, idle. User rules this window: finish in-progress work only, merge and save, no new lanes, keep the tracker updated (dhruv artifact https://claude.ai/artifact/LqEn7ZiMjYgRXZdCUtnL6o), reviewers on Opus 5.5.

## TRAPS
- The Caddy admin API finding (I2B S1) is a real edge hazard on the box today (pre-existing); no rollout until I2B's fix lands.
- Verify pushed per lane against `git ls-remote` (never assume from a refspec push). Lane agents never push; the coordinator pushes.
- Reviewers run on their own containers/ports (patch pgharness in scratch; D2's harness reads INFRX_D_TASK). Port 55432 is shared (HarnessBusy = retry, not a failure). A whole-tree run failing 69 api / 83 D tests = D-port contention.
- Workflow reviewers' long runs can be cut off by the structured-output deadline: read the fabrication_check before trusting a "pass"; the lane's own sweep tails are the full-suite evidence.
- Lane-numbered rulings collide: renumber at merge (next free R85).
- tasks.json status changes need `validate_plan.py --write-ledger`.
- G1R's config.py hunk overlaps the wire-in's only textually (merge-file clean); the wire-in's new contracts case needs the C1 hunk or it goes red in pilot.
- OB-1b's SQL change (ack requires the live claim) must be mirrored in Q3's tests/q/outboxfake.py at Q3/D2 merge.
- Hugging Face gated repo returns `*`×64 for .lfs.oid unauthenticated; registry equality needs the user's HF_TOKEN.
- `git stash` is shared across worktrees: never use it.
