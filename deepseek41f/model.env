# Metadata for the deepseek41f experiment. Data only — logic lives in common/.
EXP=deepseek41f
HF_REPO=deepseek-ai/DeepSeek-V4.1-Flash
# S3 mirror prefix. Differs from EXP for historical reasons: the mirror was
# populated before these directory names existed. Do not "fix" it without
# moving the objects.
S3_DIR=deepseek-v41
SIZE_GB=511
GATED=no
NOTES="552B MoE, FP8 weights + FP4 experts, 1M ctx, DSpark speculative"
