# HANDOFF — coordinator pause at the 85% window mark (2026-09-22 20260922T2215Z)

Written by the Fable coordinator of the infrx Marlin backend-first program (rey account window). Read this first, then the tail of `research/plan/evidence/coordinator/2026-09-22-session-02.md` (append-only record of every decision, ruling and handback) and `research/plan/evidence/coordinator/PROGRESS.md`. The previous handoff `HANDOFF-20260922T1739Z.md` is superseded by this file.

## GOAL
Implement the Marlin2B backend-first endpoint per `research/plan/18-marlin-backend-first.md` and manifest v4 (`research/plan/tasks.json`, closure of E4B = 37 tasks; tracker counts 30 backend packages). Coordinator (Fable) plans/reviews/merges; Opus 5 implements; every task gets an independent adversarial review at HEAD before a `--no-ff` merge onto `claude/backend-impl`; `main` fast-forwards at verified checkpoints. User authorization is total (see the session-02 record's operating rules: log purpose/cost/rollback before irreversible ops; backup before hosted migration; no secrets printed). Progress: 11 of 30 packages done (S1, S2M, I0, E2R, F2R, M2, M3, Q2, W2, G6B, D1R); B0 complete except the F2P wire-in.

## BRANCH STATE
- `claude/backend-impl` head **5f7ca02** (this handoff's commit follows it) — pushed. Tree collects (2485 tests) after the F2R-A follow-ups merge `5f7ca02`; contracts/w/m/i/g/q suites 1981 passed on the merged tree; a whole-tree `make check` on this head has NOT been run (D-port contention; see TRAPS).
- `main` = **f9ba5d2** (checkpoint 3 + G6B). Next ff of main: after a green whole-tree check on the current head (schedule it when no lane holds D port 55432).
- Tracker: https://claude.ai/artifact/VuAxJjrT2DDgmZ3RpUEYRa (rey account; scratchpad path `marlin-backend-tracker-rey.html`; `research/plan/scripts/progress.py` regenerates). The old dhruv-owned URL cannot be updated from other accounts.
- Merges this window (all reviewed): M2 8156f78, F2R-B 2bfb0c4, E1B(software) 164e43e, I0 0ac073e, F2P additive 9faaa57, Q2 e86b1c0, E2R 9cc3be1, W2 32cc0cb, M3 e2188f3, G6B ed6da07, F2R-A e1a33d1 (+ follow-ups 5f7ca02), D1R f302091, E3B phase 1 c7d715f (gate NOT passed, exit 3, 22 pending).
- Rulings added: R63 (task Valkey ports), R64–R78 (v2), R79–R83 (F2R-A; R83 amended: runner honesty), R61 amended (profile path segment), R62.

## LANES AT CUTOFF
| lane | worktree / branch | head | state |
|---|---|---|---|
| D2 | codex-d2 / codex/d2-admission-outbox (base f302091) | **8728aec** | ALL items committed (0011–0014 migrations: admission, outbox, expiry/GC, is_live/put_result/prompt_tokens; 0010 M3 tables; D1R follow-ups; 72 migration + 19 code mutants; conformance partition on the real store; E3B dr01/dr02 on PostgreSQL). Agent was in final verification (pytest processes running in its worktree) and never handed back. **Next: re-dispatch a "finish verification + evidence + handback" agent from 8728aec** (brief `.claude/handoff/wave3/D2.md`), then independent review on both images, then merge. |
| F2R-A follow-ups | codex-f2r-int / codex/f2r-int-followups | 85cbc7d | MERGED as 5f7ca02 before the lane's final `make api-mutants` finished; its log is in `codex-f2r-int/apps/infrx-api/` (untracked). Next session: read that log; if the D list failed only on HarnessBusy, rerun the D list alone. |
| E3B | codex-e3b / codex/e3b-backend-gate | 5e2417b | phase 1 MERGED; phase 2 (real journeys) after G1R/G2/G3/G4U/D2–D5/W3/Q3. |
| F2P wire-in | not started | — | **READY TO DISPATCH** from the integration head: brief `.claude/handoff/wave3/F2P-wirein.md` (fill the BASE line). Check first which of its items the follow-ups already did (IR-A3 parity enums, IR-A10 runner delegation are done). |
| G1R | not started | — | brief `.claude/handoff/wave3/G1R.md`; dispatch after the wire-in merges (fill BASE). |
| Others merged | codex-{m2,m3,q2,w2,g6b,d1r,e1b,i0,e2r,s2m,f2r,f2r-b,f2p,s1fix} | — | merged; branches pushed; worktrees can be removed (`git worktree remove`) when convenient. |

## IN FLIGHT AT CUTOFF (die with the process)
- D2 agent (ad9207e2494073334): verification runs in codex-d2; work is committed (see above).
- Follow-ups agent (ae384819978bee787): final mutant run only; work merged.
- No reviewers running. No coordinator detached runs pending.

## NEXT (in order)
1. `git fetch`; confirm `claude/backend-impl` = this handoff's commit; read the session-02 record tail.
2. Dispatch the **F2P wire-in** (brief above) and a **D2 finish/handback** agent (from 8728aec) in parallel — no file overlap (wire-in: contracts/fakes/config/Makefile/console; D2: state/tests/d/migrations).
3. When D2 hands back: independent review on both images (money + outbox mutants, race probes) → merge → dispatch **D3** (fenced leases, recovery, cancellation) and **Q3** (outbox drain/ack, PG rebuild) — briefs to write from handoffs/D-durable-state.md §D3 and the Q track handoff; D3 also owns the `JobStore.recover()` reaper W2/E3B asked for.
4. When the wire-in merges: dispatch **G1R** (brief above); mark F2P implemented; then G2/G3/G4U after G1R; A1 after D1R (already possible — A1 brief to write from 18/11: signup entitlement + idempotent backfill on D1R's `grant_signup_credit`, verified_user, retention policy).
5. Whole-tree `make check` on one head when the D port is free (retry loop pattern in `.claude-logs/` scripts) → ff `main`.
6. Hosted apply of 0006–0009 (+0010–0014 after D2): ONLY after I3B's backup/restore rehearsal; snapshot `snap-08732d3ac6376e850` exists for the box, but Supabase has no backup yet — take one first (operating rules).
7. Pending user inputs: P-01 rate card approval; P-19 sourced g6e/L40S price row; `HF_TOKEN` for the registry-oid equality (W3); the classifier denied using the box's stored HF credential (do not retry that path).

## TRAPS (new this window; older ones in HANDOFF-20260922T1739Z.md still apply)
- The D harness serialises on host port 55432 across ALL checkouts. A whole-tree check while any lane runs tests/d fails 69 api tests + 83 D mutants with HarnessBusy — that is the designed refusal, not code. Run tests/d / the D list with a retry loop (`.claude-logs/d-mutants-retry.log` pattern) or when no lane holds it.
- Never pre-add a mutant list to the Makefile before its file is merged (api-mutants collects nothing → exit 4).
- Merge interactions seen: W2's adapter requires M2's `local_uri` resolver (M seam tests fixed 71d8de1); F2R-A's `{visible, raw}` broke the integration HTTP engine client (fixed on the merge); D1R's migrations changed the harness's expected migration set; F2R-A's shared runner renamed `allowed_errors` → `dies_by` (W2 list). Expect similar with the wire-in (fakes/ports) — run tests/contracts + every track suite after each merge.
- Subagent forced handbacks can leave containers labelled to a dead scratch checkout (e.g. `infrx-d1-postgres-supabase`); remove them only after confirming the label points at a scratch path and no process remains.
- Rulings are numbered on the integration branch; lanes that number their own rulings must be renumbered at merge (F2R-A's R63–R67 → R79–R83 happened; fix lane-authored references).
- `git merge` conflicts in `08-contracts-v1-encoding.md` are always the trailing verification log: keep both sides.
- The classifier blocks credential exploration on the pilot box (HF token lookup) — ask the user for `HF_TOKEN` instead.
