# PREP-WORKER (I2B-R5): the product preparation loop — brief (coordinator, 2026-09-24T01:45Z)

**Gap (found by the I2B-R4 worker lane, verified by the coordinator):** no product process prepares a job. `git grep` shows `JobStore.prepared(...)` called only from `infrx/contracts/conformance/` and the E3B pilot box's emulation (`pilotbox.py` at b9529d1: `await jobs.prepared(lease, refs, prompt_tokens=PROMPT_TOKENS)`, a constant, "no tokenizer here"). Under the cutover every admission therefore sits in `preparing` forever: the gateway admits and relays, `python -m infrx.worker` (I2B-R4) executes only `queued` jobs. **This blocks the pilot.**

**Deliverable:** a second loop in `python -m infrx.worker` (same process, the W3 `WorkerService` pattern; new setting `PREPARATION_CONCURRENCY`, default 2, a 08 §5.1 row; `preflight` TUNABLE) that
1. consumes `prepare_dispatch` outbox events (D2's dispatch outbox; `infrx/state/outbox.py` `OutboxRelay`, the Valkey index the gateway/worker already share via `pilot.valkey_index`), `claim_preparation(job_id, worker_id)` (fenced lease, R46), reads the job's request + media refs through the lease (`load_work` is inference-only: find the preparation read path or define it inside D's contract with a conformance case), runs M's `MediaPreparation`/media-store `prepare(job_id, profile)` over `pilot.object_store(settings)` + `ProcessingCache(PROCESSING_CACHE_DIR)` (shared with the gateway: same directory, content-hash lookup), then `jobs.prepared(lease, refs, prompt_tokens=N)` (`infrx/state/jobstore.py` ~138; the port in `contracts/ports.py:111` lacks the kwarg — reconcile the contract: a ruling candidate, F-owned text, coordinator numbers it);
2. computes **prompt_tokens exactly as the engine counts them**: the settlement (D5) charges from the engine's usage; the hold uses `prepared_prompt_tokens`; the pilot box's constant worked only because its fake engine echoes it. Preferred: vLLM's `POST /tokenize` on `UPSTREAM` with the prepared request (chat template + the video/image placeholders as the served model expands them; W3's `models/marlin2b/tokens.py` documents Marlin's video token budget — use it to CHECK the engine's answer, not to replace it). The fake engine `tests/integration/fake_vllm.py` gains `/tokenize` (E-owned file; you may change it — update the E3B mutant anchors you touch). State the exact rule in the evidence (what happens when `/tokenize` is unavailable: the job fails preparation typed `dependency_unavailable`, lease released, the outbox relay retries; never a guess);
3. failure paths: fetch/stage errors → typed `preparation_failed` per R29 with the deadline; lease lapse → the relay's requeue (R93); a media ref another org owns → `not_found`; the drain on SIGTERM releases held preparation leases (no job stuck `preparing` with a dead lease);
4. **acceptance:** the E3B pilot box no longer emulates preparation — delete `PROMPT_TOKENS` and the box's `prepared(...)` call; all nine journey cells, the dataset resume, dr11, rc03 and rc08b pass on the mounted gateway + the REAL `python -m infrx.worker` (both loops) on namespace **e4b**; a layer-3 gate run (`--only-suites --canary`) exit 0 or 3 with your report quoted; `tests/w` + your new mutant list `tests/w/test_prep_worker_mutants.py` on the Makefile `api-mutants` line (insert after `tests/w/test_worker_main_mutants.py`); contracts conformance for the loop against F's fakes and PostgreSQL (private D task **d5**: `INFRX_D_TASK=d5 INFRX_D2_VALKEY_PORT=55467 INFRX_D2_VALKEY_CONTAINER=infrx-d5-valkey INFRX_Q_VALKEY_PORT=55498`; MinIO `infrx-prepw-minio` on **55781** with the E2 literals, `INFRX_M_S3_ENDPOINT=http://127.0.0.1:55781 INFRX_M_S3_LOCAL_CREDS=1`, AWS_* unset); every claim in the evidence has a mutant or a probe.

**Base:** branch `codex/prep-worker` @ 01103c9 = the integration head 865d352 + the I2B-R4 worker branch e540963 (merged with the coordinator's step-8b resolutions). Worktree `.claude/worktrees/codex-prepw` (venv built). Read first: `research/plan/evidence/i/I2B-R4-fb8babb.md` (the worker), `apps/infrx-api/infrx/worker/__main__.py`, `tests/integration/backend/pilotbox.py`, `research/plan/evidence/e/E3B-e57b820.md` (phase 3), D2/D5 evidence for the preparation lease and outbox (`research/plan/evidence/d/`), 08 §10 rulings R29, R46, R52, R93, R94 and §5.1. Evidence file: `research/plan/evidence/w/PREP-WORKER-<impl sha>.md` (per-item commit → case → mutant kill line; runs quoted; limits; integration requests; ruling candidates).

**Rules:** never push (the coordinator pushes), never rebase/reset/amend/bare-stash; WIP-commit as you go; own containers/ports only (d5, Q 55498, MinIO 55781, namespace e4b; never 55432–55435, d3/d4/d6, e2, e3b2, the pilot box, AWS, hosted Supabase, secrets); detach runs over 10 minutes and poll; TMPDIR outside the checkout; hold interim reports and hand back ONCE (SubagentHandback) with the SHA, the run tails and the integration requests. One review round follows: every claim must be killable.
