# Handoff — 2026-09-24T02:15Z (sofia window; unit + 3b merged; worker under verification; PREP-WORKER lane dispatched — the pilot's blocker)

Supersedes `HANDOFF-20260924T0105Z.md`. Since it: cutover fix round 141e169 verifier PASS → **step 3b merged 865d352**; interim checkpoint on 92ea6c0: **api-test 3605 green** (S3 cases ran), layer 0 green with TMPDIR outside the checkout (checkpoint2.sh fixed: 5f3e01a), mutant lists still running on the scratch; **I2B-R4 worker handed back e540963** (impl fb8babb; layer-3 backend 144/0/0, no PENDING; rehearsal PASSED) → **verifier wf_c8d406f4-b7b (task w2bxfld8s) running**; step 8b's two conflict resolutions scripted and rehearsed (Makefile list line; 08 §5.1 row + log line). **PRODUCT GAP (verified): no product process prepares a job** — `JobStore.prepared()` is called only by contracts/conformance and the pilot box's emulation (constant PROMPT_TOKENS); every admission stays `preparing` under the cutover → **PREP-WORKER (I2B-R5) lane dispatched** (Opus, agent a8196f41c1c250fa3; brief `.claude/handoff/wave3/PREP-WORKER.md`; branch `codex/prep-worker` @ 01103c9 = 865d352 + e540963 with the 8b resolutions; worktree codex-prepw; ports d5/55467/55498, MinIO 55781, namespace e4b). SSM `/model-inference/pg_journal_url` created (P4 closed). Integration head **5eeb7ca** (pushed); `main` = 01a7dfc (remote; local ref stale f9ba5d2). Manifest 29/30 in tasks.json (tracker shows 28/30: E4B box half + the worker entry point open). Rulings next free **R104**.

Rules unchanged (1125Z; ONE fix round per lane; reviews on committed heads; box via SSM with W3's helpers; secrets never in text; AWS CLI via `env -u AWS_ACCESS_KEY_ID -u AWS_SECRET_ACCESS_KEY -u AWS_SESSION_TOKEN aws --region us-east-1`). Session record `research/plan/evidence/coordinator/2026-09-22-session-02.md` (append-only, complete; NOTE its timestamps between 00:57Z and 01:45Z run up to 30 min behind the clock — the commit times are authoritative); tracker `python3 research/plan/scripts/progress.py` → publish `scratchpad/marlin-backend-tracker-sofia.html` with url https://claude.ai/artifact/Rk84qe3Yuzr2d357GoeRvZ (v30).

## NEXT (in order)
1. **Worker verifier** (w2bxfld8s) → save JSON `evidence/i/I2B-R4-verify-e540963.json`; pass → `WORKER_VERIFY="pass at e540963 (…)" bash research/plan/evidence/coordinator/merge-plan/8b-i2b-r4-worker.sh` on the integration worktree (clean tree; it resolves the two conflicts and runs tests/i) → push → tasks.json wording (coordinator edit, textual): E4B disposition += "the pilot cannot serve until the preparation loop (PREP-WORKER) lands"; I2B-R4 closed in progress-state. fix_required → one fix round on codex-worker (the agent a0bdc78d222ba64c1 can be resumed) → verifier → 8b.
2. **Checkpoint 2** after 8b on the worktree, clean tree, e3b2 free (nobody holds it now): `setsid nohup bash research/plan/evidence/coordinator/merge-plan/checkpoint2.sh > .claude-logs/checkpoint2-summary.log 2>&1 &` (TMPDIR defaults OUTSIDE the repo now; d3 ports; MinIO 55694). Then `make bench-test` in the FOREGROUND. Expect api-test green, layer 0 exit 0 with NO pending, lists all killed, W3 sigint killed, layer 3 exit 0. Then ff `main`: `git fetch origin main && git branch -f main origin/main && git push origin claude/backend-impl:main`; tracker; handoff.
3. **PREP-WORKER lane** → push → one adversarial review (Opus lenses: preparation-loop correctness on PostgreSQL + E3B journey/honesty; two refuters per blocking finding) → one fix round → single verifier → merge (`--no-ff`; Makefile list line after test_worker_main_mutants.py; 08 §5.1 PREPARATION_CONCURRENCY row; ruling candidates numbered R104+) → a focused re-gate (tests/w, tests/i, contracts, layer 0, layer 3 on e3b2) → ff `main` again.
4. **Box phase 2** (after step 2's `main`): the rollout mechanics per `infra/runbooks/rollout.md` (W1 maintenance edge → drain → W6 backup+restore check → W7 migrate 0003–0018 → W10 install RELEASE=<full sha> with S3_MEDIA_BUCKET=llm-bootcamp-641134885443, MAX_VIDEO_SECONDS=82, ENGINE_MAX_NUM_SEQS=8, WORKER_CONCURRENCY=8 → readiness incl. the worker's 8002 /metrics gauge → tests/m/test_s3.py on the real bucket → edge live) can run BEFORE the preparation loop lands (it de-risks the first hosted migration); smoke, E1B L2–L7 and `certify.py --box` need step 3's release (second `install.sh` with the new RELEASE). Log purpose/cost/rollback before each irreversible step.

## RUNS / CONTAINERS (02:15Z)
Workflows: worker verifier w2bxfld8s (d6 55437/55471/55472, MinIO 55724, namespace e2). Lanes: PREP-WORKER (d5, Q 55498, MinIO 55781, namespace e4b). Coordinator: interim checkpoint on `scratchpad/ckpt-interim` (d3 55434/55464, Q 55490, infrx-ckpt2-minio 55694; mutant-lists stage). Stale: infrx-d2-postgres; gideon-* not ours. Disposable scratches: mergeu, mergeu2, mergeu3.

## TRAPS (new)
- A temp dir INSIDE a checkout breaks E4B's unknown-tree probe (4 reds in layer 0 + the E4B list's pristine baseline): TMPDIR outside the repo, always.
- `.claude/` is gitignored: lane briefs need `git add -f`.
- The scratch `git clone --shared` of the worktree: `git reset --hard claude/backend-impl` inside it resets to the SCRATCH's branch (moved by its own merges); use `origin/claude/backend-impl` after `git fetch`.
- The fake_vllm tests bind fixed ports in the e2 block and the box's fake in e3b2's (56782): P-21 collisions with any lane's ephemeral sockets; reruns are green.
