# Handoff — 2026-09-24T05:45Z (sofia window; `main` = 8cabe20 after checkpoint 2; PILOT RELEASE 27af05a DEPLOYED; PREP-WORKER lane finishing)

Supersedes `HANDOFF-20260924T0215Z.md`. Since it: worker verifier PASS → step 8b merged 405f633; checkpoint 2 on 27af05a passed (api-test 3639 + two P-21 harness drills green on d6; layer 0 202 no PENDING; lists 2185/2186; E4B 138/140; layer 3 exit 0) → the three list items fixed (`codex/ckpt2-lists` 7a2556c merged 7a22064; sigint one-liner 2b86ae5) → **`main` fast-forwarded 01a7dfc → 8cabe20** (local main worktree realigned). PREP-WORKER's early review PASSED at c035ee4 (no blocking; 10 items folding in; lane still running, agent a8196f41c1c250fa3). **Box phase 2 executed**: hosted migrations 0003–0018 applied (digest 4524cbc0…, backup `~/infrx-backups/hosted-20260924T050746Z`-ish: see the session record W6 line), the operator seed applied on hosted (the runbook lacked it: `pilot.price_check` needs the Marlin catalog + an active card), first install reverted (gateway refused: price_source), second install **DEPLOYED 27af05a (pilot)** at 05:43Z: public edge live, metered gateway + worker + engine ready. Snapshot snap-0c3f41caccbffbfcf. Integration head (this commit)+; `main` 8cabe20.

Rules unchanged (1125Z; ONE fix round per lane; reviews on committed heads; box via SSM with `infra/rollout/ssm.sh` steps; secrets never in text; the auto-mode classifier blocks multi-step deploy commands: run window steps ONE per command). Session record `research/plan/evidence/coordinator/2026-09-22-session-02.md` (append-only, complete; the box command ids are there); tracker `python3 research/plan/scripts/progress.py` → publish `scratchpad/marlin-backend-tracker-sofia.html` with url https://claude.ai/artifact/Rk84qe3Yuzr2d357GoeRvZ.

## NEXT (in order)
1. **PREP-WORKER handback** (branch `codex/prep-worker`, worktree codex-prepw, base 01103c9) → push → targeted verifier on the handback head (the ten folded items L1–L8, J-F2–J-F4 + honesty; namespace e2 or e4b, d6 + MinIO 55724) → `PREPW_VERIFY="pass at <sha>" bash research/plan/evidence/coordinator/merge-plan/10-prep-worker.sh` (resolves Makefile + 08) → focused gate (tests/w, tests/i, contracts quick, layer 0, layer 3 on e3b2 `--only-suites --canary`, the new list) → ff `main` → **second install**: `release-bundle.sh <sha>` + fetch + `20-prepull` (no-op image) → W5 → W8 → W10 (same INSTALL_ARGS + MIGRATION_DIGEST 4524cbc0…) → W11 → public probes. Add PREPARATION_CONCURRENCY to INFRX_SET only if the lane's default is not 2.
2. **W12 smoke** needs: (a) the operator key: generate a secret in-process, `select infrx.bootstrap_operator_key(<personal org of the operator>, 'coordinator', <prefix>, sha256hex, 'sofia', 'rollout smoke')` on hosted (read `infrx/operations/service.py` KEY_PREFIX + `_context` hashing first: the hash must be what `_context` computes); (b) a verified individual user on hosted (A1) — check `public.profiles`/A1's signup tables read-only; if none, the smoke's consumer-key half stays PENDING (G6B/A1) and `verify-external.sh` runs with `LEGACY_KEY` unset → expect 401 lines; (c) `issue-key`/`revoke-key` via `python -m infrx.operations.cli` with `INFRX_OPERATOR_KEY` exported (never argv). Then W13: record RELEASE, image, engine digest, snapshot, backup, digest, command ids; release the lock.
3. **E1B L2–L7** and **`certify.py --box`** (E4B protocol v2; 4-hour soak unless the user shortens it) after the second install → release decision → tasks.json E4B box half.
4. **Runbook fixes** (coordinator, docs): add `35-seed.sh` (the operator seed, idempotent) before W10; the admin-socket restoration after a revert (`caddy reload --address localhost:2019` when the running config lost the socket); 90-revert's `BACKUP=` and `ROLLBACK_TO_UNMETERED=no-pilot-request-was-accepted` in the W10-exit-4 row; the E4B/E1B box protocol entries.
5. Open: the dependabot alert on main; P-01 (the provisional card is live on hosted, no CREDIT regime); P-19/P-21; VER-N1 (longer-than-shape values); F1/F2 of the worker verifier; the M limits; V-N1 (D5).

## RUNS / CONTAINERS (05:45Z)
Lanes: PREP-WORKER (d5/55467/55498, MinIO 55781, namespace e4b). Coordinator: none (ckpt-final scratch idle; infrx-ckpt2-minio removed by the script). Box: pilot units (marlin2b-gateway = the new gateway on 8001, infrx-worker 8002, infrx-valkey 6379, marlin2b-vllm 8000), caddy. Stale local: infrx-d2-postgres. Disposable scratches: mergeu, mergeu2, mergeu3, ckpt-interim, ckpt-final.

## TRAPS (new)
- `caddy reload` reads the admin address from the FILE being loaded: when the running config lost the socket, pass `--address localhost:2019` once.
- `90-revert.sh` needs `BACKUP=<install backup dir>` and, when no pilot request was accepted, `ROLLBACK_TO_UNMETERED=no-pilot-request-was-accepted`.
- The box checkout refuses a dirty tree (untracked files count): move artefacts aside on the same filesystem.
- The rollout's catalog publication is the operator seed (`infrx/state/seed_marlin_provisional.sql`), not a migration; `publish-marlin` needs an operator key and a provider org.
