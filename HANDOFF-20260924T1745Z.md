# Handoff — 2026-09-24T17:45Z (sofia window; pilot end-to-end proven; certification rerun in its soak; two fix lanes; corpus on the host)

Supersedes `HANDOFF-20260924T1700Z.md`. Since it: the first box certification (run1, out 20260924T165408Z; evidence committed under `models/marlin2b/results/E4B-box-4226315/run1-…`) FAILed every measurement cell because the client requests the labelled alias and the USD admission keys `infrx.price_versions` by the literal model string (only the unlabelled alias had a row) → a second price-version row was seeded (user-authorized; logged in the session record); **rerun launched 17:22:44Z, out=/opt/dlami/nvme/e4b/20260924T172244Z (poller bnytqtebm)**. Its remaining protocol-side FAILs are known: served-build (no git in the runtime image — fixed by the CERTIFY-TREE lane; `infrx-certify:<release>` image built on the box, gate 0.7) and sop-parity (the corpus's four 112 s clips exceed the deployed 82 s cap and are refused `400 unsupported_media` — the CERTIFY-TREE lane is extending its branch to treat over-cap clips as expected typed refusals). The W3 corpus (81 files, 2.7 GB) is unpacked in this host's `.claude/corpus-cache` for the E1B gateway cells. `main` 2d4a88b; served release 4226315; integration head = this commit.

Lanes: **TOKCOST** (a2fad87c25e2c8a38; `codex/tokcost`; worktree codex-tokcost) — the 16.8 s video /tokenize; **CERTIFY-TREE** (a25c20f6a41241d24; `codex/certify-tree` @ 7bb7b41 + the cap item; worktree codex-certify-tree; E4B list 154 passed at 7bb7b41; its --box now requires `--worker-metrics-url http://127.0.0.1:8002/metrics`). Both: handback → push → targeted verifier (Opus) → merge → focused gate → `main` → third install (bundle + W5/W8/W10/W11, one step per command) → certification rerun in `infrx-certify:<release>`.

Hosted state added this window (all logged in the session record with rollbacks; names only): operator key in SSM (`/model-inference/operator_key`), `signup_grant` flag true, one A1 grant, two consumer keys (one revoked; the test key also in SSM `/model-inference/e4b_api_key` and the 0600 file `scratchpad/rollout/keys/test.key`), price versions for both alias forms, a USD test grant. Runbook rows W7b/W7c/W7d cover the seeds; add W7e for the labelled alias, or better: D/G make the price lookup key on the resolved revision (ruling question, next free R106).

## NEXT
1. Certify rerun result (bnytqtebm) → fetch report/log/work via the bucket (`scratchpad/rollout/e4b-fetch.sh` pattern) into `models/marlin2b/results/E4B-box-4226315/run2-20260924T172244Z/`; expected: envelope/soak/overload/dataset measured; served-build and parity FAIL for the two protocol reasons above.
2. Step 6 recovery drills after the soak (each logged with UTC + outcome; `reconcile.md` drift 0 after each): restart.md Engine (`systemctl restart marlin2b-vllm`), Worker (SIGKILL drill), index-loss (restart infrx-valkey, rebuild from PostgreSQL, reaper), restore.md B1–B2, rollback.md against 27af05a then forward to 4226315. One step per command.
3. E1B L2–L7 from this host against the public edge (`INFRX_API_KEY` from the 0600 file): `models/marlin2b/bench.py --target gateway --base-url https://marlin2b.callbill.ai/v1 --corpus models/marlin2b/corpus/manifest.json …` per `models/marlin2b/results/E1B-protocol.md` §3; the 4 clips over 82 s (2 in `fast`) are expected refusals. Results → `models/marlin2b/results/E1B-box-<sha>/`. Only after the box is idle (never concurrently with the certification soak).
4. Release decision (`research/plan/evidence/e/E4B-release-decision.md`) + tasks.json E4B box half + W13 record; then the TOKCOST/CERTIFY-TREE merges and the third install.
5. Open: P-01, the price-lookup-by-alias question, VER-N1, worker verifier F1/F2, PREP-WORKER requests 2/3/7, M limits, V-N1, dependabot.

## RUNS / CONTAINERS (17:45Z)
Box: certify container (rerun) in `infrx-runtime:4226315…` (soak ~4 h), pilot units live. Host: no stacks; lanes on d6/e4b (TOKCOST) and no stack (CERTIFY-TREE).
