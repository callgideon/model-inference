# Handoff — 2026-09-23T16:20Z (sofia window; G2 + G3 merged, 26/30; three confirmations + one verifier running)

Supersedes `HANDOFF-20260923T1500Z.md`. Since it: **G2 merged 2391d4d** (round-3 confirmation pass at 76ba3bb; merged-tree checks green on d4: contracts 1053, tests/g+m 859, G list 306, D conformance after a coordinator RAISES entry f52308a for the pending lookup case); **G3 merged b560b51** (+ Makefile 31bfd05 `api-mutants` += `tests/g/jobs/test_jobs_mutants.py`; confirmation pass at 9133efe; merged-tree checks running `.claude-logs/g3-merged-31bfd05.log`); manifest **26/30**; F fakes round 2 handed back 0cf5cd6 (verifier running); I3B follow-up round 2 confirmed fix_required on one test gap (DRL-1; round 3 running); E3B2 fix round 2 handed back b0a64e1 (confirmation running); D5 running. `main` = 01a7dfc (checkpoint 1). Rulings R1–R94 (next free **R95**); pending input P-21.

Rules unchanged (1125Z handoff). Standing user instruction: **"safely complete and merge tasks, run max parallel agents / tasks — wherever possible"**; "update tracker" stands. Reviews/refuters/implementers on Opus 5.5; Fable plans/orchestrates. Session record `research/plan/evidence/coordinator/2026-09-22-session-02.md` (append-only). Tracker `python3 research/plan/scripts/progress.py`; sofia artifact https://claude.ai/artifact/Rk84qe3Yuzr2d357GoeRvZ (file `scratchpad/marlin-backend-tracker-sofia.html` = copy of `evidence/coordinator/progress.html`).

## BRANCH STATE (local == origin at 16:20Z for every lane; the integration branch is pushed at each record)
Integration `claude/backend-impl` = 6dedf1e+ (worktree `infrx-impl`); `main` = 01a7dfc.

| lane | branch @ head | state | next |
|---|---|---|---|
| F fakes follow-up | codex-ffakes `codex/f-fakes-followup` @ 0cf5cd6 (code f6da113) | **verifier running** wf_4a397730-fd1 (single Opus parity verifier on PostgreSQL + refuters; JSON of round 1 `evidence/f/F-fakes-followup-review-4209aee.json`) | pass → **MERGE** (then run both D conformance suites on both images); E3B2's dr04 fake pin is deleted at the E3B2 merge (R93) |
| E3B2 | codex-e3b2 `codex/e3b-phase2-gate` @ b0a64e1 (impl f883a0a; gate red only on I3B ids) | **confirmation running** wf_1c32b755-7d0 (gate lens on namespace e3b2/56700 block + honesty/cross-lane lens; key question: E3B2's `stale_pending()` vs PENDING[G2]/[G3] now that tasks.json marks both implemented with the cutover HELD) | pass → **MERGE after F fakes**: IR2-1 (TASK_BLOCKS + strict-xfail deletion in ONE commit; diff `scratchpad/e3b2/ir1-r2.diff`), IR2-3 (delete dr04's fake pin; the F fakes tree supplies the requeue id), IR2-5 procedure; message the lane if fix_required |
| I3B follow-up | codex-i3bf `codex/i3b-followup` @ 662c07c+ | **round 3 running** (DRL-1 readiness retry unpinned + cheap nonblockings; JSON `evidence/i/I3B-followup-confirm-662c07c.json`) | handback → single Opus verifier (drill lens) → **MERGE after E3B2** with IR2-2 (only the stack.py hunk: PENDING −G1R/D2/D3/M3/W3, RESIDUAL = {}), R2-A (E runner OWNED_TREES += deploy), R2-B (D-mode lock), request-1 stack.py half |
| D5 | codex-d5 `codex/d5-terminal-transaction` @ 9d0b24b+ | **running** (told 15:38Z: merge the integration head, implement `PgJobStore.lookup` per G2 R2-2/COMP-N2, drop the lookup PENDING + RAISES entries) | handback → review (3 lenses) → **MERGE after I3B** (harness += 0018; Makefile list) → E4B |
| E1B | — | GPU slices wait on the box rollout | |
| closed | G3 9133efe (b560b51), G2 76ba3bb (2391d4d), W4 (f36c17c), D4 (93ba108), G4U (7d21fa7), W-new (a15fa4b), I-schema (bc382ac), F cancel-cause (6803d2f) | | |

## RUNS / CONTAINERS
- Waiter on `.claude-logs/g3-merged-31bfd05.log` (contracts quick 1014 done; tests/g, G3 list, G list pending) — record when done.
- Workflows: F fakes verifier wf_4a397730-fd1; E3B2 confirmation wf_1c32b755-7d0 (its gate lens owns namespace e3b2 for ~40 min); scripts under the session dir `workflows/scripts/`.
- Containers: `infrx-d5-postgres` (D5's); `infrx-d1-postgres`, `infrx-d2-postgres` (lanes' default runs; leave); reviewers create `infrx-confirm-*`/`infrx-verify-*`/`infrx-e3b2-*` and remove them; `gideon-*` not ours. Coordinator merged-tree checks use d4 (55435/55465) + Q 55489.

## NEXT (in order)
1. Process results as they land (parse `result`; save JSON under `research/plan/evidence/<track>/`; record in the session log + progress-state; tracker; pass → merge `--no-ff` with the integration requests, merged-tree suites on d4, push; fix_required → SendMessage the lane, then a confirmation/verifier on the fix head). Null/empty results are NOT a pass.
2. Merge order: **F fakes → E3B2 → I3B follow-up → D5 → E4B.** Then checkpoint 2: whole-tree api-test + api-mutants on private ports (`INFRX_D_TASK=d4 INFRX_D2_VALKEY_PORT=55465 INFRX_D2_VALKEY_CONTAINER=infrx-d4-valkey INFRX_Q_VALKEY_PORT=55489`, `-rfE` into a log; console + bench) → ff `main`.
3. Cutover (HELD, coordinator, one change): G2's cutover diff + R2-1 (create_app adapters from settings; README gateway.py refs) + G3 (a) `ROUTERS = (health, models, ingress, uploads, jobs)` + G3 (e) I2B edge proxying of the jobs routes + E3B2 PENDING[G2]/[G3] bodies (phase 3) — only when D5, M adapters and E3B2 exist.
4. W4 phase B (coordinator box re-measurement; maintenance window). Open items as in the 1300Z/1330Z handoffs plus: G2 R2-4 (M stray-payload sweep or persist-the-attach), G3 nonblockings (ADM-R3-N1 legacy in-flight no-attach case; G1 Prefer parameter parsing), E3B2 IR2-6 (fake lacks D2 MC-1 → F fakes follow-up 2), I3B R2-C = P-21, D pgharness fixed lock path per port (I3B DRL-4), H3 wording for D's PgStreamStore.append docstring and R93.
5. Keep pushing lane branches at each handback; tracker after each state change; refresh this handoff at each checkpoint; at ~90 % of the window stop dispatching, write the handoff, commit, push, idle.

## TRAPS (new this window)
- A `git clone --shared` scratch sees the LOCAL lane branches as `origin/…`: a reviewer saying "the remote moved" may be reading a lane's unpushed local head.
- Manifest textual edits: find `"id": "<T>"`, then the next `"status": "planned"` and the `"disposition"` string; `validate_plan.py --write-ledger` after.
- A PENDING conformance case on PostgreSQL dies of the adapter's typed refusal, not NotImplementedError: add a `RAISES` entry (D4 H2 rule) or the strict xfail fails.
