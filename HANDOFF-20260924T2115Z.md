# Handoff — 2026-09-24T21:15Z (sofia window; v1 merged to `main` and deployed; the planning session can start)

Supersedes `HANDOFF-20260924T1745Z.md`. **Entry point for re-planning: `research/plan/20-platform-handoff-2026-09-24.md`** (the whole platform: done, pending, measured, rulings, inputs, gaps; §14 = live state). This file is the operational tail only.

## State
- `main` = **b9ad1d8** (fast-forwarded after gate-3 on 95fbb90 and the delta gate on bda1586, both green). It contains the 2026-09-24 merges: CERTIFY-TREE 4db74b6, TOKCOST 95fbb90, INTAKE-DRAIN a163953, CERTIFY-POLISH bda1586; rulings R106–R108 (next free R109); pending inputs to P-23.
- **Deployed: release `bda15866e5700f3856d7142580da842fba9bbd23`** on the pilot box (image sha256:cc2a80c9…; `LARGE_BODY_LIMIT=8`, `MAX_VIDEO_SECONDS=82`, `ENGINE_MAX_NUM_SEQS=8`, `WORKER_CONCURRENCY=8`); W12 smoke 15/15; rollback drill measured (5 s back to the previous runtime and ready); pre-window snapshot snap-09b047db6284df302; install backup `/var/backups/infrx/20260924T200647…-bda1586…` (note: it holds the 27af05a runtime the drill left in place — a revert to 4226315 reinstalls from its bundle).
- Evidence collected today: E4B run1/run2 (`models/marlin2b/results/E4B-box-4226315/`), step-6 drills (engine 172 s, worker 8 s, index loss 6 s, rollback 5 s; drift 0), E1B cells on 4226315 (`E1B-box-4226315/`, real inference in `run2-distinct-keys/`), the video /tokenize split (0.13–0.2 s warm; the 16.8 s was the engine's first video after start), the hosted drift check from the host (`infra/runbooks/drift.py`, transaction pooler; the session pooler is full while the pilot runs).

## In flight at this write
- **E1B acceptance cells on bda1586** through the edge (`scratchpad/rollout/e1b/run-all-3.sh`; results `models/marlin2b/results/E1B-box-bda1586/`): the drain fix's acceptance = zero ReadError at 0.5/s, 1.0/s and the 8-burst.
- **Certification run3** launches after them: `infra/rollout/ssm.sh scratchpad/rollout/e4b-certify3.sh RELEASE=bda1586…` (the certify image `infrx-certify:bda1586…`, `--worker-metrics-url`, `--env-file /etc/marlin2b-gateway.env`, `--release-sha`; the soak now bounded at 14,400 + 900 s) — about 5 h; then fetch (`e4b-fetch.sh` pattern → `E4B-box-bda1586/`), the release decision (`research/plan/evidence/e/E4B-release-decision.md`), tasks.json E4B box half, W13 in the record.

## Follow-ups recorded for the next wave (none blocking)
INTAKE-DRAIN N1 (finished-guard case + mutant) and N5 (`Expect: 100-continue`); E1B protocol amendment (per-cell dataset identity; flag `replayed > 0`); the worker's `/metrics` gauges (index depth, reconciler); runtime pools vs the pooler's 15 session slots; the rollback drill's public-edge 503 after readiness (⚠️ TO BE VERIFIED); P-22 price lookup key; P-23 long clips; the fleet notes (19); the second tenant (email confirmation); LARGE_BODY_LIMIT=8 validated by run3's overload cell.

## Rules that still apply
One box step per Bash command (classifier); secrets never on argv or in files that get committed; the box only through `infra/rollout/ssm.sh` with a logged op; Opus verifies/implements, Fable orchestrates; lanes never push; append-only records.
