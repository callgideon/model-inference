# Handoff — 2026-09-24T17:00Z (sofia window; PILOT END-TO-END PROVEN; E4B box certification running; two fix lanes in flight)

Supersedes `HANDOFF-20260924T1010Z.md`. Since it (user: "you have all the permission, create a new key and set it up" / "grant it, do whatever it takes, to test it out end to end and fix all the issues"): operator key minted → SSM `/model-inference/operator_key` + hosted row d554db80…; `signup_grant` flag enabled on hosted; A1 grant (10,000 CREDIT) for the confirmed dev account f997131f… (org 15e766d0…); consumer keys 142c7d81… (test) and 0fdbb31f… (revoked) via the operator CLI; the USD price version `pv_marlin2b_usd_2026_09` seeded (the smoke's 400); a USD 5.00 test grant through `public.credit_ledger` (the 402); **W12 smoke 15/15 (failures 0)**; **text + video jobs complete on the pilot with engine-exact counts** (13 = 13; 1211 = 1211; settled, USD debited, wallet 4.99984270); the video's engine `/tokenize` took 16.8 s (5 s clip) — the open performance item. E4B box certification (protocol v2) running since 16:54Z on the box: `out=/opt/dlami/nvme/e4b/20260924T165408Z` (preconditions ok, config-pin ok, served-build FAIL because the runtime image has no git → CERTIFY-TREE lane; envelope/soak (4 h)/overload/parity in progress; poll task blu19i49a); dev-host report at 4226315 saved (`research/plan/evidence/e/E4B-dev-4226315.json`: protocol 117 + recovery 71 PASS; preconditions FAIL = this host's App dev servers, the known exemption). `main` = 2d4a88b; served release 4226315; integration head (this commit)+.

Lanes running: **TOKCOST** (agent a2fad87c25e2c8a38; branch codex/tokcost from main; worktree codex-tokcost; d6/55471/55472, MinIO 55724, namespace e4b) — cut the per-video preparation latency without weakening R105 (memo by media digest, vLLM tokenize mm-cache, overlap); **CERTIFY-TREE** (agent a25c20f6a41241d24; codex/certify-tree; worktree codex-certify-tree; no stack) — certify's served-build cell reads the tree from `--release-sha` when git is absent (report says so), worker gauge compared too. Each: handback → review/verifier → merge → (TOKCOST: a third install + re-measure the video request).

Rules unchanged (window/box steps ONE per command; the classifier refuses `--pid host --user 0` docker forms and multi-step deploys; secrets: SSM in-process, 0600 files, never argv/text). Session record `research/plan/evidence/coordinator/2026-09-22-session-02.md` (append-only; every command id); tracker `python3 research/plan/scripts/progress.py` → publish with url https://claude.ai/artifact/Rk84qe3Yuzr2d357GoeRvZ (v39). Smoke keys: `scratchpad/rollout/keys/{test,revoked}.key` (0600; the test key also in SSM `/model-inference/e4b_api_key`).

## NEXT
1. **Certify result** (poll blu19i49a; ~21:00Z): fetch `<out>/report.json`, `certify.log`, `work/`, `inventory.txt` via SSM into `models/marlin2b/results/E4B-box-4226315/` (new evidence), record the report sha256; expected non-PASS: served-build (git) until CERTIFY-TREE lands (a rerun of the cell or the whole run on the next release), recovery-box PENDING until step 6.
2. **Step 6 recovery drills** (each logged with UTC + outcome; box healthy after): restart.md Engine, Worker; index-loss.md; restore.md B1–B2 (snapshot snap-0c3f41caccbffbfcf listed); rollback.md against the previous accepted release (27af05a) then forward to 4226315; reconcile.md drift 0 after each. Then the release decision (`research/plan/evidence/e/E4B-release-decision.md`) + tasks.json E4B box half.
3. **E1B L2–L7** (open-loop rate, burst, paired direct/gateway, cancellation, resume, soak) with `models/marlin2b/bench.py --target gateway` (key from `INFRX_API_KEY`, never argv) per `research/plan/evidence/e/E1B-9126adf.md` — after the certify soak (the box must be idle); L4 paired runs need the direct engine (on the box via SSM with the key.env).
4. TOKCOST / CERTIFY-TREE handbacks → review → merge → focused gate → `main` → third install (bundle + W5/W8/W10/W11 one step per command) → re-measure the 5 s and a 30 s clip.
5. W13 record + release the lock; P-01 stays open (provisional card; CREDIT admission off); the docs rows W7b/W7c/W7d are in the runbook.

## BOX STATE (17:00Z)
Release 4226315 live (gateway 8001, worker 8002 both loops, valkey, engine max-num-seqs 8); caddy with the release's edge (admin socket); certify container running (`infrx-runtime:4226315…`, no --pid host); /opt/dlami/nvme/e4b/{inventory.txt,key.env(0600),parity-e0.jsonl,20260924T165408Z/}; w3-checkout at 4226315 (its local artefacts in w3-untracked-20260924T165215Z/); backups /var/backups/infrx/…; snapshot snap-0c3f41caccbffbfcf. Hosted: 0003–0018; operator seed; price version; flags credit_admission f / legacy_usd_admission t / signup_grant t; keys: 2 legacy consumer, 1 operator, 1 test consumer (+1 revoked).
