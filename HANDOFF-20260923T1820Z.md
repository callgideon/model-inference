# Handoff — 2026-09-23T18:20Z (sofia window; F fakes + E3B2 merged, 27/30; I3B follow-up verifier + D5 running)

Supersedes `HANDOFF-20260923T1620Z.md`. Since it: **F fakes follow-up merged a2779d1** (+ G3 seam fix 51f6c6e; merged-tree checks green on both PostgreSQL images incl. the contracts list 445); **E3B2 merged d1d4a68** (+ IR2-1 d588c3e TASK_BLOCKS/strict-xfail in one commit, IR2-3 0a642ad dr04 pin) after a confirmation (fix_required at b0a64e1), rounds 2b/2c/3/4 and two verifications (gate reproduced at 0924cf2; runner pass at d0d523f); manifest **27/30** (E3B implemented; phase-3 bodies pend on the held cutover G2-R1 and D5); I3B follow-up rounds 3–5 done (a693c6e; light verifier running); D5 near its handback. `main` = 01a7dfc (checkpoint 1). Rulings R1–R94 (next free **R95**); pending input P-21 (reproduced by three lanes today as TIME-WAIT collisions on 55434/56780).

Rules unchanged (1125Z handoff). Standing user instruction: **"safely complete and merge tasks, run max parallel agents / tasks — wherever possible"**; "update tracker" stands. Reviews/refuters/implementers on Opus 5.5; Fable plans/orchestrates. Session record `research/plan/evidence/coordinator/2026-09-22-session-02.md` (append-only). Tracker `python3 research/plan/scripts/progress.py`; sofia artifact https://claude.ai/artifact/Rk84qe3Yuzr2d357GoeRvZ (file `scratchpad/marlin-backend-tracker-sofia.html` = copy of `evidence/coordinator/progress.html`).

## BRANCH STATE (all lane branches pushed at their last handback; integration pushed at each record)
Integration `claude/backend-impl` = e409d64+ (worktree `infrx-impl`); `main` = 01a7dfc.

| lane | branch @ head | state | next |
|---|---|---|---|
| I3B follow-up | codex-i3bf `codex/i3b-followup` @ a693c6e | **round-5 verifier running** wf_34c5673b-a7e (round-4 verifier passed at 95b7df7; round 5 = its four nonblocking witnesses; I3B list 109/107 on the E3B2 merge) | pass → **MERGE** `--no-ff` + **IR2-2** as ONE commit from `scratchpad/e3b2/r4-ir22.diff` (stack.PENDING RESIDUAL block deleted incl. G2, `RESIDUAL = {}`, keep `stack.OWNERS`, `test_run.KNOWN_STALE = {}`) → merged-tree checks from the REPO ROOT with the API venv (`apps/infrx-api/.venv/bin/python -m pytest tests/integration …`; layer 0 must be fully green: pendings only D5/G2-R1/I2B-R4/M1-L2; the anchor guard with KNOWN_STALE = {}); the I3B list through E's runner is part of the checkpoint gate |
| D5 | codex-d5 `codex/d5-terminal-transaction` @ c67e4f5+ (evidence WIP at 4bcac3b; lists, make check, orderings, final Supabase sweep running) | **running** (ports 55436/55467, Q 55498; told 15:38Z: merge the integration head, implement `PgJobStore.lookup`, drop the lookup PENDING/RAISES) | handback → review (3 Opus lenses: settlement/money, conformance on both images, honesty/runner) → **MERGE** (harness list += 0018; Makefile += its list; PENDING/RAISES bookkeeping; G3's F/D requests) → E4B |
| E1B | — | GPU slices wait on the box rollout | |
| closed | E3B2 d0d523f (d1d4a68), F fakes 0cf5cd6 (a2779d1), G3 9133efe (b560b51), G2 76ba3bb (2391d4d), W4 (f36c17c), D4 (93ba108), G4U (7d21fa7), W-new (a15fa4b), I-schema (bc382ac), F cancel-cause (6803d2f) | | |

## RUNS / CONTAINERS
- Waiter on `.claude-logs/e3b2-merged-0a642ad.log` (test_run + test_stage 61 done; layer 0 — I3B's two reds expected until the I3B merge; test_harness + contracts ≈ 1075; dr03/dr04 fake).
- Workflow running: I3B r5 verifier wf_34c5673b-a7e. Scripts under the session dir `workflows/scripts/`.
- Containers: `infrx-d5-postgres`(+supabase) D5's; `infrx-d1-postgres`, `infrx-d2-postgres` lanes' default runs (leave); reviewers' `infrx-verify-*`/`infrx-confirm-*` are removed at exit; `gideon-*` not ours. Coordinator merged-tree checks use d4 (55435/55465) + Q 55489 + namespace e3b2 (free now).

## NEXT (in order)
1. I3B follow-up merge (above) → D5 review/merge → **checkpoint 2**: whole-tree `make api-test` + `make api-mutants` on private ports (`INFRX_D_TASK=d4 INFRX_D2_VALKEY_PORT=55465 INFRX_D2_VALKEY_CONTAINER=infrx-d4-valkey INFRX_Q_VALKEY_PORT=55489`, `-rfE` into a log) + console + bench + layer 0 of tests/integration → ff `main`. Consider one layer-3 gate run on namespace e3b2 on the merged tree (`tests/integration` runner; ~35 min; needs a main-checkout worktree or `CORPUS_CACHE` exported) — expect exit 0 once I3B is merged.
2. E4B (last package) after D5: brief from `research/plan/18-marlin-backend-first.md`; the cutover (HELD, coordinator, one change): G2's cutover diff + R2-1 (create_app adapters from settings; preflight manifest; README) + G3 (a) ROUTERS + G3 (e) I2B edge proxying + E3B2 phase-3 bodies (journeys ×9, dataset resume, dr11, dr07/dr07c) — when D5 and the M adapters exist.
3. Open items: W4 phase B (box re-measurement); G2 R2-4 (M stray-payload sweep / persist-the-attach); E3B2 IR2-6 → "F fakes follow-up 2" after a coordinator ruling on provider_dev CREDIT admission (D2 MC-1 on the fake); I3B R3-2 (D pgharness lock path independent of TMPDIR) and R2-C = P-21; D4 Limits 4(b)/5; H3 wording (D's PgStreamStore.append docstring, R93 text); G3 nonblockings (legacy in-flight no-attach case; G1 Prefer parameters); E3B2 R4V-N1..N4 (counts() summary-line parse; _is_recovery pin; KNOWN_STALE strictness; backend -r pin) and GATE-N4/N5; Dependabot alert.
4. Keep pushing lane branches at each handback; tracker after each state change; refresh this handoff at each checkpoint; at ~90 % of the window stop dispatching, write the handoff, commit, push, idle.

## TRAPS (new since 1620Z)
- `tests/integration` lives at the REPO ROOT; run it from there with `apps/infrx-api/.venv/bin/python -m pytest` (a script that `cd apps/infrx-api` reports "file not found", exit 4).
- A lane's grep for a removed fake attribute cannot see unmerged siblings: after merging a fakes change, run every track's suites that use the fake (G3's `expired_jobs` seam).
- Refuters may confirm a mechanism yet rate it nonblocking-if-real; the newer verifier scripts count a finding as blocking only when the refuters call it blocking (`downgraded_to_nonblocking` in the result).
- The E3B2 gate's own `make api-test` starts the D harness on the shared 55432 and vkstore's fixed `infrx-d2-valkey:55463`; a reviewer must run the suites stage separately with a private task or accept that collision.
