# Handoff — 2026-09-23T06:55Z (rey window; refreshed after the cancel-cause merge)

Supersedes `HANDOFF-20260923T0512Z.md`. Merged this window since it: D2, A1, D3, W3, I2B, G1R, M4, I3B, Q3, F2P wire-in, **F cancel-cause (6803d2f + 0024da2)** → 21/30 manifest packages. Reviews/refuters/implementers run on Opus 5.5; Fable only plans and orchestrates (memory `review-agents-on-opus.md`). Ultracode is on: substantive review/verify work goes through `Workflow` with `model: 'opus'` on every `agent()`.

Same rules: WIP-commit and push lane work as you go (lanes never push; the coordinator pushes each lane branch); detach anything > 10 min; never touch another lane's containers; secrets never printed/logged/committed; AWS CLI via `env -u AWS_ACCESS_KEY_ID -u AWS_SECRET_ACCESS_KEY -u AWS_SESSION_TOKEN aws …`; never the pilot box outside logged coordinator ops. Standing user instruction (latest): **"safely complete and merge tasks, run max parallel agents / tasks — wherever possible"**; earlier "update tracker" stands.

## GOAL
Marlin backend-first program (`research/plan/18-marlin-backend-first.md`, manifest v4 `tasks.json`): 21/30 merged and reviewed; 6 in progress (E1B GPU slices, E3B phase 2, D4, G2, G4U, W4); 3 not started (D5, G3, E4B). Session record (append-only): `research/plan/evidence/coordinator/2026-09-22-session-02.md`. Tracker: `python3 research/plan/scripts/progress.py` → `evidence/coordinator/PROGRESS.md` + `progress.html`; rey artifact https://claude.ai/artifact/3a3VjYKb2Vfh5X89BYMDPZ (other accounts cannot update it — publish a fresh one per account).

## BRANCH STATE (verified local == origin at 2026-09-23T06:55Z)
Integration `claude/backend-impl` = a7531db (this worktree `infrx-impl`); `main` = f9ba5d2 (ff pending the checkpoint gate). Program base ec6c548.

| worktree | branch @ head | agent | state |
|---|---|---|---|
| codex-d4 | `codex/d4-stream-journal` @ ce7659b | a3dc7d1193c9ade11 | make check running (`/tmp/claude-1000/d4/makecheck.log`, INFRX_D_TASK=d4 55435/55465); handback next |
| codex-g2 | `codex/g2-chat-relay` @ a16d237 | a98f6b711111c4380 | item 5 follow-ups; two api-test runs in flight; told: wire-in merged at 1baf0b8; cancel-cause merged — fall back to the default cancel on `UnsupportedParameter(param="cause")` until D5's 0018 |
| codex-g4u | `codex/g4u-upload-adapter` @ a3cb5c2 | a6ce8b688ac75949c | track-first red in tests/q/test_reconcile.py against the shared `infrx-q3-valkey` (55462) while other lanes used it; told to rerun tests/q on its own Valkey and hand back |
| codex-e3b2 | `codex/e3b-phase2-gate` @ 0843485 | aff9c42bf5340b59a | gate run suites/mutants stages (namespace e3b2 56700–56799) |
| codex-w4 | `codex/w4-measured-tuning` @ ecacd50 | a2c1ab13508b58f8b | phase A (config surface + test_w4.py; full list 2 of 54 not killed → fixing); phase B needs a coordinator-run re-measurement (maintenance window; candidate.sh uses `/opt/dlami/nvme/w3-checkout`, never the unit's tree) |
| codex-fcancel | `codex/f-cancel-cause` @ 0fe8fdd | done | MERGED 6803d2f; PENDING line 0024da2 |
| codex-e1b | `codex/e1b-...` | — | software slices merged 164e43e; GPU measurement slices wait on the box rollout (I2B runbook, not done) |

Older lanes (d2, a1, f2p-wirein, q3, w3, i2b, g1r, d3, i3b, m4) are merged and pushed; leave them.

## DETACHED (each under its own session id; survives this process)
- **Checkpoint gate** on the integration tree: pid 783976, `.claude-logs/checkpoint-d5a221d.log` (empty until the first `tail -3` flushes; ends with `EXIT=0`; stages `API_TEST_EXIT`, `API_MUTANTS_EXIT`, `CONSOLE_*`, `BENCH_EXIT`); INFRX_D_TASK=d2 → **d2's port 55433 is the gate's**. Waiter shell bc0p6678h. On all-green: `git push origin claude/backend-impl:main` (ff) and record a checkpoint in the session record + progress-state `checkpoints`. Note the gate started at d5a221d; merges since (cancel-cause) are covered by the merged-tree runs below, not by the gate — say so in the checkpoint note or rerun.
- Merged-tree runs for the cancel-cause merge: `.claude-logs/dconf-merged-0024da2.log` = GREEN on d3 (44 passed, 3 skipped, 25 xfailed, 1 xpassed); `.claude-logs/contracts-merged-0024da2-d3.log` running (waiter b8lzd6xay). The earlier `.claude-logs/fcancel-merged-0024da2.log` ran on d2 while the gate held it → its reds are harness contention (3 contracts Pg-projection cases, D cases), not code.
- D4 make check: `/tmp/claude-1000/d4/makecheck.log` (86% at 06:55Z).
- G4U: `../codex-g4u/.claude-logs/track-first.log` (red in tests/q — shared Valkey), `make-check.log` pending its own loop (`makecheck.sh`).
- Docker in use: infrx-d1/d2/d4/e3b2-postgres, infrx-d2/d4/e3b2-valkey, infrx-q3-valkey (Q3's shared server; some lanes still hit it — a lane must run Q tests on its own INFRX_Q_VALKEY_PORT/INFRX_Q2_VALKEY_PORT).

## NEXT (in order)
1. Read `.claude-logs/contracts-merged-0024da2-d3.log`; green → append to the session record. Red → look at the failing names (expected only Pg-projection cases if a port collided).
2. Checkpoint gate → ff `main` on green (above). If api-test red only in D/Pg cases, that is d2 contention from the (now killed) first cancel-cause run at 06:48Z — rerun api-test alone on d2 before judging.
3. Handbacks → review workflow (Opus, lenses per track, refuters on every blocking finding, own ports/containers; script template in `scratchpad/wf/*.js` — copy from the previous session's scratchpad if missing: the review script takes head, worktree, lenses, and writes structured JSON) → save JSON under `research/plan/evidence/<track>/<TASK>-review-<head>.json` → fix rounds → merge `--no-ff` with the task's integration requests. Merge order: **G4U → D4 → G2 → E3B2 → D5**; W4 phase A when reviewed.
4. At D4's CONFIRMED head: dispatch **D5** (worktree codex-d5, branch codex/d5-terminal-transaction, port 55436/55467; coordinator merges claude/backend-impl into it first; brief `.claude/handoff/wave3/D5.md` — addendum at the end covers the cancel-cause rules). D5 owns 0018 (settlement + cancel cause), CatalogDirectory (G1R 2b), G6B adapters, conformance promotion (lift the `_D5` and cancel-cause PENDING lines), Q3 rig Pg mode.
5. G3 at G2's handback; W4 phase B after the coordinator re-measures; E4B last.
6. Keep pushing lane branches (`git -C ../<wt> push origin <branch>`) at each handback; update the tracker (`progress.py`, then republish `progress.html` to the rey artifact) after each state change; refresh this handoff at each checkpoint; at ~90% of the window stop dispatching, write the handoff, commit, push, idle.

## OPEN ITEMS (not blocking merges)
G1R request 10 ruling (next free **R86**; R85 = R-A1 retention); IR-DTO (console); Q Valkey runner env passthrough; `Admission.price_snapshot` optional revision; 08 §5 settings rows + four wire-in ruling candidates; I3B `--max-age 180` alert timer (no unit yet); hosted backup rehearsal before any hosted apply (hosted has only 0001–0002); box rollout per I2B runbook (not done; `--max-num-seqs 32` unit vs serve.sh refusal → same deploy as W3's serve.sh); HF_TOKEN registry oids (user input; do not retry the box credential); E1 corpus byte-reproducibility per CPU; task ports lie in the kernel ephemeral range (bind collisions possible; D4 derives the decoy port). P-20: engine encoder-cache limit ~72 s vs profile 120 s → interim `MAX_VIDEO_SECONDS=72` at cutover (configuration, not G2 code); W4 decides.

## TRAPS
- `cd apps/infrx-api && setsid … &` backgrounds the whole `cd` subshell; always `(cd … && setsid …)`.
- `pkill -f <string>` kills your own shell when the string is in your command line.
- progress-state `inputs` entries are dicts {id,what,blocks,owner}.
- Workflow result files wrap JSON in `{"summary",…,"result"}` → `raw.rfind('{',0,idx)` + `JSONDecoder().raw_decode`; per-agent results are in the run's `journal.jsonl`.
- Port 55432 is shared (HarnessBusy = retry). A whole-tree run failing dozens of D/Pg tests = port contention; rerun on a free task port (`INFRX_D_TASK=d3` is free now that D3 is merged).
- tasks.json status changes need `validate_plan.py --write-ledger`.
- Lane-numbered rulings collide: renumber at merge.
- `git stash` is shared across worktrees: never use it.
- Other accounts' artifacts cannot be updated: publish a fresh artifact per account and record its URL in progress-state `tracker_url`.
