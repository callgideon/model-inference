# Contracts v1 — executable encoding (F2 design refinement)

> **2026-09-21 wave-2 reconciliation:** This records the implemented v1 encoding and its historical rulings. Product-v2 requirements in `01` are a target; F2R/F2P must encode them before new adapters consume them. Do not treat target documentation as already-migrated types/SQL. See [the audit](10-wave2-platform-audit.md) and [revision tasks](11-wave3-revision-handoffs.md).

Coordinator-owned refinement of [contracts v1](01-contracts.md) that F2 encodes and every track consumes. It fixes the choices contracts v1 left to F2 (“F2 chooses concrete import/type syntax once”): file layout, vocabulary, serialization, configuration names, dependency set, test discovery and local-service namespaces. Where this file and `01`/`02` disagree, `01`/`02` win and the disagreement is a contract-revision request to the coordinator. Nothing here is a claim that code exists; F2 evidence records what was built.

## 1. Layout and ownership after F2

| Path | Content | Owner after F2 |
|---|---|---|
| `apps/infrx-api/pyproject.toml`, `uv.lock`, `.python-version` | Pinned Python environment and pytest configuration | Coordinator |
| `apps/infrx-api/infrx/config.py` | Settings object; the only module that reads the environment | Coordinator (tracks request names) |
| `apps/infrx-api/infrx/contracts/` | `ids`, `money`, `errors`, `records`, `ports`, `limits`, `fixtures/v1/*.json`, `fakes/`, `conformance/` | Coordinator; a change is a contract revision |
| `apps/infrx-api/tests/contracts/` | Runs every conformance suite against the fakes; fixture round-trips | F / coordinator |
| `apps/infrx-api/infrx/{auth,media,gateway/routes}/` | Extracted by F1 | G, M, G |
| `apps/infrx-api/infrx/{state,scheduling,worker,traces,judge}/`, `tests/{d,q,w,t,j,m,g}/` | Created by their tracks | D, Q, W, T, J, M, G |
| `apps/app/lib/contracts/` | TS DTO types, service interface, money helpers, fixtures, fixture-backed fake services | Coordinator; a change is a contract revision |
| `apps/app/tests/contracts/`, `apps/app/tests/{c,u,v}/` | Console contract tests; track suites | F / C, U, V |
| `Makefile` (repo root) | The canonical check commands | Coordinator |

Composition roots (`infrx/gateway/app.py`, console `layout.tsx`/navigation) stay coordinator-owned: tracks hand back an integration request naming the router/import/nav entry instead of editing them.

## 2. Python encoding

- Records are frozen pydantic v2 models (pydantic is already a FastAPI dependency) with `schema_version: int = 1`, `extra="forbid"`, UTC-aware datetimes serialized as RFC 3339 with `Z`, UUIDs as lowercase strings, money as decimal strings (§4). `records.py` holds exactly the contracts-v1 table: `AuthContext`, `NormalizedRequest`, `PriceSnapshot`, `Admission`, `Lease`, `Chunk`, `TerminalOutcome`, `TraceEnvelope`, `Feedback`, `JudgeRun`, plus the small value types they need (`MediaRef`, `Cursor`, `CapacityReservation`, `Usage`, `ConsentSnapshot`, `IndexEvent`).
- Ports are `typing.Protocol` classes with `async` methods in `ports.py`, one per row of the contracts-v1 ports table, same operation names. They take and return records, raise `DomainError` subclasses, and receive clock/ID/storage collaborators by constructor injection. No port exposes SQL, keys of another tenant or a raw object path.
- `fakes/` holds one in-memory adapter per port plus `FakeClock`, `SequentialIds` and `FailurePlan` (deterministic injection: fail/raise/crash-after-commit on the *n*-th call of a named operation). Fakes implement the real semantics of `02-durable-protocols.md` (idempotent admission, capacity and hold accounting, generation fencing, publication marker, single terminal settlement, bounded trace offer); they are the executable specification, not stubs that return canned values.
- `conformance/` holds importable suites `run_<port>_conformance(factory)`; `tests/contracts/` applies them to the fakes, and each owning track must apply the same suite to its real adapter (D to PostgreSQL, Q to memory and Valkey, W to the engine adapter, T to the spool sink, M to its store). Passing the suite on a fake never marks a task integrated.
- The fake engine supports the fault modes `04-verification.md` requires: prefill stall, mid-stream stall, malformed/missing usage, cancellation race, abrupt exit, reasoning delimiters split across chunks.

## 3. Vocabulary (string values are frozen)

| Enum | Values |
|---|---|
| `JobState` | `preparing`, `queued`, `running`, `succeeded`, `failed`, `cancelled`, `expired` |
| `ExecutionMode` | `sync`, `stream`, `async` |
| `TerminalCause` | `completed`, `client_cancelled`, `client_disconnected`, `sync_deadline`, `queue_wait_expired`, `deadline_exceeded`, `invalid_media`, `preparation_failed`, `engine_error`, `engine_incomplete`, `lost_after_publication`, `journal_write_failed`, `retries_exhausted`, `platform_error` |
| `UsageCertainty` | `authoritative`, `unknown` |
| `SettlementState` | `settled`, `released_free`, `held_unknown`, `released_platform_absorbed` |
| `HoldState` | `held`, `settled`, `released`, `unknown` |
| `ReservationKind` | `preparation`, `inference`, `journal_bytes` |
| `ChunkEventType` | `progress`, `delta`, `usage`, `error`, `terminal` |
| `OutboxKind` | `prepare_dispatch`, `inference_dispatch`, `usage_projection`, `trace_projection`, `feedback_projection`, `judge_projection`, `callback_delivery` |
| `TraceMode` | `off`, `minimal`, `full` |
| `TraceLossReason` | `none`, `memory_budget`, `metadata_budget`, `queue_full`, `disk_budget`, `disk_error`, `shutdown`, `malformed`, `abandoned` (R37) |
| `TraceOfferResult` | `accepted_in_memory`, `dropped` |
| `FeedbackChannel` / `AuthorRole` | `api`, `console` / `customer`, `operator`, `judge` |
| `JudgeRunState` | `dry_run`, `reserved`, `submitting`, `submitted`, `ambiguous`, `collecting`, `settled`, `quarantined`, `cancelled` |
| `UploadState` | `created`, `finalized`, `aborted`, `expired` |
| `Role` | `owner`, `member`, `operator`, `service` |

Public identifiers: `request_id` UUIDv4 string; `Inference-Id` header = `request_id`; chat id `chatcmpl-<request_id>`; `job_handle` = `job_` + 32 bytes from `secrets.token_urlsafe` (never derived from the UUID); upload handle `upl_…`, feedback id `fb_…` likewise opaque. Stream cursor / SSE `id` = `<generation>-<sequence>` (decimal integers).

### Error envelope

`{"error": {"message", "type", "code", "param", "request_id", "infrx": {…optional retry_after_s etc.}}}`. `message` is a fixed safe string per code; never upstream exception text, URLs with credentials, storage keys or secrets.

| HTTP | `type` | `code` values |
|---|---|---|
| 400 | `invalid_request_error` | `invalid_request`, `unsupported_parameter`, `unsupported_media`, `media_fetch_failed`, `context_length_exceeded`, `invalid_cursor` |
| 401 | `authentication_error` | `invalid_api_key` |
| 402 | `insufficient_quota` | `insufficient_credit` |
| 403 | `permission_error` | `forbidden`, `org_suspended`, `model_not_entitled` |
| 404 | `not_found_error` | `not_found` (also every ownership failure) |
| 409 | `conflict_error` | `idempotency_conflict`, `result_pending`, `state_conflict` |
| 410 | `gone_error` | `result_expired`, `journal_expired`, `replay_gap`, `idempotency_expired` |
| 413 | `invalid_request_error` | `request_too_large` |
| 429 | `rate_limit_error` | `capacity_exhausted`, `journal_capacity_exhausted`, `rate_limited` (always `Retry-After`) |
| 500 | `server_error` | `internal_error` |
| 503 | `server_error` | `dependency_unavailable` (always `Retry-After`) |
| 504 | `server_error` | `deadline_exceeded` |

In-stream terminal error events use the same object with codes `stream_interrupted` or `status_unknown`; `status_unknown` never asserts a committed terminal state. Internal (non-HTTP) domain errors additionally include `stale_lease`, `already_terminal`, `not_claimable`, `capacity_unavailable`, `budget_exceeded`, `consent_missing`, `ambiguous_submission`.

Headers: in `Authorization`, `Idempotency-Key` (≤255 chars), `Prefer: respond-async`, `Last-Event-ID`; out `Inference-Id`, `Preference-Applied: respond-async`, `Retry-After`, `Idempotency-Replayed: true`, `Server-Timing`. The old `X-Infrx-Accept-Async` upgrade header is superseded by `Prefer`.

## 4. Money

`Decimal` only, context precision ≥ 40, scale `0.00000001` USD. Debit = `(prompt_tokens × input_rate + completion_tokens × output_rate) / 1_000_000`, quantized once with `ROUND_HALF_UP`. Maximum hold uses the same formula on the ceilings with `ROUND_CEILING`. JSON form is a fixed-point string with exactly eight fractional digits (`"0.10000000"`); parsers accept any plain decimal string and reject exponents, NaN, floats and negative zero. TypeScript never uses `number` for money: `Money` is a branded string with BigInt-scaled (1e8) `parse/format/add/sub/compare` helpers. Fixtures include the half-up boundary, ceiling-vs-half-up divergence, zero-token requests, negative ledger deltas and a 20-digit total.

## 5. Configuration names and frozen defaults

Names reuse the older spec where the meaning is unchanged. All are read only in `infrx/config.py`; values are the provisional defaults of contracts v1.

| Name | Default | Name | Default |
|---|---|---|---|
| `INFRX_MODE` | no default — see R44 (`dev`/`test`/`pilot`; `pilot` refuses to start unauthenticated or unmetered; unset refuses to start since the cutover) | `DATABASE_URL` | unset (required in `pilot`); since the cutover `create_app` builds D5's catalog, D4's journal and the job store on one pool from it (`pilot.adapters_from_env`) |
| `MAX_REQUEST_BYTES` | 100663296 | `INTAKE_TIMEOUT_S` | 30 |
| `MAX_MEDIA_BYTES` | 67108864 | `MAX_VIDEO_SECONDS` | 120 |
| `MEDIA_FETCH_CONNECT_TIMEOUT_S` | 3 | `MEDIA_FETCH_TIMEOUT_S` | 20 |
| `MEDIA_FETCH_MAX_REDIRECTS` | 3 | `PROBE_TIMEOUT_S` | 10 |
| `PREPARATION_TIMEOUT_S` | 120 | `TRANSCODE_MIN_TIMEOUT_S` / `TRANSCODE_DURATION_FACTOR` | 15 / 0.5 |
| `QUEUE_WAIT_INTERACTIVE_S` | 10 | `QUEUE_WAIT_ASYNC_S` | 600 |
| `GENERATION_TIMEOUT_S` | 300 | `TTFT_TIMEOUT_S` / `TPOT_STALL_S` | 60 / 20 |
| `LEASE_TTL_S` | 120 | `LEASE_HEARTBEAT_S` | 40 |
| `MAX_PREPUBLICATION_RETRIES` | 2 | `SSE_KEEPALIVE_S` | 10 |
| `STREAM_BATCH_MS` | 50 | `JOURNAL_EVENT_MAX_BYTES` | 1048576 |
| `JOURNAL_JOB_RESERVE_BYTES` | 16777216 | `JOURNAL_TOTAL_BYTES` | 1073741824 |
| `JOURNAL_CHUNK_TTL_S` | 3600 | `RESULT_TTL_S` | 86400 |
| `PROCESSING_CACHE_TTL_S` | 604800 | `IDEMPOTENCY_TTL_S` | 86400 (after terminal) |
| `TRACE_CONTENT_MAX_DAYS` | 90 | `TRACE_METADATA_MONTHS` | 13 |
| `PREPARATION_CONCURRENCY` | 2 | `MAX_ACTIVE_JOBS` / `_PER_ORG` / `_PER_KEY` | 64 / 16 / 8 |
| `ENGINE_MAX_NUM_SEQS` | 8 | `WORKER_CONCURRENCY` | 10 |
| `MAX_OUTPUT_TOKENS` | 2048 | `MAX_CONTEXT_TOKENS` | 32768 |
| `TRACE_CAPTURE_BYTES` | 268435456 | `TRACE_METADATA_RESERVE_BYTES` | 8388608 |
| `TRACE_QUEUE_MAX` | 10000 | `TRACE_SPOOL_DIR` | unset (capture disabled) |
| `TRACE_SPOOL_MAX_BYTES` | 10737418240 | `TRACE_SPOOL_MIN_FREE_BYTES` | 2147483648 |
| `TRACE_FSYNC_INTERVAL_S` | 2 | `JUDGE_MODE` / `JUDGE_LIVE_BUDGET_USD` | `dry_run` / `0` |
| `VALKEY_URL` | unset ⇒ memory scheduler (harnesses); the gateway composition refuses without it unless an index is injected | `CLICKHOUSE_URL`, `S3_TRACE_BUCKET` | unset |
| `S3_MEDIA_BUCKET` | unset ⇒ `create_app` refuses to start in every mode, naming it: the object store that outlives the process has no other source, and the gateway never stages media in process memory (02 step 1). Set, `create_app` builds M1-L2's `S3ObjectStore` on it (`S3_MEDIA_PREFIX`, `S3_ENDPOINT_URL`, §5.1) and refuses unless it answers HeadBucket; a pilot `preflight.py apply` asks it from the host first (`bucket_problems`). Tests inject `objects=` | | |
| `PROCESSING_CACHE_DIR` | unset ⇒ no local processing cache; set, an absolute path (R61 (2)) | | |
| `MAX_INDEX_ITEMS` / `MAX_INDEX_BYTES` | 500 / 268435456 (Q1's scheduler index caps; moved from §5.1 by the F2P wire-in, Q2 request 3; zero refuses startup) | `ACTIVE_RATE_CARD_VERSION` | unset ⇒ no CREDIT rate card approved; set, exact text (padded refuses startup); required when `ACCOUNTING_REGIME=credit` (`validate_runtime`) |
| `PROVIDER_DEV_ALLOCATION_CEILING_CREDIT` | 0 (a `Credit` amount, never negative; the most one audited operator allocation may move into a provider dev wallet; not enforced until the allocation port exists ⚠️ TO BE VERIFIED, P-08) | | |

### 5.1 Deployment configuration (added by F2R item 7)

Deployment knobs rather than contract data: they tune one process rather than stating a number both language halves enforce, so they live in `config.DeploymentSettings` (still `infrx/config.py`, still the only environment read) instead of `contracts/limits.PilotSettings`. Folding the two objects into one later moves these names, defaults and checks unchanged.

Read by the same rules as the table above with one deliberate difference: an **empty** value is refused rather than read as unset (`MAX_MESSAGES=` in a unit file is a mistake, and serving the default for it is how an operator comes to believe they configured something they did not). A name that is absent altogether takes its default. `config.validate_runtime` calls `config.validate_deployment` **before anything mounts**, in every mode, so a value that cannot serve is a startup failure rather than a surprise on the first request that reaches it: every number here is a size or a cap that a zero or a negative disables rather than tightens.

| Name | Default | Notes |
|---|---|---|
| `TRACE_SPOOL_SEGMENT_BYTES` | 16777216 | one spool segment; T1 keeps a local default, T2 reads this ⚠️ TO BE VERIFIED against a real spool |
| `CONSOLE_CURSOR_SECRET` | unset | signs C's keyset cursors; **the console runtime's requirement, not this gateway's** — set, it must be ≥ 16 characters in any mode; unset, the gateway still starts, because a process that serves no console page has nothing to sign. C2 requires it, and the G2/I2 deployment checklist must write it |
| `DATABASE_POOL_MIN_SIZE` / `DATABASE_POOL_MAX_SIZE` | 1 / 10 | min may not exceed max; the ONE pool the catalog, the journal and the job store share (opened by the lifespan; before it opens, the startup probe connects on its own, configured alike) ⚠️ TO BE VERIFIED — D2 owns the measured numbers |
| `DATABASE_POOL_CONNECT_TIMEOUT_S` | 5 | ⚠️ TO BE VERIFIED |
| `DATABASE_POOL_STATEMENT_TIMEOUT_MS` | 15000 | zero means *no* limit in PostgreSQL, so zero is refused ⚠️ TO BE VERIFIED |
| `MAX_MESSAGES` / `MAX_PARTS` | 64 / 16 | G1's structure caps (today module constants in `gateway/routes/validate.py`) |
| `MAX_TEXT_CODEPOINTS` / `MAX_URL_CHARS` | 131072 / 8192 | same |
| `MAX_NUMBER_DIGITS` | 20 | bounded number parsing (`gateway/routes/intake.py`) |
| `LARGE_BODY_LIMIT` / `LARGE_BODY_THRESHOLD_BYTES` | 2 / 1048576 | one `LargeBodies` per process |
| `ACCOUNTING_REGIME` | `legacy_usd` | the regime new admissions are written in and the gateway dispatches on, `legacy_usd` or `credit` (any other value refuses startup, `validate_deployment`); `credit` also needs `ACTIVE_RATE_CARD_VERSION` (§5). F2P wire-in; D1R's database flags gate the same switch inside the transaction |
| `S3_MEDIA_PREFIX` | `infrx/` | M1-L2: where in `S3_MEDIA_BUCKET` (§5) the media object store keeps its objects (`media/`, `payloads/`, `uploads/` under it); path segments, each ending in `/`, else startup refuses (`validate_deployment`). No deployment's prefix may be a prefix of another's on one bucket: a collector lists and sweeps its whole prefix |
| `S3_ENDPOINT_URL` | unset (AWS S3) | M1-L2: an S3-compatible endpoint (MinIO in tests), addressed path-style; `http(s)://host[:port]` only (no path, query or credentials), else startup refuses. Credentials and region are never settings: botocore's chain reads them (the instance role on the box). With either, `create_app` refuses to start in every mode unless `S3_MEDIA_BUCKET` is set and answers HeadBucket (`pilot.object_store`), and a pilot `preflight.py apply` asks it from the host first (`bucket_problems`) |
| `INFRX_RELEASE_SHA` / `INFRX_IMAGE` | unset | the commit `install.sh` deployed (its HEAD, `--release`) and the runtime image id it built (`sha256:<64 hex>`), written by preflight `apply` (MANIFEST, required in `pilot`); the gateway exposes them as `infrx_build_info{revision, image} 1` on its loopback-only `/metrics` from startup, never read from git or docker at runtime (E4B's served-build check). A `pilot` without either refuses to start (`pilot.build_info`), and a malformed one refuses in every mode (`validate_deployment`) |
| `WORKER_HEALTH_PORT` | 8002 | I2B-R4 (W3 request 6): the loopback port of `python -m infrx.worker` (`/readyz`, `/livez`, `/metrics` with the same `infrx_build_info`); bound only after the pool, the bucket and `PROCESSING_CACHE_DIR` answered. Read by the runtime, never written by the installer (`preflight.NOT_SETTABLE`): install.sh's `wait_ready` and `60-verify-local.sh` probe 8002 |
| `PREPARATION_CONCURRENCY` | 2 (§5's `PilotSettings` default, not restated) | PREP-WORKER: the number of runners `python -m infrx.worker` runs on `prepare_dispatch` candidates (its preparation pool, beside `WORKER_CONCURRENCY`'s inference pool), and M2's per-process materialization gate; still gates nothing at admission (R1: `MAX_PREPARING_JOBS` does). Settable through the installer (`preflight.TUNABLE`). Its drain bound is `PREPARATION_LEASE_TTL_S`: an attempt still running at a SIGTERM is released and its lapsed lease requeued by the next `recover` (R93) ⚠️ TO BE VERIFIED on the box (no measured preparation latency) |

The four `DATABASE_POOL_*` names spell the prefix in full; `11 §F2R` item 7 wrote them as `DATABASE_POOL_MIN_SIZE/MAX_SIZE/CONNECT_TIMEOUT_S/STATEMENT_TIMEOUT_MS`, which this reads as one shared prefix rather than four different ones. The G1/Q1 constants are **not yet reads of these settings**: `validate.py`, `intake.py` and `scheduling/memory.py` belong to other tracks, so the wiring is an integration request, and `tests/contracts/test_config_and_imports.py::test_the_g1_and_q1_constants_match_the_deployment_defaults` pins the constants against these defaults so the two cannot drift while it is open.

Existing F1 settings (`UPSTREAM`, `MODEL_ID`, `GATEWAY_API_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `MAX_INFLIGHT`, `USAGE_LOG`, …) keep their names until G/W retire them. Schema versions: records 1, error envelope 1, trace envelope 1, spool segment **2** (raised from 1 by T1's checksummed length-prefixed format; a version-1 segment is classed `unreadable`, never misread, and none exists outside T1's own tests), rubric versioned by J.

## 6. Dependencies (pinned once by F2)

`pyproject.toml` + `uv.lock`, Python 3.12, installed with `uv sync --frozen`. Core: `fastapi`, `uvicorn`, `httpx`, `pydantic`. Extras so the request path imports only what it uses: `state` = `psycopg[binary,pool]` 3.x; `scheduling` = `valkey`; `traces` = `clickhouse-connect`, `boto3`; `judge` = `anthropic`; dev group = `pytest`. No pytest plugins: async tests drive `asyncio.run` as the baseline tests do. A track needing another package requests it from the coordinator; nobody else edits the manifest or lock. Console: `packageManager` stays `pnpm@9.15.9`, `engines.node` `>=22.18` (built-in TypeScript stripping), no new console dependency in F2. React component behaviour is verified through pure `.ts` view-model tests in the track suites and E's browser suite; F2 adds no DOM test stack.

## 7. Test discovery and canonical commands

- Python: `[tool.pytest.ini_options]` in `apps/infrx-api/pyproject.toml` with `testpaths=["tests"]`, `pythonpath=["."]`, `--import-mode=importlib` so same-named files in track directories cannot collide. Track suites live in `apps/infrx-api/tests/<track>/`.
- Console: `pnpm test` = `node --test "lib/**/*.test.ts" "tests/**/*.test.ts" "app/**/*.test.ts" "components/**/*.test.ts"` (patterns quoted so Node expands them). A guard test walks the tree and fails when any `*.test.ts` file is outside those patterns: an undiscovered test is a failed setup.
- Root `Makefile`: `make api-test`, `make console-test`, `make console-lint`, `make bench-test` (E's `models/marlin2b/tests`), `make check` (all of them). Evidence reports cite these.

## 8. Task-local services

Real-service tests use disposable containers named `infrx-<task>-<service>`, database `infrx_<task>`, object prefix `test/<task>/`, temp directories under the session scratch area, and these host ports so parallel worktrees never collide: PostgreSQL D `55432`; Valkey Q `56379`; ClickHouse T `58123` (HTTP) / `59000` (native); S3-compatible store M `59100`, T `59110`; E2 compose `55500–55599`; G/W/J/C use fakes until integration. Never point a test at Supabase production, AWS or a container another session created. D alone adds SQL migrations, numbered with the next four-digit integer after `0002`.

## 9. Console DTO surface (TypeScript)

`apps/app/lib/contracts/services.ts` declares the named operations of the contracts-v1 console row — `usage`, `balances`, `ledger`, `traces`, `traceDetail`, `traceContent`, `feedback.list/submit`, `settings.get/update`, `adminGrant`, `adminOrgs`, `judgeRuns` — each taking a trusted `SessionContext` (user, org, role, operator flag) resolved server-side plus validated filters, returning `Result<T, ServiceError>` where `ServiceError = {code, message, request_id?}` with the codes of §3. Lists use `Page<T> = {items, next_cursor}` with opaque cursors and a hard `limit ≤ 100`. `WalletBalance = {ledger_total, reserved_total, available}` as `Money`. Trace content availability is the closed set `available`, `metadata_only`, `pending`, `lost`, `expired`, `off`. Feedback DTOs carry `channel` and `author_role` as server-set fields the client cannot supply. The fixture-backed fake implements the same interface with owner/member/operator sessions, two organizations (for cross-tenant denial), pagination over >100 rows, every content state and injected failures, so U and V build against it while C implements the real services behind the identical interface.

## 10. Contract revision r1 — coordinator rulings (2026-09-20)

Rulings on the change requests raised while encoding F2. Both halves implement these; conformance suites and fixtures follow them.

| # | Question | Ruling |
|---|---|---|
| R1 | `PREPARATION_CONCURRENCY` vs admission | `PREPARATION_CONCURRENCY=2` is the host worker-pool size only. Admission reserves a `preparation` capacity unit against a separate cap `MAX_PREPARING_JOBS` (default 8, provisional); exhaustion is `429 capacity_exhausted`. Both reservations are taken in the admission transaction as `02` requires. |
| R2 | New limits vs F1 legacy settings | F1's `FETCH_TIMEOUT_S` (30), `MAX_VIDEO_MB`, `MAX_REDIRECTS` keep their names, defaults and behaviour until M1 replaces the fetch path. The contracts-v1 limits for the new path are named `MEDIA_FETCH_TIMEOUT_S` (20), `MEDIA_FETCH_CONNECT_TIMEOUT_S` (3), `MEDIA_FETCH_MAX_REDIRECTS` (3), `MAX_MEDIA_BYTES`; this supersedes the unprefixed fetch names in §5. M1 retires the legacy names at cutover. |
| R3 | Feedback body | Field semantics follow `research/traces/06` §2: `name` ∈ `thumb`, `rating`, `correction`, `comment`; `value` is boolean for `thumb`, integer 1–5 for `rating`, non-empty text for `correction`/`comment`; optional `comment`. An empty body is `400 invalid_request`. `Idempotency-Key` is required on `POST /v1/feedback` and console submit. `channel`, `author_role`, calibration membership remain server-set. |
| R4 | Deadlines | `NormalizedRequest`/job carries `accepted_at`, absolute `deadline_at`, and a `budgets` snapshot `{preparation_s, queue_wait_s, generation_s, first_token_s, stall_s}` captured at admission so later configuration changes never alter an accepted job. |
| R5 | Queue wait across requeues | Cumulative from the first durable `queued` transition; prepublication requeues do not reset it. |
| R6 | Re-admitting an existing `request_id` without the idempotency path | `409 state_conflict`, no side effects. Replay exists only through org + operation + idempotency key. |
| R7 | `recover` clock | `JobStore.recover()` takes no caller time. Adapters read the database clock inside the transaction; fakes read the injected clock that stands for it. Same rule for every expiry/lease/24 h decision. |
| R8 | Ambiguous judge run | New operator-only operation `JudgeCoordinator.resolve_ambiguous(run, operator, resolution, reason)` with `resolution` ∈ `adopt_provider_evidence` (requires the provider id; continues to collection) or `release_reservation` (terminal `quarantined`, reservation released); append-only audit record; never creates a second submission. |
| R9 | Judge consent | Consent is rechecked against the **current** consent record at `begin_submit`, not only against the snapshot taken at `reserve`; revocation after reservation blocks egress and releases the reservation. |
| R10 | Tenant coherence | Every port operation receiving two tenant-bearing arguments verifies they name the same organization and fails `not_found`/`forbidden` otherwise. Admission rechecks key revocation, org suspension and entitlement inside its transaction (`01` identity section); the fake models this with an injectable entitlement source. |
| R11 | Monetary inputs | Holds, grants via `adminGrant`, settlements and judge costs reject negative, non-finite or over-scale amounts at the port boundary; ledger deltas may be negative only for debits and compensating entries created by the store itself. Money domain is `numeric(20,8)` in both languages: |value| < 10^12, exactly the same accept/reject set in Python and TypeScript. |
| R12 | Trace mode vs content | A `minimal`-mode envelope never carries content; a sink rejects (`malformed`) rather than stores it. All accepted bytes are charged to the budget. |
| R13 | Console amendments | Accepted: `keys.list/create/revoke` in `ConsoleServices`; `adminGrant.target_org_id` (operator-only; the idempotency scope includes target org and payload); separate `usageSummary`/`usageDaily`; `TraceMode` `off`/`minimal`/`full` supersedes `07-console-spec`'s `metadata`; `UsageRow.settlement_state` is nullable for non-terminal rows; `judgeRuns` is owner and operator only. Amended: `LedgerEntryKind` keeps `purchase` as a legacy read-only value (historical rows must render; nothing creates it). Off-mode requests have no trace row, so they never appear in `traces`; `traceDetail` reached from a usage row reports availability `off`. `ERROR_CODE_HTTP_STATUS` is accepted and must equal the Python table (checked by the G0 parity test). `tsconfig` target stays ES2017 (use `BigInt()` calls). |
| R14 | Module list | §1/§2 also include `wire.py` (HTTP envelopes) and `tasklocal.py` (§8 helper). `UNKNOWN_USAGE_RECONCILE_S` = 86400 joins §5. |
| R15 | E/I ownership | E owns `models/marlin2b/corpus/**` and `models/marlin2b/tests/**` (run by `make bench-test`). I owns `infra/**` except `infra/clickhouse/`. Pinned tool downloads must use a versioned URL plus sha256. Raw evidence stays in git-ignored scratch with digests in reports until I2 provisions a durable evidence store. Journal-probe thresholds in `infra/README.md` are `est.` and become a gate only after E4 measures them. |
| R16 | Console idempotency beyond grants | `keys.create`, `keys.revoke` and `settings.update` accept an optional `idempotency_key` (same scope rule). A replayed `keys.create` returns the key's metadata with `secret: null` and `replayed: true`; a secret is shown exactly once, to the first response only. |
| R17 | Console input bounds | `MAX_KEY_NAME_CHARS` = 200, `MAX_GRANT_REASON_CHARS` = 500 (provisional). Unknown input fields are `invalid_request` in both languages (`extra="forbid"`). |
| R18 | Suspended organization fixture | The console harness includes a suspended organization and session so `org_suspended` is reachable; suspension never alters existing terminal accounting. |
| R19 | Operator operations for U3/V3 | `ConsoleServices` gains operator-only `adminSetSuspension(session, {target_org_id, suspended, reason, idempotency_key})`, `adminSetEntitlements(session, {target_org_id, model_ids, limits, reason, idempotency_key})` and `calibration.label(session, {request_id, rubric_version, label, idempotency_key})` / `calibration.list`. Calibration labels are the only path to `author_role=operator` with calibration membership; ordinary console feedback from an operator session stays `customer`. Each is audited and idempotent. |
| R20 | Per-phase deadlines | `deadline_at` (absolute) and the `budgets` snapshot are fixed at admission. The store derives and persists each phase instant at the transition into that phase, from the database clock and never beyond `deadline_at`: `preparation_deadline_at` at admission, `queue_deadline_at` at the first `queued` transition (cumulative, R5), `generation_deadline_at` and `first_token_deadline_at` at claim. Workers and the reaper compare against the persisted instants. |
| R21 | Billable terminal causes | Only `completed`, `client_cancelled` and `client_disconnected`, and only with authoritative usage, settle a debit. `sync_deadline`, `deadline_exceeded`, `queue_wait_expired` and every other cause are platform-absorbed (`released_free`, or `held_unknown` → `released_platform_absorbed` when usage is unknown). `02` makes platform-caused failures free; the platform's own deadlines are platform-caused. |
| R22 | Upload expiry code | New `410 gone_error` code `upload_expired` (fixed message "The upload window has expired."). Both halves and `error_codes.json` add it; `result_expired` is no longer used for uploads. |
| R23 | `resolve_ambiguous` shape | The provider id stays the keyword `external_id`, required when `resolution = adopt_provider_evidence` and refused otherwise. |
| R24 | Entitlements | `OrgEntitlements` = `model_ids` plus integer limits from the closed `ENTITLEMENT_LIMIT_NAMES` set; D1 aligns its schema to it. `model_ids: null` = platform default set, `[]` = nothing entitled (every admission `model_not_entitled`), a non-empty list = exactly that set. |
| R25 | `journal_write_failed` | Both a `TerminalCause` and an internal-only domain error (raised by `StreamStore.append` for an event over `JOURNAL_EVENT_MAX_BYTES`; never an HTTP status). Code sets in both languages: 27 HTTP, 2 in-stream, 8 internal. |
| R26 | Operator scope | Platform operators are platform-wide in the pilot; `calibration.label` may reach any organization's trace, tenant taken from the row, audited with the operator principal. |
| R27 | Trace accumulation | `TraceSink` gains `open(request_id, org_id, mode) -> TraceCapture`; `TraceCapture.add(part)` charges bytes to the capture budget as content accumulates and returns `False` once the budget is breached (the whole content capture is then discarded and marked lost); `finish(envelope)` hands the completed capture to the bounded queue, `abandon(reason)` releases its bytes. `offer(envelope)` remains for metadata-only envelopes. Off-mode never opens a capture; an off-mode `offer` is dropped and counted `malformed`, never raised. |
| R28 | Consent revoked while `submitting` | Release on revocation (R9) applies only from `reserved`. From `submitting` the submission may be in flight: the run becomes `ambiguous`, reservation held, resolved only through R8. |
| R29 | Deadlines bind every fenced mutation | `heartbeat`, `append`, `prepared` and `complete` fail once the store clock is past the persisted phase instant or `deadline_at`; the store terminalizes the job (`deadline_exceeded`, or `preparation_failed` past `preparation_deadline_at`) in that same operation, so the outcome never depends on reaper timing. `admit` refuses a `deadline_at` at or before the store's `db_now` and otherwise stores `min(caller_deadline, db_now + preparation + queue + generation budgets)` (audit correction R-3, 2026-09-21; the bound is derived from the store clock, never the gateway's). Leases are renewed from the stored lease; a caller-supplied lease is only a fencing token. |
| R30 | Terminal integrity | `append` refuses `terminal` events from workers; the terminal journal event is derived only from the stored outcome inside the settling transaction. `complete` with `succeeded` requires a `result_ref`. |
| R31 | Feedback provenance (Python half) | `FeedbackService.accept` always records `author_role=customer` for `api` and `console` channels, whatever the session; operator labels exist only through operator-only `label_calibration(auth, request, label, idem)` (R19); `judge` authorship only through the judge projection path. |
| R32 | Conformance strength | Every invariant named by a conformance case must be killable: the suite ships with a mutation list (one single-edit mutant per invariant) and a runner; a surviving mutant is a failed suite. A case skipped for a missing optional hook is reported as skipped, never as run. |
| R33 | Suspension scope | Suspension gates new work and configuration changes only: `keys.create`, `settings.update`, `feedback.submit`, judge operations and (in the API) admission return `org_suspended`. A suspended organization keeps every read (`usage*`, `balances`, `ledger`, `keys.list`, `settings.get`, `traces`, `traceDetail`, `traceContent`, `feedback.list`) and `keys.revoke`, so a leaked key can always be revoked. Existing terminal accounting is never altered. |
| R34 | Operator audit trail | `adminGrant`, `adminSetSuspension`, `adminSetEntitlements` and `calibration.label` each append an immutable audit entry `{id, at, actor_principal, action, target_org_id, reason, before, after, idempotency_key}`; operator-only `adminAudit(session, {target_org_id?, cursor?, limit?})` pages them. A restore never overwrites the earlier suspension entry. A replay returns the original result and the caller re-reads current state. |
| R35 | Calibration label visibility | Calibration labels are operator data: visible only through `calibration.list`; excluded from the customer's `feedback.list`, `traceDetail.feedback`, `feedback_count` and `has_feedback`, and never expose the operator principal to a customer session. |
| R36 | Cursor opacity in conformance | Exported conformance suites treat cursors as opaque: forgery cases are black-box only (cursor from another list, tenant or filter; characters altered). Shape-aware forgeries belong to implementation-specific tests. R32 applies to the console suite too (`pnpm test:mutants`). |
| R37 | Trace capture lifecycle | No trace operation may raise into the request path: `open` for `off`/`minimal` returns a no-op capture; `finish`/`abandon` are idempotent; `TraceCapture` is a context manager whose exit abandons an unfinished capture (G uses it in `finally`). `add` is synchronous, O(1), takes no lock beyond a short critical section and never touches disk. The sink reaps captures older than the job's `deadline_at` plus a grace period, releases their bytes and counts them under the new `TraceLossReason` value `abandoned`. |
| R38 | Queue wait is time spent queued | Amends R5/R20: the queue budget is cumulative time in the `queued` state, not wall time since the first `queued` transition. The store persists `queue_wait_used_s`; at every `queued` transition `queue_deadline_at = min(now + budget − used, deadline_at)`; leaving `queued` adds the elapsed interval to `used`. A prepublication requeue therefore keeps only the unspent remainder, and interactive retries remain possible within `deadline_at`. |
| R39 | Terminalize-then-refuse in a real store | When R29 makes an operation terminalize the job and refuse the caller, the adapter commits the terminalization and then returns the typed refusal; it must not raise inside the transaction that would roll the terminalization back. Every capacity check a settling transaction can fail on (journal bytes for the terminal event) happens before any wallet, outcome or reservation mutation. |
| R40 | F2 merge criterion | F2 merges when the independent reviewer's accumulated mutant corpus and the committed mutant lists are all killed by named exported cases, the mutation runners cannot report a false kill or hide a skip, and evidence is accurate. Survivors found after that are filed against follow-up task F2.1 and block the owning track's integration (not F2) unless they concern money, fencing, tenant isolation, publication/regeneration or secret handling. |
| R41 | Operator principals in customer views | No customer-session view exposes an operator principal (email/id). `LedgerEntry.actor` for a customer session is the literal `platform` for operator-made entries (grants, adjustments) and the member principal for the organization's own actions; the operator principal appears only in operator views (`adminAudit`, operator ledger view). Same rule for consent history and any other actor field. |
| R42 | Trace loss accounting | A capture contributes at most one loss count however it ends and whatever is called on it afterwards (counting is idempotent per capture). An `off`-mode capture is silent: `open`/`add`/`finish`/`abandon` store nothing and count neither a loss nor a drop, because `02` keeps off-mode jobs out of loss and coverage figures; only an `offer` of an off-mode envelope (a caller bug) is dropped and counted `malformed`. The trace suite ships an exhaustive bounded-sequence property test over capture operations (no raise, bytes never negative or leaked, at most one loss and one row per capture, `off` never stores or counts, `minimal` never stores content, lost content never reports `none`, stored content never exceeds charged bytes). |
| R43 | Feedback, calibration and rubric coherence (both halves) | One persisted shape. A calibration label is a feedback entry with `name = calibration_label`, `value` ∈ `correct`, `partially_correct`, `incorrect`, `unusable`, `calibration_set = true` (boolean; false on every other entry), required integer `rubric_version` (1–1000; null on other entries) and optional `comment`. `rubric_version` is an integer everywhere (`JudgeRun`, labels, scores), matching `research/traces/04` (`UInt16`). Python `FeedbackService.label_calibration(auth, request_id, label, rubric_version, idem, comment=None)`. R35/R41 bind the Python half too: a customer `AuthContext` never receives calibration labels from `list_owned` or the wire list, and operator-authored principals read `platform` for customer viewers. Feedback text and comment are bounded at 4000 characters, retention days are 1–90, and feedback `accept` for a suspended organization is `org_suspended` (R33) in both halves. Python gains the `OrgEntitlements` record of R24. |
| R44 | Runtime mode | `INFRX_MODE` has **no default**. `create_app` calls one coordinator-owned hook, `config.validate_runtime(settings)`: `pilot` requires authentication and metering configuration and refuses to start without it; an unrecognised value refuses to start; `dev`/`test` are explicit. While the variable is **unset** the legacy `gateway:app` entry keeps F1 behaviour unchanged (logged as `legacy`), because F1 preserved behaviour by rule; G1 replaces "unset → legacy" with "unset → refuse" at cutover, in the same change in which I2's installer writes `INFRX_MODE=pilot` and fails closed (`M-FAILCLOSED`). Track routers are modules exposing `register(app, rt)`; the coordinator adds them to the composition root on an integration request. |
| R45 | Price source | Prices never come from the request. `JobStore` receives an injectable price source (`price_for(model_revision, at) -> PriceSnapshot | None`, mirroring the entitlement source); admission snapshots from it and an unpriced model is refused. The conformance harness gains a `set_price` hook; `request.parameters["price_snapshot"]` leaves the contract and G1 rejects a client-supplied one as `unsupported_parameter`. |
| R46 | Work loading and preparation fencing | Internal operations address a job by `job_id`; tenant-facing ones by `(org, handle)`. `JobStore.claim_preparation(job_id, worker) -> Lease` and `claim(job_id, worker) -> Lease` both mint fenced leases (`Lease.kind` ∈ `preparation`, `inference`); `prepared(lease, media)` is fenced on the preparation lease (`02` §3). `JobStore.load_work(lease) -> Work{request, media_refs, prepared_refs, price_snapshot, budgets}` is the only way a lease holder reads what it must execute, and is fenced like every mutation. `MediaStore.attach(job_id, refs)` replaces the fake-only hook. |
| R47 | Trace export and content object | The public trace export never carries a storage key: `content_ref` is replaced by availability plus an opaque content handle resolved server-side. The content object is `{v: 1, request, response}` as in `research/traces/04` and the console `TraceContentBody`; Python fixtures follow. |
| R48 | Track test hygiene | Track tests never import the legacy `gateway` shim (directories collect before the top-level legacy files and the shim mutates process state at import); they build apps with `create_app()`. Helper modules use relative imports under importlib mode. Console modules reachable from `node --test` use relative `.ts` imports, never the `@/` alias and never `.tsx`. Coordinator owns `infrx/usage.py`, the `gateway.py` shim, `infrx/__init__.py` and the four top-level legacy tests; M1 and G1 keep every name the shim forwards until G1's cutover retires the shim and its tests together (this supersedes R2's "M1 retires the legacy names"). Task-local PostgreSQL ports are per task: D1 `55432`, D2 `55433`, D3 `55434`, D4 `55435`, D5 `55436`, D6 `55437`, C1 `55441`. |
| R49 | Operator view of feedback | `list_owned`/`feedback.list` never returns calibration labels to anyone, operators included; labels are read only through `list_calibration`/`calibration.list` (R35 as written). Operators do see real principals on ordinary entries. |
| R50 | `by_operator` marker | The persisted feedback shape carries a server-set boolean `by_operator` (true when the accepting `AuthContext`/session is a platform operator; never client-settable; false for labels' customers cannot see anyway). Customer viewers read `platform` as the principal of any `by_operator` entry (R41) in both halves; D1 persists the column; the marker never leaves the service. Same construction for ledger rows and consent history. |
| R51 | Pilot refuses the shared legacy key | In `pilot`, `validate_runtime` refuses to start when `GATEWAY_API_KEY` is set (a shared key is not tenant authentication and bypasses metering), naming the setting only. With `SUPABASE_URL` required, the unauthenticated allow-all path is unreachable in `pilot`. G1 additionally asserts reachability of the price source and journal at startup. |
| R52 | Preparation lease | `PREPARATION_LEASE_TTL_S` = 30, renewable by `heartbeat` (every 10 s) like an inference lease but never past `preparation_deadline_at`; a lost preparation worker is therefore reaped early enough for the bounded retries of R46 to happen under default limits. `MediaStore.attach` verifies that every ref belongs to the job's organization. Dispatch events carry their kind (`prepare_dispatch` / `inference_dispatch`) so a preparation worker can be fed from the index. Entitlement limits reject booleans in both halves. |
| R53 | The store derives the hold | Since R45 only the store knows the rates, `admit` no longer accepts a caller-computed hold: the caller supplies the validated token ceilings (`max_input_tokens`, `max_output_tokens`) and the store computes `maximum_hold` from the snapshot it takes in the same transaction (ceiling rounding, §4). A price change between gateway validation and admission therefore can never produce a hold smaller than the admitted snapshot requires. Settlement and `load_work` always use the admitted snapshot and budgets, never current values. |
| R54 | Text bounds unit and strict records | Every shared character bound counts Unicode code points in both halves (TypeScript uses the code-point length, not `.length`). Records are strict where ports are: `rubric_version` and entitlement limits refuse booleans, strings and floats; a `calibration_label` row requires `author_role = operator`; `TraceExport.content_handle` must carry the opaque-handle prefix; wire lists for a viewer are built only through the projection constructor. Idempotent replays are projected for the replaying viewer exactly like reads, and a replay never returns a row of another kind (a label key replayed through `accept` is `idempotency_conflict`). |
| R55 | Untrusted store inputs | `MediaStore.attach(job_id, refs)` reads the organization from the job row; no caller-supplied organization is trusted (the argument is removed). `admit` range-checks the request's token ceilings inside the transaction: `1 ≤ max_output_tokens ≤ MAX_OUTPUT_TOKENS`, `1 ≤ max_input_tokens`, and `max_input_tokens + max_output_tokens ≤ MAX_CONTEXT_TOKENS`, else `invalid_request` with no side effects; a zero ceiling can therefore never produce a zero hold with free output. `claim_candidate` refuses an unknown kind with a typed error. A feedback row with `author_role = operator` requires `by_operator`. On the preparation path the phase deadline is enforced before lease expiry, so R29's "terminalize in the same operation" holds there too. |
| R56 | Judge sampling policy | Evaluation consent is not retroactive: only traces created at or after the current consent's `effective_at` (and before any `revoked_at`) are eligible. A candidate carries the raw facts (`finish_reason`, HTTP status, `schema_valid`) and the sampler derives strata from them as `research/traces/06` §3.1 does: the failure stratum is `finish_reason = length` or `schema_valid = false`; status ≥ 500 is excluded as `skipped_no_output`. Feedback rows attached to a candidate count only when their organization and request match the candidate's. `by_operator` ordinary feedback is neither calibration nor customer signal, so it does not place a trace in the customer-feedback stratum. Candidates are deduplicated by `request_id` before stratifying (a conflicting duplicate is excluded, never sampled twice), `JudgeRun.sample_ids` are unique, and the scan bound is enforced by the sampler whatever the source returns. A limited (no-media) evaluation can never be an overall pass when any criterion requires media — enforced in code, not by rubric data. |
| R57 | Judge money guard | Live submission is authorized only when the mode is exactly `live`, every rate is a finite positive decimal from the approved table for the run's price version (duplicate model rows are refused; the row effective at the store clock is used), the worst-case total is recomputed by the guard as `ceiling(per_sample × samples)` — never read from a caller-supplied field — and is finite, positive and `≤` the available budget. Anything else (unknown mode, missing/zero/negative/NaN totals or rates) refuses. |
| R58 | Normalized messages and engine events | The pilot request is text plus video only (`01`): a normalized message is exactly `{role, content}` with `role` ∈ `system`, `user`, `assistant` and `content` either a string or a list of parts, each part exactly `{type: "text", text}` or `{type: "video_url", video_url: {ref}}` — one media part per staged ref, in order, the part kind matching the ref's MIME class. Anything else (untyped parts, image/audio/file/embeds parts, extra keys, other roles, tool calls) is refused before admission by G and again by the engine adapter (allow-list, never deny-list), so no customer URL can reach the engine. Engine delta events carry `visible` (customer text after the reasoning filter, including the filter's final held tail) and `raw` (unfiltered model text, for trace capture only); the journal and every customer-facing relay persist and emit `visible` only; the shared fake engine emits both keys. Usage is authoritative only when it is the last usage object of the stream, arrives after the final content delta and is consistent with the stream (`completion_tokens ≥` non-empty deltas); otherwise it is unknown or a protocol violation. Engine cancellation intents are keyed by `(job_id, generation)`; lease loss never uses `Engine.cancel` of another generation. Adapters bound the bytes of a single event and of the accumulated output. |
| R59 | Database-level masking, privileges and tenant coherence (after the D1 review) | (1) R41/R50 hold in SQL and fail closed: a principal (id, email, name) leaves the database towards a customer session only when it is provably a member of that organization (`org_members` join); everything else reads the literal `platform` — a missing, NULL or false `by_operator` never unmasks (history and legacy writers carry no marker). (2) Operator principals and operator free text are stored only in relations customers cannot SELECT (`infrx.*` / the operator audit relation); never as columns of legacy `public` tables customers already read. (3) `by_operator` may be a column of a masked customer view (same information as the literal `platform`); C masks again unless `by_operator === false` and never projects the marker into a DTO. (4) Browser roles get `revoke all` then exactly the enumerated verbs/columns the deployed console needs; append-only relations refuse TRUNCATE by statement trigger for every role; default privileges in `infrx` start empty; the role matrix attacks every verb incl. TRUNCATE and INSERT of protected columns (`api_keys.trace_mode`, `last_used_at`, `created_at`, `id` take defaults for browser roles). (5) Every customer-readable view is `security_barrier`. (6) Tenant coherence is a composite foreign key `(org_id, id)` on every tenant relation (holds, reservations, idempotency, judge reservations/samples, callback deliveries, outbox incl. `org_id`, pilot usage, ledger request link); money sign rules are CHECKs by kind with at most one usage debit per request; constraints history cannot satisfy are scoped to the pilot regime or `NOT VALID` — history is never rewritten. (7) The wallet total moves only by an AFTER INSERT trigger on the ledger, in the same transaction, for every writer; reconciliation stays as the detector. (8) Consent rows are immutable except one NULL→value `revoked_at` transition (`is distinct from`), DELETE refused by the guard itself. (9) D owns the console read surface incl. aggregate RPCs (`console_usage_summary`, `console_usage_daily`: tenant-checked, bounded, with `pending_reconciliation` and `platform_absorbed`); money leaves SQL as text, arrays as arrays, limits as a JSON object of integers; C accepts both `Z` and `+00:00` RFC 3339 forms. |
| R60 | Fairness of an unfiltered claim (`kind=None`) | Selection is two-level. Level 1: each dispatch kind is a top-level flow with its own tag and one top-level virtual time, weight 1, touched ONLY by unfiltered claims; among kinds holding an eligible candidate choose the smallest `(kind tag, arrival sequence of the candidate level 2 would hand out for that kind)` — the pick's sequence, NOT the kind's oldest candidate (corrected 2026-09-21), then `start = max(kind tag, V_top)`, `V_top = start`, `kind tag = start + cost`. Level 2: inside the chosen kind the ordinary per-kind weighted-fair rule applies unchanged (key `(tag, head sequence)`, per-kind virtual time, R-series arithmetic order). Kind-filtered claims never read or write level-1 state. Required properties, each with a killable case: a peer in the other kind waits at most kinds × tenants-in-its-kind unfiltered dispatches AT EQUAL COST, however deep a noisy backlog is and whatever the noisy tenant's weight (corrected 2026-09-21: the first text said tenants + kinds, which the ruled algorithm does not give — measured gaps 2, 4, 6, 10, 16 for 1, 2, 3, 5, 8 tenants; the bound is in service time, not slots: the level-1 charge is the raw cost, so an 11 s preparation against 1 s inference waits 12 slots, and kind-level debt is inherited across tenants of that kind — estimator owners in Q3/W must know); one organization with work in both kinds cannot starve another organization's single-kind work; a new flow arrives at ITS OWN kind's virtual time; rebuild clears level-1 state too. `tag − V[kind]` across kinds is withdrawn (it starves: lag only shrinks when the flow's own kind is dispatched). Q2's Valkey adapter reproduces exactly this rule; its differential run includes unfiltered claims with both kinds holding work. |
| R61 | Customer upload references and the pilot engine media form | (1) The customer-facing upload reference is `infrx-upload:upl_<id>` only — no organization qualifier (the organization comes from the authenticated key; a qualified form invites cross-tenant probing); `UploadCreated.destination_ref` uses the same form; ingress refuses any other spelling. (2) On the single-host pilot the engine receives inline media as `file://<local_path>` where `local_path` is a prepared-ref field written by preparation (M2): the staged object materialized under the processing-cache root, tenant- and profile-prefixed (`<root>/<org uuid>/<profile_version>/<digest16>/source.<ext>`, mirroring the object key; amended 2026-09-22 after the M2 review — two profile versions of one source must never share a file), never caller-derived, expiring with the 7-day processing cache; vLLM runs with `--allowed-local-media-path <root>`; the engine adapter validates the path is inside the root and belongs to the request's organization in addition to the durable `storage_ref` grammar. The durable object key stays the identity; the local path is a serving detail. (3) Generation supplies both EOS ids of the pinned artifact (`[248044, 248046]`) and the `<think>` prefix strip, because the `--hf-overrides` architecture remap drops them (S2M profile). |
| R62 | Model revision string form | The `model_revision` string always carries the public model id: `<public_model_id>@<revision>` (`nemostation/marlin-2b@2026-09-01` in the frozen v1 fixtures). The unprefixed `marlin-2b@2026-09-01` in three console files (`lib/contracts/fixtures/orgs.json`, `fixtures/traces.json`, `README.md`) is a defect: F2R lane B aligns them to the prefixed form. The v1 revision value itself is **not** renamed (frozen v1 evidence; 20 files carry it, including the exported conformance builders). The artifact commit (`fd111fca…`) is pinned by the v2 `serving_version` admission pin (F2P), not by rewriting `model_revision`; G1R's served-model table and D1R's registry rows map `model_revision` → `serving_version`. |
| R63 | Task-local Valkey ports | Q lanes may run their own Valkey on a per-task host port registered in `tasklocal.TASK_PORTS` (`q2` 55461, `q3` 55462; container `infrx-<task>-valkey`), read through `local_services(<task>)["valkey"]` and never hard-coded; the track port 56379 in `TRACK_SERVICES` remains the shared default. §8's PostgreSQL-only task-port table is extended accordingly. |
| R64 | v2 · A unit is a type, not a field | **A unit is a type, not a field.** `Credit`, `Usd` and `ProviderUsd` are sibling runtime classes on the Python side and disjoint brands on the TS side. A unit is never inferred from a field name, a magnitude or a regime label alone; a row states its regime and its unit, and the pair must agree. (F2P V1) |
| R65 | v2 · No conversion exists | **No conversion exists.** No function in either language maps one denomination to another, and none may be added without a product decision recording an exchange rate. The only unwrap is `raw(unit)`, which refuses a foreign unit. A v1 `PriceSnapshot` does not upgrade to a `RateCardSnapshot`; `upgrade_v1_price_snapshot_is_refused` is the named refusal. (F2P V2) |
| R66 | v2 · The wallet is resolved, never named | **The wallet is resolved, never named.** `AuthContextV2` has no wallet field, `resolve_wallet` takes no request, and a wallet not owned by the credential's identity, and of the credential's audience's wallet kind, is `Forbidden` rather than a fallback. The kind is checked explicitly and before ownership: record validators do not protect a `model_copy(update=)`/`model_construct` object. An operator credential resolves no wallet at all. (F2P V3) |
| R67 | v2 · The ledger vocabulary is closed and has no transfer | **The ledger vocabulary is closed and has no transfer.** Provider-dev credit reaching a consumer wallet is prevented by the absence of an operation, not by a check. (F2P V4) |
| R68 | v2 · `settle` cannot see the present | **`settle` cannot see the present.** It takes the admission, the usage and a time — no directory, no clock, no rate. This is how "every request settles at its admitted rate and serving revision" is structural. (F2P V5) |
| R69 | v2 · Unpriced is unserveable | **Unpriced is unserveable.** A deployment with no approved active CREDIT card is `invalid_request`, never free inference. A card that prices another deployment or serving revision is refused rather than pinned. (F2P V6) |
| R70 | v2 · A private dev deployment is `not_found` to everyone else | **A private dev deployment is `not_found` to everyone else**, including a provider credential scoped to a different endpoint or a member of a different provider. A 403 would confirm the artifact exists. (F2P V7) |
| R71 | v2 · The grant key is the identity | **The grant key is the identity.** `(user_id, initial_signup_grant)`; campaign metadata is audit only and can never re-open eligibility. (F2P V8) |
| R72 | v2 · A nonzero legacy USD balance is a rollout hold | **A nonzero legacy USD balance is a rollout hold**, expressed as a required field on the record. It is neither converted nor discarded. (F2P V9) |
| R73 | v2 · Totals are per unit | **Totals are per unit.** No DTO, function or view offers a combined "total spend" across CREDIT and USD, and an empty history answers `{}` rather than zero in an unstated unit. (F2P V10) |
| R74 | v2 · Authorization is against the current grant | **Authorization is against the current grant.** No lookup takes an "as of" time, so a snapshot taken at capture cannot authorize a later read. Provider ownership never yields customer payload; `read_customer_content` is in no role's capability set. (F2P V11) |
| R75 | v2 · An empty scope fails closed | **An empty scope fails closed** — the same reading v1's `OrgEntitlements.model_ids == ()` already has. (F2P V12) |
| R76 | v2 · Digest provenance is recorded, not assumed | **Digest provenance is recorded, not assumed.** `ServingRevision.digest_source` distinguishes served bytes from a registry oid, and `runtime_image_digest` may be absent while the runtime is a moving tag. Without this, "measured on the pilot box" would later read as "confirmed upstream". (F2P V13) |
| R77 | v2 · The fixture base is generated | **The fixture base is generated.** `fixtures/v2/*.json` comes from `fixtures.py --write`; a hand-edited fixture fails `test_fixtures_v2.py`. The console reads those same files rather than keeping byte-identical copies, so there is nothing to diff at integration. (F2P V14) |
| R78 | v2 · Pins are immutable after acceptance | **Pins are immutable after acceptance.** `AdmissionPins` and `AdmissionV2` are frozen and every identity field is required. `model_copy(update=)` and `model_construct` are not permitted on admitted `AdmissionPins`/`AdmissionV2`: both bypass the validators that tie a card to its pins, so the wire-in phase and D2–D5 enforce the prohibition at the persistence boundary (the pin columns on `jobs` are never updated). (F2P V15) |
| R79 | Admission and an oversized deadline (the settled R-3, recorded) | `JobStore.admit` **clamps**, it does not refuse: `deadline_at = min(caller_deadline, db_now + preparation + queue + generation budgets)`. The one deadline it refuses outright is an **elapsed** one (`caller_deadline <= db_now`), which is `invalid_request` with no side effects. The clamp is taken from the store's own clock and its own budgets snapshot, so a caller asking for a horizon the store cannot keep gets the horizon the store can keep, pinned at acceptance; an idempotent replay returns the pinned bound rather than re-deriving it. G subtracts no tolerance and applies no skew heuristic - the gateway has no second clock (the two obsolete gateway-margin mutants were retired with the heuristic). This was implemented in `contracts/fakes/state.py`, the conformance cases and G1's cases during the 2026-09-21 audit and is recorded here as the ruling it always was; D2 owes the same behaviour on a real SQL clock. |
| R80 | Engine delta payloads and the usage rule (R58, settled wording) | A delta payload is **exactly** `{"visible", "raw"}`. The transitional `content` alias of `raw` is removed everywhere: the shared `FakeEngine`, the two exported engine cases and W's adapter changed in one revision (F2R item 2), `PAYLOAD_COPIES` fell from 3 to 2, and the alias assertion in `tests/w` went with it. No relay, journal or export may read `raw`; a consumer that does leaks the reasoning block. The settled usage wording, in full: a usage object that is **malformed in any way** leaves usage `unknown` **whatever order it arrived in** - malformed is not "a late usage object", so a malformed one before the final delta and one after it are the same answer; **identical repeats** of the same usage object are authoritative and are emitted as **one** event, not two; and a stream with `malformed_lines > 0` can never end `completed` (R21 decides the billable causes from there). Authoritative still requires the last usage object of the stream, arriving after the final content delta, consistent with it (`completion_tokens >=` non-empty deltas). |
| R81 | Trace metadata charge (settles T1 ruling 5) | A queued trace record costs the metadata reserve `max(declared metadata_bytes, len(codec.compact_bytes(envelope)))` - the caller's number when it is the larger, what the row really occupies otherwise. Queued plus in-flight serialized bytes therefore never exceed `TRACE_METADATA_RESERVE_BYTES`, so the spool's interim 32 MiB `QUEUED_PAYLOAD_MAX_BYTES` is retired (the reviewer's 10,000 under-declared 20 KB envelopes stop at the reserve with `metadata_budget`). The frozen case `trace_bounds__metadata_exhaustion_drops_with_counters` is revised to require both halves. The accounting is one implementation, `contracts/traces_accounting.py` (`TraceCaptureBase`/`TraceSinkBase`, clock required, no crash, no failure hooks), which the fake and `traces/spool.py` both subclass; no production class imports a fake or carries a test hook. |
| R82 | Staging takes only store-produced refs (F2R item 4) | `MediaStore.stage` accepts a url/inline ref only if the store materialized it for that organization, and an upload ref only if it is a finalized upload the store resolves; `attach` likewise binds only the store's own indexed ref. A never-materialized ref, a real handle claiming other content, and another tenant's ref (as sent, or relabelled with this organization) are all `not_found`; the refs returned and bound are the store's records, never the request's copies. The tenant guard is the organization namespace of every lookup. Canonical requests carry one `video_url` part per ref, in order (R58). `duration_s` is written by preparation (M2's probe), never by a caller. The conformance harness makes authentic refs through `materialized(org_id, ref)`. |
| R83 | One mutation runner (R32/R40, mechanism) | There is **one** runner, `tests/contracts/mutants.py`, and every track's list delegates to it with a `Runner` naming its pytest target; `tests/d/migration_mutants.py` builds databases instead of pytest processes, so it delegates the kill *rule* (`assertion_kill`) and keeps `apply_error`/`setup_error` as its two named refinements of `broken_runner`. The rules, binding on every list: a mutant whose anchor appears zero or twice is `misdeclared` (an edit that lands on whichever line came first is not the declared defect); a mutant that does not **compile** is `broken_runner`, decided by `compile()` and not inferred from an exit code; every failing test must name one of the mutant's own cases (matched on the test function name or an exact parametrization id, never a substring); every death must be assertion-shaped, a typed `DomainError` subclass - the answer the contract promises, enumerated from `errors.DomainError.__subclasses__` rather than listed - or **declared** in the mutant's `dies_by`; a run that collects nothing or skips its cases is `misdeclared`; a run that does not finish inside the runner's timeout is `broken_runner`, because a hang is not a proof. Each pytest subprocess gets its own `PYTHONPYCACHEPREFIX` and `TMPDIR` inside the throwaway copy, and the committed `pyproject.toml` travels with the copy so it collects under the same import rules as the suite. Only `killed` is accepted by a suite. **Amended 2026-09-22 (F2R-A review):** an `AssertionError` raised inside `infrx/` (outside `contracts/conformance/`) is an undeclared death unless the mutant declares it in `dies_by`; every list runs the union of its named cases once unmutated per process and refuses the whole list if any fails (pristine baseline); a collection or fixture error with no tests run is `broken_runner`, never `misdeclared` or `survived`. |
| R84 | In-place amendment of unapplied migrations | A migration file may be amended in place, with a dated header note naming the amending task, only while it is applied to NO hosted or shared environment (today: 0003 onward; hosted has 0001–0002). The first hosted apply freezes every file it applies; from then on every change is a new numbered migration. Harness digests are computed from the files, so an amendment changes the recorded digest — evidence that quotes digests must say which file version. |
| R85 | Wallet owners are retired, never deleted (A1's R-A1) | An individual who owns a CREDIT wallet or a signup entitlement is never hard-deleted (the FKs refuse it). Deletion is one operator action: GoTrue soft delete, then `infrx.retire_individual` — anonymise the profile, revoke every key whose individual is the user, rename to `retired` and suspend every organization the user created **and alone owns** (a shared one, or one solely owned but not created, is left as is; a member joining a solo org concurrently with the retirement is a recorded race the operator lifts with `set_suspension`), freeze the wallet (no new hold, no grant; in-flight settlement and D5 entries still land). Ledger, entitlement and identity claim are retained as money history. Eligibility is one grant per individual UUID and per verified address (sha256 of the lower-cased, trimmed email, stored as a digest only; retained after retirement, so re-creating the address is `identity_reused`). The digest is pseudonymous personal data kept for abuse control; its retention must be bounded by the legally approved period (P-05), and erasing it is a new audited D operation plus a ruling. The signup-time R72 scope is every organization the individual created (`created_by`); the claim locks those organizations before binding, so a membership writer racing it in the opposite order can make the claim raise `40P01` (retry the same idempotent call; nothing minted). Migration numbers: 0010–0014 belong to D2, 0015 to A1. |
| R86 | Pilot composition and the ingress request context (G1R's request 10) | In `pilot`, `validate_runtime` refuses unless `app.ROUTERS` composes the metered ingress and not the legacy chat route (E3B dr17; the refusal is worded apart from preflight's so I0's `composition_gate_removed` stays killed). The ingress's request context is `AuthContextV2`, built from the key row by the one shared `auth.context.auth_context` (0009's `audience,user_id,created_by,provider_org_id,endpoint_id`; a `provider_dev` row whose `org_id != provider_org_id` is refused as `internal_error` until D1R's 0009 CHECK lands). Admission receives the requested model name as the client sent it, never the ingress's catalog resolution; the catalog resolves the name only to bound ceilings and price the request, and the store re-resolves at admission. |
| R87 | A CREDIT job's v1 debit is always zero (F2P wire-in candidate a) | For a job admitted under `accounting_regime = credit`, the v1 `TerminalOutcome.debit` is always zero and the charge is `SettlementV2.charged` at the admitted rate card only (`complete_credit` returns the outcome with debit 0 beside `SettlementV2 | None`). A consumer of v1 records never derives money from a CREDIT job's outcome; a store that writes a non-zero v1 debit on a CREDIT job has a defect. |
| R88 | `UsageRecordV2.usage`/`outcome` absence (candidate b) | `usage` and `outcome` may be absent on a `legacy_usd` row (a row written before settlement, or one whose settlement the legacy path never recorded), never on a `credit` row: a CREDIT usage record without both is refused at validation, and the unit on the row is the regime's (`unit_of(accounting_regime)`). |
| R89 | Console instant comparison (candidate c) | The console compares contract instants only through `instantKey` (`apps/app/lib/contracts/v2/types.ts`): every instant is reduced to `YYYY-MM-DDTHH:MM:SS.ffffffZ` and anything not in the contract's `Z` form is refused rather than compared. Raw string comparison of instants in console code is a defect (a mixed spelling can call a revoked grant current). Normalising an offset form belongs to C's intake, which accepts both (R59 (9)). |
| R90 | Console `AUDIT_ACTIONS` are D1's spellings (candidate d) | The console's `AUDIT_ACTIONS` (`apps/app/lib/contracts/types.ts`) are exactly D1's audit action names (`admin_grant`, `admin_set_suspension`, `admin_set_entitlements`, `calibration_label`, `admin_key_issue`, `admin_key_revoke`, `admin_publish`, `admin_job_cancel`, `admin_reconcile`, `admin_adjust`); a console page never invents a display spelling as an action value, and a new D audit action is added to both sides in one change (the wire-in's IR-C1 renamed the three older console spellings to these). |
| R91 | Idempotent replay is looked up before preparation (G2 review money-B1) | `JobStore` and `CreditJobStore` gain an additive read-only method `lookup(org_id, idem) -> (Admission, TerminalOutcome | None) | None` with admission's scope rule (org + operation + key; a changed payload digest is the same conflict `admit` raises; an expired mapping is `None`, never a new job). The acceptor calls it first whenever an idempotency key is present and answers the mapped job — a terminal job's committed result, an in-flight job's wait or stream — without preparing or staging anything; media is fetched and staged only for a request that has no mapping. The contract fakes implement it (exposed from their existing replay map) with an exported conformance case; `PgJobStore` refuses it with a typed not-implemented until D5 wires it, recorded as a PENDING partition line naming D5. |
| R92 | Restore fidelity compares effective privileges, not raw ACL arrays (E3B2 review bk01) | A relation whose `relacl` is NULL carries the owner's implicit default privileges; a backup/restore comparison (I3B's `pgrestore`) must normalise both sides with `acldefault(relkind, owner)` (and the function/schema equivalents) before diffing, so a NULL-vs-explicit-default difference is never reported as a lost privilege. The E3B2 gate's bk01 finding on `infrx.job_results` is that comparison artefact: `put_result`/`read_result` keep their access after a restore. Owner: I3B (the restore comparison); D and the restore procedure change nothing. |
| R93 | The fake's requeue event id is its fresh outbox row id (E3B2 request 3) | When a preparation lease lapses and the store inserts a fresh `prepare_dispatch` outbox row (D2 OB-5b), the index event it publishes carries THAT row's id — on PostgreSQL and in the contract fake alike. The fake is changed by the coordinator at the E3B2 merge, and the gate's dr04 fake-side exemption is dropped in the same merge; until then the fake pins the divergence explicitly. |
| R94 | The execution mode is part of the idempotency identity (G3 review ADM-1/B1) | An idempotency key identifies one operation, one canonical payload AND one execution mode (`sync`, `stream`, `async`). Reusing a key with a different mode is `idempotency_conflict` (409), exactly like a changed payload: a synchronous retry can never attach a cancelling wait to an asynchronous job, and an asynchronous replay of a synchronous job is refused, not re-answered as a 202. The mode enters the payload digest at the ingress (`Ingress.validated`), so `lookup`/`admit` see the conflict before any store write; the fakes and PostgreSQL need no change. A replay therefore always answers in the job's own mode. |
| R95 | The media object store (M1-L2 proposal) | The `ObjectStore` port's durable adapter is `infrx.media.s3.S3ObjectStore` on `S3_MEDIA_BUCKET` under `S3_MEDIA_PREFIX` (08 §5.1); no mode stages media into process memory. (1) Write-once is the server's conditional PutObject (`If-None-Match: *`), never a read then a write. (2) `head` is the whole-object SHA-256 the server verified on write (`x-amz-checksum-sha256`); an object without one (none, composite, malformed) is present and equal to no digest. (3) A 404 is "absent" only to a one-object read (head, get, describe) and to delete; to a write or a listing it is an error. Any other failure (403, NoSuchBucket, throttling, 5xx, a body that breaks mid-read, transport, a 409 conditional-write conflict) is `dependency_unavailable`, never "absent" or "not written". HeadObject's bodiless 404 cannot tell a missing bucket from a missing key; startup's HeadBucket is what refuses a missing bucket. (4) The gateway refuses to start unless the bucket answers HeadBucket with the process's own credentials; credentials and region come only from the environment's chain (the instance role on the box), never from settings text. A pilot install asks the same from the host before replacing the env file, and every refusal names the setting and the error code, never the bucket or the endpoint. (5) On one bucket, no deployment's prefix may be a prefix of another's: each collector lists and sweeps its whole prefix. (6) Until D2's durable media rows exist, one gateway process per prefix. |
| R96 | The published serving revision is the measured one (E4B proposal) | Once W3 records a digest-pinned runtime image and an engine-options digest, the serving revision a release publishes (and every admission pins, R76/R78) carries exactly those values; a placeholder digest or a moving tag fails E4B's config pin. R76's "absent while the runtime is a moving tag" no longer applies to Marlin. |
| R97 | A certification counts at one clean SHA (E4B proposal) | A release decision cites a dev-host report and a box report whose `git_head` equals `git_head_end`, both clean, at the release SHA; a local-target number is labelled `fake-engine, not a measurement` and never fills a measured column. |
| R98 | The gateway composes its durable adapters from settings or refuses (cutover proposal) | `create_app` builds each adapter it is not given: D5's catalog, D4's journal and the job store on one pool from `DATABASE_URL`/`DATABASE_POOL_*`, and the object store from `S3_MEDIA_BUCKET`, which must answer HeadBucket (M1-L2). It never stages media in process memory outside a test that injects the store. An unset `S3_MEDIA_BUCKET`, or a store to build with no `DATABASE_URL`, refuses in every mode, naming the setting. An unset `INFRX_MODE` refuses to start (R44, completed). |
| R99 | Upload references at use and the durable attach (M pilot-media proposal) | (a) An `infrx-upload:upl_<id>` source in a chat or job body is resolved at admission by the media store, never fetched, by the rule `stage` applies to an upload ref (`resolve_owned`): the caller's organization's (another's or unknown: `404 not_found`), finalized (else `not_found`, or `invalid_request` when the handle is otherwise indexed), its object at the content-addressed `source` key still carrying the finalized digest (else `not_found`), its size spent from the request's `MAX_MEDIA_BYTES` budget (`413`). (b) The upload window bounds use as well as completion: from `expires_at` on, resolution and staging of the upload are `410 upload_expired`, in the fake and every adapter. (c) `attach` is durable and write-once: it records the job's source refs where another process reads them (on PostgreSQL, 0003's `staged_media`/`job_media`), except an attach of no media, which 0003 cannot record (another process answers "not attached" for it); `prepare(job_id)` reads that record; the mediastore conformance hook `reopened()` is another process's view of the same store. (d) Re-attaching a job's exact refs (handles and digests, in order) is a no-op; any other binding - other refs, a superset, a subset, a reorder, none - is `409 conflict` and keeps the first; one attach naming a ref twice is `invalid_request`; on PostgreSQL attaches of one job are serialized on its `infrx.jobs` row. Qualifier at numbering (verifier N1): across processes on PostgreSQL a zero-ref attach over a job another process bound durably is accepted, not `conflict` (Limit 1: an attach with no media leaves no row); the `none` arm of (d) holds in-process only. |
| R100 | A negative operator adjustment is a store-made compensating entry (D5 candidate i) | (i) A negative `operator_adjustment` is recorded as a store-made compensating entry under R11: audited `admin_adjust` with a reason, never below reserved (brief open input iv). |
| R101 | Private catalog naming is <provider slug>/<endpoint name>-<env> (D5 candidate ii) | (ii) Private catalog naming is `<provider slug>/<endpoint name>-<env>` (design decision 9). |
| R102 | The data-access policy defaults are consent version 1 and trace off (D5 candidate iii) | (iii) The data-access policy defaults are consent version 1 and trace `off` (design decision 10). |
| R103 | USD admin grants stay on the legacy writer, never grant_credit (D5 candidate iv) | (iv) USD admin grants stay on the legacy `public.credit_ledger` writer and never go through `grant_credit` (R65/R72; brief open input ii). |
| R104 | Preparation's exact count travels with `prepared` (PREP-WORKER proposal) | `JobStore.prepared(lease, media=(), *, prompt_tokens=None)` (and `CreditJobStore`'s, the same operation): `prompt_tokens` is preparation's exact prompt count as the serving engine counts it, stored once with the refs and handed to the lease holder as `Work.prompt_tokens` / `WorkV2.prompt_tokens`; a bool or a non-integer is `invalid_request`, a count below 0 or above the job's `max_input_tokens` (inclusive) is `context_length_exceeded`, and a refused call changes nothing. `None` stores no count. A preparation lease reads its request through `load_work` / `load_work_credit` (the preparation read path, R46). The port's `heartbeat` renews a preparation lease as R52 says, never past `preparation_deadline_at`. Exported conformance: `dur_fence__prepared_stores_the_exact_prompt_count_once`, `credit_prepare__the_count_reaches_the_credit_work`. |
| R105 | A preparation count is the engine's, or there is none (PREP-WORKER proposal) | The count a preparation worker stores is the serving engine's own tokenization of the exact chat body the attempt will send; an answer it cannot check against the pinned profile (for a video: fewer `video_token_id`s than two-frame patches, or more than the profile's budget) is `dependency_unavailable` and the job is not prepared. A preparation worker never estimates. |
| R106 | A cancelled job's replay is terminal for that key (coordinator, box certification 2026-09-24; wording corrected the same day after the CERTIFY-TREE verifier's N6) | A keyed request whose job was cancelled by the client - a client disconnect (`client_disconnected`) or an explicit cancel (`client_cancelled`) - is answered on replay with its committed outcome (R91): `state_conflict` with `infrx.state = cancelled` (a `409` in sync mode; a stream error event in stream mode). A job the DEADLINE ended is not this case: it replays `deadline_exceeded` and the SOP client retries it (marlin-sop.md §3.6). For the SOP client (§3.5/§3.6) a cancelled replay is TERMINAL for the key: the interrupted item was cancelled, no second item was accepted, its hold stays held until the platform releases it and it carries no usage and no debit (DUR-SETTLE conformance `dur_settle__cancel_records_its_cause_and_settles_by_r21`), and the item is re-issued only under a NEW key if it is still wanted. A client that re-sends a cancelled key gets the same answer forever; a resume drill counts such items as terminal ("cancelled by the interruption" only when the first attempt was the client's own tear; a platform-side cancel is listed apart and fails the drill), never as open. |
| R107 | A preparation count may be the memo of the engine's checked answer to the same body (TOKCOST; verifier wording) | A preparation worker may store, instead of asking the engine again, a count the serving engine gave for a `/tokenize` body equal to the one this attempt would send. Equal means the same canonical JSON (keys sorted, compact separators) of `{model, messages, add_generation_prompt, mm_processor_kwargs}` as `engine_prompt_tokens` builds it. The messages carry the organization's own `file://` path, so a memo never answers across organizations. The memo may answer only when every prepared media ref has the same whole digest and profile version and the job has the same serving revision: its `model_revision` and, in the CREDIT regime, its pinned `serving_version_id`. Only a body with media is memoized; a text body is always asked. Only an answer that passed every R105 check is memoized; a refused, failed or timed-out answer memoizes nothing. The memo lives only in the worker process that asked, and that process must end whenever the engine process that answered ends (the worker unit is `PartOf=` the engine unit, which also carries a `Restart=always` restart; a memo shared between processes or kept durably needs its own ruling). An entry expires `PROCESSING_CACHE_TTL_S` after the engine answered, and a use never extends it. The memo is bounded in entries (least recently used first). A memoized count reaches the store only through `prepared(...)` (R104), under the same bounds. Each use is logged `prepared <job>: <n> prompt tokens (memo of engine /tokenize, <ms> ms)`, never as the engine's latency. |
| R108 | A connection-closing refusal first drains a declared body, within the intake bounds (INTAKE-DRAIN; verifier wording) | When the gateway answers a request on the chat ingress, `POST /v1/jobs` or an upload route with a code that closes the connection (`invalid_api_key`, `request_too_large`, `capacity_exhausted`, `deadline_exceeded`) before that request's body has been read to its end, it first reads and discards the rest of the body, and only when the request declares a `Content-Length` of at most `MAX_REQUEST_BYTES` (for `invalid_api_key`, at most 1 MiB: a caller with no valid key can never make the gateway read more). The drain stops at the first of: the end of the declared body, a receive error or disconnect, and the end of the `INTAKE_TIMEOUT_S` window that began when the route received the request, so it never makes a refusal later than that window. The answer is then the typed envelope with `Connection: close`, and the connection is never reused. A chunked body or a declared length over the bound is answered and closed at once, and a body still arriving when the window ends is cut there; such a caller may lose the refusal to a reset, so a client that must read its refusals declares the length. A refusal raised after the body was read to its end drains nothing. Drained bytes are counted, never kept or parsed. Codes outside this set are unchanged: `rate_limited` and `journal_capacity_exhausted` keep the connection. |
| R109 | Published-model projection: one resolution, one price identity, no overclaim (F2C.c) | The public catalog entry is `contracts.v2.published_model.PublishedModel` (TS twin `lib/contracts/v2/published-model.ts`), produced only by `project` from trusted rows and the enforced `serving_profile`, which refuses any record `violations` flags; a private, non-active or unpriced deployment is never published (`not_found` / `invalid_request`, R69/R70). A model string resolves only by `split_model` + `resolve` (0008's grammar; the unlabelled id takes the highest effective listing, a pin its own revision) and is priced after resolution: CREDIT by the card the effective listing names — the one public rate identity `(rate_card_version, CREDIT)` — and legacy USD by the `price_versions` row keyed by the canonical `model_revision`; the admission pin records the requested string verbatim beside the canonical identity and exactly one price identity (P-22). No price is converted between units. `availability` is separate from `capability`, and no capacity or latency figure is published. `project` refuses, and G7's CI check re-asserts, any `violations(projection, serving_profile(<runtime>))`: caps at or below the enforced value, vocabularies (modalities, modes, parameters, MIME types) as subsets, a named refusal really refused, live video only if served, the preprocessing constraints and every retention figure exactly; `ServingRetention` cannot state zero data retention or that turning capture off deletes serving content. Both languages parse the same closed record (unknown or missing field, coerced number/flag, non-canonical or wrong-unit amount → refusal) and re-serialize it to identical canonical bytes. |
| R110 | Benchmark validity: fresh inference only, bounded profiled runs (E1C) | A capacity summary counts fresh inference only. A replay that does not belong to a declared resume of the same keys makes the cell INVALID; so do missing attempts, driver lag above the declared bound, a served identity other than the declared one, and unreconciled counters. A qualifying run carries a validated `infrx.run-profile/1` (target path, bounds including outstanding holds, identity, key inventory); a resource-consuming run against a non-local target refuses to start without a runnable profile — the only exception is an explicit, logged opt-out (`bench --unprofiled smoke`: at most 4 requests of at most 512 output tokens), and an opted-out summary is INVALID. Amended at the E1C profile-flip merge (2026-09-25): the certify opt-out is gone; the certify runner stamps a coordinator base profile per cell and passes it with the key inventory to every non-local cell (CERTIFY-WIRING), stale cell outputs are removed before each client run, and an unprofiled or INVALID remote cell cannot PASS. `target.path` must agree with the run: engine ⇔ `--target direct`; public edge ⇔ https on its default port of a non-loopback host. Derived verdicts name their parser version; raw rows are never reclassified. |
| R111 | Lifecycle ports and the refusal table (F2C-L) | The three lifecycle ports (`contracts.v2.lifecycle` UploadRepository, ReadinessStore, ContentLifecycle) exist as encoded. Refusals are the closed `LifecycleRefusal` set, each raised as an existing public code; the reason rides on `DomainError.refusal`, is never serialized, and there is no new public code or status. One exception: an upload ticket records its abort reason, drawn only from `UPLOAD_ABORT_REASONS` (public facts about the caller's own bytes). |
| R112 | One-phase execution readiness (F2C-L) | `admit_ready` checks everything and records the normalized source manifest and the execution-ready marker in the admission transaction, in both regimes: CREDIT additionally pins the expectation card and the pinned revision's capability; sources are the org's own live content with the same digest, an upload only through its finalized, unexpired ticket. A refusal admits nothing. An empty manifest is ready with zero sources; no marker means `not_ready`. Preparation refuses `not_ready`; `admit`/`admit_credit` never write a marker. |
| R113 | Readiness cutover without backfill (F2C-L) | Marker-less `preparing` jobs end at `preparation_deadline_at` through the existing reaper as `preparation_failed`, their hold released free. Each previous→new runtime boundary pauses admission and drains preparing jobs until none lacks a marker. `admit_ready` dual-writes the 0003 `job_media` rows so the previous runtime stays a valid rollback target. |
| R114 | Content lifecycle (F2C-L) | Stable `content_id` per key; the generation increments on re-registration after the acknowledged delete (generation n > 1 is addressed as `key.g<n>`); eligibility is persisted at first registration; the tombstone rechecks references under the admission lock; a tombstoned key is `content_retiring` until the ack; an older generation's ack is a no-op; a tombstoned row whose claim lapsed is offered again and finished under a higher fence; the fence restarts per generation, so every claim check compares `(generation, fence)` together; the claim TTL must exceed the object-store request timeout. Deletion is authorized only by a committed tombstone of a fenced, unexpired claim; the external delete runs after that commit, outside locks, idempotent; a pass stops on DependencyUnavailable from either store. Amended at the M6 phase-2 merge (R129): writers write bare keys, so `key.g<n>` names no physical object; a generation is addressed by `(content_id, generation)` in the row only and re-registration after an acknowledged delete reuses the physical key; R129's tombstone-until-acknowledged rule plus the lease margin replaces generation-keyed names, which remain the upgrade. The 0019 "not before the claim that deleted it has lapsed" comment is implemented or retired by D10 in 0022 (M6 WR-8). |
| R115 | Lifecycle time authority (F2C-L) | All lifecycle instants use database transaction time. `now < instant` is live; equality has passed. `retain_until` is set once at terminalization: for results it is the persisted `result_expires_at`. |
| R116 | Persisted result expiry and the read classification (F2C-L slice b) | The settling transaction persists `TerminalOutcome.result_expires_at` once (a worker proposal's value is ignored); it is absent on non-successes and on pre-F2C.b records and is never recomputed from configuration — absent reads as `unavailable`. `read_outcome(outcome, db_now)` yields exactly one of pending, available, no_result, held_unknown, expired (equality has passed) or unavailable on the store clock; scrubbing happens only after the persisted expiry, through the column-by-column scrub guard in 02 (body → '' and scrubbed_at null → now, digest/bytes/identity unchanged, only when the job is settled and its expiry passed or absent). Public wire bodies are unchanged; the field never enters the 0017 journal payload. The optional→required rollout adds the jobs expiry check validated only at zero violators, never `NOT VALID` over a violating row. |
| R117 | Regime transition (G8 point 2–3; fix-round amendment; lock-bound amendment 2026-09-25) | A regime transition moves only the admission flags. It freezes the source regime's flag, waits until every transaction open when the freeze committed has ended, then drains until no source job is preparing, queued or running. One bound, the drain timeout computed before the freeze, covers all of this and every flag write: each flag UPDATE waits on the flag's row lock (held FOR SHARE by an admission in flight, D10) no longer than the remaining bound, in total and not per lock wait (`SET LOCAL lock_timeout` and `statement_timeout`). A run that exceeds the bound stops with blocker `open_transactions` (lock or open transactions) or `in_flight` (drain), enables nothing and audits nothing, and reports in `applied` exactly the flags it changed. If the freeze itself could not be taken, `applied` is empty and the system is unchanged; otherwise the freeze stays. A rerun under the same idempotency key continues from that state, then enables the target's flags, and is audited once under its idempotency key. The target is CREDIT only with an operator-approved card: not provisional, with no `P-01`, `provisional` or `pending` in its approval text, named by the public alias's effective listing, at rates the operator restates exactly. `--dry-run` writes nothing and exits 1 when blocked. No balance is converted, no USD row is relabelled and no card is rewritten; every job settles in its admitted regime at its pinned card or price version, and a USD job replayed after the switch keeps its USD settlement. Operator procedure: rerun `--dry-run` after the transition and again immediately before the runtime restart; restart only on exit 0. `require_feature … FOR SHARE` (D10) is the durable database-side guard. Source: research/plan/evidence/g/G8-6a075c5.md, G8-44ea477.md. |
| R118 | Operator-write races (G8 point 4) | An operator write whose idempotency key was recorded concurrently answers the recorded result, or `idempotency_conflict` for a different request under the same key, never the audit relation's unique violation and never a retry; the write is deduplicated by its deterministic operation id. A phased procedure (`--freeze-only`, then the enabling run) uses a distinct key per phase. Source: research/plan/evidence/g/G8-6a075c5.md §Proposed ruling text. Amendment (2026-09-25, lock-bound merge): a `--freeze-only` run may also refuse with `applied: []` when the freeze's flag write exceeds its bound; the system is then unchanged. |
| R119 | Recovery proves service, not readiness (I8 slice 5–6) | A rollback, restore or replacement passes only when a fresh public in-cap video job is accepted, succeeds, returns content with authoritative usage and settles with no wallet drift, after the change and after its reversal. `/readyz` timings are labelled readiness-only and are never reported as a cold start. A rollback target is chosen by its tree (preparation loop, applied migrations, config schema), its recorded evidence and its release bundle, never by the presence of a backup directory; a target whose tree is older than the applied schema needs a recorded proof on the newer schema. "Charged" in a settlement check is in the job's own unit: a USD job's `debit > 0`, a CREDIT job's one `inference_debit` with `debit = 0`. Restore verifies the restored bytes against the release's serving-version pins, not only the mirror's own manifest. Replacement-instance restore (image mirror, fresh NVMe, env from SSM names) is an open item (I8 2-ACC-3), not covered by this ruling. Source: research/plan/evidence/i/I8-103d20a.md. |
| R120 | Pooler discipline (I8 slice 1) | An install's knobs must PASS `pool_budget.py` (reserved headroom and the concurrent startup peak included) for the pooler mode the runtime uses; runtime connections on the transaction pooler (6543) carry no session state (no SET/RESET, session advisory locks, LISTEN, temp tables) and no server-side prepared statements (`prepare_threshold=None`); `SET LOCAL`, transaction advisory locks and role-level defaults are the safe forms; migrations and dumps use the session port inside a maintenance window. Source: research/plan/evidence/i/I8-103d20a.md. |
| R121 | The env file is versioned (I8 slice 2) | A runtime unit starts only on an env file written for its image's schema (`# INFRX_ENV_SCHEMA` header, names only), with no name outside the schema; `envcheck` runs as ExecStartPre, exits 2 on an unknown or malformed key and never prints a value. Source: research/plan/evidence/i/I8-103d20a.md. |
| R122 | Pre-admission refusal binds nothing (D10/M5) | Any refusal raised before the admission transaction commits — validation, readiness, capability, price, wallet, capacity, regime flag — leaves no job, hold, reservation, outbox or idempotency row; the same key may be retried after the cause is fixed and then replays that job; a keyless or late-key retry of a committed request UUID is `state_conflict` (409) in both regimes. Source: research/plan/evidence/d/D10-1f12cb6.md §7, research/plan/evidence/m/M5-35c401b.md. |
| R123 | Readiness is one phase (D10 slice 1) | Only the admission transaction writes the execution-ready marker, for zero media too; a preparation lease through the ready door requires it; there is no backfill; a runtime cutover pauses admission until no preparing job lacks a marker (`readiness_cutover_check()`). Until W5/G7 cut the runtime over to the ready doors, the barrier for `infrx_runtime` is the runtime calling those doors, not the grants: the cutover gate reports `runtime_unmarked_doors`, and it must read `[]` before v1 readiness is called enforced (the revoke of `infrx.admit`/`infrx.claim_preparation` from `infrx_runtime` is a D10 follow-up migration). Source: research/plan/evidence/d/D10-HANDOFF-31c2112.md §9, D10-ff62913.md §4. Amended at the union merge (DOOR-REVOKE, 0023): the two unmarked doors (`infrx.admit`, `infrx.claim_preparation`) are revoked from `infrx_runtime`; the runtime function set is 41 by name and `readiness_cutover_check` reports no unmarked door. |
| R124 | PostgreSQL decides deletion (D10 slice 2) | Content rows, leased fenced claims (compared as (generation, fence)), the reference recheck inside the tombstone and generation-checked acknowledgement; database content is deleted by scrubbing inside the acknowledgement under the column-by-column guard; the external delete is issued only while the claim has at least one store request timeout left; unreachable authority, ambiguous ownership or a live lease retain. A payload or prepared object discovered with no row names no job (owner = its key's organization) and only a running job naming the key protects it. A content key with a `.` or `..` path segment is refused by the database (retained and reported, never registered), whatever the store's normalisation. Source: research/plan/evidence/d/D10-HANDOFF-31c2112.md §9, D10-1f12cb6.md §7. |
| R125 | Persisted expiry is the only read authority (D10 slice 3) | `result_expires_at` is written once at settlement, frozen by `jobs_result_expiry_immutable` (23514 on any later change, for the owner and for `service_role`), and is the sole authority for the store, the consumer reads and the scrub; a success with none reads `unavailable`; classification uses the database clock. Source: research/plan/evidence/d/D10-HANDOFF-31c2112.md §9, D10-ff62913.md (0-D10-R1). |
| R126 | Discovery prices what admission charges (D10 slice 3, P-22) | A published USD price is read through the same resolve-then-price rule (`infrx.usd_price`) admission uses, at the same database instant; a model string admission would refuse shows no price; a retired price row, a future or unserved listing never prices. Source: research/plan/evidence/d/D10-1f12cb6.md §7, D10-ff62913.md (0-D10-R5). |
| R127 | Dedicated logins (D10 slice 4) | `infrx_runtime` and `infrx_monitor` carry their bounds (statement timeout) as role defaults, hold no role membership and no BYPASSRLS; each direct read has a policy for that role alone; the runtime may UPDATE only `jobs.updated_at` and has no `job_results` DML; `jobstore.connector(dsn, set_role=None)` keeps I8's transaction-pooler rule and sets nothing for the dedicated login. Source: research/plan/evidence/d/D10-HANDOFF-31c2112.md §9, D10-ff62913.md (0-D10-R6/R7). |
| R128 | Upload tickets across processes (M5) | The ticket authority is F2C's `UploadRepository`; a gateway keeps no per-upload state. (a) A PUT records its receipt before the destination is written, write-once; a refused PUT stores nothing and a retried one is a no-op. (b) Completion measures the destination, refuses declared size/digest/type before anything is copied, probes, writes the content-addressed source and only then commits; a crash between the write and the commit is completed by any retry; the first completion's bytes are the handle's forever. (c) Resolution of an upload handle is the ticket's: foreign or unknown 404, past `expires_at` 410, unfinalized 404; the attach binds the store's record (R82). A settings-composed gateway without a database answers `dependency_unavailable`, never process-local tickets. Source: research/plan/evidence/m/M5-35c401b.md ("Proposed ruling text"). |
| R129 | Content deletion authority and bounded caches (M6) | Content deletion is authorized only by a committed tombstone of a fenced, unexpired claim on one `(content_id, generation)`. The external delete names the key the writer wrote; it runs after that commit, outside every lock, idempotent, and only while the claim has at least one object-store request timeout left, measured from before the claim was requested — otherwise the content stays tombstoned for a later claim. A tombstoned key refuses registration (`content_retiring`, retryable 503) until its delete is acknowledged, and none is acknowledged before its delete returned, so a delayed delete never reaches a later generation (residual: a request the client abandoned that lands more than claim TTL − request timeout later; generation-keyed physical names are the upgrade and need the generation on `MediaRef` and in D10's key matching). Every object the runtime writes is registered (`origin = written`) before its bytes — destinations, sources, staged envelopes, prepared artifacts; database content is registered by D10's triggers and deleted by the acknowledgement's scrub. A pass stops on `DependencyUnavailable` from either store (retain and report); only keys under `media/`, `uploads/`, `payloads/` without a `..` segment are ever deleted; the reference recheck runs inside the tombstone's transaction under the row lock admission takes. Process maps are bounded caches of durable records. The local cache's disk is its index: expiry by file mtime, high water down to low water, never a file under a pin (on lookup, sweep and eviction alike; a pin or an evictor that lost a race with a replacement of the path acts on nothing, verified by inode), and a full cache refuses retryably after the durable artifact is written. Open until M6 WR-7 lands (D10 0022 + the F2C fake): a `written` re-registration of a live same-digest key refreshes `eligible_at = greatest(eligible_at, now() + grace)`; a `discovered` one never does. |
| R130 | Stalled dependencies are bounded per stalled call (G7) | Each PostgreSQL, object-store or catalog call a route makes answers within 20 s (past the pool's connect + statement timeouts) and media preparation within its fetch + probe timeouts + 10 s (40 s by default); a call that stops answering is a retryable 503 `dependency_unavailable` with `Retry-After`. A request meeting one stall answers within 40 s; a slow store answer followed by a stall within 40 s; a preparation that used its full 40 s followed by a stalled store call within 60 s (no per-request deadline). An admission that committed before the stall is left for the same-key retry, which answers that one job; a stalled poll never extends a sync or SSE wait past the job's deadline; an unconfirmed DELETE is a 503 that claims no state. Source: research/plan/evidence/g/G7-a0e0928.md (CM-3 wording). |
| R131 | Revocation bound on the read routes (G7, G8 finding) | A revoked key admits no new request at once; it reads or cancels its own jobs for at most `KEY_TTL` = 60 s after the gateway last fetched it while the identity source answers (the U2/A3 copy states "revocation stops new requests immediately and read access within 60 seconds"). The behaviour during an identity-source outage is P-26 (user decision), not part of this ruling. Source: research/plan/evidence/g/G7-2bbfe0e.md, G7-a0e0928.md. |
| R132 | Run-profile spend amounts are in the profile's currency and compared exactly (E1C/P-24) | A run profile's spend amounts (`bounds.spend.max_spend`, `outstanding_holds`, `rates.input_per_mtok`, `rates.output_per_mtok`) are in `bounds.spend.currency` (USD or CREDIT) and are compared exactly (decimal from the JSON text, never a float; a non-finite amount is a refusal). The legacy `*_usd` keys are read as USD only and are refused under CREDIT or mixed with the unit-neutral keys; USD is never read as CREDIT. Source: research/plan/evidence/e/E1C-credit-cap-54b3733.md. |
| R133 | E4C's criteria are predeclared and cannot be loosened after a qualifying run starts (E4C-PREP, P-18) | The E4C certificate's criteria are `models/marlin2b/results/E4B-protocol.md` §5 as amended 2026-09-25 (amendment 6, P-18) and `tests/integration/backend/certify.py` `CRITERIA`, including `latency_p95_s` 9.0, `e2e_p95_s_per_clip_minute` 90.0 and `declared_rate_per_s` 0.5: the supported rate is the declared rate when its rung passes (higher rungs are measured, never supported), the soak is fixed at 0.25 req/s × 14,400 s, and the overload burst enters through the public edge (profile class P4; a direct-gateway profile is refused, a box run without `--overload-profile` is BLOCKED). Loosening any of these after a qualifying run's start disqualifies that run. Source: research/plan/evidence/e/E4C-prep-f30b281.md. |
| R134 | App deployment configuration (I2A) | The App refuses to serve when its environment does not satisfy `apps/app/lib/deploy/env.ts` (checked once per server start; the environment is stated by `VERCEL_ENV`/`INFRX_APP_ENVIRONMENT`, never guessed); a preview never targets the production auth/DB project (recognised by its URL marker, host compared without trailing dots, never by inspecting a key); every App response except Next static assets is `private, no-store`, and no private page may be prerendered; an App release is identified by commit + deployment id from `/api/version` (the host commit wins; anything missing or malformed is `unknown`), and rollback promotes the recorded known-good deployment. The App release follows BACKEND-READY. |
| R135 | Readiness barrier (W5) | A preparation worker prepares a job only after its committed execution-ready marker: it claims through the `ReadinessStore`'s marker-gated `claim_preparation` and reads the manifest from `readiness(job_id)`. `None` is not ready for text and media alike and is never read as ready from any other record (the attach record, the request); `sources == ()` is a completed empty manifest. The wait is bounded (`ATTACH_WAIT_S`; `not_claimable` after it). Without a `ReadinessStore` (a pre-D10 composition) the durable attach record is the manifest, where a zero-media job has none: it fails closed. Every gateway composed over PostgreSQL admits through ReadinessStore.admit_ready(request, idem, expectation), for text and media in both regimes. The expectation is composition-root configuration: ACCOUNTING_REGIME, and for CREDIT exactly ACTIVE_RATE_CARD_VERSION (R69). A PostgreSQL job store without a ReadinessStore, a CREDIT runtime without a card, a readiness object that is not a ReadinessStore, or a relay whose expectation is not its own regime/card refuses to start in every mode. A refusal is answered as its lifecycle-table error, with no job, hold or outbox row. jobs.admit/admit_credit remain only for compositions over the contract fakes and never write a marker. A media job is prepared from the refs its marker names (admit_ready binds each source in the same transaction as the marker), never before them. |
| R136 | fail_preparation (W5) | A preparation worker calls `fail_preparation` only for a permanent refusal: `unsupported_media` and `request_too_large` → `invalid_media`, `context_length_exceeded` → `preparation_failed`; every other refusal lapses its lease. One transaction: terminal state, reservation and hold released, one usage projection; `stale_lease` / `already_terminal` refusals change nothing; CREDIT jobs through the same door. Until the SQL exists (D10 0022) the worker logs and takes the bounded lapse path. D10-FOLLOWUP (0022): the door is the store's fenced `fail_preparation(lease, cause)`, fenced like `prepared`, released free and settled once; only the same lease's identical call replays. |
| R137 | R105 video bound, amended (W5) | For a video, the token count must hold between one `video_token_id` per two-frame patch of the budget and the most the pinned processor gives any geometry at that budget (up to two frames fewer sampled than the budget's count, rounded per two-frame group — W4's `decide.worst_tokens`); outside that it is `dependency_unavailable`. A video item needing more than the served engine's encoder budget (`serving-version.json` `engine_limits.encoder_cache_tokens`, 16,384) is `unsupported_media` (permanent). |
| R138 | Reconciliation gauges (W5) | The worker's reaper tick publishes the reconciliation pass after each successful `recover` (`infrx_reconciliation_drift`, `infrx_holds_unknown`, `infrx_unsettleable_jobs` on the worker's `/metrics`); a failed read publishes nothing and, unless the monitor login itself is unset or refused (R139: disabled once, said once), counts a reap error. Certify's soak `reconciled_at_end` reads these series from the worker endpoint (`--worker-metrics-url`), and Prometheus scrapes both targets (W5 WR-5). |
| R139 | Committed readiness is final (W5-F5, W5-F5B, E3C F-5/F-6) | A job the admission transaction made execution-ready is answered only its committed outcome (or its handle). Every refusal of the pinned card or capability happens inside that transaction; no later step of the acceptance may answer a refusal for it. In particular the gateway's attach of the staged refs after `admit_ready` is a copy of the manifest that transaction committed: a refusal or an outage from it is logged and counted (`infrx_post_marker_refusals_total{code}`), never answered and never a reason to cancel. The job's own worker reads the committed manifest, and a job the manifest cannot prepare ends by its preparation or deadline rules and is answered as such. Only the pre-D10 door (`jobs.admit`/`admit_credit`, no marker, nothing claimable until the refs are bound) keeps the post-admission recheck, cancel-on-refusal and the retryable 503. A handler cancelled before any identity reached the caller still cancels the job on either door. **A "late rejection" (E3C s04: a capability withdrawn between the admission and the acceptance's completion, refused and cancelled) therefore exists only on the pre-D10 door; on the readiness door the same withdrawal after the marker is answered 202 (or the sync/stream outcome) and the job runs and settles once, and the withdrawal before the request admits nothing.** The contract fake refuses at admission exactly what 0019 `check_pinned_capability` refuses (every part's modality against the pinned revision, both regimes; a missing pinned revision is `not_found`). The worker's reconciliation gauges read on D10's read-only monitor login (`MONITOR_DATABASE_URL`); unset or refused (`42501`) disables them once, said once; any other failure is counted, logged and retried each tick (S3 F4). |
| R140 | Consumer key creation, grant claim and privacy in v1 (C3A) | Consumer key idempotency is per (individual, idempotency key); a replay returns the current metadata with `secret: null`; a lost first response is revoke-and-recreate, never redisplay. The App's grant claim passes `campaign_version = 'app_onboarding'` (audit metadata only, R71); held reasons are not shown to the individual. There is no mutable consumer privacy setting in v1 (capture off, P-09); a `trace_mode` other than `off` on consumer key creation is `unsupported_parameter`. |
| R141 | Consumer request detail and result lifecycle (U4) | The consumer request detail reads the request only through `consumer_jobs(p_request_id)` in the signed-in user's own session and content only through `consumer_job_result` when F2C.b `read_outcome` over the persisted fields is `available`; available vs expired is the database's `result_available`, never an App or browser clock. Content reaches the browser only from `GET /usage/<id>/result`, every answer JSON with `Cache-Control: private, no-store, max-age=0`; the browser keeps it in memory only, re-reads on a back/forward restore and drops it at the persisted `result_expires_at`. A foreign, unknown or malformed id reads as not found. No control on the page submits inference; re-running is the client's own new, separately charged request. Until 0024 ships WR-U4-2, an unknown-usage (`held_unknown`) result is the known gap, pinned by U4-P08. |
| R142 | Consumer key plaintext and Settings (U2) | Consumer API-key plaintext exists only in the first creation response and the create dialog's component state; it is never written to browser storage, a URL, a log or an analytics/error payload; a replayed creation shows no secret; the public revocation statement is P-26's text verbatim (the keys page and the Docs carry the same sentence, U2-W03). In v1 the consumer Settings page has no mutable setting: trace capture is stated as off; sharing, annotation, evaluation and training as not offered; retention as limited-period storage read from the published record on Docs, never zero retention. |
| R143 | Operator writes from the App (U3) | The App's operator writes go through `public.operator_*` RPCs executed with the operator's own JWT: operator authority and the audit actor are decided in the database (`is_operator()`, `operator:<auth.uid()>`); the App holds no service key for operator work, and the legacy `ConsoleServices.adminGrant/adminSetSuspension/adminSetEntitlements` (USD `promotional`) stay unimplemented and unused. Console idempotency keys are namespaced `app-operator:<operation>:<key>` apart from CLI keys; a reused key with another target or payload is `idempotency_conflict`; a concurrent retry is a replay. Rate publication, the one-time signup grant and unknown-usage release remain CLI-only operator operations in v1; the console states this rather than offering controls. The RPCs ship as D10 migration 0025 (WR-U3-1); until then the App answers "not confirmed". Amended 2026-09-26 (0025 merge, D10-0025): the operator RPCs refuse a non-operator caller with SQLSTATE 42501 (`forbidden: …`, from `infrx.console_operator`; the App's `forbidden`); a missing/oversize reason or key, and an adjustment that is zero, inexact or below the reserved total, is `invalid_request` (0018); an unknown individual/org or a non-consumer key is `not_found`; revoking an already revoked key answers `replayed: true` before any audit row, so that no-op does not use up its idempotency key (0-CM-1); same-key suspension/revocation calls serialize on a transaction advisory lock of the namespaced key. |
| R144 | Regime flag writes (D10-FOLLOWUP, G8) | The operator writes a regime flag only through `infrx.set_feature_flag` (an EXCLUSIVE table lock, then the guarded update), bounded by the caller's lock and statement timeouts; new admissions queue behind a waiting freeze. The App's cutover rollback (I3) uses this audited writer through the operator verb `flag` (G8-FLAG), which refuses the two regime flags, never a direct flag write. Amended 2026-09-26 (WR-G8FLAG-3): the one exception is the maintenance switch (`infra/runbooks/rollback.md#maintenance`, drills bk04/rb03): under maintenance no metered runtime exists, and the drill sets both admission flags false directly as `service_role` (0006's column grant) — a recovery action pinned word for word by rb03, not an operator regime change; every other write of a regime flag goes through `credit-transition` (R133) or `flag`. |
| R145 | Written re-registration restarts the grace (D10-FOLLOWUP, M6 WR-7) | A `written` registration of the same bytes at a live key restarts that key's retention grace (it never shortens it); a `discovered` registration never does. |
| R146 | Browser key creation requires a verified individual (D10-APP-SQL 0024) | A browser `api_keys` INSERT requires, besides 0001's owner/creator check, a verified individual (the claim path's predicate: `verified_user` evidence and a live, non-empty email; `public.consumer_may_create_key()`, 0024). A consumer wallet is not required, so verified pilot or invited owners keep creating keys. An unverified owner's keys are issued through the service/operator seams. Consumer ledger pages are read only through `public.consumer_credit_ledger(p_after, p_limit)`: SECURITY DEFINER, the caller's own consumer wallet from `auth.uid()`, keyset on `credit_ledger_wallet_created_idx`, 1..100 rows (else `invalid_request`), an opaque cursor; `public.consumer_jobs` takes the defaulted filters `p_model, p_key_id, p_from, p_to` and returns no key name. App side (APP-0024-WIRE, WR-0024W-1): the consumer ledger page is `public.consumer_credit_ledger` in the individual's own session; the App refuses a limit over 100 before the call and clamps the look-ahead to 100. The Usage filters are `consumer_jobs`' own: model (requested or revision), key id, and the window [now − range, now); an unset filter is not sent, so the unfiltered call is 0021's; a job row names no key. |
| R147 | A worker's result object is fenced output (E3C-CELLS F-1; landed in D10 0026) | `put_result` with a lease runs `fence_lease` first, as `append` does: a lease of another job is `invalid_request`; a stale, foreign, wrong-kind or expired lease is `stale_lease` and an ended job `already_terminal`, before anything is stored; past R29's instant the fence's committed terminalization is answered NULL, which the port raises as `already_terminal`; after the fence 0014's body holds (the same text replays, a different text is `state_conflict`). A result write the fence refuses ends the attempt as a refused `complete` does: nothing settled, not `platform_error`. **A call without a lease stays 0014's unfenced write**, for the known-good rollback targets whose runtime sends `{job_id, text}` (P-25); every worker from 0026 on sends its lease. Until the follow-up migration the lease-less call is not fenced and F-1 stays reachable through it (a stale generation of a pre-0026 build in a mixed-version rollout or after a rollback to 4226315/bda1586, or any service_role/infrx_runtime caller that drops the lease, can store first and end the live generation `platform_error`). Follow-up: a migration after 0026 refuses the lease-less call with `invalid_request` once no known-good rollback target predates 0026; before it, the rigs that write `{job_id, text}` move to leased calls (pgtesting.WorkerResults, tests/g relay_support World and ops/pgworld, recoverykit, test_drills, tests/d's lease-less cases). Ruled 2026-09-26 at the E3C-CELLS merge (cce9224c); amended at the D10-0026-FENCE merge (400a7e94). |

## Verification log

- 2026-09-20: Authored by the coordinator from contracts v1, durable protocols, module handoffs and the current code layout before F2 dispatch. Design only; F2 evidence will record what is implemented and any amendment requested during encoding.
- 2026-09-20: Revision r1 rulings added after two adversarial review rounds of both F2 halves; they are binding for the remaining F2 fixes and for D1/G1/J1/C1 consumers.
- 2026-09-20: Rulings R16–R19 added for the console half's round-3 change requests.
- 2026-09-20: Rulings R20–R23 added (phase deadlines, billable causes, upload expiry code, ambiguous-resolution shape) before D1 dispatch.
- 2026-09-20: Rulings R24–R26 added (entitlement defaults, internal code parity, operator scope).
- 2026-09-20: Rulings R27–R32 added after F2-py review round 3 (trace accumulation port, consent during submission, deadlines on every fenced mutation, terminal integrity, Python feedback provenance, mutation-proof conformance).
- 2026-09-21: Rulings R33–R36 added after F2-ts review round 4 (suspension scope, operator audit trail, calibration visibility, opaque cursors and mutation-proof console conformance).
- 2026-09-21: Rulings R37–R40 added after F2-py review round 4 (capture lifecycle, queue wait as time queued, terminalize-then-refuse, explicit F2 merge criterion).
- 2026-09-21: Ruling R41 added (operator principals never shown to customer sessions).
- 2026-09-21: Ruling R42 added (idempotent loss counting, silent off-mode captures, committed sequence property test).
- 2026-09-21: Rulings R43–R48 added after stage review S1 of the merged G0 tree (feedback/calibration/rubric coherence across halves, runtime mode without a default, injectable price source, work loading and preparation fencing, trace export without storage keys, track test hygiene and unowned-file ownership).
- 2026-09-21: Rulings R49–R52 added after the S1 follow-up on F2.1 (operator feedback view, by_operator marker, pilot refuses the shared legacy key, renewable short preparation lease with tenant-checked attach).
- 2026-09-21: Rulings R53–R54 added after the Python-contract review of F2.1 (store-derived hold; code-point bounds, strict records, projected replays).
- 2026-09-21: Ruling R55 added after F2.1 review round 2 (job-row tenant for attach, ceiling range checks at admission, typed unknown dispatch kind, R29 ordering on the preparation path).
- 2026-09-22: Rulings R56–R57 added after the J1 review (judge sampling policy; fail-closed judge money guard).
- 2026-09-22: Ruling R58 added after the W1 review (allow-listed normalized messages, visible/raw event convention, authoritative-usage rule, cancellation keyed by generation).
- 2026-09-21: Ruling R59 added after the D1 review (SQL-level fail-closed masking, operator data out of customer-readable tables, revoke-all + TRUNCATE refusal, security_barrier views, composite tenant FKs and money sign checks, trigger-maintained wallet total, immutable consent, D-owned aggregate RPCs). Accepted for F2.2: `MediaStore.stage` accepts only store-produced refs with a `materialized(org_id, ref)` harness hook (M1 review); `usageSummary`/`usageDaily` require `from`/`to` in `services.ts` and the exported cases together (C1). Correction: the two entries above dated 2026-09-22 were written on 2026-09-21 (hand-typed date; commit timestamps are authoritative).
- 2026-09-21: Ruling R60 added after the Q1 round-2 review (two-level fairness for unfiltered claims; the cross-kind lag comparison is withdrawn).
- 2026-09-21: R60 corrected after the Q1 round-3 review (coordinator errors): the wait bound is multiplicative and in service time, and the level-1 tie-break is the sequence of the candidate level 2 would hand out. The adapter under review already implements the corrected text (it matched an independent reference model of R60 exactly over 40 seeds × 2,500 operations).

- 2026-09-21 audit correction R-3: admission rejects an elapsed caller deadline, otherwise stores `min(caller_deadline, db_now + preparation + queue + generation budgets)`. Local fake/conformance and G1 cases now agree. Gateway tolerance subtraction and its two obsolete mutants are replaced by clamp/short-budget oracles. Real DB proof remains D2. Exact money uses a fresh explicit Decimal context and context-independent `copy_abs`. Product split supersedes App trace navigation and zero-signup/USD consumer semantics; implementation of the replacement contracts remains F2P/D1R, not an implied rename.
- 2026-09-22: Ruling R61 added after S2M (upload-reference form; pilot `file://` media form with allowed-local-media-path; both EOS ids).
- 2026-09-22: Ruling R62 added after the S2M fact review (revision string carries the public model id; console unprefixed form is a defect; v1 value not renamed; artifact commit pinned by v2 `serving_version`).
- 2026-09-22: R29 row text aligned with the R-3 correction (clamp, not refusal) after the S1 independent review noted the row and the `JobStore.admit` docstring still said "refuses".
- 2026-09-22: R61 (2) amended after the M2 independent review: the pilot local media path carries the profile version segment (`<root>/<org>/<profile>/<digest16>/source.<ext>`).

- 2026-09-22 F2R item 7 (lane B): §5.1 added — the deployment configuration table (`TRACE_SPOOL_SEGMENT_BYTES`, `MAX_INDEX_ITEMS`/`MAX_INDEX_BYTES`, `CONSOLE_CURSOR_SECRET`, the four `DATABASE_POOL_*` names, G1's structure caps and the `LARGE_BODY_*` pair) with its own frozen defaults in `config.DeploymentSettings`, an empty value refused rather than read as unset, and `validate_deployment` called by `validate_runtime` before anything mounts. Two readings recorded rather than assumed: the `DATABASE_POOL_` prefix applies to all four names, and `CONSOLE_CURSOR_SECRET` is validated (≥ 16 characters) but **not required** by the gateway, because coupling inference startup to a console signing key is the wrong dependency — C2 and the G2/I2 checklist own the requirement. The pool numbers and the segment size are ⚠️ TO BE VERIFIED. The G1/Q1 constants are not yet reads of the settings (other tracks' files): an integration request, pinned meanwhile by `test_the_g1_and_q1_constants_match_the_deployment_defaults`. The `spool segment 1` → `2` schema-version edit and `TRACE_SPOOL_SEGMENT_BYTES`'s local default in `traces/spool.py` are F2R lane A's (item 3) and are deliberately untouched here. `§5`'s existing `FETCH_CONNECT_TIMEOUT_S`/`FETCH_TIMEOUT_S`/`FETCH_MAX_REDIRECTS` rows name variables the code spells `MEDIA_FETCH_*` (r1 R2 renamed them and the table was not updated) — reported, not changed, since §5's pilot table is the contract lane's.
- 2026-09-22 F2R item 6 (lane B): console contract revision r2 — `UsageRow.accounting_regime` (`legacy_usd` | `pilot`), the legacy-nullable usage fields, `key_id`/`key_name` null together instead of the `""`/`(deleted key)` sentinels, `ApiKeySummary.trace_mode` nullable-as-off, `usageSummary`/`usageDaily` on `UsageWindowQuery` (the R59 acceptance above, now encoded), `AuditEntry.target_org_id` nullable, `JudgeRun.judge_model_version`/`consent_snapshot_at` nullable and `JudgeSample` frozen to D1's `console_judge_runs` shape `{sample_id, rubric_version, request_id, scores}` with `sample_count` exact below `JUDGE_RUN_SAMPLE_CAP` (50). Encoded in `apps/app/lib/contracts/` with 16 new killed mutants; the Python counterparts of the console-only names (`AccountingRegime`) are recorded as unpaired in `tests/contracts/test_parity_console.py` for F2P.
- 2026-09-22: Ruling R63 added after the Q2 review (per-task Valkey ports registered in `TASK_PORTS`; harnesses read `local_services`).
- 2026-09-22: Rulings R64–R78 added at the F2P additive merge (the fifteen v2 rulings proposed in `01a-contracts-v2-map.md` §8, V1–V15, numbered in order; V3 as amended, V15 added after the F2P review).
- 2026-09-22: Rulings R79–R83 added at the F2R lane A merge (the lane numbered them R63–R67 before R63–R78 existed; renumbered by the coordinator: admission deadline clamp; engine delta payloads; trace metadata charge; staging takes only store-produced refs; one mutation runner).
- 2026-09-22: R83 amended after the F2R lane A review: an `AssertionError` raised inside `infrx/` (outside `contracts/conformance/`) is an undeclared death unless the mutant declares it; the runner runs every named case once unmutated per session and refuses the list if any fails; a collection or fixture error with no tests run is `broken_runner`.
- 2026-09-22 F2R lane A post-merge follow-ups: §5 `spool segment **2**` restored (the lane-A merge had kept the integration copy's `spool segment 1`). The R83 amendment (coordinator ruling after the F2R-A review: a package `assert` is an undeclared death; a pristine baseline per list; a setup/collection error is `broken_runner`) is implemented in `tests/contracts/mutants.py`; the ruling row itself is the coordinator's to record.
- 2026-09-22: Ruling R84 added at the D2 handback (D2 amended 0003/0006/0008/0009 in place with header notes; allowed while unapplied anywhere hosted).
- 2026-09-23: Ruling R85 added at the A1 merge (R-A1 as proposed in `evidence/a/A1-38aea7f.md`, restated through review rounds 2–4; 0010–0014 are D2's migrations, 0015 A1's).
- 2026-09-23: Ruling R86 added at the cancel-cause merge (G1R integration request 10 as proposed in `evidence/g/G1R-6905d13.md`, with the S4 provider_dev refusal and the requested-name rule stated as G1R implemented them). Next free ruling R87.
- 2026-09-23: §5/§5.1 rows for the F2P wire-in names ACCOUNTING_REGIME, ACTIVE_RATE_CARD_VERSION, PROVIDER_DEV_ALLOCATION_CEILING_CREDIT added at the coordinator's schema fix; MAX_INDEX_ITEMS/MAX_INDEX_BYTES moved from §5.1 to §5 (PilotSettings since the wire-in).
- 2026-09-23: Rulings R87–R90 added at the I schema fix merge (the F2P wire-in's four §10 ruling candidates (a)–(d) from `evidence/f/F2P-wirein-92d6bc1.md`, checked against the merged code: `UsageRecordV2.usage`/`outcome` are `| None`, `instantKey` in v2/types.ts, `AUDIT_ACTIONS` in types.ts). Next free ruling R91.
- 2026-09-23: Ruling R91 added at the G2 review (fix round): the replay lookup port method; G2 authorised to add it to `ports.py` and the fakes for this fix only. Next free ruling R92.
- 2026-09-23: Rulings R92–R93 added at the E3B phase 2 review (bk01 restore-comparison artefact; the fake's requeue event id). Next free ruling R94.
- 2026-09-23: Ruling R94 added at the G3 review (the execution mode joins the idempotency identity; G3 implements it in the ingress digest with a case and a mutant). Next free ruling R95.
- 2026-09-23: §5.1 rows `S3_MEDIA_PREFIX` / `S3_ENDPOINT_URL` added by M1-L2 (`codex/m1l2-object-store`, `evidence/m/M1L2-*.md`): the S3 media object store's place in `S3_MEDIA_BUCKET` and an S3-compatible endpoint; the object-store ruling is proposed there, unnumbered. Next free ruling still R95.
- 2026-09-23: Cutover lane: §5 `INFRX_MODE` (unset refuses), `DATABASE_URL` (the three stores from it), `VALKEY_URL` (the gateway needs it or an injected index) and a `S3_MEDIA_BUCKET` row of its own (unset, or a bucket that does not answer HeadBucket, refuses in every mode; never process memory; after the M1-L2 merge); §5.1 `DATABASE_POOL_*` (the one shared pool). No ruling numbered (next free R95).
- 2026-09-23: Cutover lane: §5.1 row `INFRX_RELEASE_SHA` / `INFRX_IMAGE` (E4B's served-build check, `infrx_build_info`). Next free ruling still R95.
- 2026-09-24: Rulings R95–R98 numbered at the merges (the lanes' proposed text): R95 M1-L2's media object store, R96–R97 E4B's two candidates, R98 the cutover's adapters-from-settings rule (R95 and R98 overlap by design: the cutover's refusal names M1-L2's store). Next free ruling R99.
- 2026-09-24: Rulings R99–R103 numbered (the lanes' proposed text): R99 M pilot-media's upload references at use and the durable attach; R100–R103 D5's candidates (i)–(iv). D5's platform_cancelled cancel cause stays a pending input (unimplemented). Next free ruling R104.
- 2026-09-24: I2B-R4 (worker lane): §5.1 row `WORKER_HEALTH_PORT` (8002; `python -m infrx.worker`'s loopback `/readyz`/`/livez`/`/metrics`; read by the runtime, never written by the installer - `preflight.NOT_SETTABLE`), W3 request 6. next free ruling R104 after the merge (R95-R103 numbered by the coordinator).
- 2026-09-24: PREP-WORKER (I2B-R5): §5.1 row `PREPARATION_CONCURRENCY` (the §5 setting, now the worker's preparation pool size; no new name or default). The lane's ruling candidate - `JobStore.prepared(lease, media=(), *, prompt_tokens=None)` on the port, with the fake storing the count and two exported conformance cases - is in its evidence for numbering. Next free ruling still R104.
- 2026-09-24: Rulings R104–R105 numbered (PREP-WORKER's proposed text): R104 preparation's exact count travels with prepared; R105 a preparation count is the engine's or there is none. Its third candidate (a fenced fail_preparation) stays a pending decision. Next free ruling R106.
- 2026-09-24: Ruling R106 numbered (coordinator, from the box certification's dataset-resume cell at 4226315): a cancelled job's replay (`state_conflict`, state cancelled) is terminal for that idempotency key; the E-track client/drill classify it so (CERTIFY-TREE item 5). Next free ruling R107.
- 2026-09-24: R106 wording corrected in place (numbered hours earlier, not yet on main) after the CERTIFY-TREE verifier's N6: a deadline-ended job replays `deadline_exceeded` (retry), not `state_conflict`; a cancel carries no usage and no debit (DUR-SETTLE), the hold stays held until released - the earlier text said "billed for what it generated". The client-tear/platform-cancel distinction (verifier N5) is the drill's, added here so the label cannot overclaim.
- 2026-09-24: Ruling R107 numbered (the TOKCOST count memo; wording = the TOKCOST verifier's, mapped clause by clause to the lane's cases in research/plan/evidence/w/TOKCOST-40440d4.md). Next free ruling R108.
- 2026-09-24: Ruling R108 numbered (INTAKE-DRAIN: a connection-closing refusal first drains a declared body within the intake bounds; the verifier's wording — the lane's overclaimed that every overload refusal closes the connection: `rate_limited` and `journal_capacity_exhausted` keep it). Next free ruling R109.
- 2026-09-24: Ruling R109 numbered (F2C.c published-model projection; the fix-round wording: `project` enforces the serving profile). Next free ruling R110 (reserved R110–R114 for F2C-L's lifecycle rulings, R115 E1C benchmark validity, R116 pre-admission refusal/idempotency).
- 2026-09-24: Ruling R110 numbered (E1C benchmark validity, with the fix-round amendment). Rulings are numbered in merge order: F2C-L's six become R111–R116 at its merge, the pre-admission-refusal ruling (D10/M5) R117, I8's three R118–R120. Next free ruling R111.
- 2026-09-24: Rulings R111–R116 numbered at the F2C-L merge (78661bf0; lifecycle ports, one-phase readiness, cutover without backfill, content lifecycle incl. the reconcile path and per-generation fences, time authority, persisted expiry + read classification; texts from research/plan/evidence/f/F2C-lifecycle-9bf7a95.md). Next free ruling R117 (pre-admission refusal/idempotency, D10/M5), then I8's R118–R120.
- 2026-09-25: Rulings R117–R118 numbered at the G8 merge (e309b3fa; regime transition with the fix-round wait-out amendment; operator-write races). Numbering follows merge order, so the pre-admission-refusal ruling (D10/M5) and I8's three take the next free numbers at their merges. Next free ruling R119.
- 2026-09-25: Rulings R119–R121 numbered at the I8 merge (63011a31; recovery proves service incl. the settlement-unit clause and the schema-proof clause; pooler discipline; versioned env file — the lane's provisional numbers R109–R111 collided with the integration branch and are renumbered here). Next free ruling R122.
- 2026-09-25: Rulings R122–R127 numbered at the D10 merge (codex/d10-merge dfc4fa75, verified head 483d9eca): pre-admission refusal binds nothing (D10/M5); readiness is one phase with the interim `runtime_unmarked_doors` clause; PostgreSQL decides deletion incl. discovered orphans and dot segments; persisted expiry is the only read authority; discovery prices what admission charges; dedicated logins. Next free ruling R128 (M5 upload tickets), then M6 (R129), G7 (R130–R131), W5 (R132–R135) at their merges.
- 2026-09-25: Ruling R128 numbered at the M5 merge (codex/m5-merge ccf37b55, verified head c0c4ce25): upload tickets across processes. Next free ruling R129 (M6, at the phase-2 merge), then G7 R130–R131, W5 R132–R135.
- 2026-09-25: Rulings R130–R131 numbered at the G7 merge (codex/g7-merge 739c0414, verified head 2f7ed3ac): stalled dependencies bounded per stalled call; revocation bound on the read routes (the identity-source-outage case is P-26). R129 is reserved for M6 (phase-2 merge). Next free ruling R132 (W5).
- 2026-09-25: R117 and R118 amended at the G8 lock-bound merge (codex/g8-transition-lock b06e3757): one total bound over the freeze and every flag write on the D10 FOR SHARE schema; a refused freeze reports `applied: []`. D10 wiring request (G8-c26ef96.md V-G8TL-2): a SECURITY DEFINER `infrx.set_feature_flag` taking `lock table infrx.feature_flags in exclusive mode` so a freeze under sustained admission traffic succeeds fairly.
- 2026-09-25: Ruling R129 numbered at the M6 phase-2 merge (codex/m6-phase2-merge 0007fc16, verified head ff5aedb1): content deletion authority and bounded caches; R114 amended (generation naming: tombstone + lease margin, bare keys). R110 amended at the E1C profile-flip merge (532d075a): the certify opt-out is gone. Next free ruling R132 (W5).
- 2026-09-25: Ruling R132 numbered at the PROFILE-CREDIT-CAP merge (codex/profile-credit-cap bf7b0040, merge 93fdab4e): run-profile spend amounts in the profile's currency, compared exactly; legacy `*_usd` keys USD-only. Merge order moves W5 to R133–R136. Next free ruling R133 (W5).
- 2026-09-25: Ruling R133 numbered at the E4C-PREP merge (codex/e4c-prep b67d4b6c, merge dafd4030): E4C's predeclared criteria (P-18) and the no-loosening rule. Merge order moves W5 to R134–R137. Next free ruling R134 (W5).
- 2026-09-26: Ruling R134 numbered at the I2A-PREP merge (codex/i2a-prep 8ca672d0, merge a5ca1cd1, wiring 416da075): App deployment configuration as code. Merge order moves W5 to R135–R138 and W5-F5 to R139. Next free ruling R135 (W5).
- 2026-09-26: Rulings R140–R143 numbered at the APP-UNION round-2 merge (codex/app-union-2 f0d592f5, merge dec15244, wiring b30e6aa0): C3A keys/grant/privacy, U4 request detail, U2 key plaintext/Settings, U3 operator writes. R135–R139 stay reserved for the backend union merge (W5 R135–R138, W5-F5 R139) so the drafted cross-references hold; the table is ordered by number. Next free ruling R144.
- 2026-09-26: Rulings R135–R139 numbered at the wave4b union merge (codex/wave4b-union d9e72c9c, merge 04ae5e21): W5 readiness barrier (with the W5-ADMIT-WIRING gateway sentence), fail_preparation, R105 video bound amended, reconciliation gauges; W5-F5 committed readiness is final. R144–R145 numbered from D10-FOLLOWUP §6 (regime flag writes; written re-registration grace); R123 amended (0023 door revoke). Next free ruling R146.
- 2026-09-26: R144 amended at the G8-FLAG merge (a30631a8): the audited operator verb `flag` is the App's cutover-rollback writer (WR-G8FLAG-2); the maintenance switch drill is the recorded exception (WR-G8FLAG-3 decided: exception, not a rewrite of bk04/rb03). Next free ruling R146.
- 2026-09-26: R146 numbered at the D10-MERGE-2 merge (f95377b9; 0024 `consumer_may_create_key` / `consumer_credit_ledger` / `consumer_jobs` filters; D10-APP-SQL fix-round ruling 1-D10R-2); R143 amended for 0025's named codes, the no-op re-revoke keeping its key (0-CM-1) and the advisory-lock serialization. Next free ruling R147.
- 2026-09-26: R146 App-side clause added at the APP-0024-WIRE merge (WR-0024W-1). Next free ruling R148.
- 2026-09-26: R147 numbered at the E3C-CELLS merge (cce9224c; F-1 unfenced `put_result`, the D10 migration 0026 pending). Next free ruling R148.
- 2026-09-26: R147 amended at the D10-0026-FENCE merge (400a7e94): the fence as landed, the lease-less call kept for the P-25 rollback targets, the follow-up migration. Next free ruling R148.
