# D2 — atomic admission, durable preparation and dispatch outbox

| Field | Value |
|---|---|
| Task | D2 (track D), oracles DUR-ADMIT, DUR-CAP, DUR-OUTBOX; plus the D1R review follow-ups (a)–(g), M3 request 1, W2 requests 3–4, M3 request 4 (`is_live`) |
| Status | **implemented** (real PostgreSQL, both images; Q2's Valkey adapter on D2's own Valkey). Not integrated (no coordinator merge); **nothing applied to any hosted project**; nothing pushed. |
| Base SHA | `f302091` (claude/backend-impl, the D1R merge) |
| Implementation SHA | `8728aec` |
| Branch / worktree | `codex/d2-admission-outbox` in `.claude/worktrees/codex-d2` |
| Classification | local only. Containers created and removed by this run: `infrx-d1-postgres[-supabase]` (the D harness, labelled with this checkout, port 55432) and `infrx-d2-valkey` (127.0.0.1:55463, labelled). None were left running (`docker ps -a` at the end shows only `infrx-q3-valkey`, which belongs to another checkout and was not touched). |

## Commits (one per item)

| Item | Commit | Killing tests (named) |
|---|---|---|
| 1 admission (0011, `PgJobStore.admit/admit_credit`, frozen test clock) | `d6b3823` | `tests/d/test_admission.py::test_admission__*` (5), conformance `dur_admit__*`/`dur_cap__*` |
| 2 prepare→queued, both dispatch kinds, relay + ack (0012, `outbox.py`) | `b23f161` | `test_prepare__*`, `test_outbox__pending_redelivery_superseded_and_snapshot`, `tests/d/test_outbox_relay.py` (5 drills × fake/memory/Valkey) |
| 3 outbox expiry/GC (0013) | `1095192` | `test_outbox__expiry_and_gc_never_delete_what_a_consumer_needs` |
| 4 `is_live`, `put_result`/`read_result` (0014), prepared `prompt_tokens` | `6b986fa` | `test_results__write_once_owner_read_and_the_prepared_prompt_count`, `tests/d/test_store_requests.py` (3) |
| 5 M3 tables (0010) | `4d8ed00` | `test_media__uploads_finalize_once_and_objects_delete_only_when_idle` |
| 6 v1 JobStore conformance on the real store; E3B dr01/dr02 | `e7f2883` | `tests/d/test_jobstore_conformance.py` (strict partition), `tests/d/test_e3b_drills.py` |
| D1R follow-ups (a)–(e) | `a022250` | `check_credit_admission_rows` (+4 refusals, 1 changed control), `check_money_unit_cases`, `check_no_unit_conversion`, `check_resolve_pins`, `check_registry`, `check_credit_privileges`, `check_operator_seams` |
| mutants | `de8d921` (WIP), `c82b8a1` | `tests/d/test_migration_mutants.py`, `tests/d/test_code_mutants.py` |
| fix | `8728aec` | the dev-budget case now goes through the provider's own key (follow-up (a) had made `d1r_dev_budget_pays_for_prod` survive) |

## What was built

| Path | Content |
|---|---|
| `apps/app/supabase/migrations/0011_admission.sql` | Body of D1's `infrx.admit(jsonb)` (signature and grants unchanged). One transaction: global capacity-scope lock → idempotency compare/replay/409/410 (tombstone expiry derived from `settled_at + ttl`, so an active job never expires) → R6 → tenant coherence (idem org, media org) → recheck key revoked/foreign, org suspended, entitlement → R29/R79 (refuse `<= db_now`, clamp to `db_now + prep+queue+gen`) → R55 ceilings → capacity per scope (total/org/key/preparation/journal, 429 + Retry-After) → price (legacy: `price_versions` effective now) or pins (`resolve_admission_pins`, wallet resolved from the KEY's identity, R66) → store-derived ceiling hold (R53, `ceiling_8`; CREDIT never zero) → available balance (402) → job + hold + 3 reservations + `prepare_dispatch` + mapping. Legacy USD and CREDIT bodies are separate functions (no body reads both units). New job columns `request_record`, `idem_payload_hash`, `prepared_refs`, `prepared_prompt_tokens` (immutable / write-once). `infrx.job_admission(uuid)` is the admission document. |
| `…/0012_dispatch_outbox.sql` | `claim_preparation` (own generation, one live lease, bounded retries, R29 deadline first, R39 terminalize-commit-refuse), body of `infrx.prepare(jsonb)` (fenced preparing→queued, tenant-checked refs, prompt count ≤ `max_input_tokens`, R38 queue instant, `inference_dispatch`), `terminalize_unstarted` (preparation_failed path only: hold released per unit, reservations/attempts released, usage/trace projections), `dispatch_pending` (at-least-once, redelivery window, superseded rows acknowledged), `acknowledge_dispatch`, `dispatch_snapshot` (rebuild truth; excludes a live preparation lease and terminal jobs). |
| `…/0013_outbox_gc.sql` | `gc_outbox`: expire unacknowledged dispatch rows of terminal jobs; delete acknowledged rows past retention unless the aggregate is a live job or the row is `callback_delivery`; bounded, `skip locked`. |
| `…/0014_job_results.sql` | `infrx.job_results` (one immutable tenant-bound row per job, ≤16 MiB), `put_result` → `infrx-result:<id>` (replay same text, `state_conflict` otherwise), `read_result(org, ref)` owner-only. |
| `…/0010_media_uploads.sql` | M3 request 1: `media_uploads` (handle grammar, finalized facts exactly when finalized, one-way states, immutable after, finalized kept until expiry), `media_objects(storage_ref PK, org_id, last_used_at)`, `touch_media_object` (tenant-checked), `delete_media_object_if_idle(ref, observed)` (the conditional delete). RLS on, no browser grants, no TRUNCATE. |
| `0003` (amended in place) | `infrx.now()` also honours a test-only frozen instant (`infrx_test.clock.frozen_at`) behind the same two barriers (fixture-only table, `infrx_%` database gate). 0003 is applied to no hosted project (D1R Limits 1). Needed because conformance compares instants exactly. |
| `0006`, `0008`, `0009` (amended in place, notes in headers) | see follow-ups below |
| `apps/infrx-api/infrx/state/jobstore.py` | `PgJobStore`: `admit`, `admit_credit`, `get_owned`, `get_owned_credit`, `claim_preparation`, `prepared(…, prompt_tokens=)`, `dispatch_pending`, `acknowledge_dispatch`, `dispatch_snapshot`, `gc_outbox`, `is_live`, `put_result`, `read_result`; D3/D5 operations (`claim`, `heartbeat`, `cancel`, `complete`, `recover`, `load_work`) raise `NotImplementedError` naming the owner. Error mapping: P0001 `<code>: …` → the most specific `DomainError` (hint `retry_after=`), 23514 `credit_wallets_reserved_within_total` → 402, 55000 → `dependency_unavailable` (Retry-After 30), P0002 → `not_found`, 22023 → `invalid_request`; anything else re-raised. `connector(dsn)` = one connection per call with `set role service_role` (ponytail: pool later). |
| `…/infrx/state/outbox.py` | `OutboxRelay(store, scheduler).pump()` enqueue-then-acknowledge; a full index defers; `rebuild()` from `dispatch_snapshot`. |
| `…/infrx/state/pgtesting.py` | Test support: the conformance `Harness` over PostgreSQL (`make_jobstore_factory(fresh_database, dsn_for)`, `seed`, `PgClock`, `FailingJobStore`, hooks). `unrevoke_key`/`set_price` step around their production one-way guards for one statement, test database only (documented). |
| `…/infrx/state/test_clock.sql` | `frozen_at`, `freeze(at)`, `unfreeze()`; functions now read the clock after their own update (the old `update … returning infrx.now()` read the pre-update snapshot). |
| `…/infrx/state/credit_schema.py` | D2 seams (catalog-checked by `check_seams`), `LOCK_ORDER`, the D5 rule. |
| `apps/infrx-api/tests/d/*` | `checks_admission.py`, `checks_dispatch.py`, `checks_media.py`, `pgstore.py` (template clone per harness), `vkstore.py`, `test_admission.py`, `test_outbox_relay.py`, `test_store_requests.py`, `test_jobstore_conformance.py`, `test_e3b_drills.py`, `test_adapter_units.py`, `code_mutants.py`, `test_code_mutants.py`; D1 checks adjusted for the filled boundaries (`FILLED_RPCS`, `FILLED_BOUNDARIES`: body may change, SECURITY DEFINER + ACL may not). |

Lock order (0011 header, `credit_schema.LOCK_ORDER`): capacity scope advisory lock → idempotency row → organization (share) → key (share) → wallet (update). A grant takes only the wallet; D5 must take the wallet without the scope lock. ponytail: one global admission lock (the `total` cap is platform-wide anyway); per-scope counters if throughput matters.

## Requirement coverage

| Test | Invariant |
|---|---|
| `test_admission__one_acceptance_owns_one_job_hold_reservations_dispatch` | one USD admission = +1 job (preparing, store snapshot, ceiling hold), +1 held USD hold reserving exactly it, +3 active reservations (1,1,16 MiB), +1 `prepare_dispatch` naming the handle, +1 mapping; admitted request immutable; far deadline clamped to `db_now + horizon`; hold rounds UP (`0.11111111`×2 → `0.00000023`); one CREDIT admission = every pin == `resolve_admission_pins`, the individual's own wallet, a held CREDIT hold of `14.74560000`, USD untouched |
| `test_admission__every_refusal_is_typed_and_owns_nothing` | 26 refusals (`26 refusals typed, each leaving no job/hold/reservation/dispatch`), each its exact code and an unchanged footprint (jobs, both hold tables, reservations, outbox, mappings, both reserved totals): elapsed / equal-to-now deadline, revoked key, another org's key, suspended org, unentitled, zero/over output, zero input, context, unpriced, withdrawn price, USD 402, foreign idem scope (403), foreign media (404), legacy flag off (maintenance), unknown regime; CREDIT: unknown model, private dev deployment (404, R70), flag off, operator key (403), no wallet (404), wallet through another org (403), 402, zero hold (400), past the deployment's limits |
| `test_admission__each_capacity_scope_refuses_on_its_own` | total, org, key, preparation (R1), journal — each with the others slack, with Retry-After ≥ 1, owning nothing; another org/key still fits |
| `test_admission__replay_conflict_and_the_terminal_tombstone` | replay = same document, nothing reserved; changed payload 409; R6 without key / with a late key 409; tombstone replays at terminal+24h−1µs, 410 exactly at terminal+24h; an active job's mapping replays after 10×TTL |
| `test_admission__concurrent_admissions_and_grants_keep_the_lock_order` | 5 rounds × 8 concurrent admissions (2 orgs/keys) + 2 grants: exactly 3 admitted, no deadlock, reserved == Σ held holds, available ≥ 0 |
| `test_prepare__fenced_preparing_to_queued_with_one_inference_dispatch` | one live preparation lease; foreign worker / stale generation / inference token / expired lease → `stale_lease`; foreign media → 403; queued with refs stored, preparation unit + attempt released, queue instant `min(now+10 s, deadline)`, exactly one `inference_dispatch`; spent lease stale; expired lease superseded at generation 2 |
| `test_prepare__the_preparation_deadline_terminalizes_in_the_same_call` | claim past the deadline, a late `prepared`, and the retry bound each terminalize (`preparation_failed`, `released_free`), commit and refuse; USD and CREDIT holds released, reservations/attempts released, projections written |
| `test_outbox__pending_redelivery_superseded_and_snapshot` | a claimed unacked row is not re-sent inside its window and is re-sent after it; ack idempotent; acked never re-sent; superseded row acked, not dispatched; snapshot excludes a live preparation lease and terminal jobs, includes the job once the lease expires |
| `test_outbox_relay.py` × {fake, memory, valkey} | both kinds indexed once and acknowledged; lost ack → the SAME single candidate; duplicate delivery (replay + rebuild while held) → one `claim_preparation` wins, the other `not_claimable`, a forged lease is stale, one `inference_dispatch`; lost index + restarted relay → rebuild has every accepted job, the leased one after its lease; full index defers, never drops (memory, valkey) |
| `test_outbox__expiry_and_gc_never_delete_what_a_consumer_needs` | terminal dispatch rows expired (acked `expired: job terminal`); nothing deleted inside the retention or at its instant; deleted just after; never a live job's row, an unacked projection or a callback row; `limit` bounds each call |
| `test_results__…`, `test_store_requests.py` | result write-once (replay/409/404), owner-only read (other org, traversal, wrong id → 404); prompt count stored, refused past `max_input_tokens`, booleans refused; `is_live` true while preparing/queued, false terminal/unknown/malformed |
| `test_media__…` | 10 upload shapes refused; M3's finalize-once UPDATE wins once, not for another org; finalized immutable, kept to expiry, collectable after; `bytes ≤ max_bytes`; touch tenant-checked; conditional delete loses to a re-use; RLS on, no browser grants, no TRUNCATE |
| `test_jobstore_conformance.py` | the v1 partition below |
| `test_e3b_drills.py` | E3B dr01 and dr02 assertions verbatim, on this rig |
| `test_adapter_units.py` | the Python half with no DB (refusal mapping, R39 refusal-as-data, store limits/budgets sent, CREDIT regime, `get_owned` on a CREDIT job, `is_live` guards, prompt-count type, relay ack order, `crash_after_commit` commits first) |

### v1 JobStore conformance against the real store (70 cases, both images)

`tests/d/test_jobstore_conformance.py`: **19 pass**, **44 strict xfail** (a pending case that starts passing fails the run), **7 skipped naming the hook** (never a pass).

- Pass (19): `dur_admit__acceptance_creates_job_hold_reservations_and_outbox`, `…idempotent_replay_returns_the_same_identity`, `…a_request_uuid_is_admitted_once`, `…changed_payload_with_the_same_key_is_a_conflict`, `…crash_after_commit_then_retry_does_not_double_reserve`, `…revocation_and_suspension_are_rechecked_in_the_transaction`, `…an_idempotency_scope_belongs_to_the_requests_own_org`, `…a_deadline_must_be_one_the_store_can_keep` (R29 on the DB clock), `…a_refused_admission_reserves_nothing`, `…the_token_ceilings_are_range_checked`, `…a_replay_reports_the_original_hold_and_price`, `dur_cap__a_credit_grant_is_never_negative`, `…admission_reserves_preparation_capacity`, `…journal_reservation_must_fit_the_global_budget`, `…hold_cannot_exceed_the_available_balance`, `…a_hold_is_checked_against_available_not_the_ledger`, `…a_negative_maximum_hold_is_refused`, `…the_hold_rounds_up_never_half_up`, `…concurrent_admissions_never_oversubscribe`.
- Cannot pass yet, by owner: **D3 `claim`** (inference lease) — 16 fence/output cases; **D3 `recover`** — 4 (`dur_fence__preparation_is_claimed_and_fenced_like_execution`, `dur_output__a_lost_preparation_worker_is_reaped_within_bounds`, `…an_accepted_job_keeps_its_admission_budgets`, `…a_late_preparation_worker_finds_a_terminal_job`); **D3 `heartbeat`** — 1; **D3 `load_work`** — 1; **D3 claim then D5 `complete`** — 21 (the 4 tombstone `dur_admit__*` cases, 16 `dur_settle__*`, `dur_outbox__every_transition_emits_its_projection`); **no real store can pass** — 1: `dur_cap__total_org_and_key_limits_reject_with_retry_guidance` pairs KEY_B with ORG_A and KEY_A with ORG_B, and a key belongs to one organization (`jobs_key_belongs_to_org`, R59-6) → `invalid_api_key` (integration request 3). The per-scope limits pass in SQL (`check_admission_capacity`).
- Skipped: `publish` ×6 (StreamStore, D4), `unsettleable` ×1 (D5).

## Environment

Linux 7.0.0-1010-aws x86_64; Python 3.12.3 (`make api-env`), psycopg 3.3.6, valkey-py 6.1.1; Docker server 29.6.2. Images: `postgres@sha256:33f923b0…5e20` (16.14 + shim) and `supabase/postgres@sha256:7768d0d1…e8fd` (17.6.1.173, no shim, `INFRX_D1_IMAGE=supabase`); `valkey/valkey@sha256:d2e18f34…43d1` (8.1). Node 22.23.1 present but `apps/app/node_modules` absent; no TypeScript touched.

## Commands and results (2026-09-22 UTC; the sweep after `8728aec` — logs quoted)

| Command (in `apps/infrx-api`) | Exit | Result |
|---|---|---|
| `INFRX_MUTANTS=all uv run --frozen pytest -q -p no:cacheprovider -rs tests/d` (plain) | 1 | `1 failed, 402 passed, 7 skipped, 44 xfailed in 425.99s` — the one failure is D1R's race mutant `d1r_grant_race_arbitrates_one_index` SURVIVED once (see Limits 5) |
| same, `INFRX_D1_IMAGE=supabase` | 0 | `403 passed, 7 skipped, 44 xfailed in 502.78s` |
| the race mutant alone, 5 repeats (plain) | – | killed 4 of 5 (`round 0: only 7 of 8 callers returned: ['UniqueViolation…` ×4; 1 `1 failed`) |
| `d1r_dev_budget_pays_for_prod`, `d2_provider_job_through_any_key`, credit_admission, both images | 0 / 0 | `4 passed` / `4 passed` (the fix in `8728aec`; the previous sweep had it SURVIVED on both) |
| `uv run --frozen pytest -q -p no:cacheprovider --ignore=tests/w/test_loop_mutants.py` (≈ `make api-test`) | 1 | `6 failed, 2570 passed, 7 skipped, 44 xfailed` — all 6 in `tests/m/test_uploads.py` (4), `tests/m/test_prepare.py` (1), `tests/w/test_loop.py` (1) |
| track-first ordering (same ignore) | 1 | `6 failed, 2570 passed, 7 skipped, 44 xfailed` (same 6) |
| legacy-first ordering (same ignore) | 1 | `8 failed, 2568 passed, …` — the same 6 plus 2 in `tests/q/test_valkey_mutants.py` (`broken_runner`: `RuntimeError@vkharness.py`, `CalledProcessError`: Q's own Valkey container start, not D2) |
| those 6 tests on `git archive f302091` (the base, same venv) | 1 | `6 failed, 149 passed` — **pre-existing on the base**; nothing D2 changed is imported by `tests/m` or `tests/w` |
| `make api-test` / `make check` | 2 | **not passing on the base either**: collection error `tests/w/test_loop_mutants.py - TypeError: Mutant.__init__() got an unexpected keyword argument 'allowed_errors'` (W's list vs the shared runner). Hence the `--ignore` runs above; `make check` therefore never reached `api-mutants`, console or bench targets — **not run** (skips are not passes). |
| `uv run --frozen pytest -q tests/d/test_code_mutants.py` | 0 | `20 passed` (19 mutants killed + the list check) |
| `make integration` | – | not run (`tests/integration` not touched) |

Mutants (R32/R40), both images through the D harness (full list inside the `tests/d` runs above): 72 D2 migration mutants (68 on 0010–0014/0006/0008 in `D2_MUTANTS`, 4 `d2_*` in the D1R section for follow-ups (a)/(b)), each killed by its named check; D1R's two guard mutants re-anchored to the split guard. `d2_admission_lock_dropped` dies by the concurrency check (`round 0: 8 admitted against room for 3`, 3/3 repeats). Total D list 265. 19 code mutants (`jobstore.py`, `outbox.py`, `pgtesting.py`) through the shared runner (`Runner(name="d2", targets=("tests/d/test_adapter_units.py",))`), all killed. Mutant-run findings fixed: bare `assert`s made the runner itself crash (`_first_line` of an empty message) — every check assertion now has a message; the tombstone check raised instead of asserting; the rounding case was exact at 8 digits (moved to `0.11111111`); follow-up (a) made `d1r_dev_budget_pays_for_prod` survive (case fixed in `8728aec`). Layered invariants with no single-edit break: the CREDIT 402 (explicit check + `credit_wallets_reserved_within_total`), a terminal job in the snapshot (state filter + `dispatch_wanted`).

## Failure drills

- **Crash after commit (dr01)**: `FailingJobStore` lets `infrx.admit` commit, then raises `CrashAfterCommit`; the retry replays: 1 active job, outbox kinds `[prepare_dispatch]`, reserved == hold > 0, ledger conserved.
- **Refused admissions (dr02)**: revoked key, withdrawn price, full key each refuse typed with no outbox row; ORG_B reserved 0; journal grew by exactly one reservation.
- **Lost acknowledgment / duplicate delivery / lost index / full index**: the relay drills above, on three backends; durable state read back (outbox ack columns, attempts, job state) after each.
- **Preparation deadline**: terminalization commits before the refusal (R39) on both claim and prepared paths, for USD and CREDIT holds.
Cleanup: every container this run created was removed (`atexit`); template/clone databases lived in them.

## D1R review follow-ups

- (a) done: `jobs_credit_admission_guard` (0006) — consumer wallet only on public+active (R70 in the database); provider_dev job only through its provider's `provider_dev` key in that organization. Cases + mutants `d2_consumer_spends_on_a_private_deployment`, `d2_provider_job_through_any_key`.
- (b) done: `no_unit_conversion` and `money_unit_cases` registered, mutants `d2_usd_statement_reads_credit`, `d2_credit_ledger_rounds_to_cents`; `money_unit_cases` now runs through all 7 CREDIT amount column types (the old literal cast could not be killed by any migration); USD tokens += `console_legacy_usd_statement`, `legacy_usd`.
- (c) done: rate text == `rate_card_marlin`; reconciliation with an `unknown` hold outstanding (reserved stays `10.01440000`); repoint to a real second serving version + deployment + card refused by the pins guard; `active → proposed_public` refused; no service_role UPDATE on the 7 immutable registry relations; bootstrap onto a consumer key's hash refused (23505, the consumer key is not converted). Each refusal's SQLSTATE/message checked by hand once (listed in the session log: 23514 for guards, 23505 for the bootstrap).
- (d) recorded in `credit_schema.D5_SETTLE_HOLD_BEFORE_DEBIT`: **D5 must settle the hold (held→settled) before inserting the `inference_debit`**; a debit first trips `credit_wallets_reserved_within_total` when the hold equals the available balance. D2 never writes a zero CREDIT hold (`d2_zero_credit_hold_written`).
- (e) `usage_records` "both or neither" documented (0009). **Declined**: `console_legacy_usd_statement.as_of = infrx.now()` — browser sessions cannot execute `infrx` functions (0004), and switching the function to SECURITY DEFINER makes `is_service_client()` true for every caller (it tests `current_user`), which the cross-org test caught (`cross-org legacy statement: None`); reasoning recorded in 0008. `as_of` is a display instant, not an R7 decision.
- (f) D1R Limits 3 reworded: the signup grant is independent of legacy USD history — a nonzero USD balance never blocks, converts or funds the 10,000 CREDIT grant; `console_legacy_usd_statement` carries `rollout_hold` for that balance. Note for C0/U: `public.console_credit_ledger` projects an operator adjustment's `reason` text to the customer (actor masked `platform`, reason not) — operators must write customer-safe reasons.
- (g) recorded, not fixed: G6B/L3 input — a higher `catalog_listings` version pointing at a `proposed_public` deployment darkens the alias (`resolve_admission_pins` picks it and answers not_found); operators set the listing `effective_at` at/after activation, or resolve should skip non-active listings.

## Limits

1. Not applied to any hosted project. 0003, 0006, 0008, 0009 are amended in place (all unapplied anywhere, per D1R Limits 1); 0010–0014 are new.
2. D2 terminalizes only on the preparation path (`preparation_failed`); the settling transaction (usage row, terminal journal event, `complete`, cancel, recover) is D3/D5. Terminalization writes `usage_projection`/`trace_projection` rows with the fake's payload shape; D5 may replace it.
3. CREDIT admission covers **consumer** keys through public listings. provider_dev endpoint resolution (`resolve_endpoint_pins`) is not built: a provider_dev key is refused `not_found` by `admit_credit` (the database guard for provider_dev jobs exists). The v1 port has no CREDIT read: `get_owned` refuses a CREDIT job; `get_owned_credit` returns `AdmissionV2`.
4. The admission lock is one global advisory lock (ponytail, documented); the per-connection `connector` opens a connection per call (pool: G/coordinator wiring).
5. `d1r_grant_race_arbitrates_one_index` (D1R's) is probabilistic: 1 survival in the plain sweep, killed 4/5 on repeat. Not D2 code (0006 grant untouched); reported for D1R/coordinator.
6. Pre-existing on the base, not D2: `tests/w/test_loop_mutants.py` collection error (breaks `make api-test`/`make check`), 6 failing `tests/m`/`tests/w` tests (reproduced on `git archive f302091`), and Q's Valkey-harness flake in the legacy-first run.
7. `media_uploads.max_bytes` is bounded at the frozen `MAX_MEDIA_BYTES` default (64 MiB); `duration_s` is required when finalized, as M3 specified verbatim (an upload with no decoder cannot finalize).
8. `set_price`/`unrevoke_key` test hooks disable one trigger for one statement in the test database (production cannot undo either); the `infrx_test` clock is fixture-only and gated.
9. Results are purged by nobody yet (retention follows `jobs.result_expires_at`, D5 sets it).

## Migration / rollback (per migration; drop order reverse)

- **0014**: drop `infrx.read_result`, `infrx.put_result`, table `infrx.job_results` (only before any result is served; after that, keep it — results are customer data under retention).
- **0013**: drop `infrx.gc_outbox`, index `outbox_acknowledged_idx`. Nothing depends on it; without it outbox rows are simply kept.
- **0012**: drop `dispatch_snapshot`, `acknowledge_dispatch`, `dispatch_pending`, `dispatch_wanted`, `index_event`, `claim_preparation`, `lease_doc`, `terminalize_unstarted`, `release_hold_credit`, `release_hold_legacy_usd`; restore `infrx.prepare(jsonb)` to 0004's fail-closed stub (re-run 0004's DO block for `prepare`). Drain first: no job may be `preparing` under a lease (feature flags off stops new admissions).
- **0011**: set `legacy_usd_admission`/`credit_admission` off; restore `infrx.admit(jsonb)` to the stub; drop the helpers (`admit_legacy_usd`, `admit_credit`, `admission_*`, `job_admission`, `journal_bytes_charged`, `is_entitled`, `hold_for`, `admission_lock_key`, `refuse`, `refuse_json`), trigger `jobs_admission_record_guard` + function, index `jobs_journal_stored_idx`, constraints `jobs_request_record_bounded`, `jobs_idem_payload_hash_shape`, `jobs_prepared_refs_shape`, `jobs_prepared_prompt_tokens_within_ceiling`; the four columns may be dropped only if no admitted job needs them (never drop once jobs exist — keep them).
- **0010**: drop the two functions, `media_uploads_guard`, tables `media_objects`, `media_uploads` (only before M3 writes rows).
- **0003/0006/0008/0009 amendments**: revert the files (unapplied anywhere). If ever applied: `infrx.now()` without `frozen_at` is the old body; the guard split is behaviour-equivalent except the two new refusals.

## Handback

Next unblocked: **D3** (claim/heartbeat/cancel/recover/load_work over `infrx.attempts`; reuse `terminalize_unstarted`'s release helpers and the conformance partition — flip the D3 xfails), **D5** (settlement; the hold-before-debit rule), **E3B** (swap the postgres rig), **M3** (wire `media_uploads`/`media_objects`, `is_live`), **W3/G** (`put_result`, `Work.prompt_tokens`), **Q3** (drive `OutboxRelay.pump/rebuild` on a timer).

### integration_requests

1. **E3B**: in `tests/integration/backend/test_drills.py`, `rig("postgres", …)` → build `infrx.state.pgtesting.make_jobstore_factory(fresh_database, dsn_for)` on E2's PostgreSQL (`fresh_database` = a clone of a database migrated with every migration + `test_clock.sql` + `pgtesting.seed(conn)`, named `infrx_*`), grant `GRANT` to both orgs via `h.extra["grant"]`. dr01/dr02 pass verbatim on this rig (`tests/d/test_e3b_drills.py`).
2. **Coordinator / `tasklocal.TASK_PORTS`**: `"d2": {"postgres": 55433, "valkey": 55463}` (the D2 Valkey port is used as a literal in `tests/d/vkstore.py`, overridable by `INFRX_D2_VALKEY_PORT`).
3. **F2 conformance owner**: `dur_cap__total_org_and_key_limits_reject_with_retry_guidance` reuses KEY_B in ORG_A and KEY_A in ORG_B; give each org its own second key (e.g. `KEY_A2`) so a real store with composite key ownership can run it.
4. **Coordinator contracts (W2 request 3)**: add `Work.prompt_tokens: int | None = None`; D3's `load_work` fills it from `jobs.prepared_prompt_tokens` (stored by `prepared(…, prompt_tokens=)`). Also consider `MediaStore.put_result` vs `JobStore.put_result`: D2 ships it on `PgJobStore` (the object is in PostgreSQL).
5. **Coordinator contracts**: a v2 JobStore read (`get_owned_credit` → `AdmissionV2` + outcome) and admission (`admit_credit`) belong in `contracts/v2/ports.py`; today they are adapter methods.
6. **Makefile**: `api-mutants` += `tests/d/test_code_mutants.py`; fix or drop `tests/w/test_loop_mutants.py` (collection error on the base breaks `make api-test` and `make check`).
7. **G/coordinator wiring**: construct `PgJobStore(pool_connect, limits=settings.pilot)` with a psycopg pool whose `configure` runs `set role service_role`; run `OutboxRelay(store, scheduler).pump()` on a timer and `rebuild()` at scheduler start; `gc_outbox()` daily. `OUTBOX_RETENTION_S` (7 d) wants a `PilotSettings` field.
8. **D1R/coordinator**: `d1r_grant_race_arbitrates_one_index` is probabilistic (killed 4/5 here) — raise its rounds or make the race deterministic.
9. **G6B/L3** (follow-up (g)): listing-version activation rule above.

Nothing was applied to any hosted project; nothing was pushed.

## Verification log

- 2026-09-22: Written by the D2 implementation session at `8728aec`; counts quoted from the sweep logs (plain 22:07–22:14Z, supabase 22:14–22:23Z, orderings to 22:50Z) and the targeted reruns after them.
