# D1R — additive CREDIT and minimal provider schema

| Field | Value |
|---|---|
| Task | D1R (track D), oracles CREDIT-GRANT, CREDIT-IDENTITY, CREDIT-UNITS, CREDIT-RATE, CREDIT-SPEND (schema half), DUR-RLS, R59 privilege discipline; plus the coordinator's 2026-09-22 G6B/M3 inputs (0009) |
| Status | **implemented** (real PostgreSQL on both images). Not integrated (no coordinator merge), **not applied to any hosted project**; nothing pushed. |
| Base SHA | `9faaa57` (claude/backend-impl, F2P additive merged) |
| Implementation SHA | `e538c9a` |
| Branch / worktree | `codex/d1r-v2-schema` in `.claude/worktrees/codex-d1r` |
| Classification | local only; task-local containers created and removed by the D harness (E2R-repaired, labelled with this checkout) |

## Commits (one per item)

| Item | Commit | Content |
|---|---|---|
| 1 | `77405a3` | 0001–0005 inventory + upgrade fixture from the exact 0005 schema with accepted old-regime jobs |
| 2 | `402b758` | `0006_credit_accounting.sql` |
| 3 | `e164652` | `0007_provider_registry.sql`, operator seed `infrx/state/seed_marlin_provisional.sql` |
| 4 | `fe2ad68` | `0008_credit_read_surface.sql`, privilege maps, extended role matrix |
| 5 | `88b712b` | re-run no-op, legacy read path, fail-closed flags, Supabase README |
| 6 | `cbc13dd` | `infrx/state/credit_schema.py` seam map checked against the catalog |
| verification | `2d5e26d`, `ba47bd0`, `3957a7a`, `2a94186` | 10^5-row plans, leaky probe, single-reason cases, mutants |
| coordinator inputs | `ba913f5` | `0009_operator_seams.sql` (G6B handback ruling) |
| fix | `e538c9a` | grant race (found by the legacy-first run) + image-aware verified_user check |

## What was built

| File | Content |
|---|---|
| `apps/app/supabase/migrations/0006_credit_accounting.sql` | `infrx.feature_flags` (signup_grant/credit_admission OFF, legacy_usd_admission ON) + `require_feature`; `infrx.credit_wallets` (kind xor ownership, one per individual / personal org / provider, unit CHECK, kind/unit/owner immutable, totals moved only by triggers — R59-7, `pg_trigger_depth` guard); `infrx.credit_ledger` (closed kinds, no transfer, sign/kind rules, exactly 10000.00000000 signup into consumer only, allocation into provider_dev only, one signup row per wallet, one debit per request, debit FK to its job's wallet, append-only, TRUNCATE refused); `infrx.signup_entitlements` PK `(user_id, entitlement)`, own-wallet FK, FK to its signup ledger row, immutable; `infrx.credit_wallet_holds` (one per request ever, FK to job's org/wallet/card, reservation moved by trigger, 402 = constraint `credit_wallets_reserved_within_total`, unknown→released only); `accounting_regime` on `infrx.jobs` (NOT NULL, **no default for new rows**; existing rows `legacy_usd`) + pins (wallet, model, requested_model, deployment, serving, card, policy) with regime CHECK and immutability trigger + admission guard (flag, wallet reachability R66); `accounting_regime`/`charged_credits`/card/serving/deployment on `usage_events` (legacy writer default kept; CREDIT row = the job's pins by composite FK, `cost_usd = 0`); `infrx.credit_wallet_reconciliation`; `infrx.grant_signup_credit` (A1 seam) |
| `…/0007_provider_registry.sql` | `provider_orgs`, `provider_memberships` (viewer/developer/administrator, one current, one-way revocation), `public.models.model_uuid` + `provider_org_id` FK, immutable `model_versions` (text[] shard digests, digest_source), `serving_versions` (capability jsonb bounded, meter tokens-v1, unique label per model), `endpoints` (dev/prod), `deployment_revisions` (fixed visibility; public ⇒ prod and a public state; forward-only state graph), `rate_card_versions` (CREDIT, tokens-v1, approved, named roundings, `provisional`), `data_access_policies`, append-only `catalog_listings`; job pins are one card's own (composite FK) |
| `…/0008_credit_read_surface.sql` | `infrx.resolve_admission_pins(model)` (D2 seam); barrier views `public.console_credit_wallets`, `public.console_credit_ledger`; `public.console_wallet_summary(user)`; `public.console_legacy_usd_statement(org)`; `console_usage` with CREDIT columns **appended** |
| `…/0009_operator_seams.sql` | AuditAction extended (+admin_key_issue, admin_key_revoke, admin_publish, admin_job_cancel, admin_reconcile, admin_adjust), unique audit idempotency key + `audit_by_idempotency_key`; `api_keys.audience/user_id/provider_org_id/endpoint_id` + guard (consumer key's individual = console `created_by`; one-way revocation; one active operator key); `key_by_hash`, `revoke_key`, `bootstrap_operator_key` (sha256 only, audited, idempotent), `verified_user`, `set_suspension` (audited, replayable), `usage_records` (UsageRecordV2 shape, keyset, ≤500), `active_holds` (per regime) — service_role only |
| `apps/infrx-api/infrx/state/seed_marlin_provisional.sql` | Operator seed (not a migration): the F2P fixture registry records verbatim + **PROVISIONAL** card `rc_marlin2b_2026_09_provisional` (400/1,200 CREDIT per M, `provisional = true`, `approved_by = 'provisional - P-01 pending'`), listing v1, policy `dap_2026_09_01`; idempotent; enables nothing |
| `apps/infrx-api/infrx/state/credit_schema.py` | Seam map (signatures, callers, ordered result columns, page views, pin/hold columns) |
| `apps/infrx-api/infrx/state/{migrations.py,supabase_shim.sql}` | `SEED_MARLIN` path; shim `auth.users.email_confirmed_at` (GoTrue's column) |
| `apps/infrx-api/tests/d/{checks_credit.py,test_credit_schema.py}` | D1R checks and tests |
| `apps/infrx-api/tests/d/{checks.py,migration_mutants.py,test_migration_mutants.py}` | D1 fixtures name their regime; privilege maps extended; 111 D1R mutants + 3 scenarios; one D1 mutant retargeted (below) |
| `apps/app/supabase/README.md` | stale `org_balance` fallback line fixed (0005 comments left untouched, as instructed); D1R section |

Decisions worth reviewing: (1) `infrx.credit_holds` is D1's USD table, so CREDIT holds are `infrx.credit_wallet_holds`; no legacy view reads it, so no USD figure can absorb a CREDIT hold. (2) The platform role only **reads** the money relations; mint/hold/settle happen inside D's SECURITY DEFINER functions (D2/D5 fill `admit`/`terminalize` bodies there). (3) `charged_credits` is a new `usage_events` column (06a §3 choice); shard digests are `text[]`. (4) Consumer wallet owners are `on delete restrict` (money history): deleting an auth user who holds a wallet is refused — the identity retention policy (A1) must handle it. (5) `console_usage` is re-created in 0008 (columns appended); D1's mutant `console_usage_joins_any_key` now targets 0008, which owns the live definition.

## Requirement coverage (tests/d/test_credit_schema.py; checks in checks_credit.py)

| Test | Invariant |
|---|---|
| `test_d1r_leaves_the_0001_0005_schema_unchanged` | every constraint, trigger, policy, function body+ACL, relation/column ACL, RLS flag, column shape (with attnum) and view of 0001–0005 is identical after D1R, except 4 named allowances (jobs.price_version/price_snapshot NOT NULL relaxed; console_usage columns appended; audit action list extended); named: USD wallet trigger, `grant_credit`/`admit` ACLs, `org_wallet_summary` |
| `test_upgrade05_preserves_usd_history_and_old_regime_jobs` | on 0001-0002 + positive/negative/sign-violating USD history → 0003–0005 → 3 accepted old-regime jobs (held/settled/unknown USD holds, pilot usage, USD debit) → D1R: history, sums, jobs, holds, USD wallets identical to the digit; **no CREDIT wallet, ledger row or entitlement is created from USD history** |
| `test_credit_identity__…` | CREDIT-IDENTITY/R59-7/R67/R71: 37 violations refused one reason each (xor ownership, per-user/per-org uniqueness, unit, born-with-money, kind/owner/org immutable, totals only by ledger, transfer kind, signup amount/kind, allocation kind, debit sign/request, request link only on debits, zero, anonymous, overdraft, reused op id, edit/delete/truncate, second entitlement, entitlement into another wallet / not a signup row / blank evidence / edited); grant key asserted to be exactly `(user_id, entitlement)` |
| `test_credit_grant__one_transaction_idempotent_and_fail_closed` | one +10000.00000000 row, revision 1; retry with another op id, campaign and org replays; blank evidence 22023, shared org 55000 rollout_hold, unknown user P0002, flag off 55000 |
| `test_credit_grant__a_race_issues_exactly_one` | 10 rounds × 8 concurrent callers: one ledger row, one issuer per individual, thread errors reported |
| `test_credit_units__…` | no function/view reads USD and CREDIT amounts together except the three per-row labelled projections, none aggregates; `money_unit_cases` valid inputs round-trip to canonical text and 10^12 is refused (the rounding refusals are the service parser's — PostgreSQL numeric input rounds); usage regime rules; the deployed gateway's insert stays legacy_usd |
| `test_registry__the_operator_seed_is_the_f2p_fixtures` | seed == `rate_card_marlin`, both deployment fixtures and `serving_revision` on 52 fields; provisional; runtime digest NULL (R76); re-run no-op |
| `test_registry__ownership_immutability_and_visibility` | 46 registry violations refused (R70 visibility/state/env, provider-coherent deployments, immutable cards/listings/versions, meter/status/sign/approver, listing of private dev, card of another deployment, alias/model mismatch, re-owning a versioned model, digest shapes/provenance, label uniqueness, capability meter, membership role/uniqueness/in-place promotion/un-revoke/delete, second dev wallet, dev wallet for nobody, signup into dev wallet, provider dev wallet moved, provider renamed); 6 controls incl. shared display text |
| `test_resolve_admission_pins__…` | alias and R62 pin == `admission_pins` fixture; unknown alias/label, private dev id, malformed pin, retired, draining → P0002; card not yet effective → 22023; flag off → 55000 |
| `test_credit_admission__…` | 24 refusals (card of another deployment, wrong serving, missing/unknown policy, USD price on CREDIT job, pins on USD job, job without regime, another individual's wallet, dev wallet on prod, hold > available [named constraint], hold on another wallet/card, second hold, born settled, re-pin, regime switch, resize, unknown→settled, settled→held, delete, settlement at another card, CREDIT row with USD cost, debit from another wallet, second debit) + 4 controls; lifecycle leaves 9990.02400000 / 0.00000000 and reconciles |
| `test_credit_rate__…` | CREDIT-RATE: job admitted at v1; v2 card + listing published; resolve returns v2; job keeps v1; settlement at v2 refused, at v1 accepted |
| `test_credit_read_surface__…` | wallet summary exact text; ungranted user → wallet_id NULL + zeros; other user/provider admin → 42501, anon refused; operator/service read; pages show the individual's wallet only (not provider dev wallet); operator principal masked `platform`, visible to operator; money is text; legacy USD statement = ledger sum/count with rollout_hold iff nonzero, 42501 cross-org; leaky stable probe sees nothing foreign; both views `security_barrier` |
| `test_credit_privileges__…` | service_role SELECT-only on CREDIT money relations; no DELETE/TRUNCATE on registry/flags; flag UPDATE columns only; plus D1's `check_privileges` (19 enumerated) and `check_function_privileges` (50 functions, 14 browser-callable) on the full chain |
| `test_credit_role_matrix__…` | 21 attacks × anon/consumer/provider member/provider admin/operator refused; service: 5 seams allowed, 6 direct writes refused; sessions resolve to their users |
| `test_fail_closed__…` / `test_flags__…` | 4 guarded writes 55000 when their flag is off or its row missing; applying enables nothing |
| `test_legacy_read_path__…` | 15 deployed-console member reads + 5 admin reads execute after upgrade; USD summary = history; addCredit and gateway inserts stay legacy_usd |
| `test_rerun__…` | second application (after enabling a flag and granting): 1187 objects, 13 rows, flags unchanged |
| `test_seams__…` | 4 seams + 2 pages match `credit_schema.py` (types, order, callers) |
| `test_plans__…` | 100,001 ledger rows per tenant × 2 tenants: first page and keyset next page are `credit_ledger_wallet_created_idx` ranges with no Seq Scan/Sort; the barrier view pushes the wallet qual to the base scan |
| `test_operator_seams__…` | 0009: 12 refusals, 2 controls; bootstrap/revoke/suspension idempotent and audited; verified_user; usage/holds per regime |

## Environment

Linux 7.0.0-1010-aws x86_64; Python 3.12.3 (`make api-env`, psycopg 3.3.6); Docker server 29.6.2. Images: `postgres@sha256:33f923b05f64ca54ac4401c01126a6b92afe839a0aa0a52bc5aeb5cc958e5f20` (16.14 + shim) and `supabase/postgres@sha256:7768d0d1d377250b718a9ad07f4661d008ebe6c96ecbbc4c08f3c5e53553e8fd` (17.6.1.173, no shim, `INFRX_D1_IMAGE=supabase`). Container `infrx-d1-postgres[-supabase]` on 55432, labelled with this checkout; HarnessBusy refusals were retried (another checkout held the lock repeatedly); the container this run created was removed. The `infrx-d1-postgres` present at the end belongs to `codex-e3b` (label) and was not touched.

## Commands and results (final sweep, 2026-09-22 UTC, at `e538c9a`)

| Command (in apps/infrx-api unless noted) | Exit | Result |
|---|---|---|
| `INFRX_MUTANTS=all uv run --frozen pytest -q -p no:cacheprovider tests/d` (plain) | 0 | `241 passed in 270.71s` (20:05Z) |
| `INFRX_D1_IMAGE=supabase INFRX_MUTANTS=all uv run --frozen pytest -q -p no:cacheprovider tests/d` | 0 | `241 passed in 246.25s` (20:10Z) |
| `uv run --frozen pytest -q -s tests/d/test_credit_schema.py` plain / supabase | 0 / 0 | `21 passed in 26.31s` / `21 passed in 32.87s` |
| `make api-test` (repo root) | 0 | `2212 passed, 2 warnings in 295.09s` |
| track-first: `pytest -q tests/d tests/g tests/i tests/j tests/m tests/q tests/t tests/w tests/contracts tests/test_app_factory.py tests/test_gateway_auth.py tests/test_inflight.py tests/test_media.py` | 0 | `2212 passed, 2 warnings in 358.71s` |
| legacy-first: `pytest -q tests/test_app_factory.py tests/test_gateway_auth.py tests/test_inflight.py tests/test_media.py tests/contracts tests/d tests/g …` | 1 | `2 failed, 2210 passed` — both in E2R's `tests/d/test_pgharness.py` ownership drills: `failed to bind host port 127.0.0.1:55598/tcp: address already in use` (a concurrent run from another checkout held E2R's drill port). Not D1R code; the same two passed in this checkout's earlier and later tests/d runs (241/241). The earlier legacy-first run of this sweep's predecessor is what exposed the grant race fixed in `e538c9a`. |

Supabase-image note: the bare image's `auth.users` has no `email_confirmed_at` (GoTrue adds it on a hosted project); `verified_user`'s verified path therefore ran on the plain image only (`verified path checked`); on the Supabase image the output says `not run (auth.users has no email_confirmed_at on this image)`. The function reads the column through `to_jsonb`, so it works on both.

`make check`: **not run as a whole** — no TypeScript changed (console targets not run; `apps/app/node_modules` absent here); `make api-mutants` not run beyond `tests/d` (its D list ran in full above; other tracks' lists untouched).

Query plans (10^5 rows/tenant, `explain (analyze, timing off, summary on)`): plain — first page 0.028 ms, next page 0.035 ms, view page 40.388 ms; supabase — 0.062 / 0.092 / 69.741 ms. The view page is D1's limit (the barrier sorts the one tenant's rows), not worse; the base keyset path is O(page).

## Migration mutants (R32/R40)

111 D1R mutants (193 in the D list): `uv run python -c "…len(m.D1R_MUTANTS)…"` → `193 111 0` (none declares a non-kill outcome). All killed by their named check on **both** images in the sweep above (apply/setup errors never score as kills; the runner's self-test still classifies broken SQL as `apply_error`). New scenarios: `credit` (fresh + D1 + CREDIT fixture), `upgrade05`, `credit_volume` (the keyset-index mutant). 35 D1R mutants are in the default subset. Layered invariants with no single-edit break are asserted structurally or named here: wallet kind (xor CHECK + (wallet_id, kind) FK + guard), consumer owner (guard + entitlement FK), deployment serving/visibility (guard + card FKs / visibility CHECK), model re-owning (FK restrict + version immutability), regime switch on a job (guard + regime CHECK).

Mutant-run findings fixed: a malformed model-version control made two digest cases pass vacuously; the re-run check used a nondeterministic personal org; legacy-writer refusal and suspension replay surfaced as exceptions (now assertions); two browser-grant mutants retargeted to `function_privileges` (the schema USAGE revoke is the first wall); `console_usage_joins_any_key` retargeted to 0008.

## Failure drill

Grant race: 8 concurrent first logins for one individual (10 rounds). Before `e538c9a`, 1 of 8 callers failed with `UniqueViolation … credit_wallets_one_per_personal_org` (the insert's single arbiter was the per-user index). After: no conflict target + explicit `rollout_hold` if no wallet results; durable state per round = 1 wallet, 1 ledger row, 1 entitlement, 7 replays; mutant `d1r_grant_race_arbitrates_one_index` killed 3/3 repeated runs. Flags: each guarded write with its flag off/missing → 55000, state unchanged (transaction rolled back).

## Limits

1. Not applied to any hosted project (hosted has 0001–0002 only); coordinator applies after I3B's backup/restore rehearsal.
2. Provisional rate card (**P-01 pending**); the seed labels it; production price is a new card + listing.
3. Legacy-account transition is a rollout input (**P-02**): nonzero USD balance → `rollout_hold`; multi-member existing org → grant refused as rollout hold; no exchange rate anywhere.
4. D1R ships the admission/settlement **schema**; D2/D5 fill the transactional bodies (`infrx.admit`/`terminalize` remain D1's fail-closed stubs). CREDIT-SPEND concurrency across two models is D2/D5 evidence.
5. `verified_user` evidence = Supabase `email_confirmed_at`; A1 owns the verification policy and account deletion/retention (wallet owners are `on delete restrict`).
6. Data-access policies are platform-wide rows; the G6B note mentioned a per-deployment policy row — not modelled (request below).
7. Barrier-view pages sort the tenant's rows (40–70 ms at 10^5); a definer keyset RPC is the upgrade if needed.
8. `pg_trigger_depth` is the "moved by a trigger" test for wallet totals (documented ponytail).

## Migration / rollback notes

All four files are additive and re-runnable (proven no-op). Rollback (only before any CREDIT row exists; after that, stop admission via flags instead — never drop money history):
- **0009**: drop the eight functions and `api_keys_identity_guard`; drop `api_keys_one_active_operator`, `audit_entries_idempotency_key_idx`; drop `api_keys` columns audience/user_id/provider_org_id/endpoint_id (+ constraints) and `endpoints_owner_pair_key`; restore `audit_entries_action_check` to the four 0003 values (fails if new actions were recorded — keep it then).
- **0008**: drop the two public functions, the two views, `resolve_admission_pins`; re-create `console_usage` from 0005 (drop + create, columns appended cannot be removed by replace; re-grant SELECT to authenticated, service_role).
- **0007**: drop jobs FKs `jobs_pins_are_one_card`, `jobs_policy_version_fk`, wallet FK `credit_wallets_provider_fk`, the registry tables (listings → cards → deployments → endpoints → serving → model versions → memberships → provider_orgs, policies), `public.models` columns `model_uuid`/`provider_org_id` + constraints.
- **0006**: set flags off first; drop grant/guard functions and triggers on jobs; drop usage_events constraints/columns (`accounting_regime`, `charged_credits`, card/serving/deployment); drop jobs constraints/columns (pins, wallet, regime) and re-set `price_version`/`price_snapshot` NOT NULL (valid only if no CREDIT job exists); drop holds, entitlements, ledger, wallets, flags, reconciliation view. Old code never reads CREDIT relations, so rollback of code alone = flags off (maintenance), not schema removal.

## Handback

Next unblocked: **D2** (atomic admission on `infrx.admit`: `resolve_admission_pins` → insert job with pins + `credit_wallet_holds` row; 402 = constraint `credit_wallets_reserved_within_total`), **A1** (call `infrx.grant_signup_credit(user, evidence, campaign, op)`; `infrx.verified_user(user)` for evidence), **C0** (shapes in `infrx/state/credit_schema.py`: `console_wallet_summary`, `console_legacy_usd_statement`, `console_credit_wallets`, `console_credit_ledger` keyset `(created_at desc, entry_id desc)` per wallet), **G6B** (0009 functions), **E2R** integration.

### integration_requests

1. **Coordinator / 08 §10**: record the D1R physical names (`infrx.credit_wallets`, `infrx.credit_ledger`, `infrx.credit_wallet_holds` — `credit_holds` is D1's USD table — `infrx.signup_entitlements`, `infrx.rate_card_versions`) against 06a's logical names (`wallets`, `wallet_ledger`, `credit_holds`, `signup_grants`, `rate_cards`); migration numbers 0006–0009 consumed (next is 0010).
2. **D2**: admission writes `infrx.jobs` with `accounting_regime='credit'` and all of `CREDIT_JOB_PINS` from `resolve_admission_pins`, `price_version/price_snapshot` NULL, then `infrx.credit_wallet_holds (request_id, org_id, wallet_id, rate_card_version, amount, state='held')`; map `credit_wallets_reserved_within_total` → 402, 55000 → maintenance, P0002 → not_found, 22023 → invalid_request. Provider-dev endpoint resolution (`resolve_endpoint_pins(endpoint_id)`, card by deployment `rate_card_versions_deployment_idx`) is not built — D2 or D1R follow-up.
3. **D2 (M3 handback, verbatim)**: `media_uploads` (handle PK with the `^upl_[A-Za-z0-9_-]{22,64}$` check, org_id + RLS, state enum created|finalized|aborted|expired, max_bytes, declared_bytes/declared_digest/accepted_mime, timestamps, finalized-only columns non-null exactly when finalized, index (state, expires_at)) and `media_objects(storage_ref PK, org_id, last_used_at)` for the conditional delete. Not built in D1R.
4. **D5**: settlement = one transaction: hold held→settled (trigger releases reservation), `infrx.credit_ledger` `inference_debit` of `-charged` with `request_id` (FK to the job's wallet; one per request), `usage_events` row with `accounting_regime='credit'`, `charged_credits`, the job's card/serving/deployment (composite FK), `cost_usd = 0`, `settlement_regime='pilot'`. Cross-table equality `ledger amount = -usage.charged_credits` is D5's (not a DB constraint). G6B row 6 (`operator_adjustment` + `reconcile` operations) → D5 (ledger kind `operator_adjustment` with actor exists; audit action `admin_adjust`/`admin_reconcile` exists).
5. **A1**: G6B row 5 `grant_initial` = `infrx.grant_signup_credit`; backfill of existing verified users through the same call; deletion/retention policy for wallet owners (`on delete restrict`).
6. **G6B**: per-deployment policy row (row 7) — policies are platform-wide `data_access_policies`; if per-deployment is required, add `deployment_revision_id` to that table in a later D migration. Operator seed path writes serving_versions/deployment_revisions/rate_card_versions/catalog_listings/data_access_policies as service_role inserts (granted) — see the seed file.
7. **C0 / F2P wire-in**: console AuditAction vocabulary aligns with 0009's ten values; `api_keys.audience` is the credential audience (keys created by the console are `consumer` with `user_id = created_by`).
8. **Coordinator**: `make api-mutants` should keep including `tests/d/test_migration_mutants.py` (now 193 mutants; the full D list takes ~4–5 min per image).

Nothing was applied to any hosted project.

## Verification log

- 2026-09-22: Written by the D1R implementation session at `e538c9a`; counts quoted from the sweep logs above.
