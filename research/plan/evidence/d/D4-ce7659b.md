# D4 — persistent stream journal and replay

| Field | Value |
|---|---|
| Task | D4 (track D, durable state): persistent stream journal and replay. Oracles DUR-OUTPUT, DUR-FENCE; touched DUR-CAP (journal bytes) and API-STREAM (replay reads) |
| Status | **implemented** — real PostgreSQL on both images (pinned `postgres:16.14` + shim, and `supabase/postgres` 17.6.1.173). Not integrated: no coordinator merge, nothing applied to any hosted Supabase project, never the pilot box. The settlement after `complete`'s fence is D5's (5 StreamStore and 23 JobStore cases stay strict-xfail on it) |
| Base SHA | `740bebf8ad713a1ac0b9d47701c9278f01cbdeef` (`origin/claude/backend-impl`, `git merge --ff-only` before any code; it contains `9c1c6ed` "coordinator: TASK_PORTS registers d4 (valkey 55465) …") |
| Implementation SHA | `ce7659b` (commits `746349f` … `ce7659b`, per item below). The evidence commit follows it; the head SHA is in the handback |
| Branch / worktree | `codex/d4-stream-journal` in `.claude/worktrees/codex-d4` |
| Classification | local only. Task-local Docker: `infrx-d4-postgres[-supabase]` on 55435, `infrx-d4-valkey` on 55465 (`INFRX_D2_VALKEY_PORT`/`INFRX_D2_VALKEY_CONTAINER`), the `test_pgharness` decoy `infrx-d4-dharness-postgres` on 55475 — each created by this checkout's harness and removed at its exit. No cloud, hosted project, pilot host or paid provider; nothing pushed |

## What was built

| Item | Where | Commit(s) | Killing test (named) | Single-edit mutants (all killed, both images) |
|---|---|---|---|---|
| 9d decoy per task (first, before any tests/d run) | `tests/d/test_pgharness.py`: `decoy(task)` — d1 keeps `infrx-e2r-dharness-postgres:55598` byte-identical; any other D task `infrx-<task>-dharness-postgres` on its PostgreSQL port + 40 (d4: 55475), outside `tasklocal.all_host_ports()` and E2's 55500-55599 | `746349f` | `test_pgharness.py::test_the_decoy_is_the_tasks_own_and_d1s_is_unchanged` | hand-applied: `decoy()` returning 55598 for every task → `assert port not in reserved and not 55500 <= port <= 55599, (task, port)` failed (run quoted in the session; the edit reverted, tree clean) |
| (1) fenced append | `0017`: `infrx.append` body; `PgStreamStore.append`; `append` added to `FILLED_RPCS`, `FILLED_BOUNDARIES`, `credit_schema.SEAMS` | `7f4aa40` (SQL + adapter + harness), `543c18b` | `test_journal.py::test_append__fence_first_then_batch_then_marker`, `__oversize_event_refused_whole`, `__terminal_event_refused_anywhere_in_a_batch`, `__job_ceiling_holds_back_the_terminal_reserve`, `__empty_batch_neither_writes_nor_publishes`, `__stale_foreign_expired_and_preparation_leases_store_nothing`, `__past_the_generation_instant_append_terminalizes_and_refuses` | 15: `d4_append_without_the_fence`, `d4_append_accepts_a_preparation_lease`, `d4_r29_refusal_raised_rolls_back`, `d4_terminal_event_accepted_when_last`, `d4_event_limit_off_by_one`, `d4_oversize_event_stored`, `d4_only_the_first_event_is_measured`, `d4_job_ceiling_ignores_the_terminal_reserve`, `d4_terminal_event_waved_through`, `d4_terminal_reserve_sized_to_this_outcome`, `d4_empty_batch_publishes`, `d4_sequence_restarts_per_batch`, `d4_published_not_set`, `d4_stored_bytes_not_counted`, `d4_chunk_ttl_from_the_callers_clock` |
| (2) global budget, no global lock | `0017` header (beside 0011's lock order); the per-job ceiling | `7f4aa40`, `648db8d` | `test_journal.py::test_append__never_moves_the_global_charge` | `d4_job_ceiling_is_the_whole_reservation_plus_one` (0017), `d4_stored_counted_beside_the_reservation` (0011 `journal_bytes_charged`) |
| (3) one terminal event per terminalization | `0017`: `jobs_terminal_journal_event` (AFTER UPDATE OF settled_at, WHEN NULL → value and a journal reservation exists) + `infrx.journal_terminal_event()`; `PgStreamStore.finalize_in_transaction` (read-back) | `7f4aa40`, `543c18b` (the widest-payload room check), `627edf8` | `test_journal.py::test_terminal__every_path_writes_exactly_one_event_last` (12 paths + the 24 h release) | `d4_terminal_trigger_on_any_update`, `d4_terminal_payload_from_the_old_row`, `d4_terminal_event_in_generation_one_always`, `d4_no_terminal_event_on_cancel` (+ `d4_terminal_event_bytes_uncounted`, `d4_terminal_event_below_the_watermark` under item 5, `d4_terminal_event_legacy_regime_only` under 9a) |
| (4) replay | `0017`: `infrx.read_journal` (service-only, STABLE: one snapshot); `PgStreamStore.read_owned` | `7f4aa40`, `751c8a8`, `ce7659b` | `test_journal.py::test_read__replay_equals_committed_rows`, `__tenant_bound`, `__bounded_and_paged`, `__gap_expired_and_past_head_are_typed` | `d4_order_by_sequence_only`, `d4_read_any_tenant`, `d4_read_unbounded`, `d4_read_zero_limit_is_an_empty_page`, `d4_gap_is_an_empty_page`, `d4_past_head_is_an_empty_page`, `d4_expired_journal_reads_empty`, `d4_cursor_overflows_as_an_integer` |
| (5) pruning, gaps, usage | `0017`: `infrx.expire_journal`, `infrx.journal_usage`, watermark columns; `PgStreamStore.expire`, `.usage` | `7f4aa40`, `543c18b` (the sequence past the watermark), `a5d39b4` | `test_journal.py::test_expire__never_on_the_callers_clock`, `__prefix_then_whole_journal`, `__frees_bytes_exactly_once`, `__usage_reports_the_charge` | `d4_expire_on_the_callers_clock`, `d4_prune_without_a_watermark`, `d4_prune_not_a_prefix`, `d4_sequence_reissues_a_pruned_cursor`, `d4_terminal_event_below_the_watermark`, `d4_prune_keeps_bytes_charged`, `d4_prune_frees_twice`, `d4_terminal_event_bytes_uncounted`, `d4_usage_misreports_the_charge` |
| (6) races | `tests/d/test_journal_races.py` (6 tests, lock-step both orders + barrier stress) and `checks_journal.check_journal_races` | `865e0c7` | `test_journal.py::test_races__appends_serialize_and_expire_skips_a_locked_job` (the mutants' concurrency check) + `test_journal_races.py::test_race__*` | `d4_append_without_the_row_lock` (the sequence read before the fence's row lock), `d4_expire_waits_on_a_locked_job` (SKIP LOCKED dropped) |
| (7) adapter, harness, conformance | `infrx/state/journal.py` (`PgStreamStore`); `pgtesting`: `publish` = one chunk through the real `append`, `stream` hook, `make_streamstore_factory`, `retune` syncs the stream's limits; `test_streamstore_conformance.py`; `test_jobstore_conformance.py` re-partitioned (comments; PENDING unchanged) | `7f4aa40`, `3f8a70b` | `test_journal_units.py` (5 tests, no Docker); both conformance suites (partition below) | code (`code_mutants_d4.py`, one runner R83), 9/9: `the_refusal_is_returned_not_raised`, `the_default_event_limit_is_sent`, `chunks_built_from_the_input_not_the_rows`, `chunks_numbered_from_the_batch_not_the_rows`, `the_next_cursor_is_always_the_callers`, `a_malformed_limit_reaches_the_database`, `finalize_accepts_another_outcome`, `finalize_answers_after_expiry`, `expire_drops_the_callers_bound` |
| (8) E3B drills on PostgreSQL | `test_e3b_drills.py`: dr05, dr06, dr08, dr10, bodies verbatim from `tests/integration/backend/test_drills.py` | `ddb23a8` | the four `test_e3b_dr0{5,6,8}…`, `test_e3b_dr10…` — **all pass** | their invariants' mutants are item 1/4's (`d4_append_without_the_fence`, `d4_published_not_set`, `d4_read_any_tenant`, `d4_oversize_event_stored`) |
| (9a) CREDIT jobs | `checks_journal.check_credit_journal` via `checks_leases.credit_running` | `7028ed2` | `test_journal.py::test_credit__append_replay_and_terminal_event_on_a_credit_job` | `d4_terminal_event_legacy_regime_only` |
| (9b) privileges | `0017` grants | `7028ed2` | `test_journal.py::test_privileges__journal_operations_service_only_internals_nobody` | `d4_append_granted_to_authenticated`, `d4_journal_usage_granted_to_anon`, `d4_chunk_doc_callable_by_the_service` |
| (9c) header: rollback + lock order | `0017` header | `7f4aa40` | documentation | — |
| (9e) the pilot DSN's login role | this report, "Migration and rollback" | — | not code | — |

Commit-message corrections (history is not amended): `a5d39b4`'s subject says "10 mutants"; its
body lists the item's **9**. `3f8a70b`'s says "10 code mutants"; the list has **9** (quoted run
below).

### Design decisions worth reviewing

1. **The terminal event is a trigger, not a wrapper.** `jobs_terminal_journal_event` fires `AFTER UPDATE OF settled_at` `WHEN (old.settled_at IS NULL AND new.settled_at IS NOT NULL AND new.journal_reserved_bytes > 0)`, so 0012's `terminalize_unstarted`, 0016's `terminalize_no_usage` (cancel, R29 inside the fence, every `recover_job` cause) and D5's future `terminalize` all get exactly one event without redefining a 0012/0016 function (their D2/D3 mutants stay armed). The payload is `{state, cause, settlement_state}` of the **new** row; it lands after the newest chunk (highest generation's last sequence), else at the last inference generation (else 1) just past a prune watermark; its TTL is the newest chunk's, else the frozen default 3600 s. It adds its bytes to `journal_stored_bytes` (a nested UPDATE that does not list `settled_at`, so the trigger does not re-fire). The 24 h release (`held_unknown → released_platform_absorbed`) leaves `settled_at` alone: no second event.
2. **"No reservation, no journal."** `journal_reserved_bytes > 0` is in the WHEN clause: every admitted job reserves (0011), and an append into a zero reservation is refused by the ceiling anyway. Without it, D1's volume fixture (raw jobs moved to `succeeded` before their chunks are inserted, `checks.py` `seed_volume`) would collide with the event at (1, 1).
3. **The settling write's room is checked against the WIDEST terminal payload** (107 bytes over every state × cause × settlement, the fake's `check_terminal_capacity`): a terminalization whose held-back bytes cannot fit it is refused `journal_capacity_exhausted` and rolls back whole (cancel refused, the reaper reports the job `unsettleable`), and settles after pruning. At production sizes 1024 bytes are held back, so it never fires; with the conformance case's 180-byte reservation it does, which is why `dur_settle__one_unsettleable_job_does_not_stop_the_sweep` passes. `check_append_job_ceiling` computes the widest from the Python enums and pins the literal at the boundary (reservation 2·107 fits, 2·107−1 is refused).
4. **Append order, one transaction:** argument shape → the fence (`fence_lease(lease, ['inference'], reconcile_s)`; a returned refusal is returned as `{"refusal": …}` so the R29 terminalization and its terminal event commit, R39) → the job row (locked by the fence) and the next sequence = max(generation's max, the watermark if in this generation) → R30 type check (only progress/delta/usage/error with an object payload, anywhere in the batch) → sizes measured in SQL (`octet_length(payload::text)`; one event over the store's `journal_event_max_bytes` refuses the whole batch `journal_write_failed`) → empty batch returns `[]` having written nothing → per-job ceiling `reserved − least(1024, reserved/2)` (`journal_capacity_exhausted`, retry 30) → INSERT … RETURNING the rows (`committed_at = infrx.now()`, `expires_at = committed_at + the store's journal_chunk_ttl_s`) → `journal_stored_bytes += Σbytes, published = true`. Only `jobs.published` is written as a marker (`attempts.published`/`first_token_at` have no reader).
5. **Committed rows only.** The adapter returns chunks built from the rows `infrx.append` returned; in autocommit psycopg returns a statement's result only after ReadyForQuery, which follows the commit (module docstring). A caller must not wrap `append` in its own transaction (G2 request below).
6. **Pruning is a prefix, the watermark only grows.** Per job, `expire_journal` deletes everything at or below the newest expired chunk (a straggler committed earlier on a later clock goes with it), stores that cursor as the watermark, and decrements the bytes of exactly the deleted rows. "Expired" is not a flag: a watermark and no chunk left. A later append or terminal event lands past the watermark and is readable from it (a delta from the fake, see Limits 4).
7. **Replay reads the client's cursor as numeric** (`Last-Event-ID` is client input): any magnitude is `invalid_cursor` past the head, never an integer-overflow 500. `read_journal` is STABLE, so its head, gap and page reads share one snapshot. (Review H3(d): no deterministic interleaving can reach between two statements of one function call, so this snapshot property is NOT tested; it is a design note, not a claimed invariant.) Review J5, accepted and documented: a never-issued cursor BELOW the head in an earlier generation (e.g. (1,5) when generation 1 holds nothing and generation 2 holds (2,1..3)) is answered with the rows after it - order kept, nothing skipped - rather than `invalid_cursor`; only a cursor past the head is refused.
8. **`finalize_in_transaction` is a read-back keyed by the committed outcome** (unknown `not_found`; not terminal or a different outcome `state_conflict`; expired `journal_expired`; else the one terminal chunk), in Python over one query, like the fake's.
9. **Watermark storage:** two nullable columns on `infrx.jobs` (`journal_pruned_generation`, `journal_pruned_sequence`, CHECK both-or-neither and ≥ 1), which `jobs_guard` does not freeze; no new relation.

## Requirement coverage

SQL checks are `tests/d/checks_journal.py` (each rolled back; requests stamped by a gateway an
hour behind the database clock, `GATEWAY_SKEW`; the database clock moved explicitly; byte
counts read from the database, never computed in Python).

| Test | Oracle | Invariant |
|---|---|---|
| `test_append__fence_first_then_batch_then_marker` | DUR-OUTPUT | a live lease's batch is stored as one transaction's rows and the answer IS those rows; cursors (1,1),(1,2) then (1,3) across batches; `bytes` = the stored text's measure; `persisted_at` = the DB clock at the append (7 s after the claim), `expires_at` = that + the store's TTL (3600, then a retuned 30); `journal_stored_bytes` = Σ bytes; `published` false before, true after |
| `test_append__oversize_event_refused_whole` | DUR-OUTPUT / R25 / dr10 | an event over the limit alone, first, last or in the middle of a batch: `journal_write_failed`, nothing stored, not published, 0 bytes; at the default limit exactly 1048576 bytes is accepted, 1048577 refused; that measure is the fake's `compact_bytes` + 1 for a one-key payload (quoted: `limit 1048576 accepted, 1048577 refused (fake compact measure 1048575)`) |
| `test_append__terminal_event_refused_anywhere_in_a_batch` | DUR-OUTPUT / R30 | `[terminal]`, `[delta, terminal]`, `[terminal, delta]`, `[delta, terminal, delta]`, an unknown type, a non-object or missing payload: `invalid_request`, nothing stored; the cancel then writes exactly one terminal event, `client_cancelled` |
| `test_append__job_ceiling_holds_back_the_terminal_reserve` | DUR-CAP / R39 | reservation 8192: 7168 bytes accepted exactly, 1 more `journal_capacity_exhausted` (nothing stored); the cancel's terminal event fits the held-back 1024; widest terminal payload 107 < 1024; reservation 214 (2·107): the terminal fits; 213: cancel refused `journal_capacity_exhausted`, nothing changed (state, hold, projections, journal), the reaper reports it `unsettleable`, and after pruning it settles with one terminal event |
| `test_append__empty_batch_neither_writes_nor_publishes` | DUR-OUTPUT | `[]` → `{"chunks": []}`, no rows, not published, 0 bytes; the lost attempt is then requeued, not `lost_after_publication` |
| `test_append__stale_foreign_expired_and_preparation_leases_store_nothing` | DUR-FENCE / R46 | a preparation token, a foreign worker, a stale generation → `stale_lease`; an unknown job → `not_found`; a LIVE preparation lease on a preparing job → `stale_lease`; exactly at `expires_at` → `stale_lease`; nothing stored or published; a terminal job → `already_terminal`, its journal only `[terminal]` |
| `test_append__past_the_generation_instant_append_terminalizes_and_refuses` | DUR-FENCE / R29 / R39 | lease live, past the generation instant: `already_terminal`; the job is `failed/deadline_exceeded`, `released_platform_absorbed` (unpublished; hold returned) or `held_unknown` (published); the late batch is not stored; the terminal event is last |
| `test_append__never_moves_the_global_charge` | DUR-CAP | budget 16384 filled by two 8192 reservations; an append to the ceiling leaves `journal_bytes_charged()` at 16384 and admission still refused; 1025 more bytes refused, charge unchanged; after cancel the charge = 8192 + stored (output + terminal) and admission still refused; after pruning exactly 8192, and one admission fits again |
| `test_terminal__every_path_writes_exactly_one_event_last` | DUR-OUTPUT / R30 | 12 paths, each in its own savepoint: 0012 preparation failure (claim past the instant), cancel before and after publication, R29 via heartbeat / load_work / append, recover `retries_exhausted` / `lost_after_publication` / `queue_wait_expired` / `preparation_failed`, a second generation's journal (terminal at (2,3)), a CREDIT job: exactly one terminal event, last, payload = the stored row, bytes = its measure, `persisted_at = settled_at`, stored bytes = Σ journal; the 24 h release: still one event (payload `held_unknown`) |
| `test_read__replay_equals_committed_rows` | DUR-OUTPUT / API-STREAM | paging with limits 1, 2, 4, 100 replays exactly the committed rows (type, payload, cursor, bytes, persisted_at, expires_at); a two-generation journal (fixture rows) replays (1,1..6) then (2,1..2), never spliced |
| `test_read__tenant_bound` | R10 / dr08 | another org's read of the handle and anyone's read of an unknown handle: the same `not_found`; the owner reads it |
| `test_read__bounded_and_paged` | API-STREAM | limit 0 / −1 `invalid_request`; limit 5000 over 1001 chunks returns 1000; the rest from cursor (1,1000) |
| `test_read__gap_expired_and_past_head_are_typed` | API-STREAM / DUR-OUTPUT | empty journal: an empty page, (1,1) `invalid_cursor`; at the head: an empty page; (1,3), (2,1), (9,9), (2^40,1), (1,10^20): `invalid_cursor`; after a prune: None, (1,0), (1,1) `replay_gap`, (1,2) → the rest; all pruned: `journal_expired` for any cursor |
| `test_expire__never_on_the_callers_clock` | R7 | `expire(now + 365 d)` and `expire(now − 1 d)` prune nothing live; 1 s before the TTL nothing; exactly at `expires_at` the chunk goes |
| `test_expire__prefix_then_whole_journal` | DUR-OUTPUT | the watermark (1,2) after the first prune; a straggler (1,4) expiring after (1,5) goes with the prefix (3 removed), the watermark stays (1,5), (1,4) reads `replay_gap`, (1,5) reads (1,6); all pruned → `journal_expired`; the live attempt's next append is (1,7) (never a pruned cursor), readable from (1,6); a journal expired while running gets its terminal event at (1,2), readable from the watermark (1,1) |
| `test_expire__frees_bytes_exactly_once` | DUR-CAP | a journal ending in its terminal event: stored = Σ chunks; a partial prune frees exactly the pruned rows' bytes, the final prune reaches 0, a further pass frees nothing |
| `test_expire__usage_reports_the_charge` | DUR-CAP / G1R | `journal_usage()` = `{reserved_bytes, stored_bytes, charged_bytes, chunks}` computed independently (Σ active reservations, Σ stored, Σ max(reservation, stored), count), before and after a cancel |
| `test_credit__append_replay_and_terminal_event_on_a_credit_job` | shared journal | a CREDIT job appends, its own org replays it (ORG_A gets `not_found`), its cancel writes one terminal event, its CREDIT hold is `unknown` |
| `test_privileges__journal_operations_service_only_internals_nobody` | R59 | `append`, `read_journal`, `expire_journal`, `journal_usage`: service_role only; `journal_terminal_event()`, `chunk_doc(stream_chunks)`: nobody; the trigger is `AFTER UPDATE OF settled_at ON infrx.jobs FOR EACH ROW` |
| `test_races__appends_serialize_and_expire_skips_a_locked_job` | DUR-FENCE | two appends on one lease: the second waits (seen in `pg_stat_activity`), then gets sequence 3; `expire` with an append in flight answers at once with 0, then prunes on the next pass |
| `test_journal_races.py::test_race__two_appends_on_one_lease_are_contiguous` | DUR-OUTPUT | lock-step + 5 rounds × 8 concurrent appends of 2 events: every batch commits, cursors 1..16, no duplicate or hole, stored bytes = Σ |
| `…::test_race__append_and_cancel_have_one_order` | DUR-OUTPUT / DUR-SETTLE | cancel first: the append waits, `already_terminal`, journal `[(1,1,terminal)]`, `released_free`; append first: the cancel waits, `held_unknown`, debit 0, terminal last |
| `…::test_race__append_and_recover_never_regenerate_published_output` | DUR-OUTPUT | the reaper returns at once (SKIP LOCKED) while the append is uncommitted and its lease lapses; after the commit `lost_after_publication`, one `inference_dispatch` only, terminal after the output |
| `…::test_race__append_and_heartbeat_serialize_on_the_job_row` | DUR-FENCE | both orders: the second waits, both succeed, no lost chunk |
| `…::test_race__a_stale_generation_after_a_requeue_cannot_append` | DUR-FENCE (dr05) | generation 1's append waiting behind the requeue → `stale_lease`, not published; the same worker id's generation 2 appends at (2,1) |
| `…::test_race__expire_never_waits_on_an_append_and_an_append_waits_for_a_prune` | DUR-OUTPUT | the pruner skips a locked job; an append behind a prune in flight waits, then continues at (1,3) past the new watermark |
| `test_journal_units.py` (5, no Docker) | adapter | the store's limits are sent; an R29 refusal is raised `AlreadyTerminal` (R39); `append` answers the committed rows (cursors, byte measure), not its input; replay cursor in/out and a non-int limit refused before any call; finalize's outcome is a lookup key (not_found / state_conflict / journal_expired / state_conflict / the chunk); `expire` passes the caller's bound |
| `test_e3b_drills.py::test_e3b_dr05…/dr06…/dr08…/dr10…` | E3B | verbatim bodies on the real stores: all pass |
| `test_pgharness.py::test_the_decoy_is_the_tasks_own_and_d1s_is_unchanged` | E2R A11 | d1's decoy byte-identical; every other D task's decoy name in its namespace, port outside every reserved port and E's block, all distinct |

### Conformance partitions (exported v1 suites, real stores)

**StreamStore** (`test_streamstore_conformance.py`, all 17 `streamstore_cases()` on
`pgtesting.make_streamstore_factory`):

| | Cases |
|---|---|
| must pass (and do) | **12**: `append_commits_before_it_relays` (crash after commit through the `FailurePlan` wrapper), `the_first_append_sets_the_publication_marker`, `any_committed_chunk_is_publication`, `cursors_are_generation_then_sequence`, `reads_are_bounded_and_ownership_checked`, `a_cursor_past_the_head_is_invalid`, `no_terminal_event_anywhere_in_a_batch`, `expiry_never_runs_on_a_callers_clock`, `a_pruned_prefix_is_an_explicit_replay_gap`, `a_job_cannot_store_past_its_journal_reservation`, `a_stale_worker_cannot_append`, `an_oversize_event_is_refused` |
| strict xfail, owner D5 (each settles through `complete` before its journal step) | **5**: `dur_output__a_worker_cannot_forge_a_terminal_event` (its forged-append half passes; it then calls `complete`), `dur_output__an_expired_journal_is_gone_not_regenerated`, `dur_cap__stored_unexpired_bytes_keep_counting`, `dur_output__the_terminal_event_is_written_once_with_the_settlement`, `dur_settle__the_terminal_event_belongs_to_the_settling_transaction` — the brief expected 4; the fifth is the first one listed |
| skipped | **0** (the rig provides `jobs`, `journal_bytes`, `failures`) |

**JobStore** (`test_jobstore_conformance.py`, 70 cases):

| | D3 | D4 |
|---|---|---|
| must pass (and do) | 42 | **45** — the three former `stream` skips pass: `dur_fence__a_deadline_binds_append_and_complete`, `dur_fence__an_overdue_inference_lease_terminalizes_in_the_same_call`, `dur_settle__one_unsettleable_job_does_not_stop_the_sweep`; the `publish` cases outside PENDING (`dur_output__loss_after_publication_is_a_terminal_failure`, `dur_settle__cancelling_after_publication_reconciles`, `dur_settle__a_published_job_past_its_deadline_reconciles`) now pass on the real append |
| strict xfail `_D5` | 23 | 23 (unchanged; the two `publish` cases in it — `dur_settle__unknown_usage_is_held_then_released_as_platform_absorbed`, `dur_settle__an_unknown_usage_hold_is_never_released_on_a_callers_clock` — reach `complete` after their journal step) |
| strict xfail F2 conformance (one key across two organizations) | 1 | 1 |
| non-strict RACY | 1 | 1 (XPASS in every D4 run: cancel committed first) |
| skipped | 3 (`stream`) | **0** |

Counts, from the focused run below: `94 passed, 29 xfailed, 1 xpassed` over test_journal (20),
test_journal_races (6), test_streamstore_conformance (1 list case + 12 = 13 passed, 5 xfailed),
test_jobstore_conformance (1 list case + 45 = 46 passed, 24 xfailed, 1 xpassed) and
test_e3b_drills (9) — 20 + 6 + 13 + 46 + 9 = 94.

## Environment

| | |
|---|---|
| OS / host | Linux 7.0.0-1010-aws (the coordinator's dev host), local Docker only |
| Python / uv | Python 3.12.3 (`make api-env`, pinned `uv sync --frozen`), uv 0.11.8, psycopg 3.3.6 |
| Docker | 29.6.2, build dfc4efb |
| Images | plain `postgres@sha256:33f923b05f64ca54ac4401c01126a6b92afe839a0aa0a52bc5aeb5cc958e5f20` (16.14-bookworm) + `infrx/state/supabase_shim.sql`; `supabase/postgres@sha256:7768d0d1d377250b718a9ad07f4661d008ebe6c96ecbbc4c08f3c5e53553e8fd` (17.6.1.173, no shim); `valkey/valkey@sha256:d2e18f3410b6f616de1417f570fa55261af2898b9c5b2cfb6781ce2373ea43d1` |
| Environment variable names | `INFRX_D_TASK`, `INFRX_D1_IMAGE`, `INFRX_MUTANTS`, `INFRX_D2_VALKEY_PORT`, `INFRX_D2_VALKEY_CONTAINER` (values in the commands; no secrets used) |

## Commands and results (from `apps/infrx-api`, tree `ce7659b` unless stated)

| Command | UTC | Exit | Tail (quoted) |
|---|---|---|---|
| `make api-env` | 2026-09-23T05:1x (before coding) | 0 | `+ zstandard==0.25.0` (pinned env synced) |
| `INFRX_D_TASK=d4 INFRX_D2_VALKEY_PORT=55465 INFRX_D2_VALKEY_CONTAINER=infrx-d4-valkey INFRX_MUTANTS=all uv run --frozen pytest -q -rs tests/d` (plain) | 2026-09-23T05:51:49Z–06:07:24Z | 0 | `788 passed, 29 xfailed, 1 xpassed in 933.81s (0:15:33)` — no skip line |
| same with `INFRX_D1_IMAGE=supabase` | 2026-09-23T06:07:36Z–06:24:34Z | 0 | `788 passed, 29 xfailed, 1 xpassed in 1016.66s (0:16:56)` — no skip line |
| focused: `… pytest -q -rs tests/d/test_journal.py tests/d/test_journal_races.py tests/d/test_streamstore_conformance.py tests/d/test_jobstore_conformance.py tests/d/test_e3b_drills.py` (plain) | 2026-09-23T06:24:54Z–06:25:48Z | 0 | `94 passed, 29 xfailed, 1 xpassed in 53.35s` |
| same, supabase | 2026-09-23T06:26:51Z–06:30:43Z | 0 | `94 passed, 29 xfailed, 1 xpassed in 230.09s (0:03:50)` |
| `INFRX_MUTANTS=all … pytest -q -s tests/d/test_migration_mutants.py -k d4_` (plain) | 2026-09-23T06:25:48Z–06:26:51Z | 0 | `44 passed, 428 deselected in 61.89s (0:01:01)` (the 44 kill lines are quoted below) |
| same, supabase | 2026-09-23T06:30:43Z–06:32:13Z | 0 | `44 passed, 428 deselected in 89.27s (0:01:29)` |
| `uv run --frozen pytest -q tests/d/test_code_mutants_d4.py tests/d/test_journal_units.py` | 2026-09-23T06:32:13Z–06:32:35Z | 0 | `15 passed in 21.75s` (9 mutants + the list check + 5 units) |
| `uv run --frozen python -m tests.d.code_mutants_d4` | 2026-09-23T06:3x | 0 | `9/9 killed` |
| `INFRX_D_TASK=d4 INFRX_D2_VALKEY_PORT=55465 INFRX_D2_VALKEY_CONTAINER=infrx-d4-valkey make check` | 2026-09-23T06:33:02Z–08:46:53Z | 2 | api-test: `71 failed, 2956 passed, 29 xfailed, 1 xpassed, 2 warnings, 13 errors in 8021.58s (2:13:41)` / `make: *** [Makefile:13: api-test] Error 1`. **Every failure and error is in `tests/q`** (grouped from the log: `test_reconcile.py` 39, `test_valkey_scheduler.py` 29, `test_valkey_mutants.py` 7, `test_reconcile_mutants.py` 6, `test_mutants.py` 3) — attribution (coordinator, during this run): five full-suite runs from several lanes shared `infrx-q3-valkey` (55462) while Q3's SIGKILL drills killed and restarted it; not D4 code. `make` stops at the first failing target, so the remaining targets were run individually (next rows). No skip line was printed |
| `INFRX_Q_VALKEY_PORT=55485 uv run --frozen pytest -q -rs tests/q` (port checked free with `ss -ltn`; no pre-started container - the harness made `infrx-q3-valkey-55485`) | 2026-09-23T08:47:21Z–08:48:36Z | 0 | `192 passed in 74.69s (0:01:14)` |
| `INFRX_D_TASK=d4 INFRX_D2_VALKEY_PORT=55465 INFRX_D2_VALKEY_CONTAINER=infrx-d4-valkey INFRX_Q_VALKEY_PORT=55485 make api-mutants` | 2026-09-23T08:48:48Z–10:31:40Z | 2 | `1 failed, 2339 passed in 6170.59s (1:42:50)` — the one: `tests/w/test_w3_mutants.py::test_mutant_is_killed[sigint_not_handled]`, `broken_runner … undeclared exception deaths ['TimeoutExpired@subprocess.py']` (a W3 signal-drain case timing out under the host's load; D4 touches no `infrx/worker`). Every D list in it passed, incl. all 469 migration mutants a third time |
| `INFRX_MUTANTS=all uv run --frozen pytest -q tests/w/test_w3_mutants.py -k sigint_not_handled` (rerun alone) | 2026-09-23T10:31:59Z–10:32:23Z | 0 | `1 passed, 83 deselected in 23.20s` |
| `make console-test` / `console-lint` / `console-typecheck` / `console-mutants` (first attempt) | 2026-09-23T07:47:46Z | 2 / 2 / 2 / 0 | this worktree had no `apps/app/node_modules` (`WARN Local package.json exists, but node_modules missing`, `eslint: not found`, `Command "next" not found`); `pnpm install --frozen-lockfile --prefer-offline` (lockfile unchanged, `git status` clean) and rerun: |
| same, after the install | 2026-09-23T07:50:55Z–07:53:21Z | 0 / 0 / 0 / 0 | `# tests 283` `# pass 283` `# fail 0`; lint `✖ 2 problems (0 errors, 2 warnings)`; typecheck exit 0; mutants `5/5 self-tests as expected` … `23/23 mutants killed` |
| `make bench-test` | 2026-09-23T07:50:03Z | 2 | `1 failed, 66 passed in 6.40s` — `models/marlin2b/tests/test_leaks.py::test_a_second_ctrl_c_cannot_lose_the_summary` (`AssertionError: handler restored`), a signal-timing case on a host running several lanes' suites; D4 touches nothing under `models/` |
| `make bench-test` (rerun) | 2026-09-23T07:5x | 0 | `67 passed in 7.00s` |
| legacy-first: `INFRX_Q_VALKEY_PORT=55485 uv run --frozen pytest -q -rs --ignore=tests/d tests/test_app_factory.py tests/test_gateway_auth.py tests/test_inflight.py tests/test_media.py tests/contracts tests/g tests/i tests/j tests/m tests/q tests/t tests/w` | 2026-09-23T10:32:32Z–10:46:25Z | 0 | `2600 passed, 2 warnings in 828.62s (0:13:48)` |
| track-first: the same with the track directories before the four legacy files | 2026-09-23T10:46:25Z–11:04:41Z | 0 | `2600 passed, 2 warnings in 1087.57s (0:18:07)` |
| `make integration INTEGRATION_ARGS="--layer 1 --canary"` | not run: D4 touches nothing under `tests/integration` (brief §7.5) |

`INFRX_MUTANTS=all` inside `tests/d` runs every D migration mutant — 469 on this tree:
D1 85, D1R 111, D2 97 + D2R 10, A1 35, D3 87, **D4 44** (a `Counter` over `migration_mutants.MUTANTS`
names) — plus D2's 29, D3's 10 and D4's 9 code mutants, the runner self-tests and the pgharness
drills; every D1/D1R/D2/A1/D3 mutant is still killed on both images (the sweeps have no
failure).

### D4 migration mutants: kill text (plain run, quoted; the supabase run killed the same 44)

```
d4_append_without_the_fence: killed by append_fenced -> AssertionError: a preparation token: None
d4_append_accepts_a_preparation_lease: killed by append_fenced -> AssertionError: a preparation lease appended: None
d4_r29_refusal_raised_rolls_back: killed by append_past_the_instant -> AssertionError: (False, {'request_id': UUID('3b124e15-a1e8-4561-8e37-6e20dc04fb4e'), 'job_handle': 'job_02b28a15c8024a2ea4475deea
d4_append_without_the_row_lock: killed by journal_races -> AssertionError: ((None, {'chunks': [{'bytes': 16, 'job_id': '7a000a08-d291-4a49-83c9-320a923f4b2c', 'payload': {'content': 'a'}, 'sequence': 
d4_terminal_event_accepted_when_last: killed by append_terminal_refused -> AssertionError: a batch with a terminal event: None
d4_event_limit_off_by_one: killed by append_oversize -> AssertionError: exactly the limit was refused: journal_write_failed
d4_oversize_event_stored: killed by append_oversize -> AssertionError: an oversize event in a batch: None
d4_only_the_first_event_is_measured: killed by append_oversize -> AssertionError: an oversize event in a batch: None
d4_job_ceiling_ignores_the_terminal_reserve: killed by append_job_ceiling -> AssertionError: an append past the ceiling: None
d4_terminal_event_waved_through: killed by append_job_ceiling -> AssertionError: reservation 213: a terminal event past the reservation: None
d4_terminal_reserve_sized_to_this_outcome: killed by append_job_ceiling -> AssertionError: reservation 213: a terminal event past the reservation: None
d4_empty_batch_publishes: killed by append_empty -> AssertionError: (None, {'chunks': None})
d4_sequence_restarts_per_batch: killed by append -> AssertionError: untyped 23505
d4_published_not_set: killed by append -> AssertionError: the first committed chunk did not set the publication marker
d4_stored_bytes_not_counted: killed by append -> AssertionError: the stored bytes were not counted on the job
d4_chunk_ttl_from_the_callers_clock: killed by append -> AssertionError: the chunk instants are not the database clock at the append: [{'job_id': 'fc2194cb-de37-496f-a9a9-e22c0671020f', 'generation': 
d4_job_ceiling_is_the_whole_reservation_plus_one: killed by global_charge -> AssertionError: past the reservation: None
d4_stored_counted_beside_the_reservation: killed by global_charge -> AssertionError: an append moved the global charge
d4_terminal_trigger_on_any_update: killed by terminal_every_path -> AssertionError: (no message)
d4_terminal_payload_from_the_old_row: killed by terminal_every_path -> AssertionError: 0012 preparation failure: {'cause': None, 'state': 'preparing', 'settlement_state': 'released_free'} != {'state':
d4_terminal_event_in_generation_one_always: killed by terminal_every_path -> AssertionError: not after the second generation: [(1, 3, 'terminal'), (2, 1, 'delta'), (2, 2, 'delta')]
d4_no_terminal_event_on_cancel: killed by terminal_every_path -> AssertionError: cancel before publication: 0 terminal events in []
d4_terminal_event_legacy_regime_only: killed by credit_journal -> AssertionError: a CREDIT cancel: 0 terminal events in [(1, 1, 'delta')]
d4_terminal_event_bytes_uncounted: killed by expire_bytes -> AssertionError: the pruned journal: stored 48 != the journal's 135
d4_terminal_event_below_the_watermark: killed by expire_prefix -> AssertionError: {'chunks': []}
d4_read_any_tenant: killed by read_tenant -> AssertionError: another org replayed it
d4_read_unbounded: killed by read_bounded -> AssertionError: a page of 1001 chunks
d4_read_zero_limit_is_an_empty_page: killed by read_bounded -> AssertionError: limit 0
d4_order_by_sequence_only: killed by read_replay -> AssertionError: pages of 1 spliced the generations: [(1, 1, 'delta'), (2, 1, 'delta'), (2, 2, 'delta')]
d4_gap_is_an_empty_page: killed by read_typed -> AssertionError: None: a silent gap
d4_past_head_is_an_empty_page: killed by read_typed -> AssertionError: (no message)
d4_cursor_overflows_as_an_integer: killed by read_typed -> AssertionError: {'generation': 1099511627776, 'sequence': 1}
d4_expired_journal_reads_empty: killed by read_typed -> AssertionError: None: an expired journal read as something else
d4_expire_on_the_callers_clock: killed by expire_clock -> AssertionError: expire(2027-09-20 12:00:00+00:00) pruned a live chunk
d4_prune_without_a_watermark: killed by expire_prefix -> AssertionError: (no message)
d4_prune_not_a_prefix: killed by expire_prefix -> AssertionError: the pruned set was not the prefix up to e
d4_sequence_reissues_a_pruned_cursor: killed by expire_prefix -> AssertionError: a pruned cursor was reissued: None {'chunks': [{'bytes': 16, 'job_id': 'a4b5fb2e-8ee5-4a2c-9bc2-d0e002d1c471', 'payload
d4_prune_keeps_bytes_charged: killed by expire_bytes -> AssertionError: a partial prune freed 0 bytes
d4_prune_frees_twice: killed by expire_bytes -> AssertionError: a partial prune freed 64 bytes
d4_usage_misreports_the_charge: killed by usage -> AssertionError: journal_usage {'chunks': 2, 'stored_bytes': 32, 'charged_bytes': 0, 'reserved_bytes': 33554432} != {'reserved_bytes': 33554432, 'stor
d4_expire_waits_on_a_locked_job: killed by journal_races -> AssertionError: the call waited 5.0s on the holder's lock
d4_append_granted_to_authenticated: killed by journal_privileges -> AssertionError: authenticated may execute infrx.append(jsonb)
d4_journal_usage_granted_to_anon: killed by journal_privileges -> AssertionError: anon may execute infrx.journal_usage()
d4_chunk_doc_callable_by_the_service: killed by journal_privileges -> AssertionError: service_role may execute the internal infrx.chunk_doc(infrx.stream_chunks)
```

Lines are cut at 200 characters. Three kills are bare `assert x == y` lines without a message (`(no message)`): `d4_terminal_trigger_on_any_update` (the 0012 path's `claim(...)[0] == "already_terminal"` - with the trigger on every update the nested UPDATE re-fires it and the second terminal insert violates the one-terminal index), `d4_past_head_is_an_empty_page` (`read(conn, request, at(1, 1))[0] == "invalid_cursor"` on the empty journal) and `d4_prune_without_a_watermark` (`read(conn, request, at(1, 0))[0] == "replay_gap"`); all three are AssertionErrors raised by the named check, which is the R40 kill rule.

## Failure drill

| Injection | Durable state before → after | Duplicate / retry | Cleanup |
|---|---|---|---|
| append commits, the answer is lost (`crash_after_commit("append")`, conformance `append_commits_before_it_relays`) | chunk committed and published → the retry of the next batch continues at the next cursor; replay returns the lost answer's chunk | no duplicate chunk; nothing relayed that is not durable | none needed |
| the worker dies after its first committed chunk (dr06) | running + published → the reaper fails it `lost_after_publication`, `held_unknown`, terminal event last | never requeued; a late append `already_terminal` | 24 h release platform-absorbed, no second event |
| an append past the generation instant (R29) | running → `failed/deadline_exceeded` with its terminal event, in the append's own committed transaction; the batch not stored | every later fenced call `already_terminal` | hold released (unpublished) or quarantined (published) |
| a terminalization with no room for the widest terminal event (reservation 213) | running, 107 bytes stored → cancel refused `journal_capacity_exhausted`, nothing changed; the reaper reports it `unsettleable` | retried by every sweep | settles after its chunk is pruned |
| an append in flight while the pruner runs | the pruner skips the job (SKIP LOCKED), prunes it next pass | an append behind a prune continues past the watermark | — |

Containers: `docker ps -a` after the last run lists no `infrx-d4-*` and no `*-55485` container
(checked 2026-09-23T11:04Z); every one was removed by the harness that created it.

**Attributions recorded from the coordinator (not debugged here, as instructed):** the
`tests/q` red inside `make check` (84 failures/errors, `FFF…F.F.F` at ~86 %) is the shared
`infrx-q3-valkey` (55462) used by five concurrent full-suite runs while Q3's SIGKILL drills kill
and restart it — green alone on a private port (192 passed); the `sigint_not_handled` W3 mutant
is a known load-sensitive runner timeout on this host today (it failed the same way in the
coordinator's whole-tree gates and is killed in isolation) — killed here alone (1 passed).
**E2 compose / D2 Valkey env:** nothing changed in any file; D4 only set, per invocation,
`INFRX_D2_VALKEY_PORT=55465 INFRX_D2_VALKEY_CONTAINER=infrx-d4-valkey` for tests/d's relay drills
and `INFRX_Q_VALKEY_PORT=55485` for tests/q (and the q lists of `api-mutants` and the orderings).

## Artifacts

Run logs in `/tmp/claude-1000/d4/` on the dev host (not committed; no secrets, prompts or
URLs): `sweep-plain.log`, `sweep-supabase.log`, `focused.log`, `mut-plain-1.log`,
`makecheck.log`, `make-rest.log`, `make-console.log`, `q-private.log`, `api-mutants.log`,
`orderings.log`. Numbers quoted here are copied from them.

## Changes (paths)

| Path | Change |
|---|---|
| `apps/app/supabase/migrations/0017_stream_journal.sql` | new: watermark columns + CHECK, `chunk_doc`, `append` body, `journal_terminal_event` + trigger, `read_journal`, `expire_journal`, `journal_usage`, privileges; header (lock order, rollback) |
| `apps/infrx-api/infrx/state/journal.py` | new: `PgStreamStore` |
| `apps/infrx-api/infrx/state/pgtesting.py` | `publish` = one chunk through the real append; `hooks(conn, store, stream=None)`; `stream` hook; `make_streamstore_factory`; `retune` syncs the stream's limits (D2's code-mutant anchors untouched) |
| `apps/infrx-api/infrx/state/credit_schema.py` | `SEAMS` += `append`, `read_journal`, `expire_journal`, `journal_usage` (additive) |
| `apps/infrx-api/tests/d/checks_journal.py`, `test_journal.py`, `test_journal_races.py`, `test_journal_units.py`, `test_streamstore_conformance.py`, `code_mutants_d4.py`, `test_code_mutants_d4.py` | new (`test_journal_units.py` is the no-Docker target the code-mutant runner needs, as D3's `test_lease_units.py`) |
| `apps/infrx-api/tests/d/migration_mutants.py` | additive: `D4_MUTANTS` (44), 20 `_CHECKS` entries, `from . import checks_journal` beside the block |
| `apps/infrx-api/tests/d/test_migration_mutants.py` | additive: 17 D4 names in `ALWAYS` |
| `apps/infrx-api/tests/d/checks.py` / `checks_credit.py` | additive: `append` in `FILLED_RPCS` / `FILLED_BOUNDARIES` |
| `apps/infrx-api/tests/d/test_jobstore_conformance.py` | comments only (the partition note); `PENDING` unchanged |
| `apps/infrx-api/tests/d/test_e3b_drills.py` | additive: dr05, dr06, dr08, dr10; one import line |
| `apps/infrx-api/tests/d/test_pgharness.py` | item 9d: `decoy(task)` and its test |

No contract, port, fake, config, Makefile or `tests/integration` change.

## Migration and rollback

`0017` is additive and re-runnable; nothing earlier is edited (no R84 amendment was needed:
the trigger route works). **Rollback** (0017 alone): `drop trigger jobs_terminal_journal_event on
infrx.jobs`; `drop function infrx.journal_terminal_event(), infrx.chunk_doc(infrx.stream_chunks),
infrx.read_journal(jsonb), infrx.expire_journal(jsonb), infrx.journal_usage()`; `alter table
infrx.jobs drop constraint jobs_journal_watermark_is_one_cursor, drop column
journal_pruned_generation, drop column journal_pruned_sequence`; restore the `append` stub as
0004's boundary block writes it (`perform infrx.unimplemented('append', 'D4'); return null;` —
`create or replace` keeps its grants). Stored chunks and `journal_stored_bytes` are 0003's and
are left alone. Applied only to this checkout's task-local containers.

**Item 9e — the pilot DSN's login role (I2B request 8), operator SQL, NOT in a migration** (a
password never belongs in one). Run once by the project owner on the pilot database, the
password supplied out of band (e.g. psql `\password infrx_gateway`), then stored only in the
SSM parameter `/model-inference/pg_journal_url`:

```sql
create role infrx_gateway login noinherit;           -- then: \password infrx_gateway
grant service_role to infrx_gateway;                 -- membership only: BYPASSRLS is not inherited
alter role infrx_gateway set statement_timeout = '30s';
```

The adapter's `jobstore.connector` already runs `set role service_role` on every connection
(0004's note), which is what makes the RLS-enabled `infrx` relations and the service-only
functions usable; `noinherit` keeps the login role itself powerless until it does. ⚠️ TO BE
VERIFIED on the hosted project: that the owner role there may `grant service_role` (on
Supabase `service_role` is created by `supabase_admin`; if `postgres` lacks ADMIN on it the
grant must be run as `supabase_admin`) — P-04's owner (not D4) runs it.

## Limits

1. **D5 owns the settlement.** 5 StreamStore and 23 JobStore strict-xfail cases need `complete`; D5's `infrx.terminalize` gets its terminal event from this trigger and must not write a second one.
2. **Terminal-event TTL without chunks** (pinned in round 2, J2) is the frozen default 3600 s (the trigger sees no store limits); with chunks it is the newest chunk's TTL, so a job's chunks expire in cursor order.
3. **The widest-payload literal (107)** in `journal_terminal_event` must follow the vocabularies; `check_append_job_ceiling` fails when a longer state/cause/settlement is added (it recomputes the widest and pins 2W / 2W−1).
4. **Deltas from the fake (not in conformance).** (a) a journal fully pruned while its job still runs — the fake answers `journal_expired` for ever and restarts its sequence at 1 below its own watermark; 0017 continues past the watermark and the new chunk is readable from it. Unreachable at the default limits (1 h TTL > any job's deadline). The fake's own append then restarts at sequence 1, below its watermark (review J4: a reissued pruned cursor - F request 10). (b) Review M4: the fake re-checks the per-job ceiling and the global total on every append with the CURRENT limits; 0017 uses the job's admitted `journal_reserved_bytes` and adds no global term (it cannot raise the charge). After a retune of `JOURNAL_JOB_RESERVE_BYTES`/`JOURNAL_TOTAL_BYTES` the fake refuses appends PostgreSQL accepts. (c) Review M1: payloads with a NUL character or a non-finite number - the fake stores them; `PgStreamStore.append` refuses them `journal_write_failed` (pinned by `test_journal_units.py::test_append__refuses_what_jsonb_cannot_store_before_sending_it`, whose fake-side assertion flips when F adopts the rule - request 10).
5. **Byte measure** (corrected in round 2, review M2): the stored measure is `octet_length(payload::text)` (0003's CHECK binds `bytes` to it). jsonb adds one byte per `": "`/`", "` separator over the fake's `compact_bytes` AND re-renders every number in fixed point (numeric has no exponent form): `{"logprob": 1e300}` is 18 compact / 314 stored bytes, `{"logprob": -3.2e-07}` 20 / 24 (pinned in `check_append_oversize`), so an event under `JOURNAL_EVENT_MAX_BYTES` on the compact measure can be over it stored (the reviewers measured 1048572 compact / 45387892 stored for a list of `1e300`). The round-1 text ("one byte per separator") was wrong for numbers. Pinned for strings: 1048576 accepted, 1048577 refused (1048575 compact).
6. **ponytail:** `expire_journal` is bounded per pass by jobs (1000, sent by the adapter and asserted since round 2, H1), not chunks; `journal_usage` counts chunks with `count(*)` on every call (G2's probe cadence decides whether `pg_class.reltuples` is needed).
7. **`append` must run in autocommit.** The "committed before relayed" guarantee rests on one-statement transactions; a pool that wrapped `append` in a caller's transaction would return rows before that transaction commits (G2 request).
8. **`JOURNAL_*` limits are provisional** (08 §5; P-18/E4B validates them under load). No LISTEN/NOTIFY (the returned chunks are the in-process notification).
9. `make check` was red only in `tests/q` (shared-Valkey interference, owner Q3/coordinator) and stopped there; its remaining targets were run one by one: `api-mutants` red only in one load-sensitive W3 mutant (owner W3, killed alone), `bench-test` red once in one signal-timing case (green on rerun), console targets needed `pnpm install` in this worktree. No failure is in D4 code.
10. Two commit-message miscounts (above); history not amended.

## Handback

**Next unblocked:** D5 on this head (`infrx.terminalize`'s settlement; `complete`; the five
StreamStore `_D5` cases and the 23 JobStore ones move to must-pass).

**Integration requests:**

1. **Coordinator (merge):** `tests/integration/test_harness.py::test_the_migration_set_is_the_console_one_and_is_read_in_filename_order` expects `0016_fenced_leases.sql` last — add `# D4 (stream journal)` / `"0017_stream_journal.sql",`; add `tests/d/test_code_mutants_d4.py` to `make api-mutants` (after `tests/d/test_code_mutants_d3.py`).
2. **G2:** compose `PgStreamStore(connector(dsn), limits=settings)` beside `PgJobStore` (same DSN and connector); keep its connections in **autocommit** (Limit 7); the relay relays only the chunks `append` returned; reconnects replay through `read_owned(org, handle, cursor)` (typed `invalid_cursor` 400 / `replay_gap` 410 / `journal_expired` 410 / `not_found` 404); `checks["journal"]` = a sync wrapper over `await usage()` (e.g. ready while `charged_bytes < JOURNAL_TOTAL_BYTES`).
3. **W3:** call `await stream.expire()` from the reaper tick (`WorkerService.reap_once`, 10 s) and pass the worker the real `PgStreamStore`.
4. **I3B / coordinator:** a journal-bytes gauge in `observe/metrics.py` fed by `usage()` (`reserved_bytes`, `stored_bytes`, `charged_bytes`, `chunks`).
5. **Coordinator (`tasklocal.TASK_PORTS`):** `"d4": {"postgres": 55435, "valkey": 55465}` — already on the base (`9c1c6ed`); nothing left.
6. **D5 (handover):** (a) *terminal journal event coverage* — `infrx.terminalize` gets its terminal event from 0017's trigger: write no second one, and set `settled_at` exactly once, in the same UPDATE as the outcome (the trigger reads `state`/`outcome_cause`/`settlement_state` from that new row); `PgStreamStore.finalize_in_transaction(outcome)` is the read-back the conformance cases call after `complete`; R39's journal-capacity condition is met by the held-back reserve (≥ the widest terminal payload, 107 bytes, whenever the reservation is ≥ 214 bytes) and re-checked by the trigger, which refuses the whole settling transaction `journal_capacity_exhausted` otherwise (any money moved earlier in it rolls back too); (b) *the probe* — `PgStreamStore.usage()` (`infrx.journal_usage()`) is the journal readiness probe's body and the gauge's source; (c) *typed replay errors* — `read_owned` raises `ReplayGap` (410) below the watermark, `JournalExpired` (410) when nothing is left, `InvalidCursor` (400) past the head or for a cursor of any magnitude, `NotFound` for another tenant or an unknown handle, `InvalidRequest` for a limit < 1; (d) move the 5 StreamStore and 23 JobStore `_D5` cases to must-pass.
7. **E3B phase 2:** point dr05/dr06/dr08/dr10's `rig("postgres")` at `pgtesting.make_jobstore_factory` + its `stream` hook (all four pass here verbatim).
8. **E3B phase 2 / coordinator (`tests/integration/pgstate.py` role matrix — its RELATIONS/FUNCTIONS completeness lists — and `test_harness.py`):** 0017 adds **no relation** (nothing for RELATIONS). It adds to `infrx.jobs` the columns `journal_pruned_generation int`, `journal_pruned_sequence int` (+ CHECK `jobs_journal_watermark_is_one_cursor`) — anon/authenticated: no access (no schema USAGE, table revoked); service_role: SELECT/INSERT/UPDATE/DELETE through 0004's table grant. Functions: `infrx.append(jsonb)` SECURITY DEFINER — service_role EXECUTE only (0004's grant, body replaced); `infrx.read_journal(jsonb)` SECURITY DEFINER STABLE — service_role only; `infrx.expire_journal(jsonb)` SECURITY DEFINER — service_role only; `infrx.journal_usage()` SECURITY DEFINER STABLE — service_role only; `infrx.journal_terminal_event()` SECURITY DEFINER trigger function — nobody; `infrx.chunk_doc(infrx.stream_chunks)` (SQL, not SECURITY DEFINER) — nobody (these six are FUNCTIONS' additions). Trigger `jobs_terminal_journal_event` AFTER UPDATE OF settled_at ON `infrx.jobs` FOR EACH ROW. Request 1's `test_harness.py` line lands in a file the E3B phase-2 lane edits — the coordinator applies it at merge.
9. **W (engine deltas; corrected in round 2, M1/M2):** size deltas against the stored measure (`octet_length(jsonb::text)`, Limit 5), not `compact_bytes` - emit numbers in fixed point (or measure the stored way); and never put a NUL character, NaN or an infinity in an event payload: `PgStreamStore.append` refuses such a batch `journal_write_failed` (the fake still stores it - request 10).
10. **Coordinator → F (the fake, `contracts/fakes/state.py`), from the review:** (a) refuse a payload with a NUL character or a non-finite number `journal_write_failed`, as `PgStreamStore.append` does (M1), then flip the fake-side assertion in `test_journal_units.py`; (b) after a full prune, continue the sequence past `pruned_to` (or refuse), never restart at 1 (J4); (c) optionally measure numbers the jsonb way (fixed point) and check the ceiling against the admitted reservation (M2, M4).

## Round 2 — the D4 review (`D4-review-a4598b0.json`, fix_required)

Fix round on `codex/d4-stream-journal` from `a4598b0`; no rebase, reset, amend or push; d4
ports only. **0017 is unchanged** (every finding was a missing check, an adapter rule or a
record), so no R84 amendment. Round-2 implementation head: `a4e0f39`.

| Finding | Commit | Check (killing test) | Declared mutant(s) → kill text (plain run, quoted) |
|---|---|---|---|
| **J1** (blocking) a stale pruner resets a pruned job's watermark | `d50e9ae` | `checks_journal.stale_pruner_race`, run by `check_journal_races` (`test_journal.py::test_races__appends_serialize_and_expire_skips_a_locked_job`) and by `test_journal_races.py::test_race__a_stale_pruner_never_resets_a_pruned_jobs_watermark`: a third connection holds job 1's chunk rows, pruner B blocks inside job 1, pruner A skips it and prunes job 2, then B reaches job 2 - watermark stays (1,2), rows and bytes unchanged, `read(None)` is `replay_gap` | `d4_expire_empty_inner_resets_watermark` → `a stale pruner reset job 2's watermark to None-None` |
| **J2** (blocking) + H3(a, c) the chunkless terminal event's TTL and generation | `1a0fc84` | `check_terminal_every_path`: for the five no-output paths the terminal event is at (last inference generation or 1, 1) - (3, 1) after two requeues - and `expires_at − persisted_at` = 3600 s | `d4_empty_journal_terminal_ttl_is_not_the_default` → `0012 preparation failure: a chunkless terminal event lives 0:00:30`; `d4_chunkless_terminal_event_at_the_first_generation` → `recover retries_exhausted: a chunkless terminal event at 1-1` |
| **M1** (blocking) payloads jsonb cannot store | `850d20b` | **Rule:** `PgStreamStore.append` refuses a batch whose payload holds a NUL character (key or string, any depth) or a non-finite number with `errors.JournalWriteFailed`, before anything is sent (as R25 refuses an oversize event); jsonb would raise an untyped 22P05/22P02 (pinned at the SQL boundary in `check_append_oversize`). `test_journal_units.py::test_append__refuses_what_jsonb_cannot_store_before_sending_it` (six payloads refused, nothing sent; `"\\u0000"` text and finite floats pass; the fake still stores all six - pinned, flips when F adopts the rule, request 10) | code: `the_unjournalable_payload_is_sent`, `a_nul_in_a_key_is_sent`, `a_non_finite_number_is_sent`, `a_nul_inside_a_list_is_sent` - all `[killed]` |
| **M2** (blocking) the stored measure re-renders numbers | `c4f9263` | `check_append_oversize` (now its first assertions): `{"logprob": 1e300}` 18 compact / 314 stored, `{"logprob": -3.2e-07}` 20 / 24, and `{"x": 1e300}` (12 compact, 308 stored) refused whole under a 64-byte limit. Limit 5 and request 9 corrected above | `d4_oversize_event_stored` → `a float event of 308 stored bytes, 64-byte limit: None` (no D4 code decides PostgreSQL's measure, so the declared mutant is the limit's own) |
| **H1** (blocking) pruning bounded per pass | `e656895` | asserted, not withdrawn: `check_expire_clock` - two jobs' expired chunks, `expire_journal({now: null, limit: 1})` prunes exactly one job per pass; the adapter now sends `limit: EXPIRE_JOBS_PER_PASS` (1000), `test_journal_units.py::test_expire__passes_the_callers_bound_and_counts` | `d4_expire_unbounded_per_pass` → `pass 1 with limit 1 did not prune exactly one job's two chunks`; code `the_pruning_bound_is_not_sent` `[killed]` |
| J3 the `old.settled_at is null` clause | `76468b9` | `check_terminal_every_path`: after a cancel, `update infrx.jobs set settled_at = settled_at` succeeds and `one_terminal_last` holds | `d4_terminal_trigger_refires_on_the_same_instant` → `rewriting settled_at to itself failed: duplicate key value violates unique constraint "stream_chunks_o…` |
| M3 / H3(b) no reservation, no journal | `76468b9` | `check_terminal_every_path`: a running job set to `journal_reserved_bytes = 0` cancels and gets no chunk | `d4_terminal_event_without_a_reservation` → `a job without a journal reservation could not terminalize: journal_capacity_exhausted` |
| H4 kill for the invariant's reason | `76468b9` | `check_terminal_every_path` opens with a live job checked after admission, claim_preparation, prepare, claim, heartbeat and append: no terminal event, `settled_at` null | new `d4_terminal_event_on_every_transition` → `after prepare the queued job's journal shows a terminal event`; `d4_terminal_trigger_on_any_update` kept, its `why` now says it dies via 0003's one-terminal index (`untyped 23505`) |
| H2 xfails pin their exception | `a4e0f39` | `raises=NotImplementedError` on every `_D5` xfail (JobStore and StreamStore) and on RACY, `raises=errors.InvalidApiKey` on the F2 case | harness hygiene, no code invariant, no mutant |
| H5 stale drill docstring | `a4e0f39` | sentence deleted | — |
| H6 commit cells | this commit | item 3 and item 5 rows now cite `543c18b` | — |
| H3(d) `read_journal` STABLE | this commit | recorded as untested (design note 7): no deterministic interleaving reaches between two statements of one call | — |
| J4, M4 fake deltas | this commit | Limits 4 (a)-(c); request 10 to F | — |
| J5 never-issued cursor below the head | this commit | documented as accepted (design note 7): order kept, nothing skipped | — |

**Mutant counts now:** 51 D4 migration mutants (44 + `d4_expire_empty_inner_resets_watermark`,
`d4_empty_journal_terminal_ttl_is_not_the_default`, `d4_chunkless_terminal_event_at_the_first_generation`,
`d4_expire_unbounded_per_pass`, `d4_terminal_event_on_every_transition`,
`d4_terminal_trigger_refires_on_the_same_instant`, `d4_terminal_event_without_a_reservation`);
14 D4 code mutants (9 + 5).

### Round-2 runs (tree `a4e0f39`, `/tmp/claude-1000/d4/round2.log`)

| Command | UTC | Exit | Tail (quoted) |
|---|---|---|---|
| focused, plain: `INFRX_D_TASK=d4 INFRX_D2_VALKEY_PORT=55465 INFRX_D2_VALKEY_CONTAINER=infrx-d4-valkey uv run --frozen pytest -q -rs tests/d/test_journal.py tests/d/test_journal_races.py tests/d/test_journal_units.py tests/d/test_streamstore_conformance.py tests/d/test_jobstore_conformance.py tests/d/test_e3b_drills.py` | 11:54:15Z–11:55:28Z | 0 | `101 passed, 29 xfailed, 1 xpassed in 71.57s` |
| same, `INFRX_D1_IMAGE=supabase` | 11:56:50Z–11:59:54Z | 0 | `101 passed, 30 xfailed in 182.06s` — the RACY case went XFAIL here (complete's fence won; it died of `NotImplementedError`, as its `raises=` now requires) |
| `INFRX_MUTANTS=all … pytest -q -s tests/d/test_migration_mutants.py -k d4_`, plain | 11:55:28Z–11:56:50Z | 0 | `51 passed, 428 deselected in 81.60s` |
| same, supabase | 11:59:54Z–12:01:59Z | 0 | `51 passed, 428 deselected in 124.70s` (51 kill lines, the same mutants) |
| `uv run --frozen python -m tests.d.code_mutants_d4` | 12:01:59Z–12:02:46Z | 0 | `14/14 killed` |
| full sweep, plain: `INFRX_MUTANTS=all … pytest -q -rs tests/d` (detached) | 12:02:46Z–12:21:54Z | 0 | `802 passed, 29 xfailed, 1 xpassed in 1145.72s (0:19:05)` — no skip line; every D1/D1R/D2/A1/D3/D4 migration mutant (476) and the D2/D3/D4 code mutants killed |

Conformance partitions unchanged: StreamStore 12 pass / 5 strict-xfail `_D5`; JobStore 45 pass /
23 strict-xfail `_D5` / 1 F2 / 1 RACY (focused: 101 = 20 journal + 7 races + 6 units + 13
stream + 46 job + 9 drills).

All 51 D4 kill lines of the plain run:

```
d4_append_without_the_fence: killed by append_fenced -> AssertionError: a preparation token: None
d4_append_accepts_a_preparation_lease: killed by append_fenced -> AssertionError: a preparation lease appended: None
d4_r29_refusal_raised_rolls_back: killed by append_past_the_instant -> AssertionError: (False, {'request_id': UUID('9d7b14b2-78fb-4f87-a669-1abea1783bc3'), 'job_handle': 'job_7bc12d3c360d4d028d935e7f3
d4_append_without_the_row_lock: killed by journal_races -> AssertionError: ((None, {'chunks': [{'bytes': 16, 'job_id': '84ee4ab7-2b1c-4758-8348-3529436c1ac4', 'payload': {'content': 'a'}, 'sequence': 
d4_terminal_event_accepted_when_last: killed by append_terminal_refused -> AssertionError: a batch with a terminal event: None
d4_event_limit_off_by_one: killed by append_oversize -> AssertionError: exactly the limit was refused: journal_write_failed
d4_oversize_event_stored: killed by append_oversize -> AssertionError: a float event of 308 stored bytes, 64-byte limit: None
d4_only_the_first_event_is_measured: killed by append_oversize -> AssertionError: an oversize event in a batch: None
d4_job_ceiling_ignores_the_terminal_reserve: killed by append_job_ceiling -> AssertionError: an append past the ceiling: None
d4_terminal_event_waved_through: killed by append_job_ceiling -> AssertionError: reservation 213: a terminal event past the reservation: None
d4_terminal_reserve_sized_to_this_outcome: killed by append_job_ceiling -> AssertionError: reservation 213: a terminal event past the reservation: None
d4_empty_batch_publishes: killed by append_empty -> AssertionError: (None, {'chunks': None})
d4_sequence_restarts_per_batch: killed by append -> AssertionError: untyped 23505
d4_published_not_set: killed by append -> AssertionError: the first committed chunk did not set the publication marker
d4_stored_bytes_not_counted: killed by append -> AssertionError: the stored bytes were not counted on the job
d4_chunk_ttl_from_the_callers_clock: killed by append -> AssertionError: the chunk instants are not the database clock at the append: [{'job_id': 'dc90baaa-1b8a-4d83-9de0-24f259f1d5cc', 'generation': 
d4_job_ceiling_is_the_whole_reservation_plus_one: killed by global_charge -> AssertionError: past the reservation: None
d4_stored_counted_beside_the_reservation: killed by global_charge -> AssertionError: an append moved the global charge
d4_terminal_trigger_on_any_update: killed by terminal_every_path -> AssertionError: untyped 23505
d4_terminal_payload_from_the_old_row: killed by terminal_every_path -> AssertionError: 0012 preparation failure: {'cause': None, 'state': 'preparing', 'settlement_state': 'released_free'} != {'state':
d4_terminal_event_in_generation_one_always: killed by terminal_every_path -> AssertionError: not after the second generation: [(1, 3, 'terminal'), (2, 1, 'delta'), (2, 2, 'delta')]
d4_terminal_event_on_every_transition: killed by terminal_every_path -> AssertionError: after prepare the queued job's journal shows a terminal event
d4_terminal_trigger_refires_on_the_same_instant: killed by terminal_every_path -> AssertionError: rewriting settled_at to itself failed: duplicate key value violates unique constraint "stream_chunks_o
d4_terminal_event_without_a_reservation: killed by terminal_every_path -> AssertionError: a job without a journal reservation could not terminalize: journal_capacity_exhausted
d4_empty_journal_terminal_ttl_is_not_the_default: killed by terminal_every_path -> AssertionError: 0012 preparation failure: a chunkless terminal event lives 0:00:30
d4_chunkless_terminal_event_at_the_first_generation: killed by terminal_every_path -> AssertionError: recover retries_exhausted: a chunkless terminal event at 1-1
d4_no_terminal_event_on_cancel: killed by terminal_every_path -> AssertionError: cancel before publication: 0 terminal events in []
d4_terminal_event_legacy_regime_only: killed by credit_journal -> AssertionError: a CREDIT cancel: 0 terminal events in [(1, 1, 'delta')]
d4_terminal_event_bytes_uncounted: killed by expire_bytes -> AssertionError: the pruned journal: stored 48 != the journal's 135
d4_terminal_event_below_the_watermark: killed by expire_prefix -> AssertionError: {'chunks': []}
d4_read_any_tenant: killed by read_tenant -> AssertionError: another org replayed it
d4_read_unbounded: killed by read_bounded -> AssertionError: a page of 1001 chunks
d4_read_zero_limit_is_an_empty_page: killed by read_bounded -> AssertionError: limit 0
d4_order_by_sequence_only: killed by read_replay -> AssertionError: pages of 1 spliced the generations: [(1, 1, 'delta'), (2, 1, 'delta'), (2, 2, 'delta')]
d4_gap_is_an_empty_page: killed by read_typed -> AssertionError: None: a silent gap
d4_past_head_is_an_empty_page: killed by read_typed -> AssertionError: (no message)
d4_cursor_overflows_as_an_integer: killed by read_typed -> AssertionError: {'generation': 1099511627776, 'sequence': 1}
d4_expired_journal_reads_empty: killed by read_typed -> AssertionError: None: an expired journal read as something else
d4_expire_on_the_callers_clock: killed by expire_clock -> AssertionError: expire(2027-09-20 12:00:00+00:00) pruned a live chunk
d4_prune_without_a_watermark: killed by expire_prefix -> AssertionError: (no message)
d4_prune_not_a_prefix: killed by expire_prefix -> AssertionError: the pruned set was not the prefix up to e
d4_sequence_reissues_a_pruned_cursor: killed by expire_prefix -> AssertionError: a pruned cursor was reissued: None {'chunks': [{'bytes': 16, 'job_id': '09733389-8c45-483c-940f-6600ce5c77e4', 'payload
d4_prune_keeps_bytes_charged: killed by expire_bytes -> AssertionError: a partial prune freed 0 bytes
d4_prune_frees_twice: killed by expire_bytes -> AssertionError: a partial prune freed 64 bytes
d4_usage_misreports_the_charge: killed by usage -> AssertionError: journal_usage {'chunks': 2, 'stored_bytes': 32, 'charged_bytes': 0, 'reserved_bytes': 33554432} != {'reserved_bytes': 33554432, 'stor
d4_expire_unbounded_per_pass: killed by expire_clock -> AssertionError: pass 1 with limit 1 did not prune exactly one job's two chunks
d4_expire_empty_inner_resets_watermark: killed by journal_races -> AssertionError: a stale pruner reset job 2's watermark to None-None
d4_expire_waits_on_a_locked_job: killed by journal_races -> AssertionError: the call waited 5.0s on the holder's lock
d4_append_granted_to_authenticated: killed by journal_privileges -> AssertionError: authenticated may execute infrx.append(jsonb)
d4_journal_usage_granted_to_anon: killed by journal_privileges -> AssertionError: anon may execute infrx.journal_usage()
d4_chunk_doc_callable_by_the_service: killed by journal_privileges -> AssertionError: service_role may execute the internal infrx.chunk_doc(infrx.stream_chunks)
```

## Round 3 — the round-2 confirmation (`D4-confirm-7d3ef59.json`)

One fix commit, `e34c748` (tests + one guard); 0017 still unchanged. **A1** (blocking): the
M1 unit test refuses the bad event last, first and in the middle of a batch - code mutant
`only_the_last_event_is_checked` (`events` → `events[-1:]`). **A2**: `_journalable` also refuses
a lone UTF-16 surrogate in a string or key (jsonb's untyped 22P02); its docstring now names the
refused values (NUL, lone surrogate, NaN/±Inf) and says non-JSON Python values (bytes, Decimal,
set) are not checked - code mutant `a_lone_surrogate_is_sent`; the fake cannot measure a
surrogate (`compact_bytes` raises `UnicodeEncodeError`), so surrogates stay out of the pinned
fake delta. **A3**: `check_append_oversize` pins the limit on stored octets per event - 25 × `é`
(40 characters, 65 octets) refused at 64, exactly 64 multibyte octets accepted, two 40-octet
events accepted together - migration mutants `d4_event_limit_on_characters`
(`65 octets in 40 characters, 64-octet limit: None`) and `d4_event_limit_on_the_batch_sum`
(`two events under the limit, over it together, were refused: journal_write_failed`). **A4**:
the positive unit payload holds str, float, int, bool, null, list and object - code mutant
`a_json_scalar_is_refused`. **JL-N1**: `rewritten_to_itself` converts only `UniqueViolation`,
and `d4_terminal_trigger_refires_on_the_same_instant` declares
`expects_detail="stream_chunks_one_terminal_idx"`. **JL-N2**: `never_while_live`'s docstring
says the any-update mutant still dies by 0003's index and the heartbeat step guards nothing a
current mutant reaches. **JL-N3** (note): 0017 already defaults `limit` to 1000
(`coalesce(limit, 1000)`, clamped 1..10000) = `EXPIRE_JOBS_PER_PASS`, so omitting it from the
adapter changes no behaviour; the unit test's recorded-arguments assertion is the only thing
that kills `the_pruning_bound_is_not_sent`.

| Command (tree `e34c748`, `/tmp/claude-1000/d4/round3.log`) | UTC | Exit | Tail (quoted) |
|---|---|---|---|
| `uv run --frozen pytest -q tests/d/test_journal_units.py tests/d/test_code_mutants_d4.py` (no Docker) | 12:42:11Z–12:42:47Z | 0 | `24 passed in 35.87s` |
| `uv run --frozen python -m tests.d.code_mutants_d4` | 12:42:47Z–12:43:28Z | 0 | `17/17 killed` |
| focused, plain (`INFRX_D_TASK=d4 INFRX_D2_VALKEY_PORT=55465 INFRX_D2_VALKEY_CONTAINER=infrx-d4-valkey`, the six D4 files) | 12:43:28Z–12:44:37Z | 0 | `101 passed, 29 xfailed, 1 xpassed in 67.39s` (partitions unchanged) |
| `INFRX_MUTANTS=all … pytest -q -s tests/d/test_migration_mutants.py -k "d4_ or well_formed"`, plain | 12:44:37Z–12:45:53Z | 0 | `54 passed, 427 deselected in 75.19s` (53 D4 mutants + the list check) |

Mutant totals: 53 D4 migration mutants, 17 D4 code mutants.

## Verification log

- 2026-09-23: D4 implemented on `740bebf`; commands above run on the committed tree `ce7659b`; no hosted or shared environment touched.
- 2026-09-23: Round 2 (D4 review, fix_required): J1, J2, M1, M2, H1 and the cheap nonblocking findings fixed at `a4e0f39`; Limits 4/5 and requests 9/10 corrected in place, marked "round 2".
- 2026-09-23: Round 3 (confirmation A1-A4, JL-N1-N3) at `e34c748`; plain-image focused suite and D4 mutants, units and code mutants rerun.
