# D10 — fix round (reviewer findings on `f9b6ce5c`), head `ff62913d`

| | |
|---|---|
| Task | D10 (consumer v1, program 22); the ONE fix round on the review of handback [D10-1f12cb6.md](D10-1f12cb6.md) |
| Branch / worktree | `codex/d10-durable` / `.claude/worktrees/codex-d10-durable` |
| Base / previous head | `dff31efc` / `f9b6ce5c` (code `1f12cb6a`) |
| Commits | `36dd64e2` (SQL + checks + mutants + connector), `ff62913d` (transcript comparison), this evidence commit |
| Isolation | `INFRX_D_TASK=d10`: `infrx-d10-postgres` 55442 (plain and Supabase image), `infrx-d10-valkey` 55469. No hosted DB, box, AWS or SSM. One disclosed exception below (§5). |
| Migrations | `git diff dff31efc -- 0001..0018` empty. 0019 and 0021 edited in place (unmerged); 0020 unchanged. |

| File | sha256 | lines |
|---|---|---|
| `0019_upload_readiness.sql` | `65d3d6937766ad6e4c8550e5cc6877a610d19a19398a71954a4030e39f0d169a` | 992 |
| `0020_content_lifecycle.sql` | `06a5dfde0c342ef4666a715e411915fd5e192f2791424a29efbaa66b710e2ec4` | 493 |
| `0021_read_authority.sql` | `2aea0c8e1aa1048a9fdb8cec02f107c161651210879d35d3e159e509335f6425` | 567 |

## 1. Findings → change → regression

| Finding | Change | Regression (oracle) | Fails before | Mutant(s) |
|---|---|---|---|---|
| 0-D10-R1, 2-ACI-3 (settled expiry rewritable) | 0021 `jobs_result_expiry_guard` + trigger `jobs_result_expiry_immutable` (BEFORE UPDATE OF `result_expires_at`): once `settled_at` is set any change is 23514 `immutable: ...`. The settlement's own UPDATE (0018, `settled_at` and expiry together) passes. | `checks_reads.check_result_expiry_persisted`: owner and `service_role`, +365 days and -600 s, each 23514 with `immutable` | yes: `AssertionError: a settled job's persisted expiry was rewritten (None): None` (probe before the fix: owner and service_role both succeeded) | `d10_settled_expiry_rewritable` killed |
| 1-RI-1 (NOT VALID check, R116) | 0021: the `jobs_success_has_result_expiry ... not valid` do-block removed; comment records the operator count + a later VALIDATED migration at zero | `test_upgrade_d10::test_a_pre_0018_success_without_expiry_retires_after_the_upgrade` (0001-0018, `seed_history`, USD success expiry → NULL, 0019-0021, `register_existing`, claim → tombstone → acknowledge_delete of result + payload, `expire_journal`; no unvalidated constraint on jobs); `checks_content.check_legacy_success_scrub` (same on the merged schema) | yes: `CheckViolation ... jobs_success_has_result_expiry` in `scrub_content`'s UPDATE of `request_record` (and `assert 1 == 0` on the convalidated probe) | `d10_success_expiry_not_valid` (re-adds it) killed by `legacy_scrub`; `d10_success_without_expiry` retired with the constraint |
| 0-D10-R2 (scrub guard columns) | none in SQL (the guard was right; the oracle was missing) | `check_content_scrub`: past the expiry, scrub + digest rewrite and scrub + `bytes+1` each refused by the guard's own `append-only` 23514; a scrubbed row restamped → 23514 | via mutants (each SURVIVED at `1f12cb6a` per the review) | `d10_scrub_guard_digest`, `_bytes`, `_twice` killed |
| 0-D10-R4 (expire window) | none in SQL | `check_upload_ticket`: `upload_expire` returns 0 with open tickets inside their window and at `expires_at - 1 µs`; that ticket then acknowledges and completes (`finalized`) | via mutant | `d10_upload_expire_early` killed |
| 0-D10-R5 (consumer unit/limit, P-22 edges) | none in SQL | `check_consumer_reads`: a legacy USD job of the same individual reads `unit = USD`, hold = its `maximum_hold` to 8 dp; `consumer_jobs(null, 1000)` over 108 rows answers 100. `check_usd_resolution`: a retired newer price row (discovery = admission = `_r1`), a future listing version (not priced), the newest listing after it takes effect (`pv_next`), that listing's deployment `draining` (falls back to the literal row) | via mutants | `d10_consumer_unit_mislabelled`, `d10_consumer_limit_unbounded`, `d10_usd_price_retired_row`, `d10_resolve_ignores_served`, `d10_resolve_future_listing`, `d10_resolve_oldest_listing` killed |
| 0-D10-R6 (runtime DML) | none in SQL | `check_reads_privileges`: no table UPDATE on jobs, no INSERT/UPDATE/DELETE on job_results, jobs UPDATE columns exactly `{updated_at}`; `test_runtime_role_is_refused_what_it_was_not_granted` as the real `infrx_runtime` login: UPDATE `result_expires_at`, UPDATE `request_record`, INSERT job_results → 42501 | via mutants | `d10_runtime_updates_jobs`, `d10_runtime_writes_results` killed |
| 0-D10-R3, 2-ACI-4 (runtime holds the unmarked doors) | 0019 `readiness_cutover_check()` adds `runtime_unmarked_doors` (not a gate): the marker-less doors `infrx_runtime` still executes. NOT revoked now: the current gateway/worker call `infrx.admit` / 0012 `claim_preparation` until G7 (`admit_ready`) and W5 (`claim_preparation_ready`) are wired. | `test_ready::test_the_cutover_gate...`: lists both today; after `revoke ... from infrx_runtime` answers `[]` | yes (field absent: `KeyError`-free failure at the new assertion) | — |
| 2-ACI-2 (transcript on merged tree) | 0021 `job_admission` outbox ordered `created_at, kind (prepare, inference), event_id` (deterministic under the frozen clock). `test_lifecycle_conformance._representation` class (5): an admission's outbox as its admission-time events (drop `inference_dispatch` when `prepare_dispatch` is present), and `_canonical` renumbers `<kind:N>` placeholders by first appearance after normalization | scratch merge (below): `test_lifecycle_conformance.py` 27 passed x3 | yes: on the merged tree 1 failed (the three `result_expiry__*` cases), then after class (5) alone still `<id:11>` vs `<id:12>` (a dropped event's id shifted numbering) | — |
| 0-D10-R7, 2-ACI-1 (merge with I8) | `jobstore.connector` now carries I8's `TRANSACTION_POOLER_PORT` + `session_state_allowed(dsn)` verbatim and the exact line `        if session_state_allowed(dsn):`; D10's `set_role: bool \| None = None` is an override (None = I8's rule). D10's `TRANSACTION_PORT` deleted. | `test_reads::test_the_connector_carries_no_session_state...` passes; on the scratch merge the file resolves to D10's side with nothing of integration's lost (`git diff HEAD^1` = only `+` lines and the signature) | — | — |
| 0-D10-R8 (M5/M6 follow-ups) | not D10 code: see §4 | — | — | — |
| 1-RI-2 (tests/q on q3's Valkey) | cross-track list rerun WITHOUT `tests/q`; the earlier "324 passed / 7 skipped" row included `tests/q`, which ran on `infrx-q3-valkey` 55462 (`vkharness.TASK = "q3"`) - a lane-rule breach of the previous round, now recorded | §2 row "other tracks" | — | — |

## 2. Commands and results (all rerun by me this round)

`cd apps/infrx-api`, `INFRX_D_TASK=d10 INFRX_D2_VALKEY_CONTAINER=infrx-d10-valkey INFRX_D2_VALKEY_PORT=55469`. Driver: scratch `/tmp/claude-1000/d10fr/chain.sh`, log `/tmp/claude-1000/d10fr/chain.log` (detached, 05:04-05:40Z).

| Command | Head | Exit | Result |
|---|---|---|---|
| fails-before: new checks against the `f9b6ce5c` SQL (`test_upgrade_d10 test_reads test_content test_ready`) | `36dd64e2` tests, old SQL | 1 | 4 failed (the four regressions above), 56 passed, 3 xfailed (`fails-before.log`) |
| new mutants `-k` (14) + the three static list tests | `36dd64e2` | 0 | 19 passed; every kill an AssertionError of the named check (`new-mutants.log`) |
| `pytest -q tests/d` (default subset) | `36dd64e2` | **0** | **811 passed, 1 skipped, 8 xfailed** (was 796/1/8; +15) |
| `INFRX_D1_IMAGE=supabase pytest -q tests/d/test_ready.py test_content.py test_reads.py test_lifecycle_conformance.py test_upgrade_d10.py test_postgrest_d10.py test_catalog_pg.py` | `36dd64e2` | **0** | **126 passed, 4 xfailed** |
| `pytest -q tests/contracts` | `36dd64e2` | **0** | **1168 passed** |
| `INFRX_MUTANTS=all pytest -q tests/d/test_migration_mutants.py tests/d/test_code_mutants*.py tests/d/test_signup.py` | `36dd64e2` | **0** | **741 passed, 0 survivors** (728 - 1 retired + 14 new) |
| `pytest -q tests/d/test_lifecycle_conformance.py` | `ff62913d` | 0 | 27 passed |
| other tracks on the D10 schema, WITHOUT `tests/q`: `contracts/test_cancel_cause.py g/ops/test_publication.py g/ops/test_cli.py m/test_pilot_media.py w/test_worker_main.py contracts/v2/test_v1_projection_pg.py g/test_composition.py w/test_prep_worker.py` with `docker events` recording creates | `ff62913d` | 0 | **132 passed, 7 skipped**; the only container this run created: `infrx-d10-postgres` |
| scratch merge `claude/consumer-v1` + `ff62913d` (clone `/tmp/claude-1000/d10fr/merge`; conflicts: `jobstore.py` → D10 side, the other 13 → integration side): `test_lifecycle_conformance test_reads test_ready test_content test_upgrade_d10 test_catalog_pg` + `tests/i/test_pooler.py` | merged | 0 | 133 passed, 4 xfailed before the renumbering fix had 1 failure (ACI-2); after it `test_lifecycle_conformance.py` 27 passed three times |

## 3. Merge into `claude/consumer-v1` (coordinator)

`git merge-tree --write-tree claude/consumer-v1 codex/d10-durable` still reports 14 conflicted paths (textual; D10 must not rebase or merge). Resolution, verified in the scratch merge: **`apps/infrx-api/infrx/state/jobstore.py` → take D10's side** (it is integration's file plus D10's lines: no integration line lost; I8's `session_state_allowed` anchor present once); **every other conflicted path → integration's side** (`tasklocal.py`, the 12 F2C paths incl. fixture `.2`, 01/02 plans, F2C evidence). Then rerun `tests/d/test_reads.py tests/d/test_lifecycle_conformance.py` and I8's `tests/i/test_pooler.py` + I mutants on I8's harness.

## 4. Wiring / follow-up requests (not D10 paths)

- **W5 + G7 (R3/ACI-4):** once `claim_preparation_ready` (W5) and `admit_ready` (G7) are the runtime's only admission/preparation doors, a D10 follow-up migration revokes `infrx.admit(jsonb)` and `infrx.claim_preparation(jsonb)` from `infrx_runtime` (service_role keeps them for the previous runtime / rollback), updates `D10-runtime-functions.txt`, and I8's probe list; `readiness_cutover_check()->runtime_unmarked_doors = []` is the check. Until then the one-phase barrier for `infrx_runtime` is enforced by wiring, not by grants - proposed ruling text: *"Until W5/G7 cut over, the readiness barrier is the runtime calling the ready doors; the operator's cutover gate reports `runtime_unmarked_doors`, and it must read `[]` before v1 is called enforced."*
- **M6 (R8):** `tests/m/test_retention.py:192` - the d10 world must expect `NotFound` for `media/<org>/../../payloads/x.json` (D10 refuses dot segments, as M6 asked); its `test_pg_*` cases must create their own database under the d10 task.
- **M5 (R8):** `tests/m/test_upload_restart_stack.py::test_stack__os_processes_a_probe_refusal_is_final_on_postgres` - remove the strict xfail (D10 accepts `media_refused`).
- **Operator (RI-1):** before a later migration adds `jobs_success_has_result_expiry` VALIDATED, `select count(*) from infrx.jobs where state = 'succeeded' and result_expires_at is null` must be 0; there is no backfill (R112), so on hosted history that is never unless such jobs are purged - the check may simply never be added. The handoff §8.6 `validate constraint` step is withdrawn.

## 5. Isolation note (this round)

The scratch-merge run included `tests/i/test_pooler.py` (the review's fix recipe), whose harness is I8's (`local_services("i8")`: `infrx-i8-postgres` + `infrx-i8-pgbouncer`, created and removed by that harness; none exists now). That is outside the d10 namespace; its count is not claimed as D10 evidence. The I mutants (`tests/i/test_mutants.py -k connector`) were NOT run for the same reason - coordinator/I8 to run them on the merged tree.

## 6. Remaining effort

Optimistic 0.5 h, likely 2 h, pessimistic 5 h (confidence **medium**). Basis: all D lists green at `36dd64e2`/`ff62913d`; left are the coordinator merge (§3), the W5/G7-gated revoke migration (§4, ≈1 h once wired) and W5 `fail_preparation` (unchanged from the previous handback).

## Verification log

- 2026-09-25T05:50Z: written at head `ff62913d`; logs under `/tmp/claude-1000/d10fr/` (`chain.log`, `fails-before.log`, `new-mutants.log`, `merged.log`, `others.log`).
