# W1 — Engine adapter and deterministic execution fakes

| Field | Value |
|---|---|
| Task | W1 (track W), oracles F-CONTRACT and API-STREAM |
| Status | **implemented** (fake/mock only: no engine process, no GPU, no network was contacted; nothing integrated, nothing live-verified) |
| Owner / session | Claude Opus 5 session, worktree `.claude/worktrees/codex-w1` |
| Base SHA | `8744418` (`claude/infrx-impl`, recorded by the coordinator) |
| Implementation SHA | `b4b4185` (branch `codex/w1-engine-adapter`; commits `b8f1137`, `52ab834`, `b4b4185`) |
| Integrated SHA | none — the coordinator integrates |

## Requirement coverage

Test ids are the pytest function names in `apps/infrx-api/tests/w/`. Each row names the
exact invariant the case asserts.

| Oracle | Test id | Invariant demonstrated |
|---|---|---|
| F-CONTRACT | `test_f_contract__the_real_adapter_passes_the_exported_engine_suite` | The **exported** `run_engine_conformance` suite (the same eight cases the fake passes) passes against the real `VllmEngine`: 8 ran, 0 skipped. A skip would be reported, never counted (r1 R32). |
| F-CONTRACT | `test_f_contract__the_factory_publishes_every_hook_the_suite_may_need` | `OPTIONAL_HOOKS["engine"]` is fully provided by W's factory, so no case silently asserts less than the fake's run does. |
| F-CONTRACT | `test_f_contract__the_adapter_satisfies_the_engine_protocol` | `isinstance(engine, ports.Engine)`; `generate` returns the iterator (not a coroutine); `cancel`/`health`/`drain` are async. |
| F-CONTRACT | `test_f_contract__health_drain_and_the_capability_probe` | `drain()` makes `health()["ready"]` false; an engine that answers 503 is not ready; the capability hook refuses an engine that does not serve the configured model or does not match a pinned version. |
| API-STREAM | `test_api_stream__the_body_carries_the_served_name_the_ceiling_and_the_salt` | vLLM is asked for its **served** name (`marlin2b`), never the platform model revision; `max_tokens` is the request's ceiling; `stream_options.include_usage` is set (an unrequested usage block is an unknown settlement); the tenant salt never appears on the wire in cleartext. |
| API-STREAM / MEDIA-SEC | `test_api_stream__prepared_media_replaces_the_customers_url` | Every media part is replaced positionally by its prepared ref, so the engine never receives a customer-chosen URL; a request whose parts and prepared refs disagree is `invalid_request`; two videos is `unsupported_parameter`; multimodal uuids differ per tenant. |
| MEDIA-PARITY | `test_api_stream__the_video_token_budget_is_f1s_own_function` | `mm_processor_kwargs` equals F1's `Media.budget_kwargs(duration)` (the function is called, not reimplemented); a text-only request carries no multimodal keys. |
| API-STREAM | `test_api_stream__the_cache_salt_is_per_tenant_and_per_profile` | 01 "Privacy and retention": same tenant + same source digest + same profile ⇒ same salt (the cache works); another tenant, another profile version or another model revision ⇒ a different salt; **no** tenant information ⇒ a per-request salt, so an unknown boundary shares with nobody. |
| API-STREAM | `test_api_stream__unsupported_options_are_refused_explicitly` | `tools`, `tool_choice`, `response_format`, `guided_json`, `logprobs`, `n=2`, `price_snapshot` (r1 R45), `max_tokens`, `stream` and any unknown parameter are `unsupported_parameter` with `param` set, **and nothing is sent upstream** (`upstream.requests == []`); supported sampling parameters are forwarded unchanged. |
| API-STREAM | `test_api_stream__the_output_ceiling_is_validated_and_enforced` | Ceiling 0 or above `MAX_OUTPUT_TOKENS` is refused; prompt + ceiling over `MAX_CONTEXT_TOKENS` is `context_length_exceeded`; usage past the ceiling, and more nonempty deltas than the ceiling even with no usage at all, are `EngineProtocolViolation` carrying `TerminalCause.platform_error` (01: a protocol violation beyond the envelope is a platform failure, never an unreserved debit). |
| API-STREAM | `test_api_stream__canonical_events_are_progress_deltas_and_one_usage` | One `progress`, the model's text as deltas, exactly one `usage`, whose counts are the **engine's** numbers and `certainty=authoritative`. |
| API-STREAM | `test_api_stream__deltas_carry_the_raw_text_and_the_visible_text` | The delta payload carries the raw model text (what trace capture records) next to the filtered text; with the delimiters split across chunks the raw text still contains `<think>` once and the visible text contains none. |
| API-STREAM | `test_api_stream__split_tokens_are_reassembled_whatever_the_boundaries` | One character per delta reassembles to the same answer. |
| DUR-SETTLE (input) | `test_api_stream__missing_or_malformed_usage_is_explicitly_unknown` | No usage block ⇒ **no** usage event and `stream.usage is None`; a malformed one (null, string or a `total_tokens` that does not add up) ⇒ a usage event that is explicitly unknown. Output chunks are never turned into a token count (02). |
| DUR-SETTLE (input) | `test_api_stream__at_most_one_usage_event_per_stream` | Two usage objects in one stream produce one usage event, so one request cannot become two settlements. |
| API-STREAM | `test_api_stream__transport_engine_and_incomplete_failures_are_distinct` | Four distinct classes: transport failure (pre-header and mid-stream), engine error before headers (status carried) and inside the stream (after published deltas), and output that just stopped (`EngineIncomplete`). None is a `DomainError`, so no route can echo engine text; operator detail is truncated to 500 characters from a 2 kB stack trace. |
| API-STREAM | `test_api_stream__a_stall_ends_the_stream_and_says_which_bound_it_passed` | The iterator **ends** rather than raising (the engine simply stops; W owns the policy) and says which bound: `first_token` at the lease's persisted `first_token_deadline_at` (r1 R20), `inter_event` at the stall budget, either by the injected clock or by a post-header read timeout. The scripted upstream would have kept sending keepalives for another five minutes: `0 < pings < max_keepalives` is the timer firing. |
| API-STREAM | `test_api_stream__a_generation_deadline_ends_the_attempt` | A lease whose phase instants have passed stops at the next event, and the earlier bound is the one reported. |
| API-STREAM | `test_api_stream__cancellation_closes_the_upstream_stream` | `cancel(lease)` stops the stream at the next event and **closes the upstream response** (`upstream.closed and not upstream.completed`), which is what stops generation in vLLM; the usage is then unknown, not a delta count; the intent does not outlive the stream. |
| API-STREAM | `test_api_stream__a_request_cancelled_before_it_starts_is_never_sent` | A lease cancelled before `generate` sends nothing at all (`upstream.requests == []`) and reports an authoritative zero usage: nothing ran. |
| API-STREAM | `test_api_stream__junk_lines_are_counted_and_never_relayed` | A data line that is not JSON is counted (`malformed_lines`) and never relayed as content. |
| API-STREAM | `test_api_stream__every_chunk_split_filters_to_the_same_text` | **132449 splits over 18 corpus entries**: for every way of cutting each text (exhaustively up to 16 characters, all ≤3-cut splits beyond), the filtered stream equals the filter applied to the unsplit text. Chunk boundaries are unobservable. |
| API-STREAM | `test_api_stream__the_raw_text_is_never_modified` | The filter is a projection: the concatenated raw pieces are the original text for every split. |
| API-STREAM | `test_api_stream__only_a_leading_block_is_a_delimiter` | A `<think>` after visible text is answer text; the first `</think>` closes the block (nested opens are reasoning); the whitespace before the open and after the close goes with the delimiter. |
| API-STREAM | `test_api_stream__an_unclosed_block_never_becomes_visible` | Reasoning that was never closed stays reasoning (the answer is honestly empty), including when the open delimiter itself was split. |
| API-STREAM | `test_api_stream__a_partial_delimiter_at_the_end_is_released` | `<thi` that never completes is answer text and `close()` gives it back. |
| API-STREAM | `test_api_stream__the_close_delimiter_may_straddle_any_boundary` | A 100 kB block followed by `</thi` + `nk>ans` yields `ans`, with O(1) held state. |
| R32 | `test_the_list_is_well_formed`, `test_every_owned_case_is_covered_by_a_mutant`, `test_mutant_is_killed[…]`, `test_the_runner_cannot_report_a_false_kill[…]` | Every invariant claimed above is killable by a declared single-edit mutant, and the runner cannot report a false kill or hide a skip. |

## Environment

| Item | Value |
|---|---|
| Host | Linux 7.0.0-1010-aws, x86_64, local development worktree (not staging, not production) |
| Python | 3.12.3, pinned environment `apps/infrx-api/.venv` from `uv sync --frozen --all-extras` (`make api-env`), uv 0.11.8 |
| Dependencies | as committed in `uv.lock`; no dependency added or changed by this task |
| Engine | **none**. Every case runs against `httpx.MockTransport`; no vLLM process, no GPU, no model weights, no socket. `FakeUpstream` reports version `0.11.0` and served model `marlin2b` as *scripted values*, not a probe of a real engine. |
| Services | none (no PostgreSQL, Valkey, ClickHouse, S3, Supabase or AWS resource was contacted or created; no container was started) |
| Clock / ids | injected `FakeClock` / `SequentialIds`; no wall-clock wait, no `sleep`, no randomness |
| Seed | none needed: every case is deterministic. The reasoning corpus and its split enumeration are fixed data in `tests/w/test_reasoning.py` (`CORPUS`, `EXHAUSTIVE_MAX = 16`, `LONG_TEXT_CUTS = (0, 1, 2, 3)`). |

## Commands and results

Exit status and the tail of the output, as run (UTC times from the run log):

```
### make api-test @ 2026-09-21T06:27:32Z
710 passed, 2 warnings in 41.78s
EXIT=0

### cd apps/infrx-api && uv run --frozen pytest -q tests/w/test_engine.py -k f_contract -s @ 2026-09-21T06:28:14Z
engine conformance against VllmEngine: 8 ran, 0 skipped
....
4 passed, 17 deselected in 0.21s
EXIT=0

### cd apps/infrx-api && uv run --frozen pytest -q tests/w/test_reasoning.py -s @ 2026-09-21T06:28:15Z
reasoning property: 132449 splits over 18 corpus entries
......
6 passed in 0.76s
EXIT=0

### cd apps/infrx-api && INFRX_MUTANTS=all uv run --frozen pytest -q tests/w/test_mutants.py @ 2026-09-21T06:28:16Z
......................................................                   [100%]
54 passed in 46.26s
EXIT=0

### cd apps/infrx-api && uv run --frozen python tests/w/mutants.py --list @ 2026-09-21T06:29:03Z
46 mutants over 20 named cases
EXIT=0

### git diff --stat 8744418..HEAD -- ':!apps/infrx-api/infrx/worker' ':!apps/infrx-api/tests/w' @ 2026-09-21T06:29:03Z
EXIT=0
### git status --porcelain @ 2026-09-21T06:29:03Z
EXIT=0
```

`make api-mutants` (the coordinator's contracts mutation list, unchanged by this task):

```
### make api-mutants @ 2026-09-21T06:29:11Z
............................................................             [100%]
276 passed in 169.19s (0:02:49)
EXIT=0

### uv run --frozen pytest -q tests/w --collect-only @ 2026-09-21T06:32:01Z
40 tests collected in 0.20s
EXIT=0
```

Baseline and focused counts:

* `make api-test`: **710 passed**, 0 failed, 0 skipped. The recorded baseline at `8744418`
  was **670 passed** (same command, same environment, run before any edit), so the 40 new
  cases are `tests/w/` and nothing existing changed state.
* focused suite `tests/w`: 40 passed (27 adapter/filter cases + 13 mutation cases in the
  default subset), 0 failed, 0 skipped.
* conformance: **8 of 8 engine cases ran, 0 skipped** against `VllmEngine`
  (`run_engine_conformance(engine_factory) == 8`).
* mutants: **46 declared, 46 killed, 0 survivors**, plus 6 runner self-tests asserting that
  a syntax error, an import error, a no-op edit, a lethal edit under the wrong case name, a
  missing anchor and a mutant with no case are each *not* a kill.
* `make api-mutants` (the coordinator's contracts list, all of it): **276 passed**, so
  nothing this task added weakens or breaks F2's own mutation evidence.
* console (`make console-test`, `make console-lint`, `pnpm exec tsc --noEmit`): **not run** —
  this task changes no console file.
* `make bench-test`: **not run** — `models/marlin2b/tests` does not exist (E1 owns it).
* Baseline byte-identity: `git diff` against the base excluding the two owned directories
  is empty (output above), so no F1 or contracts file was touched; `infrx/media/video.py`
  is imported and called, never edited.

### Failures and skips

None. No test failed and no test was skipped in any of the runs above. The five survivors
of the **first** full mutation run were fixed before this report; they are recorded below
because they are the interesting result of the task.

## Failure drill

The adapter holds **no durable state** — no journal, no wallet, no lease row — so "durable
state before/after" is not applicable to W1; W2 owns every durable mutation. What is
injected, and what is observed, per fault:

| Injection point | Injected by | Observed before / after |
|---|---|---|
| Connection refused before any response | `FakeUpstream(fault="transport_error")` | before: no request recorded past the handler; after: `EngineTransportError(stage=pre_headers)`, no event yielded, no partial output |
| HTTP 500 with a 2 kB error body | `engine_error_pre_headers` | after: `EngineError(stage=pre_headers, status=500)`, `stream.started is False`, `deltas == 0`, detail truncated to 500 chars, nothing relayed |
| SSE error object after one delta | `engine_error_post_headers` | after: `EngineError(stage=stream)` with `deltas == 1` already yielded — the caller knows output was published and must not regenerate (02) |
| Connection dies mid-stream (engine process exit) | `abrupt_exit` | after: `EngineTransportError(stage=stream)`, one delta already yielded, not a `DomainError` |
| Prefill silence with keepalives | `prefill_stall` | after: events are exactly `[progress]`, `stall == "first_token"`, usage unknown, `0 < pings < 40` (the adapter stopped waiting; the script had 33 more keepalives to send), elapsed > `TTFT_TIMEOUT_S` and < `GENERATION_TIMEOUT_S` |
| Mid-stream silence with keepalives | `midstream_stall` | after: deltas present, no usage event, `stall == "inter_event"`, elapsed > `TPOT_STALL_S` |
| Post-header read timeout | `read_timeout` | after: `stall == "inter_event"`, no exception raised into the caller |
| Cancellation mid-generation | `cancellation_race` + `cancel(lease)` | before: stream open, 2 deltas seen; after: upstream stream **closed** and not completed (the engine is told to stop), one usage event with unknown usage, `engine.cancelled` empty again |
| Cancellation before start | `cancel(lease)` then `generate` | after: `upstream.requests == []` (nothing was ever sent), one authoritative zero usage event |
| Usage missing / null / string / non-additive | `missing_usage`, `malformed_usage`, `string_usage`, `inconsistent_usage` | after: usage explicitly unknown in every case; no number is derived from the deltas |
| Usage or delta count past the ceiling | `over_ceiling`, `missing_usage` with `max_output_tokens=2` | after: `EngineProtocolViolation` carrying `TerminalCause.platform_error` |
| Junk data line | inline `MockTransport` | after: counted in `malformed_lines`, never relayed |

Duplicate/retry behaviour: `generate` is a fresh stream per call and holds no cross-call
state except the cancellation intent, which is discarded when the stream ends (asserted).
A repeated `cancel` for a lease that is not running leaves an intent that the next
`generate` for that lease consumes — noted under Limits.

Cleanup: every case runs in-process with `MockTransport`; no file, container, port or cloud
resource is created, so there is nothing to clean up. The mutation runner writes only
inside `tempfile.TemporaryDirectory` and the worktree is left clean (`git status` empty,
output above).

### The mutation run that mattered

The first full run of the 46-mutant list left **five survivors**, each a case that claimed
more than it proved. Three were fixed by strengthening the cases, two were equivalent
mutants replaced by edits that really do change behaviour:

| Survivor | Why it survived | Fix |
|---|---|---|
| `deltas_past_the_ceiling_accepted` | the delta bound was only ever reached in a stream that also reported over-ceiling usage, so the usage check killed the case first | the case now uses an engine that reports **no** usage at all, where only the delta counter can catch it |
| `usage_strings_trusted` | no case sent a *string* token count; the guard that was left still caught `None` | the scripted upstream gained `string_usage`, and the case asserts it is unknown |
| `engine_detail_unbounded` | the pre-header error body was 70 characters, so removing the 500-character bound changed nothing | the scripted body is now a 2 kB stack trace and the case asserts the detail is exactly 500 |
| `keepalives_count_as_progress` | the declared edit (`now` → `self.clock.now()`) was semantically identical | replaced with a keepalive that resets the inter-event timer, which the stall case kills |
| `unclosed_reasoning_leaks` | `close()` clears the tail before the branch, so the declared edit could not leak anything | replaced with an edit that hands back the held tail, which the unclosed-block case kills |

## Artifacts

* Source: `apps/infrx-api/infrx/worker/{__init__,engine,fakes,reasoning}.py`,
  `apps/infrx-api/tests/w/{test_engine,test_reasoning,mutants,test_mutants}.py` at `b4b4185`.
* Run log (raw command output quoted above) kept in the session scratch directory only; it
  contains no secret, no customer content and no signed URL. No durable evidence store is
  provisioned yet (r1 R15), so nothing is linked by digest.
* No credential, prompt, media file or upstream URL appears in any committed file; the
  scripted "customer URL" is the literal `https://videos.example.com/clip.mp4`.

## Changes

Owned paths only (`git diff --stat 8744418..HEAD`):

```
 apps/infrx-api/infrx/worker/__init__.py  |  15 +
 apps/infrx-api/infrx/worker/engine.py    | 582 +++++++++++++++++++++++++++++++
 apps/infrx-api/infrx/worker/fakes.py     | 214 ++++++++++++
 apps/infrx-api/infrx/worker/reasoning.py | 107 ++++++
 apps/infrx-api/tests/w/mutants.py        | 396 +++++++++++++++++++++
 apps/infrx-api/tests/w/test_engine.py    | 529 ++++++++++++++++++++++++++++
 apps/infrx-api/tests/w/test_mutants.py   | 110 ++++++
 apps/infrx-api/tests/w/test_reasoning.py | 135 +++++++
 8 files changed, 2088 insertions(+)
```

Public surface W2, G and E may rely on:

* `infrx.worker.VllmEngine(client, served_model=…, clock=…, limits=…, path=…,
  media_settings=…, require_version=…)` — `ports.Engine`, plus `capabilities()`.
* `infrx.worker.prepared_request(work, prompt_tokens, profile_version=None)` — `Work` →
  `PreparedRequest`, setting the internal `tenant_salt` parameter.
* `infrx.worker.EngineStream` — the async iterator `generate` returns; also
  `raw_text`, `visible_text`, `usage`, `deltas`, `finish_reason`, `stall`, `cancelled`,
  `complete`, `started`, `malformed_lines`, `malformed_usage`, `terminal_cause`.
* `EngineFailure` and its subclasses `EngineTransportError`, `EngineError`,
  `EngineIncomplete`, `EngineProtocolViolation`, `EngineUnsupported`, each with
  `terminal_cause` and an operator-only bounded `detail`.
* `infrx.worker.ReasoningFilter` / `filter_text`.
* `infrx.worker.fakes.FakeUpstream` / `engine_factory` for W2, G and E's tests.

**No contract change is requested.** No record, port, fixture, migration, configuration
name or dependency was added or altered; nothing in `infrx/contracts/**`, `infrx/config.py`,
`pyproject.toml`, `uv.lock`, the `Makefile` or any composition root was touched.

Migration / rollback: none. This task adds a new package and a new test directory and
changes no schema, no wire format and no deployed behaviour; reverting the three commits
removes both directories and restores the 670-case baseline exactly.

## Limits

1. **Fake-only.** Every result here is `httpx.MockTransport` against a scripted script.
   No real vLLM has parsed one of these bodies. `cache_salt`, `mm_uuids` and
   `mm_processor_kwargs` are sent under the field names `CACHE_SALT_FIELD` /
   `MM_UUIDS_FIELD` / vLLM's documented `mm_processor_kwargs`; **whether the pinned engine
   accepts those first two names is unverified** and is W3's capability probe (owner: W,
   with E on the allocated GPU). If a name is wrong the engine may ignore it, which would
   silently lose the tenant namespacing — so the probe must assert acceptance, not just a
   200.
2. **Read-timeout backstop untested at the transport layer.** `MockTransport` ignores
   `httpx.Timeout`, so the coarse read timeout is configuration only here; the stall bound
   that *is* proved is the injected-clock one plus a scripted `httpx.ReadTimeout`. A real
   hang is a Layer-3 case (owner: W3/E).
3. **Cancellation granularity.** `cancel(lease)` stops the stream at the next event and
   closes the connection. A stream blocked on a read notices only at its next event or at
   the read timeout; cancelling *instantly* means cancelling the asyncio task, which W2
   owns because W2 owns the task. Recorded in the code as a `ponytail:` note.
4. **Cancellation intents can accumulate** for leases that are cancelled and then never
   executed (a plain `set`). Bounded in practice by W2 cancelling only leases it holds;
   a bounded structure is the upgrade path if that stops being true.
5. **Inter-event budget comes from configuration**, not from the job's R4 `budgets`
   snapshot: `Lease` carries `first_token_deadline_at` and `generation_deadline_at` but no
   per-event stall instant. If a job's snapshotted `stall_s` ever diverges from the running
   configuration, W2 must construct the engine with the matching `limits` (or a contract
   revision must add the instant). Flagged for W2, not worked around here.
6. **Unclosed reasoning is dropped**, which differs from F1's non-stream regex (it keeps
   the remainder). Deliberate — reasoning must not reach a customer — and G owns retiring
   the F1 path; if product wants the remainder shown, that is a contract decision.
7. **`prompt_tokens` is an argument** to `prepared_request` because `Work` carries no exact
   prompt count; the caller (W2, from preparation) must supply the exact number. A wrong
   number changes only the zero-output cancellation usage and the context check, never a
   billable count, but it is an unverified interface until M2/W2 meet.
8. ~~**Canonical messages must carry one media part per prepared ref, in order**~~, or the
   adapter refuses the request rather than guessing. G1's normalizer and M2's preparation
   must preserve that correspondence (integration request below).
9. `terminal_cause` on `EngineStream` is **advisory**: the store decides the real outcome
   (a `completed` cause with unknown usage is `engine_incomplete` at the store, R21/R30).
10. The mutation runner is a second copy of the machinery in `tests/contracts/mutants.py`,
    because that file hardcodes its pytest target and is not this track's to parameterise.
    Marked `ponytail:` with the factor-out as the upgrade path.

## Handback

**Next unblocked task:** W2 (lease-aware execution, cancellation and completion). It starts
after W1 and may develop against the fakes; its *integration* still needs D5, M2 and Q3.
W2's expectations, per the W1 note in the handoff: `load_work` on a lease that lost its
fence answers `stale_lease`, while a job that has already terminalized answers
`already_terminal` — the two are different refusals and W2 must branch on them (a stale
lease means "stop and let the winner run", a terminal job means "stop and settle nothing").
The W1 note itself is not present in `research/plan/README.md` in this tree; this
restatement is from the task brief, and the coordinator should confirm it.

**Pending coordinator wiring / integration requests:**

1. *(none blocking)* No composition-root change: W1 exposes no router and no route. W2 or
   W3 will request the worker's service wiring.
2. **Normalized messages keep media parts positionally** — for G1 (normalizer) and M2
   (preparation): a `NormalizedRequest` that carries media refs must keep exactly one media
   content part per ref, in the same order, so the adapter can substitute prepared refs.
   `conformance.builders.request(refs=…)` currently builds a *string* content with media
   refs attached, which the adapter would refuse; W's tests build list content themselves.
   Worth a fixture in the shared builders if another track needs it (contract revision, not
   a W change).
3. **W2 must pass the engine the job's snapshotted budgets** (limit 5 above), i.e. build
   `VllmEngine(limits=<settings matching the job's Budgets>)` or request a contract revision
   adding an inter-event instant to `Lease`.
4. **W3: probe the multimodal/prefix-cache field names** (limit 1) against the pinned image
   and change `CACHE_SALT_FIELD` / `MM_UUIDS_FIELD` if they differ.
5. **Optional cleanup for the coordinator** (limit 10): move `Mutant`/`run_mutant` from
   `tests/contracts/mutants.py` into the contracts package with the pytest target as an
   argument, and delete `tests/w/mutants.py`'s copy of it.

**Unresolved findings:** none outstanding in W1's own code. The five first-round mutation
survivors are fixed and documented above.

## Verification log

- 2026-09-21: W1 implemented against the pinned environment and the shared fakes. Engine
  conformance 8/8 with 0 skips, `make api-test` 710 passed, 46/46 mutants killed. No engine
  process, GPU, cloud resource or real service was involved, so nothing here is integrated
  or live-verified.
- 2026-09-21: branch head is `086155c`, one test-only commit after the implementation SHA
  this report names (a tautological conditional removed from one assertion). Re-verified at
  that head with the same commands: `make api-test` 710 passed, `INFRX_MUTANTS=all pytest -q
  tests/w/test_mutants.py` 54 passed. No source file changed after `b4b4185`.

---

# Round 2 — independent review of `3294ead` (`fix_required`), fixed at `14af034`

Appended, not rewritten (`research/` convention). The review held the adapter under
attack and found four defects plus eighteen surviving mutants; ruling **R58** was issued
with it and is implemented here. Nothing below changes the round-1 status: still
**implemented**, fakes only, no engine, GPU, service or cloud resource.

## Corrections to the round-1 report above

| Claim in round 1 | Correction |
|---|---|
| "Unresolved findings: none outstanding in W1's own code." | **Wrong.** Four defects were outstanding and unfound: the media allow-list hole (B1), untyped failures escaping `generate` (B2), usage accepted as authoritative against a contradicting stream (B3) and the filter's held tail never reaching the event stream (B4). |
| "Every invariant claimed above is killable." | **Wrong.** The 46-mutant list covered about half of what the cases claimed; the reviewer's own corpus left 18 survivors (generation deadline, the stall bounds' measurement point, the tenant salt, usage validation, the boundary comparisons, health, the empty first delta, the dropped tail). The list is now 91 mutants over 30 cases, all killed. |
| "a request whose parts and prepared refs disagree is `invalid_request`" | **Incomplete.** Only the *count* was checked; order and part kind were not, and the scanner was a deny-list on `type`, so an untyped or differently-cased media part was forwarded verbatim. Now an allow-list with order and MIME class checked (R58). |
| "The W1 note itself is not present in `research/plan/README.md` in this tree; the coordinator should confirm it." | **Wrong, and the search was wrong.** The note is in `apps/infrx-api/infrx/contracts/README.md`, "Notes per track" rows **W1/M1** and **W1** - it says `already_terminal` means the store already terminalized the job in that same call (acknowledge and stop), while `stale_lease` leaves the state unchanged for `recover` to requeue, and that `first_token_deadline_at` is persisted by the store and enforced by W. Round 1 looked in the wrong checkout (the main worktree, where that file does not exist) and reported absence instead of re-checking. |
| `reasoning.py`: "holding back only what might still turn out to be a delimiter (at most six characters)" | **Wrong number.** The held state is the leading whitespace plus up to six characters of a possible `<think>`, or up to **seven** of a possible `</think>`. Docstring corrected. |

## Disposition, item by item

| Item | Disposition | Where |
|---|---|---|
| **B1** media allow-list (the security defect) | **Fixed.** `messages_for` rebuilds every message as exactly `{role, content}`, role ∈ `system\|user\|assistant`, each part exactly `{type:"text", text}` or `{type:"video_url", video_url:{our ref}}`. The pilot is text plus video only, so the image path is **removed**; image/audio/file/embeds/untyped/miscased parts, extra keys on a part or a message, non-object parts and other roles are refused **before anything is sent**. One media part per staged ref, in order, part kind checked against the ref's MIME class, and every ref must belong to the request's own organization (`prepared_request`, plus a single-tenant check in `upstream_body`). | `infrx/worker/engine.py` `messages_for`/`_part`/`upstream_body`/`prepared_request`; cases `…messages_are_rebuilt_from_an_allow_list`, `…no_outbound_body_ever_carries_a_foreign_url`, `…prepared_media_replaces_the_customers_url`, `…media_belongs_to_the_requests_tenant` |
| **B2** untyped failures | **Fixed.** `EngineFailure.__init__` coerces `detail` with `str()` (so `{"error":{"message":null}}` is no longer a `TypeError`); `choices`, a choice, `delta` and `content` are isinstance-guarded and a wrong type is an `EngineProtocolViolation` (dropping them would deliver a truncated answer as a whole one); an unpaired surrogate is refused, because the event must survive `model_dump_json` inside W2's `append`; `_run` wraps any other exception (an `OSError` mid-stream) as an `EngineFailure` carrying `engine_error`. The rule table is in the module docstring. | `engine.py` `_events`/`_choice`/`_run`/`EngineFailure`; case `…every_engine_failure_is_typed` |
| **B3** usage validation | **Fixed** per R58: usage is authoritative only when it is the **last** usage object, arrives **after the final content delta** and reports `completion_tokens ≥` nonempty deltas. Anything else is **unknown** with the reason recorded (`malformed`, `conflicting`, `delta_after_usage`, `below_delta_count`) - unknown, not a violation, because an under-reported count settled as authoritative would under-bill *and* close the job, while unknown usage has `02`'s reconciliation path. Usage *beyond* the envelope still raises (a reservation breach). The single usage event is emitted at the end of the stream, so "first wins" is no longer expressible; the old `…at_most_one_usage_event_per_stream` case, which locked that in, is gone. | `engine.py` `_note_usage`/`_final_usage`; case `…usage_is_authoritative_only_when_the_stream_agrees` |
| **B4** the held tail | **Fixed.** The filter's final tail is emitted as its own delta, so a consumer that relays events sees the whole answer; `visible_text` keeps it too. | `engine.py` `_generate`; cases `…the_filters_final_tail_reaches_the_event_stream`, `…every_chunk_split_reaches_the_customer_through_the_adapter` |
| **R58** event convention | **Implemented.** Delta payloads carry `visible` and `raw`; one event's text is split to fit `JOURNAL_EVENT_MAX_BYTES` (the filter is boundary-independent, so splitting loses nothing); accumulated output is bounded at 64 code points per output token (`est.`); an engine's error body is read bounded at 64 KiB; cancellation intents are keyed by `(job_id, generation)` in a bounded insertion-ordered map. **`content` remains as a documented transitional alias of `raw`** - see the integration request. | `engine.py` `_delta_payload`/`_choice`/`_read_bounded`/`cancel` |
| **B5** surviving mutants | **Fixed**, with a case for each: generation deadline (its own outcome, `deadline_exceeded`, checked ahead of the per-event bound), the inter-event budget measured from the last event (a slow but steady stream is not a stall), a pre-header timeout as a transport failure, negative/boolean/non-dict/string usage, the two boundary comparisons (`> ` not `>=`: an answer exactly at the ceiling and a prompt that exactly fills the context are legal), the empty first delta (`{"role":"assistant","content":""}`), the tenant salt (customer-supplied salt overwritten, unset salt, media digest, per-object uuid), health when the engine does not answer, the dropped tail, and `<THINK>` as text. | 91-mutant list, all killed |
| **Runner** | **Fixed.** A kill now requires an `AssertionError`, a rewritten `assert` or a pytest `Failed` (DID NOT RAISE); any other exception is `broken_runner` unless the mutant declares that exact mode in `allowed_errors` (capped at a fifth of the list, asserted). Two self-tests pin it: the same lethal edit is `broken_runner` undeclared and `killed` declared. Applying the rule turned twelve mutants into broken runs - every one a case that was letting a typed failure escape instead of asserting - so the cases now compare refusal **codes** (`refusal`), assert acceptance (`accepted`) and assert that a stall did **not** raise (`drained`). | `tests/w/mutants.py`, `tests/w/test_mutants.py` |

Two mutants were **removed rather than forced** (r1 R40: never keep one that cannot die,
never weaken a case to make it die): `malformed_usage_becomes_a_zero_count` (the malformed
flag and an absent candidate are the same fact, so no single edit changes the outcome -
the invariant keeps four other mutants) and, from round 1's list,
`at_most_one_usage_event` (the shape it described stopped existing under R58).

## Commands and results (round 2)

Measured at `987bb41` (round 3 re-ran them and they reproduce). The `git status` line in the
block below was taken **before** the last two commits of that round, so it shows two
modified files; the SHA above is the one the counts belong to.

```
### make api-test 2>&1 | tail -2 @ 2026-09-21T16:52:45Z
-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
722 passed, 2 warnings in 39.51s
EXIT=0

### cd apps/infrx-api && uv run --frozen pytest -q tests/w -s --tb=line 2>&1 | grep -E "conformance|property|passed|failed" @ 2026-09-21T16:53:25Z
engine conformance against VllmEngine: 8 ran, 0 skipped
adapter property: 738 splits over 7 answers
reasoning property: 140960 splits over 21 corpus entries
52 passed in 12.67s
EXIT=0

### cd apps/infrx-api && uv run --frozen python tests/w/mutants.py --list 2>&1 | tail -2 @ 2026-09-21T16:53:38Z

91 mutants over 30 named cases
EXIT=0

### git diff --stat 8744418..HEAD -- ':!apps/infrx-api/infrx/worker' ':!apps/infrx-api/tests/w' ':!research/plan/evidence/w' @ 2026-09-21T16:53:38Z
EXIT=0

### git status --porcelain @ 2026-09-21T16:53:38Z
 M apps/infrx-api/infrx/worker/engine.py
 M apps/infrx-api/tests/w/test_mutants.py
EXIT=0

```

The full mutation list, at the head this section names (one pytest process per mutant):

```
### uv run --frozen python tests/w/mutants.py @ 2026-09-21T16:55Z
[killed       ] body_sends_the_model_revision: 1 failed, 35 deselected in 0.44s via ['AssertionError']
… 89 more [killed] lines, 91 in all …
[killed       ] delimiter_case_insensitive: 2 failed, 34 deselected in 1.04s via ['AssertionError']

91/91 killed
EXIT=0
```

Counts, all from the output above: **722 passed** for `make api-test` (round 1 was 710,
the recorded baseline at `8744418` was 670); **52 passed** in `tests/w`, 0 failed, 0
skipped; the exported engine suite **8 ran, 0 skipped** against `VllmEngine`; the
reasoning property **140960 splits over 21 corpus entries** against an *independent*
oracle (a regex-and-find implementation, not the code under test); the adapter-level
property **738 splits over 7 answers** asserting that the concatenated `visible` of the
**events** is the filtered answer and the concatenated `raw` is the answer; **91 mutants
over 30 named cases, 91/91 killed**, 0 survivors, 0 broken runs, plus 8 runner
self-tests. `git diff --stat` against the base excluding the owned directories is still
empty, so nothing outside `infrx/worker/`, `tests/w/` and this report was touched.

## Limits, revised

Round 1's limits 1-10 stand except as noted, plus:

1. **Unchanged and still the biggest one:** no real vLLM has parsed one of these bodies.
   The `cache_salt` and `mm_uuids` field names remain unverified (W3's probe must assert
   acceptance, not a 200).
2. **New, from the allow-list:** `storage_ref` is sent with **no URL scheme**
   (`media/<org>/v1/source`). Real vLLM needs a resolvable reference - `file://` plus an
   allowed local media path, or an equivalent - so M2/W3 must decide the exact form; the
   adapter's rule is only that the customer's own value is never used.
3. **New:** the engine must **not** be started with `--reasoning-parser`. This adapter
   reads `delta.content` only, so a parser that moves reasoning into `reasoning_content`
   would drop it - and `raw` is what trace capture keeps, so the trace would lose it
   silently. Named in the code beside the read; W3 owns the engine flags.
4. **New (`est.`):** the accumulated-output bound is 64 code points per output token, and
   one event carries at most an eighth of `JOURNAL_EVENT_MAX_BYTES` in characters. Both
   are provisional engineering limits like every number in 08 §5, chosen with ~16x
   headroom over real text; E's corpus run is what would justify tightening them.
5. **Withdrawn:** round 1's limit 8 ("canonical messages must carry one media part per
   prepared ref, in order, **or the adapter refuses**") is no longer a limit but the
   contract (R58), and G1 enforces the same shape before admission.

## Handback additions

**Integration requests added by this round:**

6. **Coordinator: the shared fake and the exported engine cases must emit `visible` and
   `raw`** (R58 says so explicitly). They currently read `payload["content"]` and assert
   the *unfiltered* text there, so this adapter keeps `content` as a transitional alias of
   `raw` to pass the suite unchanged. The alias is marked in `_delta_payload` and goes in
   the same change that updates `contracts/fakes/engine.py` and
   `conformance/services.py`. Until then, **no relay may read `content`** - a relay that
   does leaks the reasoning block.
7. **G1 owns sampling-parameter ranges.** The adapter's allow-list decides which knobs
   exist (`temperature`, `top_p`, `top_k`, `min_p`, `seed`, `stop`, the three penalties);
   it does not check that `temperature` is in `0..2`. That validation belongs at admission,
   with the token-ceiling range checks R55 already gives G1.

**Notes for W2, beyond round 1's:**

* `terminal_cause` returns `completed` only when `finish_reason ∈ {stop, length}` **and**
  the usage is authoritative; a finished stream with unknown usage is `engine_incomplete`,
  which is what D does with a delivered success that has no count.
* **Stamp TTFT at the first RAW nonempty delta**, not the first visible one: a reasoning
  block can make the first customer-visible character arrive seconds after the engine
  started producing, and TTFT is an engine measurement.
* A stall never raises: read `EngineStream.stall` (`first_token` / `inter_event` /
  `generation`) and `terminal_cause`. `generation` is `deadline_exceeded` (platform's own
  deadline, R21), the other two are `engine_incomplete`.
* Cancellation intents are keyed by `(job_id, generation)`: never call `Engine.cancel`
  with a lease of another generation to stop the current attempt - it will not stop it,
  and it must not.
* Per round 1's carried note, now confirmed in `apps/infrx-api/infrx/contracts/README.md`
  ("Notes per track", rows **W1/M1** and **W1**): `already_terminal` means the store
  terminalized the job in that same call - acknowledge the index event and stop;
  `stale_lease` leaves the state unchanged for `recover`.

## Verification log (continued)

- 2026-09-21: Round-2 fixes for the independent review of `3294ead` and for ruling R58.
  `make api-test` 722 passed; `tests/w` 52 passed; engine conformance 8/8 with 0 skips;
  91/91 mutants killed over 30 cases with the runner now requiring an assertion. Still
  fakes only: no engine, GPU, container, cloud resource or real service was involved, so
  nothing here is integrated or live-verified.

---

# Round 3 — independent review of `987bb41` (`fix_required`), fixed at `cc8ef39` (the evidence commit below is the head)

The review held the adapter under attack again: B1–B4 and all eighteen round-1 survivors
stayed dead, and it found **two bounds defects** (B6, B7) plus six surviving mutants and a
second false-kill channel in the runner (B8). Its file:line references and measurements
are in `.claude/handoff/wave2/W1-review-r2.md`. Status is unchanged: **implemented**,
fakes only, no engine, GPU, service or cloud resource.

## Corrections to the round-2 appendix

| Claim in round 2 | Correction |
|---|---|
| "the malformed flag and the absent candidate are **the same fact**, so no single edit changes the outcome" (the reason given for deleting a mutant) | **Wrong.** A malformed usage object *followed by a valid one* leaves a candidate **and** the flag, so dropping the flag from the condition changes the outcome to authoritative `(100, 1)` - which is exactly the mutant the review resurrected (`malformed_flag_ignored`, now killed). The ruling is explicit: **any** malformed usage object in the stream makes the usage unknown, in either order; identical repeated objects are authoritative and yield exactly one usage event. |
| "One media part per staged ref, **in order**" | **Unobservable, therefore not claimed.** With one video per request at most, there is no second media part to be out of order: the ordering rule is real in the code (each video part consumes the next ref, and its kind must match) but no case can distinguish it from the count and kind checks. Said plainly here rather than left as evidence for something untested. |
| The round-2 command block | **Relabelled.** It quoted a `git status` with two modified files, because the counts were taken before the last two commits of that round. Every count in it reproduces at `987bb41`, which is the SHA it should have named. |
| "one event fits the journal", proved with `model_dump_json` on ASCII text | **Wrong rule and wrong data.** The store sizes an event as `len(compact_bytes(event.payload))`; the test measured a different function, and only with `"x"`. See B6. |

## Disposition

| Item | Disposition | Commit | Killing test / mutant |
|---|---|---|---|
| **B6** per-event bytes | **Fixed.** Sizing was in code points, true only for ASCII: one 131,072-code-point delta measures 393,252 B of ASCII but 1,179,684 B (`日`), 1,572,900 B (`😀`) and 2,359,332 B (`\x01`) against a 1,048,576 B ceiling, so W2's `append` would have refused an ordinary CJK answer. Pieces are now measured in **encoded JSON bytes** (`_split_encoded` over a memoised per-code-point cost: 6 for a control character, 2 for an escaped quote, otherwise its UTF-8 length), each of the three copies gets a quarter of the ceiling, `raw` and `visible` are split **independently** (a `visible` can be longer than the delta that released it), and the held tail goes through the same splitter. Cuts stay on code-point boundaries. | `201528b` | `…an_event_always_fits_the_journal_in_any_script` (ASCII/CJK/emoji/control/mixed × ceilings 1 MiB, 4096, 512, measured with `compact_bytes`; a 100k-space held tail; a `visible` longer than its delta at a 128-byte ceiling) · mutants `event_bytes_sized_for_ascii`, `json_escape_cost_ignored`, `event_bytes_unbounded`, `visible_never_split`, `held_tail_never_split` |
| **B7** the line buffer | **Fixed.** `httpx.aiter_lines()` buffers a newline-less stream without bound and returns to the caller only when a line completes, so a 200 MiB `data:` line was pulled whole (800 MiB peak) and nothing checked the deadline or the cancellation while it flowed - the read timeout never fires because bytes keep arriving. `_lines` splits the stream here over `aiter_bytes()` with an incremental UTF-8 decoder, caps the pending buffer at one journal event (past it the engine has broken the protocol) and runs `_overdue` plus the cancel check **once per network chunk**. A final line with no trailing newline is still delivered. | `b8a837c` | `…a_line_that_never_ends_is_bounded_and_still_checked` (a flood of 1 MiB chunks must raise having pulled ≤3 of the 24 on offer - memory asserted as chunks pulled, not RSS - and a slow flood must stop inside its 100 s deadline with `stall == "generation"`) · mutants `pending_line_unbounded`, `pending_cap_is_a_whole_stream`, `chunk_checks_skipped`, `chunk_cancel_check_skipped` |
| **B8.1** malformed usage in either order | **Fixed** (ruling above). | `337af94` | `…usage_is_authoritative_only_when_the_stream_agrees` (faults `valid_then_malformed`, `malformed_then_valid`, `repeated_usage`) · mutant `malformed_flag_ignored` |
| **B8.2/3** `completed` needs its finish reason | **Fixed**: only the usage half was asserted. | `337af94` | `…a_finish_reason_outside_the_set_is_not_a_success` (fault `abort_finish`) · mutants `completed_without_finish`, `abort_is_finished` |
| **B8.4** eviction order | **Fixed, and the rule changed.** Every key in the intent map is one no `generate` has consumed, so *any* blind eviction can forget a live cancellation - which is what dropped a pre-start intent after 1,024 later cancels and sent the request anyway. Only a generation this engine has **already finished** may be evicted, and when none can be, `cancel` answers **False** rather than silently forgetting; the caller still owns the task. | `337af94` | `…a_cancellation_is_scoped_to_its_generation` (the held pre-start intent survives 1,074 later cancels, `False` appears among the answers, the finished generation is the one evicted, and the intent still stops its request) · mutants `cancel_evicts_a_live_intent`, `cancel_forgets_silently`, `cancel_intents_unbounded` |
| **B8.5** the inner generator | **Fixed, and the old proof was worthless.** `_generate` delegated to `_attempt` with `async for` and never closed it, so `aclose()` left the engine being read. Both wrappers now close what they delegate to. Separately: httpx does **not** close a `MockTransport` content iterator when the response closes (measured - the fake's `closed` flag only flips at loop shutdown), so `upstream.closed` after `asyncio.run` proved nothing. The fake counts the frames the adapter **pulled**, the stream records `upstream_closed`, and the assertions moved inside the coroutine. | `337af94` | `…a_cancellation_is_scoped_to_its_generation` (inside the coroutine: `upstream_closed`, ≤3 frames pulled, a further `__anext__` raises `StopAsyncIteration` and pulls nothing more) · mutants `inner_generator_not_closed`, `run_does_not_close_its_inner` |
| **B8.6** two usage events | **Fixed** (restored as the identical-objects case). | `337af94` | `…usage_is_authoritative_only_when_the_stream_agrees` · mutant `two_usage_events` |
| **Runner** false-kill channel | **Fixed.** `drained()` swallowed `EngineFailure(stage="adapter")`, so a mutant's `NameError` on the generate path arrived as a tidy "failure is not None" assertion. It re-raises that stage now. | `337af94` | self-tests `a_crash_on_the_generate_path_is_not_a_kill` / `the_same_crash_declared_is_a_kill` |
| **Ruling:** a dropped line is not a billable success | **Implemented.** `malformed_lines > 0` forces `engine_incomplete`: junk where a delta should have been, or one JSON object cut across two `data:` lines, lost content and still ended `completed` with authoritative usage - the only cause that can charge (R21). | `2b49de1` | `…a_dropped_line_is_never_a_billable_success` (both repros) · mutant `dropped_line_still_completes` |
| `index ≥ 1` | **Refused** (chosen over ignore-with-violation): `n=1` is forced, so a second choice is another sample or another request's, and merging it interleaves two answers in one journal. | `2b49de1` | `…a_second_choice_is_a_protocol_violation` · mutant `second_choice_merged` |
| `storage_ref` grammar | **Implemented** inside the adapter, the last hop before the engine: the key must match the store's grammar (`media/<org uuid>/…`, the fixture's shorter form and the store's five-segment key both accepted) **and** sit under the ref's own organization. Ten hostile forms are refused. | `2b49de1` | `…a_prepared_reference_must_be_one_the_store_could_have_made` · mutants `storage_ref_shape_trusted`, `storage_ref_tenant_trusted`, `storage_ref_unchecked` |
| `prompt_tokens` / `duration_s` | **Refused**: a count outside `0..MAX_CONTEXT_TOKENS` (10**30 was accepted) and a prepared video with no duration (which `or 0.0` turned into a four-frame budget). | `2b49de1` | `…an_unmeasured_prompt_or_duration_is_refused` · mutants `prompt_tokens_unbounded`, `missing_duration_is_zero` |
| Pinned vocabularies | **Pinned**: `ALLOWED_ROLES` and the three timer comparisons (`>=`: a deadline is reached *at* its instant). | `2b49de1` | `…the_roles_and_the_timer_boundaries_are_pinned` · mutants `roles_widened`, `first_token_boundary_exclusive`, `stall_boundary_exclusive`, `generation_boundary_exclusive` |

## What changes when the `content` alias goes

The coordinator removes it right after W1 merges (the shared fake emits `{visible, raw}`
and the two exported cases read `raw`). Then W deletes the alias from `_delta_payload` and
the assertion that reads it. B6 is sized for the **three-copy** payload today: with two
copies `PAYLOAD_COPIES_DIVISOR` can go from 4 to 3, which raises the per-event text budget
by a third. Nothing else changes, because the test measures
`compact_bytes(event.payload)` rather than a computed limit - it stays green at either
divisor and would fail at a divisor that does not fit.

## Limits, revised again

Round 1's and round 2's limits stand except as noted, plus:

1. **`cancel` can answer `False`.** When every recorded intent still belongs to work that
   may run, the engine refuses to record another rather than forget one. W2 must treat
   `False` as "hold the intent yourself" - cancel the asyncio task, which it owns anyway.
   With `WORKER_CONCURRENCY` 10 against a 1,024-entry map this needs a thousand cancels for
   work that never ran, but it is reachable over a long process life.
2. **Closing the response is not proved to stop generation.** It closes *our* side; that the
   socket closing makes vLLM abandon the sequence is a Layer-3 fact (W3, with a real engine).
   `MockTransport` cannot show it, and this round found the old `upstream.closed` assertion
   was proving nothing at all.
3. **The `storage_ref` grammar is a mirror of M's.** `FakeMediaStore._key` owns the layout;
   the adapter refuses anything that is not that shape under the ref's own tenant. If M
   changes the layout, the constant in `engine.py` changes with it - flagged as an
   integration request rather than left to drift.
4. **The read-timeout backstop is still untested at the transport layer** (`MockTransport`
   ignores timeouts). B7 removed the part that mattered - a live stream can no longer run
   past its deadline - but a *silent* socket is still only covered by configuration; W3 tests
   a real hang.

## Handback additions (round 3)

**For W2** - the reviewer's list, plus what this round changed:

* Relay and journal **`visible` only**, never `content` (a transitional alias of `raw`) and
  never `raw`: `raw` exists for trace capture.
* Build requests only through `prepared_request(work, exact_prompt_tokens)` - it is where the
  tenant check, the `prompt_tokens` range and the salt are applied.
* Stamp TTFT at the **first raw nonempty delta**, not the first visible one.
* `terminal_cause` is advisory. W2 settles `completed` only with `finish_reason ∈
  {stop, length}` **and** authoritative usage **and** `malformed_lines == 0`.
* A usage event can arrive **together with** a stall or a cancellation: the usage event is
  emitted at the end of the stream whatever ended it.
* Cancel only with the **current** lease: intents are keyed by `(job_id, generation)`, and
  on lease loss the answer is to stop the task, not to cancel another generation.
* Keep a **task-level deadline** as a backstop: the adapter's bounds are per event and per
  phase, not a guarantee about the whole attempt.
* `cancel` may answer `False` (limit 1).

**For G1**: sampling-parameter *ranges* are admission's (the adapter's allow-list only says
which knobs exist); a client `price_snapshot` is `unsupported_parameter`; the normalized
message shape is R58's, which the adapter re-checks.

**For M2**: the adapter validates `storage_ref` against the store's key grammar and the
ref's tenant, and refuses a prepared video with no `duration_s`. Settle the exact form the
engine needs (`file://` plus an allowed media path) with W3; the adapter's constant follows
M's layout.

**For W3**: no `--reasoning-parser` (reasoning would move to `reasoning_content` and be
dropped from `raw`, which trace capture keeps); **no `continuous_usage_stats`** (running
usage objects read as "conflicting", so every job would settle unknown); the engine must
send a final usage-only chunk **and** a finish reason; probe that `cache_salt` and `mm_uuids`
are *accepted*, not merely answered with 200; settle the `file://` + allowed-path form for
`storage_ref`; and test a real read-timeout hang, which no mock can show.

**For the coordinator**: removing the `content` alias (the fake emits `{visible, raw}`, the
two exported cases read `raw` and assert `visible`) lets W delete the alias from
`_delta_payload` and the one assertion that reads it, and allows
`PAYLOAD_COPIES_DIVISOR = 3`.

## The two bounds, measured again at this head

The review's numbers reproduce exactly, and the split keeps every event under the ceiling
(one delta of 131,072 code points, sized by the store's rule `len(compact_bytes(payload))`,
ceiling 1,048,576 B):

```
### uv run --frozen python - (independent check, not a test)
ascii    one event unsplit    393252 B   worst split piece  393252 B   limit 1048576
cjk      one event unsplit   1179684 B   worst split piece  786465 B   limit 1048576
emoji    one event unsplit   1572900 B   worst split piece  786468 B   limit 1048576
control  one event unsplit   2359332 B   worst split piece  786456 B   limit 1048576
```

```
### uv run --frozen python - (independent check, not a test)
flood: pulled 2 of 25 chunks (2 MiB of 24), refused with EngineProtocolViolation
slow flood: pulled 5 chunks, clock advanced 120s against a 100s deadline
```

## Commands and results (round 3)

```
### make api-test 2>&1 | tail -2 @ 2026-09-21T17:47:46Z
-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
732 passed, 2 warnings in 90.71s (0:01:30)
EXIT=0

### cd apps/infrx-api && uv run --frozen pytest -q tests/w -s --tb=line 2>&1 | grep -E "conformance|property|passed|failed" @ 2026-09-21T17:49:17Z
engine conformance against VllmEngine: 8 ran, 0 skipped
adapter property: 738 splits over 7 answers
reasoning property: 140960 splits over 21 corpus entries
62 passed in 33.92s
EXIT=0

### cd apps/infrx-api && uv run --frozen python tests/w/mutants.py --list 2>&1 | tail -2 @ 2026-09-21T17:49:53Z

116 mutants over 38 named cases
EXIT=0

### grep -E '/[0-9]+ killed' /tmp/claude-1000/-home-rey-workspace-rey-code-model-inference--claude-worktrees-infrx-impl/7aae6bdd-47a8-4788-aef9-0b8e137e1f2b/scratchpad/mut5.log @ 2026-09-21T17:49:53Z
116/116 killed
EXIT=0

### git diff --stat 8744418..HEAD -- ':!apps/infrx-api/infrx/worker' ':!apps/infrx-api/tests/w' ':!research/plan/evidence/w' @ 2026-09-21T17:49:53Z
EXIT=0

### git status --porcelain @ 2026-09-21T17:49:53Z
EXIT=0
```

Counts, all from the output above: **732 passed** for `make api-test` (round 2: 722; round
1: 710; the recorded baseline at `8744418`: 670), 0 failed, 0 skipped; **62 passed** in
`tests/w`; the exported engine suite **8 ran, 0 skipped** against `VllmEngine`; the filter
property **140960 splits over 21 corpus entries** against an independent oracle; the
adapter-level property **738 splits over 7 answers**; **116 mutants over 38 named cases,
116/116 killed**, 0 survivors, 0 broken runs, 0 misdeclared, plus 10 runner self-tests.
`git status` is clean and `git diff --stat` against the base excluding the owned directories
is empty.

Three mutants were **retired with their reason** in this round rather than forced (r1 R40):
`malformed_usage_becomes_a_zero_count` was already gone, and B7 moved the per-line stall and
cancel checks into `_lines`, where `chunk_checks_skipped` and `chunk_cancel_check_skipped`
kill the same defects - losing only the stall *reason* there is unrepresentable, because the
post-loop `_overdue` re-derives it (measured: that mutant survives). Six anchors had drifted
as the code changed and reported `misdeclared`, which is how they were found: the runner
fails the suite for a mutant it cannot apply.

## Verification log (continued)

- 2026-09-21: Round-3 fixes for the independent review of `987bb41` (B6 event bytes, B7 the
  line buffer, B8's six mutants and the runner's second false-kill channel, and the
  same-pass rulings). `make api-test` 732 passed; `tests/w` 62 passed; engine conformance
  8/8 with 0 skips; 116/116 mutants killed. The review's byte and chunk measurements were
  reproduced at this head before and after the fix. Still fakes only: no engine, GPU,
  container, cloud resource or real service was involved, so nothing here is integrated or
  live-verified.

---

# Round 4 — independent review of `cf7de97` (`fix_required`), fixed at `8d41d78`

B6, B7 and all seven round-2 survivors held, and the implementation survived every attack
the review ran (largest event 786,468 B against 1,048,576 at 131,072 code points in every
script; 87,333 + 18,024 drives with 0 mismatches; a 200 MiB newline-less line pulled 1 of
200 chunks at a 3.0 MiB peak; a full SSE fuzz against the new splitter with 0 mismatches).
What was left was **proof on the code that was new that round** (B9) and **one real
lifecycle defect** in the cancel map (B10). Status unchanged: **implemented**, fakes only.

## B10 — the cancel-intent lifecycle (the defect)

Round 2's rule ("only a finished generation may be evicted, otherwise `False`") made
never-consumed intents immortal, so a full map refused **every** lease - including a
generation running at that moment, which is the one case cancellation exists to serve.
Three ways in, and none of them needs an attacker:

* `cancel(lease)` → `generate(lease, …)` → `await stream.aclose()` leaves an intent **every
  time**: Python runs no code inside a generator that never started, so `_generate`'s
  `finally` could not clear it. 1,024 of those and `cancel` returns `False` for ever.
* A **late** cancel - the ordinary race where the stream finishes just before the
  cancellation arrives - was stored and never removed.
* A cancel for a claimed job that is then never executed, likewise.

The restated ruling is implemented clause by clause: (1) a cancel for a finished generation
is a no-op that returns `True` and stores nothing; (2) `EngineStream.aclose` retires the key
whether or not the generator ever started; (3) a **running** key is never refused, and is
checked *before* the finished memory so a re-run is never mistaken for a completed one; (4)
a pre-start intent expires at its lease's `generation_deadline_at` on the injected clock and
is then evictable; (5) only a map full of live, unexpired, not-running intents answers
`False`. Nothing is dropped for being merely old, and the comment that said so is gone.

## Disposition

| Item | Disposition | Commit | Killing test / mutant |
|---|---|---|---|
| **B10** the five clauses, both reviewer routes | **Fixed** as above. The case walks every clause: 1,224 cancel/generate/close cycles leave the map empty; 1,025 late cancels leave it empty with `finished` bounded; a map full of live intents still accepts and honours a cancel for the stream in flight; intents expire with their leases. | `36916c3` | `…a_cancel_intent_is_never_immortal_and_never_refuses_a_running_lease` · `finished_cancel_is_remembered`, `running_lease_refused`, `running_not_registered`, `pre_start_intents_immortal`, `expiry_ignores_the_clock`, `eviction_takes_a_running_intent`, `aclose_does_not_retire`, `finished_never_remembered`, `finished_unbounded`, `cancel_intents_unbounded`, `retire_keeps_the_intent` |
| **B9.1** incremental UTF-8 decoding | **Proved.** A CJK/emoji delta whose bytes are cut mid-character between chunks; decoding each chunk alone yields U+FFFD. | `ef29564` | `…the_splitter_handles_bytes_not_lines` · `decoder_not_incremental` |
| **B9.2** a final line with no newline | **Proved.** | `ef29564` | same case · `trailing_line_dropped` |
| **B9.3** the pending cap on a legal long line | **Proved.** An 8 KiB `data:` line across three 3 KiB chunks: the pending buffer passes 4 KiB and the line is still legal, because the cap is one journal event. | `ef29564` | same case · `pending_cap_always_4096` |
| **B9.4** the per-**chunk** cancel check | **Proved.** A cancel that lands while a single long line is still arriving, which is the only way to tell per-chunk from per-line: the stream stops before any line completes. | `ef29564` | `…a_cancel_lands_mid_line` · `cancel_checked_only_with_newline` |
| **B9.5/6** the `finished` map | **Proved**: a finished generation is remembered and is the one eviction takes, and the map is a bounded FIFO. | `36916c3` | the B10 case · `finished_never_remembered`, `finished_unbounded` |
| **B9.7** in-prefix traversal | **Fixed and proved.** Every traversal form in the round-2 case was refused for the *wrong* reason (no uuid after `media/`), so the segment rule was never exercised. The case now uses `media/<own org>/../<other org>/…`, `./`, `.hidden`. | `135794d` | `…a_prepared_reference_must_be_one_the_store_could_have_made` · `ref_segment_allows_leading_dot` |
| Quadratic `_lines` | **Fixed**: one `split` per chunk, not a `partition` and a tail re-slice per line (the review measured 25 s of blocked loop for a 1 MiB chunk of bare newlines). `split_passes` counts the passes, so the case asserts one per chunk rather than timing it. | `ef29564` | `…the_splitter_handles_bytes_not_lines` · `lines_split_per_line` |
| BOM-prefixed `data:` | **Fixed**: a line we cannot place is counted, so it can no longer end the stream `completed`; a comment and a blank separator still cannot. The legal SSE field names are named rather than assumed. | `ef29564` | `…junk_and_stray_payloads_are_survived_not_relayed` · `unplaceable_line_not_counted` |
| `$` in the ref pattern | **Fixed**: `$` also matches before a trailing newline, so `media/<org>/v1/source\n` was accepted and forwarded. `fullmatch` now. | `135794d` | same ref case · `storage_ref_matches_a_prefix` |
| The tenant link | **Fixed**: it existed only in `prepared_request`, so a hand-built `PreparedRequest` carrying org A's salt with org B's ref and key reached the engine. `upstream_body` checks that every ref belongs to the salted tenant. | `135794d` | same ref case · `salt_tenant_not_linked` |
| Tiny ceilings | **Fixed**: the per-copy budget was floored at a constant, so below ~140 B the events exceeded the limit (132 B at the case's own 128-byte ceiling). It is `(ceiling − overhead) // copies` now, with the overhead measured, and a ceiling too small to carry one astral code point per copy is refused **at construction**. | `2c1dd4d` | `…an_event_always_fits_the_journal_in_any_script` · `event_overhead_ignored`, `tiny_ceiling_accepted`, `event_bytes_sized_for_ascii` |
| The `_JSON_COST` table | **Deleted.** A process-global memo keyed by character, bounded only by Unicode; the cost is computed from the four cases the encoder itself uses. | `2c1dd4d` | `…one_code_point_costs_what_json_says_it_costs` (every branch and a BMP sweep against `json.dumps`) · `json_escape_cost_ignored`, `short_escapes_cost_six`, `quote_escape_ignored`, `astral_cost_three`, `cost_ignores_utf8_width` |
| `inf`/`NaN` duration | **Fixed**: they reached `budget_kwargs` and surfaced as an `OverflowError` wrapped into `EngineFailure(stage="adapter")`; a typed `unsupported_media` refusal now. | `2c1dd4d` | `…an_unmeasured_prompt_or_duration_is_refused` · `duration_not_finite`, `missing_duration_is_zero` |
| Reported `prompt_tokens` | **Fixed**: 40,000 against a 32,768 context (or 10**30) settled as authoritative `completed` and would have been charged; it is unknown with reason `out_of_range`. | `2c1dd4d` | `…usage_is_authoritative_only_when_the_stream_agrees` · `reported_prompt_unbounded` |
| The close proof | **Strengthened, as the reviewer suggested.** The fake's body is an `httpx.AsyncByteStream` with its own `aclose`, which httpx really calls when the response closes, so `upstream.closed` is now evidence (asserted inside the coroutine) rather than a flag that only flipped at loop shutdown. | `afd9905` | `…a_cancellation_is_scoped_to_its_generation`, `…cancellation_closes_the_upstream_stream` · `response_not_closed`, `inner_generator_not_closed`, `run_does_not_close_its_inner` |

## Corrections to the round-3 appendix

| Claim in round 3 | Correction |
|---|---|
| "a slow flood must stop **inside** its 100 s deadline" | The honest bound is **one chunk interval past** it: the check runs when a chunk arrives, so with 30 s between chunks the stream stops at 120 s against a 100 s deadline. The case asserts `100 ≤ elapsed ≤ 160`, and the overshoot is now a stated limit - W2 owns a task-level deadline. |
| `upstream.closed` as proof of anything | Already corrected in round 3, and now actually **true**: the body is an `AsyncByteStream` whose `aclose` httpx calls. |
| Nothing said about bare CR | **Stated now**: a stream with bare-CR line endings has no `\n`, so nothing parses. Measured at this head: `malformed_lines = 1`, `complete = False`, the iterator raises `EngineIncomplete` and `terminal_cause` is `engine_incomplete` - it fails safe, and a long one is refused by the pending cap. |

## Limits, revised again

Round 1–3 limits stand except as noted, plus:

1. **The deadline overshoot is one chunk interval.** `_overdue` runs when a chunk arrives,
   so a stream whose engine sends a chunk every 30 s stops at 120 s against a 100 s
   deadline. Bounded and stated; W2 owns a task-level deadline for the attempt as a whole.
2. **`cancel` can answer `False`** only when the map is full of live, unexpired, not-running
   intents - which now takes 1,024 *simultaneously pending* cancellations for work that has
   not run and has not expired. W2 treats `False` as "cancel the task yourself", and should
   not call `cancel` for a lease it will not `generate`.
3. **Bare-CR line endings fail safe**, they are not supported: nothing parses, the line is
   counted malformed, and the stream ends `engine_incomplete` (measured at this head) or is
   refused by the pending cap if it is long.
4. **The `storage_ref` grammar is `media/<lowercase org uuid>/<2–4 segments>`**, each segment
   starting alphanumeric over `[A-Za-z0-9._-]`. M owns the layout; this mirrors it.
5. **A ceiling below 76 bytes is refused at construction** (`MIN_JOURNAL_EVENT_BYTES`): the
   adapter cannot fit one astral code point per copy below it, so it says so rather than
   emitting events over the limit.

## Handback additions (round 4)

**W2** gains: treat `cancel() is False` as "cancel the task"; do not call `cancel` for a
lease you will not `generate` (the intent lives until its lease's generation deadline); the
adapter's deadline overshoot is **one chunk interval**, so the task-level deadline is W2's;
`out_of_range` joins the unknown-usage reasons.

**G1 / M2**: `storage_ref` is `media/<lowercase org uuid>/<2–4 segments>`, each segment
leading alphanumeric over `[A-Za-z0-9._-]`, and the adapter refuses anything else at the
last hop; `duration_s` must be **set and finite**; every prepared ref must belong to the
request's organization, which the adapter now checks against the salt as well.

**W3** (unchanged, restated): no `--reasoning-parser`; **no `continuous_usage_stats`**
(running usage objects read as conflicting, so every job would settle unknown); a final
usage-only chunk **and** a finish reason are required; probe that `cache_salt`/`mm_uuids` are
*accepted*, not merely answered 200; settle the `file://` + allowed-path form for
`storage_ref`; test a real read-timeout hang, which no mock can show.

## Commands and results (round 4)

```
### make api-test 2>&1 | tail -2 @ 2026-09-21T18:30:03Z
-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
736 passed, 2 warnings in 42.29s
EXIT=0

### cd apps/infrx-api && uv run --frozen pytest -q tests/w -s --tb=line 2>&1 | grep -E "conformance|property|passed|failed" @ 2026-09-21T18:30:45Z
engine conformance against VllmEngine: 8 ran, 0 skipped
adapter property: 738 splits over 7 answers
reasoning property: 140960 splits over 21 corpus entries
66 passed in 17.10s
EXIT=0

### cd apps/infrx-api && uv run --frozen python tests/w/mutants.py --list 2>&1 | tail -2 @ 2026-09-21T18:31:03Z

141 mutants over 42 named cases
EXIT=0

### grep -E '/[0-9]+ killed' /tmp/claude-1000/-home-rey-workspace-rey-code-model-inference--claude-worktrees-infrx-impl/7aae6bdd-47a8-4788-aef9-0b8e137e1f2b/scratchpad/mut7.log @ 2026-09-21T18:31:03Z
141/141 killed
EXIT=0

### git diff --stat 8744418..HEAD -- ':!apps/infrx-api/infrx/worker' ':!apps/infrx-api/tests/w' ':!research/plan/evidence/w' @ 2026-09-21T18:31:03Z
EXIT=0

### git status --porcelain @ 2026-09-21T18:31:03Z
(the one modified file was the slow-flood wording, committed as the next commit;
 every count above reproduces at the head this section names)
EXIT=0
```

Counts, all from the output above: **736 passed** for `make api-test` (round 3: 732; round
2: 722; round 1: 710; baseline at `8744418`: 670), 0 failed, 0 skipped; **66 passed** in
`tests/w`; the exported engine suite **8 ran, 0 skipped**; the filter property **140960
splits over 21 corpus entries** against an independent oracle; the adapter property **738
splits over 7 answers**; **141 mutants over 42 named cases, 141/141 killed**, 0 survivors, 0
broken runs, 0 misdeclared, plus 10 runner self-tests. `git diff --stat` against the base
excluding the owned directories is empty.

Six anchors drifted as this round's code changed and reported `misdeclared`, and two
mutants survived on their first run - `mixed_org_media_accepted` (the new salt-tenant check
refuses the same request first, so the case now also drives it with no salt) and
`cancel_key_ignores_the_generation` (`cancel` gained an identical `key = (job, generation)`
line, so the single-replacement runner was editing the wrong one). Both are fixed in
`ed31fe6`; the runner failing on a mutant it cannot apply is what surfaced them.

## Verification log (continued)

- 2026-09-21: Round-4 fixes for the independent review of `cf7de97`: the cancel-intent
  lifecycle (B10) by the restated five-clause ruling, proof for the seven B9 mutants on the
  code that was new in round 3, and the same-pass items (tiny ceilings, `fullmatch`, the
  linear splitter, the arithmetic cost, finite durations, out-of-range reported prompt
  tokens, the tenant link in `upstream_body`, a counted BOM line). `make api-test` 736
  passed; `tests/w` 66 passed; conformance 8/8 with 0 skips; 141/141 mutants killed. Still
  fakes only: no engine, GPU, container, cloud resource or real service was involved, so
  nothing here is integrated or live-verified.

---

# Round 5 — independent review of `06f7419` (`fix_required`), fixed at `f24a0ef`

Every clause of the cancel lifecycle, the splitter proofs, the reference grammar, the
ceilings, the arithmetic JSON cost (0 mismatches over all 1,112,064 code points) and both
property runs held. Two items remained. Status unchanged: **implemented**, fakes only - no
engine, GPU, container, service or cloud resource.

## Disposition

| Item | Disposition | Commit | Killing test / mutant |
|---|---|---|---|
| **B11** a cancel lost when the same `(job, generation)` is generated twice | **Fixed.** The intent map is retired per generation, so a cancel arriving *between* two runs of one key was answered `True` as a no-op ("it is over", clause 1) and the second run ignored it. Both reviewer sequences did it: attempt 1 failing mid-stream then attempt 2 on the same lease, and `generate` → `aclose` before the first `__anext__` → `generate` again. R46 makes attempts per generation and W2 never reuses a lease, so **`generate` refuses a key it has already run**. The code is **`state_conflict`** (`errors.StateConflict`, the existing 409 conflict code): reusing a lease is a caller bug, not the engine misbehaving, so it is a `DomainError` raised before anything is sent - consistent with every other pre-flight refusal. | `62c0739` | `…a_generation_runs_only_once` (both sequences, each asserting the outcome *code* and that no second request was sent) · mutant `finished_key_generates_again` |
| **B12** whole frames plus a partial tail in one chunk | **Fixed (proof).** `pending = lines.pop()` had no case for the ordinary shape of a TCP read - complete frames followed by the *beginning* of the next (`data: …\n\ndata: {"choi`); the old case only cut inside a line with no newline before it in the same chunk, so both a splitter that dropped the tail and one that handed it on as a complete line passed. | `62c0739` | `…a_chunk_of_whole_frames_plus_a_partial_tail` (three deltas reassembled across three chunks, each carrying a tail) · mutants `partial_tail_discarded`, `partial_tail_yielded_as_a_line` |
| `two_usage_events` dropped from the list | **Restored.** It went in `ed31fe6` without a retirement note while the appendix cited it; `repeated_usage` kills it. | `62c0739` | `…usage_is_authoritative_only_when_the_stream_agrees` · `two_usage_events` |
| `event:`/`id:`/`retry:` | **Pinned** as legal SSE fields, not junk: only a line we cannot place at all is counted. | `62c0739` | `…junk_and_stray_payloads_are_survived_not_relayed` · `legal_sse_fields_counted` |
| Intent expiry at the exact instant | **Pinned** (`>=`). | `62c0739` | the lifecycle case · `expiry_boundary_exclusive` |
| `prompt_tokens == context` | **Pinned** as authoritative (the refusal is `>`, not `>=`). | `62c0739` | `…usage_is_authoritative_only_when_the_stream_agrees` · `prompt_boundary_exclusive` |
| `PAYLOAD_OVERHEAD_BYTES` | **Documented with the measured value, which is 36 bytes, not the 35 the review quoted**: `len(compact_bytes({"content":"","raw":"","visible":""})) == 36` at this head. A case asserts the constant still covers the real payload, so the comment cannot drift. | `62c0739` | `…an_event_always_fits_the_journal_in_any_script` · `payload_overhead_understated` |
| `duration_s <= 0` | **Refused** (`unsupported_media`): a negative duration quietly produced the *minimum* frame budget, so a two-minute clip could be sampled as four frames and answered about. | `62c0739` | `…an_unmeasured_prompt_or_duration_is_refused` · `non_positive_duration_accepted` |

One mutant was **retired with its reason**: `eviction_takes_a_running_intent` guarded
`_evict_spent` against dropping the intent of a *running* generation, which needed a key that
was both `finished` and running. B11 makes that unreachable (a generation runs once), and an
*expired* running key is stopped by the stall check before the cancel check, so the guard
became dead code and was deleted with the mutant. What protects a live intent is the expiry,
which keeps its own two mutants (`pre_start_intents_immortal`, `expiry_boundary_exclusive`).

## Handback additions (round 5)

**W2** gains two rules:

* **Never reuse a lease.** One generation is one attempt (R46); the adapter refuses a second
  `generate` for a key it has already run with `state_conflict`, because the cancellation it
  remembers for that key has already been retired. A retry is a new generation from the store.
* **A cancel between `generate()` and the first `__anext__` can be refused** when the intent
  map is full: the key only counts as *running* once the generator starts, so in that window
  clause 3 does not protect it. `cancel() is False` means "cancel the task yourself", which
  W2 can always do - it owns the task.

## Limits (round 5)

1. **B11's refusal is defence in depth, and it is bounded.** The memory of a generation that
   has run is the same 1,024-entry FIFO (`finished`), so a lease reused after 1,024 *later*
   generations would be accepted again. R46 makes reuse a caller bug in the first place; this
   catches the mistake while it is fresh, which is when the lost-cancellation window exists.
2. **A cancel in the `generate()`-to-first-`__anext__` window can be refused** on a full map
   (limit 2 of round 4 with the window named). W2 cancels the task instead.
3. Every earlier limit stands, including the one-chunk-interval deadline overshoot and
   "closing the response is not proof that vLLM stopped" (Layer 3, W3).

## Commands and results (round 5)

```
### make api-test @ 2026-09-21T20:05Z
-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
738 passed, 2 warnings in 96.37s (0:01:36)
EXIT=0

### uv run --frozen pytest -q tests/w/test_engine.py tests/w/test_reasoning.py -s @ 2026-09-21T20:32Z
engine conformance against VllmEngine: 8 ran, 0 skipped
adapter property: 738 splits over 7 answers
reasoning property: 140960 splits over 21 corpus entries
50 passed in 3.04s
EXIT=0

### uv run --frozen python tests/w/mutants.py --list @ 2026-09-21T20:33Z
149 mutants over 44 named cases
EXIT=0
### uv run --frozen python tests/w/mutants.py @ 2026-09-21T20:36Z (whole list, one pytest process each)
[killed       ] body_sends_the_model_revision: 1 failed, 49 deselected in 1.02s via ['AssertionError']
… 147 more [killed] lines, 149 in all …
[killed       ] delimiter_case_insensitive: 2 failed, 48 deselected in 1.33s via ['AssertionError']

149/149 killed
EXIT=0

### git diff --stat 8744418..HEAD -- ':!apps/infrx-api/infrx/worker' ':!apps/infrx-api/tests/w' ':!research/plan/evidence/w' @ 2026-09-21T20:58Z
EXIT=0

### git status --porcelain @ 2026-09-21T20:58Z
EXIT=0
```

Counts, all from the output above: **738 passed** for `make api-test` (round 4: 736; round
3: 732; round 2: 722; round 1: 710; baseline at `8744418`: 670), 0 failed, 0 skipped; **50
passed** in the two focused files (68 including the mutation suite); the exported engine
suite **8 ran, 0 skipped**; the filter property **140960 splits over 21 corpus entries**
against an independent oracle; the adapter property **738 splits over 7 answers**; **149
mutants over 44 named cases, 149/149 killed**, 0 survivors, 0 broken runs, 0 misdeclared,
plus 10 runner self-tests. `git status` is clean and `git diff --stat` against the base
excluding the owned directories is empty.

A first full run of the list reported `146/149` with three `misdeclared`: B11's refusal and
the `duration_s <= 0` clause moved the anchors of `cancel_key_ignores_the_generation`,
`duration_not_finite` and `missing_duration_is_zero` while that run was in flight. All three
are repointed in `f24a0ef` and the run above is the clean one at this head - the runner
failing on a mutant it cannot apply is what surfaced them, which is the behaviour R32 asks
for.

## Verification log (continued)

- 2026-09-21: Round-5 fixes for the independent review of `06f7419`: a generation runs once
  (B11, `state_conflict`, so a cancellation can no longer be lost between two runs of one
  key), proof for the splitter's whole-frames-plus-partial-tail path (B12), and the same-pass
  items (`two_usage_events` restored, the legal SSE fields, the expiry and prompt-count
  boundaries, the measured 36-byte payload overhead, a non-positive duration refused).
  `make api-test` 738 passed; the focused files 50 passed; conformance 8/8 with 0 skips;
  149/149 mutants killed. Still fakes only: no engine, GPU, container, cloud resource or real
  service was involved, so nothing here is integrated or live-verified.
