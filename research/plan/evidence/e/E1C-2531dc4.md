# E1C — upload client repair, resumable dataset, load validity (track E)

## Task and status

- **Task:** E1C "Repair upload client and deliver valid resumable dataset/load measurement"
  ([tasks.json](../../tasks.json), brief [consumer-v1/05](../../consumer-v1/05-client-and-load-testing.md)
  §1–§3, §6, §7). Closes the client halves of [RV-07 and RV-08](../../21-v1-consumer-readiness-review-2026-09-24.md).
- **Status: implemented and verified locally. Still to do: the hosted smoke (the coordinator runs
  it, command below) and the UPLOAD-RESTART cell. That cell waits for M5's durable uploads.**
  Nothing was deployed. No hosted Supabase, AWS, pilot box, GPU, Docker service or paid call
  was used. Every request in this lane went to an in-process fake gateway or to the REAL
  upload router mounted in-process (`httpx.ASGITransport`).
- **Oracles:** BENCH-VALIDITY and LOAD-CLOSEDLOOP (client half). UPLOAD-RESTART is pending
  M5.
- **Owner/session:** Claude Opus 5.5 implementer, worktree `.claude/worktrees/codex-e1c-client`.

## Source

| Field | Value |
|---|---|
| Base SHA | `dff31efc` (main) |
| Implementation head | **`2531dc49`** (this report's commit sits on top) |
| Branch | `codex/e1c-client` |
| Coordinator patch applied | `547de3e8` = coordinator `bfb3a8af` (contracts/tasklocal ports; reserved `infrx-e1c-postgres` 55449, unused here) |

Commits: `7e4fbe47` E1C.1 upload conformance · `0eaa4d2e` E1C.2 dataset · `b756966a` E1C.3
validity · `a54e55e5` E1C.4 profile + §6 · `e540abdd` mutant-run fixes · `2531dc49` SIGINT
disposition fix.

## Changed paths (all owned)

`models/marlin2b/bench.py`; new `models/marlin2b/dataset.py`, `runprofile.py`,
`profiles/run-profile.v1.schema.json`, `profiles/P0-hosted-smoke.template.json`,
`profiles/smoke-corpus.json`; `models/marlin2b/tests/{fake_gateway,mutants,test_bench}.py`;
new `tests/test_{upload_conformance,dataset,validity,profile}.py`. No existing file under
`results/` was touched. `apps/infrx-api` was not changed apart from the coordinator's patch.

## What each slice delivers

1. **Upload conformance (RV-07).** `bench.upload` now uses the mounted contract:
   - create with `max_bytes/bytes/accepted_mime/digest` (`sha256:`), answered 201
     `upload_handle`, and `destination_ref` must equal `infrx-upload:<handle>`;
   - an authenticated PUT to `{--base-url}/uploads/{handle}`, whose URL is built from the
     base URL and the allowlisted handle and never from the answer. A planted `url`, or
     planted headers, reach no one;
   - an empty completion, whose handle, bytes and digest must match ours.

   The fake gateway speaks the same contract. `test_the_fake_gateway_answers_the_upload_probes_like_the_real_router`
   holds it to G4U's router probe by probe (status, error code and answer keys), so the fake
   suite can no longer pass an incompatible client.
2. **Dataset recipe** (`dataset.py run|export`). The JSONL manifest is streamed through a
   bounded queue. A stable id maps to the SOP `item_key` and then to the `Idempotency-Key`. A
   SQLite journal records state `sent` **before** the request leaves. Upload and inference
   concurrency are bounded separately.
   - A staged handle is re-staged only if the item was never sent.
   - `result_expired` is terminal and is never re-billed automatically.
   - An item that changed after it was sent is refused. It is never re-keyed.
   - A resume under another model, base URL or dataset version is refused.
   - Output retention is declared (`--retain-output text|digest`).
   - The results export records whether each answer was fresh or a replay, plus its usage.
     The failures export is taken back by `run --only` as an exact subset.
3. **Validity (RV-08).**
   - Each row records `resend` (first, transport_retry or resume), `served` (fresh or replay),
     `served_model` and upload start/end stamps.
   - Capacity numbers use **fresh answers only**. `accepted` keeps its old meaning; replays
     are counted separately and have their own denominator.
   - `validity()` marks a cell INVALID for any of these reasons:
     - an unexpected replay (a resume excuses only its own resumed keys);
     - missing attempts or an interrupted run;
     - open-loop driver lag above `--max-driver-lag` (default 1 s);
     - a served model that differs from the declared one;
     - counters that do not reconcile;
     - no profile (`unprofiled`).
   - The summary also carries `parser_version` `e1c.1`, `stages_s` and `output_lengths`
     (see below).
   - `--validate-raw` re-derives an old raw file read-only.
4. **Profile + `--validate-only`.**
   - Versioned schema `infrx.run-profile/1`, covering every §1 group. It also carries the
     coordinator's additions: `target.path`, `target.test_key_ids` and `measurement.profile_class`.
   - `runprofile.py` interprets the schema subset and cross-checks the profile against the
     run's own flags and schedule. It checks:
     - the host allowlist;
     - the manifest digest and the item ids;
     - that every over-cap item is declared in `expected_invalid`;
     - every bound;
     - projected spend, including outstanding holds;
     - that no value has a secret's shape. Offending fields are reported by path only.
   - Missing rates, holds or the key inventory block a **paid** run but not a local fixture.
   - `--profile` refuses to start unless the profile is runnable. At run time it then enforces
     `max_duration_s` and `max_drain_s`.
   - The summary's `measurement` block carries the §6 fields with named denominators. Cost per
     successful video-hour is `unavailable`, or `estimated (source, date)`, whenever the price
     is not sourced from `cloud-pricing.md`.
5. **Coordinator update 1 (S3 F5, F8/P-24).**
   - A P4 profile must set `target.path = public-edge`.
   - A P4 cell that saw no 429 or 503 is INVALID.
   - `measurement.overload_by_target_path` reports 429 and 503 counts, `Retry-After` presence
     and transport losses by path.
   - `--key-inventory` takes the sanitized list of active key-id prefixes. It refuses the run
     when any active key is outside `target.test_key_ids`. Without an inventory, a paid run is
     blocked.

## Commands, exit codes, counts

| Command | Exit | Result |
|---|---|---|
| `make api-env` | 0 | pinned venv created in the worktree |
| `make bench-test` at base `dff31efc` | 0 | **68 passed** |
| `make bench-test` at `2531dc49` | 0 | **96 passed**. New: conformance 4, dataset 6, validity 8, profile 10 |
| `apps/infrx-api/.venv/bin/python -m pytest -q tests/integration/backend/test_certify.py` | 0 | 38 passed (certify's `bench` imports still work) |
| `apps/infrx-api/.venv/bin/python models/marlin2b/tests/mutants.py` (detached) | 0 | **80 mutants: 77 killed, 3 controls survived, 0 problems**. 37 are new E1C mutants (`e1cm*` upload, `e1cd*` dataset, `e1cv*` validity, `e1cp*` profile) |
| `bench.py --validate-raw` over the committed E1B raw files | read-only | bda1586 L2-r0.5/L2-r1.0/L3 **VALID**. 4226315 L2-r0.5/L2-r1.0/L3/L5 **INVALID** (57/111/105/57 unexpected replays, as RV-08 found). Outcome counts equal the stored summaries |

## Tests that failed first, then passed

- The real-router conformance case failed on the old client with `UploadFailed`,
  `upload_status 400` at create (the RV-07 reproduction). The fake drift case failed on the
  old fake with `KeyError: 'upload_handle'`. Both pass at `7e4fbe47`.
- `test_rejections_and_failures_are_counted_apart_from_accepted` failed at E1C.3 because its
  exact denominator dict lacked the new `replayed_excluded` bucket. The expectation was
  updated.
- Mutant-run findings, all fixed in `e540abdd` and `2531dc49`:
  - e1bm02 and r19 survived: the new fake 404s a bad handle at PUT, which hid the client's own
    allowlist. The handle cases now assert that the handle never goes back out in a path.
  - e1bm14 was stale: its text changed with the fresh-only rewrite.
  - The interrupt case asserted an upload count, which a SIGINT during staging legitimately
    raises (an orphan upload). It now asserts one reference per key.
  - Control e1bc01 was reported killed because a detached process starts with SIGINT
    ignored. The case now sets the terminal's disposition.

## Hosted smoke (for the coordinator; not run here)

This is the P0 smoke through the public edge: one in-cap clip (`c015`, 5 s) and the explicit
over-cap control (`c051`, 112 s > 82 s), each sent once as `upload` and once as `video_b64`.
The key comes from the environment only. Use a fresh `--dataset-version` for every cell.

```bash
export INFRX_API_KEY=...        # the certify/E1B consumer key; never on argv
R=bda1586; TS=$(date -u +%Y%m%dT%H%MZ); OUT=models/marlin2b/results/E1C-smoke-$R
python models/marlin2b/bench.py \
  --corpus models/marlin2b/profiles/smoke-corpus.json --subset full \
  --target gateway --base-url https://marlin2b.callbill.ai/v1 \
  --model nemostation/marlin-2b@2026-09-01 \
  --forms upload,upload,video_b64,video_b64 --requests 4 -c 1 \
  --max-tokens 128 --retries 0 --seed 20260922 \
  --dataset-version e1c-smoke-$TS --label e1c-smoke-$TS \
  --out $OUT/bench.jsonl --raw $OUT/raw/smoke-$TS.jsonl
python models/marlin2b/bench.py --validate-raw $OUT/raw/smoke-$TS.jsonl --expect-model nemostation/marlin-2b@2026-09-01
```

**Expected result:**

| Clip | `upload` | `video_b64` |
|---|---|---|
| c015 (in-cap) | 200 accepted, `served: fresh`, `upload_stage: finalized` | 200 accepted, `served: fresh` |
| c051 (over-cap) | upload `finalized`, then chat 400 `unsupported_media` | 400 `unsupported_media` |

`replayed` must be 0. The summary verdict reads `INVALID: unprofiled`, which is correct:
P0 is not a capacity claim.

**If create answers 404 `not_found`:** the release carries no media store, so the upload
routes are not mounted. Record the `upload` cells as BLOCKED on G7/M5 composition, not as a
client failure.

**Optional profiled variant:**

1. Fill the `FILL:` fields of `profiles/P0-hosted-smoke.template.json`, set
   `outstanding_holds_usd`, and set `workload.dataset_version` to the value passed on argv.
2. Write the key inventory from the read-only op:
   `{"active_key_id_prefixes": [...], "taken_at": ..., "source": ...}`.
3. Run once with `--validate-only`. It must exit 0.
4. Run the same command with `--profile <filled> --key-inventory <inv.json>`.

Run the smoke **after** the D10/M5/G7 changes that the coordinator deploys, and keep the
existing E1B cells untouched.

## Wiring requests (not applied)

1. **E2C/E4C: `tests/integration/backend/certify.py` `bench_argv`.** Pass
   `--profile <cell-profile.json> --key-inventory <inv.json>` for every bench cell, with a
   per-cell `--dataset-version`. Then read `summary["validity"]["verdict"]` and
   `summary["measurement"]` into the cell verdict, so that an INVALID cell can never PASS.
   - Exact patch: `argv += ["--profile", str(profile), "--key-inventory", str(inventory)]`
     before `return argv + list(extra)`.
   - `rung_verdicts()` gains
     `("bench_validity", decide.FAIL if s["validity"]["verdict"] != "VALID" else decide.PASS, s["validity"]["reasons"], "BOX")`.
   - Test: an existing certify case fed a summary whose validity is INVALID must summarise to
     FAIL.
2. **Follow-up flip (E1C, one line, after request 1 lands).** Make `--profile` mandatory for
   a non-local gateway target in `bench._run`. Today an unprofiled run still starts and is
   only labelled INVALID/unprofiled. This keeps certify runnable until its runner passes
   profiles.
3. **Coordinator: append to `models/marlin2b/results/E1B-protocol.md` §5** (not edited here;
   it lives under `results/`):
   - "9. any replay outside a declared resume (bench parser `e1c.1` reports INVALID);
   - 10. a cell without its own `--dataset-version` (cells sharing an identity replay each
     other: the 4226315 L2/L3/L5 cells);
   - 11. a P4 overload cell that saw no 429/503, or that did not enter through the public edge
     (S3 F5)."

## Proposed rulings (next free R109)

- **R109 (benchmark validity):**
  - A capacity summary counts fresh inference only.
  - A replay not belonging to a declared resume of the same keys makes the cell INVALID. So do
    missing attempts, driver lag above the declared bound, a served identity other than the
    declared one, and unreconciled counters.
  - A qualifying run carries a validated `infrx.run-profile/1`: target path, bounds including
    outstanding holds, identity, and a key inventory.
  - Derived verdicts name their parser version. Raw rows are never reclassified.
- **R110 (for M5/D10 to decide):** does a validation refusal of a chat request (400/404 on an
  unknown or expired upload handle) consume its Idempotency-Key?
  - If it does not, a client may re-stage a handle lost to a gateway restart and re-send under
    the same key.
  - If it does, the item needs an explicit re-key.
  - Today `dataset.py` quarantines such an item. That is conservative: it never re-bills.

## Open issues

- **UPLOAD-RESTART** (create/PUT/finalize/resolve across gateway processes) needs M5's durable
  uploads.
  - Read-only check at 22:05Z: `codex/m5-uploads` has no commits beyond the port patch.
  - The in-memory store still loses handles on restart (RV-02).
  - Once M5 lands, add a conformance case that restarts the router over M5's store between
    PUT and complete.
- **F2C upload DTO fixtures:** `codex/f2c-lifecycle` has no commits beyond base (read-only
  `git log`, 22:05Z). The client is pinned to `wire.UploadCreated`/`UploadCompleted` and the
  mounted router at `dff31efc`. Rerun `test_upload_conformance.py` once F2C-L lands.
- **Served-build identity** (deployed sha, image digest) is declared in the profile but
  checked only through the served model in the stream. The build check stays with certify's
  `/metrics` probe (E4C).
- Stages come from one monotonic clock in one process. There is no cross-host clock-sync
  bound. `fetch` and `reconciliation` are listed as not measured, because the answers are
  streamed.
- `dataset.py` uses streamed chat with idempotent replay, not `/v1/jobs`. Moving to async jobs
  would add the result-fetch stage and read result expiry through GET.
- Ledger charges (CREDIT/USD) are not observed by the client. The reconciliation stays with
  E3C/E4C.

## Remaining-effort estimate

| Optimistic | Likely | Pessimistic | Confidence | Basis |
|---|---|---|---|---|
| 2 h | 4 h | 8 h | med | Remaining work: the UPLOAD-RESTART case over M5's store once it lands (1–3 h); analysing the hosted smoke plus any fix loop (0.5–2 h); review rounds (1–3 h). The pessimistic case assumes M5 changes the upload contract, or R110 requires a re-key path. |

## Verification log

- 2026-09-24 22:10Z (E1C): implemented at `2531dc49`. The counts above were re-run at that
  head. No hosted, cloud or paid operation was performed.
