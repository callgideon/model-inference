# E3B phase 2 — real-store backend gate (track E)

## Task and status

- **Task:** E3B phase 2 (track E), `.claude/handoff/wave3/E3B.md` items 0–9. Binding scope is
  [18 §E3B](../../18-marlin-backend-first.md) slices a/b/c plus the handback of
  [E3B-a7cc8f7.md](E3B-a7cc8f7.md) (left untouched).
- **Status: implemented. The gate is NOT passed.** On this base `run.py --layer 3` is
  **FAIL**, not PENDING. Every backend-stage failure is in I3B's read-only
  `tests/integration/backend/recovery/**`: merge interactions with I2B, W3 and D2, sent as
  integration request #2. No E3B-owned case fails. The gate run at the final implementation SHA `e57b820` exits **1**. Its backend stage is `{passed 87, pending 23, failed 11}`, and all 11 failures are I3B's. The runs are quoted below.
- **Round 2 (review fix round, `E3B2-review-80cef22.json`):** every finding is answered in
  §Round 2 below. The gate at
  **`f883a0a`** exits **1**, and only on I3B's reds: backend `{passed 98, pending 17, failed 11}`
  (all 11 are I3B's), `make api-test` `3120 passed` with no skip, rls 696, and mutants 174 killed
  of 191 (the 13 problems are I3B's). No E3B-owned case fails; E3B's pending ids are D5, G2 and G3
  only.
- **Oracles:** BACKEND-JOURNEY (pending), DUR-ADMIT, DUR-CAP, DUR-FENCE, DUR-OUTPUT (pending
  D4), DUR-SETTLE (pending D5), DUR-OUTBOX, DUR-RLS, MEDIA-SEC, API-MODES, API-STREAM
  (pending), API-OPS, CREDIT-SPEND.
- Nothing was deployed. No hosted Supabase, AWS, pilot box, GPU or paid call was used. This
  lane ran only `infrx-e3b2-*` and `infrx-e3b2rest-postgrest`, on 127.0.0.1:56700–56799. The
  shared D harness was used only through `make api-test` / `tests/d`, under its own lock.
- **Owner/session:** Claude Opus 5.5 implementation session, worktree
  `.claude/worktrees/codex-e3b2`.

## Source

| Field | Value |
|---|---|
| Base SHA | `740bebf8ad713a1ac0b9d47701c9278f01cbdeef` (`origin/claude/backend-impl`; contains `9c1c6ed` "TASK_PORTS registers d4 … e3b2 namespace block") |
| Implementation SHA | **`e57b820`** (the head is this report's commit on top) |
| Branch / worktree | `codex/e3b-phase2-gate` in `.claude/worktrees/codex-e3b2` |
| Integrated SHA | none (coordinator) |

Merged at base, per tasks.json / the brief: D1R, D2, D3, G1R, G6B, M3, Q3, W3, A1, I2B, I3B, M4,
W2, S2M, E2R, the F2P additive half. **Not merged:** the F2P wire-in, D4, D5, G2, G3 and G4U.
D4, G2 and G4U were in flight during this session.

## Items

Each commit names its killing case and its single-edit mutant or live drill. Items 1 and 2
share one commit, because the probe and the real rig are the same `rig()` function.

| # | Commit | What | Killing case ← mutant / live drill | Status |
|---|---|---|---|---|
| 0 | `2107a4c` | `INFRX_E2_NAMESPACE` selects the namespace. `harness.E2_PORTS` + `NAMESPACES` (e2 +0, e3b2 +1200) derive every name, port, database, Valkey prefix and S3 bucket. `compose.yaml` and `backend/compose.yaml` take the project, ports and database from the harness env and refuse without them (`:?`). PostgREST's project is `infrx-e3b` by default and `infrx-e3b2rest` otherwise, so it never falls under `harness.PREFIX`. Also derived: the decoy label in `test_services`, the SIGTERM marker in `test_run` and the temp prefix in `mutants`. The CLI port default in `fake_vllm` was derived too, then reverted in `e57b820` (see item 9). Unset, every value is E2's own | `test_harness::test_the_compose_file_publishes_exactly_those_ports_on_loopback[e3b2]` ← **e3bm13** (55532 hard-coded back in compose.yaml). e2m10 still killed | implemented; the tasklocal row for e3b2 is strict-xfail until IR #1 lands |
| 1 | `b756f14` | (a) Per-drill probe: `rig(backend, *rpcs)` pends only while one of the drill's own functions is an `infrx.unimplemented` stub, and pends on the task that stub names (`stack.stubbed`/`stub_owners`, measured on the stack). (b) `stack.PENDING` no longer names any merged task as an E3B blocker. It gains F2P ("wire-in merged (CreditJobStore port + credit fake)"), and D4/D5 are re-scoped. Journeys: D5+F2P on every cell; sync G2; SSE G2+D4; async G2+G3; upload G4U. dr11 pends on G2. (c) `RESIDUAL` keeps G1R/D2/D3/M3/W3, each with a reason, only for I3B's read-only cases. `stack.pending` refuses those ids for E3B cases | `test_stage::test_a_drill_pends_only_on_the_stubs_it_drives` ← **e3bm14** (global count); `dr05[postgres]` fails by name ("no PostgreSQL StreamStore … D4") ← **e3bm15** (probe ignores stubs); `test_stage::test_no_pending_id_names_a_merged_task_unless_it_is_a_named_residual` ← **e3bm16** | implemented |
| 2 | `b756f14` | `stack.pg_jobstore()` = `pgtesting.make_jobstore_factory` on this stack. Template `infrx_e3b2_tmpl` = every `pgstate.migration_files()` + test clock + `pgtesting.seed` + the PROVISIONAL Marlin seed (P-01). Cloned per call as `infrx_e3b2_<pid>_<n>` and dropped at exit. dr01/dr02/dr03/dr04/dr09 `[postgres]` **pass with unchanged bodies**. Live drills on the case's own clone: **db06** removes the `prepare_dispatch` insert from `admission_rows`, and dr01's own assertion reports it; **db07** removes the generation check from `fence_lease`, and generation 1's heartbeat is accepted after dr04's requeue | `dr01[postgres]` ← **e3bm17** (`infrx/state/jobstore.py` admission drops `replayed`); db06, db07 (live) | **integrated on the local real store** for dr01–dr04, dr09, db06, db07 |
| 3 | `d1ef74a` | CREDIT on the real store: `admit_credit` over the PROVISIONAL seed, with individuals from `auth.users` plus A1's `claim_signup_grant`. The template gains GoTrue's `email_confirmed_at`/`deleted_at`, which hosted has and the pinned image lacks. **dr01c**: replay gives the same job and one held hold on the individual's CREDIT wallet; pins = alias + provisional card label; legacy USD unmoved; conservation per unit (R73). **dr02c**: private dev deployment and D2 MC-1's provider_dev key → not_found; revoked → invalid_api_key; full key → capacity_exhausted; unpriced listing → invalid_request. Each leaves the footprint unchanged. The `[fake]` twins pend on F2P. **dr07c** (CREDIT settlement) pends on D5 through the probe | `dr01c[postgres]` ← live drill **db08** (`admit_credit` writes its hold as a legacy USD hold) | integrated (local real store) for admission; settlement pending D5; fake twin pending F2P |
| 4 | `6945bf7` | dr13 runs through `Reconciler(store=PgJobStore, index=ValkeyScheduler(this Valkey), now=db clock, worker_id=unique)`. The four dispatch rows are drained and acknowledged, and one job is cancelled. After a Valkey SIGKILL (depth 0), `rebuild() == 3`. The next `drain()` reads 0, because the rows were acknowledged before the loss: the acked-then-rebuilt hole, closed by the rebuild alone. The three dispatch once each and the terminal job never | dr13 ← **e3bm18** (rebuild skipped), **e3bm19** (rebuild from the pre-ack snapshot) | integrated (local real stores) |
| 5 | `349c691` | dr03/dr04 reap through W3's `WorkerService.reap_once()`, embedded (`health_port=None`) with W's `engine_factory` double. The events are read back from the worker's index. dr04: `reaped == 1`, one requeue (attempt 1), and on the real store its id is the **new** `inference_dispatch` row (FID-5). dr03: the reaper ran cleanly and enqueued nothing itself; preparation's redispatch is an outbox row the relay delivers | `dr04[postgres]` ← **e3bm20** (`reap_once` does not enqueue) | integrated (local real store); fake finding → IR #3 |
| 6 | `f93815a` | `provision_two_tenants()` goes through G6B's `Operations`: `grant_initial` (idempotency key) → `issue_key` → `tenant(secret)` balance / resolved wallet / `quote()` pins. Order note: `issue_key` refuses before a metered wallet exists, so the grant must come first. Real: `PgSignup` (IdentityDirectory + Ledger.grant_initial), a test-local `credit_wallets` read as WalletDirectory, PgJobStore. Fake: G6B's own `tests/g/ops/fakes.py` TenantStore/AuditLog/Registry/AccountView, and v2 CatalogDirectory (D5). `provisioned_by` names both sets. Cases: 10000/0 balance read back; the other tenant's wallet Forbidden; operator key → no wallet; a replayed grant under another idempotency key is the same grant (R71); no secret in logs, reprs or accepted argv | provisioning case ← live drill **db09** (the grant's four uniqueness layers dropped) | implemented: identity, grant and wallet on the real store; keys, audit and catalog on fakes |
| 7 | `21c427e` | `pgstate.RELATIONS` (62) and `FUNCTIONS` (88): every relation and SECURITY DEFINER function of `infrx`/`public` at 0016, with measured access. `access_rows()` adds a row per anon/authenticated/service_role: 450 rows, every 42501 qualified. The `rls` stage now runs 490 cases. `test_services` adds the completeness case against `pg_class`/`pg_proc` (missing and stale both fail) and a live grant inversion in a rolled-back transaction | completeness ← **e3bm21** (the `infrx.jobs` row deleted); inversion rows (live) | integrated (local real schema) |
| 8 | `a3722b0` | `mutants.all_mutants()` = E's list + `mutants_i3b.MUTANTS`. `run.mutation()` and the CLI iterate it, and `OWNED_TREES` gains `infra` | `test_run::test_the_mutation_stage_runs_every_list_through_one_runner` ← **e3bm22**, **e3bm23** | implemented; I3B's own reds surface (IR #2) |
| 9 | `03d124c`, `0843485`, `f0de778`, `e57b820` | The gate run and the merged-tree checks, plus two harness fixes the runs forced. (a) `0843485`: in the first run, `make api-test` outlived the 1800 s suite budget. `TimeoutExpired` escaped `shell()` and the run died before writing its report. `shell()` now returns exit 124 with the captured output, and the make targets get 3600 s. (b) `f0de778`: E2R's `test_the_shared_clock_moves_…` had been red at base on every stack. It asserted that `infrx_test.advance()` returns the pre-move clock, but D2 (`d6b3823`) made it return the moved clock. The probe now reports `advance_return_lag_s`, and the vacuous e2m18 kill is real again. (c) `e57b820`: item 0 had made `fake_vllm`'s CLI `--port` default import the harness. W3's mutant runner copies `fake_vllm.py` without `harness.py`, and `make check` reported 5 W3 mutants as `broken_runner`. The default is the literal 55580 again (the harness always passes `--port`), and `INFRX_MUTANTS=all pytest tests/w/test_w3_mutants.py` → `84 passed`. `03d124c` only wraps lines | `test_run::test_a_suite_that_outlives_its_budget_is_a_failed_run_not_a_traceback` ← **e3bm24**; the shared-clock case ← **e3bm25** and e2m18 | see Results |

## Requirement coverage (new or changed cases)

| Case | Oracle | Exact invariant | Store |
|---|---|---|---|
| `dr01[postgres]` | DUR-ADMIT | A crash after commit, then the idempotent retry, gives the same job (`replayed`): one job, one hold = `maximum_hold`, exactly one `prepare_dispatch`, and conservation holds | **real** PgJobStore |
| `dr02[postgres]` | DUR-ADMIT/CAP | Revoked key, unpriced model and full key each refuse typed, and leave no outbox row, no hold on the other org and only the kept job's journal reservation | real |
| `dr03[postgres]` | DUR-FENCE | Reaped through `WorkerService.reap_once`: preparation is redispatched as a second `prepare_dispatch`, the dead lease's `prepared` gets `StaleLease`, and the job is queued once | real |
| `dr04[postgres]` | DUR-FENCE/OUTBOX | `reap_once` requeues once (attempt 1), the event is in the worker's index, its id is the NEW dispatch row (FID-5), the next claim is generation 2, and there are two inference dispatches | real |
| `dr09[postgres]` | DUR-SETTLE | Cancel wins; `complete` is refused by terminalize's D3 fence; debit 0, one projection, conservation | real (the fence only; the D5 settlement is not reached) |
| `db06` | DUR-ADMIT (defect) | An admission without its `prepare_dispatch` row is reported by dr01's own assertion | real, live drill on a disposable clone |
| `db07` | DUR-FENCE (defect) | The fence holds for generation 1 after dr04's requeue under the same worker id; with the generation check removed, generation 1's heartbeat is reported | real, live drill |
| `dr01c[postgres]` | CREDIT-SPEND/DUR-ADMIT | Replay gives the same job and ONE held hold on the individual's CREDIT wallet, pins = alias + provisional card, legacy USD books unmoved, per-unit conservation | real |
| `dr02c[postgres]` | DUR-ADMIT/CAP, R66/R69/R70 | Private dev deployment and D2 MC-1's provider_dev key → not_found; revoked → invalid_api_key; full key → capacity_exhausted; unpriced listing → invalid_request; each leaves the footprint unchanged | real |
| `db08` | CREDIT-SPEND (defect) | A CREDIT hold written to the USD books is reported by dr01c | real, live drill |
| `dr01c/dr02c[fake]`, `dr07c` | — | pending F2P / D5 | — |
| `dr13` | DUR-OUTBOX | `Reconciler` over PgJobStore + real Valkey: after drain + ack, a SIGKILL and `rebuild() == 3`, the next drain reads 0, the 3 queued jobs dispatch once, the cancelled one never | real + real |
| `test_two_tenants_are_provisioned…` | API-OPS | Through `Operations`: balance 10,000/0 read back, the foreign wallet Forbidden, the operator key spends nothing, the R71 replay under another key is the same grant, no secret in logs/reprs/argv | A1 grant + wallet real; keys/audit/catalog fake |
| `db09` | API-OPS (defect) | With the grant's uniqueness dropped, the replay case reports a second grant | real, live drill |
| `test_the_role_matrix_has_a_row_for_every…` | DUR-RLS | Every `infrx`/`public` relation and SECURITY DEFINER function has a row, and no row is stale | real catalog |
| `test_a_live_grant_inversion_fails_its_matrix_row` | DUR-RLS | anon+infrx.jobs and service_role−admit flip both rows to failed, rolled back | real |
| rows `E3B-RLS-*` (450) | DUR-RLS | measured access per API role, every 42501 qualified by its message | real (the `rls` stage) |
| `test_stage` ×2 | R83 | per-drill probe; no merged task as a blocker outside `RESIDUAL` | layer 1 |
| `test_harness` namespace cases | P-03 hygiene | per-namespace compose render, port range, block overlap | layer 1 |
| `test_run` ×2 | stage wiring | I3B list plus infra through one runner; a timed-out suite is a FAIL, not a crash | layer 1 |

Kept from phase 1 and still green on this base: dr01–dr10 `[fake]`, dr12, dr14, dr15, dr16, and **dr17, which now passes**: `create_app` refuses pilot naming the legacy route, so G1R's `67e0320` is verified. Also green: db01–db05, the PostgREST cases and the phase-1 stage cases. `e3bm06` still dies on dr16 (killed in both mutation runs).

## Environment

| Item | Value |
|---|---|
| Host | `Linux 7.0.0-1010-aws x86_64`, 16 cores, shared local dev box (D4/G2/G4U/W4 and the coordinator were running concurrently); no cloud resource touched |
| Python / deps | 3.12.3 (`make api-env`, `uv sync --frozen`); psycopg 3.3.6, valkey 6.1.1, pydantic 2.13.5, httpx 0.28.1 |
| Node / pnpm | v22.23.1 / 9.15.9 (`apps/app`: `pnpm install --frozen-lockfile`, no tracked change) |
| Docker / Compose | 29.6.2 / 5.3.1 |
| PostgreSQL | `supabase/postgres@sha256:7768d0d1…` → `PostgreSQL 17.6` |
| Valkey / ClickHouse / S3 | `8.1.10` / `25.8.33.6` / MinIO (digests unchanged from E2) |
| PostgREST | `postgrest/postgrest@sha256:a312f4b2…` → `postgrest/13.0.4` |
| Namespace | `INFRX_E2_NAMESPACE=e3b2`: `infrx-e3b2-{postgres,valkey,clickhouse,s3}`, `infrx-e3b2rest-postgrest`, ports s3 56700 / clickhouse 56723 / 56790 / postgrest 56730 / postgres 56732 / valkey 56779 / fake vLLM 56780, database `infrx_e3b2`, clones `infrx_e3b2_<pid>_<n>`, template `infrx_e3b2_tmpl`, Valkey prefix `infrx_e3b2:`. The gate's tests/q Valkey was `INFRX_Q_VALKEY_PORT=56761`, following the coordinator's mid-run instruction; 56780, which the coordinator proposed, is this namespace's fake-vLLM port, so a free port of the block was used |
| Credentials | fixed local literals only; operator/consumer secrets minted at run time and never printed |
| Seed | 20260921 |

## Commands and results

UTC 2026-09-23. Env var names only. The gate command is
`INFRX_E2_NAMESPACE=e3b2 [INFRX_Q_VALKEY_PORT=56761] apps/infrx-api/.venv/bin/python tests/integration/run.py --layer 3 --canary --report <scratch>/l3-<sha>.json`,
run detached and ending in `EXIT=`.

| # | Command | Exit | Result |
|---|---|---|---|
| 1 | `git fetch origin && git merge --ff-only origin/claude/backend-impl` | 0 | base `740bebf` (contains `9c1c6ed`) |
| 2 | `make api-env`; `cd apps/app && pnpm install --frozen-lockfile` | 0, 0 | venv created; `Done in 2.8s` |
| 3 | `INFRX_E2_NAMESPACE=e3b2 … run.py --layer 2 --keep` (dev stack, 05:3xZ) | 0 | preflight/services/migrate/rls PASS (40 cases before item 7) |
| 4 | focused: `INFRX_E2_NAMESPACE=e3b2 pytest tests/integration/backend/test_drills.py` on the kept stack, after items 1+2 | 0 | `23 passed, 6 skipped` (skips = `PENDING[D4]` ×4, `PENDING[D4,D5]`, `PENDING[G2]`) |
| 5 | `INFRX_E2_NAMESPACE=e3b2 INFRX_MUTANTS=all mutants.py --layer all` on the kept stack at `a3722b0` | 1 | `{"mutants": 175, "killed": 163, "controls_survived": 4, "not_killed": 8, "pending": 0}`, problems `i3bm57` (stale) + `i3bm60`–`i3bm66` (no-cases: their case errors in I3B's `good_restore` fixture). Every e2m*/e3bm* killed |
| 6 | gate at `03d124c` (05:49Z) | crash (`EXIT=1`, traceback) | preflight…rls PASS (490 cases), backend FAIL (87/23/11), engine PASS, then `subprocess.TimeoutExpired: make api-test timed out after 1800.0 seconds`. No JSON. Fixed in `0843485` |
| 7 | gate at `0843485`, two attempts (06:2xZ) | 143 (stopped) | `services` PENDING both times: `failed to bind host port 127.0.0.1:56700` then `:56790: address already in use`. `ss` showed another lane's DB connections to 55435 using ephemeral local ports inside 567xx (the E2 R-c risk; see Limits). Stopped with SIGTERM; teardown PASS |
| 8 | gate at `0843485`, third attempt (06:28Z) | 143 (stopped) | backend FAIL (87/23/11, the same I3B cases), engine FAIL (`bind on ('127.0.0.1', 56780): address already in use`, ephemeral again). Stopped when the coordinator asked for the Q Valkey port; teardown PASS |
| 9 | gate at `0843485` with `INFRX_Q_VALKEY_PORT=56761` (07:12:55Z, 1167.1 s) | **1** | preflight/services/migrate/rls/engine/canary/teardown PASS; backend FAIL (87/23/11); suites FAIL: `tests/integration` 4 failed incl. E2R's shared-clock case (fixed in `f0de778`), `make api-test` 277 failed / 2687 passed; mutants FAIL (176/164 killed/4 controls, problems = the 8 I3B ids) |
| 10 | gate at `f0de778`, `INFRX_Q_VALKEY_PORT=56761` (07:37:50Z, 1387.3 s) | **1** | as command 13, except `make api-test` `277 failed, 2687 passed`: its 5 W3 `broken_runner` failures were this lane's (fixed in `e57b820`) |
| 11 | `INFRX_Q_VALKEY_PORT=56761 make -k check` at `f0de778` (08:01:33Z → 09:39:44Z) | **2** | the `make check` section below |
| 12 | `cd apps/infrx-api && INFRX_MUTANTS=all pytest -q tests/w/test_w3_mutants.py` after `e57b820` | 0 | `84 passed in 273.07s` |
| 13 | **the gate at `e57b820`**, `INFRX_Q_VALKEY_PORT=56761` (09:40:24Z, 1445.9 s) | **1** | the stage table below |
| 14 | `INFRX_Q_VALKEY_PORT=56761 make integration INTEGRATION_ARGS="--layer 1 --canary --report …"` at `e57b820`, default namespace (10:05:04Z, 1769.7 s) | **2** (run exit 1) | preflight SKIP (layer 1). engine PASS, 8 cases at 127.0.0.1:55580. **suites FAIL** on `tests/integration` only: `2 failed, 129 passed, 90 skipped, 1 xfailed`. The two are I3B's `test_i3b_mutant_list_is_well_formed` and `rc10`; the skips are the stack-backed cases without a stack. **`make api-test` exit 0: `2961 passed, 3 skipped, 24 xfailed, 1 xpassed in 1516.88s`**, with the D port free, so `tests/d` ran. `console-test` `283/0`; `bench-test` `67 passed`. mutants FAIL: `{"mutants": 133, "killed": 129, "controls_survived": 3, "not_killed": 1, "problems": ["i3bm57"]}`. canary PASS |
| 15 | `cd apps/infrx-api && pytest -q -rs tests/d/test_e3b_drills.py tests/d/test_jobstore_conformance.py` (10:35:03Z), then `pytest -q tests/d/test_e3b_drills.py` plain and with `INFRX_D1_IMAGE=supabase` (10:36:04Z) | 0, 0, 0 | `48 passed, 3 skipped, 24 xfailed, 1 xpassed`, with `SKIPPED [3] tests/d/test_jobstore_conformance.py:110: missing optional hook 'stream' (not a pass)` (D4's hook). Then **`5 passed`** on the plain image and **`5 passed`** on the Supabase image: D's verbatim copy of dr01/dr02/dr03/dr04/dr09 agrees with this lane's `[postgres]` rig |

### The gate at `e57b820` (command 13): exit 1

| Stage | Status | Measured (quoted from `l3-e57b820.json`) |
|---|---|---|
| preflight | PASS | docker 29.6.2, four digest-pinned images, nothing foreign |
| services | PASS | PostgreSQL 17.6, Valkey 8.1.10, ClickHouse 25.8.33.6, MinIO; `infrx_e3b2` template copy, 1 attempt |
| migrate | PASS | 0001–0016 by sha256; clock probe `moved_s 3600.0, advance_return_lag_s 0.0, at_rest_s 0.0, inside_rolled_back_tx_s 1800.0, after_rollback_s 0.0` |
| rls | PASS | **490 cases**, `failed: null`, roles anon/authenticated/postgres/service_role |
| backend | **FAIL** | `{"postgrest": "postgrest/13.0.4", "passed": 87, "pending": 23, "failed": 11, "not_run": null}`; pytest `3 failed, 87 passed, 23 skipped, 6 warnings, 8 errors in 54.06s`. All 11 failures are I3B's (next table) |
| backend-teardown | PASS | `infrx-e3b2rest-postgrest` removed |
| engine | PASS | `run_engine_conformance`, 8 cases over HTTP at 127.0.0.1:56780 |
| suites | **FAIL** | `tests/integration`: `3 failed, 184 passed, 26 skipped, 1 xfailed, 8 errors`, exactly the backend stage's I3B set. `make api-test` (exit 2, 893.7 s): `272 failed, 2692 passed, 25 xfailed`. Its tail, the 12 lines the report keeps, lists only `tests/d` cases. The count equals the 272 `HarnessBusy` refusals `make check` recorded, but this run did not record the lock holder. With the port free, the same target is green: command 14, `2961 passed`. `make console-test`: `# tests 283 / # pass 283 / # fail 0`. `make bench-test`: `67 passed` |
| mutants | **FAIL** | `{"mutants": 177, "killed": 165, "controls_survived": 4, "not_killed": 8, "pending": 0, "problems": ["i3bm57", "i3bm60", "i3bm61", "i3bm62", "i3bm63", "i3bm64", "i3bm65", "i3bm66"]}`. All 25 e3bm* and every e2m* killed; the controls e2c01, e2c02, e3bc01 and i3bc01 survived; of I3B's 78, all but the 8 named problems were killed |
| canary | PASS | python exit 1 `4 failed, 3 passed`, named; console exit 1 `node_fail 1`, named |
| teardown | PASS | the four `infrx-e3b2-*` removed, `still_named_ours_but_not_ours: []` |

**Backend failures (all I3B-owned, integration request #2):**

| Case | Cause (measured) |
|---|---|
| `recovery/test_mutant_list_i3b::test_i3b_mutant_list_is_well_formed` | `AssertionError: i3bm57`: its anchor `claimed=self.claimed)\n` no longer occurs in `infrx/worker/loop.py` (W3) |
| `recovery/test_recovery::test_i3b_rc10_…` | `Failed: I2B's rollback exists (['rollback.sh']): drive it through a rollback drill now` |
| `recovery/test_restore::test_i3b_bk01_a_…` and `bk01f[policies, columns, functions, indexes, default_acls, triggers, functions_config, rows]` (8 setup errors in the `good_restore` fixture) | `relations differ: missing [('infrx.job_results', 'r', True, False, '{postgres=arwdDxtm/postgres}')], extra [('infrx.job_results', 'r', True, False, '{}')]`. D2's 0014 `revoke all on infrx.job_results from public, anon, authenticated, service_role` leaves an owner-only ACL, and the restored table's `relacl` is NULL, which I3B's comparison renders `{}`. A merge interaction D2 × I3B. **Corrected in round 2 (ruling R92):** a NULL `relacl` is the owner's implicit default, so no owner privilege is lost and `put_result`/`read_result` keep their access after a restore. The red is a comparison artefact: I3B's restore comparison normalises both sides with `acldefault()` (I3B owns that fix). D and the restore procedure change nothing |

**Pending by id** (a case counts once per id it names; 23 distinct cases):
`D2 1 · D3 1 · D4 8 · D5 12 · F2P 12 · G1R 1 · G2 12 · G3 4 · G4U 3 · I2B 1 · M3 1 · W3 1`.
Of those, `D2 1 · D3 1 · G1R 1 · M3 1 · W3 1 · I2B 1` are I3B's four recovery cases (rc03, rc04,
rc05b, rc08b), whose ids are RESIDUAL. E3B's own 19 cases pend only on unmerged tasks:

| Pending case | Ids | Moves when |
|---|---|---|
| journeys ×9 + dataset resume | D5, F2P, G2 (+D4 SSE, +G3 async and resume, +G4U upload) | G2's cutover mounts the ingress: they then FAIL until their bodies are written |
| dr05, dr06, dr08, dr10 `[postgres]` | D4 (the `append` stub) | D4 merges: they run on the real store with no edit (the probe), given D4's pgtesting `stream` hook |
| dr07 `[postgres]` | D4, D5 | both merge |
| dr07c | D5 (the `terminalize` stub) | D5 merges: it FAILS, asking for its body |
| dr01c, dr02c `[fake]` | F2P | the wire-in merges `credit_jobstore_factory` |
| dr11 | G2 | G2 mounts the ingress: it FAILS, asking for its body |

**Detected by live drills** (the stage's `detected`, each a PASS asserting the defect is reported):
`db03`, `db04`, `db05` (phase 1, schema backstops), **`db06`, `db07`, `db08`, `db09`**
(phase 2, real store), plus I3B's `bk01b`, `bk01c`, `bk01d`.

### `make -k check` (exit 2)

Run at `f0de778`, 08:01:33Z–09:39:44Z, with `INFRX_Q_VALKEY_PORT=56761`. `e57b820` landed during
the run; its only change is `fake_vllm.py`'s CLI default.

| Target | Result (quoted) | Skips |
|---|---|---|
| `api-test` | `277 failed, 2687 passed, 25 xfailed, 2 warnings in 755.92s`. **272** are `tests/d`, every one `HarnessBusy: another run holds /tmp/infrx-d1-postgres-55432.lock (… checkout …/codex-g4u)`: G4U's lane held the shared D port. The other **5** are `tests/w/test_w3_mutants.py` `broken_runner`, caused by this lane's item-0 `fake_vllm` default, fixed in `e57b820` (W3 list re-run: `84 passed in 273.07s`) | none |
| `api-mutants` | `435 failed, 1861 passed in 4971.06s`: all 435 are `tests/d` (427 `test_migration_mutants.py`, 8 `test_signup.py`), each `HarnessBusy` (the same lock). Every other list passed, including `tests/i/test_mutants.py` (the coordinator's `37b1294` fix holds) and `tests/w/test_w3_mutants.py` | none |
| `console-test` | `# tests 283 / # pass 283 / # fail 0 / # skipped 0` | 0 |
| `console-lint` | `✖ 2 problems (0 errors, 2 warnings)` (pre-existing, `apps/app/lib/contracts/`) | — |
| `console-typecheck` | `✓ Types generated successfully`, `tsc` silent | — |
| `console-mutants` | contracts `160 mutants: 160 killed … 0 survived, 0 stale, 0 runner errors`; V `40/40`; U `64 mutants, 64 killed`; C `4 self-tests, 0 failed` + `104 mutants: 104 killed`; v2 `5/5` + `23/23` | — |
| `bench-test` | `67 passed in 6.08s` | none |

Neither pytest summary line contains `skipped`. The D share could not run under this `make
check`, because another lane held the D port for its whole duration. **The D share, run when
the port was free:** `make api-test`, which includes the whole `tests/d` suite, went green in
command 14: `2961 passed, 3 skipped (D4's stream hook), 24 xfailed, 1 xpassed`, exit 0. The E3B
drill copy went green on both images in command 15. `api-mutants`' `tests/d` lists (427 migration mutants + 8 signup) were
**not run** by this lane on a free port.


## Failure drill

| Injection | Durable state before → after | Verdict |
|---|---|---|
| crash after commit on the real `admit` / `admit_credit` / `claim` (dr01, dr01c, dr04 `[postgres]`) | one job, one hold on the right unit's books, one dispatch; requeue with a new row at generation 2 | idempotent, conserved |
| SIGKILL of `infrx-e3b2-valkey` under the Reconciler (dr13) | 4 accepted jobs, 4 dispatch rows acknowledged, 1 cancelled → depth 0 → `rebuild()` 3 → drain reads 0 → 3 dispatched once | no accepted job lost, the terminal one never re-dispatched |
| live defects on a disposable clone (db06 admission without dispatch, db07 fence without generation, db08 CREDIT hold on USD books, db09 grant without uniqueness) | the clone is dropped at process exit (`atexit`), and the template and `infrx_e3b2` are never edited (`stack.defect` refuses any database outside this process's clones) | each DETECTED by the unchanged case |
| grant inversion on `infrx_e3b2` (anon + infrx.jobs, service_role − admit) | inside `conn.transaction(force_rollback=True)` | both rows fail, then pass after the rollback |
| I3B's SIGKILLs of Valkey and PostgreSQL mid-stage (bk03, rc09) | this lane's own containers only | the later drills rebuilt their template and passed |

Cleanup: every gate run ended with `teardown PASS` and `still_named_ours_but_not_ours: []`. The
kept dev stacks were removed by the next run's preflight or by `harness.down()`. At the end
`docker ps -a` lists no `infrx-e3b2*` container (`docker ps -a | grep -c e3b2` → `0` after command 15). The D harness containers that commands 14–15 started are gone as well.

## Artifacts

Raw outputs are in the session scratch area (`<scratch>/e3b2/`, not committed), sha256 prefix:
`l3-03d124c.log 6cff6651c37ae679` (crash), `l3-0843485-bindfail.json 088fbb52be9098a5`,
`l3-0843485-bindfail2.json ff2a656a1ead0226`, `l3-0843485-run3-interrupted.json 79f1a42b0bd653d9`,
`l3-0843485.json 5d635a09b3d26394` (command 9), `l3-f0de778.json 55d8843a4f89192f` (command 10),
**`l3-e57b820.json 71cd47d817ded7e3`** (the gate, command 13), `l1-e57b820.json ae7f0ef7676f9188`
(command 14), `check-f0de778.log` (command 11), `testsd-{plain,supabase}.log` (command 15),
`mutants-dev.json fbeed0d4e308cb4f`, `matrix.txt` (the measured access tables), `ir1.diff`
(integration request #1).

## Changes

`git diff --stat 740bebf..e57b820`: 15 files changed, 1472 insertions(+), 257 deletions(-), all
under `tests/integration/`:
`backend/{compose.yaml, stack.py, test_drills.py, test_journey.py, test_schema.py, test_stage.py}`,
`{compose.yaml, fake_vllm.py, harness.py, mutants.py, pgstate.py, run.py, test_harness.py,
test_run.py, test_services.py}`, and this report. No module code, contract, migration, SQL,
Makefile, composition root, manifest or lockfile was changed. `backend/recovery/**` is
untouched. **Migrations: none.** Live defects only ever touch a per-process clone, or run inside
a rolled-back transaction. Rolling back means reverting the commits. With
`INFRX_E2_NAMESPACE` unset, `make integration` is byte-for-byte E2's (same names and ports);
`test_the_compose_file_publishes_…[e2]` pins this.

Harness additions another lane must know about:

- `harness.provision_database(database=)` now takes a database name (defaults unchanged).
- `stack.pg_jobstore()`, `stack.defect()`, `stack.credit_world()` and `stack.stubbed()` are the
  new real-store helpers.
- `mutants.all_mutants()` is now the stage's list.

## Limits

1. **Not integrated end to end; the gate is not passed.** No journey ran, because no metered
   route is mounted (G2). "Real store" here means the local, digest-pinned Supabase image, not a
   hosted project (no hosted claim).
2. **Real-store coverage today:** admission (both regimes), preparation, dispatch outbox,
   claim, reaper, cancel and fence; queue rebuild from PostgreSQL through Q3's Reconciler; the
   A1 grant; the full role/grant matrix. **Not covered:** append/replay (D4), settlement (D5),
   any HTTP route (G2/G3/G4U).
3. **G6B provisioning is partly fake.** TenantStore, AuditLog, Registry, AccountView and
   CatalogDirectory have no PostgreSQL adapter; per the coordinator they are D5's. Keys issued
   through `Operations` live in G6B's fake TenantStore, so CREDIT admission (item 3) uses its
   own key rows written in SQL. The WalletDirectory is a test-local read of
   `infrx.credit_wallets`, not a product adapter.
4. **The pinned image lacks GoTrue's `auth.users` columns.** The E3B template adds
   `email_confirmed_at`/`deleted_at` as `supabase_admin`, exactly as D's harness does. Hosted
   projects have them.
5. **Ephemeral-port collisions (E2 R-c, now measured).** The namespace block 56700–56799, like
   E2's 55500–55599, is inside the kernel's ephemeral range 32768–60999. While another lane
   churned connections to 55435, `ss` showed local ports 56702, 56712, … in use. Two of five
   gate attempts could not bind a service port, and one could not bind the fake vLLM port.
   Those runs were stopped and repeated. They are not counted as results. See integration
   request #4.
6. **The shared D port decides whether `make api-test` is green.** When another lane holds
   `/tmp/infrx-d1-postgres-55432.lock`, every `tests/d` case refuses with `HarnessBusy`: 272 in
   `api-test`, 435 in `api-mutants`. With the port free, `api-test` passes whole (command 14).
   This lane never ran `api-mutants`' `tests/d` lists on a free port.
7. **Deviations from the brief, each deliberate:**
   - (a) db06/db07/db08/db09 commit their `create or replace` to the case's **own disposable
     clone**, not to a rolled-back transaction. PgJobStore opens a new connection per
     operation, so an uncommitted replacement would be invisible to the store under test.
   - (b) db07 adds one heartbeat of generation 1 after dr04's requeue, because dr04 never
     presents the stale lease again.
   - (c) G6B's order is grant → key → tenant, not key → grant (see item 6).
   - (d) "psycopg.Error joins the transient tuple" was not applied. dr13 is sequential and
     interrupts only Valkey. The transient tuple lives in `tests/q`'s concurrent drill, which
     is Q/D's.
   - (e) Items 1 and 2 share one commit.
8. **The optional `supabase/postgres` image bump was not done.** No digest was approved (open
   input d). The PostgREST member-session case still measures the pinned pairing (`200 []`).
9. No GPU, no engine, and no latency/throughput claim. P-01: the Marlin card is provisional and
   appears here only as its version label.

## Integration requests

1. **Coordinator — tasklocal `e3b2` block.** The registered `"e3b2": {"postgres": 56701}` does
   not express the block, and `local_services("e3b2")["compose"]` still answers E's
   55500–55599. The e3b2 row of
   `test_harness::test_every_port_is_inside_the_range_tasklocal_grants_this_task` is therefore a
   **strict xfail**: it XPASSes and fails the day this lands, and then the mark goes. Exact
   diff, verified on a scratch copy (`local_services("e3b2")["compose"]` → 56700 + 56701…56799,
   `all_host_ports()` owner `e3b2/compose` for all 100; e2 unchanged):
   ```diff
   --- a/apps/infrx-api/infrx/contracts/tasklocal.py
   +++ b/apps/infrx-api/infrx/contracts/tasklocal.py
   @@ -28,10 +28,13 @@
        # D4's relay/journal drills and G2's relay drill each get a Valkey of their own
        "d4": {"postgres": 55435, "valkey": 55465},
        "g2": {"valkey": 55466},
   -    # E3B phase 2 runs the E2 stack as compose namespace `e3b2` in the reserved block
   -    # 56700-56799 (the harness derives service ports from INFRX_E2_NAMESPACE; a block is
   -    # not a single port, so it is recorded here as the namespace's first port)
   -    "e3b2": {"postgres": 56701},
   +}
   +
   +# A task's own block of a track service, replacing the track's (host port, extra ports).
   +# E3B phase 2 runs the E2 stack as compose namespace `e3b2`: E2's layout moved by +1200
   +# (tests/integration/harness.NAMESPACES), 56700-56799.
   +TASK_BLOCKS: dict[str, dict[str, tuple[int, tuple[int, ...]]]] = {
   +    "e3b2": {"compose": (56700, tuple(range(56701, 56800)))},
    }
    
    # track -> {service: (host port, extra ports)}
   @@ -73,6 +76,7 @@
        # r1 R48: a task-specific port overrides (or grants) its track's.
        for service, port in TASK_PORTS.get(task, {}).items():
            services[service] = (port, services.get(service, (port, ()))[1])
   +    services.update(TASK_BLOCKS.get(task, {}))
        if not services:
            if track in FAKE_ONLY_TRACKS:
                return {}
   @@ -92,7 +96,7 @@
        two tasks on one port is exactly the clash this module exists to prevent.
        """
        reserved: dict[int, str] = {}
   -    for track, services in TRACK_SERVICES.items():
   +    for track, services in (*TRACK_SERVICES.items(), *TASK_BLOCKS.items()):
            for service, (port, extra) in services.items():
                for number in (port, *extra):
                    owner = f"{track}/{service}"
   ```
2. **I3B (read-only here) — the reds on this base, and wiring now owed to E's runner.**
   Failing output is in the backend table above.
   - (a) rc10: I2B's `deploy/rollback.sh` exists, so write the rollback drill body.
   - (b) i3bm57: re-anchor it in `infrx/worker/loop.py` (W3 changed the anchor).
   - (c) **Corrected in round 2 (ruling R92):** `test_restore` `good_restore`:
     `pgrestore.CATALOG` compares `coalesce(relacl, acldefault(...))` (and `proacl`/`nspacl`
     likewise), so an owner-only ACL restored as NULL reads as the same privileges. No
     privilege is lost. Nothing is asked of D or of the restore procedure, and `job_results`
     must not be re-granted to anyone.
   - (d) Blocker names:
     ```diff
     --- tests/integration/backend/recovery/test_recovery.py
     -    kit.pending("G1R", "G2", why="no metered route is mounted to restart under load")
     +    kit.pending("G2", why="no metered route is mounted to restart under load")
     -    stubs = stack.unimplemented_rpcs()
     -    if stubs == 0:
     -        pytest.fail("the D RPCs are implemented: drive the DB loss through the real adapter")
     -    kit.pending("D2", "D3", why=f"{stubs} infrx RPCs are still infrx.unimplemented stubs")
     +    stubs = stack.stubbed(("admit", "claim", "terminalize")) if stack.has_stack() else {}
     +    if not stubs:
     +        pytest.fail("admission, claim and settlement are implemented: drive the DB loss "
     +                    "through PgJobStore")
     +    kit.pending(*sorted(set(stubs.values())), why=f"{sorted(stubs)} are still stubs")
     ```
     rc05b (`M3`) and rc08b (`W3`) need an owner id: no task owns the S3 ObjectStore or the
     worker entry point. Until each is renamed, E3B keeps the id in `stack.RESIDUAL` with its
     reason.
   - (e) `recoverykit.py` l.97:
     `pytest.skip(f"no {harness.PROJECT} stack: run …")`. `test_restore.py` l.28 docstring:
     `harness.PG_DATABASE`.
   - (f) `mutants_i3b.run()`: delete `_copy_with_infra` and the `mutants._copy_trees =
     _copy_with_infra` line. E's runner copies `infra/` now, and the standalone `mutants_i3b.py`
     raises `FileExistsError: …/infra` on its second copy (measured).
   - (g) With those fixed, the stage's i3bm60–i3bm66 stop being `no-cases`: their case currently
     errors in the same fixture.
3. **Coordinator (shared contract fake) — the fake's requeue event id.** `FakeJobStore.recover`
   mints the requeue `IndexEvent`'s id separately from its `inference_dispatch` outbox row's id,
   unlike PgJobStore and Q3 FID-5. dr04 therefore asserts FID-5 on the real store only. Exact
   diff in `infrx/contracts/fakes/state.py` (the requeue branch):
   ```diff
   -            event = IndexEvent(event_id=self.ids.event_id(), job_id=job.id,
   +            emitted = self._emit(job.id, OutboxKind.inference_dispatch, now,
   +                                 {"request_id": job.id, "attempt": job.attempts})
   +            event = IndexEvent(event_id=emitted.event_id, job_id=job.id,
   …
   -            self._emit(job.id, OutboxKind.inference_dispatch, now, {"request_id": job.id,
   -                                                                    "attempt": job.attempts})
   ```
   Then drop `backend == "fake" or` from dr04.
4. **Coordinator / host — reserve the task port blocks from the ephemeral range.** Command 7/8
   evidence: `net.ipv4.ip_local_port_range = 32768 60999` contains every `tasklocal` port.
   Proposed (host setting, not applied by this lane):
   `sysctl -w net.ipv4.ip_local_reserved_ports=55400-55699,56300-56400,56700-56799,58123,59000,59100,59110`,
   persisted in `/etc/sysctl.d/`. The alternative is to accept R-c's single retry and re-run on
   collision.
5. **Coordinator merges (from the brief's procedure).** When D4, G2 or G4U merge:
   - drop the id from `stack.PENDING` or move it to `RESIDUAL`
     (`test_stage::test_no_pending_id_names_a_merged_task_unless_it_is_a_named_residual`
     enforces this against tasks.json);
   - add D4's 0017 objects to `pgstate.RELATIONS`/`FUNCTIONS` from D4's request 8 (the
     completeness case fails until they are added);
   - add the `0017_stream_journal.sql` line to `test_harness::test_the_migration_set_…`.

   No other change is needed for dr05/dr06/dr08/dr10: the probe unpends them when `append` stops
   being a stub. The test also needs D4's pgtesting `stream` hook; without it they fail by name
   ("no PostgreSQL StreamStore").

## Handback

- **Next unblocked:**
  - D4 → dr05/06/08/10 `[postgres]` run as-is.
  - D5 → dr07/dr07c and the CREDIT settlement, plus the PostgreSQL adapters behind item 6's
    fakes.
  - F2P wire-in → the dr01c/dr02c `[fake]` twins.
  - G2 cutover → the journeys and dr11 fail until their bodies are written (E3B phase 3).
  - I3B fixes (#2) → the gate can reach PENDING (exit 3).
- **Gate state against this base:** FAIL (exit 1). The reds are I3B's recovery cases and
  mutant list (#2), plus `tests/d` refused during the gate's `make api-test` (the shared D port;
  green in command 14 when free). The E3B-owned cases are PENDING with no
  failure: 19 pending, on D4/D5/F2P/G2/G3/G4U only.

## Round 2 — review fix round (review `E3B2-review-80cef22.json`: fix_required)

Two sessions did this round. The first died at a session limit during its gate run, and the
coordinator saved its uncommitted edits as `c587ddc` (WIP). The second session (this text)
reviewed that snapshot, kept it (it was sound but incomplete: see item (b)), finished the
round, and ran the gate at the final head.

The branch took the integration head three times with `git merge --no-ff`:
- `a5c62a4` (`1a26137`: the F2P wire-in and G4U, both implemented in tasks.json);
- `5e9bbf1` (`94b4033`: D4 = 0017 + `PgStreamStore`);
- `6ad6080` (`1882e6c`: `75cd7bb`, the cancel-cause pending case's `raises=` pin, plus plan files).

There was no rebase, reset, amend or push. Every item has its own commit, and every commit
names the case that kills its mutant. The kill texts below are quoted from the gate's mutation
stage (`l3-f883a0a.json`, command R2-10); where its 6-line tail ends before the
location, from R2-8.

### Findings → commits → killing case ← mutant (kill text)

| Finding | Commit | Change | Killing case ← mutant or live drill: kill text (gate) |
|---|---|---|---|
| **F1** (blocking) | `1d2e0a1` | dr13 checks its premise on the store. After the drain it reads `infrx.outbox` and asserts that every `inference_dispatch` row of the four jobs has `acknowledged_at` set. Before the post-rebuild `read == 0` check it advances the DB clock past `redelivery_s`, so a claimed row that was never acknowledged would come back | live drill **db10** (the reviewer's mutant: `acknowledge_dispatch` writes `acknowledged_at = null` and still counts its rows). dr13 reports it: `pytest.raises(AssertionError, match="not acknowledged on the store")` passes in the gate's backend stage |
| **H1** (blocking) | `a85ffd6` (+ `8eba9c4`, `f06585e`) | R83 pristine baseline. `run_one` first runs each mutant's named cases on the **unmutated** copy, cached per (suite, selector, copy kind). A mutant whose cases are already red there is `baseline-red`: a problem, never a kill, and its edit is not applied. Follow-on `8eba9c4`: the baseline showed that e2m64–66 had been killed vacuously. Their `tests/d` case spawns children with `PYTHONPATH=<its api root>`, and the copy had no `infrx`. Suites under `apps/infrx-api` now run against a copied `infrx`. `f06585e`: the gate at `f97bfb7` showed e3bm26 dying of an `IndexError`, so its fake now answers red twice and the case fails at its assertion | `test_run::test_a_mutant_whose_cases_are_red_unmutated_is_baseline_red` ← **e3bm26**: killed, `test_run.py:957: AssertionError`; `…runs_against_a_copied_infrx` ← **e3bm37**: killed, `test_run.py:1076: AssertionError` |
| **H2** (blocking) | `749abe0`, `324224a` | I2B is a named `RESIDUAL` (rc08b). The residual case computes the merged set over `{**stack.PENDING, **recoverykit.PENDING}`. New `run.stale_pending()`: a `PENDING[<id>]` whose task tasks.json marks implemented or integrated, and which is not RESIDUAL, fails the backend stage. `324224a` is coordinator addition (c) below | `test_stage::test_a_pending_id_naming_a_merged_task_fails_the_stage` ← **e3bm27**: killed, `backend/test_stage.py:145: AssertionError` |
| **H3** (blocking) | `3b86d4f` | The one-runner case imports `mutants_i3b` on its own path | ← **e3bm22**: killed, `test_run.py:911: AssertionError` (R2-8; an assertion, no `ModuleNotFoundError`); **e3bm23**: killed, `test_run.py:913: AssertionError` (R2-8) |
| F2 (refuted, folded in) | `8fb3ea2` | `pgstate.write_rows()` records the **table-level** write verbs per relation and API role (186 rows, measured). The live inversion also grants `insert on public.api_keys` to authenticated | `test_services::test_the_role_matrix_holds_for_every_role` ← **e3bm36**: killed, `test_services.py:193: AssertionError` |
| F3 | `e0461a3`, `e651266` | F2P and G4U leave `stack.PENDING`. dr01c/dr02c `[fake]` are written on `credit_jobstore_factory`: one body per drill, with `credit_rig()` as the only per-store code. Measured divergence, pinned: the fake has no D2 MC-1 rule (IR2-6) | dr01c[fake] ← **e3bm28**: killed, `backend/test_drills.py:597: AssertionError` |
| F4 | `cd227fd` | dr04 `[fake]` pins the fake's event-id divergence explicitly (`event_id not in (first, again)`). R93: the coordinator changes the fake and deletes the pin in the E3B2 merge (IR2-3) | — (a pin; it turns red with the fake fix) |
| F5 | `e802700` | Live drill **db08b**: the CREDIT hold is kept and a legacy `credit_holds` row is added; dr01c must report `'usd': 1` | db08b ← **e3bm29** (the reviewer's fm11): killed, `backend/test_drills.py:756: Failed` |
| F6 | `0c69026` | `stack.defect` refuses the template, E2's database and another process's clone, before it connects | `test_stage::test_a_live_defect_is_refused_outside_this_processs_clones` ← **e3bm30**: killed, `backend/test_stage.py:168: Failed` |
| F7 | — (text) | Corrections list below | — |
| H4 | `d8b5b63`, the coordinator's `2cc281c`, then `01ecdce` | `d8b5b63` swept `infrx-e2-fake-vllm-*` in every namespace. The coordinator's `2cc281c` replaced every shared-temp sweep with a **private TMPDIR per mutant run**: litter goes with the copy, and nothing in `/tmp` is swept. The reason was that a name-based sweep deleted another lane's live mutant copy. `01ecdce` is coordinator addition (a) | ← **e3bm31** (private TMPDIR dropped): killed, `test_run.py:999: AssertionError`; ← **e3bm39**: killed, `test_run.py:997: AssertionError` |
| H5 | `5c7eb8a` | `shell()` puts each suite in its own process group and kills the group on timeout or interrupt | budget case (a grandchild must not survive) ← **e3bm32**: killed, `test_run.py:935: AssertionError`; **e3bm24**: killed, `test_run.py:932: Failed` |
| H6 | `a5c6f92` | A timed-out `make api-test` (exit 124) fails the suites stage and the run | ← **e3bm33**: killed, `test_run.py:1015: AssertionError` (R2-8) |
| H7 | `13c276d`, `3438d86` | The report records `git_head` (sha, dirty), `namespace` and per-stage `seconds`. Each suites run records its red cases by id | ← **e3bm34**: killed, `test_run.py:1029: AssertionError` (R2-8) |
| F6-findings | `bd740ee`, `11cbbae` | The make targets run with `-rs`. A `make api-test` skip outside `KNOWN_API_SKIPS` fails the suites stage. `11cbbae` (D4 merged): the one attributed skip (D4's `stream` hook) is gone, so the list is empty and any skip fails | ← **e3bm35**: killed, `test_run.py:1057: AssertionError` (R2-8) |
| F1-bk01 (R92) | — (text) | Wording corrected in place (backend-failure table, IR #2(c)). No owner privilege is lost. The restore comparison normalises ACLs with `acldefault()`, and I3B owns that fix (`33a95d8` on `codex/i3b-followup`) | — |
| F2-merge-procedure | — (text) | IR2-5 below gives the complete per-merge procedure | — |
| F3-ir1 | — (text) | IR2-1: the TASK_BLOCKS diff and the strict-xfail deletion, in one commit | — |
| F4-request4 | — (text) | P-21 (host `ip_local_reserved_ports`) only prevents new collisions. An established ephemeral socket on a block port still has to be caught by R-c's retry | — |
| F5-rc10 | — (text) | I3B owns rc10 (ruling e). `fa71117` on `codex/i3b-followup` writes it. E3B needs no change | — |
| F8 | — (text) | The phase-3 obligations table below | — |

### The coordinator's additions to this round

| Addition | Commit | Change | Killing case ← mutant: kill text (gate) |
|---|---|---|---|
| **(a)** `_temp_litter` never sweeps another lane's live copy | `2cc281c` (coordinator), `01ecdce` | `_temp_litter` is gone, and each run gets a private TMPDIR. The litter case now plants, mid-run in the shared temp directory, E2's literal names that appear in every namespace: another lane's live copy `infrx-e2-i3bm57-*` and a fake-vLLM log `infrx-e2-fake-vllm-*.log`, beside a `<project>-*` copy. All of them must survive the run | `test_run::test_a_mutant_run_keeps_its_litter_private_and_never_touches_foreign_temp_files` ← **e3bm39** (run_one sweeps the shared `infrx-e2-*` after a run): killed, `test_run.py:997: AssertionError` |
| **(b)** D4 merged | `c587ddc` (the coordinator's snapshot of the first session's edits, reviewed and kept), `0d2fd4c` | D4 leaves `stack.PENDING`. The `_NoStream` stand-in is removed, so `stack.pg_jobstore()` returns `pgtesting.make_jobstore_factory`'s harness with its `stream` hook (`PgStreamStore`, D4 request 7), and dr05/dr06/dr08/dr10 `[postgres]` run on it unchanged. The journeys' SSE cell pends on G2 only. e3bm15 is re-aimed at dr07c, the drill still held back by a stub (D5's `terminalize`). New live drill **db11**: `fence_lease` loses its generation check on the case's clone, and dr05[postgres]'s `pytest.raises` reports generation 1's append as stored | ← **e3bm15**: killed, `backend/test_drills.py:655: Failed`; db11 detects (backend stage) |
| (b) 0017 in the role matrix (D4 request 8) | `716eede`, `88fdf88` | New rows: the four SECURITY DEFINER functions (`read_journal`/`expire_journal`/`journal_usage` service-only; `journal_terminal_event` nobody; `append` keeps 0004's row); `chunk_doc(infrx.stream_chunks)` (invoker, nobody) as an `INVOKER_FUNCTIONS` row; `journal_rows()` with the watermark columns per API role, and the CHECK `jobs_journal_watermark_is_one_cursor` and the ENABLED trigger `jobs_terminal_journal_event`, each pinned to its `pg_get_*def` text. The live inversion also grants `chunk_doc`, drops the CHECK and disables the trigger (6 rows fail, then pass). The rls stage runs **696** cases. `88fdf88`: layer 1's coverage case names the new rows | inversion case ← **e3bm38** (the trigger row stops requiring `tgenabled = 'O'`): killed, `test_services.py:240: AssertionError`; completeness ← **e3bm21**: killed, `test_services.py:208: AssertionError` |
| (b) no attributed api-test skip | `11cbbae` | `KNOWN_API_SKIPS = ()` | ← **e3bm35**: killed, `test_run.py:1057: AssertionError` (R2-8) |
| **(c)** owner references in the vocabulary | `324224a`, `f97bfb7` | The residual case accepts `recoverykit.OWNERS` (the I3B follow-up's `I2B-R4`, `M1-L2`) as the only non-task ids. They must not be tasks, and they must parse whole as `PENDING[..]`. It reads statuses with `tasks.get` and pins `stack.pending`'s refusal on a synthetic RESIDUAL id too, so e3bm16 stays killed when RESIDUAL empties (the I3B lane measured it surviving otherwise). `PENDING[I2B-R4]` is pending, never stale. The stage-arithmetic XML uses ids that are not tasks (X1–X3), so it no longer breaks as tasks merge. `f97bfb7`: dr17's unreachable fallback no longer names merged G1R; it fails by name instead | ← **e3bm16**: killed, `backend/test_stage.py:136: AssertionError`; **e3bm27**: killed, `backend/test_stage.py:145: AssertionError` |

**Checked on a scratch merge** (command R2-5): `f97bfb7`, merged with `codex/i3b-followup@d99da09`,
plus only the `stack.py` hunk of I3B's request 1 (RESIDUAL emptied; G1R/D2/D3/M3/W3 out of
`PENDING`; `i3b-residual.diff 0cdce4ef58b14874`), with **no** `test_stage.py` hunk. Layer 0 gives
`141 passed, 94 skipped, 1 xfailed` (exit 0). e3bm16 and e3bm27 are both killed. I3B's pendings
there: `G2`, `M1-L2` and `I2B-R4`.

**Rulings recorded:** R92 (bk01, above); R93 (the fake's requeue event id: IR2-3); R94
(an idempotency key reused across sync/SSE/async is `409 idempotency_conflict`: phase-3 journey
assertion, below).

### Round-2 commands and results

UTC 2026-09-23, second session. Env var names only. `$SC` is the session scratch area; nothing
there is committed.

| # | Command | Exit | Result |
|---|---|---|---|
| R2-1 | `git merge --no-ff origin/claude/backend-impl` (`1882e6c`) → `6ad6080` | 0 | brings `75cd7bb`; no conflict |
| R2-2 | focused runs on the first session's kept stack (`infrx-e3b2-*`, provisioned at `5e9bbf1` with 0001–0017, `l2-d4.json`): `INFRX_E2_NAMESPACE=e3b2 pytest backend/test_drills.py -k "dr05 or dr06 or dr07 or dr08 or dr10 or db11 or db07"`; `pytest test_services.py`; `mutants.py --layer all --only e3bm38/e3bm15/e3bm21`; `pytest tests/integration` | 0, 0, 0, 1 | `11 passed, 2 skipped` (both `PENDING[D5] ['terminalize']`); `25 passed`; all three killed; `3 failed, 201 passed, 20 skipped, 1 xfailed, 8 errors`, every red I3B's. The kept stack was then torn down (`harness.down()`: the four removed, `foreign []`) |
| R2-3 | `cd apps/infrx-api && INFRX_D_TASK=e3b2 pytest -q -rs tests/d/test_e3b_drills.py`, plain and `INFRX_D1_IMAGE=supabase`, on this lane's registered port 56732 (never the shared D port), at `f97bfb7` and again at `f883a0a` | 0, 0 / 0, 0 | **`9 passed`** on the plain image and **`9 passed`** on the Supabase image, both times. D's copy is dr01, dr02, dr03, dr04, dr09 and, since D4, dr05, dr06, dr08, dr10. Containers removed by the harness: `docker ps -a \| grep -c e3b2` → `0` |
| R2-4 | layer 0 (no stack): `INFRX_E2_NAMESPACE=e3b2 pytest -q -rs tests/integration`, at `f97bfb7` and at `f883a0a` | 1, 1 | `2 failed, 139 passed, 91 skipped, 1 xfailed` both times. The two are I3B's `test_i3b_mutant_list_is_well_formed` (i3bm57) and `rc10`, both fixed on `codex/i3b-followup` |
| R2-5 | scratch clone of `f97bfb7` + `git merge codex/i3b-followup` (clean) + I3B's `stack.py` hunk only (`i3b-residual.diff`); layer 0 in e3b2; `mutants.py --only e3bm16`, `--only e3bm27` | 0 | `141 passed, 94 skipped, 1 xfailed`; both killed; I3B pendings `G2`, `M1-L2`, `I2B-R4` |
| R2-6 | scratch clone + IR2-1's diff; `pytest tests/integration/test_harness.py apps/infrx-api/tests/contracts` | 0 | `1070 passed`; `local_services("e3b2")` → compose 56700 + 99, postgres 56732; e2 unchanged |
| R2-7 | gate at `f97bfb7` (14:47:07Z → 15:24:43Z) | 1 | the same stages as R2-10. One flaw found: **e3bm26 died of `test_run.py:949: IndexError`**, not an assertion (R83). Fixed in `f06585e` (`--only e3bm26` → `test_run.py:957: AssertionError`). The report says `dirty: true`, because this evidence file was being edited, uncommitted (`git status`: that file only) |
| R2-8 | scratch clone at `f06585e`: each of e3bm12, 22, 23, 33, 34, 35 applied in turn and its case run (the gate's 6-line tails end before the location) | 1 ×6 | each dies at its assertion: `backend/test_stage.py:87` (`'PASS' == 'PENDING'`), `test_run.py:911` (the I3B ids missing), `:913` (`['i3bm22', …] == []`), `:1015`, `:1029` (`None == '<sha>'`), `:1057` (`'PASS' == 'FAIL'`) |
| R2-9 | scratch clone + R93's fake change + dr04's pin deleted (`r93.diff`); `pytest backend/test_drills.py -k "dr03 or dr04"` | 0 | `2 passed, 2 skipped` (the `[postgres]` twins skip without a stack) |
| R2-10 | **the gate at `f883a0a`**: `INFRX_E2_NAMESPACE=e3b2 INFRX_Q_VALKEY_PORT=56761 apps/infrx-api/.venv/bin/python tests/integration/run.py --layer 3 --canary --report $SC/e3b2/l3-f883a0a.json`, detached (15:26:10Z → 16:00:34Z, 2063.7 s) | **1** | the stage table below |

### The gate at `f883a0a` (R2-10): exit 1, on I3B's reds only

The report records `git_head {"sha": "f883a0a7ba8454eec568d81da9efd792e17ad4ae", "dirty": false}`
and `namespace "e3b2"`. **`l3-f883a0a.json` sha256 `b7c9993b7742fb11`** (log `1134692cd1f289c3`).

| Stage | Status | Measured (quoted from `l3-f883a0a.json`) |
|---|---|---|
| preflight | PASS | docker 29.6.2, four digest-pinned images, ports 56700/56723/56732/56779/56780/56790 |
| services | PASS | PostgreSQL 17.6, Valkey 8.1.10, ClickHouse 25.8.33.6, MinIO; 1 attempt |
| migrate | PASS | 0001–**0017** by sha256 |
| rls | PASS | **696 cases**, `failed: null` (676 + 0017's 20 rows) |
| backend | **FAIL** | `{"passed": 98, "pending": 17, "failed": 11, "not_run": null, "stale_pending": null}`; pytest `3 failed, 98 passed, 17 skipped, 6 warnings, 8 errors in 74.09s`. **All 11 failures are I3B's** (`…recovery.test_mutant_list_i3b`, `rc10`, `bk01_a`, 8 × `bk01f` setup errors in `good_restore`), which the I3B follow-up fixes. `detected`: db03, db04, db05, db06, db07, db08, db08b, db09, db10, **db11**, and I3B's bk01b, bk01c, bk01d |
| engine | PASS | 8 cases over HTTP at 127.0.0.1:56780 |
| suites | **FAIL** | `tests/integration` exit 1: `3 failed, 201 passed, 20 skipped, 1 xfailed, 8 errors`, whose `failures` are exactly the backend stage's 11 I3B ids. **`make api-test` exit 0: `3120 passed, 30 xfailed, 1 xpassed in 1391.40s`, `skips: []`**. The D port was free, so `tests/d` ran whole and no skip was reported, which is the round's first api-test with none. `make console-test` `node_pass 289, node_fail 0`. `make bench-test` `67 passed`. `unexpected_skips: null` |
| mutants | **FAIL** | `{"mutants": 191, "killed": 174, "controls_survived": 4, "not_killed": 13, "pending": 0}`. The 13 problems are all I3B's: `i3bm57` stale (W3 moved its anchor; re-anchored on `codex/i3b-followup`); `i3bm40/41/42/48/49` **baseline-red** on `bk01_a`; `i3bm60–66` **baseline-red** on `bk01f` (the R83 baseline at work: these were "killed" in round 1). **All 39 e3bm* and every e2m* killed, controls e2c01/e2c02/e3bc01/i3bc01 survived.** Kill texts are in the tables above |
| canary | PASS | python and console both fail and name the canary |
| teardown | PASS | four `infrx-e3b2-*` removed; `still_named_ours_but_not_ours: []`. PostgREST removed by the backend teardown |

**Pending by id** (17 distinct cases): `D5 12 · G2 12 · G3 4`, plus I3B's rc03 (`G1R,G2`), rc04
(`D2,D3`), rc05b (`M3`) and rc08b (`W3,I2B`), whose ids are RESIDUAL until the I3B follow-up.
The 13 cases E3B owns pend on unmerged tasks only:
`dr07[postgres]` and `dr07c` (D5); `dr11` (G2); 9 journeys (`D5,G2` for sync and SSE; `D5,G2,G3`
for async); dataset resume (`D5,G2,G3`). **No E3B-owned case fails.**

**Artifacts** (`$SC/e3b2/`, sha256 prefix): `l3-f883a0a.json b7c9993b7742fb11` (the gate),
`l3-f883a0a.log 1134692cd1f289c3`, `l3-f97bfb7.json 70a4ac25744f74b0` (R2-7),
`layer0-f883a0a.log c144fc1497b7ef3e`, `testsd-f883a0a-{plain 1bc8aaccd77379e1, supabase
fc4c3b06f2cd3261}`, `layer0-r3.log e409234b8e370f47` and `testsd-r3-{plain 7ce02e6a8a875d8f,
supabase f60ef230f7b0b248}` (at `f97bfb7`), `ir1-r2.diff 23d1719320510c57`, `i3b-residual.diff
0cdce4ef58b14874`, `r93.diff bce25ab6ef18582b`, `killtexts-f883a0a.json`.

**Changes this round:** the lane's own commits since `80cef22` touch only
`tests/integration/{backend/stack.py, backend/test_drills.py, backend/test_journey.py,
backend/test_stage.py, harness.py, mutants.py, pgstate.py, run.py, test_harness.py, test_run.py,
test_services.py}` and this report. No module code, contract, migration, SQL, Makefile or
composition root changed. **Migrations: none.** Live defects still only touch per-process clones
or rolled-back transactions.

**Limits added this round.** (1) The layer-3 gate was not repeated on the plain PostgreSQL image,
because E2's stack pins the Supabase image. Both images are covered through D's copy of the
drills (R2-3). (2) The api-test was green while the shared D port happened to be free. When
another lane holds that port, `tests/d` refuses with `HarnessBusy` (round-1 Limits 6).
(3) Journeys, dr11 and dr07c stay pending: no metered route is mounted, and settlement is D5's.

### Corrections to the round-1 text (kept for the record; only bk01 is corrected in place, per R92)

- **Backend-failure row "bk01 … restore-fidelity finding" and IR #2(c):** corrected in place
  (R92). A NULL `relacl` is the owner's implicit default, so nothing is lost. I3B's
  comparison normalises both sides with `acldefault()`. No D change, and nothing re-grants
  `job_results`.
- **Item 7 / Limits §2, "the full role/grant matrix":** overclaimed (review F2). E3B's rows
  cover SELECT reachability per relation, EXECUTE per SECURITY DEFINER function (plus 0017's
  invoker `chunk_doc`), the table-level write verbs and 0017's column/CHECK/trigger rows. D's
  `check_privileges`/`check_function_privileges` enforce column-level write grants and
  SECURITY INVOKER functions.
- **Pending-by-id, "whose ids are RESIDUAL" (I2B):** I2B was not RESIDUAL until `749abe0`.
- **Command 15, "D's verbatim copy agrees":** D's `tests/d/test_e3b_drills.py` has dr03/dr04 in
  their pre-`reap_once` shape, so it corroborates dr01, dr02, dr09 and the pre-reaper
  dr03/dr04. From D4 it also carries dr05/dr06/dr08/dr10 (command R2-3).
- **"Not merged: the F2P wire-in":** stale when written. The wire-in merged 8 minutes after the
  base.
- **"`make api-test` … 3 skipped (D4's stream hook)":** attributed by count, matching command
  15. From round 2 the reasons are recorded with `-rs`.
- **Round-1 mutation counts (177: 165 killed):** included vacuous kills (H1: i3bm40/41/42/48/49
  baseline-red; e2m64–66 vacuous until `8eba9c4`). Recounted in the gate below.

### E3B phase-3 obligations (review F8)

These cases are E3B's. Each is `pytest.fail("write the body now")` behind its pending ids, and
fails the day its blocker merges. Schedule E3B phase 3 **before, or together with,** G2's ingress
cutover and the D5 merge. Otherwise the gate turns red on them: loudly, never a false green.

| Case | Turns red when | Assertions the body must make |
|---|---|---|
| journeys × 9 (text / video_url / video_upload × sync / SSE / async) | G2 mounts the ingress (async also needs G3; every cell needs D5) | Two tenants from `provision_two_tenants()`. Per cell: **sync** `POST /v1/chat/completions` → 200 with usage. **SSE**: the first frame carries the job handle and the terminal event carries authoritative usage; replay from the journal (D4, `PgStreamStore.read_owned`) is byte-equal from any issued cursor; a cursor past the head is 400 `invalid_cursor`. **async**: 202 with a handle, `GET` status to terminal, `GET` result. An `Idempotency-Key` replay in the **same** mode returns the same job. **R94:** the same key in another mode (sync↔SSE↔async) is `409 idempotency_conflict` and writes nothing. The other tenant's status, result, replay and cancel each answer 404. After terminal: exactly one CREDIT debit = the admitted card × usage (half-up), the hold released, reserved back to its prior value, one usage projection, ledger = 10,000 − debit, USD books unmoved, pins = the `quote()` pins. **video_url**: M2 fetches and probes. **video_upload**: `POST /v1/uploads` → `PUT` → `complete` (G4U), then the handle in the request; a foreign upload handle answers 404 |
| dataset resume | G2 + G3 (+D5) | E1B's bench client interrupted and re-run with the same keys: jobs = items, one hold and one debit per item, total = Σ per-item debits, no duplicate accepted item |
| dr11 | G2 | A sync client that disconnects mid-generation cancels (`client_disconnected`, D5's 0018). The hold is released, the engine sees the cancel, and no attempt is orphaned. SSE follows G2's mode contract (detach or cancel). Conservation holds |
| dr07c | D5 (`terminalize` no longer a stub) | CREDIT settlement on PgJobStore: one debit on the CREDIT wallet at the admitted card, the hold released in the same transaction, one usage projection, and 0017's trigger writes exactly one terminal journal event. An identical retry replays; a different proposal is `AlreadyTerminal`; a late cancel returns the same outcome. USD unmoved, per-unit conservation |
| dr07 `[postgres]` | D5 | Runs **as written** (it drives `terminalize`). No body is owed |

### Nonblocking items not taken, and why

- **PENDING ids checked against the merge log** (F3 suggestion): not done. tasks.json is the
  tracker the brief names, and `stale_pending` fails any merged id wherever it appears.
- **dispatch_snapshot's OB-5 live-attempt filter in dr13** (F1 coverage note): not added. No job
  holds a live attempt at rebuild time in dr13, and D2's OB-5 checks cover the filter.
- **SECURITY INVOKER functions in the completeness case** (F2 option): not done, except
  0017's `chunk_doc` row. D's `check_function_privileges` covers every function.
- **I3B's files:** read-only here. The I3B follow-up (`codex/i3b-followup`, merging after this
  lane) fixes rc03/rc04/rc05b/rc08b ids, rc10, bk01 (R92), i3bm57 and `_copy_with_infra`.

### Integration requests IR2-1…IR2-6 (round 2; supersede round-1 #1, #3 and #5)

1. **Coordinator — IR #1, revised: tasklocal `TASK_BLOCKS` plus the strict-xfail deletion, in ONE
   commit.** The coordinator's `"e3b2": {"postgres": 56732}` row (the D harness uses it:
   `INFRX_D_TASK=e3b2`, command R2-3) is **kept**. `local_services("e3b2")` gains `compose`
   56700 + 56701…56799. `all_host_ports()` reads `TASK_BLOCKS`, and a task's port inside its own
   block is not a clash. The `_TASKLOCAL_BLOCK` strict-xfail mark in `test_harness.py` goes in
   the same commit: landing the diff alone makes the e3b2 row XPASS(strict), which is red.
   Verified on a scratch clone of `f97bfb7` (command R2-6; neither file changed after it): `local_services("e3b2")` →
   `compose (56700, 99 extra)`, `postgres 56732`; `all_host_ports()[56732] == "e3b2/postgres"`;
   e2 unchanged (`compose (55500, 99)`); `test_harness.py` + `apps/infrx-api/tests/contracts`
   `1070 passed`. Full diff: `ir1-r2.diff 23d1719320510c57` (2 files, +13/−12):
   ```diff
   --- a/apps/infrx-api/infrx/contracts/tasklocal.py
   @@ TASK_PORTS = {
        "e3b2": {"postgres": 56732},
    }
   +
   +# A task's own block of a track service, replacing the track's (host port, extra ports).
   +# E3B phase 2 runs the E2 stack as compose namespace `e3b2`: E2's layout moved by +1200
   +# (tests/integration/harness.NAMESPACES), 56700-56799. Its TASK_PORTS postgres port lies
   +# inside the block (the port the harness derives), which `all_host_ports` allows.
   +TASK_BLOCKS: dict[str, dict[str, tuple[int, tuple[int, ...]]]] = {
   +    "e3b2": {"compose": (56700, tuple(range(56701, 56800)))},
   +}
   @@ def local_services(task_id: str) -> dict[str, LocalService]:
            services[service] = (port, services.get(service, (port, ()))[1])
   +    services.update(TASK_BLOCKS.get(task, {}))
   @@ def all_host_ports() -> dict[int, str]:
   -    for track, services in TRACK_SERVICES.items():
   +    for track, services in (*TRACK_SERVICES.items(), *TASK_BLOCKS.items()):
   @@
   -            if clash is not None and clash != f"{track_of(task)}/{service}":
   +            if clash is not None and clash != f"{track_of(task)}/{service}" \
   +                    and not clash.startswith(f"{task}/"):         # inside its own block
   --- a/tests/integration/test_harness.py
   -# E3B phase 2, integration request #1: … (the 4-line comment)
   -_TASKLOCAL_BLOCK = {"e3b2": pytest.mark.xfail(
   -    strict=True, reason="integration request #1: tasklocal has no e3b2 compose block")}
   -
   -
   -@pytest.mark.parametrize("namespace", [pytest.param(ns, marks=_TASKLOCAL_BLOCK.get(ns, ()))
   -                                       for ns in sorted(harness.NAMESPACES)])
   +@pytest.mark.parametrize("namespace", sorted(harness.NAMESPACES))
   ```
2. **Coordinator — at the I3B follow-up merge (after this lane): the RESIDUAL/PENDING lines.**
   Only I3B's `stack.py` hunk is needed. Delete the `# RESIDUAL (merged; …)` block of
   `stack.PENDING` (G1R, D2, D3, M3, W3) and set `RESIDUAL: dict[str, str] = {}`. **Drop I3B's
   `test_stage.py` hunks**: this lane's `324224a` already accepts `recoverykit.OWNERS`, uses
   `tasks.get`, and pins the refusal on a synthetic id. I3B's XML replacement also names D4,
   which is merged now and would be stale. Measured: layer 0 green on that merge (above).
3. **Coordinator (R93) — the fake's requeue event id, at this lane's merge.** In the same merge
   that changes `FakeJobStore.recover`'s requeue branch (its `IndexEvent.event_id` = the fresh
   outbox row's `event_id`, round-1 IR #3's diff), delete dr04's fake branch:
   ```diff
   -        if backend == "fake":
   -            # Integration request #3, accepted (coordinator ruling b): … (3 comment lines)
   -            assert events[0].event_id not in (first, again), (events, first, again)
   -        else:
   -            assert events[0].event_id == again != first
   +        assert events[0].event_id == again != first
   ```
   Until then the pin holds: `dr04[fake]` passes on today's fake.
4. **Coordinator / host (P-21):** unchanged from round-1 #4, with the F4 caveat above.
5. **Coordinator — the merge procedure, complete (review F2-merge).** When a task id merges:
   (i) remove it from `stack.PENDING`, or move it to `RESIDUAL` with a reason if a read-only
   case still names it; (ii) remove it from the `test_journey` `COMMON`/`BY_MODE`/`BY_INPUT`
   tuples and from the dataset-resume tuple; (iii) remove every literal `stack.pending("<id>", …)`
   call that names it; (iv) set tasks.json's status (`stale_pending` and the residual case hold
   the gate to it). **Due now:** none. D4 is done in this round. The remaining ids are D5, G2
   and G3.
6. **Contract fake / ports — D2 MC-1 on the fake** (request #6 in the first session's
   round-2 draft, kept): `FakeJobStore._credit_terms` lets a provider_dev credential filed in a
   consumer's org reach the public listing, and refuses it only for the unfunded dev wallet
   (`InsufficientCredit`). PgJobStore answers `NotFound`. dr02c `[fake]` pins today's answer
   and flips with the fix.

### D5-pending cases (for D5's handback)

`dr07[postgres]` and `dr07c` (`PENDING[D5]`, `['terminalize'] are still infrx.unimplemented
stubs`); every journey cell and dataset resume (D5 is in `COMMON`). When D5's `terminalize`
stops being a stub: dr07[postgres] runs as written, and dr07c fails and asks for its body
(phase 3). I3B's rc04 also pends on the stub owners (`D5` expected).

### Handback (round 2)

- **Next unblocked:**
  - the E3B2 merge: IR2-1 and IR2-3;
  - then the I3B follow-up merge: IR2-2. The gate's remaining reds are I3B's, and that
    merge closes them.
  - D5 → dr07/dr07c; G2 → the journeys and dr11 (phase 3).
- **Gate state at this head:** see the stage table. E3B-owned cases: no failure; pending only
  on D5/G2/G3.

## Round 2b — request R3-1 (G2 and G3 merged; their cutover held)

Since the round-2 gate, G2 (`2391d4d`), G3 (`b560b51`) and the F fakes follow-up (`a2779d1`)
merged onto `claude/backend-impl`, and tasks.json marks G2 and G3 **implemented**. Their cutover
is still HELD by the coordinator (G2's integration request 1, plus G3's `ROUTERS += jobs`). On
that tasks.json, `stack.PENDING["G2"]` (12 cases) and `["G3"]` (4 cases) named merged tasks, and
layer 0 failed on this lane's own rule: `AssertionError: ({'G2', 'G3'}, set())` (I3B follow-up
Q8). New commits only, on top of `b0a64e1`.

| Item | Commit | Change | Killing case ← mutant: kill text |
|---|---|---|---|
| R3-1.1 | `e95bce5` | `stack.OWNERS = {"G2-R1": …}` holds I3B's `recoverykit.OWNERS["G2-R1"]` text **verbatim**, and `stack.PENDING` includes it. The owner lives in `stack` because E3B's cases pend through `stack.pending`, and recoverykit imports stack, not the reverse. The 9 journeys, the dataset resume and dr11 now pend on `G2-R1` (+`D5` where they did), and their reasons name the held cutover. G3 leaves the vocabulary. G2 stays only as RESIDUAL, because I3B's rc03 on this tree still names it; the I3B follow-up renames it, and IR2-2 empties RESIDUAL. In test_stage, owner references are `stack.OWNERS ∪ recoverykit.OWNERS`, and an owner that both name must carry the same text. Merged ⊆ RESIDUAL. A RESIDUAL id that this tree's tasks.json does not yet mark merged (G2 here) must name the owner reference that replaces it | e3bm16, e3bm27: killed (`--only`) |
| R3-1.2 | `cd8099b` | `run.stale_pending`, explicit: a merged task is never a pending owner, even while its cutover is held; the cutover request is (an owner reference, never stale). RESIDUAL excuses a merged id only where **every** case naming it is one of I3B's recovery cases. New case `test_stage::test_e3b_cases_pend_on_the_held_cutover_never_on_a_merged_task`: it runs the 11 E3B cutover pendings, reads their `PENDING[..]` back, and requires `G2-R1` on each, no refusal, and nothing stale. It then pins the rule on G1R (merged on every tree, made RESIDUAL): stale in an E3B case, excused in I3B's rc03 | **e3bm40** (the sync journeys re-keyed on merged `G2`): killed, `backend/test_stage.py:187: AssertionError` ("E3B cases keyed on a merged task"). **e3bm41** (RESIDUAL excuses a merged id everywhere again): killed, `backend/test_stage.py:194: AssertionError`. e3bm27 re-anchored and still killed |

| # | Command (UTC 2026-09-23) | Exit | Tail |
|---|---|---|---|
| R3-a | at `cd8099b`: `INFRX_E2_NAMESPACE=e3b2 pytest -q tests/integration` (no stack) | 1 | `2 failed, 140 passed, 91 skipped, 1 xfailed`. The two are I3B's (i3bm57 list, rc10), unchanged |
| R3-b | scratch clone: `origin/claude/backend-impl` @ `c37b42e` (≥ `a2779d1`) + `git merge --no-ff codex/e3b-phase2-gate@cd8099b` (clean); `pytest backend/test_drills.py -k "dr03 or dr04"` | 1 | as IR2-3 predicted, the F fakes follow-up turns dr04's fake pin red: `assert '00000005-…' not in ('00000004-…', '00000005-…')`, `1 failed, 1 passed, 2 skipped` |
| R3-c | same scratch, dr04's `if backend == "fake":` branch deleted (IR2-3, `r31-dr04.diff 1f03b05fc4014bb0`); same selection | 0 | **`2 passed, 2 skipped`**: dr03[fake] and dr04[fake] pass, and dr04 asserts `events[0].event_id == again != first` on the fake (the `[postgres]` twins skip without a stack) |
| R3-d | same scratch: `pytest -q tests/integration/test_run.py tests/integration/backend/test_stage.py` (16:41:49Z) | 0 | **`56 passed in 10.81s`** |
| R3-e | same scratch: layer 0, `pytest -q -rs tests/integration` | 1 | first run: `3 failed, 139 passed, 91 skipped, 1 xfailed`. The third failure, `test_busy_ports_finds_a_listener_on_a_task_local_port`, was `OSError: [Errno 98]` on 56780: `ss` showed a foreign `TIME-WAIT 127.0.0.1:56780 ↔ 127.0.0.1:55434` (another lane's client to d3 on an ephemeral port inside this block; P-21) |
| R3-f | same, rerun once that socket was gone (16:43:24Z) | 1 | **`2 failed, 140 passed, 91 skipped, 1 xfailed, 2 warnings in 18.65s`**: only I3B's two (i3bm57 list, rc10), which the I3B follow-up fixes. PENDING: `[D5,G2-R1]` ×9, `[G2-R1,D5]`, `[G2-R1]` (dr11), plus I3B's `[G1R,G2]`, `[D2,D3]`, `[M3]`, `[W3,I2B]` |
| R3-g | same scratch, then `git merge codex/i3b-followup@1a7a256` (clean, both `G2-R1` texts identical), then IR2-2 now also dropping `G2` (`r31-i3b-residual.diff 5763acb9d3a6c045`); layer 0 | 1 | `1 failed, 146 passed, 97 skipped, 1 xfailed`: `test_the_mutation_stage_runs_every_list_through_one_runner`, `AssertionError: assert ['i3bm94', …, 'i3bm104'] == []`. These are I3B mutants of `apps/infrx-api/deploy`, a tree the runner does not copy: I3B's R2-A |
| R3-h | R3-g + I3B's R2-A (`OWNED_TREES += ("apps/infrx-api/deploy",)`) | 0 | **`147 passed, 97 skipped, 1 xfailed, 2 warnings in 19.89s`**. PENDING: `[D5,G2-R1]` ×9, `[G2-R1,D5]`, `[G2-R1]` ×2 (dr11, I3B's rc03), `[I2B-R4]`, `[M1-L2]` |

Artifacts (`$SC/e3b2/`, sha256 prefix): `r31-run-stage.log ff2e2c1879b11a28`,
`r31-layer0.log 414ca4987702fd4e` (R3-e), `r31-layer0c.log e5dd84e4ca449413` (R3-f),
`r31-layer0-i3b.log 47af590cd295c1c9` (R3-g), `r31-layer0-i3b-r2a.log 4f346281d4a4f364` (R3-h).
The scratch clone was removed afterwards. No container was started in round 2b.

**Integration requests after round 2b.**
- **IR2-1** and **IR2-3** stand unchanged. R3-c verified IR2-3's deletion against the merged
  fake.
- **IR2-2**, at the I3B follow-up merge: delete the whole `# RESIDUAL (merged; …)` block of
  `stack.PENDING`, which now includes **`G2`**, and set `RESIDUAL = {}`. Keep `stack.OWNERS`.
  I3B's `recoverykit.OWNERS["G2-R1"]` is the same text, and test_stage holds them equal.
- **I3B's R2-A and R2-B** are addressed to this lane's runner (`OWNED_TREES += deploy`; D-mode
  recovery suites keep the shared TMPDIR). They are not applied here, because R3-1 did not ask
  for them. R2-A is required at the I3B merge (R3-g → R3-h).
- The day the coordinator releases the cutover (G2-R1), the 11 cases stop pending and fail
  until E3B phase 3 writes their bodies. At that point `G2-R1` also leaves `stack.OWNERS`.

## Round 2c — I3B's runner requests R2-A and R2-B, committed here

The coordinator asked this lane to commit them, because it owns the runner. They are new
commits on top of `a9b1ca9`, one per item.

| Item | Commit | Change | Killing case ← mutant: kill text |
|---|---|---|---|
| R2-A | `d8384ed` | `OWNED_TREES += ("apps/infrx-api/deploy",)` on its own line, with I3B's text verbatim, so e3bm23's anchor still occurs once. I3B's rc10 runs I2B's `rollback.sh`/`lib.sh` from the copy, and i3bm94/97/98/104 mutate them there. The one-runner case now also requires every `apps/infrx-api/deploy/*.sh` to lie in a copied tree, so the invariant can be killed on this tree too, where no I3B mutant touches `deploy/` yet | `test_run::test_the_mutation_stage_runs_every_list_through_one_runner` ← **e3bm42** (new; the line removed): killed, `AssertionError: assert ([…'apps/infrx-api/deploy/rollback.sh'] and [...]) == []` / "Left contains 5 more items". e3bm22 and e3bm23 still killed |
| R2-B | `dadbd79` | An additive `if INFRX_I3B_PG == "d" and suite under tests/integration/backend/recovery/: private = None` (I3B's round-2 diff), placed after the private-TMPDIR line. e3bm31's anchor and the litter case are untouched. **Scope, precisely:** the child then inherits the parent's TMPDIR, so D's pgharness port lock (`gettempdir()/<container>-<port>.lock`) is shared **only among runs that use the same TMPDIR** (I3B DRL-4). Two lanes with different TMPDIRs on one D port are still not serialised; a lock path independent of TMPDIR is D's request (I3B R3-2). The runner code carries this ceiling as a `ponytail:` comment. I3B's measurement, not repeated here: with the lock held, `--only i3bm87` was `killed` without R2-B and `baseline-red` (refused) with it | `test_run::test_a_d_mode_recovery_run_keeps_the_parents_tmpdir_for_the_pgharness_lock` (unit level: `private` for D mode + recovery suite is `None` for the baseline and the mutated run; outside D mode, or for any other suite, it is private) ← **e3bm43** (new; the D-mode clause disabled): killed, `test_run.py:1026: AssertionError`. e3bm31 (`:1004`) and e3bm39 (`:1002`) still killed |

| # | Command | Exit | Tail |
|---|---|---|---|
| R2c-1 | scratch clone of `a9b1ca9` + `git merge codex/i3b-followup@1a7a256` + IR2-2 as updated (the RESIDUAL block including G2 deleted; `RESIDUAL = {}`); `pytest tests/integration/test_run.py -k every_list_through_one_runner` | 1 | `AssertionError: assert ['i3bm94', 'i...8', 'i3bm104'] == []` / `Left contains 4 more items, first extra item: 'i3bm94'`; `1 failed, 45 deselected in 0.22s` |
| R2c-2 | the same, on `dadbd79` | 0 | **`1 passed, 46 deselected in 0.19s`** |
| R2c-3 | the R2c-2 scratch: layer 0, `INFRX_E2_NAMESPACE=e3b2 pytest -q -rs tests/integration` | **0** | **`148 passed, 97 skipped, 1 xfailed, 2 warnings in 36.73s`**. PENDING `[D5,G2-R1]` ×9, `[G2-R1,D5]`, `[G2-R1]` ×2 (dr11, I3B's rc03), `[I2B-R4]`, `[M1-L2]` (`r2c-merge-layer0.log f6d85586dd5d7b6e`) |
| R2c-4 | at `dadbd79`: `pytest -q tests/integration/test_run.py tests/integration/backend/test_stage.py` | 0 | **`57 passed in 10.39s`** (`r2c-run-stage.log c6eeb585038d0187`) |
| R2c-5 | at `dadbd79`: layer 0 | 1 | `2 failed, 141 passed, 91 skipped, 1 xfailed, 2 warnings in 36.09s`. The two are I3B's (the i3bm57 list case and rc10), which the I3B follow-up fixes (R2c-3) (`r2c-layer0.log cfd9db611091bd25`) |

No container was started. The scratch clones were removed. The integration requests IR2-1 and
IR2-3 (the coordinator's, at this lane's merge) and IR2-2 as updated in round 2b (at the I3B
merge) stand. I3B's R2-A and R2-B are now on this branch, so the I3B merge needs only IR2-2.

## Round 3 — confirmation fix round (`E3B2-confirm-b0a64e1.json`: 6 blocking, 12 nonblocking)

These are new commits on top of `5eb7b85`, one per item, with no rebase, reset, amend or push.
No E2 stack was needed. For every new mutant, the kill text below is its death line, measured
by applying the mutant alone in a scratch clone at `b892ab0` (R3-8); `mutants.py --only` for
each of them returns `killed` (R3-7).

### Blocking

| Finding | Commit | Change | Killing case ← mutant: death line |
|---|---|---|---|
| **G-B1 + HON-2**: the baseline cache key's copy kind was unpinned; so was the `baseline-red` entry in `summarise` | `a18f567` | New case `test_a_baseline_is_reused_only_for_the_same_suite_selector_and_copy_kind` (fake `_pytest`), with two arms. (1) Two mutants share (suite, selector) and differ in copy kind, and only the copied kind is red unmutated: two baselines run, and the statuses are `killed` and `baseline-red`. Two real pairs span both kinds: (test_observe, ob11) and (test_recovery, rc01). (2) Two mutants differ only in selector: the same two statuses. The baseline-red case gains a control arm: a `must_survive` mutant that is red unmutated is in `problems` and is not a surviving control | **e3bm44** (key without copy kind) and **e3bm45** (constant key): `test_run.py:1015: AssertionError` (`(['killed', 'killed'], [('dr01 and fake', False)])`). **e3bm46** (`"baseline-red"` dropped from the problem tuple): `test_run.py:984: AssertionError` |
| **G-B2**: `dirty` was never pinned, and it was measured only at the end of the run | `42e0819` | `Report.__init__` takes `git_head()`. `as_json` records it as `git_head` and the end state as **`git_head_end`**. New case `test_the_report_records_the_tree_at_the_start_and_at_the_end`: `harness.REPO_ROOT` points at a tmp git repo. An untracked file present at the start and removed before the end gives `git_head.dirty True` / `git_head_end.dirty False`; a clean repo gives both False, and `sha` = HEAD | **e3bm47** (`dirty` constant False) and **e3bm48** (git_head taken at the end only): `test_run.py:1131: AssertionError` |
| **G-B3**: `re.M` was unpinned and skip counts were not cross-checked | `e3eefac` | The case's fake output puts the SKIPPED line mid-output (`x / SKIPPED [3] … / 1 passed, 3 skipped in 1s`) and reads the count back. `suites()` fails when `make api-test`'s `counts['skipped']` is non-zero and no reason was parsed (`skips_without_reasons`) | **e3bm49** (`re.M` dropped): `test_run.py:1144: AssertionError`. **e3bm50** (the count cross-check removed): `test_run.py:1172: AssertionError`. e3bm35 re-anchored: `:1165` (`'PASS' == 'FAIL'`) |
| **G-B4** (dr11 vacuous under IR2-5) and **HON-1** (stale PENDING[G2]/[G3] on the merged tree) | **closed in round 2b**: `e95bce5`, `cd8099b`; declared here in `e488366` | The E3B cutover pendings are keyed on the owner reference `G2-R1`, never on merged G2/G3 (e3bm40/41, round 2b). `e488366` declares the dr11 half. The held-cutover case runs dr11 and requires a `PENDING[G2-R1]` skip, so deleting dr11's pending call while the ingress is unmounted fails; it is never an empty pass | **e3bm51** (dr11's `stack.pending` replaced by a no-op): `backend/test_stage.py:185: Failed: DID NOT RAISE <class 'Skipped'>` |

**Why `G2-R1` and not `D5`** (the refuters' alternative for HON-1). The journeys, the dataset
resume and dr11 are waiting for **one act**: the coordinator's release of the held cutover (G2
integration request 1, which also puts G3's jobs router in `ROUTERS`). D5's adapters are one
**precondition** of that release, not the release itself. D5 merging mounts no route. If these
cases pended on D5 alone, the gate would read "unblocked by D5" on the day D5 merges, while
nothing would have changed for them. The cases that need settlement name both: `PENDING[D5,G2-R1]`.
`G2-R1` is no task, so it is never stale. It leaves `stack.OWNERS` the day the cutover lands,
and then every one of these cases fails until E3B phase 3 writes its body.

**On the merged tree, no E3B case pends on a merged task.** Measured by the scratch merge into
`origin/claude/backend-impl@7ed6993` (≥ `bfbbeb0`, R3-4), reading every `SKIPPED … PENDING[..]`
line against that tree's tasks.json. The ids from `backend/stack.py` (E3B's cases) are `D5`
(planned) and `G2-R1` (owner reference) only. The merged ids (`D2`, `D3`, `G1R`, `G2`, `I2B`, `M3`,
`W3`) come only from I3B's `recoverykit.py` skips, which are RESIDUAL until the I3B follow-up
merge. With that merge and IR2-2 (R3-6), the only pending ids left are `D5`, `G2-R1`, `I2B-R4`
and `M1-L2`.

### Nonblocking

| Finding | Commit / disposition |
|---|---|
| HON-3 (R2-A), HON-4 (R2-B ceiling) | **closed in round 2c**: `d8384ed` (e3bm42), `dadbd79` (e3bm43; the same-TMPDIR ceiling is stated in the code and in §Round 2c) |
| HON-6: docstrings, and `integrated` unpinned | `7f35b10`: `PENDING[D1]` (integrated) is stale beside D1R (implemented). The docstrings state cd8099b's rule (RESIDUAL is excused only in I3B's recovery cases). **e3bm52** (`("implemented",)` only): `backend/test_stage.py:157: AssertionError` (`['D1R'] == ['D1', 'D1R']`). e3bm27 dies on the same line |
| G-N1: flaky reds were not attributable | `fa17b96` + `4c21b3b`. Measured: a bare `-rs` **replaces** pytest's default `-rfE`, so a red `make api-test` named no failing case. `SUITE_ADDOPTS = "-rfEs"` now drives the make targets and the backend suite. `shell()` keeps each FAILED/ERROR line (`failure_lines`, reported by the suites stage), and it gives the child `COLUMNS=400`, because pytest cuts summary lines at 80 columns without a tty and drops the message first. That was found red by `fa17b96`'s own case, committed before the run was read; `4c21b3b` fixes it. **e3bm53** (`SUITE_ADDOPTS` back to `-rs`): `test_run.py:1187: AssertionError`, from a real pytest run. The two E2 `test_fake_vllm` port reds are not re-attributed here: they did not reproduce (the refuter's own five reruns), and they are R-c retry candidates (IR #4 / P-21) |
| G-N2: the backend stage's seconds | `b892ab0`: the `backend` entry is added before its teardown (the teardown still runs in `finally`), and the case asserts the order. e3bm12 re-anchored and still killed |
| G-N3 / HON-5: which skips `make api-test` can emit | **Left in code, answered here.** Its skip sites on this tree: (1) `corpus cache not found ($CORPUS_CACHE or <main>/.claude/corpus-cache)` (`tests/m/test_parity.py:113`), which **every `git clone --shared` checkout hits**, because the cache lives under the main checkout's `.claude/`; (2) `task-local PostgreSQL unavailable: docker is not installed / not usable` and `task-local Valkey unavailable: …` (the tests/d, tests/q and projection `skipif`s; the gate's preflight fails first without Docker); (3) `missing optional hook …` / `… needs the optional hook …` (conformance; deterministic); (4) `the judge extra is not installed` (deterministic under `make api-env`). `HarnessBusy` and `ForeignContainer` (a held D port) are failures or errors, not skips: they fail the stage through a nonzero exit whatever `KNOWN_API_SKIPS` holds. **Each skip failing the suites stage is fail-closed by design.** The one environmental case is (1): run the gate from a worktree of the main checkout, or export `CORPUS_CACHE`, which is how the round-2 gates saw 3120 passed with 0 skipped where the confirmation's clone saw 3119 + 1. Exporting the cache from `run.py` is not done, because resolving "the main checkout" from a clone is guesswork; add it the day gates run from clones by protocol |
| G-N4: the runner's kill rule does not read the death line | **Left.** E2's and I3B's lists mostly declare no `cases`, so requiring the failing ids ⊆ `mutant.cases` would mark most of them as problems. Round 2 and round 3 enforce the death shape by hand (R2-8, R3-8), and that caught e3bm26's IndexError. Upgrade path: parse the `path:line: <Type>` line and require AssertionError/Failed or a declared `dies_by`, once every list declares its cases |
| HON-7: what dr13's clock advance pins | **Stated, not changed.** After the premise read (every drained dispatch row is `acknowledged_at is not null`, db10), advancing the clock past `relay.redelivery_s` makes `drain().read == 0` a claim about **acknowledged** rows: the outbox does not hand them out again even once the redelivery window has passed. Without the advance, `read == 0` would also hold for rows that were claimed and not acknowledged, and so were not yet due. The only defect it can catch is a drain that ignores `acknowledged_at`. No live drill injects that, so removing the advance survives, as the refuters found. A drill for it (make `dispatch_pending` ignore `acknowledged_at`; dr13 must say "the acknowledged rows came back") needs the E2 stack, which this round does not use, so it is left for the next layer-3 round |
| HON-8: IR2-6 has no named owner | **Owner named:** the coordinator first rules whether backend-first CREDIT admission refuses a provider_dev audience (as D2 MC-1 encodes in 0011). Then **"F fakes follow-up 2" (track F, the contract fakes owner; coordinator to dispatch)** adds the refusal to `FakeJobStore._credit_terms` and flips dr02c's `[fake]` pin in the same merge, as R93 did for dr04 |
| Cleanup note | `/tmp/infrx-e3b2-postgres-56732.lock` was this lane's (its content: `pid 3200880 checkout …/codex-e3b2`, from the R2-3 D-drill run at 16:01Z). It was confirmed unheld (`flock -n`) and **removed** |

### Round-3 runs

| # | Command | Exit | Tail |
|---|---|---|---|
| R3-1 | at `b892ab0`: `pytest -q tests/integration/test_run.py tests/integration/backend/test_stage.py` | 0 | **`60 passed in 10.43s`** (`r3-run-stage.log 5a866b3bc5a96d64`) |
| R3-2 | at `b892ab0`: layer 0, `INFRX_E2_NAMESPACE=e3b2 pytest -q -rs tests/integration` | 1 | `2 failed, 144 passed, 91 skipped, 1 xfailed, 2 warnings in 18.57s`. Both failures are I3B's (the i3bm57 list case and rc10) (`r3-layer0.log 000653e69636d924`) |
| R3-3 | scratch: `origin/claude/backend-impl@7ed6993` + `git merge --no-ff codex/e3b-phase2-gate@b892ab0` (clean) + IR2-3's dr04 pin deleted; `-k "dr03 or dr04"`; `test_run.py` + `test_stage.py` | 0, 0 | `2 passed, 2 skipped` (dr03 and dr04 on the fake; the `[postgres]` twins skip without a stack); **`60 passed in 10.88s`** |
| R3-4 | same scratch: layer 0 | 1 | First run: `3 failed, 143 passed`. The third is `test_busy_ports_finds_a_listener…`, `OSError: [Errno 98]` on 56780, while `ss` showed a foreign `TIME-WAIT 127.0.0.1:56780 ↔ 127.0.0.1:55436` (d5's PostgreSQL client on an ephemeral port inside this block; P-21). Rerun once that socket was gone (17:08:24Z): **`2 failed, 144 passed, 91 skipped, 1 xfailed`**, only I3B's two (`r3-merge-layer0.log e496d3cda203e36a`, `r3-merge-layer0b.log 522c88da31e43788`) |
| R3-5 | R3-4's `-rs` lines against that tree's tasks.json | — | from `backend/stack.py`: `D5 planned`, `G2-R1 (no task: owner reference)`; from `recovery/recoverykit.py` only: `D2 D3 G1R G2 I2B M3 W3` (implemented, RESIDUAL) |
| R3-6 | the same scratch + `git merge codex/i3b-followup@1a7a256` (clean) + IR2-2 (updated); layer 0 | **0** | **`151 passed, 97 skipped, 1 xfailed, 2 warnings in 21.18s`**. PENDING `[D5,G2-R1]` ×9, `[G2-R1,D5]`, `[G2-R1]` ×2, `[I2B-R4]`, `[M1-L2]` (`r3-merge-i3b-layer0.log c948e3862d719bdc`) |
| R3-7 | at `b892ab0`: `INFRX_E2_NAMESPACE=e3b2 mutants.py --only <id>` for e3bm42…e3bm53, plus e3bm12, e3bm27, e3bm35 | 0 each | all 15 `killed` (`r3-only.log affa43939b536c45`) |
| R3-8 | each of e3bm42…e3bm53 applied alone in a scratch clone at `b892ab0`, its selector run | 1 each | the death lines quoted in the tables above; every one is `AssertionError`, except e3bm51, which is `Failed: DID NOT RAISE` (`r3-shape.log 341814ae54b327c7`) |

**Integration requests after round 3.** IR2-1 and IR2-3 apply at this lane's merge (the
coordinator's). IR2-2 as updated in round 2b applies at the I3B merge. IR2-6's owner is named
above. No other request is open. The scratch clones are removed. No container was started in
round 3.

## Round 4 — final-verification fix round (`E3B2-verify-0924cf2.json`: 3 blocking, 9 nonblocking)

These are new commits on top of `0924cf2`, one per item, with no rebase, reset, amend or push.
No E2 stack was needed. The verifier's own layer-3 gate at `0924cf2` (report sha256
`0bbfad08ef69f16e`) matched round 3's claims except for the item below. Its root cause is on
this lane: **round 3 changed a line that E2's mutant e2m69 anchors on, and never ran a whole
mutation stage or an anchor scan afterwards** (R3-7 ran `--only` on 15 ids). Round 4 closes that
class of miss with a layer-0 guard.

| Finding | Commit | Change | Killing case ← mutant: death line |
|---|---|---|---|
| **GATE-B1 = RUN-B1**: e2m69 went stale at `e3eefac` | `75fa1af` | e2m69 is re-anchored on the widened line: `(failed or silent or unexpected or unread) else` → `(failed or unexpected or unread) else`. New layer-0 guard `test_every_mutant_anchor_occurs_as_declared_on_the_checkout`: every mutant in `all_mutants()` must find its `before` exactly `occurrences` times in the checkout. E2's, E3B's and I3B's lists must all be present. The only exceptions are `KNOWN_STALE`, which is **strict**: an entry that is no longer stale fails the guard too. Today it holds only I3B's `i3bm57`, which is fixed on `codex/i3b-followup` and must be removed at the I3B merge (IR2-2). With e2m69's old anchor restored, the guard fails with `stale anchors (id: occurrences found): {'e2m69': 0, 'i3bm57': 0}` (measured on a scratch clone) | **e2m69**: killed, `test_run.py:334: AssertionError` (its own case, the one the verifier named). **e3bm56** (the guard checks E's list only): `test_run.py:941: AssertionError` |
| **GATE-B2**: the skip count must not come from the `-rs` lines it is checked against | `101160c` | `counts('x / 1 passed, 3 skipped, 2 xfailed in 1s')['skipped'] == 3`, with no SKIPPED line at all. One suites arm runs through the **real** `shell()` on `3120 passed, 3 skipped, 30 xfailed in 1s` with no SKIPPED line, and gives FAIL with `skips_without_reasons == ['make api-test']` | **e3bm54** (the count read from `^SKIPPED \[(\d+)\]`, the verifier's V5) and **e3bm55** (`(\d+) skipped in`, V13): `test_run.py:1173: AssertionError` (`assert None == 3`). e3bm35, e3bm49 and e3bm50 are still killed |
| GATE-N1: the backend suite could run on a bare `-rs` | `fb05821` | The backend-stage case asserts that the recorded backend argv carries `run.SUITE_ADDOPTS` | **e3bm57** (the backend argv on `-rs`): `backend/test_stage.py:93: AssertionError` |
| GATE-N2 / RUN-N4: `_is_recovery` matched a substring | `95540cc` | The held-cutover case adds an E3B case merely *named* like a recovery case (`b.test_drills::test_e3b_recovery_like`) that pends on a RESIDUAL id. It must be stale | **e3bm58** (`"recovery" in case`): `backend/test_stage.py:205: AssertionError` (`[] == ['G1R']`) |
| RUN-N1: the `git_head` comment overstated what it catches | `12862a2` | The comment now says what the two samples catch: a tree dirty at the start and clean by the end, or the reverse. An edit made and undone between the two samples is not seen. The report still does not fail on a difference; the evidence names the start sample | comment only; e3bm47 and e3bm48 still killed |
| GATE-N3 / RUN-N2: IR2-3's wording | text (below) | On any head at or after `a2779d1` (the F fakes follow-up), **IR2-3 is `r31-dr04.diff` (sha256 `1f03b05fc4014bb0`) alone**: delete dr04's fake branch. The fake half (`r93.diff`) is already on the integration head, and `git apply --check r93.diff` fails there (the verifier's measurement). Round 4's scratch merge applied `r31-dr04.diff` cleanly | — |
| RUN-N3: IR2-1's figure | text (below) | IR2-1's `1070 passed` was measured at `f97bfb7`, on `tests/integration/test_harness.py` + `apps/infrx-api/tests/contracts`, with the full two-file diff; the verifier measured 1075 on `382b625` + `0924cf2` + IR2-1. Run it with `INFRX_D_TASK` set to a free task port, because `test_v1_projection_pg` otherwise takes D1's shared 55432. The tasklocal hunk alone turns the e3b2 row red (1 failed); the full diff gives 19 passed | — |
| GATE-N4: a failure line can still be truncated | **left** (Limits) | `COLUMNS=400` keeps the message, but pytest's own assertion-repr truncation still shortens long operands (the bk01/bk01f lines). The W `perf_pilot` reds under a deep private TMPDIR are W's environmental fragility, routed to W; `failure_lines` attributed them at once, as intended | — |
| GATE-N5: api-test's D harness uses shared names | **left** (Limits) | `make api-test`, inside the gate's suites stage, uses D's `infrx-d1-postgres:55432` (unless `INFRX_D_TASK`) **and vkstore's `infrx-d2-valkey:55463`** (unless `INFRX_D2_VALKEY_PORT`/`_CONTAINER`). Both lie outside the e3b2 block. `INFRX_D_TASK=e3b2` cannot be used during the gate, because it maps to 56732, E2's own `infrx-e3b2-postgres`. So a busy shared name fails the suites stage through a nonzero exit (round-1 Limits 6). Moving vkstore in-block, and running api-test's D harness after the stack is down, is deferred | — |

### Round-4 runs

| # | Command | Exit | Tail |
|---|---|---|---|
| R4-1 | at `12862a2`: `INFRX_E2_NAMESPACE=e3b2 mutants.py --only <id>` for e2m69, e3bm54, e3bm55, e3bm56, e3bm57, e3bm58 | 0 each | all 6 `killed` (`r4-only.log e787f7295028554c`); death lines in the table (`r4-shape.log 22ee41bacb9e7320`) |
| R4-2 | anchor-count scan over `all_mutants()` at `12862a2` | — | `210 mutants scanned; stale: {'i3bm57': 0}`: I3B's only (`r4-scan-head.log 10a248733f767504`) |
| R4-3 | scratch: `origin/claude/backend-impl@047ebf2` + `git merge codex/e3b-phase2-gate@12862a2` (clean) + IR2-3 as `r31-dr04.diff` + `git merge codex/i3b-followup@95b7df7` (clean) + IR2-2 (the RESIDUAL block including G2 deleted, `RESIDUAL = {}`, **and `KNOWN_STALE = {}`**; `r4-ir22.diff c8fed49edddce331`); anchor scan | — | **`236 mutants scanned; stale: {}`** (`r4-scan-merge.log ed6da256e7a3f68e`). With `KNOWN_STALE` left as it is, the guard fails with `stale anchors (id: occurrences found): {}`: the strict entry forces its own removal |
| R4-4 | the same scratch: `test_run.py` + `test_stage.py`; layer 0 | 0, **0** | `61 passed in 11.00s`; **`152 passed, 98 skipped, 1 xfailed, 2 warnings in 24.81s`**. PENDING `[D5,G2-R1]` ×9, `[G2-R1,D5]`, `[G2-R1]` ×2, `[I2B-R4]`, `[M1-L2]` (`r4-merge-layer0.log 980934ec57e58cb8`) |
| R4-5 | at `12862a2`: `pytest -q tests/integration/test_run.py tests/integration/backend/test_stage.py` | 0 | **`61 passed in 10.53s`** (`r4-run-stage.log fce930f5002a2383`) |
| R4-6 | at `12862a2`: layer 0, `INFRX_E2_NAMESPACE=e3b2 pytest -q -rs tests/integration` | 1 | `2 failed, 145 passed, 91 skipped, 1 xfailed, 2 warnings in 18.73s`. Both failures are I3B's (the i3bm57 list case and rc10), which the I3B follow-up fixes (R4-4) (`r4-layer0.log 8a605d8af5f63659`) |

**Integration requests after round 4 (restated whole):**
- **IR2-1 (this lane's merge):** the TASK_BLOCKS diff plus the strict-xfail deletion, in one commit (`ir1-r2.diff`, sha256 `23d1719320510c57`). The verification command and figure are as in RUN-N3 above.
- **IR2-3 (this lane's merge):** `r31-dr04.diff` only (GATE-N3).
- **IR2-2 (the I3B merge):**
  - delete the `# RESIDUAL (merged; …)` block of `stack.PENDING`, G2 included, and set `RESIDUAL = {}`;
  - keep `stack.OWNERS`;
  - **set `test_run.KNOWN_STALE = {}`**, because i3bm57 is re-anchored there.

  Full hunk: `r4-ir22.diff`.
- **IR2-6:** the owner is named in round 3.

No container was started. The scratch clones are removed.

## Verification log

- 2026-09-23: Authored from the runs above. Every count is quoted from command output or the
  JSON reports. Status **implemented, gate not passed (FAIL on I3B's reds)**. The gate was run
  at `e57b820`, and `make integration --layer 1` at `e57b820`; `make check` ran at `f0de778`,
  which differs only in `fake_vllm.py`'s CLI default. Containers of
  other sessions seen on this host (`infrx-d2-*`, `gideon-migration-order-test-…`, other
  lanes' Q/D harness containers) were never touched. Every `infrx-e3b2*` resource this session
  created was removed.
- 2026-09-23 (round 2, second session): Round 2 appended from the runs R2-1…R2-10, each count quoted from command output or `l3-f883a0a.json`. The coordinator's WIP `c587ddc` was reviewed and kept. Only the bk01 wording was corrected in place (R92); every other round-1 correction is listed, not rewritten. Status **implemented, gate not passed (FAIL on I3B's reds only)**. Only `infrx-e3b2-*` / `infrx-e3b2rest-postgrest` containers and this lane's D-harness containers (`INFRX_D_TASK=e3b2`, 56732) were started, and all were removed (`docker ps -a | grep -c e3b2` → `0`). No other lane's container, no hosted project, no AWS and no box was touched.
- 2026-09-23 (round 2b): R3-1 answered in `e95bce5` and `cd8099b`. Runs R3-a…R3-h are quoted from command output: layer 0 at the head, and a scratch merge into `c37b42e`, with and without the I3B follow-up. Every earlier line is unchanged.
- 2026-09-23 (round 2c): I3B's R2-A (`d8384ed`, e3bm42) and R2-B (`dadbd79`, e3bm43) are committed at the coordinator's request. Runs R2c-1…R2c-5 are quoted from command output. Every earlier line is unchanged.
- 2026-09-23 (round 3): Confirmation `E3B2-confirm-b0a64e1.json` answered in `a18f567`…`b892ab0`. Runs R3-1…R3-8 are quoted from command output. `fa17b96` was committed with its own case red and fixed in `4c21b3b`; that is recorded, not hidden. The lane's stale D-harness lock file was removed. Every earlier line is unchanged.
- 2026-09-23 (round 4): Final verification `E3B2-verify-0924cf2.json` answered in `75fa1af`…`12862a2`. Runs R4-1…R4-6 are quoted from command output. The e2m69 regression (round 3) is attributed to this lane. Every earlier line is unchanged.
