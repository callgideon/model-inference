# E2C addendum — E3C requests WR-1..3 and the host port reservation

Follows [E2C-3a113dc](E2C-3a113dc.md) (unchanged). Coordinator update 2 relayed E3C's
phase-1 requests (codex/e3c-integration `a9d51333`, `research/plan/evidence/e/E3C-8406c79.md` on that
branch) and the P-21 host reservation. Local only; no hosted, box or AWS access.

## Source

| Field | Value |
|---|---|
| Base / previous head | `dff31efc` / `924b1bdb` |
| Head | `f61d2f0b` (this report follows in its own commit) |
| Changed | `tests/integration/{harness.py,compose.yaml,environment.json,README.md,ENVIRONMENT.md,gates.py,test_preflight.py}` |

## WR-1 — `e3c` harness namespace

`harness.NAMESPACES = {"e2": 0, "e3b2": 1200, "e4b": 1300, "e3c": 1400}`. The +1400 layout is
56900, 56923, 56932, 56979, 56980, 56990 — inside tasklocal's `e3c` compose block
(56900–56999) with PostgreSQL on tasklocal's 56932. `test_harness.py`'s four per-namespace
cases (tasklocal agreement, block containment, container names, rendered compose) now run for
`e3c` too: all pass. E3C's `world.load_harness` load-time row can be dropped once this merges.

## WR-2 — S3-compatible repin

| Check | Command | Result |
|---|---|---|
| Old pin pullable? | `docker pull quay.io/minio/minio@sha256:14cea493…936e` | exit 1: `unexpected status from HEAD request … 401 UNAUTHORIZED` |
| New digest resolves remotely | `docker buildx imagetools inspect pgsty/minio@sha256:b6bfe723…d602372` | OCI index; linux/amd64 manifest `2b36182f…`; tags `latest` and `RELEASE.2026-08-04T00-00-00Z` resolve to the same index (Docker Hub tag list agrees) |
| Contents | `docker run --rm --entrypoint minio <ref> --version`; image labels | `RELEASE.2026-08-04T00-00-00Z (commit-id=d88f46ccee345a9c2fabe2d221d9a9e56bc11aec)`, go1.26.5; label "MinIO community fork, build by pgsty", UBI9-micro base, AGPLv3 |
| Works as the stack runs it | `infrx-e2c-selftest-minio` on 127.0.0.1:55512, `server /data --address :9000`, the stack's root user/password; boto3 s3v4 path-style | `/minio/health/live` 200 (`Server: MinIO`); create bucket, put/get `test/e2c/probe`, list by prefix, `NoSuchKey` on a missing key; container removed |
| Pull failure stays BLOCKED | new `test_an_image_that_is_not_local_is_blocked_and_never_pulled` | absent digest → preflight BLOCKED with its `docker pull` line; preflight never pulls. `run.py` already maps a failed `compose pull`/`up` to PENDING (exit 3) |

Pinned in `compose.yaml` (with the reason and verification in the comment),
`environment.json` (`note` names the former pin), `README.md` and `ENVIRONMENT.md`. Preflight
now: `consumer-local` PASS, `integration-l2` PASS (risk: e2 ports exposed), `backend-certify`
PASS (same risk). Not done by me: a full `run.py --layer 2` on the repinned stack — that
provisions E2's `e2` namespace (55500 block), outside E2C's ports; E3C and M5 already run this
digest.

Outside my paths, still on the old pin: `apps/infrx-api/deploy/rehearse.sh:43`
`MINIO_IMAGE=quay.io/minio/minio@sha256:14cea493…` (wiring request 5 below).

## WR-3 — BACKEND-LOCAL stage in consumer-local

`gates.e3c_stage` runs `apps/infrx-api/.venv/bin/python tests/integration/backend/e3c/runner.py
--out <out>/e3c` as consumer-local's last stage and reads `verdict.json["verdict"]`. The
runner's interface was read from `a9d51333` (`--out DIR`, `verdict.json`, E2C's ranking and
0/1/3/3/4 exits). Rules: an absent runner (this tree) is NOT RUN; a verdict that disagrees with
the exit code is INVALID; exit 0 with no readable verdict is FAIL; an unknown verdict is
INVALID. Tests (fake runner): pass, fail, blocked, contradiction, no-verdict, unknown, absent —
7 passed; with the consistency check removed the contradiction case fails.

Checks at `f61d2f0b`: `pytest -q tests/integration/test_preflight.py test_harness.py
test_run.py` → 108 passed (47.98 s). `git diff --check` clean.

## Host port reservation (P-21)

`/proc/sys/net/ipv4/ip_local_reserved_ports` = `55432-55499,56379,56700-56999,58123,59000,59100,59110`
(`/etc/sysctl.d/60-infrx-task-ports.conf`). This closes finding 2 of E2C-3a113dc for tasklocal's
per-lane ports and the e3b2/e4b/e3c blocks (consumer-local's e2c ports are no longer at risk).
Still exposed: E2's block 55500–55599 (55500, 55523, 55532, 55579, 55580, 55590 — reported by
preflight as `ephemeral-overlap: risk`) and the E2C self-test block 55510–55519.

## Wiring requests (additions to E2C-3a113dc)

1. **Makefile** — add `backend-local` beside the three gate targets:

```make
.PHONY: ... backend-local consumer-local backend-certify app-e2e ...
backend-local:
	apps/infrx-api/.venv/bin/python tests/integration/backend/e3c/runner.py --out "$${E3C_OUT:-$${TMPDIR:-/tmp}/infrx-e3c}" $(E3C_ARGS)
```

2. Earlier request 4 (host sysctl): now only `55500-55599` remains to add to
   `/etc/sysctl.d/60-infrx-task-ports.conf`.
3. Earlier request 2 (tasklocal decoy collision: g8/e2c/i8 ports 55473–55476 vs the D decoys)
   is **unchanged and still needed**; the reservation does not affect it.
5. **`apps/infrx-api/deploy/rehearse.sh:43`**:
   `-MINIO_IMAGE=quay.io/minio/minio@sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e  # tests/integration/compose.yaml`
   `+MINIO_IMAGE=pgsty/minio@sha256:b6bfe7239bfc83fb90d31612d9704d86039dd714f7904b3f1ad68f211e602372  # tests/integration/compose.yaml`.
   Proof: `bash apps/infrx-api/deploy/rehearse.sh` reaches its MinIO step (the current pin
   cannot be pulled on this host).

## Estimate

Unchanged from E2C-3a113dc: 1 / 3 / 6 h (optimistic / likely / pessimistic), confidence
medium. The next measurement is consumer-local after the tasklocal fix, with E3C's runner
merged and the S3 endpoint provided.

## Verification log

- 2026-09-25: E3C WR-1..3 applied in E2C's paths; MinIO repin verified locally; host
  reservation recorded. The earlier record is unchanged.

## Fix round (completed 2026-09-25T01:54Z)

Head before the round `0598f16e`; the first fix agent committed `3838a67c` and `330e04f1` and
was killed mid-round; this agent verified each of its closures from the diff and by running the
tests, then committed `19444fd1` (code) and the record below. Findings are the three lenses'
(`0-` reviewer, `1-` refuter, `2-` acceptance). Lane suites: `tests/integration/test_preflight.py`
54 passed (29 before the round), `test_harness.py` + `test_run.py` 79 passed. Every mutant a
lens named now lives in `tests/integration/mutants.py` (`e2cg01`–`e2cg12`, `e2cp01`–`e2cp06`,
run by `run.py`'s mutation stage) and was killed on a pristine baseline (18/18, run through
`mutants.run_one` from a scratch driver; log in the coordinator's scratchpad, summary in the
table). `gates.py`/`preflight.py` now take the checkout from `harness.REPO_ROOT`
(`INFRX_E2_REPO_ROOT` inside a copy) so the list can copy them; behaviour outside a copy is
unchanged.

### Per-finding disposition

| Finding | Closed by | Regression (fails with the fix reverted) | Mutant |
|---|---|---|---|
| **B1** / **E2C-ACC-3** backend-certify starts a live run for `--target=URL` / `--tar` | `3838a67c` `remote_flags()`: exact, `=value` or any `--` prefix of `--box`/`--target` counts as remote (fail closed) | `test_a_remote_certify_flag_in_any_spelling_never_starts_a_run` (4 spellings; the launcher stub raises `LIVE RUN STARTED`) + `test_a_local_certify_run_is_started` (a detector calling everything remote fails) | `e2cg01` (detect `--box` only) killed; `e2cg02` (`remote = []`) killed |
| **M1** pytest stage: setup error / nonzero exit / no junit | `3838a67c`: `code != 0 or failed or errors` → FAIL; no junit → FAIL. `19444fd1`: only an empty parametrization ran → BLOCKED | `setup-error-is-fail`, `test_a_pytest_stage_that_crashed_without_a_report_fails`, `empty-parametrization-alone-is-blocked` | `e2cg03`, `e2cg04`, `e2cg05` killed |
| **M2** consumer_local composition untested | `3838a67c` `Composed` recorder tests: stage list and order, e2c env on every api stage, S3 up/down order, layer-1 fallback with a BLOCKED `integration-l3` row, S3 BLOCKED → no endpoint, interrupt removes S3; `19444fd1` adds the D-runner states | `test_a_green_consumer_local_composition_passes_on_its_own_namespace`, `..._without_the_stack_...`, `..._without_its_s3_endpoint_...`, `..._interrupted`, `test_the_d_mutant_case_is_not_run_...` | `e2cg06` (layer always 3), `e2cg07` (PENDING = PASS), `e2cg09` (e3c stage dropped), `e2cg10` (`INFRX_D_TASK=d1`) killed |
| **M3** runner stages ignore skips | `3838a67c` `runner_stage` reads the report's per-run `counts.skipped` → BLOCKED with the skipped runs listed; exit 0 without a readable report → FAIL. The stack cases run at layer 3 (`run.py --layer 3 --only-suites`) when the `integration-l3` preflight passes, else layer 1 + a BLOCKED `integration-l3` row (never PASS with the 103 layer-1 skips) | `test_a_runner_stage_passes_only_when_every_case_ran` (clean / skips-are-blocked / pending / fail / no-report) | `e2cg07`, `e2cg08` (skips ignored) killed |
| **M4** / **E2C-RS-3** / **E2C-ACC-9** suites i, j, q, t dropped | `3838a67c` `API_SUITES = (contracts, d, g, i, j, m, q, t, w)`, tests/q on e2c's Valkey (`INFRX_Q_VALKEY_PORT`) | the composition test discovers the suites from `apps/infrx-api/tests/*/test_*.py` and asserts one `api-<suite>` stage each | `e2cg11` (tests/i dropped) killed |
| **M5** preflight oracles (never pulls, version pin, platform, stale container) | `3838a67c`: stub `docker` on PATH that logs argv and would pull successfully; a `match` that cannot match → `mismatch` → BLOCKED; `sys.platform = darwin` → BLOCKED; a `containers()` list holding a prefixed name → busy for a profile without a docker tool (`names = containers() if wanted["namespaces"]`) | `test_an_image_that_is_not_local_is_blocked_and_never_pulled`, `test_a_tool_at_the_wrong_version_is_blocked`, `test_a_host_that_is_not_linux_is_blocked`, `test_a_container_left_in_the_namespace_is_blocked` | `e2cp01` (pull before inspect), `e2cp02` (version unchecked), `e2cp03` (mismatch counts ok), `e2cp04` (platform unchecked), `e2cp05` (stale ignored), `e2cp06` (containers only with a docker tool) killed |
| **M6** / **E2C-ACC-2** consumer-local can never PASS (S3 cases skip; empty parametrizations) | `3838a67c`: the gate starts the manifest's MinIO pin as `infrx-e2c-s3` on e2c's S3 port (`docker run --pull never`, credentials by name, health-checked, removed afterwards and on interrupt) and exports `INFRX_M_S3_ENDPOINT` + `INFRX_M_S3_LOCAL_CREDS=1`; `got empty parameter set` rows are listed under `counts.empty_params`, not counted as skips | `test_the_s3_endpoint_starts_from_the_local_pin_and_is_removed`, `test_an_s3_endpoint_that_cannot_start_is_blocked`, `empty-parametrization-is-not-a-case`, `empty-parametrization-hides-no-skip`; measured: api-m and api-w below | n/a (measured) |
| **E2C-RS-1** / **E2C-ACC-1** the never-pulled test cannot fail | as M5: the stub docker's `pull` succeeds, so a pulling preflight is seen in the log and fails the case | same | `e2cp01` killed (survived at 0598f16e) |
| **E2C-RS-2** the lane ran `make api-test` on the shared D1/D2 defaults | **Recorded, see "Isolation breach" below.** Every run in this round used e2c only | n/a | n/a |
| **E2C-ACC-4** integration-l2 drives other lanes' ports (e3b2d, 55469) | `3838a67c`: the stack stage is `run.py --layer 3 --only-suites --no-mutants` (never run.py's `make api-test`, whose `make_env()` pins e3b2d/55469); `330e04f1`: the `e2` namespace preflight also checks backend/stack.py's PostgREST pair (55530/55531, derived from `stack.py`), so a busy one is BLOCKED before the run | composition test asserts `--only-suites` on every runner argv; `test_the_environment_manifest_is_self_consistent` asserts the PostgREST ports are in the `e2` namespace | n/a |
| **E2C-ACC-5** api-d mutant copies default to 55432 | `19444fd1`: `api_d_stages()` deselects `tests/d/test_signup.py::test_code_mutant_is_killed` and adds an `api-d-mutants` **NOT RUN** row while `tests/d/signup_mutants.py`'s Runner names no `INFRX_D_TASK`; the whole suite once it does (wiring request E2C-FR-1, exact patch below). Replaces `3838a67c`'s `"isolation": "partial"` annotation on a running stage, which still bound 55432 | `test_the_d_mutant_case_is_not_run_until_its_copies_inherit_the_task` (exit 3, NOT RUN, `--deselect` in argv) and the green composition test (full `tests/d` with the env) | `e2cg12` (case run regardless) killed |

Minor findings: **m1**/**E2C-RS-4**/**E2C-ACC-6** — ENVIRONMENT.md now describes the private
`mkdtemp` basetemp and states that the runner stages (`run.py`, `certify.py`) are not
mitigated; **m2**/**E2C-ACC-10** — the e2c row says the Valkey/S3 ports come from tasklocal
(55474/55475 on this branch, 55493/55494 on `claude/consumer-v1`, which wins at merge);
**E2C-RS-5** — layer-1 count at the head: see the measurement table; **E2C-RS-6**,
**E2C-ACC-7**, **E2C-ACC-8** — not done in this round (open below).

### Isolation breach (E2C-RS-2), recorded

The slice-3 base and head `make api-test` runs in [E2C-3a113dc](E2C-3a113dc.md) (lines 65–72,
91, 174) ran without `INFRX_D_TASK`, so they used D1's `infrx-d1-postgres` on 55432 and D2's
Valkey lock `/tmp/infrx-d2-valkey-55463.lock`; attempt 1 created an `infrx-d1-postgres` that
failed to bind and was then removed by the lane; the coordinator's S3 reconciliation recorded
an "owner unknown" `infrx-d1-postgres` at ~21:39Z that matches the base run. LANE-RULES §3
allows only e2c's ports and prefix. No data damage was found (the harness flocks serialised
access; the removed container was the lane's own), but the counts in that section were not
taken on the lane's namespace. Every run in this round used `INFRX_D_TASK=e2c`,
`INFRX_D2_VALKEY_PORT`/`INFRX_Q_VALKEY_PORT` = e2c's Valkey, `INFRX_M_S3_ENDPOINT` = e2c's
S3, and the D mutant case that has no per-task override was NOT RUN, not run on 55432.

### Measurement: consumer-local on the lane's ports (19444fd1)

Run from a scratch driver over `gates.main(["consumer-local", …])` with two recorded
deviations (both in the verdict's `argv`): e2c's ports set to the coordinator's assignment
55448/55493/55494 (`claude/consumer-v1`'s tasklocal; this branch's tasklocal still has the
D-decoy ports 55474/55475, wiring request 2 of E2C-3a113dc), and the `integration-l3`
preflight forced BLOCKED because `run.py --layer 3` provisions E2's `e2` namespace (55500
block), which is not this lane's — the coordinator runs layer 3. A leftover `infrx-e2c-s3`
(label `ai.infrx.e2c.checkout` = this worktree, created 01:25:39Z by the killed agent's run,
which never reached `s3_down`) was removed first. PID 379488, log
`<scratchpad>/e2c/gate-run.log`, verdict `<scratchpad>/e2c/gate-out/verdict.json`.

Run in progress at the time of this record (started 01:44Z; the api suites take ~45 min on
this host). Stage files so far: `api-contracts.log api-contracts.xml api-d.log preflight-consumer-local.json s3.log `. api-contracts: 1078 passed, 0 failed, 0 errors, 0 skipped, 0 xfailed. preflight:consumer-local PASS on
namespace ports [55448, 55493, 55494], no risks; s3 started (`infrx-e2c-s3` on 55494, `--pull
never`). The next E2C update file carries the finished verdict (`verdict.json`) and the
per-suite counts; the driver removes `infrx-e2c-s3` when the api stages end.

### Wiring requests (this round)

- **E2C-FR-1** `apps/infrx-api/tests/d/signup_mutants.py:212` (D lanes / coordinator):
  ```diff
  -RUNNER = shared.Runner(name="a1", targets=("tests/d/test_signup.py",))
  +RUNNER = shared.Runner(name="a1", targets=("tests/d/test_signup.py",),
  +                       env=("INFRX_D_TASK", "INFRX_D2_VALKEY_PORT", "INFRX_D2_VALKEY_CONTAINER"))
  ```
  Proof: `tests/contracts/test_mutants.py` (`env-probe`) shows the shared runner copies the
  named variables into a copy; with the change, `INFRX_D_TASK=e2c uv run --frozen pytest -q
  tests/d/test_signup.py -k code_mutant` must create only `infrx-e2c-postgres`
  (`docker ps -a --filter name=infrx-d1-postgres` unchanged). The gate then runs the case
  automatically (`api_d_stages` reads the Runner file; `test_a_green_consumer_local_composition…`).
- Earlier requests stand: Makefile targets `consumer-local`/`backend-certify`/`app-e2e`/
  `backend-local` (E2C-3a113dc request 1, E2C-f61d2f0 request 1); tasklocal decoy ports
  (request 2, resolved to the integration side on `claude/consumer-v1`); pytest ≥ 9.0.3
  (request 3); host sysctl `55500-55599` (request 4); `deploy/rehearse.sh` MinIO pin (request 5).
- **E2C-FR-2** `tests/integration/run.py` (E2's module; the gate composes it, not owned by
  this round): at layer 3, `suites()` reruns `tests/integration` after `backend()` has torn
  down PostgREST, so the two journey cases that need it skip and `integration-l3` is BLOCKED
  by construction (read from `run.py`; not measured here — layer 3 is the coordinator's run).
  Either keep PostgREST up until `suites()` has run, or have `suites()` at layer 3 leave out
  the backend suite its own stage already ran.

### Open (not fixed in this round, and why)

- `integration-l3` PASS is not demonstrated: the lane may not provision E2's `e2` namespace
  (LANE-RULES §3; the previous addendum made the same call), and E2C-FR-2 above means the
  stage would be BLOCKED even then.
- E2C-RS-6 (preflight duplicates harness's docker/image/port checks): left; a refactor with no
  behaviour change was not worth the round's budget. E2C-ACC-7 (quarantine owner/reason/expiry
  as a mechanism; cleanup/permissions/accounting seams): not built — the effect is conservative
  (BLOCKED) and the tests/d xfails belong to the D lanes. E2C-ACC-8 (secret shapes in profile
  values): not built; E1C owns the schema.
- The runner stages (`run.py`, `certify.py`) still use pytest's shared `/tmp/pytest-of-<user>`
  (GHSA-6w46-j5rx-g56g) until the pytest bump (request 3).
