# F2-py r5 — evidence pointer

`9ca0083` is the **evidence commit** of the r5 pass; the report itself is
[`F2-py-4657d02.md`](F2-py-4657d02.md), named for its implementation SHA, and the r6
corrections to it are appended there. This file existed only because a correction was
once written to the wrong name; it holds no claims of its own.
