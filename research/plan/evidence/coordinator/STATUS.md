# Task pipeline board (coordinator-owned, restart reconciliation source)

A task is complete only when every column is filled. A session restart can kill an agent between any two columns, so after any restart the coordinator re-derives this board from git (`git worktree list`, `git log <base>..<branch>`, `git status` in each `codex-*` worktree) and resumes each task at its first empty column. An uncommitted change in a task worktree means an agent died mid-edit: the next agent for that task reads it and continues. Nothing is marked from an agent's claim alone.

| Column | Meaning |
|---|---|
| Impl | implementation + evidence commits exist on the task branch |
| Review@head | an independent review returned `pass` **on the current branch head** (a review of an older head does not count) |
| Coord | coordinator reran the task commands and read the diff |
| Integrated | merge commit on `claude/infrx-impl`, suites rerun on the merged tree |
| Post-merge | independent critical review of the merged stage completed, findings dispositioned |

| Task | Branch | Impl | Review@head | Coord | Integrated | Post-merge |
|---|---|---|---|---|---|---|
| F1 | `codex/f1-gateway-factory` | `e9c7b26`, `97c43ea`, head `1c2e333` | r1 `fix_required` at `164074b` (2 findings, both fixed); r2 killed by restart — **gap**, covered by post-merge review | done 19:12Z | `be7e984` | **pass** 19:40Z (S0) |
| E1 | `codex/e1-corpus-bench` | head `ca2e2db` (64-clip corpus with real hashes, 32 fast subset, allowlist-output bench client) | **pass** at `ca2e2db` (round 6; rounds 1–5 `fix_required`) | suite 39 passed offline, validate 0 errors, verify 72/72, owned paths only, bench.jsonl history byte-identical, notes.md +44/−0 | merged (see log) | covered by S1 |
| I1 | `codex/i1-inventory` | head `a98f518` (30 commits; inventory, deploy design, committed checker) | **pass** at `a98f518` (round 8; rounds 1–7 `fix_required`) | checker rerun exit 0, tree clean, owned paths only, secret scan clean | merged (see log) | covered by S1 |
| F2-py | `codex/f2-contracts-py` | head `7be0d42` (records, money, errors, ports, config names, 40 fixtures, executable fakes, 136 conformance cases, exhaustive trace sequence property test, 188-mutant runner, pinned uv env, Makefile) | **pass** at `7be0d42` (round 9 under R40; rounds 1–8 `fix_required`) | clean-env `uv sync --frozen`, 542 passed, lock consistent, F1 baseline byte-identical, owned paths only | `12c70c8` | S1 running |
| F2-ts | `codex/f2-contracts-ts` | head `33218de` (31 commits; DTO types, money, fixtures, fake services, 45 exported conformance cases, 139-mutant runner with 12 self-tests, recursive discovery + guard) | **pass** at `33218de` (round 7 under R40; rounds 1–6 `fix_required`) | test 124, test:mutants 139/139, lint 0, tsc 0, package.json diff = test/test:mutants/engines only, lockfile + tsconfig untouched | `70d0f9b` | covered by S1 |

Stage reviews: **S0** F1 merged — pass · **S1** G0 contract-ready (F2-py + F2-ts + cross-language vocabulary parity merged; also covers E1/I1 merges) · then one per merge lane of `03-execution-protocol.md` (execution lane, observability lane, console lane) and one before any gate claim (G1, G2).

All other manifest tasks: `planned`, blocked on F2 (or later dependencies) — none started, none skipped.

## Log

- 2026-09-20 19:20Z: Board created after two session restarts. Reconciled from git: no task lost commits; F1 r2 review and E1/I1 fix agents were the interrupted units and are re-dispatched.
- 2026-09-20 19:40Z: **S0 post-merge critical review of F1: pass, no blocking findings.** Independent harness: old gateway vs legacy shim vs bare `create_app()`, 54 scenarios + config records, 8 environments (empty, all non-default, empty strings, malformed numerics), 0 differences; sensitivity drill (re-injected the `97c43ea` defect) detected. 29 passed; baseline test files byte-identical; order-dependence identical at base and head (pre-existing: `test_gateway_auth` must import first); import probe over all 13 `infrx` modules recorded no env read/client/app/file/queue; 400-request stress at `MAX_INFLIGHT=8` identical (peak 8, final 0); `git revert -m 1 be7e984` clean in a scratch clone (note: it also removes the F1 evidence files — keep them on any rollback). Console rerun on the merged tree: 4 passed, lint exit 0, `apps/app` byte-identical to `25b9829`.
  Dispositions of nonblocking notes: (a) never use `unittest.mock.patch.object` on the `gateway` shim — use `create_app()` or plain assignment → rule for all track briefs; (b) names the shim rebinds on modules (`hmac`, `put`, `cost`, `address_allowed`, `THINK`) are process-wide in tests → track tests use `create_app()`, never the shim; (c) `clock` default binds `time.time` at definition → tests inject a clock; (d) pre-existing inflight leak when ASGI `send` fails before the generator starts → owner G2 (API-STREAM), carried in its brief; (e) `Settings.usage_failed_log` derived once → note for config consumers; (f) strengthen the import-side-effect test to walk the package → F2-py follow-up; (g) README/HANDOFF still describe logic in `gateway.py` → README in F2-py scope, HANDOFF by coordinator at G0.
- 2026-09-20 20:50Z: Wave-1 workflow completed (16 agents, none lost). No task reached `pass` at head within its two review rounds, so nothing was merged. Coordinator issued contract revision r1 (`08` §10, commit `efce300`) answering every change request, and dispatched one fix agent per task with the latest findings (`.claude/handoff/<task>-review.json`) plus an instruction to close each defect class, not the instance. Each fix is followed by a fresh independent review at head before merge.
- 2026-09-20 22:10Z: Round status (nothing merged since F1; every task still lacks a `pass` at head). **E1** head `73c210c` reviewed `fix_required`: corpus/scheduling/denominators/guard/CLI all pass; redaction bypassed five ways → coordinator ruled an allowlist design (structural URL label, no server text recorded, argv key refusal, incremental raw rows); fix agent resumed. **I1** head `657d6a4` reviewed `fix_required`: read-only discipline proven (1,105 CloudTrail events all ReadOnly) and 25+ OBSERVED quotes exact; KMS proposal protected the wrong direction, hand-typed counts wrong, one unowned operation → ruled a permissions-boundary Deny as the required outbound control and a committed checker (`evidence/i/check_i1.py`) as the only source of counts; fix agent resumed. **F2-ts** head `7ccebfe`: class sweep done (validate→replay→write→record, keyset cursors), 51 tests; review running; its five requests ruled as R16–R19 (`e22a17b`). **F2-py** head `a0ced8f`: 470 tests, six findings + 11 class instances fixed with 22/22 revert-proofs; its four requests ruled as R20–R23 (`f1f1f9d`, incl. billable causes = completed/client_cancelled/client_disconnected only); agent resumed to apply them, then independent review.
- 2026-09-20 23:05Z: **Live-risk finding surfaced by the I1 review (not yet fixed anywhere; no production change made or authorized):** `apps/infrx-api/deploy/install.sh:12-16` swallows every `ssm get-parameter` error, line 21 truncates `/etc/marlin2b-gateway.env`, line 35 restarts the gateway, and `authenticate()` allows every request when neither `GATEWAY_API_KEY` nor `SUPABASE_URL` is set. Any parameter-read denial or SSM outage during an install run therefore leaves an unauthenticated, unmetered public gateway on the pilot hostname. Owners: I2 (installer aborts without touching the env file or restarting) and G1 (runtime refuses to start in `pilot` mode without auth + metering). Hard prerequisite of every IAM/profile change in the I1 design. Carried into the G1 and I2 briefs.
- 2026-09-20 23:05Z: Round status. **F2-py** `1e2454f` (477 tests, R20–R23 applied, no open questions) — independent review r3 running. **E1** `5f72fce` (allowlist redesign; 176-cell leak matrix 14→0, generative test, per-row flush) — re-review running. **I1** `64d1feb` reviewed `fix_required` (boundary lacked an `Allow` — coordinator's ruling wording was wrong and is corrected; inconsistent parameter path; self-descriptions still false) — fix agent resumed with a delete-don't-correct rule and checker hardening. **F2-ts** fixing B1–B3 + R16–R19, R22.
- 2026-09-21 00:20Z: **I1 integrated.** Review round 8 `pass` at `a98f518`; coordinator reran the committed checker (0 failed) before and after the merge; API suite on the merged tree 29 passed. I1 has no integration dependencies → status `integrated` (documentation and design only; nothing deployed, no resource created; every AWS call across all rounds was read-only per three independent CloudTrail lookups). Unblocks I2 once F2 lands. Reviewer notes carried into the I2 brief: evidence fifth-pass "57" superseded not removed; §Source lists `20a15d5` twice (hardening commit is `23f7991`); `M-ROLE` verification still says "no `*` action"; grant only `ssm:GetParameter`/`GetParameters`; add `kms:DescribeKey` to an enumerated boundary allow-list and consider `kms:ViaService`/encryption-context conditions; `ssm:DescribeParameters` keeps names listable; re-key (`M-KMS`) after the profile swap; state what substitutes for the staging install-abort drill if no staging host is allocated. **`O-FAILOPEN`/`M-FAILCLOSED` is step 0 of I2 and a G1 requirement.**
- 2026-09-21 00:25Z: **Correction to the previous entry.** "Checker rerun (0 failed) before and after the merge" is wrong for *after*: on the merged tree `check_i1.py` exits 2 — `branch touched owned paths only` and, as a consequence of that changed line, `quoted §Checks block matches a fresh run`. Both checks are scoped to the task branch (`git diff --name-only 25b9829..HEAD`), and on `claude/infrx-impl` that range legitimately contains F1 and coordinator commits. On the task branch head `a98f518` the checker exits 0 (coordinator run and reviewer run). All document checks (ids, references, matrix, manifest digests, SHA) pass on the merged tree. Follow-up for I2 (owner of `evidence/i/`): make the two git-scoped checks apply only when HEAD is a `codex/i1-*` branch, or take the path scope from the merge's second parent.
- 2026-09-21 01:00Z: Round status. **Integrated:** F1 (`be7e984`, S0 pass), I1 (`ee4688d`). **F2-py** `1e2454f` reviewed `fix_required` (r3): 5 protocol violations in the fakes (caller lease stored by heartbeat, unenforced preparation deadline, forgeable terminal journal event, cross-tenant judge run read, reservation freed while a submission may be in flight) and 13 surviving mutants = vacuous conformance cases; rulings R27–R32 (`eda0a63`); fix agent resumed with a committed mutation runner as the key deliverable. **F2-ts** `f947a15` reviewed `fix_required` (r4): fake sound and Python parity exact (27/2/8 codes, money cases byte-identical), but 31 claimed-invariant mutants survive the exported conformance suite and one case decodes opaque cursors; rulings R33–R36 (`b86fc0d`); fix agent resumed, mutation runner required. **E1** `5f72fce` reviewed `fix_required` (r5) on one narrow item (URL basename/hostname can carry a secret) → hashed URL labels + 4 hardenings; fix agent resumed. Lesson applied program-wide (R32): a conformance case counts only if a single-edit mutant of its invariant fails it; wave-2 briefs will require the same of every track suite.
- 2026-09-21 01:40Z: **E1 integrated.** Review round 6 `pass` at `ca2e2db`; coordinator verification on the branch head and reruns on the merged tree: `pytest models/marlin2b/tests` 39 passed, API suite 29 passed, `build.py verify` 72/72 against the shared cache. E1 has no integration dependencies → `integrated` (tooling only: no load test was run, PERF-PILOT and MEDIA-PARITY remain unproven until E4; the upload form is client-side only until M3/G4). Unblocks E2 (with F2) and W3's integration dependency. Reviewer notes carried into the E2/E4 briefs: `secret_grams()` should match `fold(prefix) + "-"` (keys like `sk-marlin2b…` lose up to four folded 8-grams at the gate; exact-case redaction at the sink has 0 misses; unreachable with the real `sk-infrx-` format); digest labels are per URL not per object; SIGTERM keeps rows but writes no summary (E2's abrupt-exit drill should expect that); hostile-server third-party tokens can be recorded in allowlisted fields (disclosed); raw-row schema change (`error_message` removed; `error_type`, `body_sha256`, `body_bytes`, `upload_status`, `interrupted` added); exit codes 2/130/1/0.
- 2026-09-21 03:10Z: Round status. **Integrated: F1, I1, E1** (E1 at `284aab5`). **F2-py** `f395bce` reviewed `fix_required` (r4): all r3 defects closed and 132/132 committed mutants genuine, but 14 of the reviewer's 45 new mutants survive on claimed invariants (incl. R29 not enforced on `append`/`complete`, hold checked against ledger total instead of available), the runner counts a collection error as a kill, 12 cases still drop assertions silently when a hook is missing, three evidence misstatements. **F2-ts** `15f067f` reviewed `fix_required` (r5): fake sound under direct attack (R33–R35 behaviour correct), but the runner counts any non-zero exit as a kill, the catalogue contains one false kill (IDEM-05), and ~20 claimed invariants are still unenforced by the exported suite (adminAudit role with target filter, R35 vacuous has_feedback check, replay adds no audit entry, operatorMember matrix, restore). Rulings R37–R41 (`46a53e0`, `56b2e55`): capture lifecycle, queue wait = time spent queued (interactive retries possible), terminalize-then-refuse ordering, **explicit F2 merge criterion (R40)**, operator principals never shown to customers. Both fix agents resumed.
- 2026-09-21 05:00Z: Round status under the R40 merge criterion. **F2-py** `79afb1c` (534 tests, 157/157 mutants genuine, runner sound, hookless factory skips visibly) reviewed r5: criteria (a) and (b) **met** — 0 survivors on claimed invariants out of the reviewer's 89-mutant corpus; blocked only by three small items (R37 not declared on the `ports.py` Protocols and an `abandoned` double count; R39 capacity check 1–3 bytes short at reserves under ~100 bytes; three evidence statements). **F2-ts** `e3dc89e` (120 tests, 130/130 mutants) reviewed r6: 237/253 corpus mutants killed by named cases; blocked by four small items (operator feedback principal visible to customers — R41; suspension accounting invariance no longer enforced; member-operator `adminOrgs` content; runner counts a runtime crash inside a declared case as a kill). Both fix agents resumed with exact lists. Notes for D1/T1/G1/W1/Q1/C1/U1/V1 briefs and follow-up F2.1 are retained in the reviewer reports and will be folded into the wave-2 briefs.
- 2026-09-21 06:40Z: **F2 console half integrated at `70d0f9b`.** Review round 7 `pass` under R40: reviewer corpus of 264 mutants → 255 killed by named exported cases, 2 equivalent, 1 fake-only by nature (V03: record committed with its effect — not observable through `ConsoleServices`; C gets it from its transaction), 3 killed by the direct money suite, 3 contrived/unclaimed, **0 surviving on a claimed invariant**; runner refuses false kills (7 injected mutants → runner errors). Reruns on the merged tree: `pnpm --dir apps/app test` 124 passed, lint exit 0. **`tsc --noEmit` exits 2 on a checkout that has never generated Next route types** (`PageProps`/`LayoutProps` come from `.next/types`); after `pnpm exec next typegen` it exits 0 — pre-existing at `25b9829`, not an F2 regression (the task worktree had the types from a build). Canonical typecheck is therefore `next typegen && tsc --noEmit`; coordinator adds `make console-typecheck` with the F2-py Makefile. F2 stays `in-progress` until the Python half merges and the G0 parity test lands. Filed for **F2.1** (non-blocking per R40): Q18 customer replay of an operator-owner's `settings.update` key must be projected like reads; Q16 exact-field-set pins for `FeedbackEntry`, `ConsentHistoryEntry`, `LedgerEntry`; `walkAll` should `assert.fail` instead of throwing; self-test label `SELF-V03-REPLICA` is really the IDEM-05 variant. Brief notes for C1/U1/V1 retained from the review (store principal + project on read incl. replays; grant reason is customer-visible; list row is `TraceListItem` only — V needs a contract-revision request for quality/status filters or extra columns).
- 2026-09-21 09:30Z: **F2-py** reached R40 (a)/(b) at `79afb1c` and has since been held only by small trace-fake items: r6 (no-op capture trusting the envelope; `m20b`), r7 (minimal-with-content guard half unpinned — a privacy invariant, fixed not deferred; evidence placement), r8 narrow review of `096615c`: both closed, 184/184 mutants genuine, coordinator clean-env rerun 539 passed / lock consistent / baseline byte-identical / owned paths only — **but the r7 `_close` rewrite regressed to double loss counting** on two routes (3,914 of 124,416 probe sequences). Ruling R42 (`fd21b21`): idempotent per-capture loss counting, silent off-mode captures, and the reviewer's sequence probe committed as an exported property test so the class cannot regress. Fix agent resumed. G0 parity test is staged (`.claude/handoff/test_parity_console.py`): dry run shows exactly one drift — console `TRACE_LOSS_REASONS` lacks `abandoned` (R37) — to be fixed by the coordinator when the test lands.
- 2026-09-21 11:30Z: **F2 Python half integrated at `12c70c8`; gate G0 closing commit follows.** Review round 9 `pass` under R40 (reviewer corpus 113 live mutants: 109 killed by named exported cases, 1 equivalent, 3 harmless, **0 surviving on a claimed invariant**; 188/188 committed mutants genuine; runner refuses false kills and hides no skip; the reviewer's 124,416-sequence probe: 0 violations of the R42 property set). Coordinator G0 work on the merged tree: added `apps/infrx-api/tests/contracts/test_parity_console.py` (12 shared vocabularies, error-code tables 27/2/8, byte-identical `money_cases.json`), fixed the one drift it found (console `TRACE_LOSS_REASONS` lacked `abandoned`, R37), added `make console-typecheck` (`next typegen && tsc --noEmit`) and `make console-mutants`, corrected a dangling evidence filename (`F2-py-459233f.md` → `F2-py-a3b5277.md`). Canonical targets on the merged tree: `make api-test` 545 passed; `make api-mutants` all declared mutants killed; `make console-test` 124 passed; `make console-lint` 0; `make console-typecheck` 0; `make console-mutants` 139/139; `make bench-test` 39 passed. F2 has no integration dependencies → `integrated`; **G0 contract-ready reached** (F1/F2 integrated, test discovery proven incl. the discovery guard, schema/ports/fixtures committed). Nothing deployed; no live verification claimed. F2.1 follow-ups (non-blocking per R40): console Q18/Q16 pins, `walkAll` assert.fail, self-test label; Python — strengthen four vacuous lattice assertions and add `crash` to the alphabet, drop the wall-clock `elapsed < 60` assertion, document the `stats()` keys, `TraceOfferResult.dropped` comment, `n33`–`n35` unpinned by design. **Stage review S1 launched** on the merged tree before any wave-2 dispatch.
- 2026-09-21 13:10Z: **Stage review S1 of the merged G0 tree (`3b875a7`): `fix_required`, narrow.** Confirmed: clean-checkout `make check` exit 0 with a scrubbed environment and no secrets; all five merges lossless (`git diff <branch-head> <merge>` empty over each task's paths); no conflict markers, media or secrets; no NEW test order-dependence (only the pre-existing legacy one); same-named track tests isolate; discovery guard works; all extras import and `infrx.contracts` loads none; ten durable invariants hold on the fakes (own scratch tests); seven numeric claims reproduce. **Blocking before the affected tracks:** B1 — the two halves enforce contradictory feedback/calibration contracts (Python stores labels as `name=comment`, free-text `calibration_set`, no `rubric_version`; console uses `calibration_label`, closed vocabulary, boolean membership, integer rubric), R35/R41 applied on the console only (Python `list_owned`/wire return operator labels and principals to customers), `rubric_version` str vs number → blocks D1, J1, C1. B2 — `validate_pilot` exists but nothing calls it and the composition root is coordinator-owned, and 08 §5 defaulted `INFRX_MODE=dev` while the I1 design requires "unset refuses" → blocks G1. **Correction of the coordinator's own G0 entry:** "cross-language parity" there covered vocabulary, error codes and money cases only — record-level and ruling-level coherence was not checked and did not hold; G0 stays reached for F1/F2 integration and discovery, with coherence patch **F2.1** required before D1/J1/C1/G1 dispatch. Rulings R43–R48 issued. Coordinator actions taken from S1: rulings; F2.1 patch dispatched (`codex/f2-1-coherence`); docs refresh (HANDOFF/CLAUDE/plan README) by coordinator; brief rules R48 folded into the wave-2 brief. Tracks untouched by B1/B2 (Q1, W1, M1, T1, U1, V1) may go once F2.1's R46 port change (which W1/M1 consume) is merged — so wave 2 waits for F2.1 as a whole.
- 2026-09-21 15:20Z: **F2.1 coherence patch implemented** on `codex/f2-1-coherence` (head `e673484`, base `1058494`): R43–R48 applied (feedback/calibration/rubric one shape on both halves incl. Python customer invisibility through one `for_viewer` projection; injectable price source; `Lease.kind`, `claim_preparation`, fenced `prepared`/`load_work`, `MediaStore.attach` as a port op; trace export without storage keys; `validate_runtime` called from `create_app` with no `INFRX_MODE` default; parity test hardened with strict status-table parsing, shared bounds and record-field parity; lattice assertions strengthened). Its new checks found two further bugs and fixed them: `wire.UploadCompleted` leaked `storage_ref` in the completed-upload body; `FakeTraceSink.crash` counted `shutdown: 0`. Implementer-reported: `make api-test` 604 passed, `make api-mutants` all declared Python mutants killed, console test/lint/typecheck green, `make console-mutants` all declared killed, legacy byte identity empty, both test orderings exit 0. **Two independent reviews running at head**: the Python-contract reviewer (accumulated mutant corpus + ≥30 new mutants on R43–R46) and the S1 stage reviewer (cross-half seams, B1/B2 closure, per-track fan-out readiness). Docs refresh from S1 action 5 committed on the integration branch (`ce5b2a2`). Wave 2 waits for both verdicts.
- 2026-09-21 17:00Z: **F2.1 reviewed twice at `e673484`.** Stage reviewer (S1 follow-up): `pass_with_actions` — B1/B2 closed at contract level, clean `make check` exit 0 with a scrubbed env, no new order-dependence, conflict-free merge, 48 parity mutations (S1's original 12 now all caught), every wave-2 track ready or ready-with-brief-note; residuals F1–F4 → rulings R49–R52. Python-contract reviewer: `fix_required` — R46 fencing held under 40 direct attacks, R45 price source correct, R47 no wire model can carry a storage key, 220/220 committed mutants genuine — but: (B1) an idempotent replay through `accept` returned an operator's calibration label and principal to a customer; (B2) money hazard created by R45 — the hold stayed caller-computed while only the store knows the rates, so a price move between validation and admission made a valid completion settle as a platform failure → ruling **R53: the store derives the hold**; (B3) `pilot` still honoured the shared legacy key and accepted whitespace-only settings → R51; (B4) 8 surviving mutants on claimed invariants (price from request again, settlement at current price, cross-tenant media in `prepared`/`prepare`, `load_work` returning current price/budgets, record validator halves); (B5) mutant count mis-measured (220, not 222). Rulings R49–R54 committed (`3408fd6`, `0e89d1f`); fix agent resumed with the combined list incl. the R48 task-local port patch, field-type parity and the code-point unit for shared text bounds.
- 2026-09-21 19:10Z: **F2.1 round 2 (`055fe96`) reviewed: `fix_required`.** Closed and verified under attack: replay privacy leak, store-derived hold (incl. 10-way concurrent admission never negative, replay after price move returns the original hold), pilot refusals (shared key, blank settings), R52 retries on default limits (three claims, fourth loss terminalizes, hold unchanged), R49/R50/R54 (no wire model can carry `by_operator`; strict records; `tc_` handles; code-point bounds byte-identical in both halves). Coordinator rerun at head: tree clean, delegated paths only, protected files untouched, conflict-free merge, scrubbed-env `make api-test` 662 passed. **Still blocking:** the R52 lease clamp (a coordinator ruling) made R29's same-operation terminalization dead code on the preparation path — late `prepared`/`heartbeat`/`load_work` answer `StaleLease` and leave the job `preparing` with its hold reserved; six surviving mutants (superseded preparation worker's heartbeat accepted — fencing; hold rounded half-up instead of ceiling — money; replayed admission re-deriving the hold from the current price; unbounded preparation re-claims; `attach` storing refs before refusing — tenant; requeue event mislabelled); three evidence statements. Ruling R55 (`ae632c0`): attach reads the tenant from the job row, admission range-checks token ceilings, typed unknown dispatch kind, operator role implies `by_operator`, phase deadline enforced before lease expiry. Fix agent resumed.
- 2026-09-21 22:40Z: **F2.1 integrated at `a666680`** (branch head `31e9d33`). Round 3 review: round-2 blockers closed and held under attack (stale-worker attack: a superseded or foreign worker never terminalizes for the new holder); two one-case survivors (`t05` output-ceiling upper bound, `t02` inference deadline-before-expiry) + fixture honesty + stale count → fixed in round 4; **coordinator verified the closure directly**: applied the reviewer's exact `t05` and `t02` mutants in a scratch copy — each now fails a named exported case (`dur_admit__the_token_ceilings_are_range_checked`, `dur_fence__an_overdue_inference_lease_terminalizes_in_the_same_call`) and the suite is green when restored; the fixture test really calls `admit`; tree clean; protected files untouched. `make check` on the merged tree: exit 0 (api-test 670 passed; api-mutants 276 passed = all declared mutants killed + runner self-tests; console-test 126 passed; lint 0 errors; typecheck 0; console-mutants 144/144; bench-test 39 passed). Rulings in force: R1–R55. **Wave 2 dispatched from the commit that adds this entry**: D1, M1, Q1, W1, G1, T1, J1, C1, U1, V1, E2 — one Opus 5 implementer each in its own `codex/<task>-…` worktree, brief = `.claude/handoff/wave2/<TASK>.md` (common brief + S1 rules + per-track notes from the reviews); each gets an independent review at head (mutation-tested) before merge; stage review S2 after the first dependency-safe group merges. F2.2 follow-ups (non-blocking): parity on remaining DTOs, 7 feedback mutants dying on the `InternalError` wrapper, `judge_budgets` per-org-per-period (D6).

## Wave 2 board (base `8744418`; briefs `.claude/handoff/wave2/<TASK>.md`; common review brief `.claude/handoff/wave2-review-brief.md`)

| Task | Branch | Impl | Review@head | Coord | Integrated | Post-merge |
|---|---|---|---|---|---|---|
| D1 | `codex/d1-durable-schema` | head `9d9857b` (impl `d840094`) | r1 `fix_required` (upgrade preserves every historical value and the RPC boundary holds; blocking: operator view unreadable by operators; R41 broken by three routes incl. fail-open `by_operator` default and operator columns on customer-readable tables; anon/authenticated can TRUNCATE ledger/usage/members/models; owner sets `api_keys.trace_mode` by INSERT; no `security_barrier` — leaky-function probe reads every wallet; consent revocation can be undone; tenant-coherence FKs and money sign checks missing; wallet total drifts under the deployed console's `addCredit`; 10 mutants surviving on claimed invariants; runner counts apply errors as kills) — ruling **R59**; findings `.claude/handoff/wave2/D1-review-r1.md`; fix pass running | — | — | — |
| M1 | `codex/m1-media-materialization` | head `7ce0dd8` | r2 `fix_required` on tests/evidence only (B1–B4 code fixes held: 348-form address corpus 0 holes incl. the legacy path, pinning/rebinding, timing, put_if_absent races; blocking: five surviving mutants on claimed SSRF/tenant guards — `is_reserved` is the SOLE guard for IPv4-compatible `::a.b.c.d` forms and its mutant was retired on a false equivalence; one false Limits sentence about `attach`) — findings `.claude/handoff/wave2/M1-review-r2.md`; fix pass running; coordinator verifies closure by applying the five mutants | — | — | — |
| Q1 | `codex/q1-memory-scheduler` | head `309fb1c` | r2 `fix_required` (B1/B3/B4/B5 proven; the B2 fix regressed unfiltered claims: `tag − V[kind]` starves a peer in the other kind behind any noisy backlog — slots [1, N+1, N+2] — and four single-edit mutants of that comparison survive) — ruling **R60** (two-level fairness); findings `.claude/handoff/wave2/Q1-review-r2.md`; fix pass running | — | — | — |
| W1 | `codex/w1-engine-adapter` | head `3294ead` | r1 `fix_required` (deny-by-type part scanner forwards untyped/other-typed parts verbatim — a customer URL can reach the engine; malformed engine payloads escape as untyped exceptions; usage accepted as authoritative when the stream contradicts it; the filter's held tail never reaches the event stream; 18 surviving mutants incl. generation deadline, tenant salt, usage validation) — ruling R58; fix pass running | — | — | — |
| G1 | `codex/g1-ingress-auth` | head `7edc108` (impl `4fa3f60`) | r1 `fix_required` (bounds, R58 allow-list, 401-in-every-mode and pilot refusals held; blocking: malformed bodies → 500 incl. a raw non-envelope 500; structure inside the 96 MiB cap unbounded — 3.4 M messages admitted, loop blocked 21.8 s, RSS +2.5 GiB; caller strings echoed/logged unbounded (log forging); zero skew margin on `deadline_at`; vacuous chunked-body proof + 14 survivors incl. the trace-off default; cutover request inexact) — findings `.claude/handoff/wave2/G1-review-r1.md`; cutover moved to G2/D2 integration; fix pass running | — | — | — |
| T1 | `codex/t1-trace-capture` | head `794b95b` (impl `d03cbbf`) | r1 `fix_required` (accounting half holds under the real sink; durability half: loop blocked ~2 s by statvfs/unlink in paused `flush` and `ack`; fd + orphan-file leak on every failed segment-header write — 300 flushes → 300 fds; cancelled flush or non-`OSError` writer failure leaks the budget for ever and loses accepted records uncounted; segment names reused after full ack + restart and frame index outside the CRC, so the "stable id" is not stable; R42 double count on writer-side drops; 8 mutants surviving on claimed invariants) — findings `.claude/handoff/wave2/T1-review-r1.md`; fix pass running | — | — | — |
| J1 | `codex/j1-judge-dryrun` | head `1ae8951` (impl `a01ee14`) | r2 `fix_required` (narrow: B1–B5 closed and every R56/R57 clause held under attack; remaining: `validate_json` lets `RecursionError` escape on a 20 KB payload; the worst-case product runs in the ambient Decimal context and can round DOWN — guard authorizes 5.56 against 5.56346 under `prec=3` (latent); `Rejected.detail` is 203 chars and echoes judge-output keys; five mutants survive on claimed clauses) — findings `.claude/handoff/wave2/J1-review-r2.md`; fix pass running; coordinator verifies closure by applying the mutants and repros | — | — | — |
| C1 | `codex/c1-console-repositories` | head `e91b288` | r2 `fix_required` (narrow: all r1 blockers proven fixed; remaining: `isOperator === true` unpinned on the authorization path + two more test gaps, NULL `key_id` from D1's LEFT JOIN denies the usage page, JSON-number money fabricates digits from 2^26, missing-function fallback forceable by an error string) — findings `.claude/handoff/wave2/C1-review-r2.md`; fix pass running | — | — | — |
| U1 | `codex/u1-usage-balance-ui` | head `7bbd38d` (impl `829127a`) | r1–r3 `fix_required` → fixed; r3 closure verified by the coordinator: reviewer mutants N34, K02, E07 applied at `7bbd38d`, each fails a named case (U1-T28, U1-T25, U1-T19), green when restored | merged-tree rerun FOUND an integration conflict (U1 deleted `usage/ranges.ts`, which merged V1 imports: 4 failing tests, typecheck error, V1 mutants 0/39) → coordinator seam `usage/ranges.ts` re-exporting the one definition; then console-test 185 pass, lint 0 errors, typecheck 0, mutants 144/144 + V1 39/39 + U1 61/61, `next build` ok | merged `184c5fb` as **implemented (fake-backed)**; `integrated` waits on C1 + D5 | S2 |
| V1 | `codex/v1-trace-list-ui` | head `7c7b124` (impl `eedb852`) | r1 `fix_required` → r2 **`pass`** at `7c7b124` (44 reviewer mutants, 0 surviving on a claimed invariant; B1–B3 re-verified on the rendered page; `retry()` recovery verified in headless Chromium) | rerun on the merged tree: console-test 157 pass, lint 0 errors, typecheck 0, tests/v mutants 39/39 | merged `9617a2e` as **implemented (fake-backed)**; `integrated` waits on C1 + T2 and the sidebar entry | S2 |
| E2 | `codex/e2-integration-harness` | head `de70190` | r1 `fix_required` (pinning, loopback binds, decoys, PENDING≠pass, canaries, RLS matrix held; blocking: teardown keyed by compose project name destroys another checkout's live stack and same-named foreign volumes; no test imports `run.py` — 7 mutants of exit mapping/canary/teardown survive + 8 on README claims; shipped mutation runner counts a child ImportError as a kill — comment-only edit 'killed'; seed reproducibility partly false) — findings + D1↔E2 rulings `.claude/handoff/wave2/E2-review-r1.md`; fix pass running (incl. E1's wall-clock bench bounds) | — | — | — |

Integration dependencies (manifest): U1 needs C1 + D5, V1 needs C1 + T2, C1 needs D1, G1 needs D2 — those merge as *implemented* code behind fakes; `tasks.json` marks them `integrated` only when the real dependency is merged and the real-service suites pass.
- 2026-09-22 00:40Z: Wave-2 handbacks so far: U1, V1, J1, M1, Q1 implemented; each has an independent review running at its head under the common review brief plus a task-specific attack list. Coordinator decisions recorded: `MediaStore.attach` is owned by track M (implemented by M1, maintained by M2/M3). Pending coordinator items from handbacks (to action at integration, none blocking the reviews): Makefile gating of per-track mutant lists (M, Q, U, V each ship one — add `api-mutants`/`console-mutants` coverage and gate Q's list behind `INFRX_MUTANTS`); config names `MAX_INDEX_ITEMS`/`MAX_INDEX_BYTES` for Q; sidebar entry for `/traces` and "Balance" relabel for `/billing`; V1 contract-revision requests (HTTP status-class filter, window-wide trace summary with an off-requests count) and J1's request to move `CandidateSource` into `ports.py` for T2; an approved per-MTok judge price row in `research/cross-cutting/cloud-pricing.md` before J2's live path (judge live submission stays refused until then).
- 2026-09-22 02:10Z: U1 and V1 reviewed `fix_required`; both fix passes running. C1 implemented, review running. Coordinator decision: the console gets NO new database dependency — C's named queries execute through D1-owned SQL functions/views via the existing Supabase client (RLS stays in play); C1's schema requests (`org_wallet_summary`, `by_operator` on ledger/feedback/consent history, ledger `actor`, operator audit relation, read views) were forwarded to the running D1 agent with "06-conformant names win; list every difference". Cross-task lesson applied: the prototype-chain allowlist class found in U1 was probed in V1 (clean) and is on C1's attack list. Coordinator-owned items queued for integration: sidebar entry `/traces`, "Balance" relabel, `(console)/layout.tsx` sidebar balance formatted through `Number` (R11), dead `components/credits-card.tsx` with an "Add credits" button, seams should take `now` from the context (fixture clock 2026-09-20T12:00Z).
- 2026-09-22 04:00Z: W1 and T1 implemented, reviews running. J1 reviewed `fix_required` → rulings R56–R57 (`356c4bb`: consent not retroactive, strata derived from raw facts, by_operator feedback is no customer signal, dedupe by request id, limited never passes; judge money guard recomputes the total and refuses everything that is not a finite positive total within budget in exactly `live` mode). Coordinator items queued for integration (F2.2): extract the trace loss accounting from `contracts/fakes/traces.py` into a neutral contracts module so T1's production sink does not subclass a `Fake…` class (no behaviour change); parameterise the contracts mutation runner's pytest target so W/M/J/Q stop duplicating it; add `TRACE_SPOOL_SEGMENT_BYTES`, `MAX_INDEX_ITEMS`, `MAX_INDEX_BYTES`, `CONSOLE_CURSOR_SECRET` to the configuration names; rule on W1's request that normalized messages keep one media content part per staged ref in order (shared builder attaches refs to string content today); move `CandidateSource`/`TraceCandidate` into `contracts/ports.py` for T2.
- 2026-09-22 06:30Z: C1 and M1 reviewed `fix_required`; both fix passes running. U1 round-1 fixes in (`148986d`), focused re-review running. Coordinator rulings given with the fix lists: M may tighten the shared `address_allowed` (site-local + an explicit deny-list of 6to4/NAT64/Teredo/ORCHIDv2/documentation/benchmark ranges so the policy does not depend on the interpreter's `ipaddress` tables — upstream CPython 3.12.0–3.12.3 lacks ranges this host's patched build has; note for I2: pin Python ≥ 3.12.4); `stage`/`attach` accept only refs present in the staging index and never read a caller-supplied `storage_ref`; no `server-only` dependency for the console — a test walks every `"use client"` module's import graph and fails if it reaches `lib/services/**`; console reads refuse a missing time range on aggregates and cap unpaginated reads; LEFT JOIN `api_keys` (nullable key id); masking fails closed unless `by_operator === false`. Pre-existing item confirmed again by two reviewers: naming the four legacy test files explicitly in an order where `test_gateway_auth.py` is not first fails 7 cases at the base too — owner coordinator/G1 (retires with the shim at cutover).
- 2026-09-22 08:00Z: **D1 implemented** (critical path) and under review; its console read surface names were relayed to C1's fix pass for an immediate rename. Q1 reviewed `fix_required`; fix pass running. Wave-2 state: implemented+review running — D1, W1, T1; fix pass running — U1 (re-review running), V1, J1, C1, M1, Q1; still implementing — G1, E2. Adapter fact for D2 from D1's measurement: BYPASSRLS is a role attribute and is not inherited, so the psycopg pool must `set role service_role` per connection; D2–D6 must schema-qualify relations because three `public` views share names with their `infrx` stores.
- 2026-09-22 09:30Z: W1 reviewed `fix_required` → ruling R58 (`edb6566`): allow-listed normalized messages (pilot = text + video only, one media part per staged ref in order), engine events carry `visible` and `raw` with customers and the journal seeing `visible` only, usage authoritative only when last/after the final delta/consistent, cancellation keyed by (job, generation), bounded event/output bytes. R58 relayed to the running G1 agent (normalizer + sampling-range validation at ingress). Coordinator F2.2 queue grows: shared `FakeEngine` must emit `visible`/`raw`; both shared mutation runners must not count a runtime exception inside a named case as a kill unless declared. Seven of seven first-round wave-2 reviews so far are `fix_required` with concrete defects behind green suites; none merges before a `pass` at head.
- 2026-09-22 11:00Z: U1 r2 → two small items (dead "Try again" on error boundaries; one false evidence sentence); V1 round 2 in, re-review running. Cross-task lesson applied again: the Next 16 `reset()` vs `retry()` finding from U1 was forwarded to V1 (which was adding `traces/error.tsx`) and put on V1's re-review checklist.
- 2026-09-22 12:30Z: E2 implemented, review running. Two coordinator observations from its handback: (1) `make bench-test` is flaky on a loaded host because E1's tests assert a wall-clock bound (`schedule_lag_s.max < 0.1`) — violates the no-wall-clock rule; owner E, to be fixed in E2's fix pass (inject the clock / assert schedule determinism, not absolute lag); until then `make check` can fail spuriously while many agents load this host. (2) D1 tested on `postgres:16` + a documented shim, E2 provisions the real `supabase/postgres` 17 image with no shim — the E2 reviewer probes D1's migrations on that image now, and stage review S2 will require D1's suite to pass on E2's image before any pilot claim. Wave-2 state: all eleven implemented; reviews running — D1, T1, E2, V1 (r2); fix passes — U1 (final small), J1, C1, M1, Q1, W1; G1 still implementing.
- 2026-09-21 07:19Z (`date -u`): **Correction to this log's timestamps.** The "Z" times written on the entries above (from `2026-09-21 15:20Z` through `2026-09-22 12:30Z`) were coordinator estimates typed by hand, not clock readings, and they ran ahead of the host clock: the commits that added them are stamped 2026-09-20T17:58Z … 2026-09-21T07:12Z (`git log --format='%h %cI' -- research/plan/evidence/coordinator/STATUS.md`). The ORDER of the entries is right; their absolute times are not — use the commit timestamps. From this entry on, log times are copied from `date -u`. (Same lesson as "no hand-typed counts": applied to the coordinator.)
- 2026-09-21 07:19Z: **V1 merged (`9617a2e`)** after r2 `pass` and a coordinator rerun on the merged tree (row above) — first wave-2 merge; status implemented, fake-backed. Reviewer notes carried forward: fixture clock is 2026-09-20T12:00Z, so the fake-backed default 24 h view is empty from 2026-09-21T12:00Z (seams must take `now` from the context — already on the F2.2 queue); E gets two browser-level checks (two-rapid-changes guard, error-boundary recovery) that cannot be proven without a DOM. **T1 reviewed `fix_required`** (six blockers, all on the durability half; B4b changes the spool frame format — free now, a version bump after T2) → fix pass running. **C1 round-1 fixes in** (`e91b288`) → r2 re-review running; C1 declined "refuse a missing from/to" because the frozen exported suite calls `usageSummary(owner, {})` and requires success → contract-revision request queued for F2.2 (make `from`/`to` required in `services.ts` and in the exported cases together); C1's eight shape expectations for D1's views (nullable `key_name`, RFC 3339 strings, real arrays, money as strings, …) go to D1's fix round. **G1 implemented**, review running. Wave-2 state: merged — V1; reviews running — D1, E2, G1, U1 (r3), C1 (r2); fix passes — T1, J1, M1, Q1, W1.
- 2026-09-21 07:24Z (`date -u`): **D1 reviewed `fix_required`** — the critical path; the migrations' data handling is sound, the privilege/masking/coherence layer is not → ruling **R59** (08 §10) and a fix pass. Handbacks in and sent to focused re-review: **Q1** `309fb1c`, **M1** `7ce0dd8`, **J1** `1ae8951`, **C1** `e91b288`. **U1** r3: two real items fixed and browser-verified; three vacuous assertions remain (test-only). Accepted for F2.2 (contract revisions, coordinator-owned): `MediaStore.stage` accepts only store-produced refs + `materialized(org_id, ref)` hook; `usageSummary`/`usageDaily` require `from`/`to` together with the exported cases. Noted for I2: pin Python ≥ 3.12.4 by digest; startup assertion that `httpx`/`httpcore` loggers stay ≥ WARNING (signed URLs). Wave-2 state: merged — V1; reviews running — E2, G1, C1 (r2), Q1 (r2), M1 (r2), J1 (r2); fix passes — D1, T1, W1, U1 (test-only).
- 2026-09-21 16:47Z (`date -u`): **U1 merged (`184c5fb`)** — second wave-2 merge. The merged-tree rerun caught a cross-task conflict no per-task review could see (both reviewed against the base): resolved with a one-line coordinator seam, recorded in the merge commit. **C1 r2** and **G1 r1** reviewed `fix_required`; fix passes running with rulings (C1: sentinel for a deleted key until the `key_id: string | null` revision, number-money bound, both RFC 3339 UTC forms; G1: authenticate before buffering, structure caps, 400 for unstorable characters, 2 s deadline skew margin, `infrx-upload:upl_…` is the public upload reference, cutover at G2/D2 integration with `install_error_handlers`). **Interruption:** an API session rate limit (reset 08:30Z) killed seven agents mid-task — D1, T1, W1 fix passes (uncommitted work preserved in their worktrees) and the Q1, J1, M1, E2 reviews; reconciled from git at 16:44Z and all seven resumed with 'commit after each item'. Nothing was lost; no task skipped. Wave-2 state: merged — V1, U1; reviews running — E2 (r1), Q1, M1, J1 (r2); fix passes — D1, T1, W1, G1 (r1 fixes), C1 (r2 fixes).
- 2026-09-21 16:53Z (`date -u`): **M1 r2, Q1 r2, E2 r1 reviewed `fix_required`**; fix passes running. Ruling **R60** (08 §10): two-level fairness for unfiltered scheduler claims. **D1 ↔ E2 probe (E2 reviewer, D1 `9d9857b` on the real `supabase/postgres` 17.6 image, no shim): migrations 0003–0005 apply cleanly**, 21 `infrx` tables all RLS, browser roles no USAGE on `infrx`; three incompatibilities ruled: (a) D1's name-gated test clock stays, E2 targets a database `infrx_e2` created from TEMPLATE postgres; (b) the image's `auth.uid()` reads the legacy per-claim GUC — every impersonation helper sets both claim forms and asserts a non-NULL uid (sent to D1 and E2); (c) task-local ports sit inside the kernel ephemeral range — one bind retry, then PENDING; harness runs serialized host-wide until E2's namespace fix. Supabase default privileges grant D1's public views to `anon` (rows filtered to 0) → explicit revoke under R59-4. Wave-2 state: merged — V1, U1; review running — J1 (r2); fix passes — D1, T1, W1, G1, C1, M1, Q1, E2.
- 2026-09-21 16:54Z (`date -u`): **J1 r2 reviewed `fix_required` (narrow)**; fix pass running. Every wave-2 task now has at least one completed independent review; all nine unmerged tasks are in a fix pass with a written finding list under `.claude/handoff/wave2/<TASK>-review-r<N>.md`: D1 (r1), T1 (r1), W1 (r1), G1 (r1), E2 (r1), C1 (r2), M1 (r2), Q1 (r2), J1 (r2). Coordinator queue unchanged (F2.2 contract revisions, per-track mutant lists in `api-mutants`, `make integration`, sidebar/labels, judge price row ⚠️ TO BE VERIFIED before J2's live path). `JudgeRun.sample_ids` uniqueness at the record level added to the F2.2 list (R56 says unique; the frozen record does not enforce it).
