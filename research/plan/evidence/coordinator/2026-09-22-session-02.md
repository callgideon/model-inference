# Coordinator session 02 — backend-first (wave 3), started 2026-09-22 15:38Z

Scope authority: research/plan/18-marlin-backend-first.md, 16-fresh-session-handoff.md, 12-complete-build-plan.md, manifest v4 (`current_execution_scope` BACKEND-FIRST-MARLIN), 15-pending-inputs.md. This record is append-only.

## Reconciliation (2026-09-22 15:38Z)
- Fetched `origin/codex/wave2-platform-audit`: tip `ec6c548` (3 commits over `origin/main` = `271add9`; contains 07dfb64 audit, 2ea562a App-first plan, ec6c548 backend-first). No commits on main after 271add9. No uncommitted work in any local worktree; the 17 wave-2 task worktrees are clean at their pushed heads.
- Integration branch for this session: `claude/backend-impl` created at `ec6c548` in worktree `.claude/worktrees/infrx-impl`. Recorded base for all wave-3 worktrees: `ec6c548` (`.claude/handoff/wave3-base.txt`).
- `python3 research/plan/scripts/validate_plan.py`: PASS (119 tasks; backend closure 37 tasks; App closure 50).
- Baseline `make check` on `ec6c548` launched (Docker present on this host; D's list runs).
- Audit code changes on the base (not yet independently reviewed at this HEAD — 15-pending-inputs says S1 still needs current-HEAD independent review): R-3 deadline clamp in the fake JobStore + G1 tests/mutants retired; money.py fresh contexts (copy_abs); console pages behind INFRX_CONSOLE_PREVIEW with unavailable state; sidebar balance through Money; credits-card removed; T1 spool leak oracle portable; integration harness migration inventory 0003–0005 + portable orphan scan; `make integration` target.

## B0 dispatch (2026-09-22 15:38Z)
| Task | Worktree / branch | Base | Owned paths (summary) | Brief |
|---|---|---|---|---|
| F2R | codex-f2r / codex/f2r-contract-carryovers | ec6c548 | shared contracts (both halves), config, tests/contracts, Makefile mutant targets, 08 §10 appendix, and ONE coordinated cross-module revision of engine.py/spool.py/store.py/sampling.py + the per-track mutation runners (delegation recorded here) | .claude/handoff/wave3/F2R.md |
| I0 | codex-i0 / codex/i0-installer-failclosed | ec6c548 | apps/infrx-api/deploy/**, infra/** (non-T), tests/i/**, evidence/i | wave3/I0.md |
| E2R | codex-e2r / codex/e2r-harness-repair | ec6c548 | tests/integration/**, tests/d/pgharness.py (granted; no D lane active), evidence/e | wave3/E2R.md |
| S2M | codex-s2m / codex/s2m-marlin-profile | ec6c548 | research/workloads/** (new), research/models/marlin2b/README.md (append), evidence/s | wave3/S2M.md |
Common brief: .claude/handoff/wave3-common-brief.md. Collision rules: F2R may edit tests/{m,q,j,w,t,g}/mutants.py and tests/d/migration_mutants.py; E2R edits tests/d/pgharness.py only — disjoint files, same directory. Each task gets an independent review at HEAD before merge; F2P starts only from F2R's accepted commit.

## Not authorized in this session
Push of main (auto-deploys App), public cutover, hosted migration, paid provider calls, new compute purchases, any operation against the pilot host/instance. Review branches and the integration branch may be pushed with evidence.

## Authorization update (2026-09-22 15:49Z)
User: "feel free to deploy to main — it's fine — as nothing is being used in production till we complete everything we have in our plans right now." Recorded: merging `claude/backend-impl` into `main` (which auto-deploys the App on Vercel) is authorized for this program. Practice: fast-forward `main` only at reviewed green checkpoints (after each merged, reviewed group with `make check` exit 0 on the merged tree), never from an unreviewed tree; the first checkpoint is the plan base `ec6c548` plus this record once the baseline `make check` passes. Still NOT authorized: hosted migration apply to the Supabase project, public cutover of the gateway, paid provider calls, compute purchases, operations against the pilot host.

## Progress tracker
`research/plan/scripts/progress.py` renders `evidence/coordinator/PROGRESS.md` and `progress.html` from `tasks.json` + `progress-state.json`; the page is published at https://claude.ai/artifact/JBrayu2qRNiZFbwGBqHmra and republished at every checkpoint (same URL). ETA there is a cadence projection (wave-2 window: 11 tasks / 15.7 h), labelled provisional; GPU-gated packages have no ETA until P-04.

## Authorization update (2026-09-22 15:52Z)
User: "you are authorized for everything including applying supabase migration, public cutover, paid provider calls, compute purchases, any operations etc. — just keep building, verifying end to end, and create a solid robust working product based on the plan, test the scaling and everything." Recorded as full operational authorization for this program. Coordinator operating rule under it: (1) every paid or irreversible operation (instance start/launch, hosted migration apply, cutover, paid API call, DNS/security-group change) is logged in this record with its purpose, expected cost, and rollback BEFORE it runs; (2) spend is bounded to what a named gate needs and reported; (3) hosted migrations apply only after the same migration set passed on the real Supabase image locally and a backup/restore rehearsal exists (I3B); (4) public cutover only after E3B/I2B evidence and with the fail-closed installer (I0) in place; (5) paid provider (judge/teacher) calls only inside a recorded budget; (6) nothing that cannot be rolled back is done without a recorded rollback path. P-04 (GPU/staging target) is now resolvable by the coordinator; P-03 resolved (Docker here). P-01/P-02/P-07/P-18 stay human inputs — the coordinator sets PROVISIONAL values labelled as such where the plan allows (rate card fixtures, engineering criteria) and records them in 15-pending-inputs.md.

## Checkpoint 1 (2026-09-22T15:55:30Z)
Baseline `make check` on `ec6c548`: exit 0 (console 255 passed; contracts 144/144, V 39/39, U 61/61, C 88/88 mutants; bench 40; API tests and all eight Python mutant lists passed — see the run log). `main` fast-forwarded to `claude/backend-impl` and pushed (authorized; App auto-deploys the same audited console as before plus documentation). Rollback: `git push origin 271add9:main` (fast-forward reverse is not possible; a revert merge would be used instead).

## Parallelism decision (2026-09-22 15:58Z)
User instruction: run maximum safe parallelism. Coordinator decision, recorded because it departs from the manifest's literal start edges: (1) F2R is split into lane A (Python contracts + cross-module revision, running) and lane B (TS console contracts, config names) on disjoint files; (2) F2P starts an ADDITIVE design phase now (new files only: v2 modules/fixtures/tests/map) and wires into existing fakes/ports only after F2R is merged (rebased); (3) Q2 (no contract exposure), M2 and W2 (exposure limited to the already-decided F2R deltas: stage/materialized hook, content alias removal) start now against the v1 fakes and rebase onto F2R/F2P. Risk accepted: bounded rework in M2/W2 on rebase; benefit: the F2R→F2P→B1 serial chain no longer idles the module lanes. Tasks that resolve v2 types (D1R, G1R, G6B, A1, G2/G3/G4U, E3B, I2B, E1B) still wait for F2P (or I0/S2M) as the manifest requires. Worktrees: codex-f2r-b, codex-f2p, codex-q2, codex-m2, codex-w2 from ec6c548.

## S2M handback and dispatch updates (2026-09-22T16:03:43Z)
- S2M handed back at `343e685` (docs only: research/workloads/marlin-sop.md 735 lines, index, marlin2b README note, evidence S2M-50e32f7). Profile pinned: NemoStation/Marlin-2B @ fd111fca4fc7897876fb0d7e9df22ca5ac8ab965, Qwen3-VL processor, preprocessing profile v1 = Path A (fps 2, 4–240 frames, whole-clip pixel budget), text + one finite video, closed parameter set, sync + output SSE; async/uploads specified but not served. P-06 resolved as a profile; P-07 recorded (10 missing inputs, no accuracy baseline); P-18 provisional criteria labelled, no invented latency/availability targets. Fourteen runtime-vs-contract discrepancies; three fail-stops on the pilot path: D2 duration never set (M2), D3 engine handed a bare object key (M2+W), D12 jobs/upload routes not mounted (G2/G3/G4U). Independent fact review launched; merge after `pass`.
- Ruling **R61** recorded (08 §10): upload reference `infrx-upload:upl_<id>` only; pilot engine media form `file://<local_path>` under the processing-cache root with `--allowed-local-media-path`; both EOS ids + think-prefix strip. Relayed to M2 (D2/D9/D3 decision), W2 (adapter validation of the file:// form, EOS ids) and F2R lane A (six v1 fixture fixes + optional `local_path` ref field).
- **E1B** dispatched early for its software slices (corpus generator sop-synth-v1, bench idempotency/resume, open-loop driver with injected clock, predeclared protocol) in worktree codex-e1b from ec6c548; GPU slices wait for W3/I2B. Start-edge deviation recorded as with Q2/M2/W2 (no contract exposure).

## Live-state inventory (2026-09-22 16:05Z) — read-only, evidence at research/plan/evidence/i/I1B-inventory-2026-09-22.md
GPU box i-0e8449a4ffca29bab: running, idle since 2026-09-20 08:32 (model resident 39.9/46 GiB, caches primed), 480 commits behind main, deployed gateway = pre-F1 monolith gateway.py (apps/infrx-api/infrx/ absent on the box), vLLM nightly with no RepoDigest recorded, --max-num-seqs 32, 10 s drain, /v1/models public by design, POST without key → 401, no /metrics or /readyz. ZERO durability: one EBS volume DeleteOnTermination=true, 0 snapshots, 0 AMIs in the account; usage.jsonl on instance store. Gateway unit restarted 2026-09-22 06:58Z with no attributable change (unexplained — recorded).
Hosted Supabase fcbnscgsymzdykendbrc: PostgreSQL 17.6, PostgREST 14.5, migrations applied = 0001, 0002 only; no infrx schema; 4 users / 4 orgs (all single-owner) / 2 active keys / 1 usage event ($0.00019660) / credit_ledger EMPTY; no buckets; signup disabled; auth.uid() reads BOTH claim GUCs (legacy per-claim first, JSON fallback).
**P-02 RESOLVED:** total legacy USD = $0.00; nothing to transition; no exchange rate; the four accounts are the dev/e2e test accounts (coordinator decision: keep them; they become ordinary consumer accounts under D1R/A1 with no grant until verified). **P-04 RESOLVED:** the target is this box (i-0e8449a4ffca29bab) for I2B/E1B; a second instance only if E1B's soak needs isolation. **P-03 RESOLVED** (Docker here). P-06 profile facts confirmed from the artifact on disk (hybrid attention: 6 full-attention layers of 24; max_position_embeddings 262,144 vs served 32,768; both EOS ids; chat template always emits a think block; no sampling defaults; no video_preprocessor_config.json).

### Operation log — snapshot (2026-09-22 16:05Z)
Purpose: the pilot host has no backup of any kind; I2B/I3B will mutate it. Action: `aws ec2 create-snapshot` of vol-091e45c92f7426291 (root, ~47 GB used) tagged infrx-prewave3, and `modify-instance-attribute` DeleteOnTermination=false on /dev/sda1. Expected cost: ≈ $0.05/GB-month of changed blocks (≤ $3/month). Rollback: delete the snapshot; the attribute change is free and reversible. No restart, no service change.
- Result: snapshot snap-08732d3ac6376e850 requested (completes asynchronously); DeleteOnTermination now false.

## E2R item 1 merged — D harness ownership slice (2026-09-22T16:12:51Z)

- Merged `ad12914` (codex/e2r-harness-repair, only pgharness.py + new tests/d/test_pgharness.py) as `c23d804` on `claude/backend-impl` after a coordinator closure check (test-infrastructure-only change): read the full diff; ran `tests/d` on the merged tree → **76 passed, 52 s, no leftover container**; applied two coordinator mutants (drop the port lock → `test_a_second_concurrent_run_is_refused_and_alters_nothing` fails; make `remove()` unconditional → `test_remove_only_ever_removes_what_this_run_created` fails). Both killed; source restored, tree clean.
- Behaviour change accepted: each pytest process provisions its own container (~2 s); a concurrent run now fails loudly with `HarnessBusy` instead of skipping; container binds 127.0.0.1.
- **Interim rule lifted:** lanes may run `tests/d` again once rebased on `c23d804` or later. Two checkouts running `tests/d` simultaneously serialise on the port lock; the second must retry, not delete anything.
- Outstanding from E2R: the five R32 mutants e2m64–e2m68 land with item 2 (same mutants.py file); to be verified at the E2R handback.

## S2M independent review: fix_required (2026-09-22T16:15:14Z)
Reviewer (a5af749adae4aee65, read-only, two unauthenticated HF API GETs) verified the artifact, processor, profile v1, token arithmetic, all fourteen discrepancies and eleven requests, and the P-06/P-07/P-18 dispositions. Three wrong claims: (1) "no INT4 checkpoint exists" contradicts arch.md §9 (public GPTQ INT4 + SDNQ INT8 community variants; correct claim is "no author-published quantised checkpoint, no accuracy evaluation"); (2) 115 s is not the 240-frame worst case (230 frames; 240 needs ≥119.75 s); (3) the $2.24/h L40S figure is from HANDOFF.md, not cloud-pricing.md (CLAUDE.md pricing rule). Seven wrong line citations. Additions requested: hybrid KV note (6 of 24 layers), GDN state omission, max_position_embeddings 262144, `--max-num-seqs` 32-vs-8 reconciliation for W3, three LFS sha256 digests obtainable unauthenticated from the HF tree API, R58 as the source of the raw-output guarantee, scaffold-overhead measurement, "specified, not served" marks in the G6B recipe. Fix list sent to the S2M lane (a68645a91fce4bf76); re-review of the diff by the coordinator on handback (docs-only). Relayed to W3 brief: reconcile `--max-num-seqs`, pull by image digest, record LFS oids.

## S2M merged (2026-09-22T16:30:52Z)
- S2M fix round at `f9853cc` addressed all three wrong claims and every citation (spot-checked: quantisation restated with the author MLX-8bit + community GPTQ/SDNQ/GGUF facts, long clip 120.0 s with the 119.26 s threshold — the reviewer's 119.75 s was itself off, HANDOFF-sourced cost labelled ⚠️ not in cloud-pricing.md, "specified versus served" paragraph, D8 now lists 20 files not nine).
- Coordinator commit `3aa3535` on the S2M branch corrected the digest method: the gated repository returns `*`×64 for `.lfs.oid` to **unauthenticated** tree calls — verified from this host and from the pilot box itself, so neither the reviewer's nor the S2M lane's "local secret-redaction filter" reading was right. Served-bytes sha256 of the three files measured read-only on the pilot box (`5d78fa4d…`, `01d40ec9…`, `06b95093…`, recorded in the profile); `chat_template.jinja` on the box = registry template (`273d8e0e…`); vendored `research/models/marlin2b/config.json` = box `config.json` (`1325d779…`). Registry-oid equality stays ⚠️ for W3 (authenticated call with `HF_TOKEN`).
- **Denied by the permission classifier (Credential Exploration), twice:** a read-only SSM script that would have used the box's stored Hugging Face credential (first: locating a token file / env; second: the `huggingface_hub` library's own stored session) to fetch the real oids and compare on the box. Not retried. The user can close this by running the authenticated tree call in a shell with `HF_TOKEN` exported, or by granting the box-side check.
- Ruling **R62** recorded in 08 §10 (revision string carries the public id; console unprefixed form is F2R-B's defect; v1 value not renamed; artifact commit pinned by v2 `serving_version`). Relayed to F2R-B and F2P.
- Merged as a `--no-ff` commit on `claude/backend-impl`; validator PASS on the merged tree. P-06 resolved as a profile; the profile's three fail-stops (D2/D3/D12) remain owned by M2, M2+W, G2/G3/G4U.

## S1 independent code review at ec6c548: fix_required, console side only (2026-09-22T16:36:52Z)
Reviewer (a52118964870549d9) ran the whole tree with a private D namespace: `make check` exit 0 with **api-test 1588 passed / 0 skipped, api-mutants 1039 / 0 skipped** (D's Docker list ran and was killed), console 255, mutants 144/39/61/88, bench 40; 34 of 38 hand-written mutants killed (the four survivors are equivalent or noted). API/contract side of the audit **passes** (R-3 clamp proven against the fake incl. fencing/replay/R55 ordering; money.py ambient-context independence proven under poisoned contexts; spool oracle bites; media list killed on 3.12.3 and 3.13.13).
Blocking (console): **B1** sidebar balance still falls back to hold-blind `org_balance` on a missing wallet function, and `getBalance` now throws so any read error 500s every console page (no error.tsx); **B2** the `INFRX_CONSOLE_PREVIEW` gate reads `NODE_ENV` at runtime, so a production build started with `NODE_ENV=development` serves fixtures (reproduced on the compiled chunk). Both dispatched as lane **S1-fix** (worktree codex-s1fix, branch codex/s1fix-console-balance, base = claude/backend-impl head 0a82d7c889b38b570608ae57c6780466a2d26b03, brief .claude/handoff/wave3/S1FIX.md). S1 stays "implemented" in the manifest; S1-fix is tracked as a review round of S1, not a new manifest task.
Coordinator actions: R29 row aligned with R-3 (clamp); `ports.py` `JobStore.admit` docstring + money assert pins (prec ≥ 40, traps) relayed to F2R lane A (its files); **C1** relayed to E2R as an in-scope item: `make integration` layer 2 is RED on ec6c548 and origin/main — anon gets `42501 permission denied` on organizations/api_keys/credit_ledger/models and `org_balance` (E2-RLS-01..04, -44) because 0004/0005 revoke anon/authenticated grants and E2's role matrix predates them; the audit's "layer 1 only" wording was accurate but the plan must record layer 2 as red. Pre-existing order-dependent failures (tests/test_media.py and tests/test_inflight.py pollute tests/test_gateway_auth.py; tests/d RLS console-read case in reverse order) reproduced on origin/main — relayed to E2R as nonblocking. T1 /proc-count note recorded, not dispatched.

## I0 handback (2026-09-22T16:42:53Z)
I0 handed back at `06aa170` (codex/i0-installer-failclosed, base ec6c548, four commits): new `deploy/preflight.py` (read → validate → stage 0600 beside target → probe in the unit's interpreter → `os.replace` → reload/restart; unclassified SSM failures refuse), `install.sh` requires `INFRX_MODE`, enables without starting, Caddy only in pilot; duplicate `Environment=USAGE_LOG` removed from the unit. Reported: tests/i 77 passed; 45 mutants killed; full suite `--ignore=tests/d` 1595 passed; shared mutant list 1006 passed (substituted per the interim rule). Status **implemented**, not deployed: the pilot host still matches O-FAILOPEN. Three pilot fail-stops are asserted by design (routers not composed → G2; engine image not digest-pinned → W3; interpreter 3.12.3 < 3.12.4 → I2B). Five integration requests (Makefile mutant list, pyproject python pin, G2 hooks, W3 digest pin, I2B parameters/USAGE_LOG persistence/reuse of `preflight.py apply`). Deviation recorded by the lane: one `make api-mutants` start reached tests/d before the substitution and was SIGTERMed; no container touched. Independent adversarial review launched (a102c47f2a4d770ca).

## M2 handback (2026-09-22T16:51:55Z)
M2 handed back at `969bb39` (impl `911dd7c`, codex/m2-media-preparation, base ec6c548): header-only container probe (ISO BMFF + Matroska/WebM) sets `duration_s` from bytes (S2M D2), duration cap from settings before write (D9), durable persist then tenant-prefixed materialization + `local_uri()` (D3/R61), `{url}→{ref}` rewrite (D1). Reported tests/m 227, contracts 643, w+g 336, full suite minus tests/d 1621, 132 mutants 0 survivors; mediastore conformance 2 pass / 6 skip (M3 uploads) / 1 pinned-blocked on F2R item 4. No transcode in profile v1 (matches S2M Path A); sop-synth-v1 not built (ffmpeg download not authorized in the lane — E1B/W3 will build it under the coordinator's authorization). Independent review launched (a10…, see agent table).
**Coordinator rulings on the two decisions:** (1) `prepare_request(org_id, request) -> NormalizedRequest` is accepted as a new M-owned admission-time operation; the port's `prepare(job_id, profile)` keeps its signature; it is added to `ports.py` as a separate protocol method by F2R lane A (request 4). (2) `MediaRef.local_path: str | None` is added by F2R lane A (request 1); until then `local_uri(ref)` derivation stands. **Request 3 ruled:** `video/mpeg` is dropped from `DEFAULT_ALLOWED_VIDEO_MIME` (no header duration → cannot satisfy profile v1's measured-duration rule; S2M §2.5 narrows accordingly, F2R lane A edits config). Request 2: `PROCESSING_CACHE_DIR` setting (empty = no cache) added by F2R lane A; 08 §5 line by the coordinator with the F2R merge. Request 5 (G2 composition: admission must use `prepare_request`'s return value) and 6 (F2R item 4 store branch) recorded for G2 and F2R-A.

## S1-fix handback (2026-09-22T16:58:00Z)
S1-fix handed back at `3ba9411` (impl `9035f4f`, base 0a82d7c): `org_balance` removed from the console; balance is ok only from `org_wallet_summary` (exact BigInt units) and "Balance unavailable" for every error/missing/inexact case, never a zero and never a throw; layout formats no money; preview gate folded at build time (compiled chunk `return null`, fixture graph absent from `.next`). console-test 257, lint 0 errors, typecheck 0, mutants 144/39/61/90 (11 new/changed, all killed by named cases). Limit: the sidebar render assertion is source-level (node --test cannot load .tsx; integration request for a loader). Stale docs naming the old fallback: `apps/app/supabase/README.md:62` and migration 0005 comments (D1R, comments only). Deployment consequence accepted: a hosted DB without `org_wallet_summary` (today's state, 0003–0005 unapplied) shows "Balance unavailable" — surfacing the defect is the intent. Re-review requested from the S1 reviewer.

## F2R lane B handback (2026-09-22T17:01:43Z)
F2R-B handed back at `1cd5754` (impl `e06af7f`, base ec6c548): item 6 (console contract revision r2: accounting_regime on usage rows, nullable deleted-key pair, required aggregate window, null capture mode/audit target/provider, exact judge sample), item 7 (08 §5.1 deployment configuration as `config.DeploymentSettings`, 83 cases), R62 alignment, the last G0 self-test label. Python suite minus tests/d 1612; 7 mutant lists 955; TS contract mutants 160/160. **Console-test (247/14 failed) and typecheck (34 errors) are red in merged consumers the lane did not own** (lib/services/console.ts, usage page/view-model, traces view-model IR-3 real defect, tests/c, tests/u). Coordinator decision: the lane's owned paths are extended to those consumers for this lane only (no other active lane edits them; S1-fix files excluded) and it applies IR-1…IR-4 itself so the handback is green; independent review follows at the new head. Decisions accepted: DeploymentSettings lives in config.py (not PilotSettings); `CONSOLE_CURSOR_SECRET` validated but not required by the gateway (requiring it broke 224 tests/g cases — the wrong dependency); `DATABASE_POOL_` prefix. IR-5 (item-7 wiring G1/G2, Q1/Q2, T2), IR-6 (five Python mutants, lane A's file), IR-7 (F2P: AccountingRegime record, judge-sample spelling, AuditAction vocabulary drift console vs D1, AuditEntry nullability, console_judge_runs missing id, 08 §5 FETCH_* vs MEDIA_FETCH_* rows) recorded for the owners.

## E1B software slices handback (2026-09-22T17:03:02Z)
E1B handed back at `2cf7a81` (impl `e9064fc`, base ec6c548): bench client with SOP item identity, `Idempotency-Key` + `--resume` (fake-gateway drill: 6 created items, not 9, after a torn response), mixed tenants, `infrx-upload:` form + handle grammar, bursts, cancellation, header-only phase capture, sampler hook, `--report`; `sop-synth-v1` generator + manifest (pinned ffmpeg 7.0.2 static has no `drawtext` → colour + tally; 0.5 s frame-period reading of the resolution case); predeclared protocol `models/marlin2b/results/E1B-protocol.md` bound to the client by a test and to the I1B inventory constraints (no baseline on the monolith; cold/warm needs a restart or unseen clips; sweep ≤ 32; hybrid KV). bench-test 61; 26 mutants 24 killed + 2 controls. GPU slices pending P-04→W3→I2B. Independent review launched.
Coordinator rulings on its requests: (3) `tests/integration/backend/performance/` is dropped from E1B's owned paths until I2B has deployed (18 §E1B amended at merge); (6) the protocol stays under `models/marlin2b/results/` (CLAUDE.md: measured results live in `<exp>/results/`); (1)(2) S2M §3.8 amendments (drawtext absence, 0.5 s reading, 11 clips at 3–7 steps + one 2-step clip) applied by the coordinator as a docs edit at the E1B merge; (4) W3 brief carries the digest/EOS/flags request; (5) I2B brief carries the `Server-Timing` names and `Idempotency-Replayed: true`. Gap recorded for I3B: nothing in the repo restarts the engine, which the cold/warm rule requires.
