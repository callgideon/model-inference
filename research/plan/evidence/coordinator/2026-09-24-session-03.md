# Coordinator record — session 03 (2026-09-24, sofia window): consumer v1 wave (program 22)

Append-only. Every op, command id, finding and dispatch in order. Predecessor: `2026-09-22-session-02.md` (ends with run3 launched). Entry point for this wave: `research/plan/24-consumer-v1-session-handoff.md`.

## Baseline (2026-09-24T21:30Z)
- `origin/main` = **dff31efc** (three docs commits on top of 726d004d: review 21 at 6db71ee3, program 22/23/24 + briefs + manifest at ba8ffc40, tracker/handoff amendment at dff31efc). `claude/backend-impl` fast-forwarded to it; new integration branch **`claude/consumer-v1`** created from dff31efc. Worktree clean.
- Plan validator: PASS (133 tasks; backend closure 50, App closure 64; 874 links / 176 docs; ledger current). `test_validate_plan.py`: 7 tests OK.
- Manifest delta 726d004d..dff31efc: 14 new tasks (S3, F2C, E2C, D10, M5, M6, W5, G7, G8, E1C, I8, E3C, E4C, U4); refined App tasks (C0, C3A, A2, A3, U1R, U2, U3, E3A, I2A, I3, E4) now carry `dispatch_after_gate: BACKEND-READY`; gate roots BACKEND-LOCAL=E3C, BACKEND-READY=E4C; corrective oracles UPLOAD-RESTART … USER-RESULTS in 04-verification; inputs P-24, P-25 added.
- **Certification run3 (bda1586) still running** (box clock 21:26Z; command 3588e56e): preconditions ok, config-pin ok, served-build ok, sop-parity ok (2 clips; over-cap refused 400 `unsupported_media`), recovery-box PEND (coordinator drills), protocol/recovery skip (`--no-stack`); **dataset-resume FAIL** (`usage recorded outside CREDIT: [job ids]` — the pilot runs `legacy_usd`; the runner asserts the CREDIT ledger — S3 classifies), **envelope FAIL** (`supported_rate_per_s: 0.5`, client exits 0 at 0.5/1.0/2.0; the 1.0 rung missed a provisional target — S3 reads the report); soak in progress (609 rows at 21:26Z; bounded 14,400+900 s → ends ≈ 01:20Z box clock), overload after it. Output dir `/opt/dlami/nvme/e4b/20260924T202924Z`. The 10-minute poller (b6kprqhsg) is dead; the coordinator polls manually.
- Host: 16 CPU, 61 GiB (53 free), root disk was 92 % → pruned docker build cache (13.6 GB), volumes (19.6 GB), unused images (5.6 GB) → 85 % (52 GB free). Removed the two stale detached scratch worktrees (`merge-tree`, `merge2`). `infrx-d2-postgres` (55433) and `gideon-migration-order-test-…` containers left as found.
- Ports in use: 22 53 443 3111 3112 7681 18789 49301 52717 55433. Lane ranges allocated below; nothing listens in 555xx besides 55433.

## Dispatch 1 (2026-09-24T21:40Z) — seven lanes, Opus, isolated worktrees from dff31efc
| Lane | Branch / worktree (`.claude/worktrees/`) | Owns | Isolation | Note |
|---|---|---|---|---|
| S3 | `codex/s3-reconcile` / `codex-s3-reconcile` | new coordinator evidence record + updates | none (read-only; box facts supplied by the coordinator) | reconciliation table, RV-01…12, inputs, "do not redo" list |
| TRACKER (support, 06) | `codex/tracker` / `codex-tracker` | `research/plan/scripts/progress.py`, `test_progress.py`, `evidence/coordinator/{progress-state.json,PROGRESS.md,progress.html,tracker-v46/,updates/}` | none | v46 snapshot preserved; overlay schema v2 |
| E2C | `codex/e2c-verify` / `codex-e2c-verify` | `tests/integration/`, `apps/infrx-api/tests/` (portability only), Makefile lines as wiring requests | ports 55510–55519, prefix `infrx-e2c-` | Linux env manifest + preflight; RV-12 harness fixes; dependency triage; wrappers |
| F2C-L | `codex/f2c-lifecycle` / `codex-f2c-lifecycle` | contracts (Python+TS) lifecycle/terminal files + conformance suites, 01/02 plan docs | none | slices a, b, d; returns after slice a so D10/M5/W5 can start |
| F2C-C | `codex/f2c-catalog` / `codex-f2c-catalog` | NEW contract files only (published-model projection, pricing identity, alias resolution) + fixtures | none | slice c; exports via wiring request; P-22 amendment text |
| E1C | `codex/e1c-client` / `codex-e1c-client` | `models/marlin2b/bench.py`, `models/marlin2b/tests/`, new dataset client/profile files, `results/` (new only) | ports 55520–55529, prefix `infrx-e1c-` | RV-07/08; profile schema + validate-only; resume recipe |
| I8 | `codex/i8-operate` / `codex-i8-operate` | `infra/`, `apps/infrx-api/deploy/`, `apps/infrx-api/tests/i/` | ports 55530–55539, prefix `infrx-i8-` | code/config slices only; every live box step is a coordinator op serialized after run3 |

Deviation from the brief's strict order, recorded: E1C and I8 start before S3/F2C complete because their RV findings (07/08/09/10) were not touched by any 2026-09-24 lane (CERTIFY-TREE, TOKCOST, INTAKE-DRAIN, CERTIFY-POLISH touched the certify runner, worker preparation memo, intake drain and certify polish only) and their first slices do not consume F2C ports; both are told to re-check F2C fixtures when they land. D10/M5/W5/G7/G8/M6 are queued behind F2C-L slice a / F2C-C. Shared decisions for the ready marker, references, expiry authority and rollout are given to F2C-L as coordinator drafts to confirm against the code, not as facts.

## Dispatch 2 (2026-09-24T21:55Z) — user: "run max parallel agents / tasks" → five more lanes (12 active)
| Lane | Branch / worktree | Owns | Isolation | Start basis |
|---|---|---|---|---|
| D10 | `codex/d10-durable` / `codex-d10-durable` | new additive migrations (next free number from the directory), `infrx/state/`, `tests/d/` | 55540–55549, `infrx-d10-`, db d10 | coordinator draft decisions D1–D5; implements F2C-L's ports as they commit (read-only polling of the sibling worktree) |
| M5 | `codex/m5-uploads` / `codex-m5-uploads` | `infrx/media/` (uploads), `tests/m/` | 55550–55559, `infrx-m5-`, MinIO prefix m5/ | same; D10 adapter when it lands, named fake meanwhile |
| W5 | `codex/w5-readiness` / `codex-w5-readiness` | `infrx/worker/`, `tests/w/` | 55560–55569, `infrx-w5-` | slices 2–4 independent; slice 1 against the ready-marker port; box warmup script for the coordinator |
| G7 | `codex/g7-catalog` / `codex-g7-catalog` | `gateway/routes/`, `openrouter/provider-models.json`, `tests/g/` minus `ops/` and minus the host-metrics test E2C repairs | 55570–55579, `infrx-g7-` | slice 4 independent; 1–3 against F2C-C / F2C-L / D10 shapes |
| G8 | `codex/g8-credit-ops` / `codex-g8-credit-ops` | `infrx/operations/`, `tests/g/ops/`, `client_example.py` | 55580–55589, `infrx-g8-`, db g8 | reuses A1 + G6B; live transition = coordinator op; P-01 blocks activation only |
Rationale recorded: the plan's "F2C before consumers" is honoured at the *integration* boundary (no lane may claim integration against a stand-in; each must name the F2C commit it implements); starting the independent slices now uses the idle slots the user asked for. Path overlap check done: E2C's portability edits (a host-metrics test in tests/g, `tests/i/test_release_bundle.py`, `infrx/observe/host.py`) are excluded from G7/I8 ownership. Still queued: M6 (after M5, same paths), E3C (runner dir held by E2C), E4C (GPU window after run3). Host load 5.3 on 16 CPUs at dispatch.

## S3 handback merged (fff8416e) and its edits applied (2026-09-24T22:20Z)
S3 (Opus, read-only, 289eef6e): all twelve RV findings **open** at dff31efc (the deployed code = dff31efc's code); run3 `dataset-resume` FAIL = regime mismatch (the ledger half asserts CREDIT, the pilot runs `legacy_usd`; the client half passed, so R106 holds live); `envelope` FAIL set by failure/refusal rows on the 1.0 rung or the 0.5 rung's rows — read from the report. New findings: F4 `record_reconciliation` has no runtime caller (soak `reconciled_at_end` always UNKNOWN; drift alerts can never fire) → W5 + I8; F5 the E1B bda1586 cells contain zero 429s (drained-429 delivery through Caddy unmeasured) → E1C/G7/E4C; F6 `configure_connection`/`connector` set role + statement_timeout as session state → not drop-in on the transaction pooler → I8; F7 the only box backup holds 27af05a (not known-good) → I8 slice 6; F8 two pre-cutover consumer keys recorded active → coordinator key inventory (read-only op) before the next `E4B_WINDOW_OK=1` run; F9 `infrx.media_uploads` (0010) and `jobs.result_expires_at` exist unwired → D10/M5/G7; F10 processor has no served-bytes digest → P-06/I8/E4C; F11 two provisional card ids → F2C-C/G8; F12 record slip.
- Correction (S3): the run3 third launch began ≈ 20:29Z box clock (out `20260924T202924Z`), not "21:29Z" as session-02's 21:35Z entry reads.
- Applied: tasks.json S3 → implemented (+evidence), E1B/E4B evidence lists; 15-pending-inputs S3 section (P-02 re-opened, P-05 second tenant, P-06, P-24, P-25); brief appends (03 §E4C.1, §I8.1, §I8.3; 01 §D10.a; 05 §2.3); E4B-release-decision log; handoff 20 §7 line. Ledger regenerated.
- Isolation note: `infrx-d1-postgres` (55432) / `infrx-d2-valkey` (55463) belong to E2C's detached `make api-test` baseline (the harness's default task-local services, host-wide lock). Lanes running tests/d with their own task id use `INFRX_D_TASK=<task>` where `tasklocal.py` reserves it — checked next.

## Coordinator update 1 to the lanes (2026-09-24T22:30Z)
- `contracts/tasklocal.py` (coordinator-owned shared config) now reserves per-lane real-service ports (bfb3a8af): d10 55442/55469, m5 55443/55470, m6 55444/55471, w5 55445/55472, g7 55446, g8 55447/55473, e2c 55448/55474/55475, e1c 55449, i8 55450/55476, e3c compose block 56900–56999 (postgres mirror 56932). 555xx is the E compose block, so the ranges given at dispatch were replaced; every lane received the patch (`git am`, identical content merges cleanly) and `INFRX_D_TASK=<task>`. E2C's detached api-test baseline keeps the default d1 (55432) and its host-wide lock.
- S3 findings routed: F4 → W5 (worker producer for the reconciliation gauges) + I8 (rule-has-producer test); F5 → E1C (target path edge vs direct in the profile; P4 drained-429 through Caddy) + G7 (route-level proof only); F6 → I8 (pooler-semantics test on the exact session-state patterns) + D10 (role-bound login or SET LOCAL); F7 → I8 slice 6 (known-good bundle from the release bundles, never the backup dir); F8 → G8 (read-only key inventory in the dry-run) and a coordinator op before the next window; F9 → D10/M5/G7/F2C-L (wire 0010 `media_uploads` and `jobs.result_expires_at`; TerminalOutcome gains the field); F10 → I8 slice 5 (processor digests) + wiring request for `serving-version.json`; F11 → F2C-C (one public rate identity) + G8/G7 (both provisional ids mapped); P-02 re-opened → G8 inventory; P-25 → I8 policy check (alert destination still missing → slice 4 BLOCKED). Tracker told to reflect S3 complete, gate wording (BACKEND-LOCAL "harness green at 95fbb90; not declared"), the run3 launch-time correction and the new lanes.

## Dispatch 3 (2026-09-24T22:45Z) — M6 phase 1 and E3C phase 1 (14 lanes active)
| Lane | Branch / worktree | Owns (this phase) | Isolation | Basis |
|---|---|---|---|---|
| M6 (phase 1) | `codex/m6-retention` / `codex-m6-retention` (base f764e396) | `infrx/media/gc.py` + NEW media modules, `tests/m/test_gc*|test_retention*|test_lifecycle*` | m6: 55444 / 55471 | points 1, 2, 4 of the brief are disjoint from M5's files; point 3 (process maps / caches in store/prepare) is phase 2 after M5 merges — design only now |
| E3C (phase 1) | `codex/e3c-integration` / `codex-e3c-integration` (base f764e396) | NEW `tests/integration/backend/e3c/` only; E3B harness and E2C's top-level files read-only | e3c compose block 56900–56999 | the red harness: scenario matrix + verdicts run on the base tree now (seam scenarios must FAIL today), phase 2 = green on the merged SHA |
Host at dispatch: load 7.8 / 16 CPUs, 51 GiB free, disk 87 % (watch: lane venvs/images). Containers up: d1/d2/d10 postgres, d2/q3 valkey.

## Clock correction (host UTC 2026-09-24T22:06Z)
The coordinator's labels above ("21:30Z" … "22:45Z") were estimated, not read; host commit times are authoritative: baseline commit 98bfdfa3 21:19Z, dispatch 1 ≈21:20–21:24Z, dispatch 2 6ef2192c 21:51Z, S3 merge fff8416e 21:53Z, update 1 f764e396 21:58Z, dispatch 3 0d180772 21:59Z. From here every label is `date -u` at the time of writing. The tracker lane (Opus, f1a202ab) flagged this: it stamps lanes with host UTC and warns on future-dated updates.

## TRACKER handback (22:06Z) → adversarial verification started
Head f1a202ab on `codex/tracker`: renderer rewritten (overlay schema 2, `apply-updates` under an O_EXCL lock with revision check and tmp+os.replace, `check`), 22 tests OK, v46 snapshot preserved under `tracker-v46/`, HTML verified headless (Playwright Chromium: filters, stale banner, 390/1280 px no overflow). Baseline rendered: gates all PENDING; backend corrections 0/14 (S3 → 1/14 after merge), App 0/12, deferred 0/57, reused baseline 44; ETA E3C unknown (no lane estimates yet), E4C/E3A/E4 blocked pending P-01/02/05/06/17/18/19/22/24/25 + no GPU window. Tracker decisions accepted: backend denominator = 13 BACKEND-CLOSURE + E1B, U4 under App; input edges as listed; rework 0.3, integration 0.5 h/task labelled as assumptions; overlapping-writer warnings acknowledged (E2C portability edits are limited to the named files; G7 owns `tests/g/` minus `ops/`; F2C-C creates new files only). Verification: `verify-lane` workflow (3 lenses → one fix round → recheck).

## F2C-C handback (22:09Z) → adversarial verification started
Head 042ea49d on `codex/f2c-catalog` (new files only): `contracts/v2/published_model.py` + TS twin `lib/contracts/v2/published-model.ts`; generated fixtures under `contracts/v2/published/` (Marlin in both regimes, the deployed serving profile 82 s / 64 MiB / 1 video / mp4+quicktime+webm / fps 2 / 4–240 frames / 200704 px, 12-parameter allow-list + 10 refused by name, ServingRetention with zdr always false, availability separate from capability); `resolve`/`price`/`project`/`serving_profile`/`violations`/`catalog_problems`; alias_compatibility.json with 11 spellings (2 priced spellings keep deployment + rates 0.10/0.30 tr-1; 9 stay not_found; guard test for new spellings). Tests: Python 89 new (tests/contracts 1164 with the 3 PG tests HarnessBusy on 55432), console 52 new (341/341), typecheck 0, lint 0 errors, 18/18 ad-hoc mutants killed. **F11 resolved**: the one public rate identity = (rate_card_version, CREDIT) of the listing's card (0008 pin) = `rc_marlin2b_2026_09_provisional` on hosted; the `…_provisional_p01` id is local to marlin_release()/certify, never hosted, refused by project(). Wiring requests (coordinator, at merge): v2/`__init__.py` `_SUBMODULES` + `lib/contracts/v2/types.ts` export; P-22 amendment text (evidence §4) → 15-pending-inputs; R109 (evidence §5) → 08 §10; G7 gets the seam test (appendix A, red today: 25 validation errors, tools, 120 s vs 82 s, zdr); D10 runs alias_compatibility.json on real PG before/after its migration. Findings routed: two definitions of "the card" (state/catalog.py:47-52 newest vs the listing's 0008 pin) → D10 (single authority) + G7; legacy-regime price_check in pilot.py:117-126 checks a CREDIT card → coordinator wiring with G7's test; config default cap still 120 s (config.py:50,99) → projection from running settings (contract test); E4B-endpoint.md + certify endpoint_doc name the minted card and 120 s → E4C regenerates from the projection; **product decision for the user**: the OpenRouter document's `cost_usd` has no USD price in the credit regime (no conversion) — left unpublished; S2M §2.5 lists video/mpeg but no code accepts it (doc drift). Verification: `verify-lane` workflow.

## Tracker published; E1C handback (22:15Z)
- Tracker snapshot rendered from the tracker lane's renderer at f1a202ab in a scratch worktree with every update file received (S3, F2C-C, M6/E3C coordinator updates; overlay revision 5; `check` 0 errors / 12 warnings) and published to the user's tracker URL https://claude.ai/artifact/Rk84qe3Yuzr2d357GoeRvZ (version 47; v46 = the historical backend-first tracker). Lane update files must carry host-UTC `at` (F2C-C stamped 22:15Z at host 22:12Z; its confidence 'med' was normalised to 'medium' by the coordinator writer). The renderer itself is still under verification; the render is a snapshot, not evidence.
- **E1C handback**: code head 2531dc49, evidence f6c7d1b8 on `codex/e1c-client`. RV-07: bench.upload speaks the mounted contract (create fields, destination_ref = infrx-upload:<handle>, PUT to {base-url}/uploads/{handle} never from the answer, empty completion; real-router in-process conformance test; old client 400 at create, old fake KeyError). New dataset.py (streamed JSONL manifest, SQLite journal written before the request leaves, bounded upload/inference concurrency, interrupt + lost ack + resume = one accepted key per item, edited item refused, different model/base-url/dataset-version refused, `--retain-output` required, failure export → `run --only`). RV-08: rows carry first/transport-retry/resume and fresh/replay; capacity counts fresh only; INVALID on unexpected replay, missing attempts, driver lag > bound, identity mismatch, unreconciled counters, no profile; parser version e1c.1; `--validate-raw`: E1B-box-bda1586 VALID, E1B-box-4226315 L2/L3/L5 INVALID (57/111/105/57 unexpected replays — RV-08 confirmed on the old cells). Run profile schema (six §1 groups) + `--validate-only` (no request) + enforced max_duration_s/max_drain_s; §6 measurement block; cost "unavailable"/"estimated (source, date)"; F5 → `target.path` (public-edge/direct-gateway/direct-engine; P4 must be public-edge and see 429/503 else INVALID); F8/P-24 → `target.test_key_ids` + `--key-inventory` refusal. Tests: bench 68 → 96; test_certify 38; bench mutant list 80 / 77 killed / 3 controls / 0 problems. Wiring requests: certify.py `bench_argv` passes `--profile`/`--key-inventory` per cell + a `bench_validity` rung verdict (E3C/E4C runner owner); then `--profile` mandatory for non-local targets; E1B-protocol.md §5 items 9–11 (coordinator at merge). Rulings proposed: capacity = fresh only + INVALID conditions (→ numbered at merge; F2C-C already holds R109, so E1C's becomes R110) and a question for M5/D10: does a 400/404 refusal of an upload handle consume the Idempotency-Key (dataset.py quarantines and never re-bills). Hosted smoke command recorded (coordinator runs it after run3; 404 on upload create = BLOCKED on G7/M5, not a client failure). Estimate 2/4/8 h medium (M5 restart test, smoke analysis, review). Verification: `verify-lane` workflow started.

## F2C-L slice a landed (22:16Z) → consumers pointed at 2d5e4743; F2C-L continues with b + d
Head d566c83b (code 2d5e4743) on `codex/f2c-lifecycle`: `contracts/v2/lifecycle.py` (records, LifecycleRefusal = 19 reasons raised as EXISTING codes, reason never serialized; Protocols **UploadRepository** create/acknowledge_put/complete/abort/resolve/expire + UploadConstraints.parse; **ReadinessStore** admit_ready/readiness (None = never completed; sources=() = completed empty)/claim_preparation; **ContentLifecycle** register/references/candidates/claim/tombstone (recheck inside)/acknowledge_delete), fakes/lifecycle.py reference adapter (reopen() = another process), conformance/lifecycle.py 19 cases (UPLOAD-RESTART, ADMISSION-READY, RETENTION-DURABLE) via V2_SUITES["lifecycle"], 11 fixtures + lifecycle_refusals.json, TS twin lib/contracts/v2/lifecycle.ts re-exported from v2/types.ts, 01/02 plan appendices. Tests: contracts 1149 (+3 HarnessBusy on 55432), test_lifecycle 48, lc_* mutants 34/34, m/w/g sanity 1107, console 294, TS contracts 156, v2 mutants 36/36; RV-02/RV-05 reproduced on real code then killed as mutants; fixture hash provisional 6cbe122b….
**Decisions**: D1 confirmed with an objection accepted by the coordinator — NO backfill (text-only cannot be decided in SQL: zero job_media rows is exactly the RV-05 ambiguity); a preparing job without a marker ends at preparation_deadline_at via the 0016 reaper as preparation_failed, hold released; only admit_ready writes a marker. D2 confirmed + claim TTL > object-store request timeout. D3 confirmed; scrub mechanics in slice b; 0014 job_results_immutable needs a replacement guard allowing only the scrub (D10). D4 confirmed: `now < instant` live, equality passed. D5 confirmed with conditions: admit_ready dual-writes 0003 job_media (rollback compatibility); old runtime writes no markers; every old→new cutover pauses admission until no preparing job lacks a marker. FinalizedSource.duration_s required (0010 finalized-facts); 0010 extends with additive nullable columns. Rulings proposed (five) → numbered R110–R114 at merge (F2C-C holds R109; E1C's → R115; the pre-admission-refusal/idempotency ruling from D10/M5 → R116). Open: contracts README table (slice d); SURFACE_VERSION bump once at the merge of both F2C lanes; 08 §5 names for upload window/grace/claim TTL/retention (D10 proposes; values P-25). Estimate b+d 5/8/12 h medium.
Coordinator ops: D10, M5, W5, G7, M6, E3C messaged with the port signatures and decisions; F2C-L continued (b, d); a read-only Opus verifier examines slice a in a scratch worktree (no fix round while the lane keeps working; findings routed back to F2C-L).

## TRACKER verified and merged; F2C-L slice a verified read-only (22:32Z)
- `verify-lane` on TRACKER (3 lenses → fix round → recheck): ACCEPT_WITH_FIXES at 803f6cb1. Blocking findings fixed in the round: apply-updates accepted future-dated updates (one bad clock then blocks every genuine update for that lane) → rejects `at` > host now + 15 min; resource-constrained ETA seams (exclusive locks, GPU window start, serial integration queue) had no killing test → oracles added; gates could render ACCEPTED with required cells missing → green computed over the roots' test_ids; malformed update (string estimate) crashed every later apply → rejected as malformed; no-lane task silently counted as zero hours → test. Merged `--no-ff` at 2cae9a4c. Coordinator follow-up (this tree): the lanes wrote confidence 'med' (the lane-rules file said "low/med/high" — coordinator error, rules file corrected) → alias normalised to 'medium' in `judge()` + a unit case; rejected ingestions cleared and re-applied: F2C-L, E1C, F2C-C now applied; coordinator updates for TRACKER (complete) and F2C-L (running, slices b/d, 5/8/12 h). Overlay revision 7; `check` 0 errors / 13 warnings; review queue E1C, F2C-C.
- Read-only verifier on F2C-L slice a (2d5e4743): ACCEPT_WITH_FINDINGS, nothing blocking; every core claim killed by a named case (46 own mutants: 27 killed, 19 survived in the exported conformance suite's gaps). Majors routed to F2C-L for its current slices: F1 the legacy regime is never exercised by the conformance suite (legacy admit_ready path unreachable; a D10 adapter forgetting the legacy marker would pass while W5 refuses every legacy job); F2 abort has no positive coverage and too_large is never asserted; F3 the delete-reconcile path (tombstone → lapsed claim → candidate → re-claim → ack → re-register generation+1) is untested — content-addressed source keys could stay content_retiring forever after a sweeper crash. Minors F4–F10 (deletion guards, admission checks, register guards, upload retry clock/mime/bytes, ticket `refusal` serialized on UploadTicket vs "never serialized", ReadinessStore docstring, Python laxer than TS on schema_version/timestamps/duration_s). Counts re-run and matched (1149 / 48 / 330 / 156 / 294 / 14 / 36; fixture hash matches; merge-tree clean onto the integration head and onto F2C-C). Ruling numbers: F2C-C R109; F2C-L R110–R114; E1C R115; pre-admission refusal/idempotency R116.

## F2C-C verified and merged (22:36Z)
`verify-lane` on F2C-C: ACCEPT_WITH_FIXES at b8de6171 (fix round 35d1e32a: `project()` now takes the enforced `serving_profile` and refuses any record `violations` flags — no `retention` argument; every guard isolated by its own test: private+active visibility, modality/stream-output/serving-revision declarations, violations() modality/mode subsets and "video on a text-only profile" via `profile_patches` in cases.json, cap boundary +1 (83 s vs 82 s, 2049 tokens), model_revision form; 15/15 reviewer mutants + 18/18 lane mutants killed; Python 98 / contracts 1173 / console 343 / TS 54). Merged `--no-ff` at 2bd7f347; coordinator wiring commit 179a1a0b: `contracts.v2._SUBMODULES` += published_fixtures, published_model; `lib/contracts/v2/types.ts` re-exports published-model.ts; **R109** numbered in 08 §10 (fix-round wording); **P-22 DECIDED** appended to 15-pending-inputs ("resolve, then price": one resolution by the catalog grammar, CREDIT priced by the listing's card, legacy USD by the canonical model_revision row `pv_marlin2b_usd_2026_09_r1`, requested_model recorded verbatim, nothing rewritten; owners D10/G7/G8). Merged-tree checks: contracts 1173 (3 PG cases deselected — port 55432 held by E2C's baseline), console 343/343, typecheck clean, validator PASS (904 links / 183 docs). Verifier caveat kept: the "minted card never written to hosted" claim is document-backed (session-02 rate_card_versions = 1 row on 09-22) until the coordinator's read-only box check (F2C-C wiring request 5) runs after run3. G7 consumer change: `project(..., capability, profile=serving_profile(<runtime>))`.

## I8 handback (23:00Z) → adversarial verification started
Code head 103d20a8, evidence d2f90ce6 on `codex/i8-operate` (no box/AWS/SSM/S3/hosted action). S1 pool budget: `infra/runbooks/pool_budget.py` from the deployed knobs — today's defaults peak 21+2 > 15 session slots (reproduces EMAXCONNSESSION); DATABASE_POOL_MAX_SIZE=6 fits (13+2); local pgbouncer stand-in (infrx-i8-pgbouncer 55477): on 6543 the session `set role`/`statement_timeout` are lost for the client and leak to others, psycopg auto-prepare breaks; SET LOCAL / xact lock / role defaults safe; transaction limit 200 (Supabase Nano/Micro documented; tier ⚠️ unverified). S2: env files carry `# INFRX_ENV_SCHEMA 1:<digest>`; `preflight.py envcheck` (names only, in-image, ExecStartPre; exit 2 on an unknown key); `privilege_probe.py` 17 denied attempts — today's postgres login FAILs, a least-privilege stand-in PASSes. S3: `infra/observe/` (durable.py read-only on 6543, host-probe.sh, canary.sh text + in-cap video with the key in a 0600 header file, deliver.py, rules.py, observe.sh; systemd units from a pinned /opt/infrx/observe copy so rollback doesn't blind them); `infra/alerts/operations.json` v1 24 rules; steps 72/73; runbook observe.md. S4: step 74 test alert + recovery — BLOCKED on P-25. S5: artifacts.py digest manifest (refuses non-matching bytes), step 80 mirror (MIRROR_URL no default), step 81 restore MODE=fetch|swap|undo timing fetch/verify/load/warm-up/ready/first-usable separately; supabase_policy.py reads backups/PITR/pooler limits. S6: known-good.py/.json — 27af05a NOT known-good, 4226315 and bda1586 known-good (from bundles + records); step 85 box check; verify-journey.sh = real in-cap video job → result → authoritative usage + the edge 503-after-readiness check; EXPECT=maintenance; drift.py --request-id; install/rollback label cold-start vs readiness-only. S7: runbooks (single-GPU outage in numbers, target allowlist, ops O1–O14, interim DATABASE_POOL_MAX_SIZE=6), steps 79 (names-only evidence export) and 86 (bounded cleanup, dry-run default). Tests: tests/i 206 + 1 strict xfail (F4), I8 mutants 59/59, test_runbooks 8, test_observe 7; rehearse.sh NOT RUN (quay.io 401); in-image import defect fixed af9e1a29. Findings: 8 of 20 alerts.json rules have no producer (F4 list); GpuUnavailable would page every minute (gateway infrx_gpu_up always 0 → overridden until WR-I8-4); RLS masks a table grant ("RLS only"). Wiring requests: WR-I8-1 prepare_threshold=None + session SETs only off 6543 (pilot.py coordinator, jobstore.py D10); WR-I8-2 infrx_db_pool_* families + record_pool in gateway/worker /metrics (coordinator at merge); WR-I8-3 large-body slot gauges (G7); WR-I8-4 collect_host(gpu=False) in the gateway (coordinator); WR-I8-5 processor digests in serving-version.json (coordinator at merge, W-owned artifact); WR-I8-6 runtime login without service_role + role-level timeout + function list + read-only monitor login (D10). Rulings proposed (service-not-readiness, pooler discipline, versioned env file) → R117–R119 at merge. Blocked: P-25 (alert destination/owner/escalation, mirror prefix approval), a Supabase access token for the policy read, D10 logins, P-24 canary spend (288 tiny requests/day) + two maintenance windows. Estimate 4/8/16 h medium. 15 coordinator ops listed in the evidence (after run3). Verification: `verify-lane` workflow; D10 and G7 messaged with their WRs.

## F2C-L complete (23:02Z) → adversarial verification started; consumers pointed at 1b9c7411
Head 1b9c7411 on `codex/f2c-lifecycle` (slice b 10c03d82, F1–F10 closure 3a21e0bf, slice d 9bf7a95f; evidence F2C-lifecycle-9bf7a95.md). Slice b: `TerminalOutcome.result_expires_at` persisted once by settlement (worker proposal ignored; absent on non-success and pre-F2C.b records; never recomputed); `v2.lifecycle.read_outcome(outcome, db_now)` → pending / available / no_result / held_unknown / expired (equality passed) / unavailable (fail-closed without persisted expiry); result_read_cases.json cross-language; old-record oracle terminal_success.json; scrub specified (result bodies are lifecycle content until the persisted expiry, then claim → tombstone → scrub; job row/outcome/usage/settlement/ledger/idempotency survive; 0014 replacement guard for D10); wire bodies unchanged; not in the 0017 journal payload; optional→required = NOT VALID check, VALIDATE at zero violations, never a backfill. Slice d: `conformance/acceptance.py` versioned transcripts (f2c-lifecycle-acceptance.1, 24 cases / 281 steps; `replay(factory) == []` accepts an adapter); 01 consumer matrix (D/M/W/G/C/E file:line); 02 rollout contract (expand → verify → drain → switch → resume + rollback conditions); contracts README rows; F2C fixture hash da29215d…. F1–F10 all closed (legacy regime seeded; abort/too_large; delete-reconcile; guards; UPLOAD_ABORT_REASONS + media_refused; strict duration_s; docstring). Tests: contracts 1162 (6 PG deselected), test_lifecycle 61, F2C mutants 70/70, other API 1666 + 15 skipped, tests/d 392 + 5 xfail, console 297, TS contracts 159, lint 0, typecheck clean, v2 mutants 41/41, self-test 14/14; PG suites not run (55432 held by other lanes — "m5-wired" noted: to check that M5 uses its own task id). Six rulings by name → R110–R115 at merge (E1C's → R116, pre-admission refusal → R117, I8's three → R118–R120). Open: Python RecordV2/Timestamp laxer than TS (inherited; strict-v2 pass later), SURFACE_VERSION bump at the F2C merge, D10 duration names + P-25 values, legacy admissions have no pinned revision (capability at ingress only), strict acceptance replay needs window keywords in the adapter factory. Estimate 0.5/1/3 h medium. Verification: `verify-lane` workflow; D10/G7/W5/M5/M6/E3C messaged.

## E1C verified and merged (23:04Z)
`verify-lane` on E1C: ACCEPT_WITH_FIXES at 6cb6c929 (fix round f928103a). Blocking findings fixed: closed-loop runs ignored `max_drain_s` (an in-flight request ran to the 600 s httpx timeout; now one shared `bench.drain()`, closed loop waits remaining duration + drain then cancels, `stopped_by = bounds.max_drain_s`, INVALID); an unprofiled run against a non-local target still started (now exits 2 before any file/request; the only opt-out is the explicit, logged `--unprofiled smoke` ≤ 4 requests / ≤ 512 output tokens or `--unprofiled certify` until E2C passes profiles; an opted-out summary is INVALID); dataset.py gained `--profile/--key-inventory` (manifest as the schedule); `target.path` cross-checked against the run (engine ⇔ direct; public-edge ⇔ https on the default port of a non-loopback host). Majors: fresh-only capacity fields, dataset refusals (base-url/dataset-version/form/profile-version, edited video after a lost ack, `--retain-output` required), profile identity/lag applied, enum and preflight bounds — all with killing mutants (e1cf01–e1cf26). Counts: bench 106, test_certify 38, mutants 106 / 103 killed / 3 controls; raw E1B files untouched. Merged `--no-ff` at cc687216; coordinator wiring commit bba27b02: E1B-protocol §5 items 9–11 + §3 profile amendment; **R110** numbered (benchmark validity); rulings now numbered in merge order (F2C-L R111–R116, pre-admission refusal R117, I8 R118–R120). Merged-tree checks: bench-test and test_certify green (numbers above), validator PASS. Remaining E1C integration items: UPLOAD-RESTART over M5's durable store; certify.py `bench_argv` passes `--profile/--key-inventory` + a `bench_validity` rung (E3C/E4C runner owner); the hosted smoke (`--unprofiled smoke`, after run3) is a coordinator op.

## G8 handback (23:17Z) → adversarial verification started
Code head 6a075c56, evidence 902f5959 on `codex/g8-credit-ops` (real PG on infrx-g8-postgres 55447 + composed gateway in CREDIT regime with a scripted engine; no hosted/box/AWS). Point 1: `account --user` (operator) and `statement --key-file` (the individual's own) return exact CREDIT strings (ledger_total, reserved_total, available, spent + holds + per-unit usage); the account resolves to the org the individual created, never the first membership; operator writes audited under 0009's actions; one 10,000 entitlement proved across callbacks, campaigns, operator keys and org changes; provider_dev wallet separate at zero. Point 2: `operations/transition.py` — read-only inventory → freeze source flag → bounded drain → enable target flags → recheck, audited once; rollback `--to legacy_usd`; `--freeze-only`; no conversion (USD moves only by a drained USD job's own USD debit; CREDIT ledger byte-identical across the switch; a USD job replayed after the switch keeps USD settlement). Point 3 (P-01): activation needs a published, non-provisional card named by the listing at rates the operator restates exactly; `publish-card` adds an approved card for the deployment the listing serves and never uses the marlin_release()-minted id; `credit-transition --dry-run` writes nothing, exits 1 when blocked, reports flags/USD+CREDIT jobs/holds/statements/wallets/drift/both provisional ids (F11)/key metadata without hashes (F8). Point 4: same-key twin of an operator write → recorded result or idempotency_conflict (raw 23505 fixed); two-connection grant races, lost ack, concurrent reconcile, typed refusals, 8 concurrent jobs + adjustments reconcile exactly. Acceptance (test_acceptance_pg): publish-card → transition → fresh individual grant + key → POST /v1/jobs 202 → exact hold → charge 0.00066000 → statement = account → second individual 404 and still 10000 → revoke → 401 (status reads 401 after KEY_TTL). Tests: tests/g/ops 80 (114 with test_operations_pg + test_signup), ops mutants 89/89 (base 66), tests/g minus ops 527, contracts 1078. Wiring requests → D10: `require_feature` FOR SHARE + volatile (admission-vs-freeze race; interim guard = final recheck + rerun --dry-run), `readiness_cutover_check()` in the transition inventory / `claim_preparation_ready` in pgworld.settle after 0019, settled-debit read replacing the 500-row `spent` page, 23514 → typed error. Open: F11 corollary — marlin_release() mints an id with upper-case T/Z that 0007's CHECK refuses (untyped 23514); revoked key served up to KEY_TTL (60 s) from auth/keys.py's cache → G7; P-01 still blocks activation (rates, unit, rounding, approval reference). Box dry-run (coordinator, after run3, on the merged tree): `credit-transition --dry-run --card rc_marlin2b_2026_09_provisional --input-rate 400 --output-rate 1200` expected exit 1 card_unapproved. d1 lock released by G8; g8 task id used since the note. Estimate 2/4/8 h medium. Verification: `verify-lane` workflow.

## M6 phase 1 handback (23:24Z) → adversarial verification started
Head 7b5d3f99 on `codex/m6-retention` (base f764e396; plain merges of F2C-L 2d5e4743 and 1b9c7411 to consume the port — merges only, no rebase). Points 1, 2, 4 done; point 3 (process maps, cache index/sidecars, cache-key versioning, high-water eviction, flock pin, writers registering rows before writing at generation_key, gc.py reduced to local-cache hygiene) designed with a test list — phase 2 after M5. `infrx/media/retention.py` RetentionCollector over ContentLifecycle: no process state; bounded pages + concurrency; claim → tombstone (recheck inside) → delete outside locks, idempotent, by generation_key (gen n>1 = `key.g<n>`) → ack; DB content scrubbed by acknowledge_delete; a DependencyUnavailable ends the pass keeping everything not yet deleted; only media/, uploads/, payloads/ keys without `..` are deleted; `run()` is the schedule hook. Test worlds: F2C's FakeLifecycle, an in-memory stand-in, real SQL on infrx-m6-postgres (`locking=False` = the broken variant); 75 retention cases (restart, pages, attach races at each boundary, held-transaction race, expired claim, outages per port call, lost ack/delete, delayed delete vs gen 2, every content kind with metadata kept, persisted expiry survives config change, failed scrub, late worker output, interrupted upload, clock equality, two collectors, restart between tombstone and delete, load + cleanup on PG, schedule survives a failed pass, F2C.d transcripts replay clean). Counts (INFRX_D_TASK=m6): retention + stand-in 84; retention mutants 20/20; tests/m 516 + 18 skipped (S3 without endpoint); M subset 23; test_gc 18; v2 test_lifecycle 48; kept oracle: old gc.MediaCollector 59 failed / 3 passed. Load: batch 8, in flight 4, pending-delete age 60–90 s (controlled), 5–7 passes to drain, no live content deleted. Wiring: composition (one RetentionCollector.run task in the root I8 picks over D10's PgLifecycle with claim TTL ≥ 300 s and the S3ObjectStore; start only after D10.b conformance + phase-2 writers) → coordinator; D10 (0020, scrub in acknowledge_delete, 0014 guard, pre-0019 object discovery/backfill, ContentIdentity job_id rule) → messaged; Makefile api-mutants += tests/m/test_retention_mutants.py → coordinator at merge; I8 phase 2 (bucket AbortIncompleteMultipartUpload rule, pass-report metrics, alerts on pending-delete age / repeated aborted passes) → recorded for the I8 follow-up; host `net.ipv4.ip_local_reserved_ports` for the task ports (P-21: a root change — **user action**; another lane's client held 127.0.0.1:55444 as an ephemeral source port and caused one 36-failure tests/m run + 3 v1_projection_pg failures, reruns green); reproduce.py rerun after phase 2. Ruling proposed (deletion authorized only by a committed tombstone of a fenced unexpired claim on one (content_id, generation); ack = scrub for DB content; pass stops on DependencyUnavailable; key allowlist; claim TTL > worst-case store call) → numbered at merge. Open: not integrated until D10.b; P-25 values unknown (parameterized); pre-0019 objects never collected until discovery; delayed-delete protection covers re-created objects only once phase-2 writers use generation_key. Estimate 10/16/26 h medium (phase 2 8/12/20). Verification: `verify-lane` workflow (merge order: F2C-L first, then M6).

## Host op: task ports reserved from the ephemeral range (23:27Z; user: "do it yourself, you have the permission")
`/etc/sysctl.d/60-infrx-task-ports.conf` → `net.ipv4.ip_local_reserved_ports = 55432-55499,56379,56700-56999,58123,59000,59100,59110`; applied live (`sysctl -p`) and persisted. Reversible: delete the file and `sysctl -w net.ipv4.ip_local_reserved_ports=`. P-21 recorded as resolved on this host (15-pending-inputs). Verified: /proc/sys/net/ipv4/ip_local_reserved_ports now lists the ranges.

## E3C phase 1 handback (2026-09-25T00:05Z) — the red harness; gate FAIL on the base tree as expected
Head a9d51333 on `codex/e3c-integration` (evidence run at 8406c798; base f764e396; owned paths only: tests/integration/backend/e3c/). runner.py provisions E2's services in namespace e3c through run.py's preflight/services/migrate; per-scenario + per-control verdict.json (PASS/FAIL/BLOCKED/INVALID/NOT RUN, reasons, JUnit properties, reproduction commands, raw refs, head at start/end, deviations, resource usage); gate = worst status, exit codes = E2C's gates.py; a missing service → every scenario BLOCKED (measured live: quay 401). world.py reuses E3B's stack and pilotbox; faults hold a process at a named step of the real code path then SIGKILL the process group Box started; nothing killed by name; operator CLI as a subprocess with the key in env; collectors as fresh processes; F2C-L updates applied (admit_ready fault points, ReadinessStore.readiness oracle, read_outcome in s07); s13 CATALOG-TRUTH; s01 runs E1C's dataset.py as the external client. **Run 3 on f764e396: 13:46 wall, 20 failed / 33 passed / 4 skipped, gate FAIL (exit 1), teardown clean.** FAIL s03 (RV-02: 404 for the upload on a second gateway and after SIGKILL); s04 (RV-05: text job prepared/run/debited while acceptance held; late rejection charged; video claimed its preparation lease before attach; permanent preparation refusal not terminal after 45 s — D-19); s05 (upload/admission/readiness crash points red; the other six recover once; post-publication crash → lost_after_publication, never regenerated, no charge); s06 (RV-03: a fresh collector deleted a live job's source, also with PostgreSQL paused; no scrub past expiry); s07 (RV-11: after restart at +86400 the gateway reports settled+86400, not the persisted expiry); s08 (paused PostgreSQL / S3 leave POST /v1/jobs unanswered > 60 s vs the 45 s bound; Valkey loss recovers); s10 (RV-09/D-31: the runtime login can `reset role` to the ledger owner, delete jobs, insert ledger grants); s13 (RV-01: zdr:true, concurrency 16, tools advertised but refused). BLOCKED s01 (E1C), s09 (G8), s11 (M6/D10). PASS s02, s12. Negative controls detected for journey-revoke, journey-tenant, roles-browser, credit-cutover, verify-repro; upload-restart/result-expiry NOT RUN (guarded cases red); admission-ready/retention-durable revert-type → phase 2. Layer 0 130 passed / 79 skipped; e3c unit 12. Wiring: WR-1 harness NAMESPACES e3c=1400, WR-2 minio repin (quay 401), WR-3 Makefile `backend-local` + consumer-local stage → E2C (messaged; Makefile line = coordinator at merge); WR-4 runtime login for the clone → D10/I8 (already requested). Findings routed: s04/s05 → W5; s07/s08/s13 + post-publication outcome text → G7; s10 → D10 (WR-I8-6). Proposed rulings (BACKEND-LOCAL verdict semantics; a stalled dependency answers a retryable 503/504 within 45 s and a same-key retry yields one job) → numbered at merge. Phase 2 (after the merges): rerun on the merged SHA without `--s3-image`, re-point the seams per lane handbacks, run the two revert controls. Estimate 3/6/12 h medium. **Verification of the harness queued** behind the four running `verify-lane` workflows (host load 11–13/16; the harness spins up a compose stack).

## E2C and G7 handbacks (2026-09-25T00:07Z); tasklocal decoy-band fix
- **Port fix (coordinator error)**: the D harness decoy is PostgreSQL port + 40 (tests/d/test_pgharness.py), so my Valkey/S3 reservations at 55472–55476 collided with the d1–d5 decoys (E2C and G7 both hit `test_the_decoy_is_the_tasks_own_and_d1s_is_unchanged`). Fixed on the integration branch: w5 valkey 55491, g8 valkey 55492, e2c valkey 55493 / s3 55494, i8 valkey 55495 (55469–55471 unchanged); contracts test 251 green; no decoy/reserved clash. Lanes still running (W5, D10, M5) told; finished lanes pick it up at merge (tasklocal.py resolves to the integration side). I8's pgbouncer stand-in at 55477 (= d6's decoy) moves to 55496 at its merge.
- **E2C handback** (head 924b1bdb, code 3a113dc9): environment.json/ENVIRONMENT.md/preflight.py (pins: Py 3.12.3, uv 0.11.8, node 22.23.1, pnpm 9.15.9, bash 5.2.21, coreutils 9.4, flock, systemd 255, git 2.43, docker 29.6.2 / compose 5.3.1, six images by digest; exits 0/3/4; ephemeral-port overlap reported as risk, never a verdict flip; dev servers :3111/:3112 recorded). RV-12 on this Linux runner: meminfo case did not fail here (reproduced with a no-procfs simulation; build case uses a fixture procfs, a Linux case keeps the real read; G mutant host_memory_not_read killed); git default branch fails only with defaultBranch=main → `--initial-branch`; 17 cases declared LINUX_USERLAND (BLOCKED skip off Linux); W4 list 109 passed, all 5 audit ids killed (the Mac survivor is not identified in the audit file). Counts: `make api-test` @dff31efc 3686 passed / 0 failed / 27 skipped / 5 xfailed (1811 s); @48e02236 3679 / 8 failed (7 HarnessBusy on the shared d2-valkey lock → 20 passed on the e2c namespace; 1 = the decoy collision above); mutant lists i 226, w3 87, w4 109, G 339 (+1 coverage fix); bench 68; console 289 / lint 0 / typecheck; test_preflight 21; run.py layer 1 232 passed / 103 skipped; skips = 24 missing S3 endpoint + 3 empty mutant subsets; 5 strict xfails classed as quarantine (a stage with any skip/xfail is BLOCKED). No product defects. Dependency alert #1: pytest < 9.0.3 (GHSA-6w46-j5rx-g56g, medium, dev-only; runtime image --no-dev; mitigation: private basetemp per pytest stage; fix = relock pytest → 9.1.1, trialled on contracts/g/w/m/j/t/i). Wrappers consumer-local.sh / backend-certify.sh / app-e2e.sh via gates.py → verdict.json (0/1/3/4); `--break-seam readiness|expiry` → FAIL; backend-certify with --box and no profile → INVALID, with a valid profile → NOT RUN (never starts a live run); consumer-local run 3 = FAIL (d: decoy; m/w BLOCKED S3; integration-l2 BLOCKED MinIO pin). Wiring (coordinator at merge): Makefile targets; pyproject pytest>=9.0.3 + uv lock; the tasklocal fix (done). Open: consumer-local cannot PASS here until the S3 endpoint + E2 layer-2 MinIO pin exist; Mac behaviour simulated; D harness leaves a Created container after a failed bind; mutant copies do not inherit INFRX_D_TASK; canonical api-test uses the shared d1/d2/q3 ports (flock). Estimate 1/3/6 h medium. **Resumed** by E3C's WR-1..3 message (harness NAMESPACES e3c=1400, MinIO repin, backend-local wrapper) → second handback expected; verification after it.
- **G7 handback** (head 182f41ec, code 2bbfe0e3; plain merges of F2C-C 042ea49d, integration d23e0dd3, F2C-L c058835a/1b9c7411; export conflicts resolved keeping both sides): point 1 GET /v1/models = F2C.c PublishedModel via project(profile=serving_profile(runtime)) from the catalog rows a consumer key resolves; lists only if violations() is empty; CREDIT lists only ACTIVE_RATE_CARD_VERSION (F11), legacy the canonical revision's USD identity; catalog outage → 503 + Retry-After; provider-models.json deleted, provider_document() renders OpenRouter v2.4 (no USD in CREDIT, concurrency = per-key admission limit, zdr false). Point 2: CREDIT ingress refuses an unapproved card before admission (no job/hold); all 11 spellings listed iff admitted; models.price_check per-regime probe. Point 3: Jobs.result_expiry removed; status/result/DELETE via read_outcome(outcome, db_now); sync answer/replay/recovery 410 past the persisted expiry or when none persisted; six states documented; not-owned/unknown/malformed → one identical 404. Point 4: 429/503 with retry guidance in sync/SSE/async/Prefer-async; E1C's upload sequence works on the mounted routes; foreign tenant 404 on PUT/complete/chat; SSRF to 169.254.x refused; CI check runs every E4B-endpoint.md example against the mounted app and /v1/models; WR-I8-3 metrics infrx_large_body_slots_in_use/_limit, infrx_large_body_refused_total, infrx_intake_drained_total{code}. G8 finding: revocation bound STATED (a revoked key may read/cancel its own jobs ≤ KEY_TTL 60 s; next POST refused immediately; tests/g/jobs/test_revocation.py). Tests: tests/g minus ops 567; G/uploads/jobs mutant lists 505 all killed (+33 G, +5 jobs, 5 re-anchored; revocation mutant 1/1); `make api-test` (INFRX_D_TASK=g7) 3912 / 1 failed (the decoy collision) / 27 skipped / 5 xfailed; console tsc clean, 351/351. Wiring: WR-1 app.py assert_release_profile at pilot startup + MAX_VIDEO_SECONDS default → 82 (limits.py:62, config.py:50/:99, test_config_and_imports.py:26; a pilot left at 120 lists nothing but still admits 83–120 s clips) → coordinator at merge; WR-2 pilot.py:287 price_source = Probe(models.price_check(...)) with WR-3a; WR-3 (D10) usd_price(model_revision), active_rate_card = the listing's card, owned reads carry result_expires_at — **HARD ORDER: G7 point 3 merges with or after D10.c, else every PG success returns 410**; optional listing_version + card-approved flag; WR-4 four Spec entries in observe/metrics.py FAMILIES (coordinator); WR-5 retire MODELS_DOC; WR-6 endpoint_doc Limits table still says 120 s / "P-20 applies 72" (E4C); WR-7 A3 reads the projection. Open: the relay still calls admit/admit_credit pending D10's ReadinessStore; M5's MediaUploads not merged here; held_unknown divergence (sync path serves the text while /result shows no content — predates G7, needs a ruling); a key cached before an identity-source outage outlives KEY_TTL (product decision); OpenRouter listing without USD (product decision); revocation bound → U2 copy. Estimate 1/3/8 h medium. **Resumed** by E3C's findings message (s07/s08/s13, post-publication text) → second handback expected; verification after it.

## M5 handback (2026-09-25T00:08Z) — verification queued (merge after D10)
Code head 35c401bd, evidence e164bf11 on `codex/m5-uploads` (plain merges of the tasklocal patch, F2C-L 2d5e4743/3a21e0bf/1b9c7411 and **D10.a dee59ed8** — so M5 merges after D10 completes; D10's later commits supersede). MediaUploads now runs on F2C's UploadRepository (uploads=) + ContentLifecycle (content=); no per-upload process state; ProcessUploads (M3) only as the no-database default and the negative control (reproduces RV-02: 10 failed / 1 passed on the old adapter). Create via UploadConstraints.parse (null max_bytes/accepted_mime still refused per v1 conformance); PUT = receipt first → content row → write-once destination (refused PUT stores nothing; retried PUT no-op); finalize = resolve → HEAD (over MAX_MEDIA_BYTES refused before download) → download → acknowledge measured → refuse wrong declared size/digest/type (durable abort) → probe (refusal aborts media_refused) → register source → write → commit; single flight per upload; downloads under the preparation gate; resolve_owned through the ticket (unfinalized = not_found, R99(a)); attach checks the ref against the tenant's content-addressed object; gc.py minimal touch (expiry via the port; sweep fails closed on a durable authority). test_upload_wiring.py drives 5 composed apps against the frozen wire DTOs (negative control: today's composition 404s at the second gateway). E1C's question answered and tested: a refusal before admission does NOT consume the Idempotency-Key. test_upload_restart.py on F2C's reference authority AND D10's PgLifecycle (crash after each durable step, duplicate/concurrent calls, window vs admitted job, two tenants, forged digest/second finalize); memory +0 B (process-local control +1.8 MB / 400 tickets); F2C.d UPLOAD-RESTART transcripts replay exactly on ProcessUploads; test_upload_restart_stack.py = separate OS process per step with real os._exit (8 passed, 1 strict xfail = D10 gap). Tests: tests/m + G upload route 486 passed / 1 failed (WR-2) / 2 skipped / 1 xfail; M/MPILOT/S3 mutant lists 321 passed / 18 S3-only skipped; test_s3_mutants on MinIO 47; 248 M mutants (46 on uploads.py); broad g/w/i/contracts 2033 / 2 failed (WR-2); tests/d after the D10 merge 415 / 18 failed (11 = D10.a vs F2C-final cases; 7 = outbox[valkey] HarnessBusy on d2's port). MinIO: the pinned quay image 401s → pgsty/minio@sha256:b6bfe723… (test_s3 40/40; E2C repins). Wiring: WR-1 pilot.py adapters_from_env "lifecycle": PgLifecycle(connect, limits=settings.pilot) on the stores' pool; build_ingress_deps(..., lifecycle=None) → MediaUploads(uploads=, content=) — needs D10's infrx/state/lifecycle.py; until then the pilot is NOT restart-safe; WR-2 tests/g/uploads/test_uploads.py:666 store.refs → resolve_owned (G7 area; coordinator at merge). Open → D10 (messaged): 0019 lacks media_refused / UPLOAD_ABORT_REASONS / past-window abort refusal; 19 acceptance-replay mismatches on D10.a; → F2C follow-up: the port has no read-only ticket() (pre-download bound is MAX_MEDIA_BYTES, not the ticket's cap; route PUT buffers up to MAX_MEDIA_BYTES under LargeBodies slots) and the F2C fake overwrites a ticket when the handle source repeats a handle; → M6 phase 2: replace the gc destination sweep with ContentLifecycle candidates/claim/tombstone over the rows M5 registers (upload_destination, source); changed test expectations tabled (expired PUT 409 after expire(), squat not_found, retries past the window 410). Ruling proposed (upload tickets across processes: receipt before write-once destination; completion verify → write content-addressed source → commit, any process's retry finishes a crash, first bytes final; handles resolve through the ticket: foreign 404, past expires_at 410, unfinalized 404 — retires R99(a)'s "otherwise indexed" arm; ticket expiry bounds new use only; a refusal before admission does not consume the key) → numbered at merge. Estimate 1/3/6 h medium. Verification queued behind the running workflows (host load); E3C's too.

## E2C follow-up handback (2026-09-25T00:12Z); host reservation extended
Head 0598f16e (code f61d2f0b) on `codex/e2c-verify`: WR-1 harness.NAMESPACES "e3c": 1400 (ports 56900/56923/56932/56979/56980/56990 inside tasklocal's e3c block; test_harness per-namespace checks pass); WR-2 MinIO repinned to pgsty/minio@sha256:b6bfe723… (quay pin confirmed 401; OCI index resolved by `latest` and RELEASE.2026-08-04T00-00-00Z; minio --version d88f46cc; functional check on 55512: health, bucket create/put/get/list, NoSuchKey) in compose.yaml/environment.json/README/ENVIRONMENT.md; new test: an unavailable pin leaves preflight BLOCKED (never pulls); run.py --layer 2 on the new pin NOT RUN (E2's e2 namespace is outside the lane's ports — coordinator runs it at the E2C merge); WR-3 consumer-local ends with backend/e3c/runner.py, verdict.json["verdict"] must match the exit code (mismatch INVALID; exit 0 with no verdict FAIL; unknown verdict INVALID; missing runner NOT RUN; 7 tests). P-21 recorded in ENVIRONMENT.md; preflight PASS for consumer-local/integration-l2/backend-certify. test_preflight + test_harness + test_run 108. Wiring: Makefile `backend-local` + .PHONY (coordinator at merge); tasklocal collision — E2C's proposed 55483–55486 would collide with the m5/m6/w5/g7 decoys (55443–55446 + 40); the coordinator's fix 2efdeebd (55491–55495) stands; pytest ≥ 9.0.3 relock (coordinator at merge); rehearse.sh:43 MINIO_IMAGE still the quay pin → deploy/ is I8's (coordinator applies at the I8 merge); host: 55500–55599 added to the reservation — **done now**: `/etc/sysctl.d/60-infrx-task-ports.conf` = 55432-55599,56379,56700-56999,58123,59000,59100,59110 (applied). Open: consumer-local cannot PASS until the port fix, E3C's runner and an S3 endpoint for m/w land on one tree. Estimate 1/3/6 h medium. Verification queued (with E3C) behind the five running workflows.

## F2C-L verified and merged; F2C implemented (2026-09-25T00:19Z)
`verify-lane` on F2C-L: ACCEPT_WITH_FIXES at c4873027 (fix round 16ce171a). Findings fixed: the tombstone's generation check was the only guard against a stale older-generation claim (fences restart per generation) → case + mutant, 02 D4 now states every claim check compares (generation, fence); the RV-05 not-ready case ran with no markers in the store (a claim gate keyed on org or on any marker passed) → the case now runs in the cutover state per regime; RESULT-EXPIRY never read through the legacy USD path (a recomputing get_owned passed) → the committed-success cases run over both regimes; the D10 scrub guard as written contradicted 0014's `bytes = octet_length(body)` check and the "no drop" expand rule → 02 now spells the guard column by column (UPDATE allowed only if old.scrubbed_at is null, new.scrubbed_at = now, new.body = '', identity/digest/bytes/created_at unchanged, job settled and expiry passed or absent; DELETE/TRUNCATE stay forbidden; the bytes check is replaced by the one named exception); the NOT VALID expiry check would have broken every later UPDATE of a violating old row (journal expiry 0017:320, aged-hold reconcile 0018:513) → the check is added validated only at zero violators, never NOT VALID over violators; four no_result rows added to the cross-language read table; the lane's own tests/d claim withdrawn (it had run on d1 without a task id — evidence corrected). Merged `--no-ff` at 78661bf0 (conflicts in v2/`__init__.py` `_SUBMODULES` and lib/contracts/v2/types.ts resolved keeping both F2C-C's and F2C-L's exports). Merged-tree checks: tests/contracts 1263 (3 PG cases deselected), console 351/351, typecheck clean, lint 0 errors. Coordinator commit b413f253: **R111–R116** numbered (lifecycle ports/refusals; one-phase readiness; cutover without backfill; content lifecycle incl. reconcile + per-generation fences; time authority; persisted expiry + read classification); **SURFACE_VERSION contracts-v2.0 → contracts-v2.1** (both F2C lanes merged; map.json regenerated; 438 pinned tests + money-units.test.ts green); **manifest F2C → implemented** (evidence: F2C-catalog-d414607, F2C-lifecycle-2d5e474, F2C-lifecycle-9bf7a95); ledger regenerated. Next free ruling R117. E2C verification launched (two handbacks, code f61d2f0b).

## Tracker published; resume brief refreshed (2026-09-25T00:30Z)

Overlay revision 23 (F2C implemented, E1C and F2C-L complete) published to the user's tracker URL as version 49. `.claude/RESUME-NOW.md` (local, git-ignored) rewritten with the merge order, the running verifications and the post-run3 box-op list. Host load 24 on 16 cores (five verify-lane workflows + D10/W5/G7 mutant reruns): E3C's verification stays queued until two workflows finish. run3 poll at 00:16Z: soak still RUNNING.

## G8 verified and merged; HANDOFF at a session limit (2026-09-25T00:46Z)

**G8** verify-lane workflow (wf_a574e402-228; 10 agents): ACCEPT_WITH_FIXES at 09f4ab2a. 18 findings (0 blocking, 6 major). Fix round (code 44ea4770, evidence 09f4ab2a): 0-G8-R1/2-ACC-1 — a USD admission that passed the flag check before the freeze committed could commit after the drain measured zero and the transition still exited 0 and audited (reproduced on real PG with two connections); fixed in `operations/transition.py`: after the freeze commits, `apply` lists the transactions open in the database (pg_locks virtualxid ⋈ pg_stat_activity) and waits them out within `--drain-timeout-s` before measuring the drain, else blocks with `open_transactions` (freeze kept, nothing audited, rerun continues); new two-connection PG regression + fake case, 3 new mutants (92/92 killed), reverted-fix control fails; runbook step 3a = rerun `--dry-run` after the transition and again immediately before the restart. 0-G8-R2/1-RULES-1/2-ACC-2 — the lane's recorded contracts command had run on the forbidden d1 port 55432 three times after the 21:57Z isolation note (22:50/22:52/22:54Z; each created and removed its own container, no foreign DB touched) and the "3 failures under load" were HarnessBusy refusals; evidence corrected append-only (G8-44ea477.md table), correct command `INFRX_D_TASK=g8 … tests/contracts` = 1078 passed. 0-G8-R3 — revoked-key job status/result reads served for up to KEY_TTL (60 s) from auth/keys.py's positive cache: confirmed, not G8-owned; acceptance docstring states the bound; **routed to G7 as wiring request 3** (ruling (a) document the bound or (b) recheck revoked_at on reads). Minor findings (freeze without audit row, same idempotency key across --freeze-only and the enabling run, PG-only survivors, footprint oracle, F11 partial) stay recorded in the workflow result (tasks/w4dsvvizd.output) as open minors; R118 encodes the distinct-key-per-phase rule. **Host caution:** a reviewer ran `pkill -f "tests/contracts" -u rey` at ≈00:30Z; D10, W5 and G7 were told to rerun any tests/contracts count from that window.

Merged `--no-ff` at **e309b3fa** (tasklocal.py kept on the integration side). Merged-tree checks: tests/g minus ops **527 passed**; `INFRX_D_TASK=g8` tests/g/ops + tests/d/test_operations_pg.py + tests/d/test_signup.py **116 passed**; ops mutants rerun (`INFRX_MUTANTS=all … tests/g/ops/test_mutants.py`, expect 98 passed / 92 killed) last status at handoff: "116 passed, 2 warnings in 265.30s (0:04:25) ". Bookkeeping commit 6ad0e487: **R117** (regime transition incl. the wait-out amendment and the operator dry-run procedure) and **R118** (operator-write races; distinct key per phase) numbered in 08 §10 (next free R119; numbering follows merge order, so D10/M5's pre-admission refusal and I8's three take the next numbers at their merges); manifest **G8 → implemented** (evidence G8-6a075c5, G8-44ea477; closure note names the remaining merged-SHA rerun with D10, the G7 ruling, the box dry-run and the post-P-01 window); ledger regenerated; tracker overlay rev 24 (G8 complete; check 0 errors / 6 warnings) rendered and committed, **not yet published** (last published v49 = rev 23); the session's coordinator tools checked in under `research/plan/evidence/coordinator/session-03-tools/` (verify-lane workflow, LANE-RULES, tasklocal patches, run3 box scripts).

**Handoff state.** Running at the limit, results not processed: verify-lane workflows I8 (fix commits on codex/i8-operate through ce33c24c; I8-103d20a.md mid-edit), M6 (fix agent's uncommitted edits in retention.py / test_lifecycle_standin.py / test_retention.py in the m6 worktree — preserve), M5 (e164bf11), E2C (0598f16e); lanes D10 (f44499ea + uncommitted 0019/0021/test_ready edits), W5 (16ff5771 + untracked handback evidence), G7 (a0e0928a) — each messaged to commit WIP with a handoff evidence file and an update file. Queued: E3C phase-1 verification (a9d51333). run3 soak still RUNNING at the 00:16Z poll; the poll process dies with the session (re-poll with session-03-tools/rollout/e4b-poll3.sh). Full resume procedure: `.claude/RESUME-NOW.md` (local) — merge order E2C/I8 any time; D10 → M5 → M6 → G7; G7 point 3 needs D10's persisted result_expires_at.

Correction (2026-09-25T00:46Z): the "ops mutants rerun … last status at handoff" text above quoted the 116-passed line of the ops+PG suite by mistake; the ops mutants rerun status at this correction is: still in flight, not completed (rerun on resume).

## run3 finished on the box; W5 handback (2026-09-25T00:47Z)

**Certification run3 (historical E4B on bda1586, legacy_usd) finished**: exit 1; report at `/opt/dlami/nvme/e4b/20260924T202924Z/report.json` on the box (not yet fetched). Cells: preconditions/config-pin/served-build/sop-parity ok; recovery-box PEND (coordinator drills); protocol/recovery skipped (--no-stack); dataset-resume FAIL (regime mismatch, S3 finding); envelope FAIL (supported 0.5/s); **soak PEND** (0.25/s for 14,400 s, client exit 0, failure_rate pass 3/3374 with 3 platform-caused; reconciled_at_end UNKNOWN as S3 finding 4 predicted); **overload ok ("honest refusals" — first live exercise of the intake drain)**; release-identity ok (one clean tree bda1586). Not E4C acceptance. On resume: fetch with session-03-tools/rollout/e4b-fetch.sh (pattern) into `models/marlin2b/results/E4B-box-bda1586/run3-20260924T202924Z/`, then update `research/plan/evidence/e/E4B-release-decision.md`; the GPU box is now free for the queued coordinator ops.

**W5 handback** (codex/w5-readiness; evidence head b2e5452b, code head 27afab76; evidence `research/plan/evidence/w/W5-27afab7.md`, wiring patch `W5-wiring-27afab7.patch`, update W5-20260925T0035Z.json ingested → review; tracker rev 25). Items: (1) readiness barrier — `PreparationRunner(readiness=)` claims through the ReadinessStore's marker-gated door; no marker = not ready, empty manifest = complete; fails closed without a store; text canary fails when removed; F2C.d transcripts replay through the runner's doors; (2) crash boundaries incl. E3C s04/s05 as explicit cases; (3) permanent refusals end the job at the first attempt via a proposed `JobStore.fail_preparation(lease, cause)` port (F2C/D10) and the video token bound becomes the processor's worst case + 16,384 encoder budget (amends R105: the old bound refused an 82 s max-geometry clip); (4) box warm-up script W5-box-warmup.sh (not yet run); S3 F4 reconciliation gauges published by the reaper (`PgReconciliation`). Tests: W5 mutants 25/25 + 1/1 PG killed; all seven W lists 551 passed / 13 skipped at 16ff5771; tests/w 291 passed at d04526b9 — **the final full run at 27afab76 did not complete (session limit): rerun**. Wiring: worker/__main__.py compose (PgLifecycle readiness + PgReconciliation); Makefile api-mutants += tests/w/test_w5_mutants.py; F2C port + D10 adapter `fail_preparation`; G7 admission must call `admit_ready` (items 1 and 4 plus D10's 0019 merge together); certify's reconciled_at_end scrapes the worker's /metrics (8002). Proposed rulings (readiness barrier, fail_preparation, R105 amendment, reconciliation gauges) numbered at its merge. Branch also carries cherry-picks of F2C (2d5e4743, 10c03d82, 3a21e0bf, 9bf7a95f) and the tasklocal patches — expected to merge clean. Isolation incident: one run ≈22:39Z without INFRX_D_TASK took the free d1 lock (own container only). **Verification not launched (session limit)** — first action on resume after the state check.

## G8 merged-tree ops mutants green; G7 second handback; D10 WIP committed (2026-09-25T00:49Z)

- **G8 merged-tree check completed**: `INFRX_D_TASK=g8 INFRX_MUTANTS=all … tests/g/ops/test_mutants.py` at e309b3fa → **98 passed (92/92 mutants killed)**, exit 0. All three G8 merged-tree checks are green (527 / 116 / 98); the "confirm on resume" note above is closed.
- **G7 second handback** (codex/g7-catalog a682d495 = WIP handoff commit; code a0e0928a; evidence `research/plan/evidence/g/G7-a0e0928.md` (Handoff section + proposed ruling texts for the stall bound and the revocation bound) and G7-2bbfe0e.md; update G7-20260925T0048Z.json ingested → review). Since 2bbfe0e3: E3C s08 — every route dependency call is bounded (20 s per store/object call, 40 s media preparation); a paused store answers a retryable 503 with Retry-After instead of hanging; a stall after admission keeps the job and a same-key retry completes it; sync/SSE polls retry through a stall to the job deadline; an unconfirmable durable cancel claims no state (5 cases, 7 new + 3 re-anchored mutants killed). E3C s05 — `lost_after_publication` documented and pinned. **G8 WR-3 revocation: option (a) chosen (2bbfe0e3)** — new admissions refused immediately; reads, events and DELETE on an existing job refused within KEY_TTL (60 s); tests/g/jobs/test_revocation.py + a killed mutant. Not bounded (user decision): a key cached before a PostgREST outage keeps working past its TTL while PostgREST is failing. tests/g 573 passed. **Full reruns at a0e0928a did not finish** (mutant lists 69 %, API suite 43 %, 0 failures so far, running detached) — rerun on resume. Wiring WR-1..7 unchanged + **WR-8** (I8: adapter-side client timeouts that still fire on a stalled server). HARD ORDER unchanged: D10 WR-3c (persisted result_expires_at on PG owned reads) merges with or before G7 point 3. Verification not launched (session limit).
- **D10** committed its WIP at 31c2112c ("close M5's findings - an abort past the window is refused, reservations in admission order"); worktree clean; no handoff evidence/update file seen yet (the lane may still write them).

## I8 verified — ACCEPT_WITH_FIXES at ce33c24c; merge deferred to resume (2026-09-25T00:55Z)

Verify-lane workflow wf_60d0f796-43b (18 agents; lens verdicts REJECT / ACCEPT_WITH_FIXES / ACCEPT_WITH_FIXES): 35 findings (1 blocking, 13 major, 21 minor). Fix round on codex/i8-operate (7f439e2f, b55b3c5e; evidence ce33c24c = `research/plan/evidence/i/I8-103d20a.md` fix-round section): **0-B1 fixed** — the durable monitor's read-only property is now enforced by a statement-recording test (first statement `set transaction read only`, every other a SELECT/SET LOCAL; mutants durable_not_read_only/durable_writes killed); **0-M1/RS-1/ACC-1 fixed** — drift.py --request-id checks settlement by regime (USD: debit > 0; CREDIT: debit = 0 plus one credit_ledger inference_debit; hold from credit_holds/credit_wallet_holds) with a real-schema CREDIT-job case; **0-M2 fixed** — pool budget headroom/startup-peak boundary cases (MAX=7 fails 15+2>15, --headroom 0 passes, MIN=MAX=6 peak 14); **0-M3/RS-3 fixed** — manifest refusal parametrized over every PINNED file, restore round trip asserts missing:/extra:; **0-M4 fixed** — step 80 exits 1 on a manifest read-back mismatch; **0-M5 fixed** — privilege probe identity checks no longer blinded by their own SET LOCAL statement_timeout; BYPASSRLS and no-role-timeout stand-in cases added; **0-M6 fixed (part)** — step 81 verifies restored bytes against the release's serving-version.json pins (`artifacts.py verify --serving-version`), a wholesale-replaced mirror is refused; **0-M7 fixed** — 72-observe-install.sh requires CANARY_KEY_PARAM (no default) and enables the canary timer only with P24_APPROVED=<ref> (else "BLOCKED (P-24)"); **2-ACC-2 fixed** — known-good.py requires a schema_proof {through, evidence} reaching --applied when hosted is ahead of the tree (bda1586 with --applied 0025 is NOT-KNOWN-GOOD without it). Every fix re-verified by the targeted recheck (fix-reverted controls fail). **Open (lane reclassified as an explicit blocker, not fixed): 2-ACC-3** — replacement-instance restore is not covered: engine/runtime images are not mirrored (pinned by registry digest; docker load of a saved tar does not restore that digest → needs a registry copy such as ECR, a coordinator/P-25 input) and step 81 restores onto the same host/NVMe. Recorded minors stay in the workflow result (tasks/w5nnqih68.output): envcheck value echo on parser rejection, step 72 empty secret on SSM read failure, ops 12/13 need INFRX_TEST_KEY/VIDEO_FILE supplied, durable test order dependence (leaked service_role), rehearsal container/workdir left behind, the pooler-semantics probe leaves a session GUC on 6543 (R110 pattern), verify-journey in-cap by bytes not duration, proposed ruling numbers R109–R111 collide (renumber at merge), slice-3 metric retention absent, interim DATABASE_POOL_MAX_SIZE=6 not load-proven, a local MinIO image (pgsty/minio@sha256:b6bfe723…) was available for the rehearsal. **Merge deferred** (session limit; no new runs started): on resume merge --no-ff (tasklocal → integration side; drop the lane's 9da9d494 copy), apply WR-I8-1/2/4/5 at the merge, renumber its three rulings (next free R119), rerun tests/i (expect ≥ 206 + new cases) and the I8 mutant runner on the merged tree, flip manifest I8 → implemented with 2-ACC-3 named in the closure note, and add "engine/runtime image mirror (ECR or equivalent)" to P-25.

## D10 handoff (2026-09-25T00:56Z)

D10 (SQL writer) handed off at **c70d5d21** on codex/d10-durable (clean tree; nothing pushed): handoff doc `research/plan/evidence/d/D10-HANDOFF-31c2112.md`, update D10-20260925T0053Z.json ingested (activity running, head 31c2112c, estimate 2/4/8 h medium). Slices: a dee59ed8, b 13aeb6d3, c f44499ea, M5-finding fixes 31c2112c (an abort past the window is refused; reservations in admission order). Last green: full tests/d at f44499ea 791 passed / 1 skipped / 8 xfailed; targeted suites at 31c2112c 195 passed / 7 xfailed; other tracks on the D10 schema 324 passed / 7 skipped; lifecycle conformance 26/26; F2C.d transcripts 24/24 after normalization; I8 privilege probe PASS (62 checks, plain image). **Unverified:** the INFRX_MUTANTS=all run of the D lists was still running with 4 failures at ≈40 % (names unknown → rerun and triage on resume); no Supabase-image runs yet (D10 suites, test_postgrest_d10, privilege probe). Done on the D10 side: G8 `require_feature … FOR SHARE`, `readiness_cutover_check`, M5 media_refused + abort reasons, the 23514 typed error, the I8 runtime/monitor logins, the M6 content functions. Open wiring for others (listed in the doc): E3C WR-4 operator enables login for infrx_runtime; G7, W5, M5, M6, I8, G8 and C0 wire the ports. Lane claim to check on resume: "the lane ports are in the kernel's ephemeral range and collide" — the host reservation `net.ipv4.ip_local_reserved_ports` was applied at 23:27Z (current value above the entry in the shell output: 55432-55599,…), so any collision after that is another container/lane on the same port, not an ephemeral source port; the harness now retries. **D10 handback is not yet a review handback** (activity running; mutant triage pending) — on resume: rerun the D mutant lists, then launch its verification; merge order D10 → M5 → M6 → G7 unchanged.

## D10 addendum (2026-09-25T01:08Z)

D10's final head is **44a25a88** (not c70d5d21; tree clean, not pushed). The INFRX_MUTANTS=all run over the D lists at 31c2112c finished: exit 1, 720 passed, **4 survived mutants** (not setup errors): d1r_usd_job_may_carry_pins and d1r_credit_job_may_carry_usd_price mutate the 0006 constraint that 0021 replaced with a v2 (repoint the mutants at the v2 body); d2_readmits_a_request_uuid_credit mutates 0011 where a later redefinition may be the body that runs (unchecked); d2_finalized_upload_rewritable mutates the 0019 guard, probably refused earlier by a 0019 constraint. Causes and fixes in §13 of D10-HANDOFF-31c2112.md; the update file's next_action is refreshed (re-ingested). Resume order for D10: fix the four, D10 suites + test_postgrest_d10 on the Supabase image, privilege probe on that image, then verification.

## M6 verified — ACCEPT_WITH_FIXES at 572e3e8e; merge deferred (after D10 and M5) (2026-09-25T01:11Z)

Verify-lane workflow wf_de7478ac-805 (8 agents; lens verdicts REJECT / ACCEPT_WITH_FIXES / ACCEPT_WITH_FIXES): 21 findings (1 blocking, 3 major, 17 minor). Fix round on codex/m6-retention (code 188a7f01, evidence 572e3e8e; update M6-20260925T0101Z.json ingested): **0-F1 fixed (blocking)** — the real-PG race test now covers two schedules (attach held before the claim, and attach opened after the claim commits but before the tombstone) crossed with the stand-in variants, so the reference recheck inside the tombstone transaction and its FOR UPDATE row lock are each enforced (the reviewer's P1/P2 stand-in mutants, each of which deleted a live job's source, now fail the test); **0-F2 fixed** — one collector instance driven across passes through `run()` with a fake sleep (lost ack on pass 1, the same instance finishes the delete on pass 2; instance-level done-set mutant killed); **2-INT-1 fixed** — the collector adopts D10's 0020 rule for delayed deletes: the external delete is sent only while the claim has at least `delete_timeout_s` (default 75 s, s3.py's worst case) left, measured on a monotonic clock; **2-INT-2 partial / open** — the real-database race oracle still runs only against M6's own stand-in schema: a `d10` world (D10Runtime over D10's `pgtesting.make_lifecycle_factory`, the real PgLifecycle) was added and is a visible skip on the branch (27 skips); in a scratch merge with codex/d10-durable it ran — it becomes real only at the D10 merge and is the gate for M6's PG claims. Recorded minors (workflow result tasks/w0msukl6b.output): StaleLease-on-ack branch untested, allowlist trailing-slash not pinned, load-test pending bound 3780 s vs the declared 90 s, stale counts in evidence/update (test_lifecycle 61 not 48; local-only oracle 71 failed / 4 passed), paging continues after an abort, sibling candidates run on after an unexpected exception, expired-state oracle asserted on the stand-in not F2C's read_outcome, one early run without INFRX_D_TASK (d1 lock only). Merge compatibility: conflicts only in F2C-L's two files (already merged) → M6 merges cleanly after them. **Merge deferred** (session limit; order D10 → M5 → M6 → G7): on resume merge after M5, wire Makefile api-mutants += tests/m/test_retention_mutants.py, run tests/m + retention mutants + the d10 world on the merged tree (2-INT-2 closes there or stays an explicit open item in the closure note), number its rulings after I8's.

## STOP (2026-09-25T01:24Z) — user: "save and stop now"

Session ended here by the user. Integration head pushed; lane WIP committed on codex/<lane> branches; verify workflows for M5 (wkttc6zzo) and E2C (w9j72hgyv) were still running and are unprocessed (journals under the session's subagents/workflows/). Resume from `.claude/RESUME-NOW.md`.

## Session 04 (dhruv window) — resume, I8 merged, verifications relaunched, run3 fetched (2026-09-25T01:31–01:41Z)

Resumed from `.claude/RESUME-NOW.md` at f486677c: all lane worktrees clean and local == origin; every agent/shell/workflow of the sofia window was dead. Findings on arrival: M5 (dfe0533a) and E2C (3838a67c, 330e04f1) had fix-round commits from their interrupted verify workflows; W5 had added 6d526f69 (the final tests/w run at 27afab7 completed: 296 passed / 11 skipped); host load 6.5, root disk 89 %.

**I8 merged** `--no-ff` at **63011a31** (tasklocal.py kept on the integration side; 53 files, +5,358). Bookkeeping 8a86073b: **R119** (recovery proves service, not readiness — incl. the settlement-unit clause and the schema-proof clause), **R120** (pooler discipline), **R121** (versioned env file) numbered in 08 §10 — the lane's provisional R109–R111 renumbered; next free R122. Manifest **I8 → implemented** (closure note names the open 2-ACC-3 replacement-instance restore); **P-25** gains the image-mirror input (ECR or equivalent, digest-verified) and a fresh-instance restore rehearsal; ledger regenerated; tracker rev 31 (I8 complete; check 0 errors / 5 warnings). Merged-tree checks detached (`setsid nohup`): `INFRX_D_TASK=i8 pytest -q tests/i` (PID 112518, log scratchpad/wave4b/logs/i8-merged-tests-i.log) and the full `tests/i/mutants.py` list (296 mutants over 131 cases; PID 151193, log …/i8-merged-mutants.log) on a `git archive` copy of 63011a31 — results appended below when they finish. WR-I8-1/2/4/5 delegated to an Opus implementer on a new branch `codex/i8-wiring` from 63011a31 (worktree `.claude/worktrees/codex-i8-wiring`).

**Relaunched (all Opus):** verify-lane workflows for **W5** (wf_d4751004-0ee; head 6d526f69/27afab76), **G7** (wf_f940feca-dd0; a682d495/a0e0928a), **E3C phase 1** (wf_eb5430b5-2b2; a9d51333); **finish-verify** workflows (new script `session-03-tools/finish-verify.workflow.js`: one agent completes the interrupted fix round against the three lenses' findings recovered from the sofia journals, then per-finding rechecks) for **M5** (wf_ddd4f0a2-75b; 3 majors: 55432 fallback in the stack drill, R82 attach binding the caller's copy, WR-2 confirmation) and **E2C** (wf_dab3b07a-9ba; 3 blocking + 12 major: backend-certify live-run guard bypass, pytest-stage error/no-junit oracles, consumer_local composition test, runner stages ignoring required skips, suites i/j/q/t dropped, preflight never-pulls/version/platform/stale oracles, api-m/api-w S3 env so the gate can reach PASS, 55432/D2 breach recorded, integration-l2 driving other lanes' ports, api-d mutant copies on 55432); **D10 lane** relaunched to fix the four surviving D mutants, run the Supabase-image suites and the privilege probe on that image, and hand back with evidence + update file.

**Box op (logged): run3 fetch.** `infra/rollout/ssm.sh session-03-tools/rollout/e4b-fetch3.sh` (command be357cf8-…; purpose: read-only export of report.json/certify.log/work; cost: minutes of box CPU + one S3 object; rollback: delete the object) → uploaded `s3://llm-bootcamp-641134885443/w4/e4b-box/20260924T202924Z.tgz`; downloaded and unpacked into **`models/marlin2b/results/E4B-box-bda1586/run3-20260924T202924Z/`** (report.json, certify.log, work/ 4.9 MB; secret scan clean). Report: runner e4b-certify, target box bda15866 (tree clean at start and end), started 2026-09-24T20:29:25Z, 15,076 s, exit 1, `backend_ready` "not decided by this runner". Stages: preconditions PASS; config-pin PASS (engine_options_digest sha256:3c4bbfac… recomputed equal); served-build PASS; recovery-box PENDING (BOX; coordinator drills); protocol SKIP and recovery SKIP (--no-stack); sop-parity PASS (2 clips; over-cap refused 400 unsupported_media); dataset-resume FAIL (24 items; usage recorded outside CREDIT — the regime mismatch S3 named); envelope FAIL (supported 0.5/s; client exits 0 at 0.5/1.0/2.0); **soak PENDING** (0.25/s × 14,400 s; failure_rate pass 3/3374 with 3 platform-caused; answered pass 3,371 accepted; host_growth pass +13.2 MiB over 476 samples; latency_drift pass p50 2.795 → 2.880 s; client_exit pass; **gpu_growth unknown** (no GPU sample) and **reconciled_at_end unknown** (no runtime producer, S3 finding 4 / W5 F4 gauges)); **overload PASS** (32 attempts, 8 accepted, 24 refused — honest refusals, the intake drain's first live exercise); release-identity PASS. Historical E4B evidence on bda1586 in legacy_usd; not E4C acceptance. The GPU box is free for the queued coordinator ops.

## Session 04: host ops O1/O2, a classifier denial, I8 checks rescheduled (2026-09-25T01:43Z)

- **I8 O1 (host, no DB)**: `pool_budget.py --runtime-port 5432` → FAIL session (peak 21 + headroom 2 > limit 15), PASS transaction (3 + 2 ≤ 200), WARN worker pool max 10 < 13; with `--set DATABASE_POOL_MAX_SIZE=6` → PASS session (13 + 2 ≤ 15), PASS transaction, same WARN (worker pool 6 < 13: runners wait up to 5 s at full load — watch `infrx_db_pool_requests_waiting`/`_timeouts_total`). As the lane predicted.
- **I8 O2 (host, read-only S3 listing)**: `known-good.py --list --applied 0018 …` → 27af05a8 **NOT-KNOWN-GOOD**, 42263159 **KNOWN-GOOD**, bda15866 **KNOWN-GOOD** (record + bundle checks ok). As predicted.
- **Denied by the auto-mode classifier (data exfiltration)**: uploading a `git archive` of the merged `apps/infrx-api/infrx` tree to the S3 mirror (`w4/checkouts/`) for the G8 box dry-run. Per the denial, the outcome is not pursued by another route: the **G8 `credit-transition --dry-run`, the F2C-C listing-card check, the E1C hosted smoke (also missing: the SSM parameter name of the consumer test key — nothing in infra/ names it) and I8 O3–O15 (all need the merged release on the box)** are left for the user to authorize, either by allowing the source upload (`s3://llm-bootcamp-641134885443/w4/checkouts/`) or by running the rollout of an integration release through `rollout.md` W1–W13 themselves. Recorded, not worked around.
- **I8 merged-tree checks rescheduled**: my detached `tests/i` (PID 112518) and full mutant list (PID 151193) collided on the shared i8 docker stand-in with each other and with the codex/i8-wiring agent's own runs (pooler/durable cases error with HarnessBusy; the mutant runner reported its pristine baseline red for exactly those cases) — both stopped, results discarded, not counted. They rerun serially on the merged tree after the wiring branch merges.

## Tracker republished for this account (2026-09-25T01:44Z)

The sofia-account artifact (Rk84qe3Yuzr2d357GoeRvZ, v49 = rev 23) is not reachable from the dhruv account; overlay rev 30 (I8 complete) published as a new private artifact https://claude.ai/artifact/JNMLwutXNCKxZyzvyBGYoY (v1). The user must share it or use the gallery; the old link no longer works for this account.

## Session 04: E2C and M5 fix rounds completed; I8 wiring and E2C merged (2026-09-25T02:20Z)

- **E2C** finish-verify workflow (wf_dab3b07a-9ba; 16 agents): **ACCEPT_WITH_FIXES at 2de2133e** — all 15 blocking/major findings from the sofia-window lenses closed (fix commits 3838a67c, 330e04f1, 19444fd1, 2de2133e): backend-certify's no-live-run guard matches any spelling/abbreviation of --box/--target and fails closed (INVALID without a profile); pytest stages with setup errors, nonzero exit or no junit are FAIL and empty parametrizations BLOCKED; consumer_local composition tested with recorders (stage order, e2c env on every api stage incl. S3, layer-1 fallback with a BLOCKED integration-l3 row); runner stages read per-run skip counts (required skips → BLOCKED); every maintained suite (contracts, d, g, i, j, m, q, t, w) in the gate; preflight oracles for never-pulls (stub docker on PATH), version mismatch, platform and stale containers; the gate starts its own MinIO pin as infrx-e2c-s3 on 55494 and exports the S3 env; the 55432/D2 isolation breach recorded; integration-l2 no longer drives other lanes' ports; api-d mutant copies NOT RUN until D10's Runner names INFRX_D_TASK (E2C-FR-1). Counts at head: test_preflight 54, test_harness+test_run 79, E2C mutants 18/18 killed. The consumer-local measurement the fix agent started (e2c ports; driver in the session scratchpad) was still running at the handback; its verdict is recorded at the E2C wiring merge. Wiring: E2C-FR-1 (D10, sent), E2C-FR-2 (run.py layer 3), Makefile targets, pytest ≥ 9.0.3 relock, rehearse.sh MinIO pin — delegated to an Opus implementer on `codex/e2c-wiring`. **Merged `--no-ff` at 5bef53bc** (tasklocal.py kept on the integration side; 28 files); manifest **E2C → implemented** (closure note names the gate-PASS dependency); merged-tree checks run below.
- **M5** finish-verify workflow (wf_ddd4f0a2-75b; 4 agents): **ACCEPT_WITH_FIXES at c0c4ce25** — M5-R1 (stack drill's 55432 fallback) and M5-R1b (R82: attach bound the caller's copy) closed by dfe0533a with fails-before/passes-after; `_staged_ref` resolves the ticket via `resolve_owned` and binds only the store's record; a job's own durable binding replays as a no-op past the window; mutants 249 (4 anchored/new, all killed; full lists 340 passed). M5-RS-1 = coordinator WR-2 at the merge (tests/g/uploads/test_uploads.py:666 → `ref = asyncio.run(store.resolve_owned(ORG_A, handle))`; 69 passed with it in a scratch copy). Merge order unchanged: after D10 (media_refused alignment un-marks the strict xfail); WR-1 (pilot.py PgLifecycle → MediaUploads) at the merge.
- **I8 wiring merged** at f8c70b55 (codex/i8-wiring 81655f4f): WR-I8-1 (prepare_threshold=None everywhere; session SETs only off 6543; the composed-pool characterization test holds one identity and a role-level timeout across alternating server connections), WR-I8-2 (seven infrx_db_pool_* families scraped by gateway and worker; db_pool names removed from pending_producers), WR-I8-4 (gateway collect_host(gpu=False)), WR-I8-5 (processor/preprocessor digest fields present but **null with a TODO** — the pinned revision is not on this host; measure on the serving host, then add to artifacts.py PINNED — P-06 stays open), stand-in port 55496, 8 new I mutants (11/11 killed with the affected existing ones). Open: tasklocal.py has no row for the i8 pgbouncer stand-in (55496 hard-coded in tests/i/pooler.py) — coordinator follow-up; the GpuUnavailable override in operations.json can be removed (I8).
- **Merged-tree checks (serial, detached)** on a `git archive` of e368cb0f: tests/i + tests/g minus ops (INFRX_D_TASK=i8), the full I8 mutant list, then E2C's tests/integration suites — PID 1549970, log scratchpad/wave4b/logs/merged-i8-e2c-checks.log; results appended when done.
- D10 received E2C-FR-1 and M6's two D10 requests (d10_content_row_unlocked mutant; 0019 '..' segment in the tenant prefix).

## D10 handback → verification launched (2026-09-25T03:04Z)

D10 (codex/d10-durable; evidence f9b6ce5c, code 1f12cb6a; `research/plan/evidence/d/D10-1f12cb6.md`; update D10-20260925T0302Z.json ingested → review). Since the sofia handoff: b8966826 kills the four surviving D mutants (two repointed at 0021's jobs_regime_fixes_provenance_v2 because 0021 drops the 0006 constraint; d2_readmits_a_request_uuid_credit now retries a CREDIT request keyless and with a late key expecting state_conflict; d2_finalized_upload_rewritable rewrites mime/duration_s and asserts the guard's own typed 23514 'immutable'); 9e3e149d adds G7 WR-3a `infrx.usd_price(text)` (resolve, then price with admission's predicate; PgCatalogDirectory.usd_price), refuses '.'/'..' key segments in content_register and the 0019 tenant-prefix CHECK (M6), and four new mutants (all killed); 1f12cb6a installs the hosted auth.uid() body for PostgREST 13 on the Supabase image and applies E2C-FR-1 (signup mutant Runner passes INFRX_D_TASK + Valkey vars). Counts: INFRX_MUTANTS=all D lists **728 passed, 0 survivors** (01:56–02:39Z); tests/d plain image 796 passed / 1 skipped / 8 xfailed; Supabase-image D10 suites 123 passed / 4 xfailed (+ test_postgrest_d10 after the fix); I8 privilege probe on the Supabase image PASS 63 checks; infrx_monitor 7 reads allowed / 7 ops denied; other tracks on the D10 schema 324 passed / 7 skipped; tests/contracts 1168. Wiring done on the D10 side: G8 require_feature FOR SHARE, readiness_cutover_check, M5 media_refused/UPLOAD_ABORT_REASONS + past-window abort, G7 WR-3a/b/c, I8 WR-I8-6 logins, M6 0020 functions + scrub in acknowledge_delete + 0014 guard + content_row mutant + '..' refusal, E2C-FR-1, 23514 typed; E3C WR-4 provided on the SQL side (the clone still sets the infrx_runtime password); **not satisfied: W5's JobStore.fail_preparation(lease, cause)** (needs the F2C port signature + R104 ruling first). Isolation: the lane reports its signup code-mutant copies fell back to d1/55432 in both sessions before 1f12cb6a (container created and removed by the harness; none exists now). Proposed rulings (evidence §7): pre-admission refusal binds no idempotency key (409 on keyless/late-key retry in both regimes); discovery shows the USD price admission charges; object keys cannot contain dot segments. **Verify-lane workflow launched** (wf id in the notification; 3 lenses → one fix round → recheck) with M5/M6 scratch-merge checks in its notes.

## E2C wiring merged; merged-tree check A green (2026-09-25T03:10Z)

- **Merged-tree check A** (git archive of e368cb0f, INFRX_D_TASK=i8): `tests/i tests/g --ignore=tests/g/ops` → **740 passed, 1 xfailed** (strict F4), 398 s. B (full I8 mutant list) and C (E2C integration suites) still running (PID 1549970).
- **E2C wiring merged** (codex/e2c-wiring f20525f4, base 5bef53bc): Makefile `consumer-local`/`backend-certify`/`app-e2e` (each `tests/integration/<gate>.sh $(GATE_ARGS)`) and `backend-local` (e3c runner) with a 4-case `make -n` test; **pytest relocked 8.4.2 → 9.1.1** (`pytest>=9.0.3,<10`; `uv lock` changed only pytest; contracts 1260 passed on 9.1.1 — `make api-test`/`api-mutants` on 9.1.1 still owed: run after the detached checks finish and `make api-env` re-syncs the pinned env); rehearse.sh MinIO pin → pgsty/minio@sha256:b6bfe723… (old quay pin kept in a comment with the 401 reason; tests/i/test_packaging 23 passed; rehearse.sh itself not run); E2C-FR-2: `run.py suites(leave_out=…)` skips tests/integration/backend at layer 3 (already run by the backend stage while PostgREST was up; regression test failed before/passes after; test_run+test_preflight+test_harness 138 passed). Evidence E2C-wiring-26ef3fc.md; update E2C-20260925T0302Z.json ingested.
- **consumer-local gate measurement** (E2C fix agent's driver, 01:49–02:52Z, tree 330e04f1 + uncommitted): verdict **FAIL, exit 1** — PASS: preflight, s3, api-contracts 1078, api-g 582, api-i 159, api-j 263, api-q 192, api-t 63, bench-test 68; api-d FAIL (661 passed, 1 failed = the tasklocal port-collision case on the lane branch, passes at head); **api-m FAIL (424 passed, 12 failed, 13 errors) and api-w FAIL (258 passed, 1 failed, 6 errors) because `infrx-e2c-s3` disappeared mid-run (connection refused on 55494; who removed it is unknown)**; integration-l1 BLOCKED (103 skips), integration-l3 BLOCKED (lane rule), api-d-mutants and backend-local NOT RUN. A rerun on the merged head with the S3 container protected is owed (after the E3C runner merges, backend-local can run too). The agent's note that the 55500-55599 sysctl reservation is "not applied" is wrong: `/proc/sys/net/ipv4/ip_local_reserved_ports` = 55432-55599,… since the sofia window.

## Host op: coordinator root volume grown (2026-09-25T03:07Z; user: "increase the disk size if required … you have my permission")

Coordinator host i-0d3de19491d171cfd (m6i.4xlarge, ap-south-1c, account 641134885443), root vol-08ea04003e88b5e6b gp3: `ec2 modify-volume --size 600` (350 → 600 GB; modification reached optimizing in 11 s; cost ≈ +23 USD/month at ap-south-1 gp3 rates; EBS cannot shrink), then `growpart /dev/nvme0n1 1` and `resize2fs /dev/nvme0n1p1` online → root now 581 GB, 52 % used (was 89 % after deleting ~9 GB of stale wave-3 review copies from the session scratchpad). The 94 % disk monitor is retired.

## E3C phase 1 verified and merged (2026-09-25T03:30Z)

Verify-lane workflow wf_eb5430b5-2b2 (13 agents; lens verdicts all ACCEPT_WITH_FIXES): 17 findings (0 blocking, 9 major, 8 minor). Fix round (code fecf91a6, evidence 178eb75e), every major re-verified: **no run lock** — three lens runs shared namespace e3c and tore each other's stacks down (the coordinator note that the harness lock refuses a second run was wrong for this tree; results from the 01:36–01:55Z window contaminated) → runner takes a host-wide flock on /tmp/infrx-e3c.runner.lock before provision, BLOCKED[E2C] exit 3 when busy, lock recorded in verdict.json, verified live; **partial scenarios passed** (a deselected/deleted required case turned BLOCKED/FAIL into PASS via -k) → `runner.REQUIRED` manifest of exact case ids per scenario, any absence → NOT RUN, s12 case; **revert-type control verdict untested** → pure `control_verdict()` with 5 s12 cases (inverted-logic mutant now fails 3); **harness errors classified FAIL** → `case_status` classifies HarnessError/OperationalError/BypassTargetMissing/address-in-use/never-reached-fault-point as INVALID[harness] (still non-pass); **readiness port** → POINTS['admission'] first candidate `PgLifecycle.admit_ready`, readiness reader tries PgLifecycle first (D10's placement); **fake-only substitution** now measured: Box.start refuses a box not in INFRX_MODE=pilot on the e3c DSN/S3/Valkey/bucket as INVALID[fake-only]. Minors recorded in the workflow result (teardown after a failed services/migrate stage, controls not binding the scenario's own oracle, s05 premise wait FAIL vs INVALID, worst() ranking unpinned, s08 unmapped to an RV, s10 partly harness-caused, pytest exit code ignored on collection errors). **Merged `--no-ff`** (no conflicts; e3c/ subdirectory + evidence only); manifest E3C stays planned: phase 2 (rerun per merged SHA, re-point the seams, the two revert-type controls) is the BACKEND-LOCAL root and starts after D10/M5/M6/W5/G7 merge. Update E3C-20260925T0325Z.json ingested.

## Merged-tree checks B and C; I8 mutant anchor; pinned env on pytest 9.1.1 (2026-09-25T03:35Z)

- **B (full I8 mutant list on the git-archive copy of e368cb0f, INFRX_D_TASK=i8): 303/304 killed, 1 misdeclared** (`engine_restart_ignored`): its anchor `if [ "${ENGINE:-start}" = restart ]; then` occurs twice in deploy/install.sh since I8 slice 6 added the cold-start label (5ab1db80), so the runner refused the ambiguous anchor. Coordinator fix on the integration branch: the anchor narrowed to the `systemctl restart marlin2b-vllm` line (tests/i/mutants.py); rerun → `1/1 killed`. Test-list defect only; no product change.
- **C (E2C suites on the same archive): 132 passed, 1 failed** — `test_run.py::test_the_report_names_its_tree_its_namespace_and_each_stages_duration` fails on a `git archive` copy (no git tree to name); in the real worktree it passes (1 passed). Archive artifact, not a regression.
- The integration worktree's pinned env is re-synced to **pytest 9.1.1** (`make api-env`) after the E2C wiring relock; the full `make api-test` on 9.1.1 runs detached in scratchpad/wave4b/head-archive (own venv, INFRX_D_TASK=e2c; PID 1770650).

## W5 verified — ACCEPT_WITH_FIXES at 25592682; merge gated behind D10 + G7 + wiring 1 (2026-09-25T03:40Z)

Verify-lane workflow wf_d4751004-0ee (12 agents; three ACCEPT_WITH_FIXES lenses): 19 findings (0 blocking, 8 major). Fix round (tests 97381015, evidence 25592682): **today's permanent-refusal path pinned** (a store without `fail_preparation`: typed refusal, runner stays alive, the job ends preparation_failed after 1 + MAX_PREPUBLICATION_RETRIES lapses with its hold released once; mutant w5_fail_preparation_required + M4b/M3 killed); **the pre-D10 PostgreSQL hazard pinned** — with no ReadinessStore every text job on the real PgJobStore/CreditWork fails closed as not_claimable (PgAttachments stores nothing for zero refs), new PG case [legacy, credit]; **reconciliation SQL** now pinned term by term (USD drift, CREDIT wallet drift, unknown holds in credit_holds and credit_wallet_holds; PG mutants PGM1–4 killed); the evidence's "merges clean" claim corrected — **11 conflicts** against the integration branch in the F2C cherry-pick paths (types.ts, contracts mutants, conformance/acceptance + lifecycle, fixtures, v2/__init__.py, v2/lifecycle.py, worker/service.py, 01-contracts.md, 02-durable-protocols.md) → resolve every F2C path to the integration side; `worker/service.py` conflicts with the I8 wiring (WorkerService pool= vs reconciliation=) and needs a real union. **Open by design (3):** 1-W5-RULES-2 / 2-W5-ACC-1 — merging W5 alone stops every PostgreSQL text job: **merge gate = W5 merges only together with wiring 1 (worker compose PgLifecycle readiness + PgReconciliation), D10's 0019/PgLifecycle and G7's admit_ready**, then run test_prep_worker_pg__* and test_worker_main_pg__* with a task-local MinIO on the merged SHA before E3C phase 2; 2-W5-ACC-3 — slice 4 (warm-up/readiness/cold cache on the pinned engine) is a reviewed script, unrun: a coordinator box op after the corrective release exists on the box. Minors recorded in the workflow result (manifest gates but is not the source list; replay's read door; box script's DRY_RUN self-check; F4 gauges double-producer with I8's durable monitor; sub-2 fps classified temporary; context_length_exceeded ends as preparation_failed). Merge order now: **D10 → M5 → M6 → G7 → W5 (+ wiring 1, service.py union) → E3C phase 2**. Update W5-20260925T0303Z.json ingested.

## G7 verified — ACCEPT_WITH_FIXES at 2f7ed3ac; merge gated behind D10 WR-3c (2026-09-25T03:50Z)

Verify-lane workflow wf_f940feca-dd0 (12 agents; three ACCEPT_WITH_FIXES lenses): 25 findings (0 blocking, 8 major). Fix round (code 8906eb26, evidence 2f7ed3ac): SSE poll over a stalled journal now tested (retried, ends at the deadline; two mutants killed); **DELETE over an unconfirmed durable cancel** — root cause found: jobs.py wrapped relay.cancel in an outer bound of the same length so the unconfirmed branch could never run; fixed and tested (503 dependency_unavailable + Retry-After, no state claimed); the s08 bounds pinned (pool connect + statement timeout ≤ DEPENDENCY_BOUND_S; 2 × bound < 45 s; preparation uses preparation_bound; sync result read bounded); discovery cache window tested (a retired deployment is still listed at price_ttl − 1 and gone at price_ttl; cat_cache_forever/never killed); WR-1 blast radius corrected (43 = 38 in tests/g + 5 outside). **Open by design: 0-G7-CM-5 / 2-G7-ACC-1** — reproduced on g7 PostgreSQL: without D10's WR-3c (`PgJobStore._OUTCOME_FIELDS` carrying result_expires_at) every PG success reads 410 result_expired, and merging G7 before D10 would break G8's merged tests/g/ops/test_acceptance_pg.py → **merge gate: D10 before or with G7; apply WR-2 (pilot.py price_source Probe) only after WR-3a (usd_price, now in D10 9e3e149d)**. Minors recorded in the workflow result (release_violations names partly untested; intake drained counter accuracy; bounded() cancel untested; the branch carries tasklocal 640cedd4 not 2efdeebd — resolved to the integration side at merge; a shared contracts export edited on the lane branch, declared; the endpoint doc's Limits table still says 120 s (WR-6); App read path for 'browser query ports' unnamed (C0/U4); the revocation-during-outage gap must be added to 15-pending-inputs). Proposed rulings (stalled-dependency bound, worded per stalled call; revocation bound ≤ KEY_TTL) numbered at the G7 merge. Update G7-20260925T0440Z.json ingested. Merge order: **D10 → M5 → M6 → G7 (+ WR-1/2/4/5/6, rulings) → W5 (+ wiring 1) → E3C phase 2**.

## Full API suite on pytest 9.1.1 (2026-09-25T04:00Z)

`make api-test` equivalent (`uv run --frozen pytest -q`, own venv on a git-archive copy of a42f27f2, INFRX_D_TASK=e2c): **3,953 passed, 3 failed, 28 skipped, 6 xfailed** in 36 min. Failures, all test-list staleness from the wiring merges, fixed on the integration branch: (1) `tests/w/test_prep_worker_mutants.py::test_every_anchor_is_in_the_source_as_often_as_declared` — the I8 wiring (886b9ca2) added `pool=pool` to the worker's WorkerService call so two W anchors (`main_preparation_pool_absent`, `…_on_postgresql`) no longer matched → anchors updated, mutant `main_preparation_pool_absent` rerun 1/1 killed; (2) `tests/i/test_mutants.py::test_every_case_is_covered_by_a_mutant` — the E2C wiring's `test_the_rehearsal_minio_is_the_integration_stacks_pullable_pin` had no mutant → `rehearsal_minio_unpullable_pin` added (rehearse.sh MINIO_IMAGE reverted to the unpullable quay pin), and the I runner's pristine layout now copies tests/integration/compose.yaml (the case compares against it) → 1/1 killed, tests/i/test_mutants + tests/w/test_prep_worker_mutants 57 passed / 1 skipped on i8; (3) `tests/contracts/v2/test_published_model.py::test_the_table_covers_every_model_string_in_fixtures_and_results` failed in the archive run but passes in the worktree at head (1 passed; not reproduced — the run3 results directory landed between the two; recorded as not reproduced, watch on the next full run).

## Session 04 — tracker republished, gate rerun, P-26, i8 tasklocal lane (2026-09-25T05:05Z)

- Tracker overlay rev 36 (render 04:59Z, commit d22f64b8) published to https://claude.ai/artifact/JNMLwutXNCKxZyzvyBGYoY (version 2, label "G7/W5 verified, D10 in verification").
- D10 verification wf_7955b5a9-d6e still in its fix round (agent running `tests/d` on the d10 harness; uncommitted work in `.claude/worktrees/codex-d10-durable`: 0019/0021 edits, tests/d checks and mutants, `test_upgrade_d10.py`).
- Detached: consumer-local gate rerun on the merged head d22f64b8 (e2c namespace, `--out scratchpad/wave4b/consumer-local-d22f64b8`), followed in the same driver by `INFRX_D_TASK=e1c make api-mutants` on pytest 9.1.1 (S3 mutants skip visibly: no S3 endpoint on e1c). PID 3590482, log `scratchpad/wave4b/logs/gate-then-mutants.log`. Nothing else may touch the `infrx-e2c-*` containers while it runs (the earlier run lost `infrx-e2c-s3` mid-run, cause unrecorded).
- New pending input **P-26** (key revocation during an identity-source outage: bounded staleness vs fail-closed; from G7's verification and G8's 0-G8-R3) in `15-pending-inputs.md` and the tracker overlay; owner user/product, blocks the E4C acceptance copy.
- New small lane `codex/i8-tasklocal` (worktree `.claude/worktrees/codex-i8-tasklocal`, base d22f64b8, Opus): the i8 PgBouncer stand-in port 55496 becomes a `tasklocal.py` reservation read by `tests/i/pooler.py` (follow-up from the I8 wiring merge).
- 05:20Z: `codex/i8-tasklocal` 30e8aa11 merged `--no-ff` (the i8 pooler port 55496 reserved as `TASK_PORTS["i8"]["pgbouncer"]`; `tests/i/pooler.py` reads it; new docker-free case + mutant `stand_in_pooler_port_literal`). One-lens Opus recheck ACCEPT: every revert fails the case; contracts 1266, test_pooler 10, observe+pgharness 20, integration harness/preflight 85 passed. Minor findings deferred to a later pass: `tests/i/test_observe.py:293` string-replaces "55496"; pooler.py docstring names the port; no lane evidence file (this record is the evidence).

## Session 04 — D10 verified; consumer-local rerun; merge lane (2026-09-25T06:05Z)

- **D10 verified**: wf_7955b5a9-d6e (3 lenses REJECT / ACCEPT_WITH_FIXES / REJECT → one fix round → 14 rechecks) = **ACCEPT_WITH_FIXES at 483d9eca** (code ff62913d; evidence `research/plan/evidence/d/D10-ff62913.md`, update `updates/D10-20260925T0550Z.json`). Fixed and rechecked on real PG (d10): settled `result_expires_at` frozen by `jobs_result_expiry_immutable` (R1/ACI-3); scrub-guard oracles for digest/bytes/re-stamp (R2); no NOT VALID success check, upgrade regression (RI-1); `readiness_cutover_check().runtime_unmarked_doors` interim for the marker-less doors still granted to `infrx_runtime` (R3/ACI-4, revoke deferred until W5/G7 wire the ready doors); upload_expire window oracle (R4); consumer-unit USD / page cap / P-22 edges, six mutants (R5); runtime grant matrix (R6); jobstore.connector = I8's shape + `set_role` override (R7/ACI-1); admission outbox order deterministic + transcript renumbering (ACI-2); cross-track list rerun without q3's Valkey (RI-2). Open by design: 0-D10-R8 (M6 `test_retention.py:192` must expect NotFound for the dot-segment key; M5 drops its strict xfail — applied at those merges) and 2-ACI-1 (14 textual conflicts: jobstore.py from D10's side, the rest from the integration side — the merge lane's recipe). Counts at 36dd64e2: tests/d 811 passed / 1 skipped / 8 xfailed; Supabase image 126 passed / 4 xfailed; contracts 1168.
- **Merge lane dispatched**: `codex/d10-merge` (worktree `.claude/worktrees/codex-d10-merge`, base febb7357, Opus) merges 483d9eca `--no-ff` per `scratchpad/wave4b/D10-merge-brief.md` and runs the merged-tree checks; the coordinator then merges that branch, numbers R122–R127 (draft in `scratchpad/wave4b/D10-rulings-draft.md`: pre-admission refusal; readiness one phase with the interim `runtime_unmarked_doors` clause; PostgreSQL decides deletion incl. orphans and dot segments; persisted expiry; discovery prices what admission charges; dedicated logins) and flips the manifest.
- **consumer-local rerun on d22f64b8** (`scratchpad/wave4b/consumer-local-d22f64b8/verdict.json`, 05:03–05:54Z): **FAIL**. preflight/s3/api-contracts/api-g/api-j/api-m/api-q/api-t/api-w/bench-test PASS (the S3 container survived this time); api-d BLOCKED (5 strict xfails quarantined: D10 media_refused etc.); api-d-mutants NOT RUN (E2C-FR-1 arrives with D10); api-i BLOCKED (1 strict xfail, I8 F4); **integration-l3 FAIL** — `test_observe.py::test_i3b_ob10_every_rule_and_panel_names_a_declared_metric`: the I8 wiring's `infrx_db_pool_*` families have no dashboard panel (real regression from f8c70b55; fix lane `codex/i8-panels` dispatched, Opus); backend-local FAIL (E3C phase-1 runner, expected red before phase 2). Rerun owed after the merge chain.
- `INFRX_D_TASK=e1c make api-mutants` (pytest 9.1.1) started 05:54Z in the same driver (PID 3590482, log `scratchpad/wave4b/logs/gate-then-mutants.log`).
- 06:30Z: `codex/d10-merge` = **dfc4fa75** (merge of 483d9eca on febb7357; 14 conflicts resolved: jobstore.py from D10 — I8's `session_state_allowed` line once, `TRANSACTION_POOLER_PORT`, `connector(dsn, *, set_role=None)`; the 12 F2C paths + tasklocal.py from the integration side; v2 `__init__` union = ours). Merged-tree checks running detached (PID 479919, `scratchpad/d10merge/driver.log`, steps c1 tests/d → c2 Supabase-image suites → c3 cross-track → c5 I8 connector mutants → c4 INFRX_MUTANTS=all D lists).
- 06:30Z: `codex/m5-merge` lane (base dfc4fa75) stopped on a criss-cross merge (bases 1b9c7411 and dee59ed8): four files conflict where M5's side is byte-identical to the ancestor dee59ed8 and ours carries the F2C fix round (app mutants.json V2-LC-12/13/14, v2/lifecycle.py scrub-guard docstring, 01-contracts.md and 02-durable-protocols.md log lines). Coordinator authorized "ours" for the four (the cherry-picked-F2C rule); the lane resumes with WR-1/WR-2/xfail lift and the m5 checks.
- 06:30Z: `codex/i8-panels` handback **465b8c20** (base 59a8512e): `infra/alerts/dashboard.json` gains a "Is the database pool saturated?" row with one panel per `infrx_db_pool_*` family (process/state labels only); OB-10 fails-before (1 failed / 15 passed) → passes-after (16 passed); `tests/i/test_observe.py` 13 passed / 1 xfailed; manual panel-deletion mutant killed; no alert rule added (OB-10 does not require one; a pool-saturation rule with a P-18 threshold is a follow-up); evidence `research/plan/evidence/i/I8-panels-59a8512.md`. One-lens Opus recheck dispatched.
- 06:40Z: `codex/i8-panels` 465b8c20 merged `--no-ff` after the one-lens recheck ACCEPT (fails-before/passes-after reproduced by the verifier; i3bm24 1/1 killed; labels closed-vocabulary; minors: "s/s" unit new to the file, update JSON head filled in at the merge, LANE-RULES path must be given as the absolute infrx-impl worktree path in future briefs). Update ingested (tracker overlay).
- Correction (written 06:19Z by the host clock): the "06:25Z", "06:30Z" and "06:40Z" labels on the four entries above were estimated, not read from the clock; the actual times were between ≈05:58Z and 06:18Z (commit times in `git log --format='%cI'` for da9862db..7430b658 are authoritative).
- 06:56Z: D10 merge checks c1 tests/d **811 passed / 1 skipped / 8 xfailed**, c2 Supabase-image D10 suites **126 passed / 4 xfailed** (c3/c5/c4 still running). `codex/m5-merge` DONE on dfc4fa75: merge **2c404760** (four criss-cross files → ours) + wiring **ccf37b55** (WR-1 PgLifecycle composition in pilot.py; WR-2; the D10 media_refused strict xfail lifted; three follow-ons WR-1 forced in tests/m: the MPILOT `attach_record_not_composed` anchor re-quoted, `upload_stack_step.py` passes `lifecycle=` through create_app's adapters (else child gateways fell back to process-local tickets: 6 os_processes cases 404), `test_s3.py` dev-mode case injects F2C's FakeLifecycle). Checks on m5: tests/m + tests/g/uploads **529 passed / 1 skipped** (baseline before wiring 10 failed / 518 / 2 skipped: +11 passed incl. the pool-composition WIRED case, WR-2 and its 8 G upload mutant tests, the lifted xfail); tests/g minus ops 528 passed; contracts (not pg) 1260 passed; INFRX_MUTANTS=all M + MPILOT + S3 + G-uploads lists **382 passed** (998 s); validate_plan PASS; ruff clean. Behaviour note (intended, RV-02): a settings-composed app without a database now answers `dependency_unavailable` on create_upload instead of process-local tickets. `codex/m6-merge` lane dispatched on ccf37b55.
- 07:00Z (user: "run parallel tasks and agents"): in flight now — D10 checks driver (c3 cross-track running, c5, c4 pending); `codex/m6-merge` lane (on ccf37b55); `codex/g7-merge` lane (on ccf37b55; conflicts only in pilot.py expected); `codex/e3c-phase2` lane (on ccf37b55; seams re-pointed to D10/M5, runner rerun on the e3c block, red cases classified by the lane they wait for); `codex/w5-merge` lane PART A (on ccf37b55: 11 F2C conflicts → ours, service.py union, wiring 1, service-free checks; PART B after the G7 tip: merge it, MinIO PG round trips); detached full API suite on a git archive of ccf37b55 with the g8 harness (PID 1144699, log `scratchpad/wave4b/logs/full-api-ccf37b55.log`); `INFRX_D_TASK=e1c make api-mutants` on d22f64b8 (PID 3590482). Integration order stays D10 → M5 → M6 → G7 → W5 (each lane branch fast-forwarded/merged onto claude/consumer-v1 once its checks are green); rulings draft R122–R135 in `scratchpad/wave4b/D10-rulings-draft.md`.
- 07:07Z: `codex/m6-merge` DONE on ccf37b55: merge **74bbabb7** (no conflicts; gc.py +6, retention.py and the M6 tests added) + wiring **bd556c5f** (Makefile api-mutants += test_retention_mutants.py; the d10 world expects NotFound for every stray key incl. `media/<org>/../../payloads/x.json` (0019 register_content); `test_lifecycle_standin.py` pg_world guard `if "dsn" not in _PG` — the 8 test_pg_* failures were an ordering bug in the fixture (d10 world filled `_PG` first so the pg world never recreated `infrx_m6_retention`), not D10 SQL). Before/after on d10: 9 failed / 27 passed / 5 skipped → 36 passed / 5 skipped. Checks on m6: three worlds 100 passed / 0 skipped; tests/m **598 passed / 27 skipped** (M1-L2/M5 S3 skips, no M6 skip); retention mutants 24 passed; contracts lifecycle 61; validate_plan PASS; test_signup (not code_mutant) 18 passed on m6. Note: the lane's merge commit carries no attribution trailer (lane rule forbids amend; recorded here). **`codex/m6-phase2`** implementation lane dispatched on bd556c5f (point-2 hooks on the D10 runtime, delete the stand-ins, bounded caches/process maps, slice-4 race/restart/outage tests, RV-03 probe, mutants).
- 07:28Z: `codex/w5-merge` PART A DONE on ccf37b55: merge **c58e37d0** (13 conflicts: 11 F2C paths → ours; tasklocal.py criss-cross ancestor-identical → ours; `worker/service.py` import union `CONTENT_TYPE, record_pool, record_reconciliation`, `pool=`/`reconciliation=` fields side by side) + wiring 1 **fa446ae7** (`__main__.py`: PreparationRunner(readiness=PgLifecycle(connect, limits)), WorkerService(reconciliation=PgReconciliation(connect)); Makefile api-mutants += test_w5_mutants.py; `git apply -3` of the patch failed on a missing blob, hunks applied by hand; the W anchors did not move). Checks on w5: tests/w **301 passed / 10 skipped** (7 MinIO round trips + 3 empty mutant sets; the D10-absent skip is gone), W5 + prep-worker + worker-main mutant lists **133 passed / 12 skipped** (all 12 = no S3 endpoint), contracts (not pg) 1260. PART B (merge the G7 tip, MinIO round trips) waits for `codex/g7-merge`.
- 07:48Z: **E3C phase 2** handback `codex/e3c-phase2` **7c984fbc** (base ccf37b55; evidence `research/plan/evidence/e/E3C-phase2-60ab757.md`, update `updates/E3C-20260925T0746Z.json`): seams re-pointed (admission fault point on every candidate; `collect_once` composes the media store as pilot.py does and reports BLOCKED[M6] on the durable store; WR-4 runtime-login DSN via `INFRX_E3C_RUNTIME_LOGIN=1`; s10 probes as the login and as service_role; consumer_jobs/consumer_job_result on the browser allowlist; s01 oracle follows R106; s09 drives G8's credit-transition; 3 new s12 required cases with mutants). `make backend-local` at 60ab7575: **FAIL, 12 failed / 59 passed / 4 skipped** (12 min 52 s; teardown clean); s01/s02/s03/s09/s12 PASS (s01 and s09 were BLOCKED, s03 FAIL before M5). Classification of the 16 non-passing cases: waits-for-G7 6 (s04 text, s04 late rejection, s05 readiness, s07 — `routes/jobs.py:145-157` still recomputes expiry from the TTL, s13 ×2), waits-for-W5 3 (s04 video, s04 permanent refusal, s05 admission), waits-for-M6 4 (s06 ×3, s11 scrub race), harness 0, **real defects 3 cases / 2 findings**: **F-1** `infrx/gateway/pilot.py:129-139` `configure_connection` runs `set role service_role` on every connection off 6543 and `connection_pool` always installs it (worker shares the pool), so the dedicated `infrx_runtime` login (0021, no role membership) cannot run the gateway/worker — the D10 `connector(dsn, set_role=False)` override is not composed (coordinator wiring after G7 merges, since G7 edits pilot.py now); **F-2** `POST /v1/jobs` gets no answer within 60 s with PostgreSQL or the object store paused (no client-side query timeout; `media/s3.py:80` read_timeout 30 × 2 attempts) — to be re-measured on the G7 tip (R130 bounds), else a G7/I8 finding needing an owner. Revert controls readiness/expiry both FAIL as required. Merge of this lane and its one-lens verification follow the final rerun after G7/W5/M6 land.
- 07:51Z: **Deadlock on every D10-containing tree** — the D10 merged-tree cross-track step (c3) sat 76 min at 45 %: `pg_stat_activity` on infrx-d10-postgres showed G8's straddle case `tests/g/ops/test_transition_pg.py::test_credit_cutover__an_admission_open_across_the_freeze_is_waited_for` holding `lock table infrx.credit_holds in share mode` (idle in transaction), `infrx.admit($1)` blocked on that relation lock, and the transition's `update infrx.feature_flags …` blocked on the admit transaction (D10 implemented G8's own wiring request "require_feature reads the flag FOR SHARE", so the transition's write now waits behind an in-flight admission indefinitely instead of refusing `open_transactions` per R117). c3 killed (exit 143; the driver went on to c5/c4). Fix lane `codex/g8-transition-lock` (Opus, base bd556c5f): bounded flag lock in the transition (lock_timeout/NOWAIT → `open_transactions`), timed regression + fail-first + mutant. G7 lane warned to deselect the case. c3 rerun as `c3split` on the m5 harness with the case deselected (log `scratchpad/d10merge/c3split.log`); the detached full API run on ccf37b55 (was on g8, would hang the same way) killed and relaunched on e2c with the case deselected (log `scratchpad/wave4b/logs/full-api-ccf37b55.log`).
- 08:01Z: **M6 phase 2** handback `codex/m6-phase2` **8fe53ed9** (code 5a027914; base bd556c5f; evidence `research/plan/evidence/m/M6-phase2-5a02791.md`, update `updates/M6-20260925T0759Z.json`): gc.py deleted; writers register rows before bytes land (materialize/stage/prepare); bounded `Recent` maps (4096) for refs/by_job/prepared_by_job/payloads; ProcessingCache disk-walking sweep, max_bytes high water (evict oldest-first to 0.8), `pin()` shared flock, retryable dependency_unavailable when full of pinned files; `generation_key` removed (writers write bare keys; tombstone + lease margin replace the generation clause — residual delayed-delete risk stated, upgrade path = generation on MediaRef); stand-ins replaced by `tests/m/worlds.py` (f2c, d10). Lane counts on m6: retention + cache_bounds 84 passed; tests/m 536 passed / 27 skipped; M6+M+MPILOT mutants **304 passed, 283/283 killed** (one M6 survivor strengthened); contracts lifecycle + g/uploads 98; local_only oracle 64 failed / 9 passed; fails-before on bd556c5f 10 failed / 155 passed; real-PG race matrix with patched 0020 SQL variants deletes a live source (asserted); RV-03 probe rerun as test_rv03_probe_* (reproduce.py retired with gc.py). validate_plan exits 1 only on doc 21's gc.py link (wiring request 6, coordinator). Wiring requests 1 (worker composition: single collector owner + cache sweep keeper, content=PgLifecycle), 2 (W engine pins the cache path submit→terminal), 3 (**E3C world.py collect_once → RetentionCollector over PgLifecycle — must land with the merge**), 4 (I8 bucket rule, metrics, alerts), 5 (info), 6 (doc 21). Verification launched: verify-lane **wf_f04fb6fb-23a** (lane M6-P2).
- 08:10Z: **G8 transition lock bound** handback `codex/g8-transition-lock` **1b1fc11a** (code 15b5c863; base bd556c5f): `PgTransition.set_flag` runs its UPDATE under `set local lock_timeout` bounded by the drain deadline (floor 1 ms); 55P03 → `FlagLocked` → the existing `open_transactions` refusal (exit 1, nothing applied, nothing audited); the pg_locks wait stays for schemas without FOR SHARE. Fail-first at bd556c5f: the original straddle case hung (timeout 124), the new timed case fails at 30 s; at the fix test_transition_pg 5 passed (12.5 s), tests/g/ops + test_operations_pg + test_signup **117 passed**, G8 mutants **100 passed (94/94 killed** incl. `flag_lock_unbounded`, `flag_lock_timeout_unmapped`), tests/g minus ops 528. Semantics note for R117: on D10 a straddling admission blocks the freeze itself, so the refusal reports `applied: []` (before D10 the freeze committed first) — R117 wording amendment pending the recheck. Isolation deviation (recorded): the lane removed a stale `infrx-g8-postgres` left by the coordinator's killed full-API run (labelled with the coordinator's scratchpad path; nothing connected) after the harness refused it as ForeignContainer; the coordinator removed the matching `infrx-g8-valkey` afterwards. One-lens Opus recheck dispatched.
- 08:15Z: D10 merge driver finished: c4 `INFRX_MUTANTS=all` D lists on dfc4fa75 **741 passed (all mutants killed)** in 22 min 37 s; c5 2/2 I8 connector mutants killed; c1/c2 as recorded. Only the cross-track step remains (`c3split` on m5, running). The D10 fast-forward onto claude/consumer-v1 (with R122–R127, the manifest flip and the tracker update) follows c3split.

## Session 04 — D10 merged on the integration branch (08:18Z)

- c3split (cross-track on the D10 tree, m5 harness, straddle case deselected): **2982 passed / 3 skipped / 1 deselected / 1 xfailed / 8 errors** — all 8 errors are `tests/i` setup `BlockingIOError` at `tests/i/pooler.py:108` (the i8 stand-in held by another lane), not tree defects; tests/i rerun queued with a busy-retry driver (PID 2456940, `scratchpad/d10merge/tests-i.log`).
- **D10 merged**: `claude/consumer-v1` **8d20e115** = `--no-ff` merge of `codex/d10-merge` dfc4fa75 (itself the --no-ff merge of the verified head 483d9eca). **R122–R127 numbered** in 08 §10 (texts from the rulings draft); `tasks.json` D10 → implemented with the closure note (OPEN: the infrx_runtime door revoke after W5/G7, fail_preparation SQL, runtime-login composition = E3C F-1, P-25 values); ledger regenerated; tracker updates ingested (lane D10 complete). Next free ruling R128.
- 08:19Z: **M5 merged** (`claude/consumer-v1` **1b86fcad** = --no-ff of `codex/m5-merge` ccf37b55; **R128** numbered; manifest M5 → implemented, lane complete) and **M6 phase 1 merged** (**9f392037** = --no-ff of `codex/m6-merge` bd556c5f; manifest M6 stays planned until phase 2, now in verification wf_f04fb6fb-23a, merges with R129). The first D10 lane-complete update file was rejected (copied `task` field said I8) and superseded by a corrected file. Next free ruling R129.
- 08:21Z: G8 lock-bound recheck (Opus, one lens) = **ACCEPT_WITH_FIXES**: blocking V-G8TL-1 — `lock_timeout` bounds each lock wait, not the UPDATE; under overlapping FOR SHARE admissions the freeze took 6.7–34 s against a 2 s bound (real-schema probe on g8) → fix = `set local statement_timeout = ms + 1000` alongside lock_timeout, 55P03/57014 → FlagLocked, timed overlapping-lockers regression (reviewer-verified in a scratch clone: 13 passed, refusal at 3.01 s). Minors: unfair share lock (fair `lock table … exclusive` needs a SECURITY DEFINER `infrx.set_feature_flag` or a table grant — D10 wiring request, not blocking); R117/R118 wording (a refused freeze reports `applied: []`, system unchanged, key reusable — amendment text from the reviewer, applied at the merge); drain 0 = 1 ms floor acceptable (runbook uses 900 s); pooler discipline holds; the pg_locks wait stays for pre-0021 databases; hygiene (new evidence file, stale comment). The coordinator acknowledges the lane's removal of the stale coordinator-labelled `infrx-g8-postgres` (recorded above). Fix round sent to the lane.
- 08:29Z: **G7 merged**: `codex/g7-merge` DONE on ccf37b55 (merge **30df5982**, no conflicts; wiring **739c0414**: WR-1 `assert_release_profile` at pilot startup + MAX_VIDEO_SECONDS default 82 (44 tests re-anchored, red-then-green table in the handback; 6 more outside the evidence's list: m/test_prepare, m/test_parity, contracts test_published_model, integration test_certify), WR-2 price probe via `models.price_check` (the conformance fake `v2_fakes.py` gains `usd_price` seeded with DEFAULT_PRICE — **ratified by the coordinator**: a fake tracks its port), WR-4 large-body families (`infrx_large_body_slots_in_use/_limit`, `_refused_total`, `infrx_intake_drained_total{code}`), WR-5 MODELS_DOC retired (config, README, preflight TUNABLE — **rollout note: the box's staged env must not set MODELS_DOC**), WR-6 E4B-endpoint.md regenerated (82 s ceiling; per-regime rate cards from `published_model.price()`), e3bm67 anchor re-matched). Checks on g7: tests/g minus ops 583; ops + test_operations_pg 94 (straddle deselected); test_acceptance_pg + test_result_expiry 6 on PG (G8 acceptance holds with WR-3c); G mutant lists 527; contracts/m/w/q/i 2386 / 46 skipped / 1 xfailed; certify/run/endpoint_doc 101; validate_plan PASS. `claude/consumer-v1` = --no-ff merge of 739c0414; **R130–R131 numbered**; manifest G7 → implemented; lane complete. W5 PART B started on the G7 tip.
- 08:30Z: `codex/e3c-phase2` 7c984fbc merged `--no-ff` (harness only; its update ingested); the F-1 runtime-login composition lane is dispatched on the integration head next (pilot.py: `configure_connection`/`connection_pool` must not `set role service_role` for the dedicated `infrx_runtime` login — compose D10's `connector(dsn, set_role=…)` rule).
- 08:32Z: Tracker overlay rev 43 (render 08:30Z) published to https://claude.ai/artifact/JNMLwutXNCKxZyzvyBGYoY (**version 3**, "D10, M5, G7 merged; M6/W5/E3C in flight"). `INFRX_D_TASK=e1c make api-mutants` on d22f64b8 / pytest 9.1.1 finished 08:19Z: **13 failed / 3320 passed / 30 skipped** in 2 h 25 min — 12 failures are `tests/i` mutants reported `broken_runner: errors outside the named cases` (the I list runs on the i8 stand-in, which other lanes held; environmental, rerun with tests/i when the stand-in is free) and **1 real list defect**: `tests/contracts` mutant `two_surface_versions` anchors `SURFACE_VERSION = "contracts-v2.0"` while F2C-L bumped the source to v2.1 (misdeclared, anchor 0 times) → micro-lane `codex/contracts-anchor` (Opus) re-anchors it. RUNTIME-LOGIN lane (`codex/runtime-login`, E3C F-1) dispatched on b3ba5dc7.
- 08:36Z: G8 lock-bound fix round DONE at **b06e3757** (code c26ef964): `set local statement_timeout = ms + 1000` beside the lock_timeout (both SET LOCAL), 55P03/57014 → FlagLocked; new PG regression `…overlapping_admissions_cannot_stretch_a_flag_write_past_its_bound` (8 lockers × 200 ms; fails 4/4 at 1b1fc11a, passes at the fix); tests/g/ops + operations_pg + signup 118 passed; ops mutants 100 passed (94/94); hand-checked edits all caught; evidence `research/plan/evidence/g/G8-c26ef96.md`; D10 wiring request V-G8TL-2 corrected to LOCK TABLE … IN EXCLUSIVE MODE inside a SECURITY DEFINER `infrx.set_feature_flag` (until it lands, a freeze under sustained admission traffic is bounded but may never succeed). Targeted recheck of V-G8TL-1 dispatched; merge + R117/R118 amendment follow.
- 08:40Z: targeted recheck of V-G8TL-1 at b06e3757 = **fixed** (probe FlagLocked at 3.01 s = bound + margin, 3/3; the lane's regression 3/3 at the fix and 3/3 failing with statement_timeout removed, no hang; both SETs are SET LOCAL; straddle case applied == [] / flag unchanged / 0 audit / same-key rerun exit 0). **Merged** `codex/g8-transition-lock` `--no-ff`; **R117 and R118 amended** in 08 §10 (one total bound; a refused freeze reports `applied: []`); the D10 wiring request for a fair EXCLUSIVE lock (`infrx.set_feature_flag`) recorded in the 08 log and in G8-c26ef96.md. Merged-tree cross-track runs no longer need the straddle `--deselect`.
- 08:41Z: tests/i on the D10 merge tree (dfc4fa75, i8 stand-in, busy-retry attempt 4): **214 passed / 1 xfailed** (F4) in 4 min 32 s — closes the 8 harness-busy errors of c3split. Full API suite on ccf37b55 (D10 + M5 merge tip, e2c harness, straddle case deselected): **4140 passed / 38 skipped / 9 xfailed / 1 failed** in 41 min (the failure triaged in the next entry).
- 08:41Z: the single full-API failure on ccf37b55 is environmental: the case runs `git ls-files models/marlin2b/results …` and the detached run used a `git archive` copy without `.git` (exit 128) — the same archive-only failure recorded for the pytest-9 run at a42f27f2; it passes in a worktree. Full API on the D10 + M5 tip is therefore green (4140 passed).

## Session 04 — login outage and full-scale resume (16:45Z)

- The coordinator account's login expired at ≈08:50Z and was restored at 16:43Z (the user ran `/login`; the session now runs as rey@callsofia.co). Killed by the outage: the W5 PART B lane (after committing the G7-tip merge 7b663206), the RUNTIME-LOGIN lane (after committing b58d4558), the contracts-anchor lane (all work already committed) and 9 of 11 agents of the M6-P2 verify-lane (lenses 0 and 2 finished: partial verdict REJECT — blocking 0-M6P2-R1 prepare registers the artifact row before writing it but no test pins the order (mutant survived); majors R2 re-materialized source collectable before admission → non-retryable not_found on D10, R3 `get()`/`local_uri()` unlink an expired file under a pin, R4 `pin()` races eviction; minors R5 bounded refs eviction of a live hand-off, R6 unpinned cache constants). All four resumed from their transcripts; the workflow resumed with `resumeFromRunId` (cached lenses, live fix round + rechecks).
- User instruction: "resume restore everything and run at full scale, max parallel tasks and agents". New parallel lanes on head 19670a8c: **E3C-RERUN** (`codex/e3c-rerun`: re-point the harness to G7/G8-lock, `make backend-local`, re-attribute the six former-G7 cases, re-measure F-2); **D10-FOLLOWUP** (`codex/d10-followup`: migration 0022 = `fail_preparation` port/adapter (R104/R133) + SECURITY DEFINER `infrx.set_feature_flag` (V-G8TL-2); the runtime door revoke stays a later migration after W5). Detached on the head: consumer-local gate then the non-I mutant lists (PID 689524, `scratchpad/wave4b/logs/gate-19670a8c.log`).
- `codex/contracts-anchor` **3a4fad52 merged `--no-ff`** (test-list hygiene; the mutant runs are its check — no separate lens, recorded as a deliberate exception: contracts list 534 passed, console v2 mutants 44/44 after the runner copy fix, pnpm test:mutants 212/212, node --test 213 pass; the console types test also carried the stale v2.0 literal).
- 16:45Z: **RUNTIME-LOGIN** handback `codex/runtime-login` **e37b33c6** (code b58d4558; evidence `research/plan/evidence/i/RUNTIME-LOGIN-b58d455.md`): pilot.py `DEDICATED_LOGINS = {infrx_runtime, infrx_monitor}` + `dedicated_login(dsn)` (bare name or Supavisor `<role>.<ref>`); `connection_pool` passes `set_role=not dedicated_login(DATABASE_URL)`; a dedicated login gets only `set statement_timeout` (nothing on 6543), every other login unchanged (R119 rollback); no new env name (rule from the DSN user; the env-schema digest is unchanged). Proofs: docker-free composition case (3 + 3 DSNs), real-PG case on m5 (gateway serves /readyz, /v1/models, POST /v1/jobs 202 and the worker pool connects as infrx_runtime; fails with set_role forced True), mutant `dedicated_login_sets_role` killed, tests/g minus ops 584, test_pooler 10 on i8, connector mutants 2/2, **E3C s10 on the dedicated login PASS** (4 passed) with `INFRX_E3C_RUNTIME_LOGIN=1`. Pre-existing failure found: `tests/d/test_composition_pg.py::…create_app_composes_the_pilot_from_settings_on_postgresql[legacy_usd]` → `unreachable at startup: price_source` at the base b3ba5dc7 as well (G7 WR-2 price probe in the legacy regime; diagnosis requested from the recheck). One-lens Opus recheck dispatched.
- 16:55Z: Tracker rev 44 (render 16:51Z) could not be republished to https://claude.ai/artifact/JNMLwutXNCKxZyzvyBGYoY — that artifact belonged to the previous account and is no longer reachable after the re-login (the tool reports it deleted/unshared). Published instead as a NEW artifact under rey@callsofia.co: **https://claude.ai/artifact/QfcUvbhNRAUsvGcQeYMR3Q** (version 1, "rev 44: after the outage, 7 lanes in flight"); later revisions go there. Detached console checks on e56dbf19 (log `scratchpad/wave4b/logs/console-checks.log`, 16:51Z): console-test / console-lint / console-typecheck / bench-test all exit 0 (bench 106 passed).
- 16:55Z: **RUNTIME-LOGIN merged**: one-lens recheck (Opus) of e37b33c6 = **ACCEPT** (owned paths only: pilot.py, tests/g/test_composition.py, tests/g/mutants.py, tests/d/test_composition_pg.py, evidence, update file; tests/g/test_composition 26 passed; 3/3 new G mutants killed; test_composition_pg credit + dedicated-login cases pass on m5; the set_role=True oracle reproduces the F-1 hang; deploy env-schema digest unchanged 1:c947948d1106e4fc; tests/i/test_pooler 10 passed on i8; 2/2 I8 connector mutants killed). `claude/consumer-v1` **445a8bcd** = `--no-ff` merge of `codex/runtime-login`. Minors carried, not fixed: RL-V1 `dedicated_login` splits on the first dot, so a contrived `infrx_runtime.a.b` role classifies as dedicated (fail-closed in practice: no service_role membership); RL-V3 no dedicated-login 6543 case in the docker-free test (probe measured [] on 6543 for both login classes; off 6543 the dedicated login gets only `set statement_timeout = 15000`, equal to 0021's role default); RL-V4 infrx_monitor is listed but nothing composes it; RL-V5 the operator CLI (`operations/cli.py build_operations`) reads the same DATABASE_URL and would fail on `set role` once the box env moves to infrx_runtime — the rollout runbook must give the CLI its own DSN (I8 doc item). The **legacy_usd `test_composition_pg` failure is pre-existing at the base b3ba5dc7** and is a test-world gap: G7 WR-2's `models.price_check` requires `catalog.usd_price(model_revision)` non-null, and the fresh() world (0001–0021 + checks_credit.apply_seed) has an empty `infrx.price_versions`, so `usd_price` is NULL and the probe correctly reports price_source unreachable → routed to the D10-FOLLOWUP lane (seed one USD price_versions row for nemostation/marlin-2b@2026-09-01 with effective_from <= the frozen clock in the legacy case, or assert the refusal). E3C-RERUN is told the full matrix with `INFRX_E3C_RUNTIME_LOGIN=1` now runs on 445a8bcd.
- 17:05Z: **I8-M6-WIRING** handback `codex/i8-m6-wiring` **e526366a** (impl 26e05761; base `codex/m6-phase2` 8fe53ed9; evidence `research/plan/evidence/i/I8-m6-wiring-26e0576.md`; update `updates/I8-20260925T0530Z.json` — its stamp predates the lane, check at ingest). Key finding: at 8fe53ed9 M6 records **no metric** (RetentionCollector fills Report only; ProcessingCache has no counters; no retention_*/processing_cache_* family in metrics.py), so OB-10 forbids panels in `rows`; delivered as a `pending` section of dashboard.json (families with closed label vocabularies + 2 rows of panels; tests/i fails the moment a pending family lands in FAMILIES, which forces the move into rows), rules in operations.json v1→v2 (alerts.json / OB-11 untouched): RetentionAborting (>=3 consecutive), RetentionStale (>1200 s), RetentionDeleteFailures, RetentionPendingDeleteOld (>900 s = claim TTL 300 + 2×300 s assumed interval), ProcessingCacheRefusing, plus the existing ProcessingCacheLarge; runbook anchors #retention-stalled/#retention-delete-failures/#processing-cache-high-water/#bucket-lifecycle-rule; new `deploy/s3-lifecycle.json` (one rule: AbortIncompleteMultipartUpload after 1 day, prefix infrx/, no Expiration; runbook merges via get → jq → put → get on $S3_MEDIA_BUCKET; NOT applied — coordinator/AWS step). Carries the i8-panels db_pool row verbatim. Lane counts: test_observe 3 failed → 16 passed / 1 xfailed; i3bm24 1/1; 3 new I mutants 3/3 killed; test_mutants 50 passed. **WR-I8-M6-1** (M6 + worker compose): declare the families in FAMILIES exactly as `pending.families`; RetentionCollector.run(metrics=None) increments passes/deleted{location}/retained{reason}/delete_failed/ack_lost, sets pending_delete_seconds, aborted{reason} + consecutive_aborted_passes, last_success_timestamp_seconds on completion; ProcessingCache(metrics=None) evicted{reason=high_water|expired}, refused before DependencyUnavailable; worker compose passes the Registry to both; then I8 moves pending.rows into rows. Merge order: after M6-P2 (its base). One-lens Opus recheck dispatched. P-25 thresholds (interval 300 s, PROCESSING_CACHE_MAX_BYTES, alert destination) remain open.
- 17:06Z: **CERTIFY-WIRING** handback `codex/certify-wiring` **b9b7df3e** (impl 5921d956; base e56dbf19; evidence `research/plan/evidence/e/CERTIFY-wiring-5921d95.md`; files certify.py, test_certify.py, e4b_mutants.py). E1C WR (1) `--unprofiled certify` superseded by (2): certify.py gains `--run-profile <base infrx.run-profile/1>` + `--key-inventory`; `cell_profile` stamps per cell (run_id-<cell>, dataset_version, manifest_sha256, seed, forms, max_tokens_mix, open-loop rate) and never loosens identity/target/bounds/spend/item_ids/profile_class; `bench_argv` passes `--profile --key-inventory` for every non-local cell; a remote target without both files (or wrong schema) leaves dataset-resume/envelope/soak/overload PENDING under owner PROFILE "BLOCKED: …", runs no client, non-zero exit, `Blocked` raised as a second guard; validity gate `bench_validity` (PASS only on VALID; remote cell without summary fails; local fake-engine unprofiled = UNKNOWN) wired into `rung_verdicts(summary=, local=)`, `envelope_summary`, soak and overload; the dataset drill is not gated (interrupted by design). WR (3) was already done by bba27b02 (E1B-protocol.md §5 9–11, §3). Lane counts: fails-before 3 failed; test_certify + test_run **95 passed** (42 + 4 new e1c cases, one runs the real `bench.py --validate-only`, no request); `INFRX_MUTANTS=all test_e4b_mutants.py` **239 passed** in 584 s (13 new mutants killed, 4 anchors moved); validate_plan PASS. No update file (no lane id for E4C; queued→review is rejected — correct). Coordinator items: the box certify command now needs a base profile (allowlist 127.0.0.1:8001 / target host, direct-gateway path, tenant_key_env, bounds ≥ 14,400 s soak + the 32-burst, item_ids over the full corpus with over-cap clips in expected_invalid) and an inventory file; the overload cell inherits the base profile_class, so the edge overload needs a separate public-edge run (S3 F5); E1C's follow-up flip (`--profile` mandatory for non-local targets, drop `UNPROFILED_OPT_OUT["certify"]`) is now unblocked; docstring/E4B box-protocol mention of the flags is an optional follow-up. One-lens Opus recheck dispatched.
- 17:07Z: **E1C-PROFILE-FLIP** micro-lane dispatched (Opus, `codex/e1c-profile-flip`, worktree `.claude/worktrees/codex-e1c-profile-flip`, base b9b7df3e = the CERTIFY-WIRING head, so it merges after it): bench.py `--profile` mandatory for non-local targets, `certify` dropped from `UNPROFILED_OPT_OUT` (smoke kept), regression tests + E1C mutants, test_certify must stay green on the local exemption. Owned paths bench.py / marlin2b tests / evidence e / updates.
- 17:14Z: **E1C-PROFILE-FLIP** handback `codex/e1c-profile-flip` **532d075a** (code bdad553; evidence `research/plan/evidence/e/E1C-profile-flip-bdad553.md`; update `updates/E1C-20260925T1713Z.json` — lane E1C is complete in the overlay, so the coordinator reopens/records it manually): `UNPROFILED_OPT_OUT = {"smoke": (4, 512)}`, `--unprofiled certify` is now an argparse error (exit 2), a paid target without `--profile` is refused in `_run` before any file or request; local/paid classification unchanged (runprofile.is_local = injected transport or loopback). Lane counts: fails-before (new test vs b9b7df3e bench.py: a paid `--unprofiled certify` run started and exited 0) → passes-after (4 cases exit 2, no request/upload/file); bench-test **107 passed**; test_certify 42 passed (local exemption holds); E1C mutants **108: 105 killed, 3 controls survived**, new e1cf27/e1cf28 + re-anchored e1cf12/e1cf13 killed. Coordinator wording to strike at the merge: E1B-protocol.md:71 "or --unprofiled certify until the E2C wiring passes profiles", the same clause in 08 §10 **R110** (amend) and session-03.md:85 (historical, leave; note the amendment). One-lens Opus recheck dispatched. Merge order: CERTIFY-WIRING → E1C-PROFILE-FLIP.
- 17:15Z: **M6-P2 verified**: wf_f04fb6fb-23a (3 lenses REJECT / ACCEPT_WITH_FIXES / ACCEPT_WITH_FIXES → 17 findings, 1 blocking, 8 major → one fix round → 9 rechecks) = **ACCEPT_WITH_FIXES at ff5aedb1** (code dea32507; update `updates/M6-20260925T1712Z.json`). Fixed and rechecked: 0-M6P2-R1 (blocking: prepare's register-before-write was untested — the writers case now attaches+prepares on f2c and d10 and asserts row-before-bytes; reviewer mutant X23 added as `m6_prepared_registered_after_the_write`, killed); R3 (`get()`/`local_uri()` unlinked an expired file under a pin — now expires through `_evict_file`, `_remove` deleted, `m6_get_ignores_pins` killed); R4/A2 (`pin()` raced eviction — after flock, `_names` compares fstat/lstat inode and raises NotFound; `_evict_file` makes the same check; 2 race tests, mutants `m6_pin_trusts_a_stale_open`/`m6_evict_takes_a_replaced_file` killed). Open, not code in this lane: **R2/A1** (major: a source fetched again after its grace can be collected between materialize and admission → D10 `bind_source` answers non-retryable not_found; reproduced on f2c and d10; kept as strict xfail `test_a_source_fetched_again_is_not_collected_before_its_admission`; **WR-7**: a written re-registration of a live key refreshes `eligible_at` under the row lock — D10 SQL (register_content) + the F2C fake → D10-FOLLOWUP lane, 0022 may CREATE OR REPLACE); **A3** (ruling: amend R114 with the lane's proposed generation-key text or order generation-keyed names; **WR-8** D10 implements or deletes 0019's "claim lapsed" comment); **A4** (wiring 1+2 must land together with P-25 declared; RETENTION-DURABLE not accepted end to end until then); **A5** (wiring 3 amended: E3C world.py → RetentionCollector and s06-live/s06-dark `set_clock` past the grace before the collectors — gates the merge). Minors R5 (bounded `Recent` can evict a live request's own hand-off at 4096 entries; ceiling documented) and R6 (unpinned cache constants). Next: merge lane `codex/m6-phase2-merge` on the integration tip with wiring 3 + doc 21 link fix, then R129, manifest M6 implemented.
- 17:16Z: **I8-M6-WIRING recheck** (Opus, one lens) = **ACCEPT_WITH_FIXES**, minors only: F1 the update file `I8-20260925T0530Z.json` is stamped 05:30Z, before its own base (08:00Z) and commits (17:04Z) — the coordinator re-stamps it as a new file at the merge; F2 fails-before is 4 failed (3 retention cases + the a1+o1→a1+o2 version assertion), not 3; F3 the oracle fires from the isdisjoint assertion ("declared now: move its panel into rows (OB-10)"), wording only. Verified: owned paths only; no retention_*/processing_cache_* family declared or recorded at 8fe53ed9 (processing_cache_bytes only by host-probe.sh); alerts.json diff empty; test_observe 16 passed / 1 xfailed at head after one 30 s harness wait; 3/3 new I mutants, i3bm24 1/1, test_mutants 50 passed; lifecycle asset one rule id infrx-abort-incomplete-multipart, prefix infrx/ = DeploymentSettings.s3_media_prefix (config.py:349), no Expiration/Transition; runbook get → jq merge → put → get on $S3_MEDIA_BUCKET only; db_pool row byte-identical to the integration branch; vocabularies match retention.py refusal/literal names; thresholds: aborted ≥3 and pending-delete 900 s from the M6 evidence, RetentionStale 1200 s and eviction reasons I8-derived (P-25 TO BE VERIFIED).
- 17:16Z: **M6-P2 merge lane dispatched** (Opus, `codex/m6-phase2-merge`, worktree `.claude/worktrees/codex-m6-phase2-merge` on a2b2d85c; merge-tree of ff5aedb1 reported no conflicts): step 1 --no-ff ff5aedb1; step 2 WR-6 doc 21 link; step 3 --no-ff e526366a (I8-M6-WIRING) + re-stamped update file; step 4 merged-tree checks (tests/m f2c+d10, retention mutants, contracts lifecycle + g/uploads, tests/w + composition, e3c no-stack, observe, validate_plan/progress check). Wiring 3 (E3C world.py → RetentionCollector; s06 set_clock past the grace) is assigned to E3C-RERUN, which owns the runner dir, after the merge SHA is known. Wiring 1+2 (worker single collector owner + cache keeper; W engine pin) = the next lane after W5 merges (they must land together, A4). WR-7/WR-8 sent to D10-FOLLOWUP.
- 17:19Z: **W5 PART B: BLOCKED** (`codex/w5-merge` **3439c50c**; evidence `research/plan/evidence/w/W5-merge-09d83a9.md`, update `updates/W5-MERGE-20260925T1717Z.json`): 7b663206 = --no-ff of the G7 tip 739c0414 (clean); 09d83a97 fixes a semantic conflict with G7 WR-1 (MAX_VIDEO_SECONDS default 82: the three over_the_cap cases pin the old 120 s profile for the admission only). Checks on the tree: tests/g + tests/m + tests/contracts **2422 passed / 1 skipped / 1 deselected** (G8 straddle, pre-lock-bound base); validate_plan PASS; tests/w with a task-local MinIO (55497): **5 failed / 303 passed / 3 skipped** — every failure logs `preparation of <id> not claimed: not_claimable`; the 12 PG mutants of the w5/prep_worker/worker_main lists are broken_runner for the same reason (no survivors). Cause = **W5 wiring request 4 not applied**: `routes/relay.py:166-168` still admits through `jobs.admit`/`admit_credit`, which never writes a readiness marker; `PgLifecycle.admit_ready` (lifecycle.py:178) has no gateway caller on any branch, so the merged tree prepares no PostgreSQL job at all (must not be deployed as is). **W5-ADMIT-WIRING lane dispatched** (Opus, same branch `codex/w5-merge`): merge the integration tip c037a8f8 first, then the relay admits through `ReadinessStore.admit_ready(request, idem, AdmissionExpectation)` for text and media in both regimes with the expectation from the composition root (regime + R69 card), fail-closed composition, G tests + 2 mutants, then the tests/w PG round trips and the three mutant lists with MinIO, tests/g+m+contracts without deselect. The first parallel run of the previous lane collided on the single w5 postgres (HarnessBusy/ForeignContainer) and was discarded; serial rerun is the recorded one.
- 17:19Z: **E1C-PROFILE-FLIP recheck** (Opus, one lens) = **ACCEPT** (owned paths only; `UNPROFILED_OPT_OUT == {"smoke": (4, 512)}`; `--unprofiled certify` = argparse error exit 2; under strace a paid target without a profile exits 2 in ≈0.4 s with no AF_INET socket/connect and no file created; fails-before reproduced (the old bench.py starts a paid `--unprofiled certify` run, exit 0); bench-test 107; test_certify 42 (loopback/injected transport still local); E1C mutants 108: 105 killed, 3 controls survived, e1cf12/13/27/28 anchored once each). Info PF-INFO-1: the certify opt-out is still described in R110, E1B-protocol.md:71, session-03.md:85 (history), progress-state.json:617-618 + progress.html (the stale E1C "REQUIRED NOW: certify.py bench_argv adds --unprofiled certify" wiring request) and updates/E1C-20260924T2300Z.json (history) — R110/E1B-protocol/overlay are struck at the merge. Nit: redundant parentheses at bench.py:2078 (e1cf13 anchors on them; leave).
- 17:19Z: **CERTIFY-WIRING recheck** (Opus, one lens) = **ACCEPT_WITH_FIXES**, blocking **CW-V1**: a remote overload cell can PASS when this run's bench refused it — `load_cells` discards the overload client's exit code, bench.py appends to --out and exits 2 before opening --raw/--out on a profile refusal, and certify never unlinks a cell's stale `<name>.jsonl`/`<name>-raw.jsonl` (the box protocol reuses `--workdir /out/work`): probe with a pre-seeded VALID overload summary + raw rows and an exit-2 client → envelope FAIL, soak FAIL, **overload PASS** (older than the diff, but it defeats the new validity gate on that cell). Fix round dispatched to the lane (unlink stale outputs per cell + client_exit on overload, regression with a pre-seeded workdir, 2 mutants; CW-V2 evidence fails-before is 4 failed not 3; optional CW-V5 local INVALID with empty reasons = FAIL). Verified: owned paths only; 95 passed; stamping never loosens the base (probe: tight bounds enforced, real `--validate-only` exit 2 on them); 6 missing/malformed profile+inventory combinations against an unroutable target → rc≠0, zero connect attempts, zero client runs, four cells PENDING owner PROFILE; validity gate edge cases (verdict not on the last line / no validity key / lowercase / non-JSON) all fail; e4b mutants 239 passed in 663 s, 13 new anchored once. Coordinator notes: **CW-V3** the overload cell from a P1 base is not S3 F5's P4 public-edge evidence — a separate public-edge P4 profile run is needed (or certify pends overload under PROFILE without a P4 edge profile); **CW-V4** parity and admission probes run outside the PROFILE gate (loopback engine only; no bench cell can start unprofiled).
- 17:25Z (user: "keep going, merge everything as it lands, keep checking the dependency graph and launching max parallel available tasks"): dependency check — launchable now without waiting on a lane result: **OPS-CLI-DSN** micro-lane dispatched (Opus, `codex/ops-cli-dsn` on feca50dd; RUNTIME-LOGIN RL-V5: `operations/cli.py build_operations` refuses a dedicated login DSN and prefers an operator DSN env var; tests/g/ops + 2 mutants; rollout README/runbook wording). Waiting on a lane result (not launchable): M6 wiring 1+2 (after M6-P2 merge and the W5 admit_ready wiring, same worker/__main__.py compose), door-revoke 0023 and G8 set_feature_flag call sites (after D10-FOLLOWUP's 0022 and the W5 merge; single SQL writer), dashboard pending→rows (after M6 wiring 1+2 declares the families), api-mutants + E3C final rerun (after the merges). Blocked on the user: box ops (W5 warm-up, G8 dry-run, E1C hosted smoke, I8 O3–O15, the S3 lifecycle rule, a public-edge P4 overload profile run — CW-V3), P-01…P-26, a GPU window for E1B/E4C. The consumer-local gate on 19670a8c is at api-w (api-contracts/d/g/i/j/m/q/t logs written); watcher re-armed.
- 17:31Z: **CERTIFY-WIRING fix round** done at **ff2d27ce** (impl 622b57fc): CW-V1 fixed (bench_argv unlinks the cell's stale `<name>.jsonl`/`-raw.jsonl` before each client run; load_cells keeps the overload client's exit and adds "the bench client exited N" to its problems; regression `test_cw_a_reused_workdir_never_lends_a_refused_cell_its_old_outputs` — before: envelope FAIL / soak FAIL / overload PASS; after: all FAIL, stale files gone); CW-V2 evidence corrected to 4 failed (the fourth = the soak/overload validity case); CW-V5 fixed (a non-VALID summary with an empty reasons list FAILs, local included). New mutants stale_outputs_kept / overload_exit_ignored / reasonless_invalid_excused (missing_summary_excused re-anchored); test_certify + test_run **96 passed** (43); e4b mutants **242 passed** in 589 s (0 survived); validate_plan exit 0. CW-V3/CW-V4 are coordinator items (recorded above). Targeted recheck of CW-V1 dispatched to the same verifier.
- 17:34Z: **M6 merged**: `codex/m6-phase2-merge` DONE at **0007fc16** (9b3bf97d = --no-ff of ff5aedb1, no conflicts; eb1c1f88 WR-6 doc 21 link; af6d4f35 = --no-ff of I8-M6-WIRING e526366a; 0007fc16 re-stamped update file I8-20260925T1704Z.json). Merged-tree checks: tests/m f2c+d10 **539 passed / 27 skipped / 2 strict xfails** (the WR-7 race on both worlds) in 176 s; retention mutants **43 killed**; contracts lifecycle + g/uploads 98; tests/w + composition **286 passed / 9 skipped / 1 failed** (the known legacy_usd price_source case); e3c no-stack 30 (gc.py is imported lazily at s06 stack time — wiring 3 still required); tests/i observe 16 passed / 1 xfailed; **OB-10 (`recovery/test_observe.py`) fails on the integration tip, pre-existing since G7's WR-I8-3 gauges**: families `infrx_large_body_slots_in_use/_limit`, `infrx_large_body_refused_total`, `infrx_intake_drained_total` (metrics.py:104–110, intake.py) have no dashboard panel → **I8-INTAKE-PANELS** micro-lane dispatched (`codex/i8-intake-panels` on 130e948b); validate_plan and progress check PASS. `claude/consumer-v1` **25826ab9** = --no-ff merge of 0007fc16. **R129 numbered**, **R114 amended** (generation naming), manifest **M6 → implemented** with the closure note (OPEN: wiring 1+2 with P-25, WR-7/WR-8 in 0022, E3C wiring 3; RETENTION-DURABLE not accepted end to end until then).
- 17:34Z: **CERTIFY-WIRING merged** (targeted recheck of CW-V1 at ff2d27ce = fixed: the verifier's probe now gives envelope/soak/overload FAIL with no stale .jsonl; regression fails with 5921d956's certify.py; the three new mutants + the re-anchored one killed; the full 242-mutant rerun is the lane's count, not independently rerun): `claude/consumer-v1` **(merge 1)** = --no-ff of `codex/certify-wiring` ff2d27ce; then **E1C-PROFILE-FLIP merged** = --no-ff of `codex/e1c-profile-flip` 532d075a → tip **130e948b**. **R110 amended** (the certify opt-out is gone), E1B-protocol.md §3 amendment struck likewise, the stale E1C "REQUIRED NOW" overlay wiring request closed. Tracker overlay rev 46 rendered (E3C forecast 2026-09-26 01:04Z–20:34Z, likely 07:34Z); publish follows. Next free ruling R132 (W5).
- 17:36Z: Tracker rev 46 published as **version 2** of https://claude.ai/artifact/QfcUvbhNRAUsvGcQeYMR3Q ("rev 46: M6, certify, profile flip merged"). E3C forecast now 2026-09-26 01:04Z–20:34Z (likely 07:34Z), critical path W5 → E3C.
- 17:36Z: **OPS-CLI-DSN** handback `codex/ops-cli-dsn` **c9cd921a** (code 952ea8c; evidence `research/plan/evidence/g/OPS-CLI-DSN-952ea8c.md`; update `updates/OPS-CLI-DSN-20260925T1734Z.json`): `build_operations(settings=None, *, environ=os.environ)` prefers $OPERATIONS_DATABASE_URL over DATABASE_URL and refuses a dedicated login (infrx_runtime/infrx_monitor, bare or role.ref, via a lazy import of `pilot.dedicated_login`) with SystemExit before `connector()` — the message names the variable, never the DSN; unparsable DSN → "<VAR> is not a valid connection string" raised from None. No config.py change (operator-shell credential like INFRX_OPERATOR_KEY), preflight schema_id unchanged c947948d1106e4fc. Lane counts: test_cli fails-before 2 → 7 passed; tests/g/ops + test_composition on g8 **112 passed** (without docker the 16 *_pg cases hit HarnessBusy on 55432 — they do not skip, pre-existing); 4 operations_* mutants killed (2 new); tests/i doc cases 12 passed. Docs: rollout README §3 + runbooks/rollout.md W7d (variable name only). Hosted follow-up (coordinator): the box's operator shell exports OPERATIONS_DATABASE_URL before the env file's DATABASE_URL moves to infrx_runtime. One-lens Opus recheck dispatched (secret-hygiene probe with a fake token through a full traceback).
- 17:36Z: **Gate attribution caveat**: the detached consumer-local gate "on 19670a8c" runs `make consumer-local` inside the infrx-impl worktree, whose HEAD advanced through the M6/certify/profile-flip merges during the run — its integration-l3 stage records git_head **b089fa98**, so this run is mixed-head and NOT attributable to one SHA; it also started a backend-local stage on the infrx-e3c-* containers (E3C's compose block) while the E3C-RERUN lane owns that block, so the two may have collided. Its verdict is informational only. Rule for coordinator gates from now on: run them from a fixed `git archive` copy or a detached worktree at the candidate SHA, never from the moving integration worktree, and never on a lane's compose block while that lane is active.
- 17:39Z: **OPS-CLI-DSN recheck** (Opus, one lens) = **ACCEPT** (7 files, all owned; lazy import confirmed, no cycle; 5 env variants carrying a fake token through `python -X dev` with faulthandler → exit 1 in ≈1 s, no dial, **0 token hits** in stdout+stderr; postgres.ref / infrx_ops / infrx_runtimex.ref reach connector(), `?user=infrx_runtime` and bare infrx_monitor refused; fails-before 2; test_cli 7; operations mutants 6 passed with unique anchors; preflight schema_id c947948d1106e4fc; docs carry no DSN/password/host; tests/i doc cases 12). Info OBS-3: with an explicit settings object the message names DATABASE_URL (cosmetic; the CLI never passes settings). **Merged**: `claude/consumer-v1` = --no-ff of `codex/ops-cli-dsn` c9cd921a. Hosted follow-up stays with the coordinator: export OPERATIONS_DATABASE_URL in the box's operator shell before the env file's DATABASE_URL moves to infrx_runtime (box-ops list).
- 17:44Z: **consumer-local gate (started on 19670a8c, mixed-head, informational)**: verdict FAIL at 17:42Z (`scratchpad/wave4b/consumer-local-19670a8c/verdict.json`): preflight/s3/api-contracts/api-g/api-j/api-m/api-q/api-t/api-w/bench-test PASS; **api-d FAIL** = only the known legacy_usd price_source composition case (810 passed / 1 failed / 8 xfailed; D10-FOLLOWUP); **api-i BLOCKED** (1 quarantined strict xfail, F4, as before); **integration-l3 FAIL** on three real tip issues: (a) `test_harness::test_the_migration_set_is_the_console_one_and_is_read_in_filename_order` pins the pre-0019 migration list; (b) role matrix `E3B-RLS-W-infrx.media_uploads-service_role` expects service_role INSERT/UPDATE/DELETE on media_uploads, which 0019:959 deliberately revokes (least privilege); (c) `test_the_role_matrix_has_a_row_for_every_relation_and_security_definer_function` — 0019–0021 relations/functions have no matrix rows; plus the backend layer-0 set: OB-10 (I8-INTAKE-PANELS in flight) and `test_restore::test_i3b_bk01h_a_restored_job_results_keeps_its_rpcs_and_stays_owner_only`; the 16 layer-0 defect detectors all still detect. **backend-local FAIL** on the e3c block (collided with E3C-RERUN's block; s06 FAIL pending M6 wiring 3, s11/s13 BLOCKED, nc-result-expiry FAIL — E3C-RERUN interprets on a clean run). The driver's non-I mutant stage (also mixed-head) was **killed** at 17:5xZ (process group 689527); api-mutants rerun on a fixed copy of the final head instead. → **L3-REBASE** micro-lane dispatched (Opus, `codex/l3-rebase` on 496fdc84): re-baseline the migration pin and the role matrix to the intended 0001–0021 privilege model from the migration text (rows for every new relation/SECURITY DEFINER function; media_uploads service_role per 0019:959 + R122–R127), fix bk01h, report any unintended privilege change as a finding rather than re-baselining it.
- 17:45Z: **I8-INTAKE-PANELS** handback `codex/i8-intake-panels` **98ffba84** (impl a4efcf1c; base 130e948b; evidence `research/plan/evidence/i/I8-intake-panels-a4efcf1c.md`, update `updates/I8-20260925T1744Z.json`): dashboard row "Is the intake saturated?" (slots in use vs limit, refused rate, drained by code — 4 fixed codes; gateway-only, no process split; runbook link #intake); found and fixed a pre-existing dead rule: `GatewayBodySlotsFull` read `infrx_large_body_in_use/_limit`, names nothing records, so it could never fire — now the real `_slots_` names, threshold unchanged; tests/i `runtime_producers()` now resolves the module constants intake.py passes to `record()`; runbook "## Intake" (slots pinned at the limit = load, not a ticket to raise LARGE_BODY_LIMIT; pinned with no traffic = leaked slot → restart the gateway; refusal/drain thresholds TO BE VERIFIED, P-25). Lane counts: OB-10 fails-before 1 failed → 16 passed; tests/i test_observe 18 passed / 1 xfailed (one HarnessBusy retry); 3 new I mutants killed; test_mutants 50; i3bm24 1/1. One-lens Opus recheck dispatched.
- 17:52Z: **I8-INTAKE-PANELS recheck** (Opus, one lens) = **ACCEPT** (7 owned paths; the drained `code` vocabulary equals intake.py's CLOSE_CODES {invalid_api_key, request_too_large, capacity_exhausted, deadline_exceeded}; compare panel pairs the two gauges; the old `GatewayBodySlotsFull` names have 0 producers at base, the new ones are recorded at intake.py:312–313/329/482–488, op/threshold unchanged, runbook #edge → #intake; OB-10 1 failed → 16 passed; tests/i observe 18 passed / 1 xfailed with the two new cases failing on base infra; 3/3 mutants with unique anchors; test_mutants 50; i3bm24 1/1; a scratch no-producer family + rule was still flagged by the producer-coverage case). Info: `runtime_producers()` matches `record(..., "inc", CONST)` textually on one line (a commented call would count; an imported constant would not). **Merged**: `claude/consumer-v1` = --no-ff of `codex/i8-intake-panels` 98ffba84. OB-10 is green on the tip again; the M6 retention/cache panels stay in `pending` until M6 wiring 1+2 declares their families.
- 18:13Z: **L3-REBASE** handback `codex/l3-rebase` **02eb87ff** (code 8a94a855; base 496fdc84; evidence `research/plan/evidence/e/L3-REBASE-8a94a85.md`, update `updates/L3-REBASE-20260925T1812Z.json`, task E2C): migration pin → explicit 0001–0021 (filename order still asserted); role matrix re-derived from the migration text — media_uploads service_role table write '' (0019:956–959, R128); content_objects/job_readiness SERVICE read only (0019:950–955); 17 boundary SECURITY DEFINER functions + usd_price + jobs_result_expiry_guard SERVICE, 12 internal functions NOBODY, consumer_jobs/consumer_job_result authenticated+service_role (0021:426–428), 7 invoker helpers NOBODY; 12 new L3-LOGIN rows for infrx_runtime/infrx_monitor (0021:458–560, R127: no login attributes, no memberships, runtime = exactly 0021's 42 functions + 13 SELECT relations + SELECT/INSERT staged_media/job_media + column UPDATE jobs.updated_at; monitor executes nothing, two reconciliation views with column grants); bk01h re-timed to the persisted `result_expires_at` (0020:439–462, R125) with RPC/ACL assertions unchanged. Counts: base fail-first rls 731/1 failed, backend 3 failed, suites 3 failed → head **rls 880 cases / 0 failed**, suites **180 passed**, backend 231 passed / 1 failed (ob10 — fixed on the tip by I8-INTAKE-PANELS), bk01 25 passed (b/c/d still detect), mutants e2m74–76 killed / control e2c02 survived, validate_plan PASS. Findings: F1 = ob10 (closed); **F2** 0021 never revoked `infrx.jobs_result_expiry_guard()` (service_role keeps 0004's default EXECUTE; trigger function, harmless) → 0022; F3 infrx_runtime still executes infrx.admit/claim_preparation (R123 interim) → 0022/0023 with the pgstate/pin patch as a wiring request. One-lens Opus recheck dispatched (independent derivation of the rows from the migration text; a pinned privilege bug is blocking). D10-FOLLOWUP told.
- 18:15Z: **E3C-RERUN** handback `codex/e3c-rerun` **601bc4d6** (base 19670a8c + plain merges 445a8bcd → 623c3479, 25826ab9 → 6b5d32b9, 130e948b → fb86c153; run 1 5b38ab08 on the G7 head with the owner login; run 2 7d0d3d7b on the merged tree with `INFRX_E3C_RUNTIME_LOGIN=1`; evidence `research/plan/evidence/e/E3C-rerun-7d0d3d7.md`, update `updates/E3C-20260925T1820Z.json`). **BACKEND-LOCAL still FAIL**. Run 1: 80 cases, 68 PASS / 9 FAIL / 3 BLOCKED (W5 3, M6 4 — collector not yet re-pointed, harness 1 — s08 S3 answered at 40.04 s vs a 40 s oracle, defects F-3 ×3 + F-1 ×1). Run 2 (whole matrix on infrx_runtime): 80 cases, **72 PASS / 8 FAIL / 0 skipped** in 12:07 — PASS s01 s02 s03 s07 s08 3/3 s09 4/4 s10 3/3 s12 33/33 s13 2/2, s06 live+dark PASS; FAIL: W5 ×3 (s04 video, s04 permanent refusal, s05 admission), **F-3** ×3 (s04 text, s04 late, s05 readiness), **F-4** ×2 (s06 scrub, s11 scrub race); every negative control that can run detects (journey-revoke/tenant, upload-restart, result-expiry, roles-browser, credit-cutover, verify-repro); nc-admission-ready / nc-retention-durable NOT RUN (revert-type on red scenarios). The six former-G7 cases: s07, s13 claims, s13 projection **PASS** (violations empty against the approved 82 s profile and the box profile); s04 text / s04 late / s05 readiness re-attributed to F-3 (+W5). **F-1 closed** (s10 passes on infrx_runtime). **F-2 closed** (R130 re-measured: PG paused → 503 dependency_unavailable in 20.0 s, bound 40 s; S3 paused on a video request → 503 in 40.0 s in preparation, R130 ceiling 60 s; afterwards the same key yields one job that settles once). **F-3 (new, = W5 WR-4)**: relay.py:167–168 admits via admit_credit/admit, no readiness marker, `PgLifecycle.admit_ready` has no caller in gateway or worker — today a text job runs and is debited while its acceptance is incomplete and a late rejection is charged (RV-05 open); with W5 as-is the marker-gated claim refuses every relay-admitted job → being fixed by **W5-ADMIT-WIRING** (in flight). **F-4 (new)**: the stream journal is never pruned — `PgStreamStore.expire` (journal.py:121 → infrx.expire_journal, 0017) has no caller in any composed process and RetentionCollector scrubs job_results/request_record but not stream_chunks, so SSE deltas outlive JOURNAL_CHUNK_TTL_S (3600 s) → assigned to the **M6 wiring 1+2 lane** (worker scheduler: RetentionCollector.run + cache keeper + journal expire), dispatched after W5-ADMIT-WIRING merges. Harness changes (owned paths, e3c unit tests 34 passed): s13 rewritten for G7's PublishedModel (real request per advertised and per refused parameter, both profiles); expiry-recompute bypass re-pointed to `Relay._owned`; new s12 regression "every bypass installs on the current tree" (red on the phase-2 world.py); s08 records answer times and applies R130 per service; new s09 G8 lock-bound case; s11 scrub race now against M6's collector; WR-3 applied (RetentionCollector over PgLifecycle, connect_timeout=5, clock past GRACE_S; the collector's connector skips `set role` on a dedicated login). Revert controls readiness/expiry still FAIL as required. Unresolved: F-3 + W5 land together → nc-admission-ready; F-4 → nc-retention-durable; the final run should use M6's worker entry once wiring 1+2 compose it; ruff F401 at scenarios_crash.py:23 pre-existing. Estimate for the final rerun 1/2/5 h. One-lens Opus recheck (harness honesty) dispatched.
- 18:16Z: **M6-WIRING lane dispatched** (Opus, `codex/m6-wiring` on 4959e10c, in parallel with W5-ADMIT-WIRING — both touch worker/__main__.py compose additively; the merge lane unions them): M6 wiring 1 (worker = single collector owner: PgLifecycle + MediaPreparation(content=lifecycle, cache max_bytes) + RetentionCollector.run + keep_cache; gateways none), wiring 2 (engine pins every file:// input submit→terminal; NotFound → re-prepare once), WR-I8-M6-1 (families declared, RetentionCollector/ProcessingCache metrics, dashboard pending → rows), E3C **F-4** (PgStreamStore.expire scheduled in the worker so stream_chunks do not outlive JOURNAL_CHUNK_TTL_S), settings PROCESSING_CACHE_MAX_BYTES / RETENTION_INTERVAL_S / CACHE_SWEEP_INTERVAL_S / JOURNAL_EXPIRE_INTERVAL_S with P-25 placeholder defaults marked TO BE VERIFIED. Dependency graph now: W5-ADMIT-WIRING (F-3) → W5 merge lane (+ M6-WIRING union) → R132–R135 + manifest W5; D10-FOLLOWUP (0022) → G8 set_feature_flag call sites + door-revoke (0023) + pgstate/pin patch; L3-REBASE recheck → merge; E3C-RERUN recheck → merge harness; then E3C final rerun on the worker entry (nc-admission-ready, nc-retention-durable) → BACKEND-LOCAL verdict; api-mutants on a fixed copy of the final head; tracker republish.
- 18:22Z: **E3C-RERUN recheck** (Opus, one lens, used the free e3c block and tore it down) = **ACCEPT**: 9 paths (7 harness files + evidence + update); plain merges only, nothing pushed; run-2 verdict re-read (head 7d0d3d7b clean, deviations [], 73 cases + 7 negative controls = 80, 0 skipped; every FAIL is a product assertion — debits 1 / inference 1 / prepared 1 before eligibility for F-3; stream_chunks deltas 5 with body/request_record 0 for F-4; RuntimeError is scored FAIL, conservative); e3c unit tests 34; the new s12 install regression fails on the phase-2 world.py; `--break-seam expiry` still FAIL; **s13 proven** by four scratch mutants (advertise 120 s video; `tools` moved; admission refuses an advertised `seed`; an accepted `seed` listed as refused) — all FAIL 2/2 against both profiles; **s06-live proven** (content_referenced → NULL makes it FAIL); WR-3 reuses `pilot.dedicated_login` (pilot.py:120), connect_timeout=5, clock past GRACE_S; s09 parks a FOR SHARE `require_feature` transaction and asserts state_conflict/open_transactions within 5+15 s with flags/wallets/ledgers unchanged; F-3/F-4 confirmed by grep at fb86c153 (relay.py:167–168; journal.py:121 with callers only in contracts/conformance; the only DELETE FROM stream_chunks is 0017:316). Minor **V-1**: s08's S3 bound moved from 40 s to 60 s after run 1 measured 40.04 s; R130's 60 s clause covers a full preparation followed by a stalled store call, but this video path is described as one stall → tighten to 40 s + a stated round-trip margin, or show the second stalled call (E3C final rerun). Info: the two revert-type controls have always been NOT RUN without `--control`; s13's running profile comes from validate.SUPPORTED/UNSUPPORTED in-process (shared tree). **Merged**: `claude/consumer-v1` = --no-ff of `codex/e3c-rerun` 601bc4d6 (harness only; BACKEND-LOCAL remains FAIL and E3C stays planned).
- 18:23Z: **L3-REBASE recheck** (Opus, one lens) = **ACCEPT**: 6 files only; every new row re-derived from the migrations (media_uploads 0010:135–137 + 0019:959 → service_role SELECT only, browsers nothing; content_objects/job_readiness 0019:952–955; 13 NOBODY definers 0019:964–975 / 0020:470–481 / 0021:420,429–430; 17 SERVICE boundaries 0019:976–986 / 0020:482–489; usd_price 0021:447–448; consumer_* views authenticated+service_role, anon false, 0021:420–428; logins 0021:473–483 nologin/noinherit, member of no role; runtime relations 0021:518–537; monitor 0021:543–549); infrx_runtime's function set = the 0021:490–512 grant loop **exactly (42 = 42)** — no settle/reconcile/grant/adjust/transition/signup writer; admit + claim_preparation remain the R123 interim doors (readiness_cutover_check reports them as runtime_unmarked_doors) → after 0022/0023 RUNTIME_FUNCTIONS drops to 40; no anon-executable SECURITY DEFINER (42501 schema denial); bk01h still proves RPCs kept + owner-only (relacl NULL, effective privileges unchanged, service_role InsufficientPrivilege on the table). Layer-3 rerun rls **880 / 0**, suites 180, backend 231/1 (ob10, closed on the tip), bk01 25 with b/c/d detecting; e2m74–76 killed, control survived; validate_plan PASS. Infos: V2 jobs_result_expiry_guard pinned SERVICE from 0004's default privilege (→ 0022, sent); V3 the three mutants mutate expectations, not grants — a SQL re-grant mutant would be stronger (D10-owned, optional); V4 rows scan infrx/public only. **Merged**: `claude/consumer-v1` = --no-ff of `codex/l3-rebase` 02eb87ff. E2C lane stays in review until a fixed-copy consumer-local run is green.
- 18:23Z: E3C-RERUN applied V-1 at **225b36ba** on `codex/e3c-rerun` (s08 bound = R130's single-stall 40 s + a named `CLIENT_MARGIN_S = 2.0` for both PostgreSQL and S3; the 60 s S3 value is gone; e3c unit tests 34) and is parked for the W5 + F-3 + F-4 SHA. Raw verdict paths: `scratchpad/run1/verdict.json`, `scratchpad/run2/verdict.json` (logs e3c-rerun.log, e3c-rerun-run2.log); they go into the final-run evidence file. 225b36ba merges with the final harness handback.
- 18:23Z: **api-mutants on a fixed copy**: `git archive ef390372` → `scratchpad/wave4b/fixed-ef390372`, `INFRX_D_TASK=e1c make api-mutants` detached (PID 2665570, log `scratchpad/wave4b/logs/mutants-ef390372.log`, started 18:23Z; ≈2 h). Early signal on the merged M6/certify/panels/L3 tree; the final head gets its own run.
- 18:40Z (user: "check if any parallel tasks is unblocked"): dependency check on b84cd0bd. Running: W5-ADMIT-WIRING, M6-WIRING (+ add-on: pin the M6 R5/R6 survivors), D10-FOLLOWUP, fixed-copy api-mutants; E3C-RERUN parked at 225b36ba. Blocked on a running lane: W5 merge lane + R132–R135 (drafted in the scratch rulings file) ← W5-ADMIT-WIRING + M6-WIRING; door-revoke 0023, G8 `set_feature_flag` call sites, the pgstate/pin re-baseline ← 0022; E3C final rerun + BACKEND-LOCAL verdict ← the W5 merge; fixed-copy consumer-local + final api-mutants ← 0022 (api-d legacy_usd) and the W5 merge (backend-local); E2C lane closure ← that gate. Already landed and not open: WR-I8-5 digest fields (merged f8c70b55, values null until measured on the serving host — box op). Blocked on the user (unchanged): box ops incl. the serving-host digest measurement (P-06), P-01…P-26, GPU window. Nothing else is launchable without a lane result; slots stay at 4 lanes + 1 detached run.
- 18:51Z: **Box read-only checks**: `10-inventory.sh` (host up, checkout bda1586, all four units active, listeners :80/:443/:22 only, four install backups); new coordinator step `78-e4b-report.sh` (tests/i/test_rollout step list updated, 8 passed) read **E4B run3's final report**: preconditions/config-pin/served-build/sop-parity PASS, dataset-resume FAIL (regime mismatch, S3 finding 3), envelope FAIL (supported 0.5/s; the 0.5 rung passes every verdict), soak PENDING (0.25/s × 14,400 s: 3/3374 platform-caused failures, host +13.2 MiB, gpu growth unknown, reconciled_at_end unknown per S3 finding 4, latency p50 2.80 → 2.88 s), **overload PASS** (32-burst: 8 accepted / 24 refused — the drain's first live exercise), release-identity PASS; `85-known-good-box.sh TARGET=bda1586` → bundle matches its sha256, checkout has it, runtime image cached: **bda1586 is a reinstallable known-good target**. The box lock is free.
- 18:51Z: **Model mirror (I8 O10, box → S3)**. Purpose: the durable copy of the served artifacts (weights, processor, tokenizer, chat template, configs) with a digest manifest, so a replacement instance can restore them (I8 slice 5, P-25). Target `s3://llm-bootcamp-641134885443/infrx/mirror/marlin2b/` (same account 641134885443; the user authorized AWS use on 2026-09-25). Cost: one ≈5 GB in-region PUT set, ≈USD 0.12/month of S3 standard storage (est.). Rollback: `aws s3 rm --recursive` on that prefix. Command: `80-mirror-artifacts.sh RELEASE=bda1586 MIRROR_URL=…`, then `81-restore-artifacts.sh MODE=fetch CLEANUP=1` to prove the restore path.
- 18:51Z: **Hosted-database operations are held**: the coordinator's tool policy denied the hosted logical dump (it carries the four test users' e-mail addresses) and hosted-DB reads from this session; the operator must run restore.md Part A (dump + local restore rehearsal + 0019–0021 rehearsal on the copy), the privilege probe, the G8 inventory/dry-run and the scoped-key issue for the hosted smoke, or grant this session a Bash permission rule for them. Everything else proceeds.
- 18:52Z: **Model mirror refused, nothing changed**: `80-mirror-artifacts.sh` exit 2 on the box — the checkout is bda1586, which predates I8, so `infra/runbooks/artifacts.py` is not on the box; the S3 prefix `infrx/mirror/marlin2b/` holds 0 objects. O10/O11 (mirror + restore-fetch) therefore run inside the rollout window of the I8-bearing release (after 40-checkout), not before. **Rollout precondition chain for that window**: final head (W5 + M6 wiring + 0022 merged) → gates G1–G6 locally → root-volume snapshot → hosted backup + restore rehearsal (restore.md Part A, **held: needs the operator or a permission rule**) → hosted migrations 0019–0022 → 40-checkout / 50-install / 60-verify / verify-external → O4–O15 (observe install with the alert destination, alert test, mirror, restore-fetch, W5 warm-up, rollback drill, evidence export, cleanup).
- 18:58Z: **INPUTS-DECIDE** delivered `2026-09-25-inputs-decisions-draft.md` (committed b0858371; ≈5,500 words, every number sourced): P-01 card `rc_marlin2b_20260925_launch` 400 / 1,200 CREDIT per 1M input/output tokens (meter tokens-v1, same as the provisional seed; holds round up to 8 places, charge rounds half-up once; max hold 14.7456 CREDIT/request; a 72 s clip ≈ 6.52 CREDIT; cost basis USD 0.24–0.45 per successful video-hour, never used to compute CREDIT); P-02 no conversion, the USD 5.00 test balance stays on its USD statement, activation notes it; P-05 public signup stays closed for the backend gate, a second verified tenant provisioned as a logged op; P-06 one box `inventory.sh` run at the I8 release fills the two digests, stop rule on mismatch; P-17 a 10-item mechanical acceptance checklist; P-18 limits from run3 (0.5 req/s; platform errors ≤1 %; TTFT p95 ≤6 s; latency p95 ≤9 s; ≤90 s per clip-minute p95 — replacing the provisional 45 s that run3 failed at 77.3 s; 32-burst through the public edge; soak 14,400 s at 0.25 req/s with GPU growth and end reconciliation measured; recovery ≤300 s engine / ≤30 s worker / ≤30 s Valkey; no availability claim); P-19 g6e.2xlarge USD 2.24208/h on-demand row (sourced; derived costs est.); P-22 already decided (R109) → close; P-24 the base run profile (allowlist 127.0.0.1:8001, direct-gateway, 3,600 requests, 14 GB, 30,720 input tokens/request, 14,700 s, four 112 s clips expected-invalid) + key-inventory shape; P-25 TTLs kept (results 24 h, journal 1 h, cache/source 7 d, idempotency 24 h, claim 300 s), retention grace 7 d → 1 h, cache cap 50 GiB, collector/sweep every 300 s, an SNS alert topic or a Slack-compatible webhook, logical-dump policy while PITR is off, known-good = known-good.py's six checks; P-26 keep the bounded 60 s staleness (new admission re-checks revocation in SQL, 0011:272), public copy provided. **Blockers it found**: (1) the run profile cannot express a CREDIT spend cap → **PROFILE-CREDIT-CAP** micro-lane (E1C) dispatched; (2) a certify run spends ≈9,200 CREDIT against the 10,000 grant → an audited `adjust +40,000` on the test tenant, operator-held (hosted write); (3) source retention unenforced until M6 wiring 1 (in flight); (4) deliver.py cannot publish to SNS → **ALERT-SNS** micro-lane (I8) dispatched; (5) the P-01 approval text must not contain "P-01"/"provisional"/"pending"; (6) two records disagree on the verified-user count (3 vs 4) — settled by a hosted read, operator-held. The coordinator ratifies the draft; the ratification patch to 15-pending-inputs.md is being produced. **SNS topic creation was refused** by the tool policy (shared-resource modification): the alert destination stays operator-created.
- 19:00Z: **Pending inputs ratified**: `15-pending-inputs.md` gains "Decisions 2026-09-25 (coordinator, under the operator's authorization)" (append-only, one row per input with sources, assumptions, enactment owner, reversibility; the P-01 approval text is a final approval without the blocking tokens); no ruling changes (R21/R106/R109/R117/R131 applied as written). Overlay: P-22 → resolved; P-01/02/05/06/17/18/19/24/25/26 stay `open` with "DECIDED 2026-09-25, enactment pending" and their enactment owners (the tracker keeps E4C "blocked pending" until each enactment is recorded and flipped). Tracker overlay rev bumped.
- 19:01Z: **P-19 enacted**: `research/cross-cutting/cloud-pricing.md` §3.1 gains the `g6e.2xlarge` row (USD 2.24208/h on-demand from the AWS sheet already pulled for production-api/08; spot USD 2.0354 meas. 2026-09-20 with its quota caveat) and an audit-log entry; derived pilot costs stay est.; overlay P-19 → resolved.
- 19:07Z: **PROFILE-CREDIT-CAP** handback `codex/profile-credit-cap` **dbc3aeef** (impl 54b3733; evidence `research/plan/evidence/e/E1C-credit-cap-54b3733.md`, update `updates/E1C-PROFILE-20260925T1910Z.json`): schema stays `infrx.run-profile/1` (additive); `bounds.spend.currency` ∈ {USD, CREDIT}; unit-neutral keys `max_spend`, `outstanding_holds`, `rates.input_per_mtok`/`output_per_mtok`; legacy `*_usd` keys accepted only as USD (deprecation warning), refused under CREDIT, mixed naming refused; `spend_projection` in Decimal from the JSON text with an exact cap comparison; `derived.spend_currency` + exact `derived.projected_spend` (the unit-named `projected_spend_usd` removed, no external reader); bench.py/certify.py unchanged, one override line in test_certify.py. Exact cases asserted: 13.5168 CREDIT per E4C-bound request; 3,600 → 48,660.48 (cap 50,000 admits, 48,660.47 refuses); 5 + 0.01 holds = 67.594 admitted at 67.594 (a float would refuse), refused at 67.5939; USD 12.16512 under both key sets. Counts: fails-before 5; test_profile 19; bench-test 110; E1C mutants 112: 109 killed / 3 controls (e1cp20–23 new); certify + test_run 96; validate_plan PASS. Proposed ruling (to number at the merge, R136): run-profile spend amounts are in `bounds.spend.currency` and compared exactly; legacy `*_usd` keys are USD-only; USD is never read as CREDIT. Follow-up: the E4C base profile (currency CREDIT, max_spend 50,000, rates 400/1,200) is committed once the card is published (P-01, operator-held hosted write). One-lens Opus recheck dispatched.
- 19:10Z: **D10-FOLLOWUP** handback `codex/d10-followup` **0d577c4e** (code 4b462094; base 19670a8c; evidence `research/plan/evidence/d/D10-followup-4b46209.md`, wiring hunks `D10-followup-wiring.patch`, update `updates/D10-20260925T1910Z.json`; the single new migration **0022_preparation_refusal_and_flag_writer.sql**, 271 lines, 0001–0021 unchanged): (1) `infrx.fail_preparation(jsonb{lease,cause,limits})` — invalid_media/preparation_failed only; replay check → 0016 `fence_lease(['preparation'])` → `terminalize_no_usage(...,'failed')` (released_free, hold released in its unit, reservations + attempt released, one usage + one trace projection); replay only for the identical lease; adapter `PgJobStore.fail_preparation`; grants service_role + infrx_runtime; (2) `infrx.set_feature_flag` SECURITY DEFINER, EXCLUSIVE table lock then the guarded UPDATE, service_role only; (3) legacy_usd composition case asserts price_source unreachable first, then seeds the USD rows and passes; (4) WR-7 in SQL: 0022 replaces `register_content`/`content_objects_guard` — a written same-digest live re-registration sets eligible_at = greatest(eligible_at, now()+grace) under the row lock, never for discovered, the guard lets eligibility move later only; (5) WR-8 retired with the header's reasoning; (6) L3 F2 `jobs_result_expiry_guard()` revoked from everyone; the door revoke stays for 0023. Lane counts (d10): tests/d **841 passed / 1 failed** (the lifecycle transcript case that needs W2) / 8 xfailed; Supabase-image D10 suites 133 passed / 1 failed (same); with W2 applied 27/27 on both images; `INFRX_MUTANTS=all` D lists **732 passed / 0 survivors** (22 new migration + 2 code mutants); contracts 1260; validate_plan PASS; **privilege_probe --role infrx_runtime PASS 64/64** on both images. Wiring at the merge: W1 G8 `PgTransition.set_flag` → `select infrx.set_feature_flag` (6 PG transition cases pass on a scratch merge); **W2 F2C (must merge together)**: the WR-7 rule in `fakes/lifecycle.py` + regenerated acceptance fixtures; W3 M6 drop the strict xfail; W4 layer 3: pgstate FUNCTIONS + RUNTIME_FUNCTIONS + the 0022 pin (test_harness 27 on a scratch merge); W5 none (W5's `_end` finds the adapter; the F2C port `JobStore.fail_preparation` still to freeze); W6 I8 optional MUST_DENY. Rulings proposed in evidence §6. **Full 3-lens verification (verify-lane workflow) dispatched** — money first.
- 19:11Z: **ALERT-SNS** handback `codex/alert-sns` **3e5e725e** (impl 0bbf336; evidence `research/plan/evidence/i/I8-alert-sns-0bbf336.md`, update `updates/I8-ALERT-20260925T1910Z.json`): deliver.py `send()` picks exactly one destination (webhook xor `ALERT_SNS_TOPIC_ARN`; both/neither/malformed → BLOCKED exit 3 naming the two variables, alert kept undelivered); `publish()` lazy-imports boto3, region from the ARN, Subject = first line ASCII ≤100, Message = full text, ClientError/BotoCoreError → exit 4 with the alert kept and state untouched (retry next run); webhook path unchanged, URL never printed; 72-observe-install takes `ALERT_SNS_TOPIC_ARN=<arn>` as a plain value into the 0600 env file (regex-checked, both-set refused before writing); 74 passes it through. Counts: fails-before 5+2; observe + ops-steps + alert_sns **31 passed / 1 xfailed**; test_rollout 8; test_mutants 50; 7 new I mutants killed, 5 existing still killed; ruff/bash -n clean. Wiring (operator/coordinator): IAM `sns:Publish` on the topic for the box instance role; create the topic + e-mail subscription; the box's system python3 needs boto3 (`apt install python3-boto3`) or SNS delivery is BLOCKED; observe.sh comment cosmetic. One-lens Opus recheck dispatched.
- 19:12Z: **M6-WIRING** handback `codex/m6-wiring` **38876865** (code 2ae944f5; base 4959e10c; evidence `research/plan/evidence/m/M6-wiring-2ae944f.md`, update `updates/M6-20260925T1910Z.json`): settings PROCESSING_CACHE_MAX_BYTES (60 GiB), RETENTION_INTERVAL_S / CACHE_SWEEP_INTERVAL_S / JOURNAL_EXPIRE_INTERVAL_S (300 s) as P-25 placeholders (TO BE VERIFIED), 0 refused, in preflight's TUNABLE list — **env schema id 1:c947948d1106e4fc → 1:55563b285260ad78**; worker compose (additive M6-WIRING blocks): PgLifecycle = media.content, ProcessingCache with max_bytes + the worker Registry, one shared PgStreamStore, `WorkerService(housekeeping=)` starts exactly one retention / cache-keeper / journal-expire task and cancels them after the drain; `expire_journal` loops `expire()` until 0; no gateway module composes any of it; `VllmEngine(pin=, reprepare=)` pins every video input from `local_uri` on an ExitStack released in `_generate`'s finally, NotFound → re-prepare once (prepared_by_job dropped) then the not_found refusal; the 12 families declared exactly as `pending.families`, `retention.record()` from `run(metrics=)` (a raising pass extends the abort streak), ProcessingCache(metrics=) evicted/refused; dashboard pending → rows, pending gone, pending_producers empty; **F-4** d10 case: one prune pass removes chunks past JOURNAL_CHUNK_TTL_S and keeps the newer job's; add-on: X6/X7/X11/X17/X19 pinned by five cache-bounds cases, R5 documented as a ponytail ceiling on `store.Recent`. Counts (m6): tests/w + tests/m **814 passed / 36 skipped / 2 xfailed**; tests/i observe 18/1 xfailed (2 failed before the row move — the oracle); recovery OB-10 16; retention mutants **62/62** (one `dies_by` declaration fixed); worker_main mutants 27/27 service-free (14 new; PG list skipped); prep_worker mutants 73 / 8 skipped; 4 new I mutants killed; tests/i packaging/envcheck/prereqs 102; validate_plan PASS. Wiring: **WR-M6W-1** contracts DEPLOYMENT_EXPECTED pin (+ the four settings; 271 passed with it), WR-M6W-2 the merge lane unions worker/__main__.py with W5 (keep both mutant anchors), WR-M6W-3 E3C's final run starts `python -m infrx.worker`, WR-M6W-4 optional. Open: the /tokenize call sends the just-put file unpinned (a high-water eviction in that window fails the attempt retryably — severity to be judged by the verifier); WR-7's strict xfail stays until 0022 merges. **Full 3-lens verification (verify-lane workflow) dispatched**.
- 19:15Z: **PROFILE-CREDIT-CAP recheck** (Opus, one lens) = **ACCEPT** with one low pre-existing finding sent to the lane's fix round: **PCC-V1** json.load admits Infinity/NaN and the schema's minimum lets them through, so `max_spend: Infinity` admits any schedule and NaN crashes instead of refusing (the base float code had the same Infinity hole and silently admitted NaN) → refuse non-finite amounts. Verified: 8 files only; bench.py/certify.py byte-identical; no reader of the removed field; Decimal(str(json)) throughout with no float in the cap path, exact comparisons (48,660.48 admitted at cap 50,000 and at 48,660.48, refused at 48,660.47; 67.594 admitted / 67.5939 refused; USD 12.16512 under both namings, refused at 12.16511; 0.1+0.2 vs 0.3 exact; 1e-6 exact; n=1e30 no overflow); CREDIT with legacy keys, mixed naming, missing cap, legacy keys with null rates all refused; the deprecation warning names key paths only; fails-before 5; bench-test 110; mutants 112: 109 killed / 3 controls; certify + test_run 96; validate_plan PASS. Infos: PCC-V2 default Decimal precision 28 (a contrived 1e30-request case rounds holds away) — optional prec=100; PCC-V3 >15–17 significant-digit literals are rounded by json before Decimal (documented ponytail limit; realistic values round-trip).
- 19:18Z: **W5-ADMIT-WIRING** handback `codex/w5-merge` **f18c72f7** (code f5784d0c; e1bcc662 = plain merge of the integration tip c037a8f8, one Makefile union; evidence `research/plan/evidence/w/W5-admit-wiring-f5784d0.md`, update `updates/W5-20260925T1916Z.json`): Relay gains `readiness` (ReadinessStore) and `expectation` (AdmissionExpectation); with a readiness store `Relay.admit` calls `admit_ready(prepared, idem, expectation)` for text and media in both regimes, refusals pass through as lifecycle DomainErrors with no job/hold, replay semantics unchanged (R91 lookup first; a None readiness is a pre-W5 job); `Relay.__post_init__` refuses an expectation of the wrong regime or card; pilot.py composes the one PgLifecycle as lifecycle + readiness whenever it builds the PgJobStore, and `pilot.admission_readiness` builds the expectation from ACCOUNTING_REGIME + ACTIVE_RATE_CARD_VERSION, raising RuntimeMisconfigured in every mode for a PG store without readiness, a CREDIT runtime without a card, or a non-ReadinessStore — **no silent jobs.admit fallback on PostgreSQL**; the old door remains only for fake-only compositions (ponytail-marked). Counts (w5 + MinIO 55497): the 8 `_pg__` W cases 5 failed pre-wiring → **8 passed** wired; tests/w **308 passed / 3 skipped** with all 7 MinIO round trips; the three W mutant lists **145 passed** (no broken_runner, no survivors); tests/g + m + contracts **2,550 passed / 27 skipped** without any deselect (the G8 straddle passes); G mutants 397 + the re-anchored `given_stores_replaced` killed, 5 new W5 G mutants killed; composition cases 57 passed / 1 failed (the pre-existing legacy_usd price_source case, fixed by 0022); validate_plan PASS. Open: **merge-order hazard** — on the lane's tree `admit_ready` refuses URL-fetched video (materialize wrote no content row) → M6 phase 2 (already merged on the tip at 25826ab9) must be under it, which the merge lane guarantees; a PG video admit_ready → worker round trip is still uncovered (W5 note 6); the `_admitted` CREDIT recheck is redundant except for `_resume`. Proposed ruling (the R132 gateway sentence) in the evidence. **Full 3-lens verification (verify-lane workflow) dispatched** on a scratch merge with the integration tip.
- 19:20Z: **PROFILE-CREDIT-CAP fix round** done at **bf7b0040**: PCC-V1 fixed (non-finite numbers refused by schema_errors and by spend_projection before the comparison; new test: Infinity/NaN cap, NaN holds, Infinity rate at n=1e9, and an Infinity cap through `bench --validate-only` — fails on dbc3aeef, passes after; mutant e1cp24 killed); PCC-V2 applied (projection under `decimal.localcontext(prec=100)`). Counts: test_profile 20; bench-test 111; E1C mutants 113: 110 killed / 3 controls; certify + test_run 96; validate_plan PASS. Targeted recheck dispatched to the same verifier.
- 19:23Z: **ALERT-SNS recheck** (Opus, one lens) = **ACCEPT_WITH_FIXES** → fix round dispatched: **F1** (medium, pre-existing but it contradicts the "URL never printed" claim): a malformed webhook URL escapes `post()` as `http.client.InvalidURL` (a ValueError) and the traceback prints URL fragments to stderr/journald (probes with a token: "URL can't contain control characters. '/SECRETTOK x'", "nonnumeric port: 'SECRETTOK'"), exit 1 with no UNDELIVERED line → catch ValueError, keep the alert (exit 4), non-https → BLOCKED through send(); F2 (low) publish() catches only botocore errors → catch Exception, keep the alert; F3 (low) a response without ResponseMetadata counts as success → default −1; F4 (low, pre-existing) ALERT_OWNER/ALERT_ESCALATION unvalidated in 72 → refuse control characters. Verified: 10 files; send() BLOCKED for both/neither/malformed ARN incl. newline injection, alert kept, neither ARN nor URL printed; state advanced only on success (no duplicate tickets; existing mutants still killed); region from the ARN; Subject ≤100 ASCII with the [TEST FIRING] marker and nonce surviving truncation; 72 refuses before any aws call or write, 0600 via the unchanged write_env; 74 BLOCKED without the env file; the runbook carries the scoped IAM statement and "sns=200 is not delivery"; 31 passed / 1 xfailed; test_rollout 8; the 7 new + 2 existing mutants killed with unique anchors; test_mutants 50 (third run alone — the shared scratchpad is contended; two earlier runs collided with the verifier's own file swap).
- 19:29Z: **PROFILE-CREDIT-CAP recheck** (one-lens Opus, bf7b0040 vs dbc3aeef): fixed=true. PCC-V1 closed (Infinity/NaN cap, holds and rates refuse with `must be finite`, projected None, no traceback; schema `$: must be finite`); PCC-V2 closed (n=1e30 contrived case refused at prec=100; every earlier exact admit/refuse value unchanged). Counts: test_profile 20 (fails-before 19/1 with the old runprofile.py), bench-test 111, E1C mutants 113: 110 killed / 3 controls, certify+test_run 96, validate_plan PASS. New **PCC-V5** (low, fail-closed, non-blocking): `schema_errors` calls `math.isfinite` on ints too, so an integer literal above ~1.8e308 raises OverflowError (bench prints its sanitized `bench failed: OverflowError (message withheld)`, exit 1; at dbc3aeef the same profile validated). Fix is `isinstance(value, float) and not math.isfinite(value)`; assigned to the E4C-PREP lane (below), the lane's one fix round being spent. Cosmetic: the projection's rate message says `bounds.spend.input_per_mtok` (unreachable through validate()).
- 19:30Z: **PROFILE-CREDIT-CAP merged**: `git merge --no-ff codex/profile-credit-cap` → **93fdab4e** (8 files, +327/−31; brings `updates/E1C-PROFILE-20260925T1910Z.json` and `evidence/e/E1C-credit-cap-54b3733.md`). **R132 numbered** in 08 §10 (run-profile spend amounts in the profile's currency, compared exactly; legacy `*_usd` USD-only; USD never read as CREDIT) — merge order puts it before W5, whose draft rows move to R133–R136 (scratchpad draft renumbered). Checks from a fixed `git archive` copy (`scratchpad/wave4b/fixed-93fdab4e`, log `wave4b/logs/checks-93fdab4e.log`): bench-test 111 passed; certify+test_run 95 passed / 1 failed — `test_the_report_names_its_tree_its_namespace_and_each_stages_duration` reads `git rev-parse HEAD`, which an archive copy cannot answer (`git_head.sha` None); rerun in a `git clone --shared` at 93fdab4e: 1 passed. So archive copies serve mutants and unit suites; anything that reads the tree identity needs a detached worktree or clone. E1C mutants and validate_plan pending in the same log.
- 19:36Z: **E4C-PREP dispatched** (Opus micro-lane, `codex/e4c-prep`, worktree `.claude/worktrees/codex-e4c-prep`, base **56c284cf**; no services). Scope, all decision-bound: (1) P-24 base profile `models/marlin2b/profiles/E4C-box.base.json` in CREDIT (cap 50,000; rates 400/1,200 per 1M tokens from the P-01 card; 3,600 requests, 14,700 s, 14 GB, 30,720 input tokens; FILL identities kept; no key inventory — that is the operator's P-02 dry-run); (2) P-18 limits into E4B-protocol §5 + certify `CRITERIA` (`e2e_p95_s_per_clip_minute` 90.0, new `latency_p95_s` 9.0 judged in the envelope cell, recovery bounds as §5 text, public-edge burst sentence), tests first; (3) P-17 acceptance checklist appended to the E4C brief (03 §E4C); (4) PCC-V5 one-liner + regression + mutant. Handback = evidence `research/plan/evidence/e/E4C-prep-<head7>.md`; the E4C lane proper stays blocked on BACKEND-LOCAL (E3C) and needs the operator-held items (card publication, `adjust +40,000 CREDIT`, scoped key issue, hosted rehearsal). Dependency check at 19:36Z: unblocked and running — D10-FOLLOWUP / M6-WIRING / W5-ADMIT-WIRING verifications (3 workflows), ALERT-SNS fix round, E4C-PREP, fixed-copy api-mutants on ef390372; blocked — W5 merge lane (on the three verdicts), door-revoke 0023 + G8 set_feature_flag call sites (on 0022 merge), E3C final rerun (on W5+F-3+F-4 SHA), E4C (on BACKEND-LOCAL + operator items), App tasks (on BACKEND-READY).
- 19:40Z: **ALERT-SNS fix-round handback** `codex/alert-sns` **37cebafa** (code head 49c92e8d; evidence `I8-alert-sns-0bbf336.md` "Fix round" section; update `updates/I8-ALERT-20260925T1932Z.json`). Lane reports: F1 fixed — `post()` builds and opens the Request inside one try, anything but HTTPError (InvalidURL/ValueError, URLError, OSError) returns −1 → exit 4 with the alert kept and no traceback; a non-https webhook is BLOCKED through `send()` (exit 3, alert kept) instead of SystemExit; new subprocess test under `python -X dev` asserts the token and host are absent from stdout+stderr and UNDELIVERED is written for the three malformed forms. F2 fixed — `publish()` catches any Exception, prints only `sns=<Type> topic=<name>`, exit 4, alert kept (botocore import removed). F3 fixed — HTTPStatusCode default −1 read inside the try; a non-dict answer is a failure. F4 fixed — step 72 refuses any control character in ALERT_OWNER/ALERT_ESCALATION with exit 2 before any write. Lane counts: observe+ops_steps+alert_sns 32 passed / 1 xfailed (the 3 new cases red before the fix), test_rollout 8, tests/i test_mutants 50, 3 new mutants (`webhook_value_error_escapes`, `sns_missing_metadata_is_success`, `observe_install_owner_multiline`), 13/13 rerun killed, ruff clean. Wiring requests unchanged and operator-held: IAM `sns:Publish`, topic creation + subscription confirmation, python3-boto3 on the box. Targeted one-lens recheck dispatched (same Opus recheck agent, fresh shared clone at 37cebafa).
- 19:52Z: **Merge-plan rehearsal** (trial merges in a shared clone `scratchpad/wave4b/trial-union`, branch `trial` from 778dea63, nothing pushed): `codex/w5-merge` f18c72f7 onto the tip — clean; `codex/m6-wiring` 38876865 onto that — one conflict, `apps/infrx-api/infrx/worker/service.py:109-113`, both branches add a field to the worker service dataclass at the same spot (W5: `reconciliation: object | None = None`; M6: `housekeeping: dict = field(default_factory=dict)`) — the union keeps both lines; `codex/d10-followup` 0d577c4e onto the tip alone — clean. Plan once the three verdicts are in: one Opus union lane (`codex/wave4b-union`, base = the tip) merges D10-FOLLOWUP (+ its wiring patch W1–W4, W2 = F2C fake WR-7 rule + regenerated acceptance fixtures together) → M6-WIRING (+ WR-M6W-1: tests/contracts DEPLOYMENT_EXPECTED + 4 settings) → W5-ADMIT-WIRING (service.py union), runs the affected suites from that fixed branch, hands back a SHA; the coordinator merges it `--no-ff`, numbers R133–R136 (W5) and the D10-followup §6 rulings, flips W5 → implemented, then messages E3C-RERUN with the SHA. E1C mutants on the 93fdab4e archive copy rerun with full output (`wave4b/logs/e1c-mutants-93fdab4e.json`, PID 3807553) after the first run's log kept only its last three lines.
- 19:47Z (clock correction: the entries above stamped 19:40Z and 19:52Z were written at ≈19:33Z and ≈19:36Z; the stamps were estimated, not read): **ALERT-SNS recheck ACCEPT** (one-lens Opus, fresh shared clone at 37cebafa, probes replayed with dummy tokens under `python3 -X dev`): F1 fixed — five malformed https forms exit 4 (`http=-1`), `http://`/`ftp://` exit 3 BLOCKED with the value unprinted; 0 token/host/traceback hits, exactly 1 UNDELIVERED line, no state file each time; the https gate now sits in `send()` so `--test` is covered too. F2 fixed — ClientError/BotoCoreError/ValueError/RuntimeError from `publish()` and from `boto3.client()` all exit 4, print only `sns=<Type> topic=<name>`, marker never appears. F3 fixed — `{}`, None, str answers and HTTPStatusCode 500 all fail closed. F4 fixed — 8 control-character cases in ALERT_OWNER/ALERT_ESCALATION exit 2 with no env file and zero aws/systemctl calls; valid values still produce the 3-line 0600 env file. Regression review of 0bbf336..37cebafa: the new except paths print only `type(e).__name__`; the only exit-0 path is an explicit 2xx; secret-pattern grep of the diff clean. New **ASN-V1** (low, non-blocking, unreachable with real botocore): an `HTTPStatusCode` that is present but not an int (None, "200") raises TypeError in `main()` with a traceback (code lines only), exit 1, UNDELIVERED not written, state not advanced (so the next cycle retries); fix `int(...)` inside the existing try — carried to the next lane that touches `infra/observe/deliver.py`. Counts: focused observe+ops_steps+alert_sns 32 passed / 1 xfailed (first try hit `/tmp/infrx-i8-pooler` lock contention from a concurrent run; clean on retry), test_rollout 8, tests/i test_mutants 50, 14/14 named mutants killed, ruff clean, `bash -n` 72/74 clean. Note for lanes: `tests/i/mutants.py` takes positional names, there is no `--only`.
- 19:47Z: **ALERT-SNS merged**: `git merge --no-ff codex/alert-sns` → **c1b74038** (brings `evidence/i/I8-alert-sns-0bbf336.md`, `updates/I8-ALERT-20260925T1932Z.json`). Post-merge checks from a shared clone at c1b74038 (`scratchpad/wave4b/clone-c1b74038`, log `wave4b/logs/checks-c1b74038.log`, PID 4015670; the i8 compose block is free now that the lane and its recheck are done): rollout, ruff, `bash -n`, focused tests/i, tests/i mutants — results appended when the run ends. Still operator-held for the alert path: SNS topic + subscription confirmation (or the Slack webhook SSM parameter), IAM `sns:Publish` on the box role, python3-boto3 on the box.
- 19:58Z: **ALERT-SNS post-merge checks** at c1b74038 (shared clone): test_rollout 8 passed; ruff (via `uv run --frozen --no-sync ruff`; the script's `.venv/bin/ruff` path does not exist) clean; `bash -n` 72/74 clean; focused tests/i observe+ops_steps+alert_sns 32 passed / 1 xfailed on the i8 block; tests/i test_mutants result appended below when the run ends. **E1C mutants at 93fdab4e** (archive copy, full output): 113 mutants, 110 killed, 3 controls survived, 0 not killed, no problems.
- 19:58Z: **E4C-PREP handback** `codex/e4c-prep` **1acf85b4** (impl f30b281b; commits 1193a7bb PCC-V5 · e83572ed P-18 + P-24 profile · f30b281b P-17 · 1acf85b4 evidence `research/plan/evidence/e/E4C-prep-f30b281.md`; no update file). Lane reports: (1) `models/marlin2b/profiles/E4C-box.base.json` sha256 c0d4aa1b…d2ff, CREDIT, cap 50,000, holds 0, rates 400/1,200, 64 corpus ids (c012/c025/c038/c051 expected_invalid), FILL identities kept, no key inventory (a separate `--key-inventory` file from the P-02 dry-run; the profile carries `test_key_ids` ["142c7d81"]); validate-only on the base exits 2 with only the 5 FILL identity errors; filled + stamped at 0.25 req/s × 3,600 it is runnable with projected 48,660.48 CREDIT (13.5168 × 3,600; 3,601 refused). (2) certify `CRITERIA`: `e2e_p95_s_per_clip_minute` 90.0, new `latency_p95_s` 9.0 judged in `rung_verdicts` next to TTFT (unknown < 60 samples); E4B-protocol §5 rows "decided (P-18, 2026-09-25)", new table (supported 0.5 req/s, soak 0.25 × 14,400, recovery 300/30/30 s, stall → 503/504 ≤ 45 s), public-edge burst rule (CW-V3), §4 row + amendment 6; recovery bounds not in CRITERIA (the recovery cell reads no number from it); soak derivation left as "half the highest passing rung" (a soak from a higher rung needs 7,200+ requests, refused by the 3,600 bound — fails closed). (3) P-17 ten-check checklist under 03 §E4C. (4) PCC-V5 fixed (`isinstance(value, float)`), message path `bounds.spend.rates.*`, mutant e1cp25 killed. Counts: bench-test 113; marlin mutants 114/111 killed/3 controls; certify+test_run 97; e4b mutants 245 (767 s; 3 new, all killed); validate_plan PASS; fails-before shown for all five new tests. Open issues raised by the lane: (a) FILL strings pass minLength-only fields (migration_version, config_version, maintenance_window, allowed_fault_targets) — proposed `validate` refusal of any "FILL…" string; (b) if rung 1.0 passes, `envelope_summary` judges P-18's limits at 1.0/s while the derived soak is refused (fails closed) — the E4C lane must choose a 0.5-only ladder or judge at the declared rate before the first qualifying run; (c) `is_local` treats 127.0.0.1:8001 as free so bench does not enforce blocks on box direct-gateway cells (certify's `profile_blocked` still requires the inventory); (d) bench prefers `MARLIN_API_KEY` when set — the box shell must export only `INFRX_API_KEY`. Proposed ruling (to number at the merge): E4C's predeclared criteria are E4B-protocol §5 as amended (amendment 6, P-18) and `certify.CRITERIA`, incl. `latency_p95_s` 9.0 and `e2e_p95_s_per_clip_minute` 90.0; loosening either after a qualifying run's start disqualifies that run. One-lens Opus recheck dispatched (fresh shared clone at 1acf85b4; replays validate-only, the projection, the P-18/P-17 numbers against the decision rows, fails-before, e4b mutants detached).
- 20:11Z: ALERT-SNS post-merge tests/i test_mutants at c1b74038: 50 passed (241 s). The i8 block is free.
- 20:11Z: **E4C-PREP recheck ACCEPT_WITH_FIXES** (one-lens Opus, shared clone at 1acf85b4). All four claims hold by replay: (1) the base profile differs from the draft §P-24 JSON only in the spend block (CREDIT keys per the amendment) and the 64 filled item ids; validate-only on the base = exactly the 5 pattern-bound FILL identity errors; filled + inventory at 0.25/s × 3,600: runnable, projected 48660.4800 CREDIT (recomputed with Decimal: 30,720 × 400/1e6 + 1,024 × 1,200/1e6 = 13.5168 per request), 3,601 refused; no USD anywhere in the spend/projection (`measurement.price.usd_per_hour` 2.24208 is the P-19 infrastructure price, used only for the USD infrastructure-cost block — not a conversion). (2) `latency_p95_s` 9.0 judged between the TTFT and e2e rows (9.0 passes, 9.01 fails, unknown at 59 samples); run3 raw rows replayed under the new CRITERIA: rung 0.5 latency 7.6688 / e2e 77.3142 / TTFT 2.4461 all pass; every §5 number equals the P-18 row; nothing dropped (no recovery key ever existed; `e4b.b.recovery` reads nothing from CRITERIA). (3) all ten P-17 checks present with the closing rule; three paraphrase drifts (item 7 lost the P-25 known-good definition; item 5 lost "passed"; item 4 lost "start timestamp"). (4) PCC-V5 fixed, e1cp25 killed. Counts reproduced: bench-test 113; marlin mutants 114/111/3; certify+test_run 97; e4b mutants 245 (691 s); validate_plan PASS; fails-before confirmed for all five new/updated tests. Findings, none blocking: **E4P-V1** (medium) — with rung 1.0 passing (run3 was one in-cap refusal away), `envelope_summary` reports supported 1.0 and derives a 7,200-request soak the P-24 bound refuses (fails closed, but a good run would FAIL on the derivation); **E4P-V2** (medium) — the overload cell validates as P1 through the direct gateway, so nothing enforces P-18's public-edge burst; **E4P-V3** (low) — FILL strings pass the minLength-only fields (migration_version, config_version, allowed_fault_targets, maintenance_window); **E4P-V4** (low) — checklist drifts above; **E4P-V5** (low) — the runner's `< 1 %` over all unanswered attempts is stricter than P-18's ≤ 1 % platform rate (note only); **E4P-V6** (low) — the 300/30/30/45 s recovery bounds are judged by a person reading drill times, no code carries them. Lane open issue 3 upgraded to medium: `is_local` treats 127.0.0.1:8001 as free, so bench does not apply blocks on box direct-gateway cells (certify mitigates via the required inventory and the over-cap error; manual bench runs are not covered); issue 4 confirmed as an operator trap that fails closed.
- 20:11Z: **E4C-PREP fix round dispatched** (the lane's one round) with coordinator decisions: V1 — `CRITERIA["declared_rate_per_s"]` 0.5 (box), supported rate = the declared rate when its rung passes (higher rungs measured, never supported), `MATRIX["box"]["soak"]` fixed at 0.25 req/s (3,600 requests), P-18 rows judged at the declared rung, §5 text; V2 — certify stamps the overload cell `profile_class` P4 (bench refuses it on a direct-gateway profile), optional `--overload-profile <path>`, otherwise the box overload cell is BLOCKED, never silently P1; V3 — `validate` refuses any string starting with "FILL", by path; issue 3 — loopback is local (blocks not applied) only for direct-engine or when no profile is given, a direct-gateway target with a profile is metered; V4 — checklist item 7 + P-25 known-good definition, "passed" and "start timestamp" restored; V5/V6 — §5 notes (stricter failure-rate rule; the drill record prints each measured recovery time next to its bound with PASS/FAIL); issue 4 — runbook line (box shell exports only INFRX_API_KEY). Mutants for V1/V2/V3/issue 3. Targeted recheck after the handback, then merge; the lane's proposed ruling (E4C's predeclared criteria = §5 as amended by amendment 6 + `certify.CRITERIA`; loosening after a qualifying run's start disqualifies it) is numbered at the merge.
- 20:32Z: **Speed-up (user instruction "run at full scale … the review process is too slow"):** integration now overlaps verification instead of following it. Verification status read from the workflow journals: D10-FOLLOWUP lenses done (ACCEPT_WITH_FIXES 4 / ACCEPT_WITH_FIXES 3 / REJECT 5; blocking D10F-1 = the generation half of `fail_preparation`'s replay key has no test, a mutant survives; majors: WR-8 retirement rationale cites M6 generation keys that phase 2 removed, W2+W3+W4 must merge together) → fix round running; M6-WIRING lenses done (2 / REJECT 8 / 7; blocking M6W-C1 pin release untested on failure/cancel/aclose paths, M6W-C2 housekeeping loop surviving a failed step untested; majors C3 pin leak when the upstream close raises, C4 reprepare job id unchecked, C5 multi-file pinning untested, C6 housekeeping cancel-after-drain untested) → fix round running; W5-ADMIT-WIRING one lens done (ACCEPT_WITH_FIXES 8; major W5W-A1 = the marker commits before the relay's attach, so a video job can be claimed while M's prepare has no source), two lenses running. Dispatched in parallel: (1) `contracts/tasklocal.py` reserves `union` (PostgreSQL 55458, Valkey 55454, S3 55455) and `revoke` (55459) — decoys 55498/55499 clear of the live 55491–55497 ports; 255 contracts port tests + test_pgharness 7 pass; commit **e607b705** pushed. (2) **WAVE4B-UNION** lane (Opus, `codex/wave4b-union`, base e607b705): merges d10-followup 0d577c4e + wiring W1–W4, m6-wiring 38876865 + WR-M6W-1, w5-merge f18c72f7 with the service.py union, then door-revoke if present; checks per step; full api-test at the end. The fix-round commits land on the same lane branches and are merged into the union as deltas. (3) **DOOR-REVOKE** as a self-verifying workflow `wf_c628a416-603` (implement 0023 + exact runtime function set with a caller audit across base/w5-merge/m6-wiring → 2 lenses → one fix round → recheck), worktree `codex-door-revoke`, INFRX_D_TASK=revoke. (4) **Rollout gates rehearsal** (Opus, read-only clone at e607b705): runs every LOCAL gate of `infra/rollout/README.md` G1–G6 and `bash -n` on the window steps, lists the REMOTE ones with their inputs (SSM names only). (5) **E3C-RERUN pre-staged**: told to merge e607b705, rebuild the e3c compose images, run preflight only, and hold the final-matrix command until the union SHA arrives. Queued behind the union handback: **P-25-ENACT** (deployed values: retention grace 3,600 s, cache high water 50 GiB = 53,687,091,200 B — note WR-M6W-1 pins a 60 GiB default (64,424,509,440), so the deployment must override it — intervals 300 s; the DeploymentSettings names only exist after M6-WIRING) and the E4C runbook lane (after the E4C-PREP fix round). Review policy from here: new lanes carry their own verification in one workflow (implement → lenses → fix → recheck) so handbacks arrive verified; merge lanes are checked by suites, not adversarial review; the one-fix-round rule stands.
- 20:34Z: **E4C-PREP fix-round handback** `codex/e4c-prep` **b67d4b6c** (impl fea6a58a; "Fix round" section in `E4C-prep-f30b281.md`). Lane reports all seven items fixed: V1 — `CRITERIA["declared_rate_per_s"]` 0.5; `envelope_summary(rungs, declared)` stops the box climb at the declared rate, judges only that rung's P-18 rows, adds `measured_passing_rate_per_s`; box soak fixed `{rate 0.25, seconds 14400}` and runs only once the declared rung is supported (else FAIL "no supported envelope rate to soak at", not run); tiny scale unchanged; `rate_fraction` removed; replay of a rung-1.0-passing ladder: before = supported 2.0 and a 14,400-request soak bench refused, after = supported 0.5 and a 3,600-request soak bench admits (48,660.48 CREDIT). V2 — overload cell stamped P4; new `--overload-profile` (public-edge profile, burst at `https://<its first allowlist host>/v1`); without it the box overload cell is PENDING on PROFILE with "BLOCKED: … pass --overload-profile"; mutant `p4_stamp_dropped` killed. V3 — `runprofile.fill_paths` + `validate` refuse any "FILL…" string by path (base now 15 errors = 5 pattern + 10 FILL; fully filled runnable); mutant e1cp26 killed. Issue 3 — `is_local(a, profile=None)`: loopback local only with no profile or direct-engine; bench/dataset pass the profile; unpriced or inventory-less loopback profiles refused; mutant e1cp27 killed. V4 — checklist items 4/5/7 restored (+ 85-known-good-box.sh, P-25). V5/V6 — §5 notes. Issue 4 — runbook line (INFRX_API_KEY only). Counts: bench-test 114; marlin mutants 116/113/3; certify+test_run 99; e4b mutants 252 (689 s; 7 new + `soak_at_the_full_rate` retargeted, all killed); validate_plan PASS; fails-before on 1acf85b4: test_profile 2 failed, test_certify 5 failed (KeyError `declared_rate_per_s`/`rate`, overload PASS instead of BLOCKED). Base profile sha unchanged. Beyond certify/runprofile/tests: one line each in `models/marlin2b/bench.py` and `dataset.py` (pass the profile to `is_local`), `tests/integration/backend/e4b_mutants.py`. Targeted one-lens recheck dispatched (same Opus recheck agent); in parallel a trial merge of b67d4b6c onto the tip 197efba8 in a shared clone is clean (no conflicts) and its suites run detached (`scratchpad/wave4b/logs/checks-e4cprep-trial.log`, PID 560662) so the merge is immediate on an ACCEPT.
- 20:41Z: **E3C-RERUN pre-staged** (`codex/e3c-rerun` **5fadfdbe**, nothing pushed): `git merge --no-ff e607b705` clean (07995f39); `tests/integration/backend/e3c` 35 passed; images pinned by digest, warm run `INFRX_E3C_RUNTIME_LOGIN=1 runner.py --pull --only s12 --control nc-retention-durable=<tree>` 20:38–20:40Z: preflight/services/migrate (0001–0021)/s12 self-test 35/35 PASS, teardown PASS, 0 infrx-e3c containers left. First end-to-end use of `--control`: nc-retention-durable passed for the right reason (on the reverted tree s06-live fails "a collector deleted a live job's media", s06-dark passes, s06 scrub fails because this head has no M6 wiring yet). Harness defect found and fixed in the lane's paths: a control tree's harness copy read this checkout's containers as another checkout's and would have skipped its scenario instead of judging it — `runner.run_env` now sets `INFRX_E2_CHECKOUT`/`INFRX_E2_STATE_FILE` for a control tree (as `mutants.py` does); regression `test_s12_a_revert_control_tree_claims_this_checkouts_stack`. New `tests/integration/backend/e3c/control_trees.sh <sha> <outdir> [nc…]` builds both control trees from `git archive` (no ref/index/worktree touched; a patch that fails to apply stops it): nc-admission-ready reverse-applies the readiness-barrier commits newest first (f5784d0c, d58139f3, fa446ae7, 16ff5771, 245dcb75, 53dc95ae) and checks nothing in the gateway calls admit_ready (all six reverse-apply cleanly on a scratch preview of union+w5-merge+m6-wiring); nc-retention-durable adds a scratch-only last migration that makes `infrx.content_referenced` say nothing is referenced (0020 cannot be reverted whole). s06-scrub and the s11 scrub race are judged on the composed worker (M6's `python -m infrx.worker` retention loop + journal prune, `RETENTION_INTERVAL_S`/`CACHE_SWEEP_INTERVAL_S`/`JOURNAL_EXPIRE_INTERVAL_S` = 2, wait ≤ 60 s) — F-4 is judged on the composed worker, not a harness-side prune; s06-live/s06-dark keep fresh collector processes (RV-03). Prepared `scratchpad/final-run.sh <UNION_SHA>`: merge, `uv sync --frozen --all-extras`, build control trees from the merged HEAD, `INFRX_E3C_RUNTIME_LOGIN=1 E3C_OUT=… make backend-local E3C_ARGS="--control nc-admission-ready=… --control nc-retention-durable=…"` detached. Wall-clock ≈ 20 min (matrix 12–13 min + controls ≈ 6 min + setup). Plan: the union lane's first SHA (pre-fix-round) goes to E3C at once for an early full run; the final run repeats on the SHA that includes the D10-FOLLOWUP/M6-WIRING/W5 fix-round deltas.
- 20:51Z: **E4C-PREP recheck ACCEPT** (one-lens Opus, shared clone at b67d4b6c, independent replays with a fake client): V1 — declared 0.5, ladder 0.5+1.0 passing → supported 0.5 / measured 1.0, soak argv `--rate 0.25 --requests 3600` admitted at 48,660.48 CREDIT; only the declared rung's P-18 rows are judged (synthetic verdict matrix), declared-rung failure → envelope FAIL, soak FAIL "no supported envelope rate to soak at" with no client call; run3 raw rungs → PASS 0.5; tiny local soak unchanged (2.0 × 10). V2 — P4 stamp on the overload cell: direct-gateway base refused by bench, box run without `--overload-profile` → PENDING owners ['PROFILE'] BLOCKED with no client call, public-edge profile → burst at `https://<edge>/v1` PASS on honest refusals; mutants `p4_stamp_dropped`, `box_burst_unblocked`, `burst_off_the_edge` killed. V3 — 15 errors on the base (5 pattern + 10 FILL paths), 5 remaining with only the pattern fields filled, fully filled runnable; e1cp26 killed; profile sha unchanged. Issue 3 — unpriced or inventory-less direct-gateway loopback profiles refused; direct-engine loopback stays local; e1cp27 killed. V4/V5/V6/issue 4 present; amendment 6 appended, nothing rewritten. Counts: bench-test 114; marlin mutants 116/113/3; certify+test_run 99; e4b mutants 252 (933 s; list 243 → 250, none removed, `soak_at_the_full_rate` re-anchored); validate_plan PASS; fails-before 7 on 1acf85b4; no USD/conversion in the diff. New low, non-blocking, carried to the E4C runbook lane: **E4P-V7** `--overload-profile` pointing at a profile without a `target` block crashes `load_cells` with KeyError (no false PASS, no report cell) — validate the edge profile's `target.allowlist`; **E4P-V8** a remote `tiny`-scale target with a direct-gateway profile and no edge profile FAILs the overload cell ("bench client exited 2") instead of BLOCKED — fails closed; **E4P-V9** an unprofiled bench/dataset run on the box still treats 127.0.0.1:8001 as free (by design of the fix-round spec) — runbook rule: never run bench or dataset on the box without `--profile`.
- 20:51Z: **E4C-PREP merged**: `git merge --no-ff codex/e4c-prep` → **dafd4030**. Pre-run checks on the identical trial merge (clone `scratchpad/wave4b/trial-e4cprep`, log `wave4b/logs/checks-e4cprep-trial.log`): bench-test 114, certify+test_run 99, marlin mutants 116/113/3/0, validate_plan PASS (932 links / 218 docs); e4b mutants (252) taken from the recheck's run at the same content. **R133 numbered** (E4C's predeclared criteria and the no-loosening rule; P-18); merge order moves W5's draft rows to R134–R137.
- 20:52Z: **M6-WIRING verified — ACCEPT_WITH_FIXES**, fix-round head **28dd1af2** (code 3ed20d23; workflow wf_03853dc8-51c, 10 agents, 98 min). Lenses: rules 2 minor; correctness REJECT (C1 blocking: no oracle for pin release on failure/cancel/aclose paths — a success-only release mutant survived 137 tests; C2 blocking: the housekeeping `every()` loop dying on a failed step had no test, two mutants survived; C3 major: pin leaks when the upstream stream's close raises, reproduced; C4 major: reprepare job id unchecked; C5 major: only one file:// input pinned in tests; C6 major: housekeeping cancel-after-drain untested; C7/C8 minor); acceptance 7 minor. Fix round (the lane's one): C1 `test_worker_main__every_exit_path_releases_every_pin`; C2 `…a_housekeeping_loop_outlives_a_failed_step` (raise once, ≥ 3 runs); C3 `try: await inner.aclose() finally: stream.pins.close(); self._retire(key)` + backstop in `EngineStream.aclose`; C4 fakes record (job_id, profile) and assert the lease's id; C5 two-clip pinned world; C6 the OWNER case wraps `loop.drain` and asserts housekeeping alive during the drain, gone after stop(); counts 821/36/2. All six rechecks observed fixed in scratch `git archive` copies (worktree untouched). Minor findings (C7 expired-eviction counter, C8, F1–F7, RULES-1/2) are carried in the lane's evidence for a later M6 pass — not blocking the union. **D10-FOLLOWUP fix round** landed at **c584f54a** (a0db994c: the `fail_preparation` generation half now asserted — generation+1 after the end → `already_terminal`, two new mutants; 9d4bd28f: 0022 header comment rewritten to M6 phase 2's real mechanism (tombstone + amended R114), sha256 re-recorded; c584f54a: evidence/update say W2+W3+W4 are one atomic merge set) — rechecks: D10F-2 and ACC-1 fixed, D10F-1 and ACC-2 pending. W5-ADMIT-WIRING: two lenses done (major W5W-A1 + 9 minor), the third running. The union lane was told to merge both deltas after its planned steps and to re-run the step checks.
- 20:56Z: **Rollout gates rehearsal at e607b705** (Opus, read-only shared clone; no box/AWS/hosted/secrets; nothing committed). G1 PASS (a symlinked `.venv` shows as untracked because `.gitignore`'s `dir/` patterns match directories only — use `make api-env` or `.git/info/exclude`). G2 BLOCKED for api-test/api-mutants: the e1c block (55449) is held by the coordinator's own `INFRX_D_TASK=e1c make api-mutants` on ef390372 (pid 2665631, > 2 h 20 min at the time) and the full suite also needs q3/d2 Valkey and the i8 pooler stack; parts run without service ports: tests/i minus the 8 i8_stack cases 219 passed / 1 xfailed, console-test 351, console-lint 0 errors / 2 warnings, console-typecheck OK, bench-test 111, console-mutants 212/212 + 104/104. G3 PASS (image build 23 s; in-image `preflight.py probe --mode pilot` ok:true — the README's "false by design" is stale; `build -q` prints the config digest while `image inspect .Id` (what install.sh records) is the manifest id — same image). G4 PASS (`engine_problems` [] ; serve.sh pins vllm/vllm-openai@sha256:4cbfd34a…). **G5 FAIL — harness defect, not runtime**: `rehearse.sh` 47 PASS / 1 FAIL; the worker `infrx_build_info` check reads a body `http()` truncates to 300 chars (rehearse.sh:296; the worker body is 1,689 chars since the `infrx_db_pool_*` families) and evals it inside single quotes, which the apostrophe in the db_pool HELP text breaks; with the full body fetched the check PASSES (the worker serves `infrx_build_info{process="worker",revision="e607b705…",image="sha256:5930c298…"} 1.0`). G6 NOT RUN (REMOTE: SSM names `/model-inference/pg_journal_url` (created 2026-09-24T01:03Z per rollout.md — README's "DSN decided" is stale), `supabase_url`, `supabase_service_role_key`, `marlin2b_api_key`, `/INFRX-SUPABASE-PROD/db_password`, `/model-inference/operator_key`; W6 dump/restore check; G6B scoped-key issue/revoke). Window steps: every step script passes `bash -n`; 20-prepull's sed extracts the digest-pinned engine image; the release bundle builds offline (17,494,325 B, round trip + `git bundle verify` OK); 30-pause's archived deploy dir is self-contained; migrate.py's offline plan from 0018 = 0019/0020/0021 with digest e5bb62bc…, in-image plan refuses without a DSN and does not echo an unreachable one; known-good.py: at --applied 0018 bda1586/4226315 KNOWN-GOOD, at --applied 0021 all three NOT ("applied beyond the tree: 0019–0021 with no schema_proof"); e607b705 at 0021 fails only the record check (expected until it has served); at e607b705 every file the O-steps need exists (absent at bda1586/4226315). **Problems**: HIGH — once the window applies 0019+ there is no qualifying rollback target (R2/O12 would run old code on an unproven schema): add `schema_proof {"through": …}` entries for bda1586/4226315 by running their store tests on the new schema, or record R3 maintenance as the only fallback → queued as KNOWN-GOOD-PROOF after the union merge (needs the final migration set 0022/0023). MEDIUM — G5 harness bug above; **`infra/rollout/ssm.sh --help` (or any first argument `cat` accepts) sends `cat`'s help text as a script to the pilot instance** (no help path, step file existence unchecked — a hazard; never call ssm.sh with a non-file first argument until fixed); rollout.md §1 INSTALL_ARGS carries `DATABASE_POOL_MAX_SIZE=6` only as a comment (at defaults `pool_budget.py` FAILs: peak 21 + headroom 2 > 15 session slots) and README step 8 omits `ENGINE_MAX_NUM_SEQS` (50-install defaults to 32, §1 pins 8). LOW — 45-s3-check pins pytest 8.4.2 while uv.lock is at 9.1.1 (GHSA-6w46-j5rx-g56g); steps 71/72/74/80/81/86 read repo files absent at every known-good checkout (fail closed after an R2 revert — run from the release archive or state the precondition); stale README statements (RELEASE "on claude/backend-impl", G3, G6); leftover Created container `pensive_galois` from an I8 rehearsal (removed by the coordinator now); `.gitignore` symlink note. Operator inputs listed by the rehearsal (SSM names only, no values) are carried into the rollout-window checklist. **ROLLOUT-FIXES** self-verifying lane dispatched for the tooling items (G5 harness, ssm.sh guard, INSTALL_ARGS/README/50-install pin, pytest re-pin, step preconditions, README refresh).
- 20:58Z: **D10-FOLLOWUP verified — ACCEPT_WITH_FIXES**, head **c584f54a** (code 9d4bd28f; workflow wf_7803a93b-8b9, 8 agents, 108 min; `open` = []). Lenses: rules 4 minor (RI-1: on its own the branch turns `test_the_versioned_acceptance_transcripts_replay_exactly` red — exactly and only the W2 dependency, disclosed); acceptance 2 major + 1 minor; correctness REJECT (D10F-1 blocking: the generation half of `fail_preparation`'s replay key had no oracle — a mutant dropping `'generation', v_lease->'generation'` from `v_mark` survived on real PG; D10F-2 major: the 0022 header's WR-8 retirement cited M6 `generation_key` (`<key>.g<n>`), which M6 phase 2 (d81154dc) removed — writers and the collector use bare keys, so one of the three safety legs was false and the residual late-landing-DELETE window was undocumented; D10F-3/4/5 minor: replay row lock (`for update`) unenforced by a test, WR-7 same-digest / guard `new.state='live'` clauses without oracles (safe direction), G8's PG suite does not catch a weaker flag lock — only D10's checks do). Fix round: a0db994c adds the assertion `fail(conn, dict(lease, generation=lease['generation']+1), cause)[0] == 'already_terminal'` + mutants `d10_fail_prep_replay_ignores_generation` / `…_ignores_worker` (all 11 fail_prep mutants killed; full lists `INFRX_MUTANTS=all` 734 passed / 0 survivors); 9d4bd28f rewrites the WR-8 paragraph (comment only) to M6 phase 2's real mechanism — the tombstone refuses re-registration (`content_retiring`) until the delete is acknowledged, the ack checks only the current claim fence after the store's delete returned, deletes are issued only with a request timeout left, keys are bare — and names the residual explicitly (a delete the client abandoned can land after a later acknowledged delete and a re-registration's write); R129 already accepts that residual with generation-keyed names as the upgrade, so no new ruling; **0022 sha256 now 1f44b4ae89506dc75cb1d9cbf26208935906058833a18fa518518085aea5e706** (278 lines), 0001–0021 unchanged; c584f54a: evidence §4/§5 + `updates/D10-20260925T2050Z.json` say W2+W3+W4 are one atomic merge set and W1 is needed for V-G8TL-2 (scratch merge e481584b + 9d4bd28f: without the patch test_harness 1 failed / 26, lifecycle suites 2 failed / 108; with the whole patch test_harness 27, retention + lifecycle + followup + composition_pg + g8 transition_pg 117, contracts 1260). All four rechecks observed fixed in scratch copies (worktree clean). Minor findings carried in the lane's evidence for a later D pass (row-lock two-connection check; null-digest re-registration case; G8's `changed is True` tightening into W1 — the union lane may apply that one-line tightening if the transition test allows). Two of three union inputs are now verified (D10-FOLLOWUP c584f54a, M6-WIRING 28dd1af2); W5-ADMIT-WIRING's third lens is still running.
- 21:06Z: **Dependency check (user request):** no manifest task is newly unblocked — E4C's start dependency E3C is `planned` until BACKEND-LOCAL passes on the union SHA; U4 and every App task carry `dispatch_after_gate: BACKEND-READY`. Unblocked coordinator-level work dispatched as self-verifying workflows: **E4C-RUNBOOK** (`codex/e4c-runbook`, base b0cc5090: the two-tenant journey profile and the P4 public-edge overload profile bases, the E4C runbook + evidence template, E4P-V7/V8 fixes, the V9/issue-4 operator rules) and **KNOWN-GOOD-PROOF** (`codex/known-good-proof`, base b0cc5090, d10 block: `schema_proof` entries for bda1586/4226315 against the candidate schema 0001–0022 (+0023 when it exists) in the format `known-good.py` reads, or an honest BLOCKED with the R3-only fallback). Queued: P-25-ENACT on the union head. Tracker overlay → rev 52 (head b0cc5090; lane notes for E4C/I8/D10/M6/W5/E3C; W5 estimate refreshed); republish to follow.
- 21:08Z: **User decision (binding, recorded): App completion tasks run now, in parallel, ahead of the BACKEND-READY gate** ("you can run App completion tasks in parallel as well … so we can deploy the v1 platform"). This overrides program 22's dispatch rule (`dispatch_after_gate: BACKEND-READY`, brief 04 "Dispatch feature work after accepted E4C") for dispatch only: implemented App code still does not accept APP-LOCAL/APP-PILOT, whose gates (E3A, E4) keep their evidence requirements, and the backend gates (E3C BACKEND-LOCAL, E4C BACKEND-READY) are unchanged. App task graph from the manifest: wave 1 (start dependencies satisfied) **C0, U1R, A2, A3**; wave 2 **C3A** (after C0), **U4** (after U1R), **U2, U3** (after U1R and C3A); wave 3 E3A (after E3C), I2A, E4 (gates). Dispatched as one self-verifying workflow (each lane: implement → 2 adversarial lenses → one fix round → targeted recheck; wave-2 lanes branch from the tip and merge their predecessor lane branches first). Worktrees `codex-app-{c0,u1r,a2,a3,c3a,u2,u3,u4}` on `codex/app-<id>` from the tip; task-local PostgreSQL reserved in `contracts/tasklocal.py` for the real-RLS lanes (app-c0 55451, app-c3a 55452, app-u3 55453, app-u4 55456, app-u1r 55457, app-a2 55460; A3/U2 fakes only). Merge order onto `claude/consumer-v1` as verdicts arrive: C0 → U1R → A2/A3 → C3A → U2/U3/U4, each with console-test/lint/typecheck/mutants from a fixed clone.
- 21:10Z: **App wave dispatched** as workflow `wf_77b93903-38d` (8 lanes, base 46776646; wave 1 C0/U1R/A2/A3 in parallel, wave 2 C3A after C0, U4 after U1R, U2/U3 after U1R + C3A; each lane implement → 2 lenses → one fix round → recheck; handbacks write `updates/<ID>-<ts>.json` with status review). Also running: WAVE4B-UNION, DOOR-REVOKE wf_c628a416-603, ROLLOUT-FIXES wf_07b7b9ae-ded, E4C-RUNBOOK and KNOWN-GOOD-PROOF (dispatch pending in this window), W5-ADMIT-WIRING verification wf_6310ff0e-5d3, E3C-RERUN pre-staged, api-mutants on ef390372.
- 21:13Z: **Manifest amendment for the user's App decision**: the eight dispatched App tasks (C0, U1R, A2, A3, C3A, U4, U2, U3) carry `dispatch_after_gate_original: BACKEND-READY` + `dispatch_override {by: user, at: 2026-09-25}` and a closure_amendment line; E3A/I2A/I3/E4 keep the gate (their substance is App delivery against the accepted backend). `validate_plan.py` accepts a recorded user override for the dispatch rule (acceptance unchanged); ledger regenerated; `progress.py check` PASS with the eight App lanes registered (rev 53; the tracker's "overlapping writers" warnings are the manifest's shared `tests/a/` and `tests/u/` paths — each lane adds its own files there).
- 21:15Z: **Tracker republished**: https://claude.ai/artifact/QfcUvbhNRAUsvGcQeYMR3Q version 3 = overlay rev 53 (head b0cc5090 at render; App lanes C0/U1R/A2/A3 running, C3A/U4/U2/U3 queued; E4C-PREP merged; ETA BACKEND-LOCAL forecast 2026-09-26 00:49–09:55Z likely 03:25Z on the W5 → E3C path). Read in full before publishing (254 lines, 546,451 bytes; lines 201/215/217 via cut).
- 21:17Z: **Correction:** the 21:0xZ dependency-check entry said E4C-RUNBOOK and KNOWN-GOOD-PROOF were dispatched; only their worktrees existed then. Both are running now as self-verifying workflows: **E4C-RUNBOOK** `wf_f174b010-a8c` (`codex/e4c-runbook`: P4 public-edge overload profile base, two-tenant journey profile base, E4P-V7/V8 certify fixes with mutants, `models/marlin2b/results/E4C-runbook.md` with the window steps, FILL sources, operator-held items, drill record format and the P-17 tick-off) and **KNOWN-GOOD-PROOF** `wf_57e4423b-04c` (`codex/known-good-proof`, d10 block: proof driver + `schema_proof` entries for bda1586/4226315 on the candidate schema through 0022 (+0023 when present), honest R3-only fallback text if a target cannot serve on the new schema). **api-mutants on ef390372** (archive copy, INFRX_D_TASK=e1c, 2 h 46 min): 3,480 passed / 7 failed / 30 skipped — the 7 failures are all `broken_runner` in the tests/i mutant list (`probe_timeout_identity_blind`, `probe_identity_reads_its_own_timeout`, `drift_credit_charge_unchecked`, `drift_usd_only`, `drift_ignores_wallet_drift`, …): `test_privilege_probe.py` / `test_rollback_drill.py` cases erroring outside the named cases because the e1c block has no pooler stack (those cases need `INFRX_D_TASK=i8`, where the same list passed 50/50 at c1b74038 today). Every other list green on ef390372. Lesson recorded: the full `make api-mutants` needs the i8 block for tests/i (or the tests/i list is run separately on i8); the final-head gate run will use a per-list block map.
- 21:42Z: **ROLLOUT-FIXES verified — ACCEPT** (workflow wf_07b7b9ae-ded, 3 agents, 41 min; head 63057975, code 956d8c29; evidence `research/plan/evidence/i/ROLLOUT-FIXES-956d8c2.md`, update `updates/ROLLOUT-FIXES-20260925T2125Z.json`). Delivered: G5 — `rehearse.sh`'s worker `infrx_build_info` check fetches the whole /metrics body and compares it as a variable (no eval of a response body): rehearsal 47/1 before → 48 PASS after (namespace infrx-rollout-fixes, aws stubbed to exit 97, teardown clean); `ssm.sh` — `-h/--help` exits 0, a non-file step exits 2 with the usage, both before any aws call, `cat -- "$step"`; test + mutant `ssm_sends_a_non_step`; install pins — rollout.md §1 INSTALL_ARGS carries `DATABASE_POOL_MAX_SIZE=6` inside INFRX_SET, 50-install requires `ENGINE_MAX_NUM_SEQS` (`:?`, the file's convention) instead of defaulting to 32, README step 8 points at INSTALL_ARGS with `ENGINE_MAX_NUM_SEQS=8`; test: INSTALL_ARGS pass `pool_budget.py` at peak 13 (peak 21 > 15 before); 45-s3-check pytest re-pinned 9.1.1 with uv.lock's hash (the other four pins already matched), test parses the heredoc against uv.lock; steps 71/72/74/80/86 (+81's fetch path; MODE=undo stays available) get a fail-closed guard "BLOCKED: this step needs an I8+ checkout (missing <path>)" exit 3 before any action, six mutants killed; README refresh (RELEASE on claude/consumer-v1, G3 sentence removed, G6 pg_journal_url dated, rollback table: no qualifying target after 0019+ until a schema_proof exists → R3 fallback, G1 symlink note, verification-log entry). Counts (lane + lens reruns): test_rollout + test_ops_steps 20; tests/i minus mutant runs 183 / 1 xfailed; tests/i test_mutants 50; named mutants 12/12; every mutant on a changed file 39/39; bash -n 10 scripts; ssm.sh probes (--help/-h 0 calls; nonexistent/directory/bad pair exit 2 with 0 calls; good call 1 send-command with the exact payload); validate_plan PASS. Minor findings carried: RF-C1 the ssm.sh header's usage example (`50-install.sh RELEASE=<sha>`) now contradicts the required MIGRATION_DIGEST/ENGINE_MAX_NUM_SEQS; other rehearse.sh checks still eval short bodies (no apostrophes today). **Merged** `git merge --no-ff codex/rollout-fixes` → **a1e06236**; post-merge checks from a shared clone (rollout+ops_steps, bash -n, named mutants on the i8 block, validate_plan) running in the background (task br4xhqw7k).
- 21:43Z: ROLLOUT-FIXES post-merge checks at a1e06236 (shared clone): test_rollout + test_ops_steps 20 passed; bash -n on ssm.sh, every step and rehearse.sh clean; named mutants ssm_sends_a_non_step / cutover_drops_engine_concurrency / ssm_drops_arguments 3/3 killed on the i8 block; validate_plan PASS (932 links / 219 docs). Pushed.
- 21:44Z: **Progress poll.** Union lane `codex/wave4b-union` is at **9d61d1e1** (d10-followup delta c584f54a → 0e26f095; m6-wiring 28dd1af2 → 1bf18139 + WR-M6W-1 21694661; w5-merge f18c72f7 → 9d61d1e1 with the service.py field union) and is running its check suites. **E3C-RERUN told to run the EARLY full matrix on 9d61d1e1** (final-run.sh; ~20 min; not the final verdict — W5-ADMIT-WIRING's fix-round delta follows). W5-ADMIT-WIRING verification: three lenses done, fix round running. DOOR-REVOKE: implemented, two lenses running. App wave: A2 implemented → lenses done → fix round running; A3/C0/U1R still implementing. E4C-RUNBOOK: implemented, lenses running. KNOWN-GOOD-PROOF: implementing.
- 22:25Z: **E3C early full run on union 9d61d1e1** (merged as 43178b58 on `codex/e3c-rerun`; `INFRX_E3C_RUNTIME_LOGIN=1 make backend-local` with both revert controls; 25:35 wall at host load 44–64): **gate FAIL, 5 failed / 76 passed**; s01/s02/s03/s05 (9/9, now green)/s07/s08/s09/s10/s12 (34/34)/s13 PASS; s04 2/4, s06 2/3, s11 1/3. F-2 re-measured within bound (PostgreSQL 20.1 s, S3 40.1 s ≤ 42 s). Controls: nc-retention-durable PASS for the right reason; nc-admission-ready PASS for the WRONG reason (reverting W5 wiring 1 whole dropped the `PgLifecycle` import M6-WIRING now shares → the tree's worker died with a NameError). **Six harness defects fixed on the lane (9107a5c6; e3c unit tests 37):** H-1 a revert-type control whose tree cannot start is INVALID[harness], not a detection; H-2 control cases write to `<out>/<control>/cases` (they had overwritten the main run's s04/s06 evidence); H-3 `control_trees.sh` strips only the worker's `readiness=PgLifecycle(...)` argument (asserted once), refuses a tree still composing the barrier, fails on any undefined name (ruff F821); H-4 s04 "permanent refusal" re-pointed to W5's permanent class (`context_length_exceeded`; a deleted staged object is `not_found` = temporary); H-5 the scrub oracle is per content row (0020 keeps the request record until `settled_at + retention_s` ≈ 604,800 s; the result body and journal are gone at +4,000 s by the worker's own housekeeping → **F-4 closed**); H-6 s11 live reconcile uses 3 s per delta and a job that settled before the wallet check is INVALID, not FAIL. Validation run `--only s04,s06,s11` + controls on 9107a5c6: s04 3/4, s06 3/3, s11 3/3; both controls PASS for the right reasons (readiness tree: "executed before durable eligibility: preparation 1, prepared 1, inference 1, debits 1"; retention tree: live media deleted, request record deleted 600,799 s early). **F-3 closed** on the union. **Projected gate on the union with the fixed harness: FAIL on F-5 alone.** New **F-5** (product, blocking): after `admit_ready` the relay still runs the legacy capability recheck (`relay.py:216` → `_admitted` 243–300); when it refuses, the client gets a 4xx for a job that already ran and was charged (request f61b82d3: prepared 22:17:58, `unsupported_media` answered 22:18:06, job `succeeded`). New **F-6** (least privilege, non-gate): `PgReconciliation` is composed on the worker's `infrx_runtime` pool but reads `infrx.wallet_reconciliation` / `credit_wallet_reconciliation`, granted to `infrx_monitor` only (0021:550) → InsufficientPrivilege every tick (39/46 worker logs), gauges never published. Evidence `research/plan/evidence/e/E3C-early-9107a5c.md` (branch codex/e3c-rerun, head c7c9d518; the coordinator merges the harness with the final run), update `updates/E3C-20260925T2230Z.json`. **W5-F5 dispatched** as a self-verifying workflow `wf_5e483fbc-a92` (`codex/w5-f5` from the union head 9d61d1e1, e2c block): F-5 — skip the post-admission recheck on the admit_ready path if R110's admission transaction covers every refusal it can raise (prove by table), else answer the committed outcome, never a refusal or a second charge; F-6 — compose the reconciliation reader on the I8 monitor login when configured (optional setting, DEPLOYMENT_EXPECTED), else publish nothing and log once; no grant to `infrx_runtime`. The union lane merges the W5-ADMIT-WIRING fix-round delta and `codex/w5-f5` when they land; E3C's final run follows on that SHA.
- 22:41Z: **DOOR-REVOKE verified — ACCEPT_WITH_FIXES**, head **273990a0** (impl acc53094; workflow wf_c628a416-603, 7 agents, 130 min; `open` = []). Delivered: `apps/app/supabase/migrations/0023_runtime_unmarked_door_revoke.sql` revokes execute on `infrx.admit(jsonb)` and `infrx.claim_preparation(jsonb)` from `infrx_runtime` (service_role keeps both; house header with ORDER/ROLLBACK, re-runnable); the runtime function set is 43 before (0021's 42 + 0022's `fail_preparation`) and **41 after**, pinned by name as `RUNTIME_FUNCTIONS` in `tests/d/checks_reads.py`, cross-checked by script against the new I8 allow-list (`DOOR-REVOKE-runtime-functions.txt`) and the layer-3 map; caller audit: on `codex/w5-merge` the PostgreSQL runtime never calls the two doors (relay → `readiness.admit_ready`, pilot.py:334 refuses a PgJobStore without a ReadinessStore, preparation runner claims via `claim_preparation_ready`, worker/__main__ always wires it); on the base and `codex/m6-wiring` they are still called (relay.py:167-168, preparation.py:214) → **merge gate: 0023 only with/after W5** (satisfied on the union since 9d61d1e1; rollback to a pre-W5 runtime needs the 0023 rollback grant first). Tests on the runtime login itself: the two doors refused by name (42501), `admit_ready` / `claim_preparation_ready` / `fail_preparation` still run, service_role still runs the revoked two, `readiness_cutover_check()` reports `runtime_unmarked_doors = []`; fails-before shown with 0023 absent. tests/d at 1d0a418d 837 passed / 9 failed (the composition dedicated-login case fails on the branch alone by design — pre-W5 relay; the lifecycle transcript needs W2; 7 outbox_relay[valkey] HarnessBusy from a concurrent lane, 20 passed on rerun); on a temporary union merge test_composition_pg + test_reads + test_ready + test_schema_postgres + tests/g/test_composition 101 passed / 3 xfailed; I8 privilege_probe with the new list exit 0 (63 checks), fails on exactly the two revoked functions with the old list; validate_plan PASS; the layer-3 880-case suite not run (e2 namespace ports). Lenses: correctness ACCEPT (DR-CM-1 major = the merge gate, DR-CM-2/3 minor), rules ACCEPT_WITH_FIXES (DR-1 major: `tests/integration/mutants.py` e2m75 anchor moves with 0023 — fixed as wiring patch **v2**; DR-2 major = the merge gate; DR-3 minor). Rechecks all observed. Wiring: **W-DR1 v2** `research/plan/evidence/d/DOOR-REVOKE-wiring-v2.patch` (pgstate RUNTIME_FUNCTIONS 41, test_harness 0023 pin, mutants.py e2m75 re-anchor; after D10F's W4); W-DR2 I8/operator probe with the new allow-list (adding `infrx.admit` to MUST_DENY optional). Union lane told to merge 273990a0 + W-DR1 v2 now.
- 22:42Z: **E4C-RUNBOOK verified — ACCEPT_WITH_FIXES**, head **7b6ab239** (impl e46079ea; workflow wf_f174b010-a8c, 10 agents, 84 min; `open` = []). Delivered: `models/marlin2b/profiles/E4C-edge.overload.base.json` (public-edge, allowlist marlin2b.callbill.ai from the Caddyfile, class P4, open-loop rate 1000 × 32 requests with --burst 32, CREDIT bounds 13.5168 per request → 432.5376 for the burst under a 433 cap, input bytes ≤ 210,000,000 for the 206,105,764-byte corpus; validate-only: 13 FILL errors as committed, runnable filled, accepted by certify's `--overload-profile` with the burst going to https://marlin2b.callbill.ai/v1); `E4C-box.two-tenant.base.json` (the schema already carries multi-tenant `tenant_key_env` lists; keys INFRX_API_KEY/INFRX_API_KEY_B, public-edge P0, closed-loop concurrency 2; after the fix round 24 requests over forms upload/video_b64/video_url/text (3 of each per tenant), max output 3,072, cap 299 CREDIT for projected 298.5984; a one-tenant run and an undeclared active key are refused); certify **E4P-V7** (an overload profile without a target / non-object target / empty allowlist → overload cell PENDING on PROFILE with a BLOCKED message, no KeyError) and **E4P-V8** (any remote target without `--overload-profile` is BLOCKED); mutants `remote_burst_unblocked_off_the_box`, `edge_host_unchecked`, `edge_cap_below_the_burst` + re-anchored `box_burst_unblocked` killed; `models/marlin2b/results/E4C-runbook.md` (pre-window steps with every operator-held item marked, the window inside rollout.md W1–W13, the freeze table per FILL field, fill/validate-only commands, the certify invocation with a per-cell table, the two-tenant journey legs, the drill record format, evidence layout, P-17 tick-off). Lenses (both ACCEPT_WITH_FIXES): majors RB-1 the key-inventory order made every certify cell refuse (tenant-2 prefix outside `test_key_ids`) → **key phases split**: certify runs on an inventory whose only active prefix is 142c7d81 (`keys-certify.json`, a fresh dry-run after 0.5/0.6), the tenant-2 key is issued after certify, the journey gets `keys-journey.json`; RB-2 the §3 box validate-only line exited 2 (rate 0.5 vs 0.25) → `--rate 0.5 --requests 3600` with a note that certify stamps 0.5/1.0/2.0 and 0.25 per cell copy; RB-3 the journey lacked video_url/text legs, a sync + idempotent-replay leg and a foreign-call leg (404 on the other tenant's job) → added, with `--cancel-fraction 0.4` seed 20260922 cancelling seqs 4/7/8/18 (≥ 1 per tenant) asserted against the rebuilt schedule; minors (migration_version source, both P-06 digests, LOAD-CLOSEDLOOP/MARLIN-SOP mapping in check 1, the 10,000 balance assumption, P-25 steps, the P-18 commit check) fixed in the runbook. All six rechecks observed (the workflow passed a mistyped full SHA; the verifiers resolved the real head 7b6ab239ed42). Counts: bench-test 115, certify+test_run 102, marlin mutants 116/113/3, e4b mutants `INFRX_MUTANTS=all` **255 passed (19:27)** at the impl head, validate_plan PASS, E4C-box.base.json sha unchanged. Open items carried into the runbook: `weights_sha256` defined as sha256 of the compact JSON list `serving-version.json` `model.weight_shard_digests` (ratify at the freeze); the burst's box→edge→box path unmeasured (box-side /health 200 precondition); `/etc/marlin2b-gateway.env` must carry no MARLIN_API_KEY name (names-only pre-check). **Merged** `git merge --no-ff codex/e4c-runbook` → **2f2ca075**; post-merge checks from a shared clone (bench-test, certify+run, marlin mutants, validate_plan, full e4b mutants) detached (`wave4b/logs/checks-e4crb.log`, PID 2937480). **WR-E4C-RB-1 applied** by the coordinator: the "Window runbook:" line appended after the box-shell rule in 03 §E4C's checklist.
- 22:46Z: **App wave, wave 1 verdicts** (workflow wf_77b93903-38d journal; each lane implement → 2 lenses → one fix round → rechecks): **C0** fix head **ab0b5179** — lenses ACCEPT_WITH_FIXES ×2 (majors: `consumerSession()` untested for an auth outage/config failure mapped to signed_out → fixed with tests + two reviewer mutants killed; the composed layout decision → `consoleShell(session, isOperator, routes)` in console.ts, fixed; C0-V2 the real-RLS cross-tenant tests skip in `pnpm test`/`make check` → coordinator wiring WR-4 `console-c0-real` Makefile target (INFRX_D_TASK=app-c0, Supabase image, 11/11); C0-V3 production reads still fixture-backed on the pages → WR-1 (revised, full layout.tsx) + U1R/U4's pages); wiring WR-1/3/4 coordinator, WR-5 D10 SQL (paginated `public.consumer_credit_ledger`), WR-6 session.ts (A2/U3), WR-7 rerun after 0022. **U1R** fix head **7482c21a** — lens 0 REJECT (blocking U1R-V-01 the CREDIT production/preview gate had no test → `creditSource(real, env)` pure function + test, fixed; V-02 "Spent" scans the whole ledger → D10 WR-3(b) partial index `credit_ledger_wallet_credits_in_idx` with an EXPLAIN proving test), lens 1 ACCEPT_WITH_FIXES (`actor` per-row `visible_principal()` removed from the ledger select, fixed; the sidebar's legacy USD "$" labelled Balance → WR-1+WR-2 layout/sidebar patch + `sidebarCredits()` lane side, composed check provided); wiring WR-3 D10, WR-4 U4 route path, WR-5 A3/U3 format helpers, WR-6 C0 read port swap. **A2** fix head **cd4a688e** — lens 0 REJECT (two blocking: the /welcome balance read and `emailSettled` untested → `welcomeWallet(read)` with unavailable/not_issued states, A2-ENUM-04 rate-limit/enumeration cases; majors: no browser tests → request-level flow tests (no new tooling), the grant RPC call untested → `CLAIM_RPC`/`claimArgs` exported and `grant-pg.test.ts`), lens 1 ACCEPT_WITH_FIXES (first-login grant claim `afterSignIn(claim, next)`, sign-in continues to `next` only for a replayed grant); all six rechecked; wiring WR-A2-1 middleware public paths (`/signup`, `/verify-email`; required for public signup), WR-A2-2 Makefile console-mutants runner (23/23), WR-A2-3 low; blocked for hosted onboarding on P-05 (confirm-email ON, custom SMTP, rate limits, redirect allowlist, templates, `signup_grant` flag, `SUPABASE_SERVICE_ROLE_KEY` in the App server env) — operator/coordinator items. **A3** fix head **7b0ea93f** — lens 0 REJECT (blocking A3-V1 no test of what the pages render — a provisional card could show as approved → `content.test.ts` renders provisional note/unit labels/limits/retention/revocation/disclosure/grant), lens 1 ACCEPT_WITH_FIXES (runner file collision with A2 → renamed `run-catalog-mutants.mjs` / `catalog-mutants.json`); both rechecked; wiring WR-1 `tests/g/test_app_examples.py` (15 recorded calls replayed against the mounted routes; two negative controls fail as required), WR-2 Makefile line, WR-3 Vercel `INFRX_API_BASE_URL` (operator), WR-4 G7 `PROVISIONAL = True` constant → derive from the card's approval. C3A and U4 are implementing; U2/U3 wait for C3A. Dispatched: **APP-UNION** lane (Opus, `codex/app-union` from ca226130): merges C0 → U1R → A2 → A3 and applies the coordinator-owned wirings exactly from the evidence files (layout.tsx, contracts promotion, `console-c0-real`, sidebar patch + composed check, middleware public paths, both Makefile mutant-runner lines, `tests/g/test_app_examples.py`), runs the console suites + `console-c0-real` + api tests, reports merge-tree against the backend union. **D10-APP-SQL** self-verifying workflow (`codex/d10-app-sql` from door-revoke 273990a0, revoke block): migration **0024** = credits-in partial index (U1R WR-3(b)), `public.consumer_credit_ledger(p_after, p_limit)` SECURITY DEFINER keyset read (C0 WR-5), additive `consumer_jobs` filters (U1R WR-3(a)); ownership/cap/plan tests + mutants. **G7-PROVISIONAL** micro-lane queued next (A3 WR-4).
- 22:48Z: **G7-PROVISIONAL** dispatched as self-verifying workflow `wf_7c5673a9-1a2` (`codex/g7-provisional` from ca226130, g7 block): A3 WR-4 — publish `pricing.credit.provisional` from the card's approval record (fail closed when unreadable) instead of the `PROVISIONAL = True` constant; tests in test_catalog_truth + a G mutant.
- 23:13Z: **W5-ADMIT-WIRING verified — ACCEPT_WITH_FIXES**, head **50d85530** (code fba75f6e, tests only; workflow wf_6310ff0e-5d3, 7 agents, 234 min; `open` = []). Lenses: acceptance ACCEPT_WITH_FIXES (major W5W-A1 the marker opens the barrier before the relay's attach lands; 7 minor), rules ACCEPT_WITH_FIXES (2 minor), correctness ACCEPT_WITH_FIXES (majors W5AW-1 same hazard, reproduced only in the in-memory World; W5AW-2 on the lane tree alone URL-fetched video is refused 404 — merge only with/after 25826ab9 (M6 phase 2), zero conflicts with the tip; minors: the CREDIT-without-card fail-closed path claimed but unasserted, no legacy-with-card test, the `adapters_from_env` readiness-only-beside-PgJobStore rule untested, the R132 gateway sentence not matching the implementation). Fix round: **the race does not exist on PostgreSQL** — 0019 `admit_ready` calls `bind_source` (job_media rows) in the same transaction as the marker, `MediaPreparation.attached` reads those rows, the relay's later attach is a write-once no-op; the in-memory World is fixed (`tests/g/relay_support.py` ManifestAttachments) and a PG window case runs the worker in that window and prepares; the PG video round trip (upload + data: fetched, through admit_ready and the `python -m infrx.worker` process) committed as `tests/g/test_relay_readiness_pg.py::test_w5_pg__the_worker_process_runs_a_video_admit_ready_admitted` (the fetched shape on the lane tree alone is a strict xfail until the merge); the tip merge itself was denied to the lane by the permission system (a lane may not merge shared refs — correct) and is the union lane's. Counts (reran by the verifiers): test_relay_readiness 20 / 2 xfailed, the PG file 2 / 2 xfailed, tests/w 308 / 3 skipped, W mutant lists 145 (a first run at host load 80+ had 4 non-reproducing failures), G mutants as recorded. Both rechecks observed on scratch clones (lane head and lane + 9e986016). **Draft ruling correction (W5AW-6)** appended to the scratchpad draft: the R134 gateway sentence now states the composition-root expectation (regime; CREDIT exactly `ACTIVE_RATE_CARD_VERSION`, R69), the four fail-closed start refusals in every mode, refusals answered as lifecycle-table errors with no job/hold/outbox row, `jobs.admit/admit_credit` only over the contract fakes and never writing a marker, and media prepared from the refs the marker names. Minor findings (fail-closed assertions, legacy-with-card case, `adapters_from_env` composition test) carried for a later W pass. Union lane told to merge the 50d85530 delta after door-revoke.
- 23:13Z: E4C-RUNBOOK post-merge checks at 2f2ca075 (shared clone): bench-test 116 passed; certify + test_run 102 passed; marlin mutants 116 / 113 killed / 3 controls / 0 not killed; validate_plan PASS; e4b mutants `INFRX_MUTANTS=all`: 255 passed in 1162.98s (0:19:22). All green.
- 23:26Z: **KNOWN-GOOD-PROOF verified — ACCEPT_WITH_FIXES**, head **f5293057** (impl 913cfa9e; workflow wf_57e4423b-04c, 7 agents, 128 min; `open` = []; evidence `research/plan/evidence/i/KNOWN-GOOD-PROOF-aab4b41.md`, update `updates/KNOWN-GOOD-PROOF-20260925T2214Z.json`). **Both known-good targets (bda1586, 4226315) are proven through 0023**: each target's own tests/d runs from a `git archive` copy with the candidate's migration directory in place of its own (the old harness has no reuse-a-database mode, so its template is built from 0001–0023) — 26/26 suites PASS (387 passed, 10 cases skipped by name, 5 xfailed), exercising the old admit/prepare/claim/lease/settle (both regimes)/journal/outbox (fake, memory, Valkey)/operations/signup/catalog/PG gateway-composition SQL on the new catalog, plus a probe walking admit → claim → complete → get_owned → read_result. The 10 old cases that fail on the new schema are named with reasons in the driver's SHAPE list (6 list the old catalog/grants/EXECUTE surface or re-apply an old migration; 1 UPDATEs media_uploads against 0019's check constraint — the old runtime never writes that table; 1 validates terminalize's raw output strictly and trips on 0020's `result_expires_at` — the runtime reads only `_OUTCOME_FIELDS`; 2 read a result before an outcome, which 0020 refuses with `result_pending` — the runtime reads only committed outcomes, the probe proves it). Not proven, stated in the README/record: anything past 0023 (`known-good.py` NOT-KNOWN-GOOD at `--applied 0024`); the old release on the `infrx_runtime` login (0023 revokes its doors on purpose; the old releases predate that login — a revert uses the target's own env file, which R2 restores); hosted rows written before the window; the Supabase image (plain PostgreSQL + shim). `known-good.py` for both targets: 0018 KNOWN-GOOD → KNOWN-GOOD; 0022/0023 NOT → KNOWN-GOOD; 0024 NOT (exit 1); the rollback.md step-1 `--list --applied 0023` gives bda1586/4226315 KNOWN-GOOD, 27af05a NOT. Deliverables: `infra/runbooks/schema_proof.py <sha> [--candidate] [--suite] [--task]` (PASS/FAIL/SKIP per suite, exits 0/1/2, full log per suite; optional `SCHEMA_PROOF_DSN` read-only history check), `tests/i/test_known_good_proof.py` (3 cases), mutants `known_good_proof_ignores_through`, `known_good_record_unproven`, `schema_proof_trusts_moved_statements`, `known-good.json` `schema_proof {through 0023, evidence, files: sha256 of 0019–0023}` on both entries, README §2 rollback paragraph + log line. Fix round (all three majors rechecked): a skipped or zero-passed suite is FAIL (fake-suite case); the CLI-split history check requires whole statements verbatim, in order, covering the file; the record binds the proven 0019–0023 file hashes (known-good.py refuses a checkout whose migrations after the target's tree differ). Counts: tests/i on d10 minus the 6 i8-block cases 224 passed / 1 xfailed; named mutants 4/4; validate_plan PASS. Lane-rule lapse recorded by the lane: early runs used D2's Valkey (55463) through the old vkstore default before the driver was fixed to the d10 Valkey (55469); no d10 containers remain. Coordinator follow-up: after window step 6, the read-only hosted history check `SCHEMA_PROOF_DSN=<hosted> schema_proof.py <sha> --suite tests/d/test_adapter_units.py`. **Merge path:** the branch carries the 0022/0023 merges (byte-identical to door-revoke), so it lands through the union lane (told: merge the tip 1a14c2b8 first, then f5293057; README conflict = keep both).
- 23:27Z: **APP-UNION handback** `codex/app-union` **7ebed92d** (code 1cc03146; evidence `research/plan/evidence/coordinator/app-union-1cc0314.md`, update `updates/APP-UNION-20260925T2325Z.json`): C0 ab0b5179 → ae49d947 + WR-1 (whole layout, e47c2a2f) + WR-3 (contract types `lib/contracts/v2/consumer.ts`, 2049d615; `ConsumerClient` not promoted — it would pull the adapter into the contract) + WR-4 (`console-c0-real`, 7ce5b20a); U1R 7482c21a → f5b18aad + WR-1/WR-2 composed into C0's layout + `tests/u/sidebar-credits.test.ts` (ceb13f9c); A2 cd4a688e → f4cc45cc + WR-A2-1 middleware + `tests/a/public-routes.test.ts` (1ae23f38) + WR-A2-2 runner (a90567ed) + the C0 ROUTES follow-up (db8448a6: unverified → /verify-email, onboarding → /welcome; A2-ROUTE-03 fails on the old layout) — kept, it is what C0's evidence asked for when A2's routes ship; A3 7b0ea93f → fffab549 + WR-1 `tests/g/test_app_examples.py` (f0fe2793) + WR-2 catalog runner (1cc03146). No textual conflicts; compositions limited to re-anchored C0 mutants (CREDITS-07/11/12, C0-SES-02) and one accepted form in `tests/c/credits.test.ts`. Checks at 1cc03146: console-test 508 tests / 485 pass / 0 fail / 23 skipped (the real-database files: C0 12, U1R 6, A2 5); lint 0 errors / 2 old warnings; tsc clean; mutants contracts 212/212 (14 self-tests), V 40/40, U 97/97, C 142/142, A2 45/45, A3 catalog 46/46; `console-c0-real` 12/12 (C0's fix round added a case); `tests/g/test_app_examples.py` + tests/contracts 1267 passed (negative controls exit 1 as required); validate_plan PASS; merge-tree vs `codex/wave4b-union` 9d61d1e1 no conflicts; WR-7 data point: C0's real stack on an export of the merged tree with 0001–0022 12/12 (scratch, not the gate). Open from composing: the console layout calls `getUser()` twice per render (C0 session + U1R sidebar read) — U1R WR-6 (one shared port) closes it. Carried: C0 WR-2/WR-5/WR-6/WR-7, U1R WR-3(a)(b)(c)/WR-4/WR-5/WR-6, A2 WR-A2-3, A3 WR-3/WR-4 (G7-PROVISIONAL lane running). **Merged** `git merge --no-ff codex/app-union` → **68128815**; post-merge checks from a shared clone detached (`wave4b/logs/checks-appu.log`; console suites, mutants, `console-c0-real` on the app-c0 block, api examples + contracts, validate_plan). **App wave 2:** U4 fix head d76f912f (V-01 browser-side result lifecycle → pure drivers in request-view-model.ts; V-02 poller stop condition → `pollLoop` MAX_POLLS then onStop once; V-03 `consumer_job_result` returns the body of a held_unknown success the API withholds → **WR-U4-2** D10 SQL, sent to the D10-APP-SQL lane for 0024); C3A impl 33b88daf, fix round running (majors: actions.ts composition untested, two adapters calling `claim_signup_grant` with different metadata, an unverified individual's JWT can insert an api_keys row through PostgREST → **WR-C3A-4** D10 policy tightening, also sent to D10-APP-SQL).
- 23:35Z: **G7-PROVISIONAL verified — ACCEPT**, head **e4a3dbba** (code 0a01779a; workflow wf_7c5673a9-1a2, 3 agents, 47 min; evidence `research/plan/evidence/g/G7-provisional-0a01779.md`). `routes/models.py` drops `PROVISIONAL = True`; `provisional(card)` = `unapproved(card.approved_by) is not None` (G8's own rule from `operations/transition.py`, the one publish-card and the CREDIT transition refuse by), passed as `credit_provisional` to `published_model.project`; the contract and fixtures are unchanged. Fail closed: an absent/blank/marker-bearing approval publishes provisional true and the card stays listed (a provisional card is priced and admissible, 0007:244); an unreadable catalog stays the 503 `dependency_unavailable` (R130); no default of False anywhere. The seed card publishes true; the P-01 launch card with the 15-pending-inputs approval text publishes false (a scratch real-PG case ran G8's `publish-card --approved-by` and saw the column flip and /v1/models give false with the same 400/1,200 rates; `--approved-by 'ok p-01 later'` refused). Tests first (the approved-card case failed on the old code); mutants `provisional_flag_constant`, `provisional_flag_fails_open` + the two existing ones killed. Counts: tests/g minus ops/mutants 556; `INFRX_MUTANTS=all` test_mutants 395 (35 min); contracts published_model 98; validate_plan PASS. Minor findings carried: G7P-R1 the two record writers set `rate_card_versions.provisional` with the narrower case-sensitive `"P-01" in approved_by` while discovery uses `unapproved()` (fail-closed mismatch; D10/G8 should derive the column from the same rule); G7P-R2 discovery does not read the `provisional` column (only hand-written SQL could produce a clean-text provisional row); G7P-R3 A3's oracle comment `content.test.ts:159` is stale (text only). **Merged** `git merge --no-ff codex/g7-provisional` → **2b112a05**; post-merge checks from a shared clone detached (`wave4b/logs/checks-g7p.log`).
- 23:45Z: APP-UNION post-merge checks at 68128815 (shared clone): console-test 508 / 485 pass / 0 fail / 23 skipped; lint 0 errors; typecheck clean; console-mutants all runners green (contracts 212, V 40, U 97, C 142, A2 45, A3 catalog 46/46); `console-c0-real` exit 0 on the app-c0 block; `tests/g/test_app_examples.py` + tests/contracts 1267 passed; validate_plan PASS (938 links / 225 docs). All green. Union lane round 2 landed on `codex/wave4b-union`: door-revoke 1b8d9d91 + W-DR1 v2 a5a08519, w5-merge delta 9c4dcf9c, tip merge a51eeed9, known-good-proof **74183655** (handback pending). App wave: C3A fix round done (rechecks in), **U2 and U3 implementing**. Dependency check (user request): E3A's start dependency E3C is unmet, I3's (I2A) and E4's (E3A) too; **I2A's start dependencies (I1, F2P) are met** → the user's App-dispatch decision is extended to I2A (manifest `dispatch_override`; preparation only until the backend is accepted). Dispatching **I2A-PREP** (App hosting config, env separation, auth callback/domain configuration as code + runbook, preview-branch credential isolation tests; no live deploy) and **E3A-PREP** (coordinator lane: the browser + real-adapter App journey harness on the free e4b compose block, gate NOT claimed — E3A proper runs after BACKEND-LOCAL). E1B (start deps met, no lane) needs a box window and is not on the deployment path; deferred until the new release is on the box.
- 23:50Z: G7-PROVISIONAL post-merge checks at 2b112a05: tests/g minus ops/mutants 557 passed; contracts published_model 98; validate_plan PASS; the named G mutants: 5 passed / **1 failed — `test_every_case_is_covered_by_a_mutant`**: the APP-UNION merge (A3 WR-1) added `tests/g/test_app_examples.py::test_app_journey__every_docs_example_is_served_by_the_mounted_routes` to the G directory with no mutant covering it, so `make api-mutants` is red on the tip since 68128815 (an integration gap, not a G7 defect). Follow-up sent to the APP-UNION agent: one G mutant on `codex/g-app-examples-mutant` (from 8d9e5b5f) killed by that test (accept `tools: []` or drop the idempotency replay), proven with the every_case/well_formed checks. Dispatched (user request to start every unblocked parallel task): **I2A-PREP** `wf_e714e949-a6a` (`codex/i2a-prep` from fd40748c: environment matrix as code with preview-credential refusal, auth callback/domain allowlists, private no-store on consumer routes, release identity, `infra/app/README.md` deploy/rollback runbook; no live deploy) and **E3A-PREP** `wf_06c06a68-c32` (`codex/e3a-prep` from fd40748c, e4b compose block: the App journey runner reusing the E3C world, a pinned browser test dependency (sanctioned), journey specs with feature-detected NOT RUN cells, two break-seam controls, a verdict in the gate schema; the APP-LOCAL gate is not claimed).
- 23:55Z: Tracker overlay rev 60 (E3A lane recorded as `queued`: E3A-PREP is a coordinator preparation lane, the gate is not claimed; `progress.py check` PASS, 0 errors, 14 overlap warnings) committed 9c41d4c6 and published as artifact version 4 after the full chunked read. **W5-F5 implementation handed back** at 39f48b37 on `codex/w5-f5` (base 9d61d1e1; F-5 e592c6d0 = option (a): the post-admission recheck runs only on the pre-D10 door, with the refusal-reason table showing `admit_ready` covers every recheck refusal; F-6 3a6195b2 = reconciliation reader on `MONITOR_DATABASE_URL`, off-and-said-once when unset or refused; fails-before recorded, 9 new mutants killed); the two verification lenses are running. Routed its wiring requests: WR-W5F5-1 (monitor grant + policy on `infrx.credit_wallet_holds`) to the D10-APP-SQL lane as a 0024 addendum; WR-W5F5-2 (preflight MANIFEST `MONITOR_DATABASE_URL`) queued as union round 3 with `codex/w5-f5`; WR-W5F5-3 (fake alignment) optional, deferred. Draft ruling R138 (committed readiness is final) added to the rulings draft.
- 23:58Z: G-mutant follow-up handed back at 43c5ae89 (`docs_resume_not_replayed` on `gateway/routes/jobs.py` killed by A3 WR-1's app-examples test; the mutant copy links `apps/app` read-only so the test finds the recorded Docs calls) and merged `--no-ff` as 266f2cdc; merge checks (named G mutants every_case/well_formed/docs_resume_not_replayed, test_app_examples + the default G subset, validate_plan) running from a shared clone before the push. **P25-ENACT dispatched** (`wf_77ac74e5-386`, `codex/p25-enact` fast-forwarded to union 74183655, m6 docker block): grace 3,600 s as a deployment setting wired at the collector seam, cache cap 50 GiB (low water 0.8), 300 s intervals pinned, the operations.json cache alert at 50 GiB naming P-25, dump-cadence and known-good-record runbook sections; two lenses, one fix round. **P-25 NVMe read** (logged read-only SSM step on the box): 441,786,048,512 B size / 10,889,547,776 B used / 408,379,801,600 B available — the cap fits; 15-pending-inputs P-25 row updated (meas.).
- 2026-09-26 00:01Z: **Lane collision found and stopped.** The U3 implementer (App wave, agent ab6f814fa7d22a2ba) committed d90933c8 / a273cac3 / 9c89593c on `codex/d10-app-sql` (the WR-U4-2 and WR-C3A-4 addenda to 0024) and ran tests on the D10 lane's port 55459, outside U3's owned paths; the D10-APP-SQL implementer's full-list mutant run died when its container was replaced. U3 was told to stop writing to that branch/port and to state the commits in its handback; the D10-APP-SQL implementer (a38fc8391176ce53f) owns the three commits as inbound wiring (review, correct by new commits only), appends WR-W5F5-1, reruns the D suites and the full d10 mutant list at the final head. D10 stays the only SQL writer; the D10-APP-SQL workflow's lenses verify the whole 0024.
- 2026-09-26 00:12Z: **Correction to the collision entry above.** U3 made no commits on `codex/d10-app-sql` and ran nothing on 55459 (its handback states so; its only contact was read-only `git log/show`). The three addendum commits were made by a second instance of the D10-APP-SQL implementer itself: a `SendMessage` to an agent that is still running inside a workflow resumes a second instance from its transcript, in the same worktree and on the same port, while the workflow's original instance keeps running. The same happened to U3 (my stop message resumed a second U3 instance; the workflow's instance had already committed 34002734 / d8da867e / af5ecbad; the resumed one added the evidence statement d300d455). Rule from now on: never message a workflow's in-flight agent; wait for the workflow result, route addenda through the coordinator's own wiring or a new lane. The D10-APP-SQL implementer was told which instance continues (the workflow's) and which stops after its running pytest. U3 handback received (code head d8da867e, evidence `u/U3-d8da867.md`, WR-U3-1 = operator RPC migration for D10 as the next migration after 0024, WR-U3-2 coordinator wiring in `app/actions.ts`); its workflow lenses run next.
- 2026-09-26 00:28Z: D10-APP-SQL duplicate instance (A) stopped after reporting: it also committed WR-W5F5-1 as 6cff4f9d + 12f92a6f (tests first, then SQL) before the correction arrived; the workflow instance (B) committed the evidence/updates as 5055aae5 (branch head). 0024 sha256 at 12f92a6f `985d01ea…7d50`; 0001–0023 byte-identical to 273990a0. Plain PG16 at 12f92a6f: 626 passed / 2 failed (both pre-existing: the DOOR-REVOKE W5 gate case and `test_lifecycle_conformance…replay_exactly`, which fails identically with 0024 removed — carry to the union handback); `test_port_d10` 10/10; 11 new 0024 mutants + 4 re-anchored killed; the full `INFRX_MUTANTS=all` list never completed at any head. **Open for the workflow's lenses/fix round:** on the Supabase image 5 cases fail, all on the WR-C3A-4 path (`consumer_may_create_key()` / `auth.users` privileges): `test_port_d10` key-insert + re-run, `test_postgrest_d10` browser role matrix, `test_schema_postgres` browser-roles, `test_credit_schema` operator_seams. A stopped `infrx-revoke-postgres` container remains on 55459 for the harness to replace.
- 2026-09-26 00:33Z: **I2A-PREP merged** (`codex/i2a-prep` 8ca672d0 → a5ca1cd1; verdict ACCEPT_WITH_FIXES: I2A-R2 NEXT_PUBLIC_APP_URL no longer required-but-unread, I2A-R3 the host commit wins the release identity, F1/I2A-R1 README restored and the coordinator ratifies `apps/app/instrumentation.ts`, `apps/app/app/api/version/route.ts` and `apps/app/.env.example` as I2A scope; minors F3–F6/R5–R7 carried in the evidence). Coordinator wiring 416da075: WR-I2A-1 `/api/version` public (+ I2A-ROUTE-01 in tests/a), WR-I2A-2 `console-built` target in `make check`, and the carried F2/I2A-R4 closed (trailing-dot / upper-case production host still refused; fails-before 12/1 → 17/0 on tests/i2a + tests/a/public-routes). Ruling **R134** (App deployment configuration) numbered; W5 rows move to R135–R138, W5-F5 to R139. The lane's proposed named gate "APP-MERGE" is recorded as runbook wording only (the App release follows BACKEND-READY; no new manifest gate). Merge checks running from a shared clone (console-test/lint/typecheck/console-built, A mutants, validate_plan) before the push. Overlay rev 61 rendered (I2A lane → review, head 8ca672d0).
- 2026-09-26 00:41Z: **App wave complete** (`wf_77b93903-38d`, 64 agents): C3A verified ACCEPT_WITH_FIXES at 91af4caf (0-C3A-VCM-1/2 + 1-C3A-V-1/2 fixed; 1-C3A-V-3 = the unverified-JWT `api_keys` insert, closed by 0024 WR-C3A-4), U4 at d76f912f (V-01/V-02 fixed; V-03 = unknown-usage result body, closed by 0024 WR-U4-2), U2 at 2bd04497 (five findings fixed and rechecked, incl. the blocking "failed key read rendered as 'No keys yet'"), U3 at b80c4cd8 (two blocking + one major fixed and rechecked: DUR-RLS on the operator views can fail, form retry protection wired, Unavailable rendering tested). **APP-UNION round 2 dispatched** (the round-1 union agent resumed — a completed agent, so no duplicate): `codex/app-union-2` from 35bd43dd, merge order C3A → U4 → U2 → U3, `tests/u/run-mutants.mjs` as the union, wirings WR-U4-1 (console-pg), WR-U3-2 (operator port in app/actions.ts), WR-U2-1/2/3 (Settings nav, browser plaintext reader removed, REVOCATION_COPY from A3), WR-U3-4, optional real-stack make targets; the two known-gap cases flip at the 0024 merge. WR-U3-1 (operator RPCs) is queued as a D10 **0025** micro-lane after 0024's verdict. Overlay rev 62 rendered.
- 2026-09-26 00:51Z: **P25-ENACT verified** ACCEPT_WITH_FIXES at a15393c5 (`wf_77ac74e5-386`; code 5240bd8c + fix round 1c62eef8): P25R-1 (the 3,600 s grace reaches only worker-registered content; the gateway's own `PgLifecycle` still stamps 604,800 s for fetched/uploaded sources and payloads) recorded with a widened WR-P25-1; the dump cadence reconciled with A9 (the 7-newest prune supersedes the post-A8 removal); the known-good record command carries every `--set`; minors carried (no upper bound on `RETENTION_GRACE_S`; preflight TUNABLE edit outside the owned list — accepted, the schema test requires it). Routed into the backend union **round 3** with WR-P25-1 (gateway grace, proof test + mutant), WR-P25-2/3 (wording), WR-P25-4 (`test_observe` KNOWN_UNPRODUCED). 15-pending-inputs P-25 row updated (WR-P25-5). Overlay rev 63 (P25-ENACT as a support lane).
- 2026-09-26 00:57Z: **WAVE4B-UNION rounds 1–2 handed back** at 452b175e (code 74183655; evidence `coordinator/wave4b-union-7418365.md`): first-parent merges d10-followup 9d4bd28f (+W1–W4 5b7eb5e8), m6-wiring 28dd1af2 (+WR-M6W-1 21694661), w5-merge f18c72f7 (service.py resolved: reconciliation then housekeeping), door-revoke 273990a0 (+W-DR1 v2 a5a08519), w5-merge delta 50d85530, tip 1a14c2b8, known-good-proof f5293057. 0001–0021 unchanged; 0022 sha256 1f44b4ae…e706, 0023 0ea64faa…7f46. Checks green at each step (tests/d 845, contracts 1286, tests/m 577, tests/w 322, rls matrix 886/0 on 0001–0023 with the runtime function set at 41, privilege probe PASS 62 on the DOOR-REVOKE list, retention mutants 62/62, prep/worker/w5 mutants 152 killed, door-revoke list 101, KGP suites 25, known-good.py --applied 0023 KNOWN-GOOD and --applied 0024 NOT-KNOWN-GOOD, validate PASS); `make api-test` 4437 passed / 44 failed, all one cause **F1** = `test_observe` KNOWN_UNPRODUCED still listing UnsettleableJobs (= WR-P25-4; the 43 tests/i mutants cascade as broken_runner). F2 README nit, F3 two PgLifecycle objects (follow-up). **W5-F5 fix round** at 1ea5047f: no runtime change (R1 = WR-W5F5-2 is the coordinator's; R2 = attach-after-admit_ready refusal pinned as a strict-xfail probe + WR-W5F5-4 follow-up lane). **Round 3 go** sent: w5-f5 1ea5047f + WR-W5F5-2, p25-enact a15393c5 + WR-P25-1..4 (P25-4 = F1), F2 doc fix, tip 65445218 merged in; then the full check set incl. `make api-test`; the E3C final run takes the resulting SHA.
- 2026-09-26 01:00Z: **W5-F5 verdict** ACCEPT_WITH_FIXES at 1ea5047f (`wf_5e483fbc-a92`): R1 (test_packaging red until WR-W5F5-2) closes in the union round 3; **R2 open** — after `admit_ready` writes the marker, a refusal from `media.attach` (relay.py:297-303 at that head) still cancels and re-raises, so a job that ran and settled can be answered 409/4xx (same shape as F-5; the lane pinned it as a strict-xfail probe 7d28e115). Dispatched **W5-F5B** (`wf` launched now; `codex/w5-f5b` from 1ea5047f, e2c block): no refusal after the marker may cancel or answer the job an error (attach moved before the marker or made non-refusing with a metric, per the pending ruling), union F3 (one `PgLifecycle` for M6's lifecycle and W5's readiness, mutants re-anchored), WR-W5F5-3 (FakeLifecycle.admit_ready refuses text/missing revision like 0019; contract-fake edit ratified for this lane). It merges after the union (round 4 or onto the tip) before the E3C gate is declared.
- 2026-09-26 01:02Z: **APP-UNION round 2 merged** (`codex/app-union-2` f0d592f5, code 2fad3510 → dec15244): C3A f7ebc726, U4 f702f989, U2 caabf2e4, U3 6883dbe0 (tests/u/run-mutants.mjs as the union: 204 mutants + 2 self-checks), wirings WR-U4-1 (console-pg), WR-U3-2 (operator port + U3-W02), WR-U2-1 (Settings nav), WR-U2-2 (browser plaintext reader removed), WR-U3-4 (Operator label), console-c3a-real/console-u3-real targets, a broken plan link repaired. WR-C3A-1 verified in U2; WR-C3A-5 withdrawn (A2's claimSignupGrant is the one grant adapter). **WR-U2-3 decided as option (a)** (the re-export drags a type-only import of models/catalog.ts through I2A's source walk): both copies stay, guarded by U2-W03 (b30e6aa0). Lane checks: 636 console tests (585 pass, 51 real-DB skips), lint 0 errors, typecheck clean, mutants contracts 212, V 40, U 204, C 176, A2 45, A3 46, console-built 22, console-c0-real 12/12, console-pg 6/6 + 7/1 todo (U4-P08 gap), console-c3a-real 9/9, console-u3-real 9/9, validate PASS. Manifest: **C3A, U4, U2, U3 → implemented** (closure notes name the 0024/0025-dependent cases); rulings **R140–R143** numbered (R135–R139 stay reserved for the backend union). Overlay rev 64. Coordinator checks running from a shared clone at b30e6aa0 before the push.
- 01:19Z: **APP-UNION round-2 checks green** (fixed clone at b30e6aa0, `scratchpad/wave4b/checks-appu2`): install, console-test (586 pass), console-lint, console-typecheck, console-built (22/22), console-mutants (2 self-checks as expected; 46/46 killed), validate_plan PASS — every log ends `exit 0`. **Push of 6badd4e1 denied** by the tool policy this window ("out-of-place publication"); the tip stays local until the user pushes (`git push origin claude/consumer-v1`). Tracker: `apply-updates` applied 3 of 10 lane update files (7 rejected as stale/malformed: C3A/U2/U3 superseded by the manual flips at 01:02Z; both I2A files carry confidence "medium-low", not in the schema) → overlay rev 65. Dependency scan (start_dependencies over the manifest, non-Lab, planned): everything launchable is running (W5 in the union round 3 + W5-F5B; E3C final run waits on the union SHA; E3A-PREP and 0024 in their fix rounds) except **I3** (start deps I2A + F2P; I2A-PREP merged, live half operator-held) → **I3-PREP dispatched** as a preparation lane under the user's App-ahead decision: workflow wf_eed7ff75-8f5, `codex/i3-prep` from 6badd4e1 (worktree codex-i3-prep, no compose block, task-local PG app-i3 55461 only if needed). Owned: infra/app/operations.md (combined checks, auth/credit cutover + rollback, browser error monitoring, alert delivery, App rollback with the schema/contract compatibility rule, retention/resource budgets), infra/app/rollback.py (offline target check; applied migration is an argument), App error boundaries + POST /api/client-errors (bounded, sanitised, no dependency) + instrumentation onRequestError, apps/app/tests/i3/, tests/integration/ops/, optional infra/alerts/app.json (rules.py merge = WR). G5 (conditional, APP-M3) is outside program 22 — not dispatched. Stale notification: agent ae6bb3027beb6c9c8 = L3-REBASE (merged 02eb87ff at 18:23Z) — nothing to do. Lane states at this mark: union round 3 (acfc5fe8b1907e979) running on 05d63097; W5-F5B implementing (0a230353 relay refusals, 6dbaae64 one PgLifecycle); D10-APP-SQL fix round (57ced756 GoTrue columns, 3b6dee82 key-insert predicate = verified individual, no wallet required); E3A-PREP fix round after two REJECTs (33b5a027 delegated cells/stated environment/no leaked process, 5285d88e operator-controls NOT RUN until an operator acts).
