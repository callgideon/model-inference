# Lane rules — consumer v1 wave (program 22). Coordinator = Fable (this session). You = an Opus implementer.

1. Work ONLY in your worktree (the path in your brief). Your branch is codex/<lane>, base dff31efc (= main). Commit early and often on your branch with clear messages. NEVER: push, rebase, reset --hard, amend a pushed commit, cherry-pick onto other branches, bare `git stash`, or touch any other worktree or the coordinator's tree.
2. Own only your listed paths. If you need a change outside them (composition roots `infrx/gateway/app.py` / `pilot.py` / worker main, `Makefile`, lockfiles, `tasks.json`, CLAUDE.md, shared contracts `__init__`/`index.ts` exports unless you own them, other lanes' modules), write a WIRING REQUEST in your handback: exact file, exact patch, and a test proving the composed result. Do not apply it.
3. Isolation: use ONLY your assigned port range / container-name prefix / DB name / object prefix. Never connect to the hosted Supabase database, the pilot box (i-0e8449a4ffca29bab), marlin2b.callbill.ai, or AWS (no `aws` CLI, no SSM, no S3). Only the coordinator does that; ask for it in the handback. No secrets in files, logs, argv or evidence; never print environment values that look like keys or DSNs with passwords.
4. Tooling: `make api-env` once in your worktree (pinned `uv sync --frozen` into `apps/infrx-api/.venv`); focused suites: `cd apps/infrx-api && uv run --frozen pytest -q <path>`; console: `cd apps/app && pnpm install --frozen-lockfile` once, then `pnpm test`/lint/typecheck via the root `make console-*`. Anything that may exceed 10 minutes: run detached (`setsid nohup <cmd> > <abs-log> 2>&1 < /dev/null &`) and poll the log. Docker is available (shared daemon; name every container/network/volume with your prefix; remove them when done).
5. Tests: write the failing regression at the seam FIRST (prove it fails against the old behavior — record that), then implement, then rerun. Keep every existing test list green. Every new test needs a failure oracle (what broken behavior it catches). No snapshot-only tests for semantics.
6. Evidence and handback: write `research/plan/evidence/<track>/<TASK>-<head7>.md` (append-only directory; never overwrite an existing file) with: base/head SHAs, changed paths, exact commands + exit codes + pass/fail/skip counts, failed-then-passed regressions, wiring requests, open issues, and a remaining-effort estimate (optimistic/likely/pessimistic hours, confidence low/medium/high, basis). Also write `research/plan/evidence/coordinator/updates/<TASK>-<YYYYMMDDTHHMMZ>.json`: {"task","slice","activity" (running|review|blocked),"head","worktree","branch","owned_paths","commands":[{"cmd","exit","summary"}],"estimate":{"optimistic_h","likely_h","pessimistic_h","confidence","basis","at"},"blockers":[],"wiring_requests":[],"evidence":[paths]}. Commit both.
7. Your final message = the handback summary (<= 60 lines): head SHA, what is done vs not, test counts, wiring requests, open issues, estimate. It is data for the coordinator, not prose for a human.
8. Product invariants (do not regress): 82 s finite-video cap; CREDIT and USD are exact, separately labelled units, never converted; one-time 10,000 CREDIT per verified individual, no refill; no ZDR / 120 s / concurrency-16 / universal-tool claims; PostgreSQL is the authority and Valkey is rebuildable; never regenerate published output or settle twice; do not rebuild earlier waves — repair and reuse. Migrations 0001–0018 are immutable; only the D10 lane writes SQL.
9. Read before designing: research/plan/22-consumer-v1-implementation.md, your brief in research/plan/consumer-v1/, your task record in research/plan/tasks.json, research/plan/21-v1-consumer-readiness-review-2026-09-24.md (the RV findings you close), research/plan/04-verification.md (the 2026-09-24 corrective oracles), research/plan/08-contracts-v1-encoding.md §10 (rulings; next free number R109 — propose ruling text in your handback, the coordinator appends).
