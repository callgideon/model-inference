# I2B — Reproducible Marlin endpoint deployment independent of frontends (software half) — review fix round

| Field | Value |
|---|---|
| Task | **I2B** (track I), brief `.claude/handoff/wave3/I2B.md`; binding scope `research/plan/18-marlin-backend-first.md` §I2B slices a/b/c. This round: the review `research/plan/evidence/i/I2B-review-5d68e50.json` (on `claude/backend-impl`) |
| Status | **implemented** — both confirmed blocking findings (M1, S1) and every nonblocking fold-in (M2–M8, FC-1..5, S2–S5) fixed, one commit each. **Not integrated, not deployed**: the box rollout is the coordinator's; the pilot configuration is still refused today by design (G2, W3/coordinator, D2+), unchanged by this round |
| Owner/session | Opus 5.5 implementation session, 2026-09-23 |
| Base SHA | `d18177e565d8f63c76dc1935718eb302e49e2135` |
| Reviewed head | `5d68e50` (implementation `574a7e0`, report `research/plan/evidence/i/I2B-574a7e0.md`, preserved unchanged) |
| Implementation SHA | **`3c48066`** (head of the fix round; this report is committed after it) |
| Branch / worktree | `codex/i2b-deployment` in `.claude/worktrees/codex-i2b` |
| Integrated SHA | none — the coordinator owns integration into `claude/backend-impl` |
| Oracles | `BACKEND-DEPLOY`, `DEPLOY-FAILCLOSED`, `OPS-RECOVER` (`research/plan/04-verification.md`) |

## The deployed host is not changed by this task

Nothing here ran against `i-0e8449a4ffca29bab`, AWS or hosted Supabase: no AWS call, no SSM
command, no hosted SQL. Row `O-FAILOPEN` still describes the box. Docker was used locally
only: the rehearsal in its own namespace (`REHEARSAL_NS=infrx-i2b-fix`, every container,
network and volume `infrx-i2b-fix-*`, label `ai.infrx.rehearsal=infrx-i2b-fix`, all removed,
teardown quoted below) and one throwaway `infrx-i2b-fix-validate` Caddy per Caddyfile
(`--rm --network none`). No other lane's container was touched.

## The fix round, item by item

Every commit below is one item; every behaviour change has a named killing case and a
mutant in `tests/i/mutants.py` (R32/R40/R83). "Rehearsal" = a check in `deploy/rehearse.sh`
that ran against the real binaries (command 6).

| Item | Sev. | Commit | What changed | Killing case(s) | Mutant(s) | Rehearsal |
|---|---|---|---|---|---|---|
| **M1** | blocking | `ad52f61` | `90-revert.sh` ends with `READY_S=${READY_S:-900} "$d/drain.sh" resume` after `systemctl restart marlin2b-vllm`: step 8's backup holds the maintenance site 30-pause made active, so rollback.sh restored it and the endpoint stayed 503. The unused `pre-$RELEASE.Caddyfile` save is deleted from `30-pause.sh`; runbook R2 and step 5 say so | `test_ops_recover__the_r2_revert_reopens_the_edge_on_the_restored_runtime` (Host harness; runs **the steps' own lines**: 30-pause's edge install + pause, `install.sh` pilot → exit 4, then 90-revert's tail from `rollback.sh` on) | `revert_leaves_edge_in_maintenance` | step 7: pause → second deploy → `rollback.sh` → 503 maintenance (restored) → `drain.sh resume` → 200 |
| **S1** | blocking | `c61d60a` | both Caddyfiles: `admin unix//config/admin.sock` (the `caddy_config` volume, mounted only into Caddy); one `caddy_reload` in `lib.sh` with `--address unix//config/admin.sock`, used by `caddy_site` and `rollback.sh`; residual risk in `infra/README.md` §5.2 (Caddy is in-container root with the certificate volume; `docker exec` reaches the socket); `verify-external.sh`'s :2019 check kept | `test_backend_deploy__the_edge_hides_operator_paths_and_sanitizes_health` (both admin lines; every `caddy reload` in `deploy/*.sh` names the socket; only Caddy mounts `caddy_config`); `…drain_closes_the_edge…` (the reload's exact argv) | `admin_api_widened`, `maintenance_admin_widened`, `reload_on_default_admin` | step 5: `GET 127.0.0.1:2019/config/` from the box namespace → `ERR URLError`; steps 6–7: every pause/resume/rollback reload goes through the socket |
| M2 | nonblocking | `6e9ff33` | `apply` checks `conn.info.transaction_status` after each file; a file that ended the transaction (its own COMMIT/ROLLBACK) stops the plan: rollback, no later file, exit **4** with a message that says what may already be committed (not exit 3's "nothing changed") | `test_deploy_failclosed__a_migration_that_ends_the_transaction_stops_the_plan` | `transaction_end_unchecked` (`failure_not_rolled_back` re-anchored: its anchor would have appeared twice) | step 8: `0010_commits.sql` (`create table …; commit;`) → exit 4, history still 0001..0009 |
| M3 | nonblocking | `6218dea` | `plan` runs `set transaction_read_only = on` (the `default_` form only reached later transactions) | `test_backend_deploy__migrate_plans_read_only_and_names_every_pending_file` | `plan_not_read_only` (re-anchored) | — (the plan only SELECTs; the reviewer's drill D11 is the behavioural evidence) |
| M4 | nonblocking | `c9cba72` | test only: typos of the rollback statement (`no-pilot-request-accepted`, `yes`, a padded copy) are refused, nothing stopped or written | `test_deploy_failclosed__rollback_never_returns_a_pilot_to_an_unmetered_runtime` | `attestation_any_nonempty` | — |
| M5 | nonblocking | `da99941` | docs: `infra/README.md` §8 and runbook R2 state the statement is **operator-attested, not checked**, corroborated by a read-only count of hosted pilot job/ledger rows since step 8 (zero) and recorded with it | — (prose) | — | — |
| M6 | nonblocking | `f53aa86` | `local_migrations` refuses a file with `VACUUM`, `ALTER SYSTEM` or `CONCURRENTLY` (create/drop/reindex) at `plan`, naming it; none of D's 0001–0009 matches | `test_deploy_failclosed__migrate_refuses_a_history_it_cannot_explain` (three new files) | `outside_transaction_accepted` | step 8's plans over D's real files still pass |
| M7 | nonblocking | `37e4f43` | `50-install.sh` refuses to start without `MIGRATION_DIGEST` (step 6's applied digest or `nothing-pending`) and echoes it into the record; the script and runbook step 8 say it is the operator's statement (the box cannot see hosted history), not a check | `test_backend_deploy__every_rollout_step_is_strict_bash_that_names_no_secret`, `test_backend_deploy__the_cutover_keeps_the_engines_concurrency` (no digest → install never runs) | `cutover_without_migration_step` | — |
| M8 | nonblocking | `64a12ba` | `plan` prints applied rows as `version name` (history is matched on versions, as the CLI does; the name makes a rename visible) | `test_backend_deploy__migrate_plans_read_only_and_names_every_pending_file` | `applied_names_hidden` | step 8 log: `applied: 0001 init, 0002 seed_models` |
| FC-1 | nonblocking | `2cb74b7` | `50-install.sh` hands install.sh `INFRX_SET="ENGINE_MAX_NUM_SEQS=${ENGINE_MAX_NUM_SEQS:-32} ${INFRX_SET:-}"` (the box's 32; W3's serve.sh defaults to 8); `40-checkout.sh` and runbook step 7 forbid an engine restart between steps 7 and 8 (the installed unit's `--max-num-seqs 32` vs the checked-out serve.sh); worker unit gains `KillSignal=SIGTERM`, `KillMode=mixed`. Already true before this round: the engine unit passes no flag, `docker stop -t 30`, `TimeoutStopSec=60`; the worker is `PartOf=`/`After=` the engine; `PROCESSING_CACHE_DIR=/opt/dlami/nvme/processing` is written by preflight (a manifest key); `ENGINE_MAX_NUM_SEQS` is already a tunable | `test_backend_deploy__the_cutover_keeps_the_engines_concurrency` (runs the step's bytes against a stand-in checkout: 32 by default, 16 when given) | `cutover_drops_engine_concurrency` | — |
| FC-2 | nonblocking | `1aa06d5` | `preflight.TUNABLE_SHAPES`: `ENGINE_MAX_NUM_SEQS` and `WORKER_CONCURRENCY` are `positive_int` (0 passed the runtime's `_coerce` and crash-looped vLLM) | `test_deploy_failclosed__a_tunable_is_written_and_typed_by_the_runtime` (=0/-1/abc, `WORKER_CONCURRENCY=0` refused by shape; =32 written) | `engine_seqs_zero_accepted` | step 3: `ENGINE_MAX_NUM_SEQS=0` → exit 2, env/units byte-identical, gateway not restarted |
| FC-3 | nonblocking | `6be5cdb` | test only: `--set MAX_ACTIVE_JOBS=4\nUPSTREAM=http://10.0.0.5:8000` is refused, file byte-identical, message names the setting and not the value | `test_deploy_failclosed__a_setting_outside_the_schema_installs_nothing` | `tunable_shape_unchecked` | — |
| FC-4 | nonblocking | `cd33ccb` | `edge_install` validates **both** sites with the pinned Caddy; `drain.sh pause` whose reload fails stops nothing and exits 4 saying the edge is still open | `test_backend_deploy__a_pilot_install_opens_the_edge_only_after_readiness` (one validate per file), `test_ops_recover__drain_closes_the_edge_before_stopping_the_worker` (a failing reload) | `maintenance_site_unvalidated`, `pause_reload_failure_ignored` (`drain_leaves_edge_open` re-anchored) | step 5 installs through the two validations |
| FC-5 | nonblocking | `ee1f13f` | test only: `INFRX_MODE=""` and `prod` → exit 2 with no git/docker/systemctl event and no backup | `test_deploy_failclosed__only_a_committed_checkout_is_deployed` | `install_mode_guard_removed` | step 3 "no mode" (unchanged) |
| S2 | nonblocking | `2ce0db5` | `migrate.connect` turns any connection failure into `Refused("MIGRATE_DATABASE_URL: the connection failed (<type>); the value is not echoed")` | `test_deploy_failclosed__a_connection_failure_never_echoes_the_dsn` (real `connect`, two parser-failing DSNs carrying the marker); the missing-DSN check now asserts its own message | `connect_error_unwrapped` (`missing_dsn_attempted` stays killed — it survived until the assertion was added) | — |
| S3 | nonblocking | `1ec5361` | backups are root-only (directories 0700, `files.tar` 0600 via `umask 077`) and a run the preflight refuses deletes its own backup (the backup must precede `apply`, which replaces the file it captures); `infra/README.md` §5.2 row "Install backups" and runbook step 11 say they hold past secrets and the coordinator prunes them | `test_deploy_failclosed__a_refused_install_changes_nothing_on_the_host` (no backup left), `test_ops_recover__rollback_restores_every_replaced_file` (modes) | `refused_backup_kept`, `backup_dir_world_readable`, `backup_archive_world_readable` | step 7's rollback reads the backup |
| S4 | nonblocking | `d57af81` | `verify-external.sh` writes each key into a 0600 `mktemp` header file with the `printf` builtin and passes `-H @file`; runbook steps 2, 6 and 10 read secrets with `read -rs` or from SSM, never on a command line | `test_backend_deploy__verify_external_never_puts_a_key_on_a_command_line` (fake curl records argv and the header file) | `key_in_curl_argv` | — (runs from the coordinator host) |
| S5 | nonblocking | `f7f2669` | runtime check: `10-inventory.sh` and `60-verify-local.sh` (the steps that read the env file) run against a canary env file with the box binaries stubbed and must print names only; `-e NAME=value` joins the secret shapes | `test_backend_deploy__the_read_only_steps_print_names_never_values`, `…every_rollout_step_is_strict_bash_that_names_no_secret` | `verify_local_prints_values`, `inventory_prints_values`, `readme_inline_env_value` | — |
| rehearsal | — | `3c48066` | `rehearse.sh` runs in its own namespace (`REHEARSAL_NS`, default `infrx-i2b`; the label is per namespace, so a teardown never removes another rehearsal's containers) and gains the checks in the last column | — | — | command 6 |

**Deviation from the dispatch, stated:** FC-1 asked for the worker unit's
`TimeoutStopSec=330`. W3's proposal was for a host-process unit, where `TimeoutStopSec` *is*
the drain bound. This worker is a `docker run` client: the drain bound is `docker stop -t 330`
(one generation, 300 s, plus 30), and systemd must outlast docker or it kills only the client
while the container keeps running — the invariant
`test_ops_recover__every_container_unit_stops_later_than_docker_does` pins (`worker_killed_before_drain`).
So the unit keeps `docker stop -t 330` / `TimeoutStopSec=360`, and gains W3's
`KillSignal=SIGTERM` / `KillMode=mixed`. Also not done literally: "preflight TUNABLE gains
PROCESSING_CACHE_DIR" — it is already a **manifest** key (written with the default
`/opt/dlami/nvme/processing` in every mode); listing it as a tunable too would break the
schema partition `test_backend_deploy__the_config_schema_is_every_name_the_runtime_reads` pins.

## Requirement coverage

Every case below is in `apps/infrx-api/tests/i/`, and every one is named by at least one
mutant of `tests/i/mutants.py` that it kills (`test_every_case_is_covered_by_a_mutant`).
Rows marked **new** or **changed** are this round's; the rest are carried forward from
`I2B-574a7e0.md` unchanged.

### I2B.a — packaging (`test_packaging.py`)

| Test ID | Invariant | Killing mutant(s) |
|---|---|---|
| `test_backend_deploy__every_image_is_pinned_by_digest` | both Dockerfile bases, Valkey and Caddy are `@sha256:` references; the runtime units run `${INFRX_IMAGE}`, which the manifest accepts only as `sha256:<64 hex>` | `dockerfile_base_by_tag`, `valkey_by_tag`, `install_caddy_unpinned`, `runtime_image_floating`, `image_shape_accepts_tags` |
| `test_backend_deploy__the_image_runs_nothing_as_root` | the image's last `USER` is unprivileged | `image_runs_as_root` |
| `test_backend_deploy__the_units_are_valid_systemd` | `systemd-analyze verify` over every shipped unit (now including the worker's `KillSignal`/`KillMode`), only the absent-on-this-host `serve.sh` path tolerated | `unit_syntax_broken` |
| `test_ops_recover__every_container_unit_stops_later_than_docker_does` | `TimeoutStopSec` > `docker stop -t N`, which names the unit's own container | `worker_killed_before_drain`, `engine_default_drain`, `gateway_stops_other_container` |
| `test_ops_recover__the_worker_drain_outlasts_one_generation` | worker stop budget > 08 §5 `GENERATION_TIMEOUT_S` (300 s); gateway ≥ 120 s | `worker_drain_shorter_than_generation` |
| `test_ops_recover__the_worker_drains_before_the_engine_stops` | worker is `PartOf=` and `After=` the engine; runs `python -m infrx.worker`; tini on gateway and worker; no timer | `worker_not_part_of_engine`, `worker_signal_not_delivered` |
| `test_backend_deploy__runtime_containers_run_unprivileged_and_bounded` | per-process uid, read-only root, `--cap-drop ALL`, `no-new-privileges`, memory and pid bounds; only the gateway writes the media root | `gateway_writable_root`, `worker_shares_gateway_uid`, `worker_writes_media`, `valkey_capabilities_kept` |
| `test_backend_deploy__the_runtime_listens_only_on_loopback` | gateway `--host 127.0.0.1`, Valkey `--bind 127.0.0.1 --protected-mode yes`; the installer refuses a non-loopback engine or index address | `gateway_public_bind`, `valkey_public_bind`, `loopback_engine_url_widened`, `loopback_index_widened` |
| `test_deploy_failclosed__a_public_engine_bind_is_refused` | every `-p` serve.sh publishes is loopback, else a pilot is refused | `public_engine_bind_allowed` |
| `test_backend_deploy__the_engine_takes_its_settings_from_the_validated_file` | the engine unit passes no argument and reads the env file; engine and gateway recreate the media root with its owner | `engine_second_setting_source`, `engine_reads_no_settings`, `media_root_created_by_docker` |
| `test_backend_deploy__one_env_file_configures_every_runtime_unit` | every runtime unit's `EnvironmentFile` and `--env-file` is the validated file | `worker_reads_other_env` |
| `test_backend_deploy__the_config_schema_is_every_name_the_runtime_reads` | manifest ∪ `TUNABLE` ∪ `NOT_SETTABLE` partitions exactly the names `config.from_env` reads | `tunable_dropped_from_schema` |
| `test_deploy_failclosed__a_setting_outside_the_schema_installs_nothing` **changed** | a mistyped name, a secret, a manifest key, `JUDGE_MODE`, a name twice, **a value with a newline (a second `UPSTREAM`)**: refused, file byte-identical, no `systemctl`, the value never echoed | `unknown_setting_accepted`, `setting_given_twice_accepted`, **`tunable_shape_unchecked`** |
| `test_deploy_failclosed__a_tunable_is_written_and_typed_by_the_runtime` **changed** | a schema name reaches the file; `abc`, `-1`, a zero pool are refused by the probe; **`ENGINE_MAX_NUM_SEQS` 0/-1/abc and `WORKER_CONCURRENCY=0` refused by shape; 32 written** | `tunables_not_rendered`, `runtime_not_validated`, **`engine_seqs_zero_accepted`** |
| `test_deploy_failclosed__pilot_runs_only_the_pinned_runtime_image` | pilot without an image id or with a tag is refused; the probe runs in the image offline, file on stdin | `image_not_required_in_pilot`, `probe_on_host_despite_image`, `probe_online`, `image_probe_values_in_argv` |
| `test_deploy_failclosed__a_pilot_image_must_be_unprivileged_and_carry_the_worker` | inside the image, pilot refuses euid 0 and a runtime without `infrx.worker.__main__` | `root_runtime_accepted`, `worker_entries_unchecked` |
| `test_deploy_failclosed__pilot_needs_w3s_recorded_engine_pin` | no `serving-version.json` → `PENDING(W3)`; present, it must contain the digest serve.sh runs | `serving_version_optional`, `serving_version_digest_unchecked` |
| `test_deploy_failclosed__a_host_below_its_disk_budget_installs_no_pilot` | free space under the declared budget refuses a pilot | `disk_budget_skipped`, `disk_budget_compares_wrong`, `disk_budget_lowered` |
| `test_backend_deploy__the_edge_hides_operator_paths_and_sanitizes_health` **changed** | as before (private paths 404, sanitized health, no route to the engine, bounded bodies, contract envelopes) **plus: both Caddyfiles' admin API is `unix//config/admin.sock`; every `caddy reload` in `deploy/*.sh` names it; only Caddy mounts `caddy_config`** | `metrics_public`, `edge_proxies_engine`, `health_body_passed_through`, `edge_body_unbounded`, `declared_length_unchecked`, `health_down_is_empty`, **`admin_api_widened`, `maintenance_admin_widened`, `reload_on_default_admin`** |
| `test_backend_deploy__maintenance_answers_every_request_with_the_retry_envelope` | maintenance proxies nothing; `retry_after_s` equals `Retry-After` | `maintenance_proxies`, `maintenance_retry_mismatch` |

### I2B.b — scripts (`test_scripts.py`, `test_migrate.py`)

| Test ID | Invariant | Killing mutant(s) |
|---|---|---|
| `test_deploy_failclosed__a_refused_install_changes_nothing_on_the_host` **changed** | a denied SSM read and the repository's pilot today: exit 2, env and units byte-identical, no unit added, no `systemctl`/`chown`, no state dir, edge untouched, **no backup left (it holds the previous secrets)** | `preflight_refusal_ignored`, `units_before_preflight`, **`refused_backup_kept`** |
| `test_deploy_failclosed__only_a_committed_checkout_is_deployed` **changed** | **an empty or unknown mode stops before git, the build and the backup**; a dirty tree or HEAD ≠ `RELEASE` stops before the build; git calls name `safe.directory` | `dirty_checkout_deployed`, `release_unchecked`, `git_refuses_root`, **`install_mode_guard_removed`** |
| `test_backend_deploy__a_dev_install_pins_the_image_it_probed` | image probed = image written; units are the repo's bytes; engine started not restarted unless `ENGINE=restart`; runtime after engine health; no edge in dev; the backup holds the previous files | `image_not_pinned_in_env`, `engine_restarted_by_install`, `engine_restart_ignored`, `runtime_before_engine_health`, `edge_in_dev`, `backup_skipped` |
| `test_backend_deploy__a_pilot_install_opens_the_edge_only_after_readiness` **changed** | pilot order ends with the edge after both `/readyz`; **both sites, normal and maintenance, validated with the pinned Caddy offline** | `edge_unvalidated`, `worker_readiness_skipped`, **`maintenance_site_unvalidated`** |
| `test_ops_recover__a_runtime_that_is_not_ready_leaves_the_edge_alone` | not ready: exit 4 naming `rollback.sh <backup>`, edge untouched | `edge_opened_unready`, `worker_readiness_skipped` |
| `test_ops_recover__drain_closes_the_edge_before_stopping_the_worker` **changed** | pause: maintenance active and reloaded (through the admin socket) before the runtime stops; **a failed reload stops nothing, exit 4, "still OPEN"**; resume only after readiness | `drain_leaves_edge_open`, `maintenance_not_sticky`, `resume_opens_edge_unready`, `worker_readiness_skipped`, **`pause_reload_failure_ignored`, `reload_on_default_admin`** |
| `test_ops_recover__rollback_restores_every_replaced_file` **changed** | env and unit bytes come back, new units removed, gateway restarted; **backup directories 0700, archive 0600** | `rollback_keeps_new_units`, `rollback_restores_nothing`, `backup_skipped`, **`backup_dir_world_readable`, `backup_archive_world_readable`** |
| `test_deploy_failclosed__rollback_never_returns_a_pilot_to_an_unmetered_runtime` **changed** | §8: pilot → no-mode refused before anything stops unless the **exact** statement is given (typos refused) | `rollback_to_unmetered_allowed`, `rollback_restores_nothing`, **`attestation_any_nonempty`** |
| `test_ops_recover__the_r2_revert_reopens_the_edge_on_the_restored_runtime` **new** | runbook R2 in order, with the steps' own lines: after pause → exit-4 install → 90-revert's tail, the monolith env is back and the **normal** site is active, reloaded after `/health`, after the engine restart | `revert_leaves_edge_in_maintenance` |
| `test_backend_deploy__migrate_plans_read_only_and_names_every_pending_file` **changed** | `plan` sets **`transaction_read_only`** first, lists applied rows **as `version name`** and pending files with sha256, prints the digest, writes nothing | `plan_not_read_only`, **`applied_names_hidden`** |
| `test_deploy_failclosed__migrate_applies_only_the_reviewed_plan` | lock first; an edited file is a different digest; the reviewed plan runs in order with one commit | `reviewed_digest_not_enforced`, `digest_blind_to_content`, `lock_not_taken`, `commit_per_file` |
| `test_deploy_failclosed__a_failed_migration_rolls_back_the_whole_plan` | a failure in the second file: exit 3, rollback, no history row, no commit | `failure_not_rolled_back`, `failure_reported_as_success` |
| `test_deploy_failclosed__a_migration_that_ends_the_transaction_stops_the_plan` **new** | a file that ends the transaction: exit 4, no history row, no later file, no commit, named | `transaction_end_unchecked` |
| `test_deploy_failclosed__a_connection_failure_never_echoes_the_dsn` **new** | through the real `connect`, a DSN libpq cannot parse: exit 2, the marker never printed | `connect_error_unwrapped` |
| `test_deploy_failclosed__migrate_refuses_a_history_it_cannot_explain` **changed** | no history table, an unknown version, a gap, a file outside the grammar, two files per version, **a VACUUM / ALTER SYSTEM / CONCURRENTLY file**, no DSN (**refused for the missing value**): exit 2 | `no_history_assumed_empty`, `unknown_version_ignored`, `gap_filled`, `odd_file_skipped`, `duplicate_version_accepted`, `missing_dsn_attempted`, **`outside_transaction_accepted`** |

### I2B.c — rollout (`test_rollout.py`)

| Test ID | Invariant | Killing mutant(s) |
|---|---|---|
| `test_backend_deploy__every_rollout_step_is_strict_bash_that_names_no_secret` **changed** | every step parses, `set -euo pipefail`, guards `RELEASE`; **the cutover guards `MIGRATION_DIGEST`**; no DSN password, JWT, access key, inline `--value` **or inline `docker -e NAME=value`** | `step_without_release_guard`, **`cutover_without_migration_step`, `readme_inline_env_value`** |
| `test_backend_deploy__ssm_carries_a_step_byte_for_byte` | base64 round trip; exit is the invocation's; only `NAME=VALUE` travels | `ssm_drops_arguments`, `ssm_ignores_status`, `ssm_accepts_any_argument` |
| `test_ops_recover__the_revert_restores_the_tree_before_the_runtime` **changed** | revert checks out the previous HEAD before `rollback.sh`, then restarts the engine; the pause records HEAD before replacing the edge (the Caddyfile save is gone) | `revert_runtime_before_tree`, `pause_forgets_previous_head` |
| `test_backend_deploy__the_cutover_keeps_the_engines_concurrency` **new** | the cutover step's own bytes hand install.sh `ENGINE_MAX_NUM_SEQS=32` unless another value is given, pilot mode, engine restart; without `MIGRATION_DIGEST` install.sh never runs | `cutover_drops_engine_concurrency`, `cutover_without_migration_step` |
| `test_backend_deploy__verify_external_never_puts_a_key_on_a_command_line` **new** | no key in any curl argv or in the output; each key is in the header file curl reads | `key_in_curl_argv` |
| `test_backend_deploy__the_read_only_steps_print_names_never_values` **new** | 10-inventory and 60-verify-local over a canary env file print the names and never a value | `verify_local_prints_values`, `inventory_prints_values` |

### I0 cases (owned: `tests/i`)

Unchanged this round; `test_prereqs.py` and the I0 cases in `test_install.py` as in
`I2B-574a7e0.md` (integration requests 3 and 6 still apply).

## Environment

| Item | Value |
|---|---|
| Classification | **local** developer host; no cloud, no GPU; Docker containers `infrx-i2b-fix-*` only |
| OS | `Linux 7.0.0-1010-aws x86_64`, Ubuntu 24.04 |
| Python | test venv `apps/infrx-api/.venv` → `Python 3.12.3` (`uv 0.11.8`, `uv run --frozen`); runtime image → `image python 3.12.14 uid 10001` (rehearsal step 0) |
| psycopg | `3.3.6` (venv; the S2 case uses the real `psycopg.connect`) |
| Docker | `Docker version 29.6.2, build dfc4efb` |
| systemd (for `verify`) | `systemd 255 (255.4-1ubuntu8.17)` |
| bash | `GNU bash, version 5.2.21(1)-release` |
| Images | unchanged pins (see `I2B-574a7e0.md`): Caddy `caddy@sha256:14a9c00d…` (2.11.4), Valkey, supabase/postgres, python, uv by digest |
| Seed | none |

## Commands and results

Run from `apps/infrx-api` unless noted; environment variable **names** only.

| # | Command | Exit | Result (quoted tail) |
|---|---|---|---|
| 1 | per-item focused runs during the round: `.venv/bin/python -m pytest -q -p no:cacheprovider tests/i/<file>` and `.venv/bin/python tests/i/mutants.py <names>` for each item's mutants and every re-anchored neighbour | 0 | every item's mutants `killed` (e.g. M1: `3/3 killed`; S1: `3/3 killed`; S2 after the missing-DSN assertion: `2/2 killed`); one intermediate `survived: ['missing_dsn_attempted']` (S2's wrapper made the default connection a refusal too) and one `misdeclared: ['drain_leaves_edge_open']` (FC-4 moved its anchor) were fixed before their commits |
| 2 | `docker run --rm --name infrx-i2b-fix-validate --network none -v $PWD/deploy/<f>:/etc/caddy/Caddyfile:ro caddy@sha256:14a9c0… caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile` for both Caddyfiles (at `c61d60a`'s tree) | 0, 0 | `Valid configuration` twice |
| 3 | `timeout 900 uv run --frozen pytest -q -p no:cacheprovider tests/i --ignore=tests/i/test_mutants.py` (at `3c48066`) | 0 | `98 passed in 35.92s` |
| 4 | `uv run --frozen python tests/i/mutants.py --list \| tail -1` | 0 | `159 mutants over 78 named cases` (136 before this round: +23, one per row of the table above) |
| 5 | `INFRX_MUTANTS=all uv run --frozen pytest -q -p no:cacheprovider tests/i/test_mutants.py` (detached, `setsid nohup … > /tmp/claude-1000/i2b-fix/i-list.log`, started 2026-09-23T03:02:24Z at `3c48066`) | 0 | `166 passed in 471.35s (0:07:51)` (finished 2026-09-23T03:10:23Z) — 159 mutants, every one `killed`, + 5 runner self-tests + 2 list checks |
| 6 | `REHEARSAL_NS=infrx-i2b-fix bash apps/infrx-api/deploy/rehearse.sh /tmp/claude-1000/i2b-fix/rehearsal` (repo root, detached, release `3c48066aa5583978080ac1de08435504953a1f0d`) | 0 by the script's own rule (`exit "$FAILED"` after `REHEARSAL PASSED`); the detached status was not captured separately | `REHEARSAL PASSED`, `teardown: nothing infrx-i2b-fix-* left`; `grep -c '^PASS'` → `43`, `grep -c '^FAIL'` → `0` |
| 7 | legacy-first ordering: `uv run --frozen pytest -q -p no:cacheprovider tests/test_gateway_auth.py tests/test_app_factory.py tests/test_inflight.py tests/test_media.py tests/i --ignore=tests/i/test_mutants.py` | 0 | `127 passed, 2 warnings in 41.40s` |
| 8 | the same, `tests/i` first (track-first) | 0 | `127 passed, 2 warnings in 29.75s` |
| 9 | `make check` | — | **not run** this round: the round changes only `deploy/`, `infra/`, `tests/i` and this report; command 8 of `I2B-574a7e0.md` stands for the rest, and not run is not a pass |

### The rehearsal (command 6), quoted

```
=== 0. the box: pinned images, one network namespace
release 3c48066aa5583978080ac1de08435504953a1f0d
runtime image sha256:dc573222f9aca033d15701c8b3f022b31eaa1fe63df46437eeecd4ddbb215348
image python 3.12.14 uid 10001
=== 1. fresh dev deploy through install.sh (real preflight probe in the real image)
PASS install.sh dev exits 0 (got 0)
PASS INFRX_IMAGE in the env file is the image the probe ran in
gateway container: user=10001:10000 ro=true capdrop=[ALL] secopt=[no-new-privileges] mem=8589934592 pids=512 image=sha256:dc573222f9aca033d15701c8b3f022b31eaa1fe63df46437eeecd4ddbb215348
PASS the gateway runs the pinned image as 10001, read-only, no capabilities
=== 2. calls on the deployed path (loopback, legacy key: dev mode)
PASS GET /v1/models 200 (got 200)
PASS chat without a key is 401 (got 401)
PASS chat with a wrong key is 401 (got 401)
PASS chat with the key reaches the engine: 200 (got 200)
=== 3. fail-closed drills against the running deploy
PASS a denied SSM read: exit 2 (got 2), env/units byte-identical, gateway not restarted
PASS a throttled SSM read: exit 2 (got 2), env/units byte-identical, gateway not restarted
PASS a mistyped tunable: exit 2 (got 2), env/units byte-identical, gateway not restarted
PASS a tunable the runtime cannot read: exit 2 (got 2), env/units byte-identical, gateway not restarted
PASS an engine sequence count of 0: exit 2 (got 2), env/units byte-identical, gateway not restarted
PASS the repository's pilot today (refused before the probe: engine pin, W3 record, disk): exit 2 (got 2), env/units byte-identical, gateway not restarted
PASS the in-image pilot probe refuses: ROUTERS not composed (G2), worker entry absent (W3)
PASS no mode: exit 2 (got 2), env/units byte-identical, gateway not restarted
=== 4. the pilot-only index unit (pinned Valkey, loopback, read-only, no persistence)
PASS valkey answers PONG on 127.0.0.1 (got PONG)
valkey container: user=999:1000 ro=true capdrop=[ALL]
PASS valkey runs as 999, read-only, no capabilities
=== 5. the edge: real Caddyfile, pinned Caddy, in the box (plain HTTP address, no ACME)
PASS public /health is exactly {"ok":true}
PASS the admin API does not answer on the shared loopback :2019 (got ERR URLError)
PASS /metrics is 404 not_found at the edge
PASS /readyz is 404 not_found at the edge
PASS /internal/x is 404 not_found at the edge
PASS an authenticated call through the edge is 200 (got 200)
97 MiB body -> HTTP/1.1 413 Request Entity Too Large sent 1 MiB request_too_large
PASS a body over MAX_REQUEST_BYTES is 413 request_too_large at the edge
PASS a normal body still passes the edge after it (200)
PASS the gateway down is public health {"ok":false} 503, nothing else (503 {"ok":false} )
=== 6. drain: maintenance at the edge first, then the runtime stops; resume after readiness
PASS maintenance answers 503 dependency_unavailable
PASS the gateway is stopped
PASS maintenance survives a Caddy restart (health: 503 {"ok":false} )
PASS resumed: public health up again (200 {"ok":true} )
=== 7. R2 in runbook order: pause, a second deploy (MAX_ACTIVE_JOBS=4), rollback.sh to the first, resume
PASS second deploy exits 0 (got 0)
PASS the second env carries MAX_ACTIVE_JOBS=4
PASS the env file is the first deploy's again
PASS the gateway restarted onto it and is ready
PASS rollback.sh restored the edge as it was backed up - maintenance, through the admin socket (503 {"ok":false} )
PASS drain.sh resume (90-revert's last line) reopens the edge (200 {"ok":true} )
=== 8. migrate.py against the pinned supabase/postgres (own network, no host port)
PASS apply with an unreviewed digest refuses: exit 2 (got 2), history 0001,0002
PASS a failing migration rolls the whole plan back: exit 3 (got 3), history 0001,0002
PASS no table from 0003-0010 survived the rollback (got t)
PASS a history the files cannot explain (0002 missing) refuses: exit 2 (got 2)
PASS the reviewed plan applies: exit 0 (got 0), history 0001..0009
PASS a second plan has nothing pending (nothing pending)
PASS a migration with its own COMMIT stops the plan: exit 4 (got 4), history still 0001..0009
=== result
REHEARSAL PASSED
teardown: nothing infrx-i2b-fix-* left
```

The in-image pilot probe line (step 3) is unchanged from `I2B-574a7e0.md`: `"ok": false`
with the G2 `ROUTERS` composition refusal and `PENDING(W3): infrx.worker.__main__`. The
step-8 migrate log shows M8's plan output on the real database
(`applied: 0001 init, 0002 seed_models`) and M2's refusal:
`0010_commits.sql ended the transaction (a COMMIT or ROLLBACK inside a migration): what ran
before it may be committed without its history rows; nothing after it ran`.

**What the rehearsal stands in for** is unchanged (one `--network none` namespace every
`--network host` unit joins, a sandbox root, a mini-systemd running the installed units'
Exec lines, a docker wrapper for names and paths, E2's fake engine; the scripts, units,
Caddyfiles, image and migrations are the real ones). New this round, with the real
binaries: the admin API absent from the shared loopback, every Caddy reload through the
socket (pause, resume, restart, rollback), the R2 order, a COMMIT inside a migration on
the pinned `supabase/postgres`, and `ENGINE_MAX_NUM_SEQS=0` refused.

## Failure drill

Carried forward from `I2B-574a7e0.md`, with this round's changes:

| Injection | Durable state before | After | Retry / cleanup |
|---|---|---|---|
| SSM read denied or throttled during `install.sh` | running deploy, env file E, units U | E and U byte-identical; gateway not restarted; no unit added; edge untouched; exit 2; **the backup the run made is removed** | rerun once the read succeeds |
| `ENGINE_MAX_NUM_SEQS=0` (rehearsal 3) | same | same, exit 2, before any engine restart | a positive value |
| R2 after a paused, not-ready cutover (rehearsal 7, Host case) | maintenance active; new env | previous env; `rollback.sh` restores maintenance; `drain.sh resume` → normal site after `/health` | — |
| a migration with its own COMMIT (rehearsal 8, `test_migrate`) | history 0001..0009 | history unchanged; the file's table **committed** (the hazard M2 names); exit 4 | resolve with D; the message says what may be committed |
| a migration with VACUUM/ALTER SYSTEM/CONCURRENTLY | any | `plan` refuses (exit 2), nothing run | D ships it as its own step |
| a malformed `MIGRATE_DATABASE_URL` | any | exit 2, `…: the connection failed (ProgrammingError); the value is not echoed` | fix the value |
| `drain.sh pause` whose reload fails | edge serving | nothing stopped; exit 4 "still OPEN" | fix the edge, pause again |

## Artifacts

No raw log is committed; results above are transcribed from these files (scratch area of
this session, not durable storage):

```
37d4bd99051c6d3053be8a8f77ccc24edd8934aba05e3b97040d597578f673e9  /tmp/claude-1000/i2b-fix/rehearsal.log
b2641ea0e78db4ee14963ac6af15cabaf526b0dee3d3bf2bc0caf64d55177cdc  /tmp/claude-1000/i2b-fix/i-list.log
```
No credential, parameter value, customer content or signed URL is in the code, tests, logs
or this report: the rehearsal's key and database password are local literals, and the
tests' canary/marker values are `support.MARKER`-derived.

## Changes

Owned paths only: `apps/infrx-api/deploy/**`, `apps/infrx-api/tests/i/**`, `infra/README.md`,
`infra/rollout/**`, this report.

| Path | Change this round |
|---|---|
| `deploy/Caddyfile`, `Caddyfile.maintenance` | `admin unix//config/admin.sock` (S1) |
| `deploy/lib.sh` | `caddy_reload` with `--address` (S1); `edge_install` validates both sites (FC-4) |
| `deploy/drain.sh` | a failed maintenance reload stops nothing, exit 4 (FC-4) |
| `deploy/rollback.sh` | reloads through `caddy_reload` (S1) |
| `deploy/install.sh` | backup `umask 077`; a refused preflight removes its backup (S3) |
| `deploy/migrate.py` | transaction-status check, exit 4 (M2); `transaction_read_only` (M3); outside-transaction refusal (M6); `version name` in `plan` (M8); names-only connection failure (S2) |
| `deploy/preflight.py` | `TUNABLE_SHAPES` (FC-2) |
| `deploy/infrx-worker.service` | `KillSignal=SIGTERM`, `KillMode=mixed` (FC-1) |
| `deploy/rehearse.sh` | own namespace; S1/M1/M2/FC-2 checks |
| `infra/rollout/steps/30-pause.sh`, `90-revert.sh` | M1 |
| `infra/rollout/steps/40-checkout.sh`, `50-install.sh` | FC-1 window note; `ENGINE_MAX_NUM_SEQS=32`; `MIGRATION_DIGEST` (M7) |
| `infra/rollout/verify-external.sh` | keys through a 0600 header file (S4) |
| `infra/rollout/README.md` | steps 2, 5, 6, 7, 8, 10, 11 and R2 (M1, M5, M7, FC-1, S3, S4) |
| `infra/README.md` | §5.2 edge-admin residual risk (S1) and "Install backups" row (S3); §8 operator attestation (M5); three log entries |
| `tests/i/` | 6 new cases, 12 changed (tables above); `mutants.py` 136 → 159 |

**Migration / deploy / rollback implications.** No SQL file changed. Operator interface
changes: the cutover step needs `MIGRATION_DIGEST=<digest|nothing-pending>`; a migration
may now exit 4; a refused install leaves no backup directory; `rehearse.sh` takes
`REHEARSAL_NS` and labels containers `ai.infrx.rehearsal=<ns>` (was
`ai.infrx.i2b.rehearsal=1`). A host already running an edge from the previous I2B commit
(admin on :2019) would need one `docker restart caddy` after the new file is in place,
because a reload through the socket cannot reach an instance still listening on :2019; the
box never ran I2B, and `edge_install` recreates any Caddy not of the pinned image, so the
rollout is unaffected.

## Limits

Carried forward from `I2B-574a7e0.md` §Limits 1–13 unchanged (nothing deployed or integrated;
the branch's `serve.sh` is the base's; the rehearsal is dev mode on a stand-in host; box facts
to confirm in `10-inventory.sh`; worker as a separate process; one env file for gateway, worker
and engine; Valkey without a password; `est.` budgets; per-host image ids; `migrate.py` and the
CLI history; the disk check measures the host it runs on; §5 hardening not in the runbook; the
track-local `compile()` shim). New this round:

14. **M2 detects, it does not prevent.** A file that commits mid-plan has already committed
    what came before it when `apply` notices; exit 4 says so. D's files contain no transaction
    control today (only plpgsql `begin`/`end`), and a static refusal of `commit` would misfire
    on procedure bodies.
15. **M7 is a statement.** `MIGRATION_DIGEST` proves the operator named step 6's outcome, not
    that the hosted history matches it; the box cannot see hosted history.
16. **M5 is an attestation.** `rollback.sh` cannot verify that no pilot request was accepted;
    the corroborating hosted count is the coordinator's step.
17. **S5 runs the two steps that read the env file** (10-inventory, 60-verify-local). The steps
    that run deploy scripts print what those scripts print, which I0's and this suite's
    marker cases cover; they were not each run under a canary here.
18. **The R2 edge after a revert is the release's normal site** in front of the restored
    gateway (the pre-I2B Caddyfile is no longer saved; the EBS snapshot holds it). Both proxy
    `127.0.0.1:8001`; the release's also hides `/metrics`, sanitizes `/health` and bounds bodies.

## Handback

**Next unblocked:** unchanged — the coordinator's review of this round, then I3B consumes
`deploy/` and `infra/rollout/`; the rollout waits on G2 + the worker composition root + D2+
adapters + G6B keys + I3B's hosted backup rehearsal.

**Integration requests** (no shared file was edited). Requests 1–9 of `I2B-574a7e0.md` stand
unchanged (pyproject pin; config names incl. `WORKER_HEALTH_PORT`; `test_prereqs.py` at W3's
merge; the worker composition root; the shared runner's `compile()` gate; drop the three
`dies_by=("TypeError",)` at merge; G2 `Server-Timing`; D's login role for
`/model-inference/pg_journal_url`; E's image pairing). New:

10. **Coordinator — merge order (FC-1a):** merge this branch's `deploy/` in the same integration
    step as W3's `serve.sh` (or before it). `claude/backend-impl` still carries the base's
    `marlin2b-vllm.service` with `--max-num-seqs 32`, which W3's `serve.sh` refuses (exit 2), and
    no suite on that tree reads the unit against `serve.sh`.
11. **Coordinator/W3 — `WORKER_CONCURRENCY`:** the cutover now keeps `ENGINE_MAX_NUM_SEQS=32`;
    W3's rule is `WORKER_CONCURRENCY = ENGINE_MAX_NUM_SEQS` (default 10). If that is wanted at
    cutover, add `WORKER_CONCURRENCY=32` to `50-install.sh`'s `INFRX_SET` (one word; the shape is
    already `positive_int`), or decide it with W3's sweep.

**Unresolved findings:** none of the review's items is left open; the two literal deviations
(worker `TimeoutStopSec`, `PROCESSING_CACHE_DIR` as a tunable) are explained above.

## F2.2 carryover items touched

None directly (as in `I2B-574a7e0.md`). Item 7's deployment half gains the `positive_int`
shapes (FC-2) and the `--set` newline case (FC-3).

## Verification log

- 2026-09-23: Authored for the review fix round from the commands transcribed above, all local
  (developer host, Docker containers `infrx-i2b-fix-*` only, all removed). No AWS call, no SSM
  command, nothing run against the pilot host or hosted Supabase, no paid provider.
  `I2B-574a7e0.md` is preserved unchanged as the record of the reviewed implementation.
  The rehearsal's image tag `infrx-i2b-fix-runtime:<release>` was removed afterwards.

## Round 2 — 2026-09-23: the confirmation review of `0653c8a`

The confirmation review (`research/plan/evidence/i/I2B-confirm-0653c8a.json` on
`claude/backend-impl`) closed M1 and S1 as claimed. It confirmed two **new** blocking findings
(RB-1, RB-2, each confirmed by two refuters) and listed twelve nonblocking ones. This section
is appended; everything above it describes `3c48066` and still stands except where a row
here changes it. **Round-2 implementation SHA: `7c422ae`** (the report commit follows it).

| Item | Sev. | Commit | What changed | Killing case(s) | Mutant(s) | Rehearsal (command R2-4) |
|---|---|---|---|---|---|---|
| **RB-1** (+RB-3) | blocking | `4c2bd2a` | `rollback.sh` takes `ENGINE=restart`: after `daemon-reload` and **before** `systemctl restart $units` it restarts `marlin2b-vllm` and waits for `127.0.0.1:8000/health` (`ENGINE_READY_S`, default 900), because the restored monolith's `/health` asks the engine. When step 8 failed on the engine itself, the gateway used to be restarted first, its readiness wait failed, and the engine was never restarted. `90-revert.sh` calls `ENGINE=restart "$d/rollback.sh" "$BACKUP"` and has no engine restart of its own. RB-3: both exit-4 messages say the edge stays as it was, then *fix it, then `drain.sh resume`*. The step header and runbook R2 say the same and name the logs to read | `test_ops_recover__r2_restores_the_engine_before_the_gateway_that_asks_it` **new**. Host harness: step 8 fails on the engine; the curl stub fails `8001/health` until `systemctl restart marlin2b-vllm` appears in the events. With the engine restart failing: exit 4, the message, no gateway restart, no reload, edge still maintenance. With it succeeding: exit 0, engine restart < engine health < gateway restart, normal site. The M1 case now slices 90-revert from the start of the `rollback.sh` line, so the `ENGINE=restart` prefix is kept | `revert_engine_not_restarted`, `rollback_engine_restart_ignored`, `rollback_engine_failure_ignored` (and `revert_leaves_edge_in_maintenance`, `revert_runtime_before_tree` re-anchored) | step 7 runs `ENGINE=restart rollback.sh`; its mini-systemd log shows `restart marlin2b-vllm` then `restart marlin2b-gateway` |
| **RB-2** | blocking | `9edd308` | `_positive_int` is `re.fullmatch(r"[1-9][0-9]*", value)`, serve.sh's own grammar. `isdigit` accepted `007` and `٣`, which serve.sh refuses at the engine restart, and `int('²')` crashed preflight with a traceback | `test_deploy_failclosed__a_tunable_is_written_and_typed_by_the_runtime` (`007`, U+0663, U+00B2 refused by shape) | `positive_int_by_isdigit` | — |
| RB-4 | nonblocking | `5e9baae` | a site that fails `caddy validate` stops `edge_install` with exit 4 (`… does not validate with the pinned Caddy; the edge was not changed`), not the old undocumented exit 1. Runbook step 8's exit 4 cell names it | `test_backend_deploy__a_pilot_install_opens_the_edge_only_after_readiness`: the stub fails the maintenance mount; exit 4, no `etc/caddy`, no reload or `run -d` | `validate_after_put` | — |
| RB-5 | nonblocking | `b0f40b7` | the plan-time scan strips `--` and `/* */` comments and matches one statement head per line (`[^;\n]*`). The code states what it misses (a multi-line head, `DETACH PARTITION CONCURRENTLY`, `CREATE DATABASE`); those fail at `apply` with exit 3, rolled back | the history-refusal case: one file whose only `vacuum`/`concurrently` sit in a block comment, an inline comment and a string literal now plans (exit 0); the CIC refusal case is single-line | `comments_scanned`, `scan_spans_lines` | step 8's plans over D's files still pass |
| RB-6 | nonblocking | `e1ca9ca` | wording only. The exit-4 message says statements after the COMMIT in this file ran too and no later file ran; the docstring says a re-apply then fails on the objects already made | (message asserted by prefix) | — | step 8: exit 4 |
| RB-7 | nonblocking | `9d914f4` | `50-install.sh` accepts `MIGRATION_DIGEST` only as 64 hex or exactly `nothing-pending` (exit 2 otherwise); runbook step 8 says so | the cutover case: 64 hex runs install; unset, `nothing-pendng`, `x` and upper-case hex do not | `migration_digest_any_string` | — |
| RB-8 | nonblocking | `7748715` | runbook step 7 and `40-checkout.sh` say the no-engine-restart window is procedural. Nothing masks the unit, and `Restart=always` loops until step 8 (the recovery) or R2 | — (prose) | — | — |
| RB-9 | nonblocking | — | none: the review records `rollback_skips_edge_reload` as killed by the M1 case | — | — | — |
| C-S1 | nonblocking | `7c422ae` | the rehearsal's docker wrapper records every named volume it creates (`$work/volumes`), and teardown removes exactly those. `docker rm -f -v` also removes its containers' anonymous volumes. The `left` check uses the label and the recorded names, not a substring name filter that took a prefix-sharing namespace's volumes | — (rehearsal harness) | — | teardown: `nothing infrx-i2b-fix2-* left`; recorded volumes `infrx-i2b-fix2-caddy_config`, `infrx-i2b-fix2-caddy_data`; afterwards `docker ps -a` and `docker volume ls` on `infrx-i2b-fix2` → 0 and 0 |
| C-S3 | nonblocking | `de068e3` | test only: an earlier backup survives a refused run | `test_deploy_failclosed__a_refused_install_changes_nothing_on_the_host` | `refused_install_rm_all_backups` | — |
| C-S4 | nonblocking | `a3a6195` | test only: the fake curl logs the header file's mode and path. The case asserts `0o600`, a path under its own `TMPDIR`, and an empty `TMPDIR` after the run | `test_backend_deploy__verify_external_never_puts_a_key_on_a_command_line` | `hdr_file_world_readable`, `hdr_file_kept_after_exit` | — |
| C-S5 | nonblocking | `663b956` | secret shape: a credential-named variable (`…KEY|SECRET|TOKEN|PASSWORD|DSN`) assigned a literal, over the steps and the runbook. No current line matches: they read values with `read -rs` or `$(aws ssm …)` | `test_backend_deploy__every_rollout_step_is_strict_bash_that_names_no_secret` | `readme_inline_key_literal`, `step_inline_key_literal` | — |
| C-S6 | nonblocking | `55953c9` | `verify-external.sh` writes response bodies to a `mktemp` file, and `trap 'rm -f "$hdr" "$out"' EXIT` removes both it and the header file. The fixed `/tmp/infrx-verify.body` is gone | covered by C-S4's empty-`TMPDIR` assertion | (`hdr_file_kept_after_exit`) | — |

RB-3 has no commit of its own. Its message sits in the `die` that RB-1 moved into
`rollback.sh`, so it is in `4c2bd2a`.

### Round 2 commands and results (at `7c422ae`)

| # | Command | Exit | Result (quoted tail) |
|---|---|---|---|
| R2-1 | per-item focused runs: `.venv/bin/python -m pytest …` on the touched files and `.venv/bin/python tests/i/mutants.py <names>` for every new or re-anchored mutant | 0 | every listed mutant `killed`. Two harness mistakes were caught before commit: the Host curl stub's escaped newline broke the generated stub (pytest failures, fixed), and `verify-external.sh`'s new body variable collided with the request-body variable (renamed `out`) |
| R2-2 | `timeout 900 uv run --frozen pytest -q -p no:cacheprovider tests/i --ignore=tests/i/test_mutants.py` | 0 | `99 passed in 62.31s (0:01:02)` |
| R2-3 | `uv run --frozen python tests/i/mutants.py --list \| tail -1` | 0 | `172 mutants over 79 named cases` (+13 this round) |
| R2-4 | `REHEARSAL_NS=infrx-i2b-fix2 bash apps/infrx-api/deploy/rehearse.sh /tmp/claude-1000/i2b-fix/rehearsal2` (repo root, background, exit appended to the log) | 0 | `REHEARSAL PASSED`, `teardown: nothing infrx-i2b-fix2-* left`, `EXIT 0`; `grep -c '^PASS'` → `44`, `grep -c '^FAIL'` → `0`; release `7c422ae2d2e9b4ef4b866adfc951fd17204c71c9`, runtime image `sha256:846ce5dd…` |
| R2-5 | `INFRX_MUTANTS=all uv run --frozen pytest -q -p no:cacheprovider tests/i/test_mutants.py` (detached `setsid nohup … > /tmp/claude-1000/i2b-fix/i-list2.log`, started 2026-09-23T03:51:05Z) | 0 | `179 passed in 385.54s (0:06:25)` (finished 2026-09-23T03:57:35Z): 172 mutants, every one `killed`, plus 5 runner self-tests and 2 list checks |
| R2-6 | legacy-first ordering (as command 7 above) | 0 | `128 passed, 2 warnings in 28.84s` |
| R2-7 | track-first ordering (as command 8 above) | 0 | `128 passed, 2 warnings in 28.63s` |
| R2-8 | `make check` | — | **not run** (same scope reason as command 9); not run is not a pass |

Rehearsal R2-4, quoted:

```
=== 0. the box: pinned images, one network namespace
release 7c422ae2d2e9b4ef4b866adfc951fd17204c71c9
runtime image sha256:846ce5ddf49238f54a8f0214bc7335285d690bddc01578887d8990c8e665c75b
image python 3.12.14 uid 10001
=== 1. fresh dev deploy through install.sh (real preflight probe in the real image)
PASS install.sh dev exits 0 (got 0)
PASS INFRX_IMAGE in the env file is the image the probe ran in
gateway container: user=10001:10000 ro=true capdrop=[ALL] secopt=[no-new-privileges] mem=8589934592 pids=512 image=sha256:846ce5ddf49238f54a8f0214bc7335285d690bddc01578887d8990c8e665c75b
PASS the gateway runs the pinned image as 10001, read-only, no capabilities
=== 2. calls on the deployed path (loopback, legacy key: dev mode)
PASS GET /v1/models 200 (got 200)
PASS chat without a key is 401 (got 401)
PASS chat with a wrong key is 401 (got 401)
PASS chat with the key reaches the engine: 200 (got 200)
=== 3. fail-closed drills against the running deploy
PASS a denied SSM read: exit 2 (got 2), env/units byte-identical, gateway not restarted
PASS a throttled SSM read: exit 2 (got 2), env/units byte-identical, gateway not restarted
PASS a mistyped tunable: exit 2 (got 2), env/units byte-identical, gateway not restarted
PASS a tunable the runtime cannot read: exit 2 (got 2), env/units byte-identical, gateway not restarted
PASS an engine sequence count of 0: exit 2 (got 2), env/units byte-identical, gateway not restarted
PASS the repository's pilot today (refused before the probe: engine pin, W3 record, disk): exit 2 (got 2), env/units byte-identical, gateway not restarted
PASS the in-image pilot probe refuses: ROUTERS not composed (G2), worker entry absent (W3)
PASS no mode: exit 2 (got 2), env/units byte-identical, gateway not restarted
=== 4. the pilot-only index unit (pinned Valkey, loopback, read-only, no persistence)
PASS valkey answers PONG on 127.0.0.1 (got PONG)
valkey container: user=999:1000 ro=true capdrop=[ALL]
PASS valkey runs as 999, read-only, no capabilities
=== 5. the edge: real Caddyfile, pinned Caddy, in the box (plain HTTP address, no ACME)
PASS public /health is exactly {"ok":true}
PASS the admin API does not answer on the shared loopback :2019 (got ERR URLError)
PASS /metrics is 404 not_found at the edge
PASS /readyz is 404 not_found at the edge
PASS /internal/x is 404 not_found at the edge
PASS an authenticated call through the edge is 200 (got 200)
97 MiB body -> HTTP/1.1 413 Request Entity Too Large sent 1 MiB request_too_large
PASS a body over MAX_REQUEST_BYTES is 413 request_too_large at the edge
PASS a normal body still passes the edge after it (200)
PASS the gateway down is public health {"ok":false} 503, nothing else (503 {"ok":false} )
=== 6. drain: maintenance at the edge first, then the runtime stops; resume after readiness
PASS maintenance answers 503 dependency_unavailable
PASS the gateway is stopped
PASS maintenance survives a Caddy restart (health: 503 {"ok":false} )
PASS resumed: public health up again (200 {"ok":true} )
=== 7. R2 in runbook order: pause, a second deploy (MAX_ACTIVE_JOBS=4), ENGINE=restart rollback.sh to the first, resume
PASS second deploy exits 0 (got 0)
PASS the second env carries MAX_ACTIVE_JOBS=4
PASS 90-revert's rollback restarts the engine, healthy, before the gateway (restart marlin2b-vllm restart marlin2b-gateway )
PASS the env file is the first deploy's again
PASS the gateway restarted onto it and is ready
PASS rollback.sh restored the edge as it was backed up - maintenance, through the admin socket (503 {"ok":false} )
PASS drain.sh resume (90-revert's last line) reopens the edge (200 {"ok":true} )
=== 8. migrate.py against the pinned supabase/postgres (own network, no host port)
PASS apply with an unreviewed digest refuses: exit 2 (got 2), history 0001,0002
PASS a failing migration rolls the whole plan back: exit 3 (got 3), history 0001,0002
PASS no table from 0003-0010 survived the rollback (got t)
PASS a history the files cannot explain (0002 missing) refuses: exit 2 (got 2)
PASS the reviewed plan applies: exit 0 (got 0), history 0001..0009
PASS a second plan has nothing pending (nothing pending)
PASS a migration with its own COMMIT stops the plan: exit 4 (got 4), history still 0001..0009
=== result
REHEARSAL PASSED
teardown: nothing infrx-i2b-fix2-* left
```

Artifacts (scratch, not durable):

```
4e971862892d1565f9128ad13fb23c8eab2e235a6f77319a7b2d3bb042f4715a  /tmp/claude-1000/i2b-fix/rehearsal2.log
686377c91f616d20dd80bce738c82466a5c775598fbd4515a22a413e58ac3959  /tmp/claude-1000/i2b-fix/i-list2.log
```

### Round 2 limits and operator changes

- **Limit 18 is changed.** On R2 the engine is restarted onto its restored unit and must be
  healthy before the gateway restarts. Exit 4 from `90-revert` now means the engine or the
  gateway did not come up. The edge stays in maintenance, and the runbook says to fix it and
  then run `drain.sh resume`.
- **The fake engine does not reproduce the monolith's `/health`-asks-the-engine semantics.**
  The Host case models them with a curl stub; the rehearsal shows only the restart order.
  The dependence itself comes from reading `infrx/gateway/routes/health.py` at `d18177e`
  (the reviewer's evidence) and was not observed on the box.
- **The M6 scan is a heuristic.** The code names what it misses; each miss fails at `apply`
  with exit 3 and is rolled back.
- **New operator-visible exit.** `edge_install` exits 4 on a site that does not validate
  (was 1).
- **Rehearsal teardown.** It now reads `$work/volumes`, so a work directory reused across
  runs removes the union of their volumes. All of them are this namespace's.

Integration requests: unchanged from the section above (1–11).

### Round 2 verification log

- 2026-09-23: Round 2 appended from the commands transcribed above. Everything was local:
  Docker containers `infrx-i2b-fix2-*` only, all removed, and the image tag
  `infrx-i2b-fix2-runtime:<release>` removed afterwards. No AWS call, no SSM command,
  nothing run against the pilot host or hosted Supabase.
