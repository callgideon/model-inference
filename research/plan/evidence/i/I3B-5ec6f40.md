# I3B fix round — review 48d3bb2 (RS-1..RS-8, M1..M6, D1..D5)

## Task and status

- **Task:** the I3B review fix round (`research/plan/evidence/i/I3B-review-48d3bb2.json` on
  `claude/backend-impl`): two confirmed BLOCKING items (RS-1, RS-2) and 17 nonblocking.
- **Status: implemented (software half). Not integrated.** Every item is closed in its own
  commit with a killing case and a mutant where it is behaviour; the documentation-only
  items (RS-5, D2, D4, D5) say so. Box and hosted drills remain coordinator operations
  (P-18); the five PENDING drills are unchanged (G1R/G2, D2/D3, M3, W3/I2B, I2B).
- Nothing deployed; no AWS call, no box, no hosted Supabase. Docker only in this lane's own
  compose project **`infrx-i3b-fix`** (ports 562xx) and the runbook dry run's
  `infrx-i3b-restore` (127.0.0.1:55470) and `infrx-i3b-fix-pgclient-*` (`--rm`), all removed.
- The earlier report `I3B-32f94b3.md` is kept as history; this file supersedes its
  requirement table where rows changed (rc06 wording: D2).

## Source

| Field | Value |
|---|---|
| Base SHA | `5552c405f030f83b873c6af0582e445fc257af8e` |
| Reviewed head | `48d3bb2` (impl `32f94b3`) |
| Fix implementation SHA | **`5ec6f40`** |
| Branch / worktree | `codex/i3b-recovery-observe` in `.claude/worktrees/codex-i3b` |
| Integrated SHA | none (coordinator) |

## Per-item table

| Item | Commit | Change | Killing case | Mutant(s) | Mutant result |
|---|---|---|---|---|---|
| **RS-1** (blocking) | `424e982` | `pgrestore.restore()` calls `refuse_live_target()` before any write: refuses a target whose `host/port/user/dbname` equals the source recorded in `meta.json` at dump time (`user` included: hosted projects share the regional pooler host), and a target with an `infrx` schema, a `public` table or an auth row | `bk01e_a` (0001-0009 live-shaped target, 0 auth rows: raises, `pg_default_acl` and `auth.users` identical before/after), `bk01e_b` (empty own source: raises, unchanged) | i3bm46 (emptiness guard), i3bm47 (identity guard) | i3bm46 killed, i3bm47 killed |
| **RS-2** (blocking) | `f203415` | `bk01f[family]`: a good restore is the template of a copy damaged in one family (drop policy / column default / revoke function grant / drop index / widen a default privilege / change one row); `compare()` must name exactly that family. **Measured:** the reviewer's `public` default-privilege grant to anon is a no-op (the source's `public` defaults already grant anon ALL, restored as-is), so the case widens `infrx`'s instead | `bk01f[policies,columns,functions,indexes,default_acls,rows]` | i3bm60-63 (drop `policies`/`functions`/`indexes`/`default_acls` from `CATALOG`), i3bm64 (row md5 -> constant) | i3bm60 killed, i3bm61 killed, i3bm62 killed, i3bm63 killed, i3bm64 killed |
| RS-3 | `c7a3fb6` | `CATALOG` compares `t.tgenabled` and `p.proconfig` | `bk01f[triggers]` (disable trigger user), `bk01f[functions_config]` (search_path pin changed) | i3bm65, i3bm66 | i3bm65 killed, i3bm66 killed |
| RS-4 | `13c3b0d` | `run()` drops `DETAIL:`/`CONTEXT:` lines (key values, whole rows) from re-raised stderr; restore.md A4 states `LOCAL`'s `password=` as the one deliberate non-secret exception (check needs two passwords; `PGPASSWORD` holds hosted's) | `rb06` | i3bm55 | i3bm55 killed |
| RS-5 | `fe50aca` | `.gitignore`: `*.dump`, `infrx-backups/`; restore.md A9 retention (kept until A8 is verified and logged, then `rm -rf`; longer = logged coordinator decision) | - (config/docs; `git check-ignore` confirms both patterns) | - | - |
| RS-6 | `917e2cf` | rollback.md maintenance SQL wrapped in `set role service_role;` ... `reset role;` | `rb03` (order pinned) | i3bm56 | i3bm56 killed |
| RS-7 | `c0d5ed7` | `fingerprint()` raises unless `show transaction_read_only` = on | `bk01_a` | i3bm48 (drops the SET; the reviewer's rv14) | i3bm48 killed |
| RS-8 | `88fe062` | `i3b/runbook-dryrun.sh` sources restore.md's own A3-A9 `bash` blocks (extracted at run time) against a hosted-shaped stand-in; `i3b/runbook-dryrun.log` its output. The run found A9's new removal line exited non-zero on success; fixed in the same commit | the committed log (exit 0) | - | - |
| M1 | `622b6c1` | a failed source's previous samples stay in the state; a first-run failure writes no state | `ob14` | i3bm26, i3bm27 | i3bm26 killed, i3bm27 killed |
| M5 | `df3d803` | `--max-age` (older file source = `ScrapeFailed`); an exposition with no sample = `ScrapeFailed` | `ob15` | i3bm28, i3bm29 | i3bm28 killed, i3bm29 killed |
| M3 | `e135ae3` | `Registry.clear(family)`; `collect_gpu`/`collect_disks` clear before re-reading | `ob08` (no GPU series after failure - none frozen, none fabricated; no bytes for a mount that failed after answering) | i3bm35, i3bm36, i3bm37 (the reviewer's rv13) | i3bm35 killed, i3bm36 killed, i3bm37 killed |
| M4 | `48806c4` | PlatformFailureRate status carries `⚠️ TO BE VERIFIED (P-18)`; `ob10` enforces exact-or-⚠️. Minimum denominator not added (the threshold itself is unmeasured) | `ob10` | i3bm38 (the reviewer's rv06b) | i3bm38 killed |
| M2/M6 | `3b34147` | `Registry(process, mounts=...)`: mount names validated at construction, only configured mounts accepted (route builds it from `rt.metrics_disks`); docstring says which labels are enums, which configuration, which (gpu index, revision) patterns | `ob16` | i3bm70-74 (i3bm72-74 = the reviewer's rv11/rv12/rv04b) | i3bm70 killed, i3bm71 killed, i3bm72 killed, i3bm73 killed, i3bm74 killed |
| D1 | `56c5f62` | `reconcile()`: settled debit == `price_snapshot.debit(usage)` | `rc01` | i3bm34 (the reviewer's rv15) | i3bm34 killed |
| D2 | `9b8e8d6` | rc06 docstring: the index is lost BETWEEN worker runs (a lease across the kill is rc09's). Evidence row, corrected: "rc06 index - SIGKILL of the Valkey between worker runs; the next claim after the kill fails loudly" | - (wording) | - | - |
| D3 | `232db86` | rc05b: any class in an `infrx.media` module with `head/get/put_if_absent` (not the Protocol, not the in-memory double); rc08b: also a console script into `infrx.worker` or a worker module with a `__main__` guard | `rc05b`, `rc08b` (still PENDING today) | i3bm39 (a `MinioObjectStore`), i3bm57 (a script guard in `worker/loop.py`) | i3bm39 killed, i3bm57 killed |
| D4 | `6637cb8` | restart.md: the box worker drill SIGKILLs the process (`systemctl kill -s KILL`), with pass condition and ⚠️ | - (docs) | - | - |
| D5 | `5ec6f40` | reconcile.md: tell a released hold from a new terminal job by its original `settled_at` until D3/D5 return a distinct record; header states D1's invariant | - (docs) | - | - |

None of the 19 was skipped as another lane's. `.gitignore` (RS-5) is outside the brief's owned
paths; it was edited because the fix list named it - the coordinator may move it.

## Environment

| Item | Value |
|---|---|
| Host | `Linux 7.0.0-1010-aws x86_64`, local development box, classification **local** |
| Python | 3.12.3 (the worktree's `make api-env` venv) |
| Docker / Compose | 29.6.2 / v5.3.1 |
| PostgreSQL / Valkey | `supabase/postgres@sha256:7768d0d1…` (PostgreSQL 17.6) / `valkey/valkey@sha256:d2e18f34…` |
| Namespace | a copy of the worktree at `5ec6f40` in `/tmp/claude-1000/i3b-fix/tree`, re-namespaced by `sed` over `tests/integration/**` and `pgrestore.py` only: `infrx-e2`->`infrx-i3b-fix`, `infrx_e2`->`infrx_i3bfix`, `infrx-e3b`->`infrx-i3b-fix-e3b`, `infrx-i3b-pgclient`->`infrx-i3b-fix-pgclient`, ports 555xx->562xx (postgres 56232, valkey 56279). No other file differs from the commit |
| Credentials | local literals only, via `PGPASSWORD` |
| Privilege | `rc07` mounts a tmpfs with passwordless `sudo -n` |

## Commands and results

All UTC 2026-09-23, in the namespace copy.

| # | Command | Exit | Result (tail) |
|---|---|---|---|
| 1 | `TMPDIR=<empty> pytest -q tests/integration/backend/recovery -rs` (layer 1: stack state hidden) | 0 | `26 passed, 26 skipped` - 21 "no infrx-i3b-fix stack", 5 PENDING (`[G1R,G2]`, `[D2,D3] 1 infrx RPCs are still infrx.unimplemented stubs`, `[M3]`, `[W3,I2B]`, `[I2B]`) |
| 2 | `tests/integration/run.py --layer 2 --keep` | 0 | preflight, services (`infrx-i3b-fix-{clickhouse,postgres,s3,valkey}`, PostgreSQL 17.6), migrate 0001-0009, rls `cases 40, failed null`; teardown skipped (`--keep`) |
| 3 | `pytest -q tests/integration/backend/recovery -rs --junitxml=…` (stack up) | 0 | **`47 passed, 5 skipped`** - the 5 are PENDING `[G1R,G2]`, `[D2,D3] 11 infrx RPCs are still infrx.unimplemented stubs`, `[M3]`, `[W3,I2B]`, `[I2B]` |
| 4 | `mutants_i3b.py --layer all --report …` (detached) | 0 | **`{"mutants": 69, "killed": 68, "controls_survived": 1, "not_killed": 0, "pending": 0, "problems": null}`** (41 earlier + 28 new; layer-2 mutants ran against the live stack, none pending) |
| 5 | `pytest -q tests/integration/backend/recovery/test_observe.py` (worktree, layer 1) | 0 | `16 passed, 2 warnings` |
| 6 | `research/plan/evidence/i/i3b/runbook-dryrun.sh` (STACK=infrx-i3b-fix-postgres) | 0 | A3 dump (`schemas ["public"]`, `auth_tables ["auth.users"]`, 17.6, SHA256SUMS), A5 `restored`, A6 `"equal": true`, A7 0003-0009 applied + legacy statements (`UPDATE 1`, `INSERT 0 1`, `ROLLBACK`), flags `credit_admission f / legacy_usd_admission t / signup_grant f`, `(0 rows)` drift, A9 `backup removed`; full log committed |
| 7 | teardown: `docker compose -p infrx-i3b-fix … down -v`, state file and tree removed | 0 | 4 containers, 3 volumes, network `infrx-i3b-fix_default` removed; `docker ps -a` shows no `*i3b*` container, no `infrx-i3b-fix` volume, no tmpfs mounted, `/tmp/infrx-i3b-fix-state.json` removed |

Measured (junit properties of command 3, `meas. local`): bk01 dump 0.8 s / restore 2.3 s /
327,126 bytes / 53 rows; bk02 backup+restore+check 2.02 s, 45,985 bytes, apply 0003-0009
0.21 s; bk03 PostgreSQL SIGKILL -> connection 1.3 s; rc07 `.part` files left 1. Box/hosted
windows stay ⚠️ (P-18).

## Mutants

The 28 new mutants and the control, from the command-4 report (the other 40 earlier mutants: all `killed`):

| Id | Invariant | Status | pytest summary |
|---|---|---|---|
| i3bc01 | CONTROL: a comment-only edit in the metrics module must SURVIVE | SURVIVED | `16 passed, 2 warnings in 0.62s` |
| i3bm70 | M2: `mount` is the configured set, not any word matching the pattern | killed | `1 failed, 15 deselected in 0.52s` |
| i3bm71 | M2: a mount name outside its syntax is refused at construction | killed | `1 failed, 15 deselected in 0.53s` |
| i3bm72 | M6: a process name outside its syntax is refused | killed | `1 failed, 15 deselected in 0.55s` |
| i3bm73 | M6: an undeclared label is an error, never silently dropped | killed | `1 failed, 15 deselected in 0.54s` |
| i3bm74 | M6: the histogram bounds are the declared ones | killed | `1 failed, 15 deselected in 0.54s` |
| i3bm35 | M3: a lost GPU keeps no frozen memory/utilization series | killed | `1 failed, 15 deselected in 0.59s` |
| i3bm36 | M3: a mount that stops answering keeps no old byte counts | killed | `1 failed, 15 deselected in 0.54s` |
| i3bm37 | M3: a failed nvidia-smi fabricates no memory reading | killed | `1 failed, 15 deselected in 0.55s` |
| i3bm26 | M1: a failed source's previous samples are carried into the state | killed | `1 failed, 15 deselected in 0.57s` |
| i3bm27 | M1: a failure on the first run leaves the next run a first run | killed | `1 failed, 15 deselected in 0.54s` |
| i3bm28 | M5: a file source older than --max-age is ScrapeFailed | killed | `1 failed, 15 deselected in 0.56s` |
| i3bm29 | M5: an exposition with no sample is ScrapeFailed, not silence | killed | `1 failed, 15 deselected in 0.56s` |
| i3bm38 | M4: an unmeasured threshold carries the ⚠️ marker | killed | `1 failed, 15 deselected in 0.43s` |
| i3bm34 | D1: reconcile catches a settled debit that is not price snapshot x usage (a consistent overcharge in ledger and outcome alike) | killed | `1 failed, 11 deselected in 0.52s` |
| i3bm39 | D3: rc05b fails once any class implements the ObjectStore port | killed | `1 failed, 11 deselected in 0.44s` |
| i3bm57 | D3: rc08b fails once any worker module runs as a script | killed | `1 failed, 11 deselected in 0.40s` |
| i3bm46 | RS-1: a restore refuses a non-empty target before any write | killed | `1 failed, 16 deselected in 3.51s` |
| i3bm47 | RS-1: a restore refuses the backup's own source | killed | `1 failed, 16 deselected in 3.01s` |
| i3bm48 | RS-7: the check's session is read-only (A6 reads LIVE hosted) | killed | `1 failed, 16 deselected in 5.06s` |
| i3bm60 | RS-2: the check compares policies | killed | `1 failed, 16 deselected in 5.04s` |
| i3bm61 | RS-2: the check compares functions and their grants | killed | `1 failed, 15 deselected in 4.59s` |
| i3bm62 | RS-2: the check compares indexes | killed | `1 failed, 16 deselected in 4.57s` |
| i3bm63 | RS-2: the check compares default privileges | killed | `1 failed, 16 deselected in 5.48s` |
| i3bm64 | RS-2: the check compares row CONTENT, not only counts | killed | `1 failed, 16 deselected in 5.65s` |
| i3bm65 | RS-3: the check compares whether a trigger is enabled | killed | `1 failed, 16 deselected in 5.68s` |
| i3bm66 | RS-3: the check compares a function's pinned configuration (search_path) | killed | `1 failed, 16 deselected in 5.45s` |
| i3bm55 | RS-4: a failed client's DETAIL/CONTEXT lines (row data) are not re-raised | killed | `1 failed, 5 deselected in 0.31s` |
| i3bm56 | RS-6: rollback.md runs the maintenance switch as service_role, as bk04 | killed | `1 failed, 5 deselected in 0.29s` |

## Failure drill

The two new refusals: a restore pointed at a live-shaped database (bk01e_a) and at its own
source (bk01e_b) raise before the first write; `pg_default_acl` and `auth.users` are
byte-identical afterwards. The damaged-copy drill (bk01f) proves each catalog family and row
content is compared. All earlier drills (rc01-rc10, bk01-bk04) re-ran green in command 3.

## Limits

1. **Not integrated**, as before: no process emits the metrics; the integration requests
   of `I3B-32f94b3.md` stand, amended below.
2. The target guard compares `host/port/user/dbname` textually: `localhost` vs `127.0.0.1`
   for the same server is not detected as the source (the emptiness guard still refuses a
   populated one).
3. `_object_store_adapters()` does not see a module that fails to import (missing optional
   dependency); marked `ponytail:` in the probe.
4. No `make check` / layer-3 gate in this round; the changed files run in commands 1-5.
5. An `infrx-e2-*` stack (created 01:54:51Z today, ownership label = the `codex-i3b`
   checkout, i.e. left by this lane's previous session) was running when this round started.
   One early `pytest` of `test_recovery.py` from the worktree ran rc06/rc08/rc09 against it
   (its Valkey was SIGKILLed and restarted by rc06/rc09, as those drills do); every later run
   used `infrx-i3b-fix` only. It was **not** torn down here: the coordinator decides.

## Handback

### integration_requests (amendments to I3B-32f94b3.md)

- **I2B (request 2):** the alert timer adds `--max-age 180` to the evaluator command.
- **Coordinator (request 1):** construct `Registry("gateway", mounts=<METRICS_DISKS keys>)`
  (`route.register` does this itself from `rt.metrics_disks` when `rt.metrics` is unset);
  worker/reaper registries likewise take their configured mounts.
- **Coordinator:** RS-5 touched the root `.gitignore` (outside the owned paths) as the fix
  list asked; keep or move.
- **Coordinator:** remove or keep the leftover `infrx-e2-*` stack labelled with the
  `codex-i3b` checkout (Limit 5).
- **E (request 8):** the I3B list is now 69 mutants (68 + 1 control) and uses
  `-k "bk01f and <family>"` selectors; the well-formedness case reads the case name before
  ` and `.

## Verification log

- 2026-09-23: Authored from commands 1-7 above; counts quoted from command output, the
  junit file and the mutant report. Status **implemented (software half)**; not integrated.
