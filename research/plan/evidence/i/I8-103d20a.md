# I8 — continuous operations, least privilege, restoration (code/config half) — 103d20a

Lane I8 (consumer v1 wave, program 22), branch `codex/i8-operate`, base `dff31efc` (+ the
coordinator's port-reservation patch `9da9d494`, `git am` as instructed), head **`103d20a8`**.
Brief: [consumer-v1/03 §I8](../../consumer-v1/03-operations-and-verification.md); closes the
code half of RV-09 / RV-10 ([review 21](../../21-v1-consumer-readiness-review-2026-09-24.md)),
test ids OPS-CONTINUOUS / OPS-RECOVER / VERIFY-REPRO. **Nothing here ran against the box,
AWS, S3, SSM or hosted Supabase.** Every live step is a coordinator op, listed at the end,
TO RUN BY COORDINATOR after certification run3.

## Commits (one per slice, fixes separate)

| Commit | Slice |
|---|---|
| `97bca38f` | 1 — pooler budget from the deployed knobs; transaction-pooler semantics on a local PgBouncer |
| `3a5a3319` | 2 — versioned env schema checked before every runtime start; least-privilege probe |
| `860e6cd7` | 3 — continuous monitoring: durable + host truth, canary, merged versioned rules, delivery, timers |
| `248582a9` | 4 — alert delivery proof (marked TEST alert + recovery), BLOCKED on P-25 |
| `597d01bd` | 5 — model mirror with digest manifest, timed restore, backup/PITR read |
| `5ab1db80` | 6 — known-good rollback target by tree + record; the drill proves service, not readiness |
| `103d20a8` | 7 — runbook/deploy integration: ordered ops, drill, restore, policy, allowlist, evidence export, cleanup |
| `ffd6c231`, `cdce81dc`, `af9e1a29`, `955b713f`, `fbe5a81d` | fixes found by the mutation baseline, the mutation runner and the in-image check (below) |

## Changed paths (53 files, +4714 / −13)

- `infra/runbooks/`: `pool_budget.py`, `privilege_probe.py`, `artifacts.py`, `supabase_policy.py`,
  `observe.md` (new); `drift.py` (`--request-id`); `README.md`, `rollback.md`, `restore.md`,
  `rollout.md` (sections added, logs appended).
- `infra/observe/` (new): `durable.py`, `host-probe.sh`, `canary.sh`, `deliver.py`, `rules.py`,
  `observe.sh`, `systemd/infrx-{observe,canary}.{service,timer}`.
- `infra/alerts/operations.json` (new, version 1: 24 rules + the GpuUnavailable override +
  `pending_producers`); `alerts.json` untouched (version 1).
- `infra/rollout/`: `known-good.py`, `known-good.json`, `verify-journey.sh`; steps `71`, `72`,
  `73`, `74`, `79`, `80`, `81`, `85`, `86`; `README.md` log.
- `apps/infrx-api/deploy/`: `preflight.py` (env schema header + `envcheck`),
  `marlin2b-gateway.service` / `infrx-worker.service` (envcheck ExecStartPre), `install.sh` /
  `rollback.sh` (labelled timings), `rehearse.sh` (mini-systemd honours `$$`).
- `apps/infrx-api/tests/i/`: `pooler.py`, `conftest.py`, `test_pooler.py`, `test_envcheck.py`,
  `test_privilege_probe.py`, `test_observe.py`, `test_ops_steps.py`, `test_artifacts.py`,
  `test_rollback_drill.py` (new); `mutants.py` (+59 mutants, layout copies I8's dirs),
  `test_prereqs.py`, `test_rollout.py` (pins updated).

## Commands, exit codes, counts

| Command (from `apps/infrx-api` unless noted) | Exit | Result |
|---|---|---|
| `make api-env` (repo root) | 0 | pinned env |
| `uv run --frozen pytest -q tests/i` at base (before any I8 change) | 0 | 159 passed, 160 s |
| `uv run --frozen pytest -q -rxXs tests/i` at `103d20a8` | 0 | **206 passed, 1 xfailed, 296 s** |
| `python tests/i/mutants.py <44 I8 mutants>` (exported tree `597d01bd`) | 1 | 41/44 killed: `stand_in_pooler_in_session_mode`, `gateway_starts_unchecked` broken_runner (undeclared death), `canary_failure_exits_zero` survived → fixed in `cdce81dc`, `955b713f` |
| same, the 3 + 11 slice-6 + 2 more, exported `955b713f` | 0 | **16/16 killed** |
| same, all **59** I8 mutants, exported `103d20a8` | 0 | **59/59 killed**, 359 s (pristine baseline green) |
| `docker run … infrx-runtime(cdce81dc) python /app/deploy/preflight.py envcheck --mode pilot` on a rendered pilot env / + `DATABASE_POOL_MAX=4` | 0 / 2 | `ok true` / refused naming `DATABASE_POOL_MAX` — in the real image (Python 3.12.14) |
| `docker run … python /repo/infra/runbooks/pool_budget.py --env-file /dev/stdin` (step 71's form) at `cdce81dc` | 1 (traceback) | **defect found**: no `infrx` import in the image (package at `/app`) → fixed `af9e1a29`; then defaults FAIL (21 > 15), `DATABASE_POOL_MAX_SIZE=6` PASS (13 + 2 ≤ 15) in the image |
| `REHEARSAL_NS=infrx-i8-rehearsal deploy/rehearse.sh` | — | **NOT RUN** past step 0: `quay.io/minio/minio@sha256:14cea4…` pull → 401 on this host; the runtime image it built was used for the two in-image checks above; teardown clean |

Test prerequisites (declared): Linux + docker for `test_pooler` (7), `test_privilege_probe`
(1 of 2), `test_observe` durable case (1) — the `infrx-i8-postgres` (55450, database
`infrx_i8`, real schema 0001–0018 + shim) behind `infrx-i8-pgbouncer` (55477) of
`tests/i/pooler.py`; without docker they skip visibly as NOT RUN. `systemd-analyze` for the
unit checks (`test_envcheck`, `test_observe`, existing `test_packaging`). Everything else runs
on stubs (PATH seam). New cases: 39 (pooler 7, envcheck 4 (+9 params), privilege 2, observe
11, ops steps 6, artifacts 4, rollback drill 5).

## Findings (with the failing oracle first)

1. **Pooler budget (slice 1).** `pool_budget.py` computes, from `DATABASE_POOL_*` read by the
   runtime's own `deployment_from_env`, the gateway unit's `--workers` and the DSN's port:
   session peak **21** (gateway 10 + worker 10 + operator CLI 1) + headroom 2 > **15** — the
   2026-09-24 `EMAXCONNSESSION`, reproduced: the stand-in pooler (max 15 session clients)
   refuses the real `pilot.connection_pool` pools at their peak; at `DATABASE_POOL_MAX_SIZE=6`
   the computed peak 13 is admitted whole. Limits: session 15 meas. (Supavisor's text);
   transaction clients 200 = Supabase's documented "Max pooler clients" for Nano/Micro
   ([compute-and-disk](https://supabase.com/docs/guides/platform/compute-and-disk)); the
   project tier is ⚠️ TO BE VERIFIED (est. Nano/Micro from pool_size 15; O8 reads
   `max_client_conn`). Worker demand (8 + 2 + reaper) exceeds 6: the interim needs WR-I8-2's
   wait telemetry; the real fix is the transaction port (WR-I8-1 + D10).
2. **Transaction-pooler semantics (slice 1, F6).** On the stand-in (Supabase documents the same
   for 6543: "Session-level state is lost between transactions … set and reset,
   session-level advisory locks, listen and notify, and temporary tables"; "Transaction mode
   does not support prepared statements",
   [connecting-to-postgres](https://supabase.com/docs/guides/database/connecting-to-postgres)):
   - UNSAFE: `pilot.configure_connection`'s `set role service_role` + `set statement_timeout`
     and `jobstore.connector`'s `set role` — lost for their client (`postgres`, timeout `0`)
     and **leaked to another client** (`service_role`, `15s`); psycopg's default auto-prepare
     (`prepared statement "_pg3_0" does not exist` once served by another server); the composed
     runtime pool fails the same way (characterization case); a session advisory lock (not
     used). Today the `postgres` login's BYPASSRLS hides the role loss (D-31).
   - SAFE: 0011's `pg_advisory_xact_lock` in one statement, `SET LOCAL` in an explicit
     transaction, a login role's `ALTER ROLE … SET statement_timeout` (all exercised); the
     migrations' function-level `set search_path` applies per call by construction (not
     exercised separately). The runtime sends no LISTEN, session advisory lock or temp table
     (pinned by a static case).
3. **Env schema (slice 2).** The runtime silently keeps the default for a mistyped name
   (`DATABASE_POOL_MAX=4` → pool max 10). Files are now written with
   `# INFRX_ENV_SCHEMA 1:<digest>`; `preflight.py envcheck` (unknown/doubled/missing/forbidden/
   withdrawn names, shapes, the schema version, then the runtime's own probe; names only) runs
   in both runtime units' ExecStartPre, in the image, over stdin, `--network none`.
   Cross-version: an older release's install writes its own env and units (no header, no
   check), consistent both ways.
4. **Least privilege (slice 2).** `privilege_probe.py` attempts 17 operations (42501 only
   counts; RLS-only denials reported apart) + identity checks through the pooler. Stand-in
   least-privilege login: PASS; today's `postgres` login, service_role membership and a
   TRUNCATE over-grant: FAIL. Finding for D10: under RLS, a table INSERT privilege is masked
   ("denied (row-level security only)") — the probe says so instead of calling it clean.
5. **Monitoring (slice 3, F4).** **8 of 20 alerts.json rules have no producer at all**
   (ComponentDown, QueueStalled, QueueSaturated, RejectionsHigh, PlatformFailureRate,
   LeaseLost, ReaperTerminalized, UnsettleableJobs) and 3 more (ReconciliationDrift/Stale,
   UnknownUsageBacklog) fire only because `durable.py` now publishes them from PostgreSQL.
   Pinned + strict xfail (flips when W5/G wire them). **GpuUnavailable would page every
   minute** on the box: the gateway container's `collect_host` has no nvidia-smi
   (`infrx_gpu_up{process="gateway"} 0`); `operations.json` overrides the rule to the host
   probe's gauge (WR-I8-4 removes the cause). Durable truth (ready backlog age from the
   outbox, stuck/overdue jobs, stuck holds, overdue unknown usage, GC lag, recent platform
   failures) replaces the rebuildable index / in-process counters where they mislead.
6. **Delivery (slice 4).** BLOCKED: no destination, owner or escalation exists (P-25). The
   proof is scripted (`74-alert-test.sh`, marked `[TEST FIRING] … NO ACTION REQUIRED`, nonce,
   recovery); without P-25 every delivery exits 3 and keeps the alert (never a silent pass).
7. **Artifacts (slice 5, F10).** The manifest refuses a directory that does not serve the
   pinned bytes; the processor files (`processor_config.json`, `preprocessor_config.json`)
   have no pin in serving-version.json — recorded, WR-I8-5. Restore times fetch / verify /
   engine load (cold start) / warm text / warm video / runtime ready / first usable
   separately. Mirror prefix: input (the bucket is another project's).
8. **Rollback (slice 6, F7).** `known-good.py --list --applied 0018`: 27af05a NOT-KNOWN-GOOD
   (no preparation loop, recorded not known-good), 4226315 and bda1586 KNOWN-GOOD (tree + record;
   the bundle check needs the coordinator's `--bundles`). The drill proves a fresh public
   in-cap video job + result + authoritative usage + `drift.py --request-id` SETTLED after
   the rollback **and** the roll-forward; the public 503-after-readiness is a named check;
   install/rollback print readiness-only and cold-start timings apart.
9. **Monitor independence (slice 3 fix).** Units that ran from the runtime checkout would
   go blind in the rollback drill (bda1586 has no `infra/observe`): they run from a pinned
   copy `/opt/infrx/observe`.

## Wiring requests (not applied; owners named)

- **WR-I8-1** (coordinator composition + D10): `infrx/gateway/pilot.py` — pool
  `kwargs={"autocommit": True, "prepare_threshold": None}`, the probe connection
  `AsyncConnection.connect(..., autocommit=True, prepare_threshold=None)`,
  `configure_connection(statement_timeout_ms, *, session_state=True)` doing the two SETs only
  when `session_state`, and `connection_pool` passing `session_state=urlsplit(dsn).port != 6543`;
  `infrx/state/jobstore.py connector` — `prepare_threshold=None`, `set role` only off 6543.
  Proof: `tests/i/test_pooler.py::…the_composed_runtime_pool_breaks_on_the_transaction_pooler_today`
  must then be rewritten to "every answer has one identity and a timeout" with a login role
  carrying `statement_timeout` (the stand-in `infrx_i8_login` of the same file shows it holds),
  and `KNOWN_SESSION_SET` shrinks. Until then: stay on 5432 with `DATABASE_POOL_MAX_SIZE=6`.
- **WR-I8-2** (metrics module owner): families `infrx_db_pool_connections{state=size|available|max}`
  (gauge), `infrx_db_pool_requests_waiting` (gauge), `infrx_db_pool_requests_total`,
  `infrx_db_pool_wait_seconds_total`, `infrx_db_pool_timeouts_total`,
  `infrx_db_pool_connection_errors_total`, `infrx_db_pool_connections_lost_total` (counters),
  and `record_pool(reg, stats)` fed at each scrape from `pool.pop_stats()` (psycopg_pool:
  `pool_size, pool_available, pool_max, requests_waiting, requests_num, requests_wait_ms,
  requests_errors, connections_errors, connections_lost`) in the gateway's `/metrics` route
  and the worker's. Proof: `tests/i/test_observe.py` pinned case fails "a pending producer
  landed" → I8 moves the names out of `operations.json` `pending_producers`; then add the
  dashboard panels.
- **WR-I8-3** (G): `infrx_large_body_in_use` / `infrx_large_body_limit` gauges at scrape from
  the gateway's `LargeBodies` (in use, `LARGE_BODY_LIMIT`). Same proof path.
- **WR-I8-4** (metrics/route owner): `infrx/observe/route.py` —
  `await asyncio.to_thread(collect_host, rt.metrics, disks, gpu=False)` (no GPU in the gateway
  container). Then I8 removes the GpuUnavailable override; `test_observe` GPU case keeps
  passing (host gauge), the E-track `test_observe.py` FAULTS set the gauge directly.
- **WR-I8-5** (W): `models/marlin2b/serving-version.json` `model.processor_config_digest`,
  `model.preprocessor_config_digest` (served bytes; the O9 manifest measures them), then I8
  adds both to `artifacts.py` `PINNED`.
- **WR-I8-6** (D10): (a) the runtime login — LOGIN, no membership in postgres/service_role,
  `ALTER ROLE … SET statement_timeout = '15s'`, only D10's grants — and its function list as
  a file for `privilege_probe.py --allow-functions`; (b) a read-only monitor login
  (`default_transaction_read_only = on`) able to read what `durable.py` reads
  (`infrx.jobs` state/admitted_at/queued_at/updated_at/deadline_at/settled_at/outcome_cause/
  result_expires_at/request_id, `infrx.outbox` kind/acknowledged_at/claimed_at/available_at,
  `infrx.credit_holds` state/reconcile_after/request_id, `infrx.stream_chunks` expires_at,
  `infrx.job_results` request_id, the two reconciliation views) — or one SECURITY DEFINER
  `infrx.ops_snapshot()` returning those counts, which durable.py would call instead.

## Proposed rulings (next free R109)

- **R109 — service, not readiness.** A rollback, restore or replacement passes only when a
  fresh public in-cap video job is accepted, succeeds, returns content with authoritative
  usage and settles with no wallet drift, after the change and after its reversal. `/readyz`
  timings are labelled readiness-only and never reported as a cold start. A rollback target
  is chosen by its tree (preparation loop, applied migrations, config schema), its recorded
  evidence and its release bundle — never by the presence of a backup directory.
- **R110 — pooler discipline.** An install's knobs must PASS `pool_budget.py` for the pooler
  mode the runtime uses; runtime connections on the transaction pooler carry no session
  state (no SET/RESET, session advisory locks, LISTEN, temp tables) and no server-side
  prepared statements; migrations and dumps use the session port inside a maintenance window.
- **R111 — the env file is versioned.** A runtime unit starts only on an env file written for
  its image's schema (`# INFRX_ENV_SCHEMA`), with no name outside it.

## Open issues

- P-25 (alert destination/owner/escalation; the mirror prefix approval; a Supabase access
  token for the policy read), D10 (runtime + monitor logins, function list), P-24 (canary
  spend: 288 tiny requests/day on the canary tenant; the drills' windows) — each blocks only
  its op below.
- The 8 unproduced alerts.json rules (W5/G). `ResultContentPastExpiry` will fire until D10/M6
  content cleanup (RV-03/RV-11) — truthful, not noise.
- `durable.py` uses the runtime login (moved to 6543) until D10's monitor login; read-only is
  enforced per transaction. The canary defaults to the certify/E1B key; a dedicated canary
  tenant is better. The observe units run as root (docker, systemctl, nvidia-smi).
- PgBouncer stands in for Supavisor: O7 `--pooler-semantics` observes the hosted 6543.
- `verify-journey.sh` is tested against a stub API (shape from `wire.JobAccepted/JobStatus/
  JobResult`); its first real run is O12.
- The rehearsal (G5) cannot run on this host (minio pull 401): the coordinator's G5 covers
  the ExecStartPre change end to end.

## Remaining effort (I8 as a whole)

Optimistic 4 h / likely 8 h / pessimistic 16 h, confidence **medium**. Basis: the code half is
done and tested; left are the coordinator's ops O1–O14 (two maintenance windows ≈ 1 h each
on the box, the rest minutes), the WR follow-ups on I8's side (move pending producers, drop
the GPU override, extend PINNED, rewrite the characterization case: ≈ 1–2 h), and inputs
(P-25, D10) whose timing is outside the lane.

## Coordinator ops, in order (TO RUN BY COORDINATOR, after certification run3)

Host ops from the repository root at the I8 release; box ops are
`infra/rollout/ssm.sh infra/rollout/steps/<step> NAME=VALUE…`, one per command, each logged
first. Mirror of `infra/runbooks/rollout.md` §4 (O1–O14).

1. host `apps/infrx-api/.venv/bin/python infra/runbooks/pool_budget.py --runtime-port 5432` (FAIL expected) and `… --set DATABASE_POOL_MAX_SIZE=6` (PASS).
2. host `apps/infrx-api/.venv/bin/python infra/rollout/known-good.py --list --applied 0018 --set S3_MEDIA_BUCKET --set MAX_VIDEO_SECONDS --set WORKER_CONCURRENCY --set LARGE_BODY_LIMIT --set DATABASE_POOL_MAX_SIZE --bundles s3://llm-bootcamp-641134885443/releases/`
3. the I8 release through rollout.md W1–W13 with `INFRX_SET="S3_MEDIA_BUCKET=llm-bootcamp-641134885443 MAX_VIDEO_SECONDS=82 WORKER_CONCURRENCY=8 LARGE_BODY_LIMIT=8 DATABASE_POOL_MAX_SIZE=6"`, `MIGRATION_DIGEST=nothing-pending`.
4. `infra/rollout/ssm.sh infra/rollout/steps/71-pool-budget.sh`
5. `infra/rollout/ssm.sh infra/rollout/steps/72-observe-install.sh RELEASE=<I8 release> CANARY_VIDEO=<in-cap clip on the box>` (+ `ALERT_WEBHOOK_PARAM=… ALERT_OWNER=… ALERT_ESCALATION=…` when P-25 exists; + `MONITOR_DSN_PARAM=…` when D10's login exists)
6. `infra/rollout/ssm.sh infra/rollout/steps/73-observe-status.sh`
7. `infra/rollout/ssm.sh infra/rollout/steps/74-alert-test.sh` → owner confirms nonce → `infra/rollout/ssm.sh infra/rollout/steps/74-alert-test.sh RESOLVE=<nonce>` (BLOCKED until P-25)
8. host `read -rs PROBE_DATABASE_URL; export PROBE_DATABASE_URL; apps/infrx-api/.venv/bin/python infra/runbooks/privilege_probe.py --role postgres --pooler-semantics` (baseline FAIL expected; again with D10's login + `--allow-functions` → PASS)
9. host `read -rs SUPABASE_ACCESS_TOKEN; export SUPABASE_ACCESS_TOKEN; apps/infrx-api/.venv/bin/python infra/runbooks/supabase_policy.py`
10. `infra/rollout/ssm.sh infra/rollout/steps/80-mirror-artifacts.sh RELEASE=<I8 release> MIRROR_URL=<approved prefix>`
11. `infra/rollout/ssm.sh infra/rollout/steps/81-restore-artifacts.sh RELEASE=<I8 release> MIRROR_URL=<prefix> MODE=fetch CLEANUP=1`
12. window: `TIMEOUT_S=3600 infra/rollout/ssm.sh infra/rollout/steps/81-restore-artifacts.sh RELEASE=<I8 release> MIRROR_URL=<prefix> MODE=swap`; host `infra/rollout/verify-journey.sh`; host `apps/infrx-api/.venv/bin/python infra/runbooks/drift.py --request-id <id>`
13. window, rollback drill ([rollback.md](../../../../infra/runbooks/rollback.md#known-good-rollback-drill)): `infra/rollout/ssm.sh infra/rollout/steps/85-known-good-box.sh TARGET=bda15866e5700f3856d7142580da842fba9bbd23`; `infra/rollout/ssm.sh infra/rollout/steps/30-pause.sh RELEASE=bda15866e5700f3856d7142580da842fba9bbd23`; host `EXPECT=maintenance infra/rollout/verify-journey.sh`; `infra/rollout/ssm.sh infra/rollout/steps/40-checkout.sh RELEASE=bda15866e5700f3856d7142580da842fba9bbd23`; `TIMEOUT_S=3600 infra/rollout/ssm.sh infra/rollout/steps/50-install.sh RELEASE=bda15866e5700f3856d7142580da842fba9bbd23 MIGRATION_DIGEST=nothing-pending ENGINE_MAX_NUM_SEQS=8 INFRX_SET="<as op 3>"`; host `infra/rollout/verify-journey.sh`; host `drift.py --request-id <id>`; then the same four box ops + two host ops with `RELEASE=<I8 release>`; `85-known-good-box.sh TARGET=<I8 release>`.
14. `infra/rollout/ssm.sh infra/rollout/steps/79-evidence-export.sh`
15. `infra/rollout/ssm.sh infra/rollout/steps/86-cleanup.sh` then, logged, `infra/rollout/ssm.sh infra/rollout/steps/86-cleanup.sh DRY_RUN=0` (+ `RESTORE_ID=<id>` after 12)

## Mutation final

`tests/i/mutants.py`, the 59 I8 mutants (slices 1-7), run from `git archive 103d20a8` with the
worktree's pinned interpreter: `59/59 killed`, `EXIT=0 WALL=359`. The default
`tests/i/test_mutants.py` subset is part of the 206-pass suite run; the full I list
(`INFRX_MUTANTS=all`, 296 mutants) was not run by this lane.
