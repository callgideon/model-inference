# U1 — Usage and promotional balance views

## Task and status

- **Task:** U1 (track U, oracle CONSOLE-FLOWS), session: Claude Opus 5 implementation agent.
- **Status: implemented.** The pages and their view models are complete and green against the
  fixture-backed `ConsoleServices` fake. **Not integrated:** no real service, database, cloud or paid
  provider was contacted, and no console server action exists (C1 owns them). Per the handoff, U1
  integrates after C1 and D5.

## Source

- Base SHA: `8744418` (`plan: F2.1 integrated (a666680); wave 2 dispatched`)
- Implementation SHA: **`9bef497`**
- Branch: `codex/u1-usage-balance-ui`, worktree
  `/home/rey/workspace/rey/code/model-inference/.claude/worktrees/codex-u1`
- Integrated SHA: none yet (coordinator integrates into `claude/infrx-impl`)

Commits:

```
9bef497 U1: mutation runner for the view models (R32), 24 mutants, 24 killed
6bb4c28 U1: usage and balance pages rendered from the view models
9a5a619 U1: usage and promotional-balance view models with node:test cases
```

## Requirement coverage

All logic is in pure `.ts` view models with relative imports (R48); the `.tsx` files hold markup
only. Test IDs are the case names in `apps/app/tests/u/`.

| Test ID | Exact invariant demonstrated |
|---|---|
| U1-T01 | The URL is untrusted input: an unrecognised `range` falls back to the default rather than producing an empty page; `all` means "no filter", not a filter value `"all"`; a repeated parameter yields its last value, never an array in a query; an empty string is not a cursor. |
| U1-T02 | A filter + pagination state serializes and parses back identically, including the cursor trail; default values are absent from the URL (page 1 unfiltered is the bare path `/usage`); `ledgerHref` does the same for `/billing`. |
| U1-T03 | Every field the page sends is a `USAGE_QUERY_FIELDS` name (an unknown field is `invalid_request`); the summary query is the whole scope (no `cursor`, no `limit`) while the page query carries both; `to` is the injected clock and `from` is `to` minus the selected range; the `key_id` and `model` filters reach the service; the fake accepts both queries. |
| U1-T04 | A keyset walk at the console page size visits every usage row of the scope exactly once, in the same order as a walk at the contract maximum (no row dropped or repeated at a page boundary — the fixture seeds timestamp ties there); the cursor trail makes every visited page reachable *backwards* with identical ids; `pageNumberOf` tracks the walk; stepping back from page 1 stays on page 1; `firstCursorState` returns to page 1. |
| U1-T05 | A cursor is bound to its filters, so changing one discards cursor and trail; the reset state is accepted; and the reason is shown to be real — the same cursor with a changed `model` is refused `invalid_cursor`, for which the page offers a restart, not a retry. |
| U1-T06 | `null` is loading, an ok-but-empty page is empty (not an error and not a row of zeroes), rows are ready, and a failure is an error carrying the code, a safe message and a recovery: retry for 429/500/503/504, restart for `invalid_cursor`, none for `forbidden`/`invalid_request`/`not_found`/`org_suspended`. Failure drill: after an injected `dependency_unavailable`, the retry succeeds and returns exactly what the failed call would have. |
| U1-T07 | **A hold is never a charge.** Only a `settled` row puts a non-zero amount in the charged column; an unsettled or `held_unknown` row shows `$0.00` charged with its `max_hold` in a separate Held column, and the two are never the same string; released and platform-absorbed rows show no charge and no hold. |
| U1-T08 | **Estimated tokens are never shown as usage.** `usage_certainty: "unknown"` renders `—` with an explanation even when token counts are present in the row; an authoritative row renders the counts. |
| U1-T09 | The four settlement states plus "not terminal yet" are five distinct labels with distinct explanations; the held explanation says "not a charge" and the platform-absorbed one says "not charged". |
| U1-T10 | The summary's charged figure is `displayMoney(summary.cost)` and the pending figure is `displayMoney(summary.pending_reconciliation)` — different figures, differently labelled, the pending one marked "not a charge"; the fixture has both non-zero and unequal, plus platform-absorbed requests. |
| U1-T11 | Money keeps all eight fractional digits through display (`$0.00000001`, `-$0.00012345`), a daily cost is formatted from `Money` and not a float, and a timestamp renders as UTC to the minute regardless of the server locale or timezone. |
| U1-T12 | The model filter offers the models present, sorted and unique, and always keeps the active selection selectable. |
| U1-T20 | The balance card shows the wallet's three figures — available, promotional credit (ledger total), reserved — each from its own field, with available emphasised; the fixture has outstanding holds so the three are distinct. |
| U1-T21 | `available = ledger_total - reserved` (the contract identity) holds for the fake's wallet, is checked to one unit of 1e-8 USD, and a wallet that disagrees is reported rather than rendered silently. |
| U1-T22 | Three balance states, not two: a new organization with no grants and no ledger history gets operator-grant guidance ("ask an operator", "no card / nothing to buy"); a spent-out or over-reserved wallet gets the exhausted state; a funded wallet gets neither. A zero total *with* history is not a new organization. |
| U1-T23 | No customer-facing balance string offers payment or calls promotional credit revenue (`add credits|add card|top up|buy|purchase|invoice|checkout|revenue|pay now`), and the notice says explicitly that it is promotional and not a cash balance. |
| U1-T24 | A credit renders with a leading `+`, a debit keeps its `-`, zero gets neither, and the eighth digit survives. |
| U1-T25 | All four ledger kinds render, including the legacy `purchase` (R13); an operator-made grant reads `platform` from the service and is labelled "infrx platform" with nothing resembling a principal (R41); a grant's reason is customer-visible text shown verbatim; a usage debit has no actor and no reason. |
| U1-T26 | The ledger walks forward on its own cursors with no entry twice, every page is reachable backwards, the link a Previous control produces parses back to the same state, and an injected ledger failure becomes a retryable error state. |

Not covered here, by instruction: browser-level keyboard and focus behaviour (E's e2e suite — see
Handback), and anything requiring a real service.

## Environment

- Linux 7.0.0-1010-aws, local development worktree. **Classification: local, fixtures only.** No
  staging, no production, no cloud call, no Docker container started, no paid provider.
- Node v22.23.1 (type stripping, no build step for tests), pnpm 9.15.9, Next 16.3.5, TypeScript 5.
- Console dependencies installed from the committed lockfile before the session
  (`pnpm install --frozen-lockfile`); no dependency added or changed.
- Data: `apps/app/lib/contracts/fixtures/*.json` through `createFakeConsoleServices()`. The fixture
  clock is frozen at `2026-09-20T12:00:00.000Z`; tests inject it as `NOW` and the pages read it from
  the seam, so nothing in U1 depends on wall-clock time. No seed or randomness.
- Python environment: not used. `make api-env` / `make api-test` were **not run** — U1 changed no
  Python file.

## Commands

UTC 2026-09-21T06:17Z – 06:19Z, run from the worktree root (canonical `make` targets, R43/S1).

| Command | Exit | Result |
|---|---|---|
| `make console-test` | 0 | `# tests 145 / # pass 145 / # fail 0 / # skipped 0` (126 before U1, +19 U1 cases) |
| `make console-lint` | 0 | `✖ 2 problems (0 errors, 2 warnings)` — both warnings pre-existing, in coordinator-owned `lib/contracts/conformance.ts:3924` and `lib/contracts/fake-services.ts:80`; none in U1 files |
| `make console-typecheck` | 0 | `✓ Types generated successfully` then `tsc --noEmit` silent |
| `pnpm --dir apps/app build` (run as `cd apps/app && pnpm build`) | 0 | `✓ Compiled successfully` · `Finished TypeScript` · `✓ Generating static pages (7/7)`; `/usage` and `/billing` both `ƒ` (server-rendered on demand) |
| `cd apps/app && node tests/u/run-mutants.mjs` | 0 | `2 self-checks, 2 as expected; 24 mutants, 24 killed, 0 not killed` |
| `make console-mutants` | 0 | `144 mutants: 144 killed by a named declared case, 0 survived, 0 stale, 0 runner errors, 28.4s` |

`make api-test`, `make api-mutants`, `make bench-test`: **not run** (no Python or benchmark file
touched).

## Results

```
# tests 145
# suites 4
# pass 145
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 700.449292
```

```
ok   SELF-NOOP self-check expected survived, got survived (the suite passed)
ok   SELF-BROKEN self-check expected runner-error, got runner-error (every case failed, so the copy is broken)
killed  U1-M01 the summary shows the held total where the charged total belongs — by "U1-T10 …"
killed  U1-M02 money is formatted through Number instead of the Money helpers — by "U1-T07 …"
killed  U1-M03 a day's cost loses its precision to a float — by "U1-T11 …"
killed  U1-M04 the balance card shows the granted total as the available figure — by "U1-T20 …"
killed  U1-M05 a credit loses its plus sign and reads like a debit — by "U1-T24 …"
killed  U1-M06 the wallet identity is checked as a sum instead of a difference — by "U1-T21 …"
killed  U1-M07 a spent-out organization is greeted as a brand-new one — by "U1-T22 …"
killed  U1-M08 an operator's ledger entry names the principal instead of the platform (R41) — by "U1-T25 …"
killed  U1-M09 stepping forward forgets the page it came from — by "U1-T26 …"
killed  U1-M10 stepping back reads the trail without consuming it — by "U1-T26 …"
killed  U1-M11 the page number stops counting the page being shown — by "U1-T26 …"
killed  U1-M12 changing a filter keeps a cursor minted for the old filters — by "U1-T05 …"
killed  U1-M13 the cursor never reaches the query, so every page is page one — by "U1-T03 …"
killed  U1-M14 a hold is rendered in the charged column — by "U1-T07 …"
killed  U1-M15 usage the engine did not report is shown as a token count anyway — by "U1-T08 …"
killed  U1-M16 a platform-absorbed failure is presented as a settled charge — by "U1-T09 …"
killed  U1-M17 the model filter is dropped from the query, so a filtered view shows everything — by "U1-T03 …"
killed  U1-M18 the range is ignored and `from` becomes `to` — by "U1-T03 …"
killed  U1-M19 an unrecognised range in the URL is passed to the service instead of falling back — by "U1-T01 …"
killed  U1-M20 a stale page link offers a retry that cannot work instead of a restart — by "U1-T05 …"
killed  U1-M21 a refusal nobody can retry out of is offered as retryable — by "U1-T06 …"
killed  U1-M22 an empty result is reported as ready, so a filter miss looks like a zero row — by "U1-T06 …"
killed  U1-M23 timestamps are rendered in the server's locale instead of UTC — by "U1-T11 …"
killed  U1-M24 the trail is dropped from the URL, so Previous cannot survive a reload — by "U1-T02 …"

2 self-checks, 2 as expected; 24 mutants, 24 killed, 0 not killed
```

(The `— by "U1-Txx …"` names are truncated here for width; the runner prints each killing case's full
name.)

Failures and skips: none. No case was skipped for a missing hook.

**Conformance.** U1 implements no `ConsoleServices` adapter — it is a consumer of one — so there is
no U1 adapter to run `runConsoleServicesConformance` against. The suite is run against the fake it
does consume, inside `make console-test` (`tests/contracts/services.test.ts`), and the coordinator's
`make console-mutants` re-confirms the exported suite still kills all 144 declared contract mutants
after U1's changes. Two runs of the mutation list, mine and the coordinator's, were needed because
they judge different things: the contract suite cannot see a page's view model, and U1's cases are
not what track C runs.

**Mutation honesty.** The first mutant run exposed two invariants the cases claimed but could not
break: `tokensView`'s certainty check (the fixture's unknown rows also have null token fields, so the
check was redundant *in the fixture*) and the key/model filters reaching the query. The assertions
were added — a crafted unknown-certainty row that *does* carry counts, and direct field assertions on
the scope query — rather than the mutants removed.

## Failure drill

- **Injection point:** `services.failNext("usage", "dependency_unavailable")` and
  `failNext("ledger", "dependency_unavailable")` — the fake's documented pre-write injection.
- **Durable state before/after:** U1 has none. It performs read operations only; it creates no server
  action, no mutation and no migration. The drill's assertion is the read equivalent: after the
  injected failure, the same query returns a result deep-equal to the successful call before it
  (U1-T06), so the failed read changed nothing and the retry the page offers actually works.
- **Duplicate/retry behaviour:** the "Try again" affordance is a plain anchor to the current URL (a
  fresh server request) and "Back to the first page" is a link that drops cursor and trail; neither is
  a mutation, so a double-click cannot double-apply anything. The retry/restart choice comes from the
  error code (`recoveryFor`), tested in U1-T06 and U1-T05.
- **Cleanup:** nothing to clean. Each request builds a fresh fake; the mutation runner works on a
  temporary copy under `$TMPDIR` and removes it (`--keep` to retain), leaving the source untouched —
  `git status` is clean after a run.

## Artifacts

No external artifacts, no logs in durable storage, no credentials, prompts or signed URLs. Everything
quoted above is reproducible from the implementation SHA with the commands in the table. Fixture data
only; no customer content exists.

## Changes

Owned paths, all under `apps/app`:

```
 app/(console)/billing/balance-card.tsx         |  63 +++      (new, markup)
 app/(console)/billing/page.tsx                 | 212 +++---   (rewritten)
 app/(console)/billing/view-model.ts            | 156 +++      (new, pure)
 app/(console)/usage/fake-console-context.ts    |  37 +        (new, the one service seam)
 app/(console)/usage/loading.tsx                |  18 +        (new, Next loading UI)
 app/(console)/usage/page.tsx                   | 270 +++---   (rewritten)
 app/(console)/usage/ranges.ts                  |  21 -        (deleted; range table moved into the tested view model)
 app/(console)/usage/states.tsx                 | 102 +        (new, error/empty/pager markup)
 app/(console)/usage/usage-chart.tsx            |  22 +-       (prop type is now the contract-backed DayView)
 app/(console)/usage/usage-controls.tsx         |  79 +-       (filters now include model; transitions via the view model)
 app/(console)/usage/view-model.ts              | 450 +        (new, pure)
 tests/u/usage-view-model.test.ts               | 412 +        (new)
 tests/u/billing-view-model.test.ts             | 242 +        (new)
 tests/u/run-mutants.mjs                        | 415 +        (new)
```

Nothing outside `apps/app/app/(console)/usage/`, `apps/app/app/(console)/billing/`,
`apps/app/tests/u/` and this evidence file was touched. `apps/app/lib/contracts/**` is byte-identical
(`git diff 8744418..9bef497 -- apps/app/lib` is empty), which is why `make console-mutants` still
reports 144/144.

- **Contract change requests: none.** The frozen DTOs, `Money` helpers and `ConsoleServices` were
  sufficient. One *gap* is noted under Limits (no model-catalogue read), handled inside U1 without a
  contract change.
- **Migrations: none.** No SQL, no schema, no DDL — D owns migrations.
- **Deploy/rollback:** pure presentation. Rollback is reverting the three commits; no durable state
  or retention obligation is involved. The pages read only the fake, so deploying them before C1
  would show fixture data — they must be integrated together with C1's provider (see Handback).

## Limits

- **Fixtures, not a real service.** Every result above is "implemented", never "integrated". The
  pages read `createFakeConsoleServices()` through one seam
  (`app/(console)/usage/fake-console-context.ts`) and the fixture clock is frozen, so a range filter
  is computed against `2026-09-20T12:00:00Z`. Owner: C1 (real services) + coordinator (the swap).
- **Session is the fixture owner session.** The pages do not call `getSession()`; tenant resolution
  through real auth is C1's, and until then no role, member or suspended-organization view is
  exercised against a real session. Owner: C1. The suspended-organization *read* path is available in
  the fake (`sessions.suspendedOwner`) but is not yet wired to a page, because the page has no way to
  choose a session — deliberately left for the integration, not silently faked.
- **Model filter options come from the visible page.** `ConsoleServices` has no model-catalogue read,
  so the model select offers the models on the current page plus the active selection. A filter for a
  model absent from the page must be typed into the URL. Marked in the code; the lazy alternative to a
  contract revision. Owner: U (revisit in U2) or C1 if a catalogue read is added.
- **The cursor trail rides in the URL.** A deep walk makes a long URL (~60 characters per page with
  the fixture's cursors, and a real implementation's signed cursors will be longer). Marked
  `ponytail:` in `view-model.ts` with the upgrade path (server-side page token). Owner: U.
- **No total page count**, by construction: an opaque keyset cursor knows neither the total nor the
  last page, and counting the table on every page view is the thing keyset pagination exists to avoid.
  The pager shows "Page N" with First/Previous/Next.
- **Browser behaviour is unverified here.** Keyboard traversal, focus order, screen-reader output and
  the absence of a payment control in the rendered DOM are asserted only at the view-model and markup
  level (links and native form controls, `aria-label` on each select, `aria-disabled` on a spent
  direction, `aria-busy`/`sr-only` on the loading state). No browser was started. Owner: E (see
  Handback).
- **`components/credits-card.tsx` and `lib/credits.ts` are now unused** by any page but were not
  touched — they are outside U1's owned paths. Owner: coordinator (see integration requests).
- **`make api-test` not run** (no Python change), `make bench-test` not applicable. Neither is a pass.
- **Provisional copy.** Every customer-facing sentence about settlement, reconciliation and
  promotional credit is a first draft written to the contract's semantics; it has had no product
  review.

## Handback

**Next unblocked task:** U2 (keys and privacy/settings controls) — its start dependency is U1 and it
can proceed against the same fake and the same seam. U3 also starts after U1. Integration of U1
itself waits on C1 and D5.

**Pending coordinator wiring / integration requests** (exact changes, all outside U1's owned paths):

1. **Replace the fake seam with C1's provider.** `apps/app/app/(console)/usage/fake-console-context.ts`
   exports `consoleContext(): { services: ConsoleServices; session: SessionContext; now: Date }` and
   is the *only* place either page obtains data. At integration, have it return C1's services, a
   `SessionContext` mapped from `getSession()` (`lib/session.ts` already provides every field:
   `userId`, `email`, `orgId`, `orgName`, `role`, `isOperator`) and `now: new Date()`. No page change
   is needed. The file is named for what it is so it cannot be mistaken for production wiring.
2. **Add U1's mutation run to the check commands.** Suggested: a `test:mutants:u` script in
   `apps/app/package.json` (`node tests/u/run-mutants.mjs`) and a root `Makefile` target
   `console-mutants-u`, or fold it into `console-mutants`. Both files are coordinator-owned; the
   command as run today is `cd apps/app && node tests/u/run-mutants.mjs` (exit 0 = all killed).
3. **Navigation label.** The `/billing` page is now "Balance" (`metadata.title` `Balance · infrx`)
   because there is nothing to bill in the free pilot. `components/sidebar.tsx` still labels it
   "Billing" with a `CreditCard` icon, and `app/(console)/layout.tsx` passes it a balance from
   `getBalance()` — sidebar and layout are coordinator-owned. Suggested: label "Balance", a non-payment icon, and (at C1
   integration) the footer figure from `balances().available` rather than `lib/session.ts`'s
   `org_balance` RPC.
4. **Dead shared code after this change.** `components/credits-card.tsx` (which carries a disabled
   "Add credits" button) and `lib/credits.ts` have no remaining caller. Safe to delete once U2/U3
   confirm they need neither; U1 left them untouched as they are not its paths.
5. **Legacy types.** `lib/types.ts`'s `UsageSummary`, `UsageDay` and `LedgerRow` (number-typed money)
   are no longer used by the usage or billing pages. They remain for other pages; the contract types
   in `lib/contracts/types.ts` are what U1 uses.

**Flows for E's e2e suite** (CONSOLE-FLOWS, browser-level — U1 cannot assert these):

1. Tab order and Enter/Space on the three filter selects (`aria-label`s "Filter by API key",
   "Filter by model", "Time range"), and that choosing a value navigates and **drops `cursor` and
   `trail` from the URL**.
2. Tab to First/Previous/Next in the `nav[aria-label="Usage pages"]` and `nav[aria-label="Ledger
   pages"]`, Enter to page, and that a spent direction is a non-focusable `aria-disabled` span rather
   than a focusable dead link.
3. Back/forward browser navigation across a walk: the trail in the URL must restore the same page,
   and a reload of a deep page must render the same rows.
4. A stale cursor pasted into the URL: the error panel appears with "Back to the first page" and no
   "Try again"; and an unreachable service shows "Try again" whose activation re-requests.
5. Focus and announcement on the loading state (`aria-busy`, the `sr-only` "Loading usage…") when
   navigating into `/usage`.
6. **No control anywhere on `/usage` or `/billing` accepts money**: no "Add credits", no "Add card",
   no checkout, no invoice download — the visual half of U1-T23.
7. Both pages at a small viewport: the usage table and the ledger table must remain readable, with the
   Charged and Held columns distinguishable (the point of U1-T07 is lost if they collapse).
8. Member and operator sessions on both pages once C1 lands: a member must still read usage and
   balances, and no operator principal may appear in the ledger's "By" column (R41).

**Unresolved findings:** none outstanding. The two invariants the mutation run found unenforced were
fixed in the same session (see Results).

## Verification log

- 2026-09-21: Written from the commands in the Commands table, run at the implementation SHA
  `9bef497`. Every count and every result line is quoted from command output; none is hand-typed.
  No real service, cloud, container or paid provider was contacted, and nothing was pushed.

---

# Review round 1 — appended 2026-09-21

Independent review of `a1b3138` returned **fix_required** with items B1–B4 plus four non-blocking
items. All are addressed at implementation SHA **`c93d138`**; nothing the review recorded as verified
was touched. Nothing in the original report above is deleted — corrections are stated here.

## Source (round 1)

- Implementation SHA: **`c93d138`**, on `codex/u1-usage-balance-ui`, same worktree.
- Commits:

```
c93d138 U1 review B1-B3: prototype-safe lookups, bounded URL state, page models
b03e61b U1 review B1: route error boundaries and a billing loading state
```

## Disposition

### B1 — prototype-chain crash at the trust boundary: **fixed**

`range in RANGES` was true for every `Object.prototype` member, so `?range=toString` produced
`now - <function>` = `NaN`, and `new Date(NaN).toISOString()` throws: one query parameter for a dead
page. Fixed at the root and swept:

| Lookup | Before | After |
|---|---|---|
| `parseUsageFilters` range allowlist | `range in RANGES` | `isRangeKey()` → `Object.hasOwn(RANGES, value)` |
| the range-table read itself | `RANGES[filters.range]` | `rangeMs()` → `Object.hasOwn` with the default as fallback, so a key from *any* caller (not just the parser) is safe |
| `explainError` hint table | `HINTS[code]` | `Object.hasOwn(HINTS, code)` |
| `recoveryFor` status table | `ERROR_CODE_HTTP_STATUS[code]` | `Object.hasOwn(...)`, and `undefined` is treated as "no status" |
| `ledgerKindLabel` (billing) | already `Object.hasOwn`-guarded in round 0 — unchanged, now asserted | |
| `settlementView` switch | exhaustive per the type; an out-of-vocabulary value returned `undefined` and crashed the markup | `default:` branch returning a row that says "Unknown … nothing is presented as charged" |

Swept and found clean: no other bracket lookup keyed by an untrusted string exists in either view
model or either page; `keyId`/`model`/`cursor`/`trail` are only ever compared or passed as values.

New cases: **U1-T13** asserts eight prototype keys (`toString`, `__proto__`, `constructor`,
`hasOwnProperty`, `valueOf`, `isPrototypeOf`, `propertyIsEnumerable`, `toLocaleString`) fall back to
the default range and produce a parseable `from`; that a *forged* range key reaching
`usageScopeQuery` directly does not throw (`assert.doesNotThrow`, because `RANGES.__proto__` is an
object and `now - object` is `NaN`); that an unknown error code yields the fallback message and
`recovery: "none"`; and that an unknown settlement state returns a view rather than `undefined`.

Route-level error states: `usage/error.tsx` and `billing/error.tsx` (client error boundaries with a
`reset` control, showing no text from the thrown error — a thrown error can carry internals) and
`billing/loading.tsx` to match the usage route's existing loading skeleton.

**Limit:** the end-to-end check of `GET /usage?range=toString` is *not run*. The console layout
resolves a real session, so an unauthenticated request never reaches the page, and U1 has no auth to
supply. The crash and the fix are demonstrated at the view-model level (U1-T13, and mutant U1-M26,
which restores the unguarded table read and is killed by the `doesNotThrow` assertion). The route
check belongs to E's e2e suite.

### B2 — surviving mutants on claimed invariants: **fixed, four confirmed and three more pinned**

Each reviewer mutant is now a declared mutant in `tests/u/run-mutants.mjs` and each is killed:

| Reviewer item | The claim that was not enforced | Mutant | Killed by |
|---|---|---|---|
| R01 | the cost is read *only* when settled — every unsettled fixture row happens to have `cost` 0, so the fixture could not tell the two apart | `U1-M33` (`displayMoney(settled ? row.cost : ZERO_MONEY)` → `displayMoney(row.cost)`) | U1-T07, now with a crafted `held_unknown` row carrying `cost 0.50` and `max_hold 0.75`: charged `$0.00`, held `$0.75` |
| R06 | each settlement state's *explanation* is its own | `U1-M34` (`released_free` given the platform-absorbed sentence) | U1-T09, which now asserts five distinct details as well as five distinct labels, and matches each one's substance |
| R07 | an unsettled row never claims a charge | `U1-M35` (the non-terminal detail → "This has been charged.") | U1-T09 (`/nothing has been charged/i`, `/ceiling rather than a price/i`) |
| B03 | both halves of the new-organization test matter | `U1-M38` (`!hasHistory && isZeroMoney(...)` → `!hasHistory`) | U1-T22, now asserting a *funded* wallet with `hasHistory: false` is `funded` — reachable, because the ledger read beside the balance can fail on its own |

Cheap unclaimed ones, now pinned: `U1-M36` (a missing count on an *authoritative* row rendered as
zero — U1-T08), `U1-M37` (the outcome column dropping `terminal_cause` — U1-T09), `U1-M39` (the
exhausted state dropping the operator ask — U1-T22).

### B3 — service errors rendered as empty, logic in `.tsx`: **fixed**

Every service result now becomes a state inside a view model, and the `.tsx` files branch on
`state.kind` only:

- `usagePageModel({filters, usage, summary, daily, keys})` → `summary`, `daily`, `rows`, `keys` states
  plus `keyOptions`, `models`, `keyNotice`, `here`, `firstHref`, and the pager's page number and three
  hrefs. A failed `usageDaily` is an error panel, not "No requests in this range yet."; a failed
  `keys.list` is a visible notice, not a silently short filter list.
- `balanceCardState(balance, ledger, state)` → the card as a state, so a failed `balances` read
  renders an error instead of the card vanishing (a missing card and an unloaded card look identical,
  and both read as "no money").
- `billingPageModel({state, balance, ledger})` → the balance state and the ledger state with its
  pager hrefs.
- Page sizes are named in the view models: `PAGE_SIZE`, `LEDGER_PAGE_SIZE`, `HISTORY_PROBE_LIMIT`
  (with `ledgerPageQuery` / `historyProbeQuery` building the queries). No literal `limit` survives in
  a `.tsx`.
- `hasLedgerHistory(ledger, state)` is computed and tested in the view model, and **treats a failed
  read as history**: reporting "no history" from an error greets an established organization with
  "ask for your first grant".
- `ErrorPanel` now takes the error state itself, which removed the last re-derivation (`?? ""`
  fallbacks) from the page.

New cases: **U1-T15** (each of the four reads failed in turn: an error state with `recovery: "retry"`,
never empty, and the other three parts unaffected; an empty *successful* read is still empty),
**U1-T16** (page number, `firstHref`, `previousHref`, `nextHref`, and that `here` is the URL being
shown), **U1-T27** (the history rule and the card-as-state), **U1-T28** (the billing page model's
branches and the named page sizes).

### B4 — evidence corrections

1. **The filter controls are not native `<select>`.** They are `@base-ui/react/select` through
   `components/ui/select.tsx` (`import { Select as SelectPrimitive } from "@base-ui/react/select"`),
   driven by a client component that calls `withFilter` + `router.push`. The `aria-label`s are on the
   `SelectTrigger`s. The round-0 report's "native form controls" phrasing was wrong; keyboard
   behaviour therefore comes from the library, which is one more reason the keyboard flows are E's to
   verify in a browser.
2. **Line counts are quoted, not typed.** `git diff --stat 8744418..c93d138`:

```
 apps/app/app/(console)/billing/balance-card.tsx    |  54 ++
 apps/app/app/(console)/billing/error.tsx           |  27 +
 apps/app/app/(console)/billing/loading.tsx         |  13 +
 apps/app/app/(console)/billing/page.tsx            | 187 +++--
 apps/app/app/(console)/billing/view-model.ts       | 277 ++++++++
 apps/app/app/(console)/usage/error.tsx             |  27 +
 .../app/(console)/usage/fake-console-context.ts    |  37 +
 apps/app/app/(console)/usage/loading.tsx           |  18 +
 apps/app/app/(console)/usage/page.tsx              | 270 +++++---
 apps/app/app/(console)/usage/ranges.ts             |  21 -
 apps/app/app/(console)/usage/states.tsx            | 101 +++
 apps/app/app/(console)/usage/usage-chart.tsx       |  22 +-
 apps/app/app/(console)/usage/usage-controls.tsx    |  79 ++-
 apps/app/app/(console)/usage/view-model.ts         | 603 +++++++++++++++++
 apps/app/tests/u/billing-view-model.test.ts        | 370 ++++++++++
 apps/app/tests/u/run-mutants.mjs                   | 617 +++++++++++++++++
 apps/app/tests/u/usage-view-model.test.ts          | 753 +++++++++++++++++++++
 research/plan/evidence/u/U1-9bef497.md             | 297 ++++++++
 18 files changed, 3515 insertions(+), 258 deletions(-)
```

3. **"All logic in `.ts`" is accurate as of `c93d138`**, which it was not at `a1b3138`: the four
   result-handling decisions the review found in `page.tsx` are now in the view models (B3). What
   remains in `.tsx` is `state.kind` branching and element attributes.
4. **The comment about an unknown filter's "typed refusal" was wrong and is gone.** The service
   answers an unknown `key_id` with an **ok, empty page** — the comment claimed a refusal. The code
   now does what the comment promised, the way V1 does it: `keyFilterNotice` checks the filter against
   the organization's own `keys.list` and says "This link filters by an API key that does not belong
   to this organization… Clear the key filter." With the key list itself unreadable there is nothing
   to check against, so no notice is invented and the key-list error is what the page shows
   (**U1-T17**, mutant `U1-M43`).

### Non-blocking items: all four applied

- **Trail bounded.** `MAX_CURSOR_CHARS` = 512 and `MAX_TRAIL_PAGES` = 50, applied when parsing *and*
  when walking forward; an over-long cursor or trail entry is dropped. `?trail=abc`×1500 now parses to
  an empty trail, because **a trail without a cursor is dropped entirely** — page one has nothing
  behind it, so such a trail is noise at best and a forged back-stack at worst (**U1-T14**, mutants
  `U1-M29`–`U1-M32`). The remaining ceiling is recorded as a `ponytail:` note: past 50 back-steps the
  oldest entries are dropped, the page number under-reports, and "First" is the way home.
- **`--only` with no match fails.** It printed "0 mutants, 0 killed" and exited 0 — a green run that
  tested nothing. It now prints `--only <ids> matched no mutant of 46` and exits 1.
- **Payment-wording scan extended.** **U1-T18** scans every string the usage view model produces
  (summary labels and hints, all five settlement labels and details, the token notes, every error hint
  a reader can provoke, the key notice) against
  `add credits|add card|top up|\bbuy\b|purchase|invoice|checkout|revenue|pay now|credit card`, and
  U1-T23 now scans the ledger kind labels with `"Purchase (legacy)"` as the single named exception
  (R13 requires the legacy row to render).

## Commands (round 1)

UTC 2026-09-21T06:46Z – 06:47Z, from the worktree root.

| Command | Exit | Result |
|---|---|---|
| `make console-test` | 0 | `# tests 153 / # pass 153 / # fail 0 / # skipped 0` (126 before U1, +27 U1 cases) |
| `make console-lint` | 0 | `✖ 2 problems (0 errors, 2 warnings)` — the same two pre-existing warnings in coordinator-owned files; none in U1 files |
| `make console-typecheck` | 0 | `✓ Types generated successfully`, then `tsc --noEmit` silent |
| `cd apps/app && pnpm build` | 0 | `✓ Compiled successfully`, `✓ Generating static pages using 15 workers (7/7)` |
| `cd apps/app && node tests/u/run-mutants.mjs` | 0 | `2 self-checks, 2 as expected; 46 mutants, 46 killed, 0 not killed` |
| `make console-mutants` | 0 | `144 mutants: 144 killed by a named declared case, 0 survived, 0 stale, 0 runner errors, 31.4s` |

`make api-test` / `api-mutants` / `bench-test`: **not run** — still no Python or benchmark file
touched. Not a pass.

```
# tests 153
# suites 4
# pass 153
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 919.472646
```

```
ok   SELF-NOOP self-check expected survived, got survived (the suite passed)
ok   SELF-BROKEN self-check expected runner-error, got runner-error (every case failed, so the copy is broken)
…
2 self-checks, 2 as expected; 46 mutants, 46 killed, 0 not killed
```

Each fix was written with its case first: every mutant listed above restores the reviewed defect and
is killed by the named case, which is the executable form of "the test fails without the fix".

## Requirement coverage added in round 1

| Test ID | Exact invariant demonstrated |
|---|---|
| U1-T13 | No URL value reaches a prototype lookup: eight `Object.prototype` names are not ranges and produce a parseable window; a forged range key cannot throw; an unknown error code falls back with `recovery: "none"`; an out-of-vocabulary settlement state returns a view that presents nothing as charged. |
| U1-T14 | The URL cannot be used to make the server do unbounded work: an over-long cursor or trail entry is dropped, the trail is capped at `MAX_TRAIL_PAGES` keeping the most recent pages, a trail without a cursor is dropped entirely, and walking forward cannot grow it either. |
| U1-T15 | Each of `usage`, `usageSummary`, `usageDaily` and `keys.list` failing on its own is an error state with a retry — never an empty one — and leaves the other three ready; an ok-but-empty read is still empty. |
| U1-T16 | The page model computes the page number and the First/Previous/Next hrefs, `here` is the URL being shown (so a retry re-runs *this* request), page 1 has no Previous, and an empty page has no pager. |
| U1-T17 | A key filter naming another organization's key is explained rather than rendered as an empty table, because the service answers ok-and-empty; with the key list unreadable no notice is invented and the key-list error is shown instead. |
| U1-T18 | No string the usage view model produces offers payment or calls promotional credit revenue. |
| U1-T27 | A failed ledger read counts as history, so a funded or zero wallet whose history is unreadable is never greeted as brand new; the balance card is a state, so a failed `balances` read is an error rather than a missing card; a non-reconciling wallet says so through the model. |
| U1-T28 | The billing page model states each branch (ready / empty / error) and the page sizes are named in the view model (`LEDGER_PAGE_SIZE`, `HISTORY_PROBE_LIMIT`). |

## Limits (round 1 changes only)

- The `GET /usage?range=toString` route check is **not run** (no auth available to U1); proven at the
  view-model level only. Owner: E.
- The trail cap means a walk deeper than 50 pages loses its oldest back-steps and the page number
  under-reports. Marked `ponytail:` with the upgrade path.
- The filter selects are a third-party component (`@base-ui/react/select`); their keyboard and focus
  behaviour is unverified here. Owner: E.
- Every other limit in the round-0 report stands unchanged.

## Handback (round 1)

**Integration requests** — the five from round 0 stand (swap `fake-console-context.ts` for C1's
provider; wire `node tests/u/run-mutants.mjs` into the check commands; the `/billing` → "Balance"
navigation label and icon; `components/credits-card.tsx` and `lib/credits.ts` now have no caller;
`lib/types.ts`'s number-typed money types are unused by these pages), plus one added by this round:

6. **`app/(console)/layout.tsx` formats the sidebar balance through `Number`** — `getBalance()`
   returns `Number(data ?? 0)` and the layout passes it to `money()` in `lib/format.ts`. That is a
   float where R11 requires a canonical eight-digit decimal string, so the sidebar can show a
   different figure from the balance card on the same screen. Coordinator-owned (layout, sidebar,
   `lib/session.ts`, `lib/format.ts`). At C1 integration the figure should come from
   `balances().available` and be formatted with `displayMoney`.

**Flows for E** — the eight from round 0 stand, plus:

9. `GET /usage?range=toString`, `?range=__proto__`, `?cursor=<600 chars>` and `?trail=` repeated
   1500 times must each render the page (the first three fall back to defaults; the last shows page
   one), not a server error and not a multi-hundred-kilobyte response.
10. An unexpected throw inside either route must render the error boundary with a working "Try again",
    rather than a truncated page.

## Verification log

- 2026-09-21: Round-1 review fixes appended at implementation SHA `c93d138`. Every count and result
  line is quoted from the command output in the table above; the diffstat is `git diff --stat`
  output. No real service, cloud, container or paid provider was contacted; nothing was pushed.

---

# Review round 2 — appended 2026-09-21

Round-2 review of `148986d` confirmed B1–B4 fixed (including the rendered `?range=toString` check the
round-1 report had to leave as a limit) and returned two blocking items, B5 and B6, plus five
non-blocking ones. All are addressed at implementation SHA **`2077eb6`**.

## Source (round 2)

- Implementation SHA: **`2077eb6`**, on `codex/u1-usage-balance-ui`, same worktree.
- Commit: `2077eb6 U1 review B5/B6: boundaries recover with retry(), kind labels guarded`

## Disposition

### B5 — "Try again" in both error boundaries could never work: **fixed**

Both boundaries took `reset` and called it from the button. The shipped documentation of the installed
Next — `apps/app/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md`
(Next 16.3.5), read for this fix rather than recalled — is explicit:

- `retry`: "the function will try to **re-fetch and re-render** the error boundary's children. If
  successful, the fallback error component is replaced with the result of the re-render." Every
  example in the file, including `global-error`, uses `({ error, retry })` with
  `onClick={() => retry()}`.
- `reset`: "**In most cases, you should use `retry()` instead.** However, if you have a specific
  reason to clear the error state and re-render the error boundary's children **without re-fetching
  the contents**, you can use the `reset()` function."

Both `/usage` and `/billing` are Server Components, so every throw the boundary catches happened on
the server; re-rendering without re-fetching replays the same errored payload, which is exactly what
the reviewer observed in a headless browser (no recovery after the fault cleared, recovery once
`retry()` was wired). Fixed in `usage/error.tsx` and `billing/error.tsx`: both destructure `retry` and
call `onClick={() => retry()}`. Neither shows anything from the thrown error — a thrown error can
carry internals, and a Server Component error reaches the client as a digest in any case.

The copy moved into a pure module, `usage/boundary.ts` (`boundaryCopy(scope)` and
`BOUNDARY_RECOVERY_PROP`), because a `.tsx` file cannot be loaded by `node --test` (R48) and the
wording is the part worth pinning. New case **U1-T19** then covers what is testable without a
browser:

- `BOUNDARY_RECOVERY_PROP === "retry"`;
- both `error.tsx` files, read as source, are `"use client"`, destructure `retry`, contain
  `onClick={() => retry()}`, contain no `reset` outside the prose that explains why, and interpolate
  none of `error.message` / `error.stack` / `error.digest`;
- the copy of each scope differs, says the reader is unaffected, and passes the payment-wording scan.

Mutants: `U1-M48` (the usage boundary's signature back to `{ reset }`), `U1-M49` (the balance
boundary's button back to `reset()`), `U1-M50` (the "unaffected" sentence removed) — all killed by
U1-T19. A mutant in a `.tsx` file is killable precisely because the case reads the file rather than
importing it.

**Still not verifiable here:** that the button *recovers* once the fault clears is a browser
behaviour. It is listed for E below, and the reviewer has already observed both the failure and the
fix.

### B6 — the round-1 evidence claimed a guard that did not exist: **fixed, and the sentence corrected**

The round-1 appendix said `ledgerKindLabel` was "already `Object.hasOwn`-guarded in round 0 —
unchanged, now asserted". **That was false.** `billing/view-model.ts` read `return KIND_LABELS[kind];`,
so `ledgerKindLabel("toString")` returned a *function*, `ledgerKindLabel("refund")` returned
`undefined`, and `ledgerRowView({kind: "constructor"})` put a function into the table as a React
child. The claim was wrong twice over: there was no guard, and no assertion covered it. This
paragraph is the correction; the round-1 table's row for `ledgerKindLabel` is superseded by it.

Fixed: `Object.hasOwn(KIND_LABELS, kind) ? KIND_LABELS[kind] : UNKNOWN_KIND_LABEL` with
`UNKNOWN_KIND_LABEL = "Other"`. U1-T25 now asserts five prototype keys and an out-of-vocabulary kind
(`refund`) all yield the string `"Other"`, that the returned value is always a string, and that a row
carrying such a kind still renders. Mutant `U1-M47` restores the bare lookup and is killed by U1-T25.

**The complete sweep, re-grepped at `2077eb6`** — every bracket lookup in both view models and the
boundary module, with its guard:

| Location | Expression | Key comes from | Guard |
|---|---|---|---|
| `usage/view-model.ts:86` | `[value]` | — | not a lookup: an array literal in `many()` |
| `usage/view-model.ts:227` | `RANGES[range]` | URL (`?range=`) | `Object.hasOwn(RANGES, range)`, else `RANGES[DEFAULT_RANGE]` (`rangeMs`) |
| `usage/view-model.ts:271` | `ERROR_CODE_HTTP_STATUS[code]` | service error code | `Object.hasOwn`, else `null`; `undefined` also treated as "no status" |
| `usage/view-model.ts:295` | `HINTS[code]` | service error code | `Object.hasOwn`, else the service's own message |
| `usage/boundary.ts:37` | `UNAFFECTED[scope]` | in-process literal | `Object.hasOwn`, else the usage sentence (guarded anyway, and asserted) |
| `billing/view-model.ts:138` | `KIND_LABELS[kind]` | service DTO | `Object.hasOwn`, else `UNKNOWN_KIND_LABEL` — **this round's fix** |

Non-bracket reads of the same class: `settlementView`'s `switch (row.settlement_state)`
(`usage/view-model.ts:331`) is exhaustive over the contract vocabulary **and** has a `default:` branch
(round 1), so an unknown value renders "Unknown" instead of returning `undefined`. `searchParams`
properties are read by literal name only (`params.range`, `params.key`, `params.model`,
`params.cursor`, `params.trail`), so a URL cannot choose which property is read. **No bracket lookup
exists in any `.tsx` file** (grep over all six). That is the whole surface.

### Non-blocking items: all five applied

| Item | Where | Case | Mutant |
|---|---|---|---|
| Last usage page → `nextHref === null` | `usagePageModel` | U1-T16 walks to the end and asserts no Next, a usable Previous, and that `here` carries the cursor | `U1-M52` |
| Billing page ≥ 2 → Previous present, `here` carries the cursor; last page → no Next | `billingPageModel` | U1-T28 | `U1-M53` |
| `balanceCardState` on a later page with an empty item list keeps the page cursor | `hasLedgerHistory` | U1-T27, U1-T28 (`notEqual(state.kind, "new")`) | `U1-M54` |
| A 512-character cursor is accepted, 513 refused | `usableCursor` | U1-T14 (the bound is inclusive, or the cap would break a real deep walk instead of a forged URL) | `U1-M51` |
| A row reporting only one of the two token counts shows neither | `tokensView` | U1-T08 (both halves) | `U1-M56` |
| The `keys.list` failure notice carries a retry link like the panels | new `InlineError` in `states.tsx`, used by the usage page | the state's `recovery: "retry"` is asserted in U1-T15; the link itself is markup (E) | — |
| "Clear the key filter" is a link that also resets the walk | `clearKeyFilterHref` in `usagePageModel` | U1-T16 (the href carries neither `key=` nor `cursor=`) | `U1-M55` |

## Commands (round 2)

UTC 2026-09-21T07:07Z – 07:10Z, from the worktree root.

| Command | Exit | Result |
|---|---|---|
| `make console-test` | 0 | `# tests 154 / # pass 154 / # fail 0 / # skipped 0` (126 before U1, +28 U1 cases) |
| `make console-lint` | 0 | `✖ 2 problems (0 errors, 2 warnings)` — the same two pre-existing warnings in coordinator-owned files |
| `make console-typecheck` | 0 | `✓ Types generated successfully`, then `tsc --noEmit` silent |
| `cd apps/app && pnpm build` | 0 | `✓ Compiled successfully in 1921ms` · `✓ Generating static pages using 15 workers (7/7) in 1344ms` |
| `cd apps/app && node tests/u/run-mutants.mjs` | 0 | `2 self-checks, 2 as expected; 56 mutants, 56 killed, 0 not killed` |
| `make console-mutants` | 0 | `144 mutants: 144 killed by a named declared case, 0 survived, 0 stale, 0 runner errors, 33.9s` |

```
# tests 154
# suites 4
# pass 154
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 1262.677751
```

```
ok   SELF-NOOP self-check expected survived, got survived (the suite passed)
ok   SELF-BROKEN self-check expected runner-error, got runner-error (every case failed, so the copy is broken)
…
2 self-checks, 2 as expected; 56 mutants, 56 killed, 0 not killed
```

`--only` with no match, checked separately: `node tests/u/run-mutants.mjs --only NOPE` prints
`--only NOPE matched no mutant of 56` and exits `1`.

`make api-test` / `api-mutants` / `bench-test`: **not run** — still no Python or benchmark file
touched. Not a pass.

One mutant was **stale on first run** (`U1-M50`'s `find` had the wrong indentation) and the runner
reported it as stale rather than killed, which is the behaviour it exists to have; the `find` was
corrected and the mutant is killed.

## Requirement coverage added in round 2

| Test ID | Exact invariant demonstrated |
|---|---|
| U1-T19 | Each route's error boundary is a Client Component that destructures `retry` and calls `retry()` — the only recovery that re-fetches a Server Component throw — uses `reset` nowhere, interpolates no part of the thrown error, and carries per-route copy that tells the reader nothing was charged and offers no payment. |
| U1-T25 (extended) | `ledgerKindLabel` returns a string for any input: five prototype keys and an out-of-vocabulary kind all yield `"Other"`, and a row carrying such a kind still renders. |
| U1-T14 (extended) | The cursor bound is inclusive: a cursor of exactly `MAX_CURSOR_CHARS` is accepted, one character longer is dropped. |
| U1-T16 (extended) | The last usage page offers no Next and still offers Previous; `here` carries the cursor of the page shown; "clear the key filter" drops both the filter and the cursor. |
| U1-T27 / U1-T28 (extended) | A later ledger page counts as history even when its own item list is empty; the ledger pager has both edges right and page ≥ 2 keeps its cursor in `here`. |
| U1-T08 (extended) | A row reporting only one of the two token counts shows neither. |

## Limits (round 2 changes only)

- **That the boundary's "Try again" recovers** cannot be asserted without a browser. U1-T19 pins the
  contract (the prop, the call, the absence of `reset`); the behaviour is E's, and the reviewer has
  observed it both broken and fixed.
- The `InlineError` retry link is markup; only the state's `recovery` is asserted here. Owner: E.
- Every limit recorded in rounds 0 and 1 stands, except the round-1 limit about the unverified
  `?range=toString` route, which the round-2 reviewer verified in a rendered check.

## Handback (round 2)

**Integration requests:** the six from rounds 0 and 1 stand unchanged (swap `fake-console-context.ts`
for C1's provider; wire `node tests/u/run-mutants.mjs` into the check commands; `/billing` → "Balance"
navigation label and icon; `components/credits-card.tsx` and `lib/credits.ts` now have no caller;
`lib/types.ts`'s number-typed money types unused by these pages; and
**`app/(console)/layout.tsx` formats the sidebar balance through `Number`**, an R11 violation in
coordinator-owned code — take it from `balances().available` and format with `displayMoney`).

**Flows for E:** the ten from rounds 0 and 1, plus:

11. **Boundary "Try again" recovers once the fault clears** — on both `/usage` and `/billing`: force a
    throw in the server render, confirm the boundary appears, clear the fault, click "Try again", and
    confirm the page renders. This is the behaviour `reset()` could not provide and `retry()` does;
    it is the one part of B5 that no unit case can hold.
12. A ledger row whose `kind` is outside the contract's vocabulary renders as "Other" rather than a
    blank cell (pairs with U1-T25, which covers the label but not the rendered table).

## Verification log

- 2026-09-21: Round-2 review fixes appended at implementation SHA `2077eb6`. The Next behaviour cited
  for B5 is quoted from the installed package's own documentation file, not from memory. The lookup
  sweep table was produced by re-grepping both view models and all six `.tsx` files at this SHA. Every
  count and result line is quoted from the command output above. No real service, cloud, container or
  paid provider was contacted; nothing was pushed.

---

# Review round 3 — appended 2026-09-21

Round-3 review of `dfda5cd` confirmed B5 (with the boundary verified recovering in a real browser) and
B6 fixed, and returned three test-only items — B7, B8, B9 — plus two cheap ones (P04, E04) and two
evidence nits. All are addressed at implementation SHA **`829127a`**.

Every one of B7–B9 is the same defect in three places: **an assertion that could not fail.** Each was
written against a value that satisfied it however the code behaved, so the invariant the case named was
not being checked. The fix in each case is the assertion, and the proof in each case is the mutant the
reviewer named, now declared here and killed.

## Source (round 3)

- Implementation SHA: **`829127a`**, on `codex/u1-usage-balance-ui`, same worktree.
- Commit: `829127a U1 review B7-B9: three assertions that could not fail, and the mutants`

## Disposition

### B7 — the later-page history assertion was vacuous: **fixed** (reviewer N34 → `U1-M57`)

`balanceCardState(balance, emptyLedger, {cursor: "c1", trail: []})` used `balance`, the **funded**
owner wallet. A funded wallet is never `new` — `balanceState` returns `new` only when the ledger total
is zero — so `notEqual(state.kind, "new")` held whether or not the page cursor was consulted at all,
and the reviewer's N34 (dropping `state` from the `hasLedgerHistory` call) survived the whole suite.

The case now uses the **zero** wallet, for which `new` is reachable, and asserts both directions so the
cursor is what distinguishes them:

- empty ledger, no cursor → `new` (an empty first page of a zero wallet really is a new organization);
- empty ledger, `cursor: "c1"` → `exhausted` (the cursor is history, so the same wallet is not new);
- and the two differ.

`U1-M57` is the reviewer's N34 edit verbatim
(`balanceCardModel(wallet, hasLedgerHistory(ledger, state))` → `hasLedgerHistory(ledger)`); it is
killed by U1-T28.

### B8 — the unknown-kind label was compared only against its own constant: **fixed** (K02 → `U1-M58`)

`assert.equal(ledgerKindLabel(key), UNKNOWN_KIND_LABEL)` is true for *any* value of the constant,
including `""` — which is precisely the blank cell the guard was added to prevent, so K02 survived. The
case now pins the value: `assert.equal(UNKNOWN_KIND_LABEL, "Other")`, alongside the existing
comparisons. `U1-M58` sets the constant to `""` and is killed by U1-T25.

### B9 — the boundary guard was checked for the wrong failure: **fixed** (E07 → `U1-M59`)

The assertion was `boundaryCopy("__proto__").detail.includes("undefined") === false`. Removing the
guard does not produce "undefined": `UNAFFECTED["__proto__"]` is `Object.prototype`, which interpolates
as `[object Object]`. The check passed on the broken code, so E07 survived.

Fixed as an equality, which required one small implementation change: an unrecognised scope now falls
back to the usage copy **whole** rather than to the usage *sentence* inside a "your balance" frame —
half a fallback is a sentence nobody wrote, and it is also what makes the equality assertable. The case
now asserts, for `__proto__`, `constructor`, `toString` and `nope`, that `boundaryCopy(weird)` deep-equals
`boundaryCopy("usage")` and that the detail matches neither `object Object` nor `undefined`.
`U1-M59` removes the guard and is killed by U1-T19.

### P04 — the clear-key href: **no fix needed, now asserted**

The reviewer asked which behaviour was intended and reported seeing the model filter dropped. It is
**not** dropped: `withFilter(filters, {keyId: null})` replaces only the patched field and resets the
walk, so for `{range: "7d", keyId: "k1", model: "m1", cursor: "c9", trail: ["c8"]}` the href is

```
/usage?range=7d&model=m1
```

Intended: clearing one filter must keep the others, or "clear the key filter" silently widens the range
and the model too. The case now parses the href back and asserts the whole filter state
(`{range: "7d", keyId: null, model: "marlin-2b@2026-09-01", cursor: null, trail: []}`), rather than
checking for absent substrings only. `U1-M61` makes the link clear the model as well and is killed by
U1-T16.

### E04 — the no-thrown-text check: **strengthened**

It matched the literal `{error.message}` — one spelling of many; `{String(error)}`, `{error.digest}` in
an attribute, or a `console.log(error)` would all have passed. The case now strips comments and the
props type annotation from each boundary's source and requires that the word `error` appear nowhere in
what remains, while still asserting the prop is declared. `U1-M60` adds `<p>{String(error)}</p>` to the
usage boundary and is killed by U1-T19.

### Evidence nits: corrected here

- **"grep over all six `.tsx` files"** (round 2, B6 sweep) is wrong: there are **ten** `.tsx` files
  across the two owned directories — `usage/page.tsx`, `usage/error.tsx`, `usage/loading.tsx`,
  `usage/states.tsx`, `usage/usage-chart.tsx`, `usage/usage-controls.tsx`, `billing/page.tsx`,
  `billing/error.tsx`, `billing/loading.tsx`, `billing/balance-card.tsx` (`ls` counts 10). The grep
  itself covered all of them (`"usage/"*.tsx "billing/"*.tsx`) and the finding stands: **no bracket
  lookup exists in any `.tsx` file.** Only the count in the prose was wrong.
- **"Non-blocking items: all five applied"** (round 2) heads a table of **seven** rows: the five the
  reviewer listed, plus the `InlineError` retry link and the clear-key link. The heading should read
  "all applied (seven rows: the five listed, plus two the same pass invited)". The rows themselves are
  accurate.

## Commands (round 3)

UTC 2026-09-21T07:23Z – 07:25Z, from the worktree root.

| Command | Exit | Result |
|---|---|---|
| `make console-test` | 0 | `# tests 154 / # pass 154 / # fail 0 / # skipped 0` |
| `make console-lint` | 0 | `✖ 2 problems (0 errors, 2 warnings)` — the same two pre-existing warnings in coordinator-owned files |
| `make console-typecheck` | 0 | `✓ Types generated successfully`, then `tsc --noEmit` silent |
| `cd apps/app && node tests/u/run-mutants.mjs` | 0 | `2 self-checks, 2 as expected; 61 mutants, 61 killed, 0 not killed` |
| `cd apps/app && pnpm build` | 0 | `✓ Compiled successfully in 723ms` · `✓ Generating static pages using 15 workers (7/7) in 456ms` |
| `make console-mutants` | 0 | `144 mutants: 144 killed by a named declared case, 0 survived, 0 stale, 0 runner errors, 49.9s` |

```
# tests 154
# suites 4
# pass 154
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 758.643136
```

```
ok   SELF-NOOP self-check expected survived, got survived (the suite passed)
ok   SELF-BROKEN self-check expected runner-error, got runner-error (every case failed, so the copy is broken)
…
killed  U1-M57 the balance card ignores the page cursor when deciding history (reviewer N34) — by "U1-T28 …"
killed  U1-M58 the unknown-kind label becomes the blank cell the guard exists to prevent (reviewer K02) — by "U1-T25 …"
killed  U1-M59 the boundary copy's scope guard is removed, so an unknown scope interpolates an object (reviewer E07) — by "U1-T19 …"
killed  U1-M60 the boundary puts the thrown error's own digest on the page (reviewer E04) — by "U1-T19 …"
killed  U1-M61 clearing the key filter clears the model filter with it (reviewer P04) — by "U1-T16 …"

2 self-checks, 2 as expected; 61 mutants, 61 killed, 0 not killed
```

The case count is unchanged at 154 because all five items sharpened existing cases rather than adding
new ones; the mutant count went 56 → 61. `make api-test` / `api-mutants` / `bench-test`: **not run** —
no Python or benchmark file touched. Not a pass.

Cross-reference for the coordinator's direct check: the reviewer's `N34`, `K02` and `E07` are
`U1-M57`, `U1-M58` and `U1-M59`, declared with the edits as described, and each names the case that
kills it. `node tests/u/run-mutants.mjs --only U1-M57,U1-M58,U1-M59,U1-M60,U1-M61` reports
`5 mutants, 5 killed, 0 not killed`.

## What this round says about the suite

Three vacuous assertions is a pattern, not three accidents: each was written by asserting the
*expected* outcome on data that already produced it, instead of on data that could produce the wrong
one. A mutant is the only thing that catches that, which is why the list is the deliverable and not the
green run. The three that survived were all in cases added under review pressure in rounds 1 and 2 —
the cases written first, in round 0, were the ones with mutants from the start.

## Verification log

- 2026-09-21: Round-3 review fixes appended at implementation SHA `829127a`. The P04 behaviour is
  quoted from a printed href, the `.tsx` count from `ls`, and every command result from the output in
  the table. Two earlier evidence statements are corrected above rather than edited in place. No real
  service, cloud, container or paid provider was contacted; nothing was pushed.
