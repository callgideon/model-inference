# U1R — CREDIT usage and balance views

## Task and status

- **Task:** U1R (track U, product app; oracles CREDIT-UNITS, CONSOLE-FLOWS, APP-JOURNEY usage/balance step). Opus implementer, lane `app-u1r`.
- **Status: implemented, handed back for review.** Dispatched ahead of BACKEND-READY by the recorded user decision (session-03 log, 21:08Z); implemented code does not accept APP-LOCAL/APP-PILOT.
- Base `46776646` (claude/consumer-v1 tip). Implementation head **`ae8c388a`**; this evidence file and the tracker update are committed on top of it (see handback for the final head).
- Branch `codex/app-u1r`, worktree `.claude/worktrees/codex-app-u1r`. Nothing pushed, rebased or stashed.

Commits (oldest first):

```
76b24b33 U1R: exact unit-labelled CREDIT/USD display helpers in lib/format.ts (tests first)
3430f068 U1R: consumer CREDIT read adapter over PostgREST (wallet, ledger, credits-in, legacy USD, consumer_jobs), fail-closed parsers, tests first
602290ab U1R: Credits view model — exact CREDIT figures, one-time grant, low/zero/no-wallet states, ledger rows, separate legacy USD (tests first)
1960f216 U1R: Usage view model over consumer_jobs — hold vs charge, per-row unit, tokens, window cut, detail links; reconciling fixture (tests first)
6a34fbf1 U1R: Usage and Credits pages on the consumer CREDIT reads (real session in production, gated fixture in preview)
53363db9 U1R: real-PostgreSQL suite — concurrent CREDIT world read through the App adapter as the browser principal
4bcf61bf U1R: 29 mutants for the CREDIT seams in the U runner (93/93 killed)
ae8c388a U1R: keep C's PREVIEW-01 mutant target intact; type the pg PostgREST double
```

## Changed paths (all inside the owned set)

| Path | What |
|---|---|
| `apps/app/lib/format.ts` | `credits()`, `usd()`, `amount(value, unit)`, `signedAmount()` — exact decimal-string display through the contract helpers; CREDIT never has "$", USD always says USD. `money()` kept (float, catalog prices only) and documented as not for balances. |
| `apps/app/app/(console)/billing/credit-reads.ts` | `CreditReads` port + `postgrestCreditReads(client, userId)` over the user's own Supabase session: `console_credit_wallets` (owner_user_id + kind filter), `console_credit_ledger` (wallet_id filter, keyset on `credit_ledger_wallet_created_idx`), bounded credits-in read, `console_legacy_usd_statement`, `consumer_jobs`. Fail-closed row parsers (JSON numbers, wrong unit, malformed instants → `internal_error`); PostgREST/transport errors → typed `dependency_unavailable` / `forbidden` / `invalid_cursor`; never throws. Ledger cursor is validated before any query exists (no filter injection). |
| `apps/app/app/(console)/billing/credit-view-model.ts` | Credits page model: Available / Reserved / Spent / Balance in credits; identity check; `no_wallet` / `exhausted` / `low` / `funded` with 402 `insufficient_credit` guidance; one-time grant line; ledger rows with request links; legacy USD statement state; page model with wallet-error propagation. |
| `apps/app/app/(console)/billing/credit-fixture.ts` | Reconciling in-memory `CreditReads` (SQL ordering), for preview and tests. |
| `apps/app/app/(console)/billing/credit-context.ts` | Page glue: preview gate → fixture; else `createClient()` (anon key + user JWT, RLS on) + `auth.getUser()` id → `postgrestCreditReads`. |
| `apps/app/app/(console)/billing/{page.tsx,credit-card.tsx,loading.tsx}` | Credits page (route `/billing`, title "Credits"); `balance-card.tsx` removed. |
| `apps/app/app/(console)/usage/credit-view-model.ts` | Usage page model over `consumer_jobs`: status, model/revision, tokens, charge vs hold per settlement state in the job's own unit, window cut on the ordered keyset stream, request-detail links. |
| `apps/app/app/(console)/usage/{page.tsx,usage-controls.tsx}` | Usage page: credit card + request table + pager + time-window select; `usage-chart.tsx` removed (no CREDIT daily aggregate exists). |
| `apps/app/app/(console)/usage/fake-console-context.ts` | `previewAllowed()` = `consoleContext(env) !== null` — one fixture gate; C's PREVIEW-01 mutant target unchanged. |
| `apps/app/tests/u/*` | 4 new node suites (27 cases), the real-PostgreSQL suite (6 cases) + its world script, 29 new mutants in the U runner. |

## Requirement coverage (brief §U1R)

| Brief item | Where | Test IDs |
|---|---|---|
| 1. exact CREDIT available/reserved/spent; separately labelled legacy USD; no float totals; one-time grant, no refill/expiry | `credit-view-model.ts`, `format.ts`, `credit-reads.ts` | U1R-F01..F04, B01, B02, B05, B07, R04, R05, P01, P02, P04 |
| 2. usage list correlates status, model/version, timestamps, units, settlement; pending/unknown explicit, no assumed zero; pagination/date boundaries consistent and indexed | `usage/credit-view-model.ts`, `consumer_jobs` keyset | U1R-U01..U08, R03, P03, P06 |
| 3. low/zero funds, active reservations, cancelled/failed/expired, adjusted ledger entries, delayed settlement; detail link; empty/unavailable states; UI totals vs durable ledger under concurrent requests | both view models; `credit_world.py` + `credit-pg.test.ts` | U1R-B03, B04, B06, B08, U04, U06, U07, P01..P06 |

Invariants held: no "$" on CREDIT anywhere (B01, U02, P03); CREDIT and USD never summed (per-unit parsing, U02/P03/P04); no payment/top-up/refill/"requests included" copy (B05); a failed read is an error state with retry, never a zero or a fake balance (B04, B08, R04, U07); production never serves fixtures (preview-context tests + compiled chunk shows `function f(a=process.env){return null}` for the gate); no privileged credential in browser bundles (grep of `.next/static` for `service_role|SERVICE_ROLE|CONSOLE_CURSOR_SECRET|fixture text` → 0 files); nothing logged.

## Tests first — fails-before record

| Suite / case | Before implementation | After |
|---|---|---|
| `tests/u/credit-format.test.ts` | `SyntaxError: '../../lib/format.ts' does not provide an export named 'amount'` — 1 fail | 4/4 |
| `tests/u/credit-reads.test.ts` | `ERR_MODULE_NOT_FOUND .../billing/credit-reads.ts`; first implementation then failed U1R-R02 (the forged-cursor case caught a query being built before validation) — fixed by validating first | 7/7 |
| `tests/u/credits-view-model.test.ts` | `ERR_MODULE_NOT_FOUND .../billing/credit-view-model.ts`. Old behaviour for the same wallet via v1 `balanceFigures`: `["Available now: $9,975.65432099","Promotional credit: $9,987.654321","Reserved: $12.00000001"]` — dollars and no Spent, which B01 refuses. B08 wallet-failure branch added first: 7 pass / 1 fail (ledger shown "empty" after a failed wallet read), then fixed | 8/8 |
| `tests/u/usage-credits-view-model.test.ts` | `ERR_MODULE_NOT_FOUND .../usage/credit-view-model.ts`; mutant M23 survived the first version, so U04 gained the released-hold assertion | 8/8 |
| `tests/u/credit-pg.test.ts` (real PostgreSQL) | new seam; proven discriminating by mutants run against it in scratch copies: M13 (Available shows balance) → P01 fails; M21 (pending shows a zero charge) → P03 fails; M05 (ledger not wallet-scoped) → P05 fails | 6/6 |

## Commands (UTC 2026-09-25 21:15–21:46, worktree root unless noted)

| Command | Exit | Result |
|---|---|---|
| `cd apps/app && pnpm install --frozen-lockfile` | 0 | lockfile unchanged |
| `make api-env` | 0 | pinned venv |
| `make console-test` | 0 | `# tests 384 / pass 378 / fail 0 / skipped 6` (baseline 351/351; +27 new cases; the 6 skipped are `credit-pg.test.ts`, which needs `U1R_PG_DSN` and says so) |
| `make console-lint` | 0 | `0 errors, 2 warnings` — both pre-existing (`lib/contracts/conformance.ts`, `lib/contracts/fake-services.ts`) |
| `make console-typecheck` | 0 | typegen + `tsc --noEmit` clean |
| `make console-mutants` | 0 | contracts self-test 14/14; contracts 212/212 killed; V 40/40; **U 2/2 self-checks, 93/93 killed (64 U1 + 29 U1R)**; C self-test 4/4, 104/104 killed, 0 stale |
| `cd apps/infrx-api && INFRX_D_TASK=app-u1r uv run --frozen python ../app/tests/u/credit_world.py` | 0 | world: 10 concurrent CREDIT flows + exhaustion race (2 admitted, 4 refused 402 `insufficient_credit`), `assert_no_drift` clean; `credit-pg.test.ts` 6/6 pass (run twice, both green) |
| same, against scratch copies with mutants M13 / M21 / M05 | 1 / 1 / 1 | each killed by P01 / P03 / P05 respectively |
| `cd apps/app && pnpm build` | 0 | `/billing` and `/usage` `ƒ`; compiled preview gate `return null`; no fixture/secret strings in `.next/static` |
| `docker ps -a \| grep u1r` | — | 0 containers left (the D harness removes its own at exit) |

Environment: Linux 7.0.0-1010-aws, Node 22, pnpm 9.15.9, Next 16.3.5; task-local PostgreSQL `infrx-app-u1r-postgres` on 127.0.0.1:55457 (plain postgres 16.14 image + shim, D harness), database `infrx_app-u1r_credit`, frozen DB clock. No hosted Supabase, pilot box, AWS/SSM, or other lane's port was touched. No dev server was started.

## Real-database oracle (how the UI is checked against the ledger)

`credit_world.py` reuses the D harness (labelled/locked container, `seed_admission`, the real `admit`/`claim`/`terminalize`) — no grant/hold/settlement SQL is written. Ten threads race CONSUMER_1's wallet through settled ×4, cancelled-with-usage, unknown (published, no usage → `held_unknown`), free (`invalid_media`), absorbed (`deadline_exceeded`), running and queued; a legacy USD grant and job; CONSUMER_2's own job; then an operator adjustment leaves room for two holds and six admissions race (2 admitted, 4 refused 402). `credit-pg.test.ts` then runs **the App's unchanged `postgrestCreditReads`** against a PostgREST double that renders each call as PostgREST's SQL (`json_agg` of the select / RPC) and executes it via `psql` as role `authenticated` with a JWT subject (both claim forms), and compares with owner-run durable queries:

- P01 wallet = Σ ledger, reserved = Σ active holds; card reconciles; wallet reads **low** at `0.00000001 credits` with the 402 guidance.
- P02 Spent = −Σ `inference_debit`; ledger walk at page size 2 visits every entry once; Σ = balance.
- P03 usage walk at page sizes 1/2/7 across identical timestamps = exactly the org's jobs; Σ settled CREDIT charged = Spent; Σ held/unknown holds = Reserved; no charge shown for any non-settled job; USD rows say USD; all five settlement states present.
- P04 legacy statement = Σ `public.credit_ledger.delta_usd`, labelled USD, rollout hold shown.
- P05 CONSUMER_2's session: own wallet only; none of CONSUMER_1's jobs; C1's wallet id → empty ledger and `0` credits-in; C1's org legacy statement → `forbidden` (42501).
- P06 window: 24h inside → all rows; 7d eight days later → none.

## Wiring requests (not applied)

1. **Navigation label** — `apps/app/components/sidebar.tsx`: `{ href: "/billing", label: "Balance", icon: CreditCard }` → `{ href: "/billing", label: "Credits", icon: CreditCard }` (03-app-spec: Credits; the route keeps its URL). Proof: page title is "Credits" (`billing/page.tsx`); add `assert.match(readFileSync("components/sidebar.tsx","utf8"), /href: "\/billing", label: "Credits"/)` to a coordinator-owned test.
2. **Sidebar balance in CREDIT** — `apps/app/app/(console)/layout.tsx` still shows `getBalance(session.orgId)` (legacy USD `org_wallet_summary`, `lib/credits.ts`). Patch:
   ```ts
   import { consumerCreditReads } from "./billing/credit-context";
   import { credits } from "@/lib/format";
   // replace the getBalance line:
   const wallet = await (await consumerCreditReads()).reads.wallet();
   const balance = !wallet.ok ? null : wallet.value === null ? "No credits yet" : credits(wallet.value.available);
   ```
   Proof: `tests/u/credit-reads.test.ts` U1R-R01/R04 (exact value or typed failure, never a zero) + `credit-pg.test.ts` P01 (available = durable). Coordinate with C0 (owner of `lib/credits.ts` successor).
3. **D10 SQL (only D10 writes SQL)** — (a) `consumer_jobs` gains optional `p_model`, `p_key_id`, `p_from`, `p_to` (and returns `key_id`, key name) so APP-08's per-key/model filters can exist; until then the page offers only the time window and says "Filtering by API key or model is not available yet". (b) a `console_credit_totals(p_user)` (credits-in and spent) to replace the bounded non-debit read behind "Spent" (`CREDITS_IN_BOUND = 100`, `ponytail:` note in `credit-reads.ts`). Proof: P02/P03 would assert against it unchanged.
4. **U4 route** — every usage row and ledger debit links to `/usage/<encodeURIComponent(request_id)>` (`requestDetailHref`). U4's page must be `apps/app/app/(console)/usage/[requestId]/page.tsx` (inside the U1R path; U4 owns the file). Proof: U1R-U06, B06.
5. **Other lanes' money display** — `lib/format.ts` `money()` is a float formatter and stays only for catalog price display; `app/(console)/models/page.tsx` (A3) and `app/(console)/admin/page.tsx` (U3) should use `credits()` / `amount(value, unit)` for any balance, charge or CREDIT rate.
6. **C0 overlap** — C0's real query port may subsume `postgrestCreditReads`; if it lands the same five reads, only `billing/credit-context.ts` changes. The v1 USD view models (`usage/view-model.ts` summary/daily/key-filter parts, `billing/view-model.ts`) are no longer rendered but are kept with their 64 U1 mutants until C0/V confirm no consumer; `usage/fake-console-context.ts` is still used by `/traces`.

No mutant-list wiring needed: `tests/u/run-mutants.mjs` is in the owned path and already in `make console-mutants`.

## Proposed ruling (coordinator numbers it)

"U1R: the consumer Usage and Credits pages read only through the signed-in user's own Supabase session (anon key + JWT, RLS on): `console_credit_wallets` filtered `owner_user_id = <auth user>` and `kind = consumer`; `console_credit_ledger` filtered by that `wallet_id`; `consumer_jobs`; `console_legacy_usd_statement` for the wallet's personal org. Spent = Σ(non-debit entries) − ledger_total, bounded, else shown unavailable. Any read failure is a typed error rendered with its recovery — never a zero, never fixture data. A job's charge is shown only when `settlement_state = settled`; holds are shown only while `held`/`unknown`."

## Unresolved inputs

- **P-01** (approved Marlin CREDIT rate): the low-funds threshold is a display constant (`LOW_FUNDS` = 1,000 credits, 10% of the grant, `ponytail:` note); replace with "fewer than N requests at the current rate" once the rate card is readable in the App.
- **P-02** (legacy USD transition): the statement shows any nonzero balance as a rollout hold, "not converted, cannot be spent here"; no conversion is implied.
- The consumer read surface is wallet-anchored (R66): an individual with legacy USD history but no CREDIT wallet yet sees "No credits yet" and no legacy section until the grant lands.
- Base lacks D10-FOLLOWUP 0022; nothing here depends on it.

## Remaining effort

Optimistic 1 h / likely 2 h / pessimistic 4 h, confidence medium — basis: lane code, tests, real-DB oracle and mutants are complete; remaining is one review/fix round and the coordinator-side wiring (nav label, sidebar CREDIT balance, D10 filters/totals, U4 route) which are outside this lane.
