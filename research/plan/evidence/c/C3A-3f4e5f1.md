# C3A — Trusted consumer actions — evidence (head 3f4e5f1c)

Lane C3A (track C, App), program 22. Worktree `.claude/worktrees/codex-app-c3a`, branch `codex/app-c3a`.

- Base: `46776646` (claude/consumer-v1 tip at dispatch). Predecessor merged first: `git merge --no-ff codex/app-c0` (C0 `ab0b5179`) → `69774fdd`, no conflicts.
- Head (code): `3f4e5f1c`. Commits: `33058670` tests first · `cf72d7e3` adapter + server actions · `567f0723` real-DB tests · `3f4e5f1c` mutants + store chain test.
- No push, rebase, reset, amend or stash. Nothing hosted touched (no Supabase project, pilot box, AWS/SSM). Task-local PostgreSQL `app-c3a` (55452) + this lane's PostgREST container/network, removed at exit (checked: no `c3a` container or network left).

## Changed paths (69774fdd..3f4e5f1c)

| Path | What |
|---|---|
| `apps/app/lib/services/actions.ts` (new) | The action adapter: `createConsumerActions()` (`createKey`, `revokeKey`, `claimGrant`), `supabaseKeyStore`, `sameOrigin`, `operatorCommand`/`runOperatorCommand`, `SIGNUP_CAMPAIGN` |
| `apps/app/app/actions.ts` | Shared server actions: `createConsumerKey`, `revokeConsumerKey`, `claimSignupGrant`, `operatorAction`; `signOut` now same-origin guarded |
| `apps/app/lib/services/console.ts` | One-token extension: `keyOf` exported (reused, not duplicated) |
| `apps/app/tests/c/actions.test.ts` (new) | 17 unit cases |
| `apps/app/tests/c/actions-postgrest.test.ts` (new) + `tests/c/realdb/actions_stack.py` (new) | 9 real-DB cases (skip visibly without the stack) + the stack script |
| `apps/app/tests/c/client-boundary.test.ts` | The walker stops at `"use server"` modules (the one legitimate crossing), with a positive/negative check |
| `apps/app/tests/c/mutants.json`, `tests/c/run-mutants.mjs` | 32 C3A mutants; `actions.test.ts` in the runner's suite |

## Brief items → where

1. **Narrow ports, current-context authorization, cross-site protection, nothing trusted from the client.**
   - Key create/revoke → `public.api_keys` through the individual's OWN JWT (`supabaseKeyStore`; 0001 owner policies + 0004 column grants decide). The org is `account.orgId`, the creator `account.userId` (C0's `resolveConsumerContext`: verified + own consumer wallet); audience is the column default `consumer` (not writable by the browser role). Revoke/find are scoped `id + org_id + audience=consumer` and revoke also `revoked_at is null`.
   - Onboarding/grant retry → `public.claim_signup_grant(session user, 'app_onboarding', null)` through the service-role client, server-side only; the DB derives verification and owns idempotency (R71). No grant SQL in the App.
   - Supported privacy settings: **none is mutable in v1** — consumer capture is off (P-09 in `15-pending-inputs.md`; U2 brief: no trace-sharing/annotation consent until its permission and deletion path exists), `api_keys.trace_mode` is not browser-writable, and no settings write port exists. So there is deliberately no settings action; `createKey` refuses `trace_mode` other than `off` with `unsupported_parameter` ("content capture is not available for consumer keys"). U2 renders truthful static copy.
   - Operator actions → `operatorCommand` (session `is_operator`, required reason ≤500, required idempotency key, allowlisted fields per action, exact CREDIT decimal string for adjustments, no zero, no "set balance", actor = `operator:<session user>`), `runOperatorCommand(command, port)`. **No App-reachable audited port exists** (G6B's operations live in `infrx`, which PostgREST does not expose), so `operatorAction` answers `dependency_unavailable` with "use the audited operations CLI" — never a silent success (WR-C3A-3).
   - Cross-site: every mutating action (and `signOut`) checks `sameOrigin(headers())` (Origin host must equal `x-forwarded-host`/`host`), on top of Next 16's own server-action Origin check.
   - Inputs are allowlisted (`badInput`, reused from C1): a smuggled `org_id`, `role`, `audience`, `amount`, `created_by`, `actor`, `unit` is `invalid_request` before any write.
2. **Idempotency/conflicts, one-time plaintext, no optimistic success.**
   - Double-click/concurrent/in-process retry: `createKey` with an `idempotency_key` single-flights per (individual, key): one insert; the replay returns the key's CURRENT metadata with `secret: null, replayed: true`; a different payload under the same key is `idempotency_conflict`; a failed attempt is forgotten so its retry can write. The secret is never stored (the memo holds a promise of the summary only).
   - Lost response: fixed copy "If a new key appears in your list, revoke it - its secret cannot be shown again". ponytail ceiling: the replay memory is per process; a cross-instance retry after a lost response mints a second key (revocable). Durable replay = WR-C3A-3(b).
   - Revoke is idempotent (a second click answers the first `revoked_at`), tenant-scoped and allowed while suspended (R33).
   - Grant: concurrent callbacks/claims resolve in the DB (one entitlement); `granted` vs `already_granted`; denials (`identity_reused`, `rollout_hold`, `retired`) are `{status:"held"}` with no reason (no enumeration); `unverified` → `forbidden`; outage/config failure → `dependency_unavailable`, never a grant; a row for another user or a non-string amount → `internal_error`.
   - Server actions revalidate only after an ok result (`/api-keys`, or the layout after a grant): the UI re-reads committed state.
   - DB error text never reaches a caller (fixed messages; `42501` → `forbidden`, else `dependency_unavailable`).
3. **Real DB role boundaries and callback retries; operator distinct from consumer; no duplicated grant SQL.** See the real-DB table below. Operator commands are a separate function with operator authority, reason, idempotency key and session actor.

## Commands (all from the worktree; exit codes as observed)

| Command | Exit | Result |
|---|---|---|
| `cd apps/app && pnpm install --frozen-lockfile` | 0 | done |
| `make api-env` | 0 | pinned env |
| `cd apps/app && node --test tests/c/actions.test.ts` at `33058670` (tests only) | 1 | **fails before**: `lib/services/actions.ts` absent (1 file-level failure) |
| same at `cf72d7e3` | 0 | 16/16 pass (after aligning two expected timestamps to `keyOf`'s 6-digit projection) |
| same at `3f4e5f1c` | 0 | 17/17 pass (+ store chain case) |
| `cd apps/infrx-api && INFRX_D_TASK=app-c3a INFRX_D1_IMAGE=supabase uv run --frozen python ../app/tests/c/realdb/actions_stack.py` | 0 | 9/9 pass, 0 skip; durable read-back: exactly **1** `signup_grant` ledger entry for the fresh individual (first attempt exited 1 at seed: bare image lacks GoTrue columns → reused `checks_signup.gotrue_columns`, as tests/d does) |
| `cd apps/app && pnpm test` | 0 | 419 tests: 398 pass, 0 fail, 21 skipped (12 C0 real-stack + 9 C3A real-stack) |
| `cd apps/app && pnpm lint` | 0 | 0 errors, 2 pre-existing warnings (conformance.ts, fake-services.ts) |
| `cd apps/app && pnpm exec next typegen && pnpm exec tsc --noEmit` | 0 | clean |
| `cd apps/app && node tests/contracts/run-mutants.mjs --self-test && pnpm test:mutants` | 0 | 212/212 killed |
| `cd apps/app && node tests/v/run-mutants.mjs` | 0 | 40/40 killed |
| `cd apps/app && node tests/u/run-mutants.mjs` | 0 | 64/64 killed |
| `cd apps/app && node tests/c/run-mutants.mjs --only <C3A ids>` (first run) | 1 | 32 killed, 1 runner error (C3A-OP-06: the declared case failed by TypeError, not assertion) → mutant dropped (the `hasOwn` guard is structural; the case still asserts `invalid_request` for `set_balance`) |
| `cd apps/app && node tests/c/run-mutants.mjs --self-test && node tests/c/run-mutants.mjs` | 0 | 174/174 killed (142 prior + 32 C3A), 0 survived/stale/runner errors |
| WR-C3A-1 applied in the tree, then `pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm test`; then `git restore` | 0/0/0 | composed result: 419 tests, 398 pass, 0 fail, 21 skip; boundary walker green with the dialog importing `@/app/actions` |

Accessibility: no UI file is changed on this branch (the UI changes are WR-C3A-1 for U2); the e2e accessibility suite was not run here.

## Tests and failure oracles

Unit (`tests/c/actions.test.ts`, fake store/RPC):

| Case | Oracle (defect it catches) |
|---|---|
| key create minted server-side | org/creator not from context; plaintext stored; hash ≠ sha256(secret); prefix mismatch; extra columns written |
| smuggled fields refused | client org/role/audience/amount/creator reaching the insert |
| name/capture validation | unbounded name; consumer key opting into capture |
| only ready, unsuspended | unverified/onboarding/signed-out/unavailable minting; suspended minting (R33) |
| replay never mints twice | second insert; secret on replay; stale metadata on replay |
| double-click | two inserts; two secrets |
| per individual and payload | idempotency key shared across users; replay of a different payload |
| failed write fixed text | DB message leak; RLS refusal read as outage; failure remembered so retry never writes |
| revoke scoped/idempotent/suspended | second revoke `not_found`; other tenant's key revoked; suspended cannot revoke |
| revoke malformed/unready | malformed id or unready context reaching the store; DB text leak |
| grant for the session user | input-derived user; float amount |
| grant never called unready | service client built for unverified/signed-out/unavailable |
| grant denials | reason leaked; outage as grant; foreign row accepted; missing service key text leaked |
| same-origin | look-alike origin; missing Origin; forwarded host ignored |
| operator validation | non-operator; missing reason/idempotency key; float/zero/non-canonical amount; client actor/unit; arbitrary action |
| operator port | no port reported as success; port exception text leaked |
| supabase store chain | revoke without org/audience/active scope; insert writing audience/trace mode/id |

Real DB (`tests/c/actions-postgrest.test.ts`, PostgREST v13.0.4 + Supabase PG, every migration):

| Case | Observed |
|---|---|
| DUR-RLS grant reachability | anon and an individual → `42501` on `claim_signup_grant`; `infrx.grant_signup_credit` not exposed |
| 5 concurrent claims, one verified individual | 1 `granted` + 4 `already_granted`, all `10000.00000000`; context turns `ready`; `console_wallet_summary.ledger_total = 10000.00000000`; a later claim `already_granted`; durable: 1 ledger entry |
| forged onboarding for an unverified user | DB answers `unverified` → `forbidden`; still no wallet |
| key create | row in own personal org, `audience=consumer`, `user_id` = creator (0009 guard), `key_hash = sha256(secret)` |
| forged context → other org / shared org | RLS refuses → `forbidden` (the DB backs the action) |
| revoke | own key once + idempotent; C1 → C2's key `not_found` and untouched; suspended C2: create `org_suspended`, operator-audience key `not_found`/untouched, own key revoked |
| non-owner member of a shared org | `not_found`, key untouched |
| **fails-before (old behaviour)** | the current `app/(console)/api-keys/actions.ts` update on an already-revoked key → `23514` whose message names the key id (the old action returns `error.message` verbatim); the adapter's second click answers the first revocation |
| known gap | an **unverified** individual's JWT can insert an `api_keys` row directly into their own org: `ACCEPTED (gap)`. The action refuses; the key is unfunded (no wallet). WR-C3A-4 |

## Wiring requests (not applied)

**WR-C3A-1 (U2 / coordinator) — the API Keys page uses the shared actions.** Files: `apps/app/app/(console)/api-keys/create-key-dialog.tsx`, `revoke-button.tsx`; delete `apps/app/app/(console)/api-keys/actions.ts` (returns DB `error.message` verbatim, 23514 on a second revoke, first-membership `getSession`, no verification/wallet gate, no idempotency). Proof: applied in this tree, `tsc`/`lint`/`pnpm test` green (419/398/0/21), then restored. U2 open point: the existing `rememberKey` keeps the plaintext in `sessionStorage` for the snippets; the brief says "not plaintext storage or redisplay" — recommend U2 drop it. Exact patch:

```diff
diff --git a/apps/app/app/(console)/api-keys/actions.ts b/apps/app/app/(console)/api-keys/actions.ts
deleted file mode 100644
index 5eafc535..00000000
--- a/apps/app/app/(console)/api-keys/actions.ts
+++ /dev/null
@@ -1,52 +0,0 @@
-"use server";
-
-import { revalidatePath } from "next/cache";
-import { generateKey, hashKey, keyPrefix } from "@/lib/keys";
-import { getSession } from "@/lib/session";
-import { createClient } from "@/lib/supabase/server";
-
-export type CreateResult = { ok: true; id: string; key: string } | { ok: false; error: string };
-
-/** Mints a key, stores only its SHA-256, and hands the secret back exactly once. */
-export async function createApiKey(name: string): Promise<CreateResult> {
-  const trimmed = name.trim();
-  if (!trimmed) return { ok: false, error: "Give the key a name." };
-  if (trimmed.length > 64) return { ok: false, error: "Name is too long (64 characters max)." };
-
-  const session = await getSession();
-  if (session.role !== "owner") return { ok: false, error: "Only owners can create keys." };
-
-  const key = generateKey();
-  const supabase = await createClient();
-  const { data, error } = await supabase
-    .from("api_keys")
-    .insert({
-      org_id: session.orgId,
-      created_by: session.userId,
-      name: trimmed,
-      prefix: keyPrefix(key),
-      key_hash: await hashKey(key),
-    })
-    .select("id")
-    .single();
-  if (error) return { ok: false, error: error.message };
-
-  revalidatePath("/api-keys");
-  return { ok: true, id: data.id as string, key };
-}
-
-export async function revokeApiKey(id: string): Promise<{ error?: string }> {
-  const session = await getSession();
-  if (session.role !== "owner") return { error: "Only owners can revoke keys." };
-
-  const supabase = await createClient();
-  const { error } = await supabase
-    .from("api_keys")
-    .update({ revoked_at: new Date().toISOString() })
-    .eq("id", id)
-    .eq("org_id", session.orgId);
-  if (error) return { error: error.message };
-
-  revalidatePath("/api-keys");
-  return {};
-}
diff --git a/apps/app/app/(console)/api-keys/create-key-dialog.tsx b/apps/app/app/(console)/api-keys/create-key-dialog.tsx
index 52bcdf64..650213e3 100644
--- a/apps/app/app/(console)/api-keys/create-key-dialog.tsx
+++ b/apps/app/app/(console)/api-keys/create-key-dialog.tsx
@@ -16,7 +16,7 @@ import {
 import { Input } from "@/components/ui/input";
 import { Label } from "@/components/ui/label";
 import { rememberKey } from "@/lib/keys";
-import { createApiKey } from "./actions";
+import { createConsumerKey } from "@/app/actions";
 
 export function CreateKeyDialog() {
   const router = useRouter();
@@ -26,6 +26,8 @@ export function CreateKeyDialog() {
   const [error, setError] = useState<string | null>(null);
   const [secret, setSecret] = useState<string | null>(null);
   const [copied, setCopied] = useState(false);
+  // One idempotency key per opened dialog: a double-click or retry replays, never mints a second key.
+  const [attempt, setAttempt] = useState(() => crypto.randomUUID());
 
   function reset(next: boolean) {
     setOpen(next);
@@ -34,6 +36,7 @@ export function CreateKeyDialog() {
       setSecret(null);
       setError(null);
       setCopied(false);
+      setAttempt(crypto.randomUUID());
       router.refresh();
     }
   }
@@ -42,13 +45,16 @@ export function CreateKeyDialog() {
     e.preventDefault();
     setPending(true);
     setError(null);
-    const result = await createApiKey(name);
+    const result = await createConsumerKey({ name, idempotency_key: attempt });
     setPending(false);
-    if (result.ok) {
+    if (!result.ok) setError(result.error.message);
+    else if (result.value.secret === null) {
+      setError("This key was already created by an earlier attempt and its secret cannot be shown again. Revoke it and create a new one.");
+    } else {
       // Kept in this tab only, so the Models snippets are runnable without a re-paste.
-      rememberKey(result.id, result.key);
-      setSecret(result.key);
-    } else setError(result.error);
+      rememberKey(result.value.id, result.value.secret);
+      setSecret(result.value.secret);
+    }
   }
 
   async function copy() {
diff --git a/apps/app/app/(console)/api-keys/revoke-button.tsx b/apps/app/app/(console)/api-keys/revoke-button.tsx
index 8e9e00a2..de34bc01 100644
--- a/apps/app/app/(console)/api-keys/revoke-button.tsx
+++ b/apps/app/app/(console)/api-keys/revoke-button.tsx
@@ -5,7 +5,7 @@ import { useRouter } from "next/navigation";
 import { Loader2, Trash2 } from "lucide-react";
 import { toast } from "sonner";
 import { Button } from "@/components/ui/button";
-import { revokeApiKey } from "./actions";
+import { revokeConsumerKey } from "@/app/actions";
 
 export function RevokeButton({ id, name }: { id: string; name: string }) {
   const router = useRouter();
@@ -14,9 +14,9 @@ export function RevokeButton({ id, name }: { id: string; name: string }) {
   async function revoke() {
     if (!confirm(`Revoke "${name}"? Calls using it start failing within a minute.`)) return;
     setPending(true);
-    const { error } = await revokeApiKey(id);
+    const result = await revokeConsumerKey(id);
     setPending(false);
-    if (error) toast.error(error);
+    if (!result.ok) toast.error(result.error.message);
     else {
       toast.success(`Revoked ${name}`);
       router.refresh();
```

**WR-C3A-2 (U3 / coordinator) — remove the arbitrary balance edit.** `apps/app/app/(console)/admin/actions.ts` `addCredit` inserts free-form `delta_usd` rows into the legacy `public.credit_ledger` with the service role (a direct balance edit, USD, no idempotency, no audit identity). Delete it and its form in `admin/page.tsx:18,126`; U3's operator forms call `operatorAction({action, …, reason, idempotency_key})` from `@/app/actions`, which today answers `dependency_unavailable` ("use the audited operations CLI") until WR-C3A-3(a). Test proving the composed result: `tests/c/actions.test.ts` "operator: …" cases + the boundary walker; U3 adds its page test.

**WR-C3A-3 (D10 + G, coordinator) — durable ports the App cannot reach today.**
(a) Operator: expose G6B's audited `grant_initial`, `adjust`, `set_suspension`, `revoke_key` to the App server (service-role `public.operator_*` wrappers calling the same `infrx` functions with `p_actor`, `p_reason`, `p_idempotency_key`, or an operator HTTP surface on infrx-api); then `app/actions.ts` passes a port instead of `null` (one line) and `OperatorPort.run` maps the command.
(b) Key-create idempotency across instances: e.g. `public.create_consumer_key(p_name text, p_prefix text, p_key_hash text, p_idempotency_key text)` (security invoker, owner policy still applies, unique `(created_by, idempotency_key)`, replay returns the row without any secret). Then `supabaseKeyStore.insert` calls it and the per-process memo becomes a fast path.

**WR-C3A-4 (D10) — close the unverified direct-insert gap.** `api_keys_insert_owner` (0001) checks ownership and `created_by = auth.uid()` only. Proposed additive policy condition: the creator owns a consumer credit wallet (or `infrx.verified_user(auth.uid()).verification_evidence_ref is not null`). Proof hook: flip the "known gap" case in `actions-postgrest.test.ts` to `assert.ok(error !== null)`.

**WR-C3A-5 (A2 / coordinator) — onboarding and callback call the grant.** The onboarding page (A2) calls `claimSignupGrant()` and renders `granted`/`already_granted` (show the balance from C0's reads, not the returned amount), `held` ("we could not add the signup credit automatically; contact support"), `forbidden` (verify first), `dependency_unavailable` (retry). The GET auth callback cannot pass the Origin check, so it composes directly: `createConsumerActions().claimGrant((await consumerSession()).context, () => createAdminClient() as unknown as RpcClient)` (idempotent and bound to the session user, so a cross-site trigger grants nothing extra). Then `consoleShell`'s `routes.onboarding` names the route.

**WR-C3A-6 (coordinator, optional) — make target for the real-DB actions stack**, e.g. `app-c3a-realdb: cd $(API) && INFRX_D_TASK=app-c3a INFRX_D1_IMAGE=supabase uv run --frozen python ../app/tests/c/realdb/actions_stack.py`. The track-C mutant runner is already in `make console-mutants`.

## Proposed rulings (coordinator numbers them)

- Consumer key idempotency is per (individual, idempotency key); a replay returns current metadata with `secret: null`; a lost first response is revoke-and-recreate, never redisplay.
- The App's grant claim passes `campaign_version = 'app_onboarding'` (audit metadata only, R71); held reasons are not shown to the individual.
- No mutable consumer privacy setting in v1 (capture off, P-09); `trace_mode` other than `off` on consumer key creation is `unsupported_parameter`.

## Unresolved inputs / open issues

- P-05 (public onboarding, email/callback config) — A2/I2A; this lane's grant path is tested locally only.
- P-09 — no privacy mutation until the permission/deletion path exists.
- Operator actions are unavailable in the App until WR-C3A-3(a); operators keep the audited CLI.
- Cross-instance key-create replay (WR-C3A-3(b)); unverified direct key insert (WR-C3A-4).

## Remaining effort

C3A itself: done pending review. Wiring (coordinator/U2/U3/A2): optimistic 1 h, likely 2 h, pessimistic 4 h; D10 ports WR-3/4: 3/5/8 h. Confidence medium (basis: WR-1 already proven composed; WR-3 depends on D10/G scheduling).

## Fix round (2026-09-25, code head `dd0d1a45`)

Review findings 0-C3A-VCM-1, 0-C3A-VCM-2, 1-C3A-V-1, 1-C3A-V-2, 1-C3A-V-3. Commits: `bb9f8a15` tests first · `dd0d1a45` fix. No push/rebase/reset/amend/stash; nothing hosted touched; the lane's containers/network removed at exit (checked).

| Finding | Status | What changed |
|---|---|---|
| 0-C3A-VCM-1, 1-C3A-V-1 (actions.ts composition untested) | **fixed** | The composition moved into `consoleActions(deps)` in `lib/services/actions.ts`: Origin check first, then session/context/store, then the adapter, then `revalidate` only on `ok`; `signOut` ends the session only same-origin; `operator` re-reads authority from the session and, with no port, answers `dependency_unavailable`. `app/actions.ts` now only supplies `headers`, `consumerSession`, the cookie client, `getSession` and `revalidatePath`; each export is a one-line delegation. Three seam cases + mutants SEAM-01..07 (the reviewer's V-30/V-31/V-32 and 1-V-1 (1)(2)(3) re-expressed at the seam). Residual: a mutant inside a one-line delegation in `app/actions.ts` is still untestable under R48 (no `next/*` in node --test). |
| 0-C3A-VCM-2 (revoke ok only when revoked: real-DB only) | **fixed** (offline half) | `memoryStore({ rlsFiltersRevoke: true })` (RLS filters a non-owner's update to zero rows, no error): the revoke must be `not_found` and the key stays active. Mutant C3A-REV-06 = review V-05, killed offline. The stack half is WR-C3A-6 (restated below, proven composed). |
| 1-C3A-V-2 (two grant adapters, two campaigns) | **fixed** (C3A side) | C3A drops its grant adapter: `claimGrant`, `GrantOutcome`, `SIGNUP_CAMPAIGN = "app_onboarding"` and the `claimSignupGrant` server action are deleted. The App's one adapter is A2's `app/(auth)/grant.ts` (`SIGNUP_CAMPAIGN = "consumer-v1"` in `flow.ts`, already wired into the callback and `/welcome` on `codex/app-union`). Nothing on any App branch imported C3A's grant (checked u1r/u2/u3/u4/a2/union). The real-DB grant cases now call `claim_signup_grant` exactly as A2's `claimArgs` does and still pin the DB boundary and the 5-way race. Grant mutants C3A-GRANT-01..06 retired with the code. **WR-C3A-5 withdrawn.** |
| 1-C3A-V-3 (unverified direct insert into api_keys) | **not fixed** (D10 SQL) | Unchanged: the stack still prints `ACCEPTED (gap)`. WR-C3A-4 restated below; coordinator tracks it before E4. |

### Commands (fix round)

| Command | Exit | Result |
|---|---|---|
| `cd apps/app && node --test tests/c/actions.test.ts` at `bb9f8a15` (tests only) | 1 | **fails before**: `lib/services/actions.ts` does not provide `consoleActions` |
| same at `dd0d1a45` | 0 | 18/18 pass (17 − 3 grant + 1 RLS-filtered revoke + 3 seam) |
| `cd apps/app && pnpm test` | 0 | 420 tests: 399 pass, 0 fail, 21 skipped (12 C0 + 9 C3A real-stack) |
| `cd apps/app && pnpm lint` | 0 | 0 errors, 2 pre-existing warnings |
| `cd apps/app && pnpm exec next typegen && pnpm exec tsc --noEmit` | 0 | clean |
| `cd apps/app && node tests/contracts/run-mutants.mjs --self-test && pnpm test:mutants` | 0 | 212/212 killed |
| `cd apps/app && node tests/v/run-mutants.mjs` | 0 | 40/40 killed |
| `cd apps/app && node tests/u/run-mutants.mjs` | 0 | 64/64 killed |
| `cd apps/app && node tests/c/run-mutants.mjs --self-test && node tests/c/run-mutants.mjs` | 0 | 176/176 killed (142 prior + 34 C3A: 26 kept, 6 grant retired, REV-06 + SEAM-01..07 added), 0 survived/stale/runner errors |
| WR-C3A-6 applied in the tree, `make console-c3a-real`, then `git restore Makefile` | 0 | 9/9 real-DB pass, 0 skip; durable: 1 `signup_grant` ledger entry for the fresh individual; `ACCEPTED (gap)` still printed (V-3) |
| WR-C3A-1 applied in the tree, `tsc --noEmit`, `node --test tests/c/client-boundary.test.ts tests/c/actions.test.ts`, then restored | 0/0 | composes unchanged (23/23); the action signatures did not change |

New/changed cases and their oracles:

| Case | Oracle |
|---|---|
| action seam: cross-site refused before any session, store or refresh | Origin check removed or moved after resolution (SEAM-01/02); cross-site sign-out ends the session (SEAM-03) |
| action seam: refresh only after an acknowledged change | unconditional refresh = optimistic success (SEAM-04); a successful change never refreshes (SEAM-05) |
| action seam: operator is the session's, unavailable with no port | silent operator success (SEAM-06); authority not from the session (SEAM-07) |
| key revoke: a revoke that changed nothing is ok only when the key really is revoked | an RLS-filtered, still-active key reported revoked (REV-06 = review V-05) |

### Wiring requests after the fix round

- **WR-C3A-1** unchanged (proven again above).
- **WR-C3A-2**, **WR-C3A-3** unchanged.
- **WR-C3A-4 (D10, restated)** — `api_keys_insert_owner` (0001: `is_org_owner(org_id) and created_by = auth.uid()`) admits an unverified individual's direct insert (stack case 9, `ACCEPTED (gap)`). D10 adds a condition in a new migration (0001 is immutable) that the creator is verified, e.g. through a `security definer` public helper over `infrx.verified_user(auth.uid()).verification_evidence_ref is not null` (the `infrx` function is not executable by `authenticated`, so the policy cannot call it directly). D10 must check pilot/invited owners still pass. Proof: in `tests/c/actions-postgrest.test.ts` case 9, replace the diagnostic with `assert.ok(error !== null, "an unverified individual cannot insert a key")` and run `make console-c3a-real` on the merged SHA.
- **WR-C3A-5 withdrawn** (C3A has no grant adapter; A2's `grant.ts` is the one).
- **WR-C3A-6 (restated against `codex/app-union`, exact)** — beside `console-c0-real`:

```diff
--- a/Makefile
+++ b/Makefile
@@ .PHONY
-.PHONY: integration consumer-local backend-certify app-e2e backend-local check api-env api-test api-mutants console-test console-lint console-typecheck console-mutants bench-test console-c0-real
+.PHONY: integration consumer-local backend-certify app-e2e backend-local check api-env api-test api-mutants console-test console-lint console-typecheck console-mutants bench-test console-c0-real console-c3a-real
@@ after the console-c0-real recipe
+# C3A DUR-RLS / CONSOLE-FLOWS: the trusted actions through real Supabase PostgreSQL + PostgREST
+# (Docker; fails visibly without it). Gate for C3A / APP-M1 and E4.
+console-c3a-real:
+	cd $(API) && INFRX_D_TASK=app-c3a INFRX_D1_IMAGE=supabase uv run --frozen python ../app/tests/c/realdb/actions_stack.py
```

Proof: the run above (applied to this tree's Makefile, same recipe).

### Proposed rulings (replaces the grant one above)

- The App has one `claim_signup_grant` adapter: A2's `app/(auth)/grant.ts`, campaign `consumer-v1` (`flow.ts`). C3A's shared actions carry no grant.
- Console server actions are composed in `consoleActions` (tested); `app/actions.ts` only supplies Next's request APIs and clients, one delegation per export.

### Remaining effort (after the fix round)

C3A: done pending re-review. Wiring WR-1/2/6: optimistic 0.5 h, likely 1 h, pessimistic 2 h. D10 WR-3/WR-4: 3/5/8 h. Confidence medium (basis: WR-1 and WR-6 proven composed in this tree; WR-3/4 depend on D10/G scheduling).
