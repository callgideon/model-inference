# J1 — Dry-run sampler, rubric and score validation

## Task and status

| Field | Value |
|---|---|
| Task | **J1** — deterministic consented sampling, versioned rubric, judge-output validation, worst-case dry-run cost. Oracles JUDGE-SCORES, F-CONTRACT |
| Owner / session | Claude Opus 5 (1M context), implementation session `session_01XbryjFN2xhdKwhuUQvbyFn` |
| Status | **implemented** — the focused suite, the whole `make api-test` suite and all 35 declared mutants pass; the exported `judge` conformance suite passes against the **shared fake**. Not integrated: no real `JudgeCoordinator`, no ClickHouse candidate source, no router wired into the composition root. Nothing was pushed, deployed or submitted to a provider; no network egress and no paid call was made. |

## Source

| Field | Value |
|---|---|
| Base SHA | `8744418` (integration branch `claude/infrx-impl`; F1, F2 both halves, F2.1, E1, I1, rulings R1–R55) |
| Implementation SHA | `d997be7` |
| Integrated SHA | — (coordinator merges) |
| Branch / worktree | `codex/j1-judge-dryrun` in `.claude/worktrees/codex-j1` |
| Commits | 3: `28608d2` the `infrx/judge` module, `22b3751` the `tests/j` suite, `d997be7` the R32 mutant list plus the two test fixes it forced |

## Requirement coverage

Test IDs are pytest function names in `apps/infrx-api/tests/j/`. Every invariant below
has at least one single-edit mutant that the named case kills (see **Results**).

### Consent — "trace opt-in is not evaluation consent"

| Test ID | Invariant demonstrated |
|---|---|
| `test_trace_opt_in_is_not_evaluation_consent` | Selection is gated on `ConsentSnapshot.allows_evaluation(now)`, i.e. `evaluation_consent` **and** `trace_mode = full` **and** a *current* record. Five separate withholdings each refuse with `consent_missing` **before any candidate is examined**, so there is no partial draw: evaluation not consented, `minimal` mode, `off` mode, revoked, and not yet effective. A full-mode trace on its own selects nothing. |
| `test_one_orgs_consent_never_authorizes_another_orgs_traces` | r1 R10: the consent's `org_id` must equal the org being sampled, else `not_found`. |
| `test_another_tenants_trace_is_never_a_sample` | A candidate whose `org_id` differs is excluded `not_owned`, even though the source port was asked for one tenant — the port is trusted to answer, not to be the tenant boundary. |
| `test_a_dry_run_without_current_evaluation_consent_refuses` | The same three refusals through `plan_dry_run`, i.e. on the composed path. |

### Exclusions — missing, expired and lost content

| Test ID | Invariant demonstrated |
|---|---|
| `test_content_that_cannot_be_read_is_excluded_with_its_reason` | All five non-`available` `ContentState` values are excluded, with `lost`, `expired` and `missing` (pending / metadata_only / off) kept apart as distinct reasons. 02: "revoked/expired/missing content is skipped before egress." |
| `test_a_non_full_trace_is_excluded_even_with_consent` | A `minimal`-mode trace is excluded `trace_mode_not_full` even under full evaluation consent. |
| `test_a_trace_already_labelled_at_this_rubric_version_is_excluded` | A trace already labelled at rubric version 1 is excluded `already_calibrated` there and **is** a candidate at version 2 (J4: a new version is a new series). |
| `test_unreadable_content_never_reaches_the_plan` | The same exclusions through `plan_dry_run`; every excluded trace is reported, so "why was this not judged" is a lookup. |

### Stratification — the ~50-label calibration design

| Test ID | Invariant demonstrated |
|---|---|
| `test_the_design_is_25_uniform_15_failures_10_feedback` | `research/traces/06` §3.9's design exactly: from 80 eligible candidates the draw is `{failures: 15, feedback: 10, uniform: 25}` = 50, and the 30 overflow candidates are reported `stratum_full` rather than spilled into another stratum. |
| `test_a_stratum_that_cannot_be_filled_is_reported_not_backfilled` | An undersupplied stratum yields fewer samples and a recorded `shortfall`; it is never backfilled from another stratum, because J3 grades agreement per stratum and 50 uniform samples are not this design. |
| `test_a_failing_trace_claims_the_failures_stratum_before_feedback` | `06` §3.1's ordering (always → feedback → sample): a failing trace that also carries customer feedback is a `failures` sample, counted once. |
| `test_a_design_size_is_a_nonnegative_integer` | A design size that is negative, a boolean or a float is refused. |

### r1 R43 — ordinary customer feedback is not a calibration label

| Test ID | Invariant demonstrated |
|---|---|
| `test_ordinary_customer_feedback_is_a_stratum_not_a_calibration_label` | For each of `thumb`, `rating`, `correction`, `comment`: the row has `calibration_set = False` and `rubric_version = None`, the trace is a **`feedback`-stratum sample**, and `calibration_labels` is empty. Only a `calibration_label` entry with `calibration_set` is calibration membership, so the console's thumbs-up can neither exclude the trace as "already labelled" nor become an operator verdict. |
| `test_a_judges_own_score_is_not_customer_feedback` | A `judge`-authored row is not customer feedback, so the judge's first run cannot move every trace it scored into the feedback stratum and grade itself. |
| `test_an_operator_label_is_not_the_feedback_stratum` | A label at another rubric version keeps the trace eligible, as a `uniform` sample: it is operator data, not a customer signal. |

### Determinism

| Test ID | Invariant demonstrated |
|---|---|
| `test_the_same_seed_picks_the_same_samples_whatever_the_scan_order` | Ranking by `blake2b(seed ‖ request_id)` makes the draw independent of scan order (the reversed candidate list gives the identical sample ids) and dependent on the seed (a different seed gives a different draw). `06` §3.1 uses `cityHash64` for the same reason: a run that dies half way does not re-roll the dice. |
| `test_selection_is_stable_when_the_population_grows` | A trace already drawn stays drawn when later traces arrive, which is what makes an interrupted calibration resumable. |
| `test_a_dry_run_is_repeatable` | The same property through `plan_dry_run`. |

### Rubric as versioned data

| Test ID | Invariant demonstrated |
|---|---|
| `test_a_rubric_version_is_an_integer_in_range` | r1 R43: an integer in 1..1000 on the rubric, and therefore on runs, samples and scores. `"1"`, `True`, `1.0`, `0`, `1001` and `None` are each refused; 1000 is accepted. |
| `test_a_rubric_needs_criteria_with_distinct_names_and_real_ranges` | No empty criteria list, no duplicate names, no empty score range. |
| `test_the_pass_rule_belongs_to_the_rubric_version` | `06` §3.3's rule: pass iff relevance ≥ 4, groundedness ≥ 4, format ≥ 3, refusal ≥ 3 (completeness is scored but not in the rule). Also, separately: a criterion in the rule with **no score** fails it, and a media-dependent criterion in the rule can never pass with `media=False` whatever the caller's dictionary says. |

### Judge output validation (untrusted input)

| Test ID | Invariant demonstrated |
|---|---|
| `test_a_well_formed_result_is_accepted_with_its_rubric_version` | A valid result becomes `JudgeScores` carrying the rubric version, the five scores and `limited=False`. |
| `test_a_result_that_is_not_an_object_is_rejected` | A string, `None`, a list and an integer are each `not_an_object` — a rejection with a reason, never a raise. |
| `test_a_missing_or_extra_field_is_rejected_by_name` | Closed key set: a missing criterion is `missing_field` and an extra key `unexpected_field`, each naming the field. |
| `test_a_criterion_needs_exactly_a_score_and_a_rationale` | Six malformed criterion shapes are `malformed_criterion`. |
| `test_a_score_outside_the_allowed_range_is_rejected` | 0, 6, −1 and 99 are `score_out_of_range`; 02: "Validate JSON structure and allowed scores before projection." |
| `test_a_score_that_is_not_an_integer_is_rejected` | `True`, `False`, `4.0`, `"4"` and `None` are `score_not_an_integer` — a float would round into the projection and a boolean would compare as one. |
| `test_the_range_boundaries_themselves_are_accepted` | 1 and 5 are accepted, so the bound is inclusive rather than off by one. |
| `test_a_rationale_is_required_and_bounded` | A missing, blank or non-text rationale is refused; exactly 300 characters is accepted and 301 is `rationale_out_of_bounds`. |
| `test_a_rationale_bound_counts_code_points` | r1 R54: 300 astral-plane code points are accepted, 301 refused. |
| `test_notes_are_bounded_text` | 500 characters accepted, 501 and a non-string refused. |
| `test_overall_pass_must_be_a_boolean_and_must_match_the_rubric` | `"true"` and `1` are refused; a claim that contradicts the rubric's rule in either direction is `overall_pass_inconsistent`. The pass bar belongs to the rubric version, not the model. |

### Missing media ⇒ limited, never a groundedness pass

| Test ID | Invariant demonstrated |
|---|---|
| `test_a_sample_without_media_is_marked_limited` | The `limited` flag is set on the sample at selection, so the prompt, the validator and the report read one fact. |
| `test_without_media_groundedness_is_not_scored_at_all` | A groundedness score arriving for a media-less sample is **rejected** (`media_dependent_score_without_media`), not accepted and ignored: it is a claim about frames that were never sent. |
| `test_a_limited_evaluation_is_accepted_and_can_never_pass` | A limited result is accepted with `limited=True`, no groundedness score and `overall_pass=False`; a *claimed* pass is refused rather than silently downgraded, including with every other criterion at 5. 02: "No media means no groundedness pass: record limited evaluation." |
| `test_with_media_groundedness_is_required` | The converse: with media, an absent groundedness score is `missing_field`. |

### Duplicate delivery

| Test ID | Invariant demonstrated |
|---|---|
| `test_results_deduplicate_by_run_sample_and_rubric_version` | 02: the dedupe key is `(run_id, sample_id, rubric_version)`. A replayed delivery returns `first=False` and the **first** outcome, with one entry stored; a different rubric version, sample or run is a new entry. |
| `test_a_rejection_is_an_outcome_and_dedupes_like_one` | A replayed malformed result does not write a second error row. |

### Cost dry run, and the egress guard

| Test ID | Invariant demonstrated |
|---|---|
| `test_the_estimate_is_a_worst_case_at_the_versioned_rate` | The per-sample amount is `money.maximum_hold` (ceiling rounding) on the configured ceilings at the injected versioned rate; the total is the ceiling of per-sample × samples; zero samples cost zero. |
| `test_reasoning_tokens_are_billed_as_output_and_included` | `06` §3.8: adaptive thinking is billed as output, so `billed_output_tokens = output + reasoning` and the shipped ceilings include a thinking budget. A reservation counting only `max_tokens` would under-reserve by the whole thinking budget. |
| `test_the_reservation_is_never_reduced_by_a_batch_discount` | `06` §3.8's ×0.5 batch price is **not** applied: halving a worst case turns it into an average. |
| `test_a_ceiling_that_would_reserve_nothing_is_refused` | A zero or negative output ceiling, a boolean and a float are refused — the r1 R55 defect (a zero ceiling makes a zero hold and then bills whatever arrives) in the judge's currency. |
| `test_a_sample_count_is_a_nonnegative_integer` | −1, `True`, `1.5` and `"5"` are refused. |
| `test_the_shipped_rate_table_is_empty_and_says_why` | Prices come only from `research/cross-cutting/cloud-pricing.md`; that document prices GPU capacity and carries **no per-MTok row for any judge model**, so `APPROVED_RATES` is empty, the estimate is unpriced with `price_version is None`, and the note carries the `⚠️ TO BE VERIFIED` marker naming the document. |
| `test_an_unknown_model_is_unpriced_rather_than_free` | An unpriced model answers "unpriced", never zero. |
| `test_a_rate_row_is_validated_at_the_boundary` | A negative rate, a float, an exponent, `NaN`, and an empty price version / model / source are each refused: a negative output rate would turn a debit into a credit and a row with no source is a price nobody approved. |
| `test_a_rate_row_normalizes_to_the_money_scale` | Rates go through `money.parse` (no floats anywhere) and lookup is by model. |
| `test_the_defaults_can_never_authorize_a_submission` | 08 §5: `JUDGE_MODE=dry_run`, `JUDGE_LIVE_BUDGET_USD=0`. Each guard refuses **on its own account** (asserted by message), so flipping one setting is not enough to spend money. |
| `test_a_pricing_estimate_without_a_hard_maximum_cannot_authorize_a_submission` | 02, verbatim: an unpriced estimate is refused even in live mode with a funded budget. |
| `test_a_worst_case_over_the_budget_is_refused` | The worst case must fit the configured budget. |

### Zero egress / no provider SDK on the dry-run path

| Test ID | Invariant demonstrated |
|---|---|
| `test_the_judge_package_never_imports_the_provider_sdk` | A regex over every `infrx/judge/*.py` finds no `import anthropic` / `from anthropic …`, and the regex is shown to fire on the two lines it forbids, so the check is not vacuous. |
| `test_importing_the_dry_run_path_loads_no_provider_sdk` | In a **fresh interpreter**, importing `infrx.judge`, `plan_dry_run` and `require_live_submission` leaves no `anthropic*` module in `sys.modules`. |
| `test_the_provider_sdk_is_importable_so_the_check_means_something` | `anthropic` 0.125.0 **is** installed (the pinned `judge` extra), so the two checks above are not passing merely because the package is absent. |

### F-CONTRACT

| Test ID | Invariant demonstrated |
|---|---|
| `test_the_plan_projects_into_the_frozen_judge_run_record` | The plan becomes a `records.JudgeRun` with `state=dry_run`, `reserved_cost=0`, an **integer** `rubric_version` (r1 R43), the selected sample ids, no submit intent and no batch id; and it round-trips through the record's JSON form. An estimate reserves nothing, so it cannot consume another run's budget. |
| `test_the_shared_coordinator_keeps_a_dry_run_reservationless` | Reserving the plan through the shared `FakeJudgeCoordinator` — the boundary D6 will implement — yields `dry_run` with zero reserved cost, leaves `available()` at zero, and `begin_submit` refuses `budget_exceeded`. J keeps no second budget table (03). |
| `test_the_exported_judge_conformance_suite_passes_against_the_shared_fake` | All 13 exported `judge` cases run (`ran == len(cases) == 13`) with **zero** skips collected, and every optional `judge` hook in `OPTIONAL_HOOKS` is provided — so no missing hook can be read as a pass (r1 R32). |

### R32 mutant list

| Test ID | Invariant demonstrated |
|---|---|
| `test_the_list_is_well_formed` | Unique mutant names, every mutant names an existing case and states an invariant, and no mutant is a no-op edit. |
| `test_the_required_invariants_each_have_a_mutant` | The brief's five invariants (consent check, stratification bounds, score range, dedupe key, limited flag) each have at least one declared mutant, named explicitly so a later edit cannot drop one. |
| `test_mutant_is_killed` | Each selected mutant is killed by the cases it names. |
| `test_the_runner_cannot_report_a_false_kill` | The runner's own honesty: a no-op edit **survives**, a syntax error is `broken_runner`, a lethal edit under the wrong case name **survives** (a kill must come from the case claiming the invariant), a stale anchor is `misdeclared`, and a mutant naming no case is `misdeclared`. |

## Environment

| Field | Value |
|---|---|
| Classification | **local**, single host. No cloud, no container, no external service, no credential used. |
| Host | `Linux 7.0.0-1010-aws x86_64` |
| Python | `3.12.3 (main, Aug 31 2026, 10:18:26) [GCC 13.3.0]`, pinned by `apps/infrx-api/.python-version` = `3.12` |
| Package manager | `uv 0.11.8 (x86_64-unknown-linux-gnu)`, environment installed with `make api-env` (`uv sync --frozen --all-extras`) |
| Key packages | `pydantic 2.13.5`, `pytest 8.4.2`, `anthropic 0.125.0` (installed, deliberately never imported) |
| Services | none. J uses fakes until integration (08 §8: no task-local port for J), so no container was created and none needs removing. |
| Environment variables | none set for the runs except `INFRX_MUTANTS` (values `all` / unset) where noted. `JUDGE_MODE` and `JUDGE_LIVE_BUDGET_USD` were never exported; the suite passes settings objects in process. |
| Seeds | selection seeds are test inputs, not RNG state: `"j1-calibration-2026-09"` (`test_sampling.py`), `"j1-dry-run"` and `"other"` (`test_dryrun.py`), `"drill"` (the failure drill). Nothing in the module or the suite reads a system RNG or a clock. |

## Commands

All from the worktree root `/home/rey/workspace/rey/code/model-inference/.claude/worktrees/codex-j1`
(or `apps/infrx-api` where shown), 2026-09-21 06:14Z–06:25Z.

### 1. Focused suite — `uv run --frozen pytest -q tests/j` (exit 0)

```
........................................................................ [ 71%]
.............................                                            [100%]
101 passed in 7.84s
```

Per file:

```
test_sampling    22 passed in 0.21s
test_rubric      29 passed in 0.25s
test_cost        29 passed in 1.38s
test_dryrun      9 passed in 0.21s
test_mutants     12 passed in 8.55s
```

### 2. Whole Python suite with every mutant — `INFRX_MUTANTS=all make api-test` (exit 0)

```
1058 passed, 2 warnings in 219.37s (0:03:39)

[exited with code 0]
```

The two warnings are pre-existing third-party deprecations from
`fastapi`/`starlette` `testclient`, unrelated to this task.

### 3. Canonical target — `make api-test` (exit 0)

```
START 2026-09-21T06:19:10Z
771 passed, 2 warnings in 32.70s
api-test exit=0
END 2026-09-21T06:19:43Z
```

The default target runs the mutant *subsets* (contracts' nine, track J's five), which
is why it collects fewer cases than run 2; run 2 is the same suite with
`INFRX_MUTANTS=all`.

### 4. Track J's whole mutant list — `uv run --frozen python tests/j/mutants.py` (exit 0)

```
[killed       ] consent_check_skipped: 1 failed, 88 deselected in 0.36s
[killed       ] consent_from_the_snapshot_only: 1 failed, 88 deselected in 0.35s
[killed       ] consent_of_any_org_accepted: 1 failed, 88 deselected in 0.35s
[killed       ] foreign_trace_selected: 1 failed, 88 deselected in 0.34s
[killed       ] stratum_bound_ignored: 1 failed, 88 deselected in 0.34s
[killed       ] design_collapses_to_one_bound: 2 failed, 87 deselected in 0.35s
[killed       ] failures_lose_their_priority: 2 failed, 87 deselected in 0.35s
[killed       ] shortfall_reported_as_filled: 1 failed, 88 deselected in 0.35s
[killed       ] selection_follows_the_scan_order: 1 failed, 88 deselected in 0.36s
[killed       ] seed_ignored: 1 failed, 88 deselected in 0.35s
[killed       ] any_feedback_counts_as_calibration: 1 failed, 88 deselected in 0.34s
[killed       ] a_label_at_any_version_excludes: 1 failed, 88 deselected in 0.35s
[killed       ] judge_scores_count_as_customer_feedback: 1 failed, 88 deselected in 0.34s
[killed       ] unreadable_content_selected: 6 failed, 83 deselected in 0.36s
[killed       ] non_full_trace_selected: 1 failed, 88 deselected in 0.35s
[killed       ] limited_flag_lost: 1 failed, 88 deselected in 0.34s
[killed       ] limited_result_not_marked: 2 failed, 87 deselected in 0.34s
[killed       ] missing_criterion_counts_as_a_pass: 1 failed, 88 deselected in 0.35s
[killed       ] media_criterion_passes_without_media: 1 failed, 88 deselected in 0.34s
[killed       ] media_criteria_scored_without_media: 1 failed, 88 deselected in 0.35s
[killed       ] score_range_unchecked: 4 failed, 85 deselected in 0.35s
[killed       ] boolean_score_accepted: 2 failed, 3 passed, 84 deselected in 0.35s
[killed       ] closed_key_set_opened: 1 failed, 88 deselected in 0.34s
[killed       ] rationale_bound_ignored: 2 failed, 87 deselected in 0.35s
[killed       ] overall_pass_claim_trusted: 2 failed, 87 deselected in 0.35s
[killed       ] rubric_version_is_not_an_integer: 1 failed, 88 deselected in 0.38s
[killed       ] dedupe_key_drops_the_rubric_version: 1 failed, 88 deselected in 0.35s
[killed       ] duplicate_overwrites_the_first_outcome: 2 failed, 87 deselected in 0.37s
[killed       ] dry_run_can_submit: 1 failed, 88 deselected in 0.34s
[killed       ] zero_budget_can_submit: 1 failed, 88 deselected in 0.35s
[killed       ] unpriced_estimate_can_submit: 1 failed, 88 deselected in 0.34s
[killed       ] over_budget_can_submit: 1 failed, 88 deselected in 0.35s
[killed       ] reasoning_tokens_not_reserved: 1 failed, 88 deselected in 0.34s
[killed       ] zero_output_ceiling_accepted: 1 failed, 4 passed, 84 deselected in 0.35s
[killed       ] a_shipped_rate_appears_from_nowhere: 2 failed, 87 deselected in 0.35s

35/35 killed
```

`uv run --frozen python tests/j/mutants.py --list` (exit 0), last line:

```
35 mutants over 33 named cases
```

### 5. Conformance suite against the adapter J consumes

Run inside case 1 as
`test_the_exported_judge_conformance_suite_passes_against_the_shared_fake`, which
asserts `ran == len(judge_cases()) == 13` with an empty skip list. The adapter under
test is `infrx.contracts.fakes.FACTORIES["judge"]` — the **shared fake**, because J1
ships no `JudgeCoordinator` adapter (D6 owns the real one). Per the contracts README
that is **implemented**, never integrated.

### 6. Console commands — not applicable

`make console-test`, `make console-lint` and `pnpm --dir apps/app exec tsc --noEmit` were
**not run**: this task changed no file under `apps/app` (see **Changes**), so there is
nothing of the console's for it to regress. Recorded as "not run", not as a pass.

### 7. Baseline byte-identity for code this task does not own

```
$ git diff --name-status 8744418..HEAD
A	apps/infrx-api/infrx/judge/__init__.py
A	apps/infrx-api/infrx/judge/cost.py
A	apps/infrx-api/infrx/judge/dryrun.py
A	apps/infrx-api/infrx/judge/rubric.py
A	apps/infrx-api/infrx/judge/sampling.py
A	apps/infrx-api/tests/j/fakes.py
A	apps/infrx-api/tests/j/mutants.py
A	apps/infrx-api/tests/j/test_cost.py
A	apps/infrx-api/tests/j/test_dryrun.py
A	apps/infrx-api/tests/j/test_mutants.py
A	apps/infrx-api/tests/j/test_rubric.py
A	apps/infrx-api/tests/j/test_sampling.py
```

Twelve additions, all under this task's owned paths; not one existing file is modified,
so every F1/F2 path is byte-identical to `8744418`. No F1 code was lifted or copied.

## Results

| Measure | Observed | Expected |
|---|---|---|
| Focused suite `tests/j` | **101 passed**, 0 failed, 0 skipped, exit 0 | all pass |
| Whole Python suite, all mutants (`INFRX_MUTANTS=all make api-test`) | **1058 passed**, 0 failed, 0 skipped, exit 0 | all pass, no regression against `8744418` |
| Canonical `make api-test` | **771 passed**, 0 failed, 0 skipped, exit 0 | all pass |
| Track J mutants | **35/35 killed**, 0 survived, 0 misdeclared, 0 broken_runner | every claimed invariant killable (R32) |
| Exported `judge` conformance cases | **13 ran, 0 skipped**, against the shared fake | 13 ran, 0 skipped |
| Failures | none | — |
| Skips | none. `test_the_provider_sdk_is_importable_so_the_check_means_something` would skip if the `judge` extra were absent; with `make api-env` it ran and passed. | — |

Two mutants **survived the first run** and were fixed before this report; both were
weaknesses in my own cases, not defects in the module (details in **Failure drill**).

## Failure drill

There is no durable state in J1: the module is pure plus one injected read port, and
durable judge state (runs, reservations, consent history) belongs to
`JudgeCoordinator`, which D6 implements. The drills below therefore exercise (a) the
in-process state that *does* accumulate, (b) the refusal boundaries, and (c) the
mutation harness, which is this task's injection mechanism.

### Drill A — 35 injected single-edit defects

Injection point: one textual edit per mutant in `infrx/judge/{sampling,rubric,cost}.py`,
applied to a **copy** of `infrx/` + `tests/` + `pyproject.toml` in a
`tempfile.TemporaryDirectory`. Durable state before: worktree clean. After: worktree
clean (`git status --short` empty; the temp directory is removed by its context
manager). Outcome: 35/35 killed, quoted above. A kill requires pytest exit 1, at least
one failure, and **every** failing test naming one of that mutant's own cases, so a
syntax error or an import failure is `broken_runner` and fails the run exactly as a
survivor would — which the five `test_the_runner_cannot_report_a_false_kill` cases
demonstrate against this runner.

The two first-run survivors, both fixed in `d997be7`:

1. `missing_criterion_counts_as_a_pass` survived because `Rubric.passes` has two
   overlapping guards — "a media-dependent criterion cannot pass without media" and "a
   criterion in the rule with no score fails it" — and each masked the other, so the
   case proved neither. `test_the_pass_rule_belongs_to_the_rubric_version` now asserts
   both separately, and there is a mutant for each.
2. `zero_budget_can_submit` survived because `test_the_defaults_can_never_authorize_a_submission`
   asserted only that *some* `BudgetExceeded` was raised, and with the zero-budget
   guard removed the over-budget guard raised instead. The case now matches the
   message, so each of the three guards is refused on its own account. This is the
   money path, so the weaker assertion mattered: it would also have hidden a future
   edit that made a zero budget fall through whenever the worst case happened to be
   zero.

### Drill B — duplicate delivery, consent revoked after planning, unpriced live run

Command: `uv run --frozen python - <<'PY' …` from `apps/infrx-api` (the script is in the
session scratch area, and every fact it prints is also asserted by a named case above).
Output:

```
DRILL 1 duplicate delivery, deduped by (run, sample, rubric version)
  before           : 0 entries
  first delivery   : True -> entries 1 relevance 5
  replay delivery  : False -> entries 1 relevance 5 (first outcome retained)
DRILL 2 consent revoked between planning and submission
  planned          : 50 samples, live_submission_enabled False
  after revocation : ConsentMissing - consent_missing: org 1a1a1a1a-0000-4000-8000-000000000001 has no current evaluation consent
DRILL 3 the shipped defaults, and an unpriced estimate in live mode
  mode             : dry_run | priced False | live_submission_enabled False
  defaults         : BudgetExceeded - budget_exceeded: judge mode dry_run cannot authorize a live submission
  live+budget      : BudgetExceeded - budget_exceeded: an unpriced estimate cannot authorize a live submission
  priced worst case: 5.67700000 USD for 50 samples at test-rates-v1 (test rates, not an approved price)
```

Reading of drill 1: the "durable" state is the `ScoreLedger`, 0 entries before, 1 after
the first delivery, **1 after a rewritten replay of the same (run, sample, rubric
version)**, and the retained score is the first one (relevance 5, not the replay's 2).
A rewritten late result cannot overwrite a stored score, and a replay writes no second
row. Drill 2: the plan that selected 50 samples refuses entirely once the consent
record is revoked — the revocation is read from the record handed in, and the refusal
precedes any candidate examination. Drill 3: the shipped defaults refuse for mode, and
live mode with a funded budget still refuses because the estimate has no hard maximum
(02). `5.67700000` USD is the worst case under **test** rates, not an approved price.

Cleanup: nothing to clean — no container, no file outside the session scratch area, no
external resource. `git status --short` is empty after every run.

## Artifacts

| Artifact | Location |
|---|---|
| Implementation | `apps/infrx-api/infrx/judge/` at `d997be7` (5 files, 810 lines) |
| Tests | `apps/infrx-api/tests/j/` at `d997be7` (7 files, 1322 lines) |
| Mutant list | `apps/infrx-api/tests/j/mutants.py`; `uv run --frozen python tests/j/mutants.py --list` prints all 35 with their invariants |
| Command transcripts | quoted inline above; raw background-run output is in the session scratch area, which is not durable storage. No separate raw-evidence store exists yet (R15 leaves that to I2). |
| Secrets / customer content | none. No credential, no signed URL, no customer prompt, no storage key appears in this report or in the code; the only identifiers quoted are the conformance builders' fixed test UUIDs. |

## Changes

### Owned paths added

| Path | What it is |
|---|---|
| `infrx/judge/sampling.py` | `TraceCandidate`, the `CandidateSource` port, `CalibrationDesign` (25/15/10), `Stratum`, `Exclusion`, `rank`, `select` |
| `infrx/judge/rubric.py` | `Criterion`, `Rubric` (integer version), `MARLIN_VIDEO_V1`, `validate_output`, `JudgeScores`/`Rejected`, `dedupe_key`, `ScoreLedger` |
| `infrx/judge/cost.py` | `ProviderRate`, `TokenCeilings`, the `RateTable` port, `StaticRateTable`, the empty `APPROVED_RATES`, `estimate_worst_case`, `require_live_submission`, `live_submission_allowed` |
| `infrx/judge/dryrun.py` | `DEFAULT_CEILINGS`, `MAX_CANDIDATES`, `DryRunPlan` (incl. `judge_run()` projection), `plan_dry_run` |
| `infrx/judge/__init__.py` | the public surface; no provider SDK import |
| `tests/j/` | `fakes.py` (relative-import helper, R48), four test modules, `mutants.py`, `test_mutants.py` |

### Contract changes

**None requested.** J1 consumes `records.JudgeRun`, `records.ConsentSnapshot`,
`records.Feedback`, `records.ContentState`, `records.TraceMode`, `contracts.money`,
`contracts.errors` and `contracts.limits` as frozen, and adds nothing to
`infrx/contracts/**`. Two new *J-owned* boundaries are declared inside
`infrx/judge/`, and neither replaces an existing port:

* `sampling.CandidateSource` — J's read side over T's trace projection plus D's
  feedback rows. There is no contracts-v1 port for reading trace candidates
  (`TraceSink` is write-only), so this is a new J interface, faked in tests. If the
  coordinator would rather it lived in `contracts/ports.py`, that is a contract
  revision to rule on, not something this task should have taken.
* `cost.RateTable` — the injected provider price source, deliberately shaped like
  `JobStore`'s `price_for` (r1 R45).

### Migration / deploy / rollback

No migration, no schema, no deployment change, nothing wired into the composition
root. Rollback is reverting three commits: the module is imported by nothing outside
`tests/j/` yet, so removal cannot affect the gateway, a worker or the console.
Forward-looking notes for D6 and T2, from `06` §2–§3 and this implementation:
`judge_samples`/score rows need an integer `rubric_version` and a **`limited`** marker
(a media-less evaluation has no groundedness row at all and can never be a pass), and
the score dedupe key is `(run_id, sample_id, rubric_version)`.

## Limits

1. **Fake-only.** Every result here is against fakes and pure functions. The `judge`
   conformance suite passed against the **shared fake**, which the contracts README
   defines as *implemented*; there is no real `JudgeCoordinator` (D6), no ClickHouse
   candidate source (T2) and no PostgreSQL. J1 must not be marked integrated.
2. **No approved provider price exists.** `research/cross-cutting/cloud-pricing.md` is
   the only permitted price source and it has **no per-MTok row for any judge model** —
   it prices GPU capacity. `APPROVED_RATES` is therefore empty, every estimate is
   unpriced, and live submission is disabled by the guard. Marked
   **⚠️ TO BE VERIFIED**; the owner is the coordinator, who must add an approved price
   row (or name another approved source) before J2 can submit anything. The
   `5.67700000` USD figure in the drill comes from a **test** rate row and is not a
   price claim. `research/traces/06` §3.8's tables ($5/$25 per MTok for Opus 5, ×0.5 for
   batches) are `est.` in a research document, not an approved price row, and were
   deliberately **not** copied into the code.
3. **The default ceilings are `est.`** `DEFAULT_CEILINGS` (7,348 input / 2,048 output /
   1,024 reasoning) come from `06` §3.4/§3.8's estimates for K=8 frames at 1000 px.
   Nothing has measured them. In particular `06` marks the adaptive-thinking token count
   `⚠️` and unmeasured; a reservation computed from an ordinary-looking thinking budget
   is only as safe as that number. J2 must re-derive them from the actual
   `max_tokens`/thinking configuration it sends and from measured usage, and treat any
   increase as a new reservation, not an adjustment.
4. **The rubric is a draft.** `MARLIN_VIDEO_V1` is `06` §3.3's first-draft rubric and
   its pass rule; `06` marks it draft until validated on ~50 human labels, which is J3's
   job. Its scores must not be promoted to a quality claim before then.
5. **The batch discount is not modelled**, on purpose (a worst case must not carry an
   average's discount). J2's reconciliation is where the actual, discounted cost is
   recorded against the reservation.
6. **`plan_dry_run` takes `now` and `since` from the caller.** There is no clock in the
   module, which keeps the unit tests deterministic, but it means whoever schedules the
   run owns the window. When D6 exists, `now` must be the database clock (r1 R7), not a
   host clock — an earlier `now` would let a revoked consent look current.
7. **Stratum sizes are a fixed design, not adaptive.** An undersupplied stratum is
   reported as a shortfall, so a tenant with few failures yields fewer than 50 labels;
   J3 must read `Selection.shortfall` and `counts` rather than assume 50.
8. **The candidate scan is bounded at 200** (`06` §3.4's batch item ceiling). A tenant
   with more eligible traces than that in the window is sampled from the first 200 the
   source returns, so the *source's* ordering, not just the seed, decides which 200 are
   considered. T2 should return them in a stable order; otherwise selection is
   reproducible only for a fixed candidate set, which is what the determinism cases
   assert.
9. **No console, no route, no migration touched**, so console tests and lint were not
   run at all (recorded as "not run"). E owns any cross-module test of this path.

## Handback

### Next unblocked task

**J2 — consent/budget coordinated submission and collection.** Its start dependency is
J1; its *integration* dependencies are D6 (real `JudgeCoordinator`) and T2 (score
projection), neither of which exists, so J2 can be implemented against the shared fake
but not integrated. J2 should: call `cost.require_live_submission` before constructing
any provider client (that is the only sanctioned door); recheck current consent
immediately before egress through `JudgeCoordinator.begin_submit`; reserve the
worst-case amount **including** outstanding runs; disable the SDK's automatic retries
(`06` §3.7 notes `max_retries=2` by default, which is exactly the double-submit hazard);
and treat a timeout with no known provider id as `ambiguous` with the reservation held
(R28/R8).

### Pending coordinator wiring / integration requests

1. **Approved provider price row (blocking for J2's live path, non-blocking for J1).**
   `research/cross-cutting/cloud-pricing.md` has no per-MTok rate for any judge model.
   Either add an approved row there (model, input and output rate per MTok, a price
   version and a source) or name another approved source. Until then
   `judge.APPROVED_RATES` stays empty and `require_live_submission` refuses. **No file
   of mine needs to change** when the row appears: the table is injected.
2. **Where `CandidateSource` should live.** J declared it in `infrx/judge/sampling.py`
   because contracts v1 has no port for reading trace candidates. If the coordinator
   wants it in `infrx/contracts/ports.py` (so T2 implements a frozen shape and J
   consumes it), that is a contract revision; the exact shape to freeze is
   `async candidates(org_id: str, *, since: datetime, limit: int) -> tuple[TraceCandidate, ...]`
   with `TraceCandidate` as defined in `sampling.py`.
3. **No router, no config name, no dependency requested.** J1 mounts nothing on the
   gateway (`app.ROUTERS` unchanged, R44), reads no new environment variable
   (`JUDGE_MODE` and `JUDGE_LIVE_BUDGET_USD` already exist in 08 §5 and `limits.py`), and
   adds no package (`anthropic` is already the pinned `judge` extra and is deliberately
   not imported). Nothing outside my owned paths needs to change for this task to merge.
4. **For D6, when it implements the relation:** score rows need an integer
   `rubric_version` and a `limited` marker, and the score dedupe key is
   `(run_id, sample_id, rubric_version)`. For T2: a media-less evaluation writes **no**
   groundedness row (not a null score with a pass), and the projection — not this
   module — is where duplicate suppression by stable event id lives.

### Unresolved findings

None open against another track. One observation while writing the drill script, for
whoever touches the fakes next and **not** a defect in a shipped path: `Record` is a
frozen pydantic model, so `model_copy(update=…)` bypasses the field validators — a
string passed as `revoked_at` stays a string instead of becoming a UTC datetime. The
coordinator-owned `FakeJudgeCoordinator.revoke_consent` passes a real `datetime`, so it
is correct today; it is only a trap for a future caller who passes text.

## Verification log

- 2026-09-21: J1 implemented on `codex/j1-judge-dryrun` from base `8744418`. 101 focused
  cases, 1058 in the whole Python suite with every mutant enabled, 35/35 track-J mutants
  killed, 13/13 exported `judge` conformance cases run with zero skips against the shared
  fake. Two first-run mutant survivors were weaknesses in this task's own cases and were
  fixed in `d997be7`. No real service, no network egress, no provider call, no push, no
  deployment. Live submission remains disabled: the approved price document carries no
  per-MTok row for any judge model (⚠️ TO BE VERIFIED, coordinator).

---

# J1 round 2 — appended after the independent review of `7876c2c` (fix_required)

Appended, not rewritten (`evidence/README.md`: the log is append-only). Everything above
describes `d997be7`; everything below describes the fix package on top of it.

## Task and status (round 2)

| Field | Value |
|---|---|
| Status | **implemented** — every focused case, the whole `make api-test` suite and all **77** declared mutants pass; still not integrated (no real `JudgeCoordinator`, no ClickHouse candidate source, no router) and still no egress, no provider call, no push. |
| Implementation SHA | `a01ee14` (was `d997be7`) |
| Commits added | `74a11dd` B1–B5 and rulings R56/R57; `a01ee14` the five dishonest kills the new mutant classification exposed |
| Rulings applied | **R56** (judge sampling policy) and **R57** (judge money guard), read from `08-contracts-v1-encoding.md` §10 |

## Per-item disposition

| Item | Disposition | Where | The test that fails without it | Declared mutant(s) |
|---|---|---|---|---|
| **B1** scan bound not enforced | **fixed** | `dryrun.py` `scan_bound()` validates `limit` as an `int` (not `bool`) in 1..200 and `plan_dry_run` **truncates** the returned rows; the bound is refused before the source is read | `test_the_scan_bound_holds_against_a_source_that_ignores_it` (drives the new `fakes.OverReturningSource`, which ignores `limit` — the old well-behaved fake truncated for us, which is exactly why the old case proved nothing), `test_a_scan_bound_that_is_not_one_is_refused` (0, −5, `True`, 2.5, `"10"`, `None`, 201, 10⁹), `test_the_boundary_values_of_the_scan_bound_are_accepted` | `scan_bound_not_enforced`, `scan_bound_range_unchecked`, `scan_bound_accepts_a_boolean` |
| **B2** duplicate rows → duplicate samples | **fixed** | `sampling.deduplicate()` runs before stratifying: an identical repeat is excluded `duplicate_row`, a **conflicting** repeat excludes the whole `request_id` as `conflicting_duplicate` (R56) | `test_a_repeated_row_is_sampled_once`, `test_a_conflicting_duplicate_is_excluded_rather_than_sampled_twice`, `test_deduplication_accounts_for_every_row`, `test_duplicate_rows_from_the_source_never_become_duplicate_samples`; sample-id uniqueness asserted on the plan **and** on the `JudgeRun` | `duplicates_sampled_twice`, `conflicting_duplicate_is_kept`, `identical_duplicates_dropped_silently` |
| **B3** live guard failed open on money | **fixed, per R57** | `cost.require_live_submission(settings, estimate, *, rates, at)` **recomputes** `ceiling(per_sample × samples)` and returns it; it never reads `estimate.worst_case_total` to authorize (only to refuse a stale report), requires `judge_mode == "live"` exactly, a positive budget, a **priced** estimate, a rate effective at the supplied clock whose `price_version` still matches, a finite positive total and `total <= budget`. `ProviderRate` refuses a zero/negative/non-finite rate and a naive `effective_at`; `StaticRateTable` refuses duplicate `(model, effective_at)` rows and returns the **latest effective** row (`max`, not `first`). `worst_case` puts its result through `money.parse`, so a total outside `numeric(20, 8)` is a typed refusal rather than a silently out-of-domain amount, and `decimal` exceptions cannot escape | `test_the_guard_recomputes_the_total_and_never_trusts_the_estimate` (the four forgeries from the review, verbatim), `test_only_the_exact_mode_live_authorizes` (8 spellings), `test_a_budget_that_is_not_positive_never_authorizes`, `test_a_worst_case_over_the_budget_is_refused_and_the_boundary_is_inclusive` (**total == budget** authorizes, one unit of scale more does not), `test_the_rate_effective_at_the_clock_is_used_whatever_the_row_order` (**both orderings**), `test_the_rate_table_refuses_duplicate_rows_for_a_model`, `test_a_rate_row_is_validated_at_the_boundary` (now including `"0"`), `test_a_withdrawn_rate_disables_submission_even_for_a_priced_estimate`, `test_an_unrepresentable_worst_case_refuses_rather_than_raising_arithmetic` | `guard_trusts_the_estimates_total`, `stale_estimate_accepted`, `priced_from_per_sample_only`, `mode_compared_loosely`, `dry_run_can_submit`, `zero_budget_can_submit`, `over_budget_can_submit`, `budget_boundary_exclusive`, `rate_lookup_takes_the_first_row`, `rate_lookup_ignores_the_clock`, `duplicate_rate_rows_accepted`, `a_zero_or_negative_rate_accepted`, `naive_effective_at_accepted`, `worst_case_escapes_the_money_domain` |
| **B4** foreign feedback rows trusted | **fixed** | `TraceCandidate.own_feedback` keeps only rows whose **org and request** match; `calibration_labels` and `has_customer_feedback` read it. The docstring's claim is now the code's behaviour | `test_another_orgs_label_cannot_exclude_this_orgs_trace`, `test_a_row_for_another_request_does_not_move_a_trace_into_the_feedback_stratum` | `feedback_org_not_checked`, `feedback_request_not_checked` |
| **B5a** "every invariant has a mutant" was false for `dryrun.py` | **fixed** | 7 mutants now target `dryrun.py`, including the `JudgeRun` projection and the no-SDK-import checks; `test_every_judge_module_is_covered` fails if any judge module has no mutant | `test_every_judge_module_is_covered`, `test_the_required_invariants_each_have_a_mutant` | `scan_bound_*`, `consent_checked_after_the_read`, `judge_run_is_not_a_dry_run`, `judge_run_reserves_money`, `provider_sdk_imported_on_the_dry_run_path` |
| **B5b** the source was read before the consent refusal | **fixed** | `sampling.check_consent()` is extracted and called by `plan_dry_run` **before** `source.candidates(...)`; `select` keeps its own call for direct callers | `test_a_refusal_never_reads_the_traces` asserts `source.calls == []` for four refusals (no evaluation consent, minimal mode, revoked, wrong org) | `consent_checked_after_the_read` |
| **B5c** docstring cited `tests/j/test_no_egress.py`, which does not exist | **fixed** | `dryrun.py` now cites the import-hygiene cases in `tests/j/test_cost.py` by their real location | — (documentation) | — |
| **R56** consent not retroactive | **applied** | `_within_consent_window`: eligible only at/after `effective_at` and before `revoked_at` | `test_evaluation_consent_is_not_retroactive` (the boundary instant is **in**, one second earlier is out; a trace after a future-dated revocation is out) | `consent_window_ignored`, `consent_window_start_ignored`, `consent_window_end_ignored` |
| **R56** raw facts, strata derived | **applied** | `TraceCandidate` carries `finish_reason`, `http_status`, `schema_valid`; the `failure` flag is **gone**. `failed = finish_reason == "length" or schema_valid is False`; `http_status >= 500` is excluded `skipped_no_output` | `test_the_failure_stratum_is_derived_from_the_raw_facts`, `test_a_request_that_produced_no_output_is_skipped` | `schema_invalid_is_not_a_failure`, `truncation_is_not_a_failure`, `no_output_is_graded_anyway` |
| **R56** `by_operator` ordinary feedback | **applied** | neither calibration (that is `calibration_set`) nor customer signal | `test_an_ordinary_entry_the_platform_made_is_neither_signal_nor_label` **builds such a row** (`author_role=customer`, `by_operator=True`), which is what the surviving-by-omission mutant needed | `calibration_keys_on_by_operator`, `platform_entry_counts_as_customer_signal` |
| **R56** no-media pass in code | **applied** | `Rubric.passes` returns False whenever `not media and self.requires_media`, independently of thresholds; `JudgeScores.__post_init__` refuses `limited and overall_pass and media_required` | `test_no_media_no_pass_does_not_depend_on_a_threshold` (a rubric whose media criterion has `pass_at=None`), `test_the_scores_record_refuses_a_no_media_pass` | `media_criterion_passes_without_media`, `scores_record_allows_a_no_media_pass` |
| **R56** `CandidateSource` docstring | **applied** | the port now states: this tenant only, `full` mode, `since`, at most `limit`, **deduplicated and in a stable order**, raw facts, and only this request's feedback rows; plus the note that the coordinator may move it and `TraceCandidate` into `contracts/ports.py` | — (contract documentation) | — |
| **Nonblocking** `validate_output` truly never raises | **applied** | non-string keys rejected `non_string_key`; `_brief()` renders untrusted values and catches the `ValueError` that `repr(10**5000)` raises; `Rejected.detail` truncated at 200 characters; the `JudgeScores` construction is wrapped, so even the record's own R56 floor answers with a rejection | `test_a_hostile_payload_is_rejected_rather_than_raised`, `test_every_rejection_detail_is_bounded` | `non_string_key_reaches_the_key_arithmetic` (declares `TypeError`), `brief_lets_an_unprintable_value_raise` (declares `ValueError`), `detail_unbounded` |
| **Nonblocking** `parse_judge_json` | **applied** | `object_pairs_hook` refuses duplicate keys (`json.loads` silently keeps the last); `validate_json` wraps parse + validate and never raises | `test_parsing_refuses_a_duplicate_key`, `test_parsing_refuses_malformed_json_without_raising`, `test_a_parsed_result_validates_like_a_built_one` | `duplicate_json_keys_accepted` |
| **Nonblocking** Rubric/Criterion validation | **applied** | `pass_at` within the range, int-only ranges (no bool/float), `overall_pass`/`notes` refused as criterion names, text bounds within the shared 4,000-character ceiling | `test_a_criterion_is_validated` (10 cases), `test_the_text_bounds_are_validated` (6 cases) | `bounded_int_accepts_a_boolean`, `bounded_int_ignores_the_range`, `reserved_criterion_name_allowed` |
| **Nonblocking** `select` validates `rubric_version` | **applied** | `True`, `"1"`, `1.0`, 0, 5000 and `None` are `invalid_request` | `test_the_rubric_version_is_validated` | `rubric_version_unchecked_in_select` |
| **Nonblocking** bounded `ScoreLedger` | **applied** | `ScoreLedger(run_id, rubric_version, sample_ids)`: a foreign run, a foreign rubric version or an unplanned sample is refused and stored nowhere, so the ledger is bounded by the plan | `test_a_ledger_belongs_to_one_run_and_its_planned_samples` (60 unplanned deliveries cannot grow it) | `ledger_accepts_any_run`, `ledger_accepts_any_sample`, `ledger_accepts_any_rubric_version` |
| **Nonblocking** assert before reading `.reason` | **applied** | `fakes.rejection()` asserts `isinstance(..., Rejected)` first and names the *type* rather than repr-ing a hostile value; every `.reason` read in the suite goes through it | the five previously `AttributeError`/`TypeError` kills are now assertions (see **Results**) | — |
| **Nonblocking** runner distinguishes assertions from crashes | **applied** | the runner runs with `--tb=line` and classifies each death; an exception that is not `AssertionError`/`Failed` is `broken_runner` unless the mutant declares it in `dies_by` | `test_the_runner_cannot_report_a_false_kill[an_undeclared_exception_death_is_not_a_kill]`, `test_the_same_defect_is_a_kill_once_its_exception_is_declared` (asserts it is the **same edit**, declared), `test_the_death_classifier_reads_each_shape` | — (the runner's own self-tests) |

### Verified-good areas left alone

The review listed consent instants at every boundary, mode/content exclusions, candidate
tenancy, determinism, stratification bounds, pass-rule thresholds, malformed-score
rejection, dedupe of late duplicates, `Decimal`/ceiling arithmetic, import hygiene and
counts as good under attack. Their cases and mutants are unchanged except where a signature
had to move (`require_live_submission` gained `rates`/`at`; `ScoreLedger` gained its run
binding; `TraceCandidate.failure` became the three raw facts) — and in each of those the
assertions were carried over, not weakened.

## Results (round 2, quoted from command output)

### 1. Focused suite — `uv run --frozen pytest -q tests/j` (exit 0)

```
........................................................................ [ 77%]
..........................................                               [100%]
186 passed in 22.15s
```

Per file (was 101 cases at `d997be7`, now **186**):

```
test_sampling    37 passed in 0.27s
test_rubric      55 passed in 0.29s
test_cost        53 passed in 1.48s
test_dryrun      19 passed in 0.28s
test_mutants     22 passed in 18.94s
```

### 2. The mutant run — `uv run --frozen python tests/j/mutants.py` (exit 0)

Last line, and the `--list` summary:

```
77/77 killed
mutants exit=0
```

```
77 mutants over 67 named cases
```

The five that the **new classification** caught on its first run (and which the old runner
had reported as kills) — full first-run tail, before the round-2 fixes:

```
72/77 killed; broken_runner: ['judge_scores_count_as_customer_feedback', 'score_range_unchecked', 'overall_pass_claim_trusted', 'zero_budget_can_submit', 'a_shipped_rate_appears_from_nowhere']
```

and after fixing them, the same five re-run alone:

```
[killed       ] judge_scores_count_as_customer_feedback: 1 failed, 163 deselected in 0.73s
[killed       ] score_range_unchecked: 5 failed, 159 deselected in 0.66s
[killed       ] overall_pass_claim_trusted: 3 failed, 161 deselected in 0.67s
[killed       ] zero_budget_can_submit: 4 failed, 160 deselected in 0.82s
[killed       ] a_shipped_rate_appears_from_nowhere: 2 failed, 162 deselected in 0.70s

5/5 killed
```

Two of those five were **real defects in the code**, not bad mutants, and both are on the
paths the review was worried about:

1. `validate_output` could still raise: `JudgeScores`' own R56 no-media floor threw
   `ValueError` out of a function documented never to raise. It is caught and answered as
   `inconsistent_result`, so a twice-checked invariant cannot become a 500 on the collect
   path.
2. `fakes.rejection` rendered the accepted value into its assertion message, and `repr` of
   a result carrying a 5,000-digit score raises — the helper written to make kills
   assertion-shaped was itself a crash. It names the type instead.

The other three were bad mutants: an unbalanced parenthesis (a broken copy, reported
`broken_runner` rather than a kill, which is the runner working), a mutant that left
`budget` as `None` so the later comparison died with a `TypeError`, and one that referenced
`timezone`, which `cost.py` does not import.

### 3. The runner's self-tests (mutant honesty)

```
tests/j/test_mutants.py::test_the_runner_cannot_report_a_false_kill[a_no_op_edit_survives] PASSED
tests/j/test_mutants.py::test_the_runner_cannot_report_a_false_kill[a_syntax_error_is_not_a_kill] PASSED
tests/j/test_mutants.py::test_the_runner_cannot_report_a_false_kill[a_lethal_edit_under_the_wrong_case_name_is_not_a_kill] PASSED
tests/j/test_mutants.py::test_the_runner_cannot_report_a_false_kill[an_undeclared_exception_death_is_not_a_kill] PASSED
tests/j/test_mutants.py::test_the_runner_cannot_report_a_false_kill[a_missing_anchor_is_a_failure] PASSED
tests/j/test_mutants.py::test_the_runner_cannot_report_a_false_kill[a_mutant_with_no_case_is_a_failure] PASSED
```

(taken from `uv run --frozen pytest tests/j/test_mutants.py -v --no-header -k false_kill`.)

### 4. Both orderings of the rate history

```
tests/j/test_cost.py::test_the_rate_effective_at_the_clock_is_used_whatever_the_row_order[rows0] PASSED [ 50%]
tests/j/test_cost.py::test_the_rate_effective_at_the_clock_is_used_whatever_the_row_order[rows1] PASSED [100%]
============================== 2 passed in 0.16s ===============================
```

`rows0` is `(TEST_RATE, NEWER_RATE)` and `rows1` the reverse; both answer `test-rates-v2`
at the clock, and the mutant `rate_lookup_takes_the_first_row` dies on `rows1`.

### 5. Whole Python suite

`make api-test` (exit 0) and `INFRX_MUTANTS=all make api-test` (exit 0):

```
856 passed, 2 warnings in 39.43s
plain api-test exit=0
```

```
1179 passed, 2 warnings in 348.97s (0:05:48)
api-test-all exit=0
```

Was 771 / 1058 at `d997be7`; the growth is this task's 85 new focused cases plus the larger
default mutant subset. The two warnings are the same pre-existing `fastapi`/`starlette`
`testclient` deprecations. **No failures, no skips.** A second `make api-test` run gave
`856 passed, 2 warnings in 40.02s`, so the count is stable rather than order-dependent.

### 6. Conformance, unchanged

`test_the_exported_judge_conformance_suite_passes_against_the_shared_fake` still asserts
`ran == len(judge_cases()) == 13` with an empty skip list, against
`infrx.contracts.fakes.FACTORIES["judge"]` — the **shared fake**. J1 still ships no
`JudgeCoordinator` adapter, so this remains **implemented**, never integrated.

### 7. Mutant coverage by module

```
mutants per module: {'cost.py': 18, 'dryrun.py': 7, 'rubric.py': 22, 'sampling.py': 30}
declared dies_by: {'non_string_key_reaches_the_key_arithmetic': ('TypeError',), 'brief_lets_an_unprintable_value_raise': ('ValueError',)}
```

`dryrun.py` had **0** at `d997be7`, which is what made "every invariant has a mutant"
false; `test_every_judge_module_is_covered` now fails if any judge module has none. The two
`dies_by` declarations are the only exceptions to assertion-shaped kills, both on the
"never raises" invariant, and each has a self-test proving the undeclared form of the same
edit is a runner error.

## Failure drill (round 2)

Command: `uv run --frozen python - <<'PY' …` from `apps/infrx-api` (the script lives in the
session scratch area; every fact it prints is also asserted by a named case). The five
drills are the four attacks the review named plus duplicate delivery:

```
DRILL 1 (B1) a source that ignores the scan bound
  source returned : 5000 rows; asked for 200
  rows processed  : 200 | samples 25
  limit=      0: InvalidRequest (source read 0 times)
  limit=     -5: InvalidRequest (source read 0 times)
  limit=   True: InvalidRequest (source read 0 times)
  limit=    2.5: InvalidRequest (source read 0 times)
  limit=1000000000: InvalidRequest (source read 0 times)
DRILL 2 (B2) duplicate and conflicting candidate rows
  identical twice : ('00000001-0000-4000-8000-000000000001',) | ['duplicate_row']
  conflicting     : () | ['conflicting_duplicate', 'conflicting_duplicate']
DRILL 3 (B3/R57) forged estimates against the recomputing guard
  total=None      : refused - budget_exceeded: an unpriced estimate cannot authorize a live submission
  total=-5        : refused - budget_exceeded: an unpriced estimate cannot authorize a live submission
  1e6 x 999 = 0.01: refused - budget_exceeded: the estimate says 0.01 but the worst case is 113540.00000000: re-estima
  rows old,new: effective price_version test-rates-v2
  rows new,old: effective price_version test-rates-v2
DRILL 4 (B4) feedback rows belonging to another org or request
  org-B label     : selected
  other request   : ['uniform']
DRILL 5 duplicate delivery into a run-bound ledger
  before          : 0 entries
  first delivery  : True -> entries 1 relevance 5
  replay          : False -> entries 1 relevance 5 (first outcome retained)
  unplanned sample: False -> unexpected_sample | entries 1
```

Readings. **Drill 1**: a source returning 5,000 rows for a 200-row request has 200 of them
processed, and every invalid bound is refused with the source read **zero** times.
**Drill 2**: an identical repeat yields one sample plus a `duplicate_row` exclusion; a
conflicting pair yields **no** sample and two `conflicting_duplicate` exclusions, so one
trace can never land in two strata. **Drill 3**: all three forgeries refuse — the first two
because a half-filled report is not a priced estimate, the third because the recomputed
worst case (113,540.00000000) is not the 0.01 the report claimed; and the effective price
version is `test-rates-v2` for **both** row orderings. **Drill 4**: an org-B label leaves
the org-A trace *selected* (it was previously excluded as already calibrated), and a row for
another request leaves the trace in the `uniform` stratum rather than promoting it to
`feedback`. **Drill 5**: the ledger holds one entry across a rewritten replay, keeps the
first outcome, and refuses a sample the run never planned.

Durable state: none — J1 has no durable state, and the mutation harness writes only inside
a `tempfile.TemporaryDirectory`. `git status --short` is empty after every run; no
container, no cloud resource, no credential, no network call.

## Changes (round 2)

Owned paths only; still **no** file outside them is modified, so every F1/F2 path stays
byte-identical to `8744418`:

```
A	apps/infrx-api/infrx/judge/{__init__,cost,dryrun,rubric,sampling}.py
A	apps/infrx-api/tests/j/{fakes,mutants,test_cost,test_dryrun,test_mutants,test_rubric,test_sampling}.py
A	research/plan/evidence/j/J1-d997be7.md
```

(`git diff --name-status 8744418..HEAD` shows 13 `A` lines and no `M` line.)

Contract changes: still **none requested**. Two signature changes inside J's own module,
worth naming because J2 consumes them:
`require_live_submission(settings, estimate, *, rates, at) -> Decimal` (it now returns the
amount a submission may reserve) and `ScoreLedger(run_id, rubric_version, sample_ids)`.
`TraceCandidate` lost its `failure` flag and gained `finish_reason`, `http_status` and
`schema_valid` (R56), and `ProviderRate` gained a required `effective_at` (R57) — all three
are the shapes the coordinator would freeze if `CandidateSource`/`RateTable` move into
`contracts/ports.py`.

## Limits (round 2)

The nine limits recorded at `d997be7` stand, with these amendments:

1. **Limit 2 is unchanged and still the blocking one.** `research/cross-cutting/cloud-pricing.md`
   has no per-MTok row for any judge model, so `APPROVED_RATES` is empty and every estimate
   is unpriced. The `113540.00000000` and `5.67700000` figures in the drills come from
   **test** rates, not an approved price. ⚠️ TO BE VERIFIED, owner: coordinator.
2. **Limit 8 (the scan bound) is narrower now but not gone.** The bound is enforced by
   truncation, so a source returning more rows cannot expand the work; but *which* 200 rows
   are considered is still the source's ordering. The `CandidateSource` docstring now
   requires a stable, deduplicated order, and that requirement is **not** testable from
   here — a real adapter must prove it. Owner: T2.
3. **New: `plan_dry_run` takes `now` from the caller and uses it for the rate lookup too.**
   With D6 this must be the database clock (r1 R7); an earlier `now` would both make a
   revoked consent look current and select an older, cheaper rate row.
4. **New: the guard's staleness check compares amounts, not intent.** It refuses an estimate
   whose total disagrees with the recomputation, which is right, but it cannot tell a stale
   report from a forged one. That distinction belongs to J2's persisted submit intent.
5. **New: `JudgeScores.media_required` is carried on the record** so R56's floor is
   checkable without the rubric. If D6/T2 persist scores, that column has to exist or the
   floor is only enforced in process.
6. **Unchanged: nothing here is integrated.** The conformance pass is against the shared
   fake; no real adapter, no ClickHouse, no PostgreSQL, no provider call.

## Handback (round 2)

**Next unblocked task:** still **J2**, unchanged, and now with a narrower door: it must call
`cost.require_live_submission(settings, estimate, rates=…, at=<database clock>)` and reserve
**the Decimal it returns**, never a number from a report.

**Integration requests** — the four from `d997be7` stand, amended:

1. **Approved provider price row** (blocks J2's live path). Now needs one more field than
   before: R57 requires an `effective_at` per row, so an approved row is
   `(model, input per MTok, output per MTok, price_version, source, effective_at)`. Nothing
   in my files changes when it arrives; the table is injected.
2. **Where `CandidateSource`/`TraceCandidate` should live.** Unchanged request, updated
   shape after R56: `async candidates(org_id, *, since, limit) -> tuple[TraceCandidate, ...]`
   with `TraceCandidate` carrying raw facts (`finish_reason`, `http_status`, `schema_valid`,
   `media_available`, `content_state`, `trace_mode`, `started_at`, `model_revision`) and
   only its own request's feedback rows. The adapter must return a stable, deduplicated
   order.
3. **No router, no config name, no dependency requested.** Unchanged.
4. **For D6/T2.** Unchanged, plus: persist `limited` **and** `media_required` on a score row
   (R56's floor needs both), and `judge_samples` should carry the stratum so J3 can report
   agreement per stratum rather than assuming 50 uniform labels.

**Unresolved findings:** none open against another track. The `model_copy` observation from
round 1 stands (a frozen pydantic `Record` does not re-validate on `model_copy`; the
coordinator-owned fake passes a real `datetime`, so it is correct today).

## Verification log

- 2026-09-21 (round 2): fix package for the independent review of `7876c2c`. B1–B5 closed,
  rulings R56 and R57 implemented, and the non-blocking hardening applied. 186 focused cases
  (was 101), 77 mutants (was 35) covering all four judge modules with 77/77 killed, and the
  runner now distinguishes an assertion failure from an exception raised in a test body —
  which immediately exposed five dishonest kills, two of them real code defects
  (`validate_output` could raise through the record's own R56 floor; the rejection helper
  repr-ed a hostile value). No real service, no network egress, no provider call, no push,
  no deployment. Live submission remains disabled: the approved price document still carries
  no per-MTok row for any judge model.
