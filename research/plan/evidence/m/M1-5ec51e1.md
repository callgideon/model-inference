# M1 — bound and secure URL/base64 materialization

## Task and status

| | |
|---|---|
| Task | M1 (track M, media), oracles MEDIA-SEC and F-CONTRACT |
| Owner / session | Claude Opus 5 implementation session, 2026-09-21 |
| Status | **implemented** (fakes and injected collaborators only; not integrated — no route, no real object store, no live fetch) |

## Source

| | |
|---|---|
| Base SHA | `8744418` |
| Implementation SHA | `5ec51e1` — the tree every result below was measured on. Renaming this file to match it is the only later commit, and it carries no code. |
| Commits | `c63af5a` fetch path + adversarial suite, `4fb8f0c` MediaStore adapter + shared suite, `26c41b8` mutation list + runner, `5ec51e1` the two staging failure drills, the `index_before_the_payload` mutant and this report |
| Branch / worktree | `codex/m1-media-materialization` in `.claude/worktrees/codex-m1` |
| Integration target | `claude/infrx-impl` (coordinator only; nothing pushed) |

## Requirement coverage

Test ids are the pytest function names in `apps/infrx-api/tests/m/`; the mutant that
must die for each claim is named in `tests/m/mutants.py`.

| Oracle | Test id | Exact invariant |
|---|---|---|
| MEDIA-SEC | `test_the_connection_is_pinned_to_the_validated_address` | the request URL carries the validated IP, the `Host` header and `sni_hostname` carry the original host, and the raw path/query are unchanged (mutants `connects_to_the_name_not_the_address`, `no_sni_for_the_original_host`, `host_header_from_the_pinned_url`) |
| MEDIA-SEC | `test_an_ipv6_answer_is_pinned_as_a_bracketed_literal` | an IPv6 answer is pinned as `https://[addr]/…`, not string-joined |
| MEDIA-SEC | `test_a_rebinding_resolver_cannot_reach_an_internal_address` | resolver answers public then internal: the second answer is validated before use, and no connection is made to it (`one_answer_is_enough`, `later_hops_unresolved`) |
| MEDIA-SEC | `test_one_private_answer_among_many_is_refused` | one private answer among many refuses the whole name; no request is sent |
| MEDIA-SEC | `test_no_internal_address_form_is_reachable` (18 forms) | loopback, private, link-local/169.254.169.254, CGNAT, unspecified, v4-mapped metadata, NAT64 `64:ff9b::/96`, 6to4 `2002::/16`, Teredo, a scoped literal and non-address text are all `blocked-address` (`reserved_addresses_allowed`, `cgnat_allowed`, `unparseable_address_allowed`) |
| MEDIA-SEC | `test_a_dns_failure_or_an_empty_answer_is_a_refusal_not_a_crash` | a resolver error or empty answer is a typed refusal (`empty_answer_accepted`, `resolution_failure_swallowed`) |
| MEDIA-SEC | `test_every_redirect_hop_is_validated_and_repinned` | hop 2 is parsed, resolved, validated and pinned again (`later_hops_unresolved`, `later_hops_unvalidated`) |
| MEDIA-SEC | `test_a_redirect_chain_over_the_budget_is_refused` | at most `MEDIA_FETCH_MAX_REDIRECTS` (3) hops plus the first request (`redirect_budget_ignored`) |
| MEDIA-SEC | `test_a_redirect_without_a_location_is_refused` / `test_a_redirect_to_another_scheme_or_to_credentials_is_refused` | a 3xx with no destination is refused, not read as a body; `file:`/`gopher:`/credentialed redirect targets are refused before connecting |
| MEDIA-SEC | `test_credentials_in_the_url_are_refused`, `test_only_http_urls_with_a_host_are_fetched` | userinfo and non-http(s)/hostless URLs never become a fetch (`credentials_in_the_url_accepted`, `any_scheme_fetched`, `hostless_url_accepted`) |
| MEDIA-SEC | `test_an_oversize_body_is_aborted_mid_stream` | the cap is enforced on the stream and the body is abandoned mid-read, so a `Content-Length: 10` in front of 320 bytes gains nothing (`streaming_cap_removed`) |
| MEDIA-SEC | `test_a_declared_oversize_body_is_refused_before_it_is_read` | a declared oversize length is refused with zero bytes read (`declared_length_ignored`) |
| MEDIA-SEC | `test_a_compressed_body_is_refused_outright` | any `Content-Encoding` other than identity is refused before a byte is read (the gzip bomb) and the request asks for `identity` (`content_coding_accepted`) |
| MEDIA-SEC | `test_a_slow_body_stops_at_the_aggregate_deadline` | the aggregate `MEDIA_FETCH_TIMEOUT_S` is checked between chunks on the injected clock, so a slow-loris body is dropped part-read (`aggregate_deadline_not_checked_between_chunks`) |
| MEDIA-SEC | `test_a_non_video_response_is_refused`, `test_an_undeclared_type_falls_back_to_the_extension_only`, `test_a_non_200_response_is_refused_without_its_body`, `test_an_empty_body_is_not_a_video` | content-type allow-list with an extension fallback only for undeclared types; only 200 carries media; an empty body is not media (`any_type_is_a_video`, `any_status_is_content`, `an_empty_body_is_a_video`) |
| MEDIA-SEC | `test_the_client_follows_nothing_and_trusts_no_environment` | `follow_redirects=False`, `trust_env=False`, separate connect/total budgets (`httpx_follows_redirects_itself`, `proxy_environment_trusted`, `the_connect_budget_is_the_whole_budget`) |
| MEDIA-SEC | `test_only_one_fetch_happens_per_source` | once-only: one request and one resolution per source, and a refusal is not retried |
| MEDIA-SEC | `test_a_refusal_tells_the_caller_and_the_log_nothing_about_the_url`, `test_a_transport_timeout_is_a_timeout_refusal` | the caller gets the fixed message of `media_fetch_failed`; the log gets `host=` and a reason class from a closed vocabulary — no query string, credentials, path or upstream exception text, and no exception chaining (`upstream_text_echoed`, `a_timeout_is_an_unknown_failure`) |
| MEDIA-SEC | `test_a_data_url_is_decoded_strictly_and_within_a_bound`, `test_an_oversize_data_url_is_refused_before_it_is_decoded`, `test_a_data_url_must_be_base64_and_an_allowed_video_type` | `validate=True`; the size bound is applied to the encoded text *before* decoding (proved by an oversize *and* invalid payload reporting `too-large`); only base64 and only allow-listed types (`base64_not_validated`, `data_url_bound_after_decode`, `any_data_url_type_accepted`, `a_plain_data_url_is_decoded_as_base64`) |
| MEDIA-SEC | `test_a_fetched_source_becomes_a_tenant_scoped_content_addressed_object`, `test_two_organizations_never_share_an_object`, `test_the_same_source_twice_is_one_object` | the key is `media/<org>/<profile>/<digest16>/source`: built from tenant + digest + profile version, never from the URL; two orgs never share an object; identical bytes are written once (`key_without_the_tenant`, `key_without_the_profile_version`, `resolve_ignores_the_tenant`) |
| MEDIA-SEC | `test_an_object_is_never_replaced_by_different_content`, `test_a_second_payload_for_one_request_id_cannot_replace_the_first` | write-once: different content under an existing key is `409`, never an overwrite (`an_object_can_be_replaced`) |
| MEDIA-SEC | `test_an_unsupported_source_is_refused_before_anything_is_stored`, `test_an_oversize_source_never_reaches_the_object_store` | only http(s)/`data:` sources are materialized, without reaching the fetcher; the store bounds what a fetcher hands back, whatever the fetcher's own limit was (`any_source_materialized`, `materialized_bytes_unbounded`) |
| MEDIA-SEC / F-CONTRACT | `test_staging_makes_the_canonical_payload_durable_with_a_digest_and_a_size` | the canonical `NormalizedRequest` (so every source's digest, size, type and duration) is durable at a derived key with a measured digest and size; `request.payload_ref` is not read, so `../../etc/passwd` is not a path (`payload_key_from_the_request`, `payload_not_stored`, `payload_size_from_the_reference`) |
| MEDIA-SEC | `test_a_foreign_or_oversize_reference_is_not_staged`, `test_a_request_may_only_be_staged_for_its_own_org`, `test_one_handle_cannot_carry_two_different_objects_in_one_request`, `test_a_staged_handle_keeps_the_content_it_has`, `test_an_upload_reference_is_resolved_not_trusted` | the trust boundary at `stage`: another tenant's ref is `404`, over `MAX_MEDIA_BYTES` is `413`, a duplicated handle with two digests is `400`, an existing handle keeps its content, an upload handle is resolved rather than believed |
| MEDIA-SEC | `test_a_refused_request_stages_nothing_at_all` | all or nothing: after any refusal no ref, no payload and no object exists, and the corrected retry stages cleanly (`stage_indexes_as_it_goes`) |
| MEDIA-SEC | `test_attach_takes_the_tenant_from_the_job_row` | R52/R55: the org comes from the job row, every ref is checked against it, an unknown job is `404` with refs or without, and a refused attach stores nothing (`attach_trusts_the_refs_tenant`, `attach_without_a_job_row`) |
| F-CONTRACT | `test_the_adapter_satisfies_the_media_store_protocol`, `test_the_exported_conformance_suite_runs_the_cases_m1_owns`, `test_a_deferred_operation_is_a_skip_and_never_a_silent_pass`, `test_the_operations_another_task_owns_are_not_answered` | the adapter is a `ports.MediaStore` with every operation async; the exported `run_mediastore_conformance` cases M1 owns pass against it; the rest raise `MissingHook` and are reported as skips, and the adapter itself raises rather than answering an M2/M3 operation |

## Environment

Local development host, no cloud, no GPU, no container. Ubuntu 24.04.4 LTS,
Linux 7.0.0-1010-aws x86_64. Python 3.12.3 from the pinned environment
(`make api-env` → `uv sync --frozen --all-extras`, uv 0.11.8), httpx 0.28.1,
pydantic 2.13.5, pytest 8.4.2, no pytest plugins. No network, no sockets, no sleeps and
no wall-clock dependence in any case: the resolver, the transport and the monotonic clock
are injected. Classification: **local, layer 1**.

## Commands

All from the worktree root; UTC times as recorded in the run log.

| Command | Exit | Result |
|---|---|---|
| `make api-test` (06:23:16Z) | 0 | `752 passed, 2 warnings in 36.56s` |
| `uv run --frozen pytest -q tests/m` (06:23:53Z) | 0 | `82 passed in 8.70s` |
| `uv run --frozen pytest -q tests/m/test_fetch.py` (06:24:21Z) | 0 | `51 passed in 2.13s` |
| `uv run --frozen pytest -q tests/m/test_store.py` (06:24:22Z) | 0 | `23 passed in 0.88s` |
| `uv run --frozen pytest -q tests/m/test_store.py -k conformance -s` (06:24:23Z) | 0 | `1 passed, 22 deselected in 1.29s` plus the skip report quoted below |
| `INFRX_MUTANTS=all uv run --frozen pytest -q tests/m/test_mutants.py` (06:24:26Z) | 0 | `54 passed in 84.81s (0:01:24)` |
| `INFRX_MUTANTS=all uv run --frozen pytest -q --collect-only tests/m/test_mutants.py \| grep -c test_mutant_is_killed` | 0 | `51` |
| `uv run --frozen pytest -q tests/test_media.py tests/test_app_factory.py tests/test_gateway_auth.py tests/test_inflight.py` (06:25:53Z) | 0 | `7 failed, 22 passed, 2 warnings in 1.63s` — see below |
| same four legacy files from a `git archive 8744418` copy (06:25:55Z) | 0 | `7 failed, 22 passed, 2 warnings in 2.25s` — identical at the base SHA |

`make console-test` / `console-lint` / `tsc`: **not applicable** (no console file touched).
`make bench-test`: not run (E1 owns `models/marlin2b/tests`; it does not exist).
No environment variable was set other than `INFRX_MUTANTS`. No seed (nothing random).

### Conformance against the real adapter

```
mediastore conformance against infrx.media.store.MediaStaging:
  ran 2 of 9: ['media_sec__a_foreign_media_reference_is_not_staged', 'media_sec__a_partial_request_stages_nothing']
  skipped media_parity__staging_is_content_addressed_and_tenant_namespaced: needs prepare (M2 owns it)
  skipped media_sec__a_refused_upload_stays_refused: needs create_upload (M3 owns it)
  skipped media_sec__an_expired_upload_window_says_so: needs create_upload (M3 owns it)
  skipped media_sec__an_upload_is_owned_verified_and_immutable: needs create_upload (M3 owns it)
  skipped media_sec__another_org_cannot_resolve_or_finalize: needs create_upload (M3 owns it)
  skipped media_sec__oversize_and_unsupported_uploads_are_refused: needs create_upload (M3 owns it)
  skipped media_sec__staging_never_replaces_an_existing_object: needs create_upload (M3 owns it)
```

Two of the nine exported cases ran and passed; seven are **skips, not passes** (R32),
each naming the operation another M task owns. `test_a_deferred_operation_is_a_skip_and_never_a_silent_pass`
asserts the mechanism itself: `run_mediastore_conformance` without a `skipped` list
*raises* rather than counting a skip, and the adapter raises `NotImplementedError` for
those operations outside the harness. The staging-key invariants gated behind M2's
`prepare` (tenant and profile version in the key, one tenant per object) are covered by
M1's own cases and mutants instead, listed above.

### Results

| Suite | Passed | Failed | Skipped |
|---|---|---|---|
| `tests/m` (fetch, store, mutant subset) | 82 | 0 | 0 |
| whole API suite (`make api-test`) | 752 | 0 | 0 |
| exported mediastore conformance against `MediaStaging` | 2 | 0 | 7 (named above) |
| mutation list, `INFRX_MUTANTS=all` | 54 (51 mutants + 3 meta tests) | 0 | 0 |

Every one of the 51 declared mutants is **killed**: pytest exited 1 and only the tests the
mutant names failed. `test_the_runner_cannot_report_a_false_kill` exercises the six ways a
run can prove nothing (syntax error, no-op edit, a real defect under the wrong test name,
a missing anchor, no test named, a selection that collects nothing) and asserts none of
them is accepted as a kill.

Three candidate mutants were **dropped rather than shipped as survivors**, and the reason
is a property of Python 3.12 rather than of the tests: removing the v4-mapped unwrap or
the private/loopback/link-local clause from the reused F1 address policy changes no
answer, because `is_global` already refuses that space, and `aiter_raw` versus
`aiter_bytes` is unobservable while content coding is refused outright. The decisive
guards on those same paths (`is_reserved`, `is_global`, the parse failure, the coding
refusal) each have a killed mutant, so no claimed invariant is left unkillable.

### Pre-existing legacy-subset failure (not a regression)

Running only the four top-level legacy test files fails 7 `tests/test_gateway_auth.py`
cases. The identical 7 failures reproduce from a pristine `git archive 8744418` copy
(commands above), and they pass in the full run (`752 passed`), which is the canonical
command. This is the collection-order hazard R48 describes (the `gateway` shim mutates
process state at import and directories collect before top-level files); nothing in M1
imports the shim, `gateway.py` and `infrx/media/video.py` are byte-identical to the base
(`git diff --stat 8744418` lists neither), and the legacy media tests themselves pass.
Owner: coordinator / G1 at cutover.

## Failure drill

Both drills are committed tests, so they are rerunnable rather than described.

1. **Storage fault at the payload write** (`test_a_fault_at_the_payload_write_stages_nothing_and_the_retry_completes_it`).
   Injection: an object store that raises `DependencyUnavailable` for any `payloads/` key,
   after one source has already been materialized.
   Durable state before: one media object (`media/<org>/v1/<digest16>/source`), one indexed
   ref, no payload. After the refused `stage`: **unchanged** — no new object, no second ref
   indexed (the fresh reference in the same request is not left behind), no payload
   recorded, so acceptance cannot proceed on half a request. Retry once the store recovers:
   one payload object added, both refs returned, no second media copy
   (`len(objects) == before + 1`). Mutant `index_before_the_payload` proves the ordering is
   what makes this true.
2. **Crash between the object write and staging** (`test_a_crash_between_the_object_and_the_index_leaves_a_retryable_orphan`).
   Injection: the process is replaced by a new `MediaStaging` over the same bucket after
   `materialize` and before `stage`.
   Durable state before the restart: exactly one object, keyed by its own digest, with no
   index entry — an orphan M3's collector can identify from the key alone. After the retry:
   the same key and the same handle (content-addressed), byte-identical bucket contents, and
   one staged request. Duplicate/retry behaviour: idempotent, no second copy and no second
   handle.
   Cleanup: no cleanup needed — every drill runs in memory in the test process; no
   container, temporary directory or cloud resource was created by this task at all.

## Artifacts

No raw artifacts beyond the repository: every result above is reproducible with the
commands as given. The run log kept while writing this report lives in the session scratch
area (git-ignored, per R15) and contains only the outputs quoted here. No credentials, no
customer content, no signed URLs; the test fixtures use documentation addresses
(93.184.216.34, 2606:4700:4700::1111) and a 32-byte synthetic MP4 header.

## Changes

Owned paths only, 1852 insertions, no deletions, no file outside them:

| Path | What |
|---|---|
| `apps/infrx-api/infrx/media/fetch.py` (new) | the pinned, bounded fetch path; `parse_source`, `video_mime`, `decode_data_url`, `MediaFetcher`, the closed `REASONS` vocabulary, `digest_of` |
| `apps/infrx-api/infrx/media/store.py` (new) | `ObjectStore` protocol, `InMemoryObjectStore`, `MediaStaging` (`materialize`, `stage`, `attach`, `resolve_owned`, `staged_payload`) |
| `apps/infrx-api/tests/m/support.py`, `test_fetch.py`, `test_store.py`, `mutants.py`, `test_mutants.py` (new) | the adversarial fixtures, the two suites, the mutation list and its runner |
| `research/plan/evidence/m/M1-26c41b8.md` | this report |

`infrx/media/video.py` is untouched and byte-identical: the new path *imports* F1's
`address_allowed` rather than copying or editing it, so one address policy serves both
paths and every name the `gateway.py` shim forwards still resolves. No contract file, no
composition root, no manifest, no lock file, no migration, no coordinator record was
touched. No contract revision is requested.

Migration / deploy / rollback: none. Nothing is wired into `create_app`, no route, no
schema, no configuration name added (`MEDIA_FETCH_TIMEOUT_S`, `MEDIA_FETCH_CONNECT_TIMEOUT_S`,
`MEDIA_FETCH_MAX_REDIRECTS`, `MAX_MEDIA_BYTES` already exist in `contracts.limits` and are
read from the injected `PilotSettings`, never from the environment in this module).
Rolling back is deleting the new files; the legacy path is unaffected either way.

## Limits

No inferred pass. What is *not* demonstrated:

1. **TLS verification against the pinned address is not exercised end to end.** The pin is
   proved at the request level (URL host = validated IP, `Host` and `sni_hostname` = the
   original host, and httpcore uses `sni_hostname` as `server_hostname` for the handshake).
   No test opens a real TLS connection, so certificate validation against a pinned IP is
   **pending** a layer-2/3 case with a real server and a real certificate. Owner: E or M at
   G1/G2.
2. **No real object store.** Only `InMemoryObjectStore` exists; the S3-compatible adapter
   (`head`/`put` map to HeadObject/PutObject, task-local port 59100 per 08 §8) is an
   integration request below and its conformance run is **pending**.
3. **Seven of nine exported conformance cases did not run** (skips listed above). M1's
   adapter is not conformance-proven for uploads (M3) or preparation (M2).
4. **No real DNS.** `resolve_all` (the default `getaddrinfo` resolver) is never executed by
   a test; every case injects a resolver. A scoped literal such as `169.254.169.254%eth0`
   is refused, but real dual-stack answer ordering is unverified. Owner: M at G1.
5. **Nothing is integrated.** No gateway route calls `materialize`/`stage`; the legacy
   `prepare_video` path is still what the running app uses. This report claims
   *implemented*, never integrated.
6. **`http://` sources are still accepted** (F1 behaviour, validated and pinned per hop).
   Refusing plaintext outright is a policy decision for G1, not a silent change here.
7. **The whole body is held in memory** (bounded by `MAX_MEDIA_BYTES`, 64 MiB), as the F1
   path does. M2 needs a file for ffprobe/ffmpeg; if a streaming sink is wanted instead of
   `bytes`, that is M2's call and a one-parameter change.
8. **The mutation runner copies the package per mutant** (51 subprocesses, ~45–85 s
   wall-clock depending on host load), which is why the default suite runs a five-mutant
   subset.

## Handback

**Next unblocked task:** M2 (versioned preprocessing and tenant cache) — its start
dependency M1 is implemented. M2 owns `prepare`, and it should replace the
`NotImplementedError` in `MediaStaging.prepare`, reuse `MediaStaging._key` for the
prepared key, and take the `Fetched.data` bytes to a temporary file for probing rather
than adding a second fetch path. M3 (uploads) is likewise unblocked and owns
`create_upload`/`finalize_upload`; the six conformance cases skipped above become its
acceptance evidence, and the three optional-hook names the harness expects
(`put_object`, `admitted`) are already wired in `tests/m/test_store.py`'s factory.

**Integration requests (coordinator-owned paths; I changed none of them):**

1. **Root `Makefile`** — add M's mutation list to the full mutant target so a surviving
   guard fails the same gate as the contracts list:
   `api-mutants: cd apps/infrx-api && INFRX_MUTANTS=all uv run --frozen pytest -q tests/contracts/test_mutants.py tests/m/test_mutants.py`.
2. **`apps/infrx-api/pyproject.toml`** — when a real object store is wanted, a `media`
   extra (or reuse of the existing `traces` extra, which already pins `boto3>=1.35,<2`).
   My preference is to reuse `traces`' boto3 pin rather than add a second one; no manifest
   edit is needed until that adapter is written. Nothing in M1 imports boto3.
3. **`infrx/gateway/app.py` (composition root, G1's cutover)** — the pilot request path
   should construct `infrx.media.store.MediaStaging(objects, limits=settings.pilot,
   fetcher=infrx.media.fetch.MediaFetcher(settings.pilot))` once per app and call
   `await staging.materialize(org_id, source)` per media part at intake, then
   `await staging.stage(org_id, normalized_request)` before admission, mapping the typed
   `DomainError`s straight to the envelope (`media_fetch_failed`, `unsupported_media`,
   `request_too_large`, `invalid_request`). M1 exposes no router, so there is no
   `register(app, rt)` to add yet.
4. **`infrx/config.py`** — no new name required. If G1 wants the S3 media bucket, the
   existing `S3_MEDIA_BUCKET` (08 §5) is already there and unread.

**Unresolved findings:** the pre-existing legacy-subset failure documented above (R48
collection-order hazard, coordinator/G1); and the note that `MediaStore.attach` is named
by no wave-2 task brief — M1 implemented it because `stage` is useless to D/W without it
and R52/R55 make it a tenant-isolation guard, but the coordinator should record who owns
it from here.

## Verification log

- 2026-09-21: Authored from the runs above at implementation SHA `26c41b8`. Every count and
  every quoted line is command output; the conformance skip report and the legacy-subset
  comparison are copied verbatim. No live, paid, cloud or container operation was performed.

---

# Round 2 — independent review at `9e2c0a5`: fix_required

Appended, not rewritten. The file name records round 1's SHA; **round 2's results were
measured on `72010a2`** (the head at the time of writing, with this appendix committed on
top of it). Review disposition per item, then the re-measured results.

## Disposition

| Item | Disposition | Where |
|---|---|---|
| **B1** address policy hole (`fec0::/10` site-local, 192.88.99.0/24, 2001:20::/28, 3fff::/20) | **fixed** in `infrx/media/video.py` (M-owned, refusing direction only): after the existing checks the policy now states `is_site_local` and an explicit `DENIED_NETWORKS` deny-list — 2002::/16, 64:ff9b::/96, 64:ff9b:1::/48, 2001::/32, 2001:20::/28, 3fff::/20, 192.88.99.0/24, 198.18.0.0/15, 192.0.0.0/24 — so the answer no longer depends on the interpreter's tables. The v4-mapped unwrap is kept and is now **killable**: `support.PUBLIC_MAPPED` (`::ffff:93.184.216.34`) has a case of its own, since without the unwrap every mapped form is refused as reserved space. 24 address forms are asserted directly and through a fetch. | `test_the_address_policy_does_not_depend_on_the_interpreters_tables`, `test_a_v4_mapped_public_address_is_judged_as_the_v4_it_names`, `test_no_internal_address_form_is_reachable` (24 params); mutants `site_local_allowed`, `denied_networks_not_checked`, `mapped_address_not_unwrapped`, `cgnat_allowed`, `unparseable_address_allowed` |
| **B1** correction to round 1's sentence about the mapped-unwrap mutant | **corrected**: round 1 claimed the unwrap was unkillable. It was unkillable *against the fixtures that existed then* (every mapped address in them is also internal). With a mapped **public** address in the suite it is killable, and the mutant is shipped. ~~The claim that survives is narrower and is stated in the list itself: the interpreter's `is_reserved` has no mutant, because on this build it also covers NAT64 and the explicit deny-list now covers the rest, so removing it changes no answer.~~ **[Wrong; corrected in round 3, B-R2-1.1: `::127.0.0.1`-style IPv4-compatible forms and reserved space outside `::/96` are refused by `is_reserved` alone, so it is load bearing and now has a mutant of its own.]** | `tests/m/mutants.py` header comment on the address block |
| **B2** signed URL + pinned IP logged by httpx/httpcore | **fixed**: `fetch.py` sets the `httpx` and `httpcore` loggers to `WARNING` at import (`silence_transport_logs`), so no request line is emitted whatever the process's logging config does afterwards. The test captures **every** logger at DEBUG through `caplog`, emits the exact lines httpx (INFO) and httpcore (DEBUG) would write, and drives the success, redirect and refusal paths; it asserts that no path, query, signature, `Location` value or IP appears, and that the only line present is `host=… reason=…`. Process logging config is integration request 5. | `test_no_logger_in_the_process_writes_a_url_an_ip_or_a_location`; mutant `transport_logs_not_silenced` |
| **B3** deadline did not bound DNS or a stalled read | **fixed**, three parts: resolution runs inside the hop's remaining budget (`asyncio.wait_for`, mapped to `timeout`); every request carries what is left of the budget as httpx's per-phase timeouts (`extensions["timeout"]`), so one stalled read cannot double the total; and a real-clock backstop (`asyncio.timeout`, the aggregate limit **plus `BACKSTOP_GRACE_S` = 1 s** so a typed refusal naming its host wins the race) bounds the phases no injected clock can see. | `test_a_resolver_that_never_answers_is_bounded`, `test_each_request_carries_what_is_left_of_the_budget`, `test_a_body_that_stalls_for_ever_is_bounded_by_the_backstop` (bounded by the test's own `wait_for`, so a missing backstop fails instead of hanging); mutants `resolution_not_bounded`, `request_budget_is_the_whole_budget`, `no_real_clock_backstop`, `hop_deadline_not_checked` |
| **B4/R10** `Accept-Encoding` mutant survived on a fresh-transport assertion | **fixed**: the assertion now reads the header off the request the transport actually received. | `test_a_compressed_body_is_refused_outright`; mutant `asks_for_compression` |
| **B4/S03** `stage` returning the caller's ref for a known handle | **fixed**: `stage` returns the **indexed** ref, and a request carrying the same handle and digest with forged `bytes`, `mime`, `duration_s` and `storage_ref` is staged and attached as the object's own facts. | `test_a_known_handle_is_staged_as_the_object_the_store_has`; mutant `stage_returns_the_callers_ref` |
| **B4/R13** hop-start deadline check survived | **fixed**, and the reason it survived is now gone: with B3's `wait_for` a non-positive budget refused by accident, so `_pin` floors its timeout at 1 ms and the hop-start check is the single deadline authority. The slow redirect chain (ticking clock + 302 chain) asserts the reason, that only two requests were sent, and that no third hop was resolved. | `test_a_slow_redirect_chain_stops_at_the_aggregate_deadline`; mutant `hop_deadline_not_checked` |
| **B4/S01** `materialize` indexing before the object write | **fixed**: a put fault on a `media/` key leaves no indexed ref and no object. | `test_no_ref_is_indexed_without_an_object_behind_it`; mutant `materialize_indexes_before_the_write` |
| **Cross-track hazard**: `profile_version` path traversal, caller `storage_ref`, `attach` of an unstaged ref, `stage` of a never-materialized ref | **partly fixed, one part is a contract revision request.** Fixed: `profile_version` is validated against `^[a-z0-9][a-z0-9._-]{0,63}$` **inside `_key`**, so every key M builds (source now, prepared later) goes through it; `org_id` is validated there and before any fetch; `storage_ref` is never read from a caller-supplied ref (rebuilt, or the indexed ref's); `attach` accepts only refs this store staged for the job's org and stores the indexed ones. **Not fixed as ruled:** `stage` still accepts a url/inline ref that was never materialized, because the shared conformance suite requires it — see the request below. | `test_a_profile_version_cannot_escape_the_tenants_prefix`, `test_a_malformed_tenant_is_refused_before_anything_is_fetched`, `test_only_a_staged_ref_can_be_attached_to_a_job`, `test_a_known_handle_is_staged_as_the_object_the_store_has`; mutants `profile_version_unvalidated`, `org_unvalidated`, `attach_accepts_an_unstaged_ref`, `stage_returns_the_callers_ref` |
| Nonblocking: cookies disabled | **fixed** — `client.cookies.clear()` before every hop's request; `Set-Cookie` on hop 1 never reaches hop 2. | `test_a_redirect_carries_no_cookie_from_the_hop_before`; mutant `cookies_carried_between_hops` |
| Nonblocking: https→http downgrade | **fixed** — new reason `insecure-redirect`; a fetch that started as http is unchanged. | `test_a_redirect_may_not_downgrade_to_plaintext`; mutant `a_redirect_may_downgrade` |
| Nonblocking: `raw_host` for resolver, SNI and Host | **fixed** — `raw_host_of()` is used for all three; `https://faß.de/v.mp4` resolves, addresses and verifies `xn--fa-hia.de`. | `test_the_resolver_and_the_wire_agree_on_one_spelling_of_the_host`; mutant `decoded_host_used_instead_of_the_wire_form` |
| Nonblocking: `data:`/`javascript:` Location | **already covered and now asserted** — `parse_source` refuses them as `unsupported-scheme` on any hop. | `test_a_redirect_to_another_scheme_or_to_credentials_is_refused` |
| Nonblocking: honour `ALLOWED_VIDEO_MIME` | **fixed** — `MediaFetcher(..., allowed_mime=…)` and `decode_data_url(..., allowed=…)`; the module default is still the legacy constant, and G passes the setting in (integration request 3). No environment read added. | `infrx/media/fetch.py` |
| Nonblocking: validate `org_id` before fetching | **fixed** — `valid_org` raises `invalid_request` before any outbound request; six malformed forms cost no resolution. | `test_a_malformed_tenant_is_refused_before_anything_is_fetched`; mutant `org_unvalidated` |
| Nonblocking: recompute the digest in `materialize` | **fixed** — measured from the bytes being stored; a fetcher returning a lying digest cannot name the key or the handle. | `test_the_digest_is_measured_not_taken_from_the_fetcher`; mutant `digest_taken_from_the_fetcher` |
| Nonblocking: different content ⇒ different handle, and a handle clash | **fixed** — both asserted; the clash guard is exercised by seeding the index, since a 160-bit collision is not constructible. | `test_different_content_gets_a_different_handle_and_a_different_key`, `test_a_handle_that_already_names_other_content_is_a_conflict`; mutant `handle_clash_ignored` |
| Nonblocking: `put_if_absent` on the `ObjectStore` protocol | **fixed** — the protocol is `head` + `put_if_absent` (S3 `HeadObject` + `PutObject` with `If-None-Match: *`), so write-once is one atomic operation rather than a read and a write a concurrent staging can interleave with. `InMemoryObjectStore.seed` is a test seam and not part of the port. | `infrx/media/store.py` |
| Nonblocking: rename `test_every_guard_in_the_owned_modules_has_a_mutant` | **fixed** — now `test_the_mutation_list_covers_the_owned_modules`, asserting the mutated file set and a floor of 65. | `tests/m/test_mutants.py` |
| Nonblocking: evidence nits | this appendix states the measured SHA explicitly and corrects the round-1 mutant sentence. The file keeps round 1's name so the audit trail is not rewritten. | here |

One fixture under attack was edited, deliberately and minimally:
`test_every_redirect_hop_is_validated_and_repinned` now redirects to
`https://internal.test/v.mp4` instead of `http://…`, because the new downgrade guard would
otherwise refuse the hop before the address check the case exists to prove. The invariant
(hop 2 is re-parsed, re-resolved, re-validated, one request sent) is unchanged and the
assertions are the same.

## Round-2 results (quoted from command output)

| Command | Exit | Result |
|---|---|---|
| `make api-test` | 0 | `781 passed, 2 warnings in 50.08s` |
| `uv run --frozen pytest -q tests/m` | 0 | `111 passed in 13.26s` |
| `uv run --frozen pytest -q tests/m/test_fetch.py` | 0 | `70 passed in 1.92s` |
| `uv run --frozen pytest -q tests/m/test_store.py` | 0 | `31 passed in 0.50s` |
| `INFRX_MUTANTS=all uv run --frozen pytest -q tests/m/test_mutants.py` | 0 | `72 passed in 120.38s (0:02:00)` |
| `INFRX_MUTANTS=all … --collect-only … \| grep -c test_mutant_is_killed` | 0 | `69` |
| `uv run --frozen pytest -q tests/test_media.py` (legacy media, untouched file) | 0 | `7 passed, 2 warnings in 1.08s` |
| `git diff --stat 8744418 HEAD -- apps/infrx-api/infrx/media/video.py` | 0 | `1 file changed, 30 insertions(+), 1 deletion(-)` — the address policy only |

69 mutants, **all killed** (72 = 69 + the 3 meta tests, one of which exercises the six ways
a run can prove nothing). Round 1 had 51; the 21 added cover every guard this round
introduced, and one round-1 mutant (`reserved_addresses_allowed`) was replaced by the three
decisive ones above.

Conformance against the real adapter is unchanged at **2 of 9 run, 7 skipped** with the
same report (quoted in round 1, re-run above): the fixes did not cost a case, and the two
that run — `media_sec__a_foreign_media_reference_is_not_staged` and
`media_sec__a_partial_request_stages_nothing` — still pass.

Legacy tests: `tests/test_media.py` passes and the legacy test files are byte-identical to
the base (`git diff --name-only 8744418 HEAD` lists no file under `tests/` except
`tests/m/`). The 7 pre-existing `tests/test_gateway_auth.py` failures on a partial
collection are unchanged and documented in round 1.

## Contract revision request (the one item not fixed as ruled)

The ruling "`stage` and `attach` accept ONLY refs present in the staging index for that
org" is implemented for `attach`. For `stage` it contradicts the shared conformance suite,
so the adapter would have to fail or hide a case rather than pass it:

* `infrx/contracts/conformance/builders.py:68-71` — `b.media()` builds a `MediaRef` with a
  caller-chosen handle, `digest=digest(handle)` and `storage_ref=f"media/{org_id}/v1/source"`,
  i.e. a ref no store ever materialized.
* `infrx/contracts/conformance/services.py:353-355` — `media_sec__a_partial_request_stages_nothing`
  ends with `staged = await harness.port.stage(b.ORG_A, b.request(harness, refs=(good,)))`
  and then `assert await harness.port.resolve_owned(b.ORG_A, good.handle) == staged[0]`, so
  the suite **requires** that a never-materialized ref stages and becomes resolvable.
  `media_sec__a_foreign_media_reference_is_not_staged` (services.py:158-174) likewise expects
  `RequestTooLarge` for an unmaterialized oversize ref, which requires the size check to run
  before any index-membership check.

Requested revision: `MediaStore.stage` accepts only refs the store itself produced, with a
harness hook (`materialized(org_id, ref)`, alongside `put_object`/`admitted`) for the suite
to pre-register fixtures, and `FakeMediaStore` following the same rule. Until then M1
implements the sanitising half, and the residual risk is stated below rather than claimed
away.

## Limits added or changed this round

1. **`stage` still accepts a never-materialized url/inline ref** (above). With the fixes,
   what such a ref can do is bounded: the key is rebuilt from a validated tenant, a
   validated profile version and the ref's digest, so it cannot traverse out of the
   tenant's prefix, name another tenant's object or overwrite anything; a handle the store
   already has always resolves to the object's own facts; ~~and `attach` refuses it, so no
   job can execute on it. What remains possible is a tenant staging a reference to its own
   nonexistent object, which fails its own preparation.~~ **[False; withdrawn and replaced
   by the round-3 correction to this item (B-R2-2): `stage` indexes the ref, so `stage`
   then `attach` succeeds. `attach` refuses only a ref that never went through `stage`, and
   the residual risk is own-tenant caller-shaped facts.]** Owner: coordinator (contract), then M.
2. **`BACKSTOP_GRACE_S` = 1 s** means the worst-case wall-clock for one fetch is
   `MEDIA_FETCH_TIMEOUT_S + 1` when a dependency ignores every other bound. Provisional.
3. **A timed-out `getaddrinfo` may linger**: `resolve_all` runs `getaddrinfo` in the default
   executor, so `wait_for` abandons the *wait*, not the OS resolver thread, which returns
   when the system resolver does. Under a stalled resolver the pool can hold threads for
   the system timeout. Owner: M at G1 (a resolver with its own timeout, or a thread-free
   resolver). Not reproduced in a test.
4. **The `httpx`/`httpcore` level is process-wide.** The module sets it at import, which is
   deliberate (B2) but global: a process that re-raises those levels after importing
   `infrx.media.fetch` would log request lines again. Integration request 5 states the
   process-side rule.
5. **TLS verification against a pinned address remains unexercised end to end** (round-1
   limit 1, unchanged): the pin, SNI and Host are asserted at request level only.
6. `test_a_resolver_that_never_answers_is_bounded`, `test_a_body_that_stalls_for_ever…` and
   the redirect-downgrade case use **real time** (50 ms budgets, one 2 s test-side bound).
   They are deterministic in outcome — the future they wait on is never resolved — but they
   are not clock-free, which is why they are named here.

## Integration requests (round 2; 1–4 from round 1 stand)

5. **Process logging configuration (G1/I2):** the gateway's logging setup must not lower the
   `httpx`/`httpcore` levels below `WARNING`. `infrx.media.fetch` sets them at import, so the
   rule is only "do not undo it, and do not configure a root handler that bypasses logger
   levels". Suggested startup assertion for G1:
   `assert logging.getLogger("httpx").getEffectiveLevel() >= logging.WARNING`.
6. **Python ≥ 3.12.4 for the I track:** this host's 3.12.3 build answers `is_private`/
   `is_global` for 6to4 and NAT64 differently from upstream 3.12.0–3.12.3. The media policy
   no longer depends on that, but any other code that reasons about address ranges does, so
   the pinned interpreter for deployment should be ≥ 3.12.4 and the image pinned by digest.
7. **`MediaFetcher(allowed_mime=…)`:** G should pass `settings.allowed_video_mime` (the
   legacy F1 setting) so one environment variable governs both paths until the legacy path
   retires; the module default is the same constant.
8. **`ObjectStore` is now `head` + `put_if_absent`** (S3 `HeadObject` + `PutObject` with
   `If-None-Match: *`). Whoever writes the S3 adapter implements exactly those two, and the
   `traces` extra's `boto3` pin is enough (no new dependency).

## Verification log (appended)

- 2026-09-21: Round 2 after the independent review of `9e2c0a5` reported fix_required. B1,
  B2, B3, B4 (four survivors) and the cross-track hazard addressed as above; one part of the
  hazard ruling raised as a contract revision request with the exact builder and case lines.
  Results measured on `72010a2`; every count quoted from command output. No live, paid,
  cloud or container operation was performed.

---

# Round 3 — review of `7ce0dd8`: fix_required on tests and evidence

Appended. The code fixes of round 2 held under the reviewer's 348-form address corpus and
timing/rebinding attacks; what failed was killable-proof (five surviving mutants on claimed
guards) and one false sentence. Round-3 results were measured on ``f7ad418``.

## Blocking items

| Item | Disposition | Commit | Killing test / mutant |
|---|---|---|---|
| **B-R2-1.1** `is_reserved` survives; the "changes no answer" equivalence claim is wrong | **fixed, and the claim was wrong as stated.** `::127.0.0.1`, `::a9fe:a9fe` and `::10.0.0.1` are `is_global` and not `is_private` on this build (only `::/8` being *reserved* refuses them), and reserved space outside `::/96` (`4000::1`) is refused by `is_reserved` alone. All four are in the corpus, the `reserved_addresses_allowed` mutant is restored and dies, and `::/96` is stated in `DENIED_NETWORKS` so the policy does not depend on the interpreter's tables. Both wrong sentences (the round-2 disposition table and the `mutants.py` header) are corrected in place with the r2 correction noted. | `c9fcc24` | `test_the_address_policy_does_not_depend_on_the_interpreters_tables`, `test_no_internal_address_form_is_reachable`; mutant `reserved_addresses_allowed` |
| **B-R2-1.2** `is_multicast` survives | **fixed**: 224.0.0.1, 239.255.255.250, ff02::1 and ff05::1:3 are in the corpus and the mutant dies. | `d879c2f` | same two cases; mutant `multicast_allowed` |
| **B-R2-1.3** `attach` lookup not tenant-scoped | **fixed**: the reviewer's repro is a committed case — materialize for ORG_B, relabel the ref `org_id=ORG_A`, attach to an ORG_A job — and it is `not_found` with `by_job` empty. | `289e19b` | `test_the_attach_lookup_is_inside_the_jobs_tenant`; mutant `attach_lookup_is_not_tenant_scoped` |
| **B-R2-1.4** `PROFILE_VERSION_RE` leading dot | **fixed**: `..`, `.`, `.v1`, `._`, `..v1` are in the hostile forms, so widening the first character class to allow a dot dies. | `f314087` | `test_a_profile_version_cannot_escape_the_tenants_prefix`; mutant `profile_version_may_start_with_a_dot` |
| **B-R2-1.5** `valid_org` fullmatch → match | **fixed**: `<uuid>\n`, `<uuid>/../<other org>` and `<uuid>x` are in the malformed-tenant forms. | `f314087` | `test_a_malformed_tenant_is_refused_before_anything_is_fetched`; mutant `org_matched_as_a_prefix` |
| **B-R2-2** Limits §1 false | **corrected below**, in full. | ``5d359f9` (in place) + this section` | — |

## Correction to round 2's Limits §1 (B-R2-2)

Round 2 wrote, of a never-materialized ref: *"`attach` refuses it, so no job can execute on
it."* **That is false and is withdrawn.** `stage` indexes such a ref
(`self.refs.update(pending)`), so it is an indexed ref from then on and `stage` followed by
`attach` **succeeds**. What `attach` refuses is a ref that was never passed through
`stage` at all — which is a narrower and much less interesting guarantee.

What is actually true at this head:

* A ghost ref is **tenant-bound and traversal-free**: the key is rebuilt from a validated
  tenant, a validated profile version and a validated digest, so it cannot leave the
  tenant's prefix, name another tenant's object, or overwrite anything. `attach` binds only
  refs staged for the job's own org, looked up in that org.
* A ghost ref **can still carry caller-shaped facts** for its own tenant: `bytes`, `mime`
  and `duration_s` are the request's claims, and the `storage_ref` may have no object
  behind it. An **alias ghost** — a new handle carrying the digest of an object the same
  tenant really has — resolves to that real object's key while keeping the caller's claimed
  facts. Both stay inside one tenant; neither reaches another.
* Residual risk, therefore: a tenant can hand its own job media metadata the store did not
  measure. It is closed by the contract revision requested in round 2 (F2.2: `stage`
  accepts only store-produced refs, with a `materialized` harness hook so the shared suite
  can pre-register its fixtures).

**Rule for M2, which must hold whatever F2.2 decides:** never take a preparation budget,
a duration, a pixel count or a size from a `MediaRef`'s fields. `HEAD` the object, compare
the digest to the ref, and probe the bytes; a ref is a pointer plus a claim, not a
measurement. `01` already says probe metadata is not a resource limit — this is the same
rule one level earlier.

## Also in this pass (each with a case that fails without it and a mutant)

| Item | Disposition | Commit | Killing test / mutant |
|---|---|---|---|
| `_key` did not validate the digest | **fixed**: `valid_digest` (`^sha256:[0-9a-f]{64}$`) is applied where the key is built, so a server-side `model_copy(update={"digest": "sha256:../../../x"})` is `invalid_request` and stages nothing. | `6f2df0d` | `test_a_digest_cannot_carry_a_path_into_a_key`; mutant `digest_unvalidated_in_the_key` |
| Stale budget after DNS | **fixed**: `remaining` is recomputed and re-checked after `_pin`, so the request carries what is left *after* the lookup. The case uses a resolver that spends the whole budget and then answers with a public address: no request is sent. | `6f2df0d` | `test_the_budget_is_recomputed_after_the_name_is_resolved`; mutant `budget_not_recomputed_after_dns` |
| `secure` fixed at hop 0 | **fixed**: `secure = secure or scheme == "https"`, so `http → https → http` is refused at the third hop with the https hop's signed `Location` never leaving in plaintext. | `6f2df0d` | `test_a_chain_that_became_confidential_stays_confidential`; mutants `secure_decided_at_the_first_hop`, `a_redirect_may_downgrade` |
| `allowed_mime` plumbing untested | **fixed**: one case drives a fetcher configured with `{"video/webm"}` through both paths — a `video/mp4` response and a `video/mp4` `data:` URL are refused, the webm forms are accepted. | `6f2df0d` | `test_the_allow_list_the_fetcher_was_given_is_the_one_that_is_used`; mutants `fetch_ignores_the_configured_allow_list`, `data_urls_ignore_the_configured_allow_list` |
| Connect budget cap untested | **fixed**: with a 2 s aggregate limit the connect budget is 2 s, not the 3 s default. | `6f2df0d` | `test_each_request_carries_what_is_left_of_the_budget`; mutant `connect_budget_not_capped` |
| Stale comment at `fetch.py:39-42` | **fixed**: it said the policy is inherited from F1's tables; it now says M tightened it in place so it states the ranges itself. | `6f2df0d` | — |

## Integration request 5, restated precisely (supersedes the round-2 wording)

The module sets the `httpx` and `httpcore` logger levels at import, which is necessary but
not sufficient: a `dictConfig` naming `httpx` (or any `httpcore.*` child) **re-opens the
leak**, because an explicit level on the logger or a child overrides what this module set.
G1/I2 therefore:

1. call `infrx.media.fetch.silence_transport_logs()` (or import the module) **after** all
   logging configuration, not before — `dictConfig` with `disable_existing_loggers` or an
   explicit `httpx` entry runs later and wins;
2. assert at lifespan startup, after configuration, that no transport logger is below
   WARNING, children included:

   ```python
   for name, logger in logging.root.manager.loggerDict.items():
       if name == "httpx" or name == "httpcore" or name.startswith(("httpx.", "httpcore.")):
           assert not isinstance(logger, logging.PlaceHolder)
           assert logger.level in (logging.NOTSET, *range(logging.WARNING, logging.CRITICAL + 1)), name
           assert logging.getLogger(name).getEffectiveLevel() >= logging.WARNING, name
   ```

3. never configure a handler that bypasses logger levels (a root handler at DEBUG is fine;
   a handler attached directly to `httpx` with its own level is not).

The reason is the leak itself, which is worth restating: at INFO httpx writes
`HTTP Request: GET https://<validated ip>/<path>?<signed query> "HTTP/1.1 200 OK"` for
every hop, and httpcore writes the same target at DEBUG.

## Round-3 results (quoted from command output)

| Command | Exit | Result |
|---|---|---|
| `make api-test` | 0 | ``794 passed, 2 warnings in 57.15s`` |
| `uv run --frozen pytest -q tests/m` | 0 | ``124 passed in 28.89s`` |
| `uv run --frozen pytest -q tests/m/test_fetch.py` | 0 | ``80 passed in 1.74s`` |
| `uv run --frozen pytest -q tests/m/test_store.py` | 0 | ``34 passed in 0.31s`` |
| `INFRX_`83 passed in 82.89s (0:01:22)`=all uv run --frozen pytest -q tests/m/test_mutants.py` | 0 | ``83 passed in 82.89s (0:01:22)`` |
| `INFRX_`83 passed in 82.89s (0:01:22)`=all … --collect-only … \| grep -c test_mutant_is_killed` | 0 | ``80`` |
| `uv run --frozen pytest -q tests/m/test_store.py -k conformance -s` | 0 | ``ran 2 of 9` + 7 skips, `1 passed, 33 deselected in 0.20s`` |
| `uv run --frozen pytest -q tests/test_media.py` (legacy, F1 path) | 0 | ``7 passed, 2 warnings in 0.35s`` |
| `git diff --stat 8744418 HEAD -- apps/infrx-api/infrx/media/video.py` | 0 | ``1 file changed, 31 insertions(+), 1 deletion(-)`` |

80 mutants, all killed. The five the reviewer found surviving are among them by
name: `reserved_addresses_allowed`, `multicast_allowed`, `attach_lookup_is_not_tenant_scoped`,
`profile_version_may_start_with_a_dot`, `org_matched_as_a_prefix`. Conformance is unchanged
at 2 of 9 run, 7 skipped naming `create_upload` (M3) and `prepare` (M2). The legacy media
path is untouched and green; `video.py`'s only diff from the base is the address policy,
every change of it in the refusing direction.

## Limits (round 3)

1. **Round-2 Limits §1 is withdrawn and replaced** by the correction above: a
   never-materialized ref that has been through `stage` *is* attachable, and the residual
   risk is own-tenant caller-shaped facts until F2.2.
2. Round-2 limits 2–6 stand unchanged (`BACKSTOP_GRACE_S`, a lingering `getaddrinfo`
   thread, the process-wide transport log level, no end-to-end TLS test, the three
   real-time cases).
3. `::/96` in `DENIED_NETWORKS` has no mutant, and the list says why: on this build it is
   the same refusal `is_reserved` already gives, and it is stated so the policy still holds
   where `::/8` is not reserved. Every other check in `address_allowed` is load bearing and
   has a killed mutant.

## Verification log (appended)

- 2026-09-21: Round 3 after the review of `7ce0dd8`. Five surviving mutants fixed with a
  case and a list entry each (one commit per item), the false Limits §1 sentence withdrawn
  and replaced, four small code fixes (digest in the key, budget recomputed after DNS,
  sticky `secure`, and the two untested plumbings) each with a failing-without-it case and a
  mutant, and integration request 5 restated for `dictConfig`. Counts quoted from output; no
  live, paid, cloud or container operation was performed.
