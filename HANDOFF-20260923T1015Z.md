# Handoff — 2026-09-23T10:15Z (rey window; refreshed after the G2/G4U/W4 handbacks and the G3 dispatch)

Supersedes `HANDOFF-20260923T0655Z.md`. Since it: **F cancel-cause merged** (6803d2f + 0024da2), **I schema fix merged** (bc382ac: the F2P wire-in names ACCOUNTING_REGIME/ACTIVE_RATE_CARD_VERSION/PROVIDER_DEV_ALLOCATION_CEILING_CREDIT entered preflight's TUNABLE; 08 §5/§5.1 rows), rulings **R86–R90** written (next free **R91**), W4 phase A / G4U / G2 handed back and reviewed (all three `fix_required`, fix rounds running; G2's review still running), **G3 dispatched** (stacked on G2), **W-new fix lane dispatched**. 21/30 manifest packages merged. Reviews/refuters/implementers run on Opus 5.5; Fable only plans and orchestrates. Ultracode on: reviews go through `Workflow` with `model: 'opus'` on every `agent()`.

Same rules as before: WIP-commit and push lane work as you go (lanes never push; the coordinator pushes each lane branch); detach anything > 10 min; never touch another lane's containers; secrets never printed/logged/committed; AWS CLI via `env -u AWS_ACCESS_KEY_ID -u AWS_SECRET_ACCESS_KEY -u AWS_SESSION_TOKEN aws …`; never the pilot box outside logged coordinator ops. Standing user instruction: **"safely complete and merge tasks, run max parallel agents / tasks — wherever possible"**; "update tracker" stands.

## GOAL
Marlin backend-first program (`research/plan/18-marlin-backend-first.md`, manifest v4 `tasks.json`): 21/30 merged; in progress E1B (GPU slices), E3B2, D4, G2, G4U, W4, G3; not started D5, E4B. Session record (append-only): `research/plan/evidence/coordinator/2026-09-22-session-02.md`. Tracker: `python3 research/plan/scripts/progress.py` → `evidence/coordinator/PROGRESS.md` + `progress.html`; rey artifact https://claude.ai/artifact/3a3VjYKb2Vfh5X89BYMDPZ (publish with `url` + the repo's progress.html path; other accounts publish a fresh artifact and record its URL in progress-state `tracker_url`).

## BRANCH STATE (verified local == origin at 2026-09-23T10:15Z after the coordinator's pushes)
Integration `claude/backend-impl` = b1fcf9a (this worktree `infrx-impl`); `main` = f9ba5d2 (ff pending the checkpoint below). Program base ec6c548.

| worktree | branch @ head | agent | state |
|---|---|---|---|
| codex-d4 | `codex/d4-stream-journal` @ ce7659b | a3dc7d1193c9ade11 | make check done (71 tests/q reds = the shared-Valkey cross-kill, told to rerun tests/q on INFRX_Q_VALKEY_PORT=55485); running api-mutants; handback next |
| codex-g2 | `codex/g2-chat-relay` @ 2d742aa | a98f6b711111c4380 | HANDED BACK (impl e5e7d3a; fakes, not integrated); review wf_cc07f35b-21f running (4 lenses); cutover diff HELD (apply with D4, D5, M adapters, W-new, E3B2 bodies) |
| codex-g4u | `codex/g4u-upload-adapter` @ 6ad81ba+ | a6ce8b688ac75949c | review fix_required at f3c8055 (JSON evidence/g/G4U-review-f3c8055.json; R1 control-body deadline untested, H1 malformed mutant + fold-ins) → fix round running; confirmation workflow next |
| codex-e3b2 | `codex/e3b-phase2-gate` @ e57b820+ | aff9c42bf5340b59a | gate runs (namespace e3b2 56700–56799; tests/q on 56780) |
| codex-w4 | `codex/w4-measured-tuning` @ 086a127+ | a2c1ab13508b58f8b | phase A review fix_required at db12a5a (JSON evidence/w/W4-review-db12a5a.json; 6 blocking BS-1 BS-2 D1 D2 D3 HON-1; 21 nonblocking) → fix round running; phase B = coordinator box re-measurement (maintenance window; candidate.sh uses `/opt/dlami/nvme/w3-checkout`) |
| codex-g3 | `codex/g3-jobs` @ 2d742aa (base) | a532cdcf3b6126358 | implementing per `.claude/handoff/wave3/G3.md` (stacked on G2; fakes; own list tests/g/jobs/; tells it when G2's head moves) |
| codex-wnew | `codex/w-consumed-parameters` @ e87fa69+ | ac1efe1bc2d3847c0 | W-new: CONSUMED_PARAMETERS in VllmEngine.check_parameters (G2 request 2, blocking for the pilot); must answer where max_tokens reaches the engine; merge after a quick verify |
| codex-ischema | `codex/i-schema-wirein-names` @ 4c7de56 | done | MERGED bc382ac |
| codex-fcancel | `codex/f-cancel-cause` @ 0fe8fdd | done | MERGED 6803d2f + 0024da2 |
| codex-e1b | — | — | software slices merged 164e43e; GPU slices wait on the box rollout (I2B runbook, not done) |

## DETACHED / RUNS
- **Checkpoint at 90aadcf**: `.claude-logs/checkpoint2-90aadcf.log` (INFRX_D_TASK=d5 55436, INFRX_D2_VALKEY_PORT=55467 infrx-d5-valkey, INFRX_Q_VALKEY_PORT=55490): api-test **2993 passed / 0 failed**; api-mutants running (waiter bxtq8t8h7; ~1:40 expected). The 8892c27 gate gave console-test 289/289, typecheck OK, lint 0 errors, console-mutants 104/104, bench red (not reproduced: 67 passed on rerun) and one W3 timing mutant (killed on rerun) — see the session record. **On api-mutants green: `git push origin claude/backend-impl:main`** (the docs-only commits after 90aadcf ride along) and record a checkpoint in the session record + progress-state `checkpoints`.
- Containers: `infrx-d5-postgres`, `infrx-q3-valkey-55490` are the checkpoint's; `infrx-d1/d2/d4-postgres`, `infrx-q3-valkey-55485` are lanes'; `gideon-migration-order-test-*` is not ours. The shared `infrx-q3-valkey` (55462, was running without a published port) and a stale `infrx-d1-postgres` left by G4U's killed loop were removed today; the harness recreates them.
- Workflows running: G2 review wf_cc07f35b-21f (task w5hl3nuio).

## NEXT (in order)
1. Process results as they land: workflow → parse `result` (`raw.rfind('{',0,idx)` + `JSONDecoder().raw_decode`), save JSON under `research/plan/evidence/<track>/<TASK>-review-<head>.json`, record, **pass** → merge `--no-ff` with the task's integration requests (Makefile lists: `tests/g/uploads/test_uploads_mutants.py` (G4U), `tests/w/test_w4_mutants.py` (W4), G3's list; harness lines), run the merged tree's relevant suites on private ports, push; **fix_required** → SendMessage the lane the confirmed blocking + nonblocking items, then a confirmation workflow (2 lenses + refuters) on the fix head.
2. Merge order: **W-new (small, verify + merge) → G4U (after confirmation) → D4 (handback → review) → G2 (after review/fix) → E3B2 → W4 phase A → G3 → D5**; D5 dispatch at D4's CONFIRMED head (worktree codex-d5, branch codex/d5-terminal-transaction, ports 55436/55467 — the checkpoint run holds them now; brief `.claude/handoff/wave3/D5.md` incl. the cancel-cause addendum). G2's cutover diff only when D4, D5, M adapters, W-new and E3B2 bodies exist.
3. Keep pushing lane branches at each handback; tracker after each state change (progress.py, then publish); refresh this handoff at each checkpoint; at ~90 % of the window stop dispatching, write the handoff, commit, push, idle.

## OPEN ITEMS (not blocking merges)
IR-DTO (console); Q Valkey runner env passthrough + `INFRX_Q_VALKEY_CONTAINER` (G4U request e); `Admission.price_snapshot` optional revision; the stale §5.1 paragraph about G1/Q1 constants (doc debt); I3B `--max-age 180` alert timer; hosted backup rehearsal before any hosted apply (hosted has only 0001–0002); box rollout per I2B runbook (not done; `--max-num-seqs 32` unit vs serve.sh refusal → same deploy as W3's serve.sh); HF_TOKEN registry oids (user input); E1 corpus byte-reproducibility per CPU; task ports in the kernel ephemeral range; P-20 (interim `MAX_VIDEO_SECONDS` 72 today; W4 computed 82 as the ceiling at the 16384 budget, valid only with ≥ F−2 source frames; E1 `--max-num-batched-tokens 32768` keeps 120 s if adopted after the box run); the Q3 drill's unbounded `while not w.leases` wait (Q/E4B follow-up); the W3 `sigint_not_handled` mutant is timing-sensitive under load (W follow-up); G2's coordinator requests (daily `gc_outbox` timer, worker composition over `rt.lifetime.reconciler.index`, `MediaCollector` settings PROCESSING_CACHE_MAX_BYTES/MEDIA_GC_INTERVAL_S — the last two are new config names: preflight TUNABLE + 08 §5.1 rows at the cutover).

## TRAPS
- Five concurrent full suites cross-kill the shared `infrx-q3-valkey` (55462) through the Q3 sigkill drills: every full-suite run needs `INFRX_Q_VALKEY_PORT=<port nothing listens on>` and NO pre-started container (the harness names it `infrx-q3-valkey-<port>` and the drills kill THAT name). The D2 Valkey lock (`INFRX_D2_VALKEY_PORT`, default 55463) is also shared: set it per run with `INFRX_D2_VALKEY_CONTAINER`.
- A whole-tree gate whose pytest shows ~1 min CPU per hour and an idle Pg session is hung in that cross-kill: kill it and rerun on private ports (ptrace is denied for non-descendants, so you cannot inspect it).
- The gate wrapper's `| tail -N` hides failure names and makes `$?` the tail's: run api-test with `-rfE` into a log when you need names.
- `cd apps/infrx-api && setsid … &` backgrounds the whole subshell; always `(cd … && setsid …)`. `pkill -f <string>` kills your own shell when the string is in your command line.
- progress-state `inputs` entries are dicts; `reviews` entries need `{phase, since}`; never `json.dump` tasks.json (it re-encodes non-ASCII) — edit it textually or restore it from git.
- Workflow result files wrap JSON in `{"summary",…,"result"}`; per-agent results in the run's `journal.jsonl`.
- Port 55432 is shared (HarnessBusy = retry). Lane-numbered rulings collide: renumber at merge. `git stash` is shared: never.
