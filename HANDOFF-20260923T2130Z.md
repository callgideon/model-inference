# Handoff — 2026-09-23T21:30Z (sofia window; acceleration mode: one fix round per lane, 8 lanes in parallel, GPU box in use)

Supersedes `HANDOFF-20260923T1820Z.md`. Since it: the I3B follow-up merged (90b81c7 + IR2-2 08725c0; checks green); the user ordered **much sooner completion, fewer review rounds, max parallel agents** and **authorised GPU use** (live box found, no new instance). Policy now: **ONE fix round per lane** (all findings, blocking + nonblocking) → single verifier → merge; reviews start on committed heads before handbacks. `main` = 01a7dfc (checkpoint 1; remote); manifest 27/30. Rulings R1–R94 (next free **R95**; candidates proposed by M1-L2, E4B, M pilot-media — coordinator numbers at merge).

Rules unchanged (1125Z handoff) plus: box commands via SSM only (no SSH key here), W3's `wrap`/`ssm`/`out` helpers, every command id in the evidence; secrets never in text. Standing user instruction: "safely complete and merge tasks, run max parallel agents / tasks"; "update tracker". Session record `research/plan/evidence/coordinator/2026-09-22-session-02.md` (append-only, complete); tracker `python3 research/plan/scripts/progress.py`; sofia artifact https://claude.ai/artifact/Rk84qe3Yuzr2d357GoeRvZ.

## LANES (all branches pushed at their last handback; integration `claude/backend-impl` = 64c0fdc+)
| lane | worktree / branch @ head | state | next |
|---|---|---|---|
| D5 | codex-d5 `codex/d5-terminal-transaction` @ c67e4f5+ | review at c67e4f5 fix_required (8 blocking = test gaps + merge readiness; money confirmed correct; JSON evidence/d/D5-review-c67e4f5.json); lane finishing its runs, then its ONE fix round (incl. re-merge of the integration head, RAISES resolved to its lifted partition) | handback → single verifier (money on both images) → **MERGE FIRST**, together with the phase-3 branch (its tripwires on D5) |
| CUTOVER | codex-cutover `codex/cutover-mount` @ 6cb8ebe+ (milestone 43fe900: ROUTERS = health, models, ingress, uploads, jobs; adapters from settings; gateway.py retired) | items 3–5 (Caddy proxying, preflight, evidence) + E4B's B1 (fixture placeholder digest → measured pins, case tied to serving-version.json) | handback → one-round review → merge after D5 |
| M1-L2 | codex-objstore `codex/m1l2-object-store` @ e1bb54f+ (S3ObjectStore on botocore; 27 mutants; MinIO conformance) | review fix_required (A1 fixture creds flag; A2 error-vs-absent cases; JSON evidence/m/M1L2-review-e1bb54f.json) → fix round running | verifier → merge after cutover |
| M pilot-media | codex-mpilot `codex/m-pilot-media` @ 6cb8ebe+ | running: M3-U1 upload-ref resolution in the real staging; M3-U2 durable attach + rebuildable cache index for a separate worker process | handback → review → merge after M1-L2; then phase 3 drops the M3-U1/U2 pendings |
| E3B phase 3 | codex-e3b3 `codex/e3b-phase3-bodies` @ 3037a83+ (journeys ×6 run, 3 video_upload pend M3-U1; dataset resume, dr07c, dr07[postgres], rc04b, dr11, rc03, dr17 bodies pass on namespace e3b2; G2-R1 retired; D5's IR2 diff + Makefile applied as own commits) | full layer-3 gate re-running | handback → one-round review → merge WITH D5 |
| E4B | codex-e4b `codex/e4b-certify` @ 37a4652+ (certify runner, protocol, endpoint doc, release decision PENDING; 77 mutants) | review fix_required (9 runner-honesty gaps F1–F9; JSON evidence/e/E4B-review-37a4652.json) → fix round running | verifier → merge; box half after the rollout |
| BOX | codex-box `codex/box-measure` @ d35a2c8+ | **maintenance window OPEN since 19:47:06Z** on i-0e8449a4ffca29bab; E0 was running (SSM 2c299ba4); lane resumed to finish E0→E1(→E3)→decide→L0/L1/L8→close | handback → record W4 phase-B decision (P-20) + E1B rows; phase 2 (rollout + L2–L7 + E4B box half) after the code lanes merge. **If this session dies with the window open**: restore `/etc/caddy/Caddyfile` from `/opt/dlami/nvme/w4-logs/Caddyfile.live-20260923T194704Z` (sha256 ff47f706…) and `docker exec caddy caddy reload --config /etc/caddy/Caddyfile --adapter caddyfile --address unix//config/admin.sock`; verify the engine unit `marlin2b-vllm` is active with the step-1 args |
| E1B | — | direct-engine cells in the box window; L2–L7 after the rollout | |
| closed | I3B follow-up (90b81c7), E3B2 (d1d4a68), F fakes (a2779d1), G3 (b560b51), G2 (2391d4d), W4, D4, G4U, W-new, I-schema, F cancel-cause | | |

## RUNS
- Interim whole-tree gate on 602b1e0: api-test 3305 green, layer 0 153, console 289 + lint/typecheck + 104 mutants, bench 67 (foreground; the nohup SIGINT artefact is recorded); **api-mutants running since 19:10Z** (`.claude-logs/gate-interim-api-mutants.log`, waiter). Checkpoint 2 reuses it and reruns only what D5 touches.
- Workflows running: none besides lanes' own. Scripts under the session dir `workflows/scripts/`.
- Containers: infrx-d5-*/infrx-q3-valkey-55498 (D5), infrx-cutover-* (d3), infrx-mpilot-* (d4), infrx-e3b2-* (phase-3 gate), infrx-e2-* (E4B/objstore runs, transient), infrx-d2-postgres (stale default), gideon-* (not ours).

## MERGE ORDER
D5 (+ phase 3 together) → cutover → M1-L2 → M pilot-media → E4B → checkpoint 2 (api-test rerun for tests/d + contracts + the new lists; layer 0; bench in the foreground) → ff `main` → box phase 2: I2B rollout on the box (hosted Supabase **backup first**, migrate 0010–0018+, `install.sh INFRX_MODE=pilot` with `--set S3_MEDIA_BUCKET=llm-bootcamp-641134885443` (role already permits; prefix infrx/), pinned serve.sh per W4 phase B) → E1B L2–L7 → E4B box half (`certify.py --box`) → release decision.

## OPEN ITEMS
P-19 (g6e price row), P-20 (decided by W4 phase B tonight), P-21 (host reserved ports; TIME-WAIT collisions hit four lanes today), Dependabot alert; the dev host's two `next-server` processes (App dev servers since 09-20) are exempt from E4B's App/Lab precondition for local runs; E3B2 IR2-6 (fake lacks D2 MC-1) → "F fakes follow-up 2" after a provider_dev CREDIT ruling; D pgharness lock path (I3B R3-2); Q3 drill's unbounded wait; W3 `sigint_not_handled` load sensitivity.
